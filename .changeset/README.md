# Changesets

This directory tracks version bumps for @napplet packages.

Run `pnpm changeset` to create a new changeset.
Run `pnpm version-packages` to apply changesets and bump versions.
Run `pnpm publish-packages` to build and publish to npm.
