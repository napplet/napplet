---
phase: 137-public-napplet-nubs-amendments-nub-identity-nub-class-1-bundled
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/sandwich/Develop/nubs/.git/HEAD
  - .planning/phases/137-public-napplet-nubs-amendments-nub-identity-nub-class-1-bundled/137-01-SUMMARY.md
autonomous: true
requirements:
  - NUB-IDENTITY-07
  - CLASS1-03

must_haves:
  truths:
    - "A new branch `nub-identity-decrypt` exists on `~/Develop/nubs` based from the `nub-identity` draft branch."
    - "The `nub-identity-decrypt` branch has BOTH `NUB-IDENTITY.md` (from `nub-identity` branch base) and `NUB-CLASS-1.md` (merged in from `nub-class-1` branch) present in the working tree."
    - "The branch contains zero amendment content yet — it's a clean merge base with both source documents ready for amendment on wave 2."
    - "`~/Develop/nubs` remote is NOT pushed to; no PR opened — human gates both actions per `feedback_no_private_refs_commits`."
  artifacts:
    - path: "/home/sandwich/Develop/nubs/NUB-IDENTITY.md"
      provides: "Existing NUB-IDENTITY spec (~212 lines) present on new branch, ready for amendment"
      min_lines: 200
      contains: "NUB-IDENTITY"
    - path: "/home/sandwich/Develop/nubs/NUB-CLASS-1.md"
      provides: "Existing NUB-CLASS-1 spec (~60 lines) present on new branch, ready for amendment"
      min_lines: 50
      contains: "NUB-CLASS-1"
    - path: ".planning/phases/137-public-napplet-nubs-amendments-nub-identity-nub-class-1-bundled/137-01-SUMMARY.md"
      provides: "Summary of branch setup + merge outcome"
      min_lines: 20
  key_links:
    - from: "~/Develop/nubs/nub-identity-decrypt branch"
      to: "~/Develop/nubs/nub-identity branch"
      via: "branch base (checkout -b nub-identity-decrypt nub-identity)"
      pattern: "nub-identity"
    - from: "~/Develop/nubs/nub-identity-decrypt branch"
      to: "~/Develop/nubs/nub-class-1 branch"
      via: "merge commit bringing NUB-CLASS-1.md onto amendment branch"
      pattern: "Merge branch 'nub-class-1'"
---

<objective>
Set up a single `nub-identity-decrypt` branch on `~/Develop/nubs` that contains BOTH `NUB-IDENTITY.md` (from the `nub-identity` draft branch base) and `NUB-CLASS-1.md` (merged in from the `nub-class-1` draft branch) so the bundled Phase 137 amendment PR can touch both files from one branch per CLASS1-03.

Purpose: CLASS1-03 locks the bundle strategy — one PR touching both files. Master has NEITHER file (verified: `git ls-tree master` returns empty for both); the files live only on their respective draft branches. This plan merges the two draft branches into a single amendment branch so wave 2 plans (137-02, 137-03) can amend both files from one working tree.

Output:
- New local branch `nub-identity-decrypt` on `~/Develop/nubs` with both source files present
- Zero amendment content yet — amendments happen on waves 2+
- `137-01-SUMMARY.md` recording the branch state + merge commit SHA for audit
- ZERO `git push`, ZERO `gh pr create` — human gates both per in-repo convention
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/137-public-napplet-nubs-amendments-nub-identity-nub-class-1-bundled/137-CONTEXT.md
</context>

<interfaces>
<!-- Branch state of ~/Develop/nubs verified during planning (2026-04-23): -->
<!-- * master: has NEITHER NUB-IDENTITY.md NOR NUB-CLASS-1.md (git ls-tree master returns empty for both) -->
<!-- * nub-identity branch: has NUB-IDENTITY.md only (3 commits: abcccf3, d05c19e merge from master, 56795c9 initial) -->
<!-- * nub-class-1 branch: has NUB-CLASS-1.md only (2 commits: 31c89af, 9bf7437 initial) -->
<!-- * Currently checked out: nub-media -->

<!-- Repo convention (from ~/Develop/nubs/CLAUDE.md): -->
<!-- * Spec files live on PR branches only, NOT master -->
<!-- * Each NUB spec opens as its own PR — but CLASS1-03 explicitly permits bundling if review convenience prevails -->
<!-- * Commit message convention from existing branches: "docs: add NUB-CLASS-1 spec" / "fix: update registry link" — freeform, no strict prefix -->

<!-- Bundle-strategy rationale (from CONTEXT.md Decision 2): -->
<!-- * Both amendments share the same security surface (NIP-17 decrypt + CSP violation reporting) -->
<!-- * One PR = lower review overhead for maintainers -->
<!-- * Same milestone, same release -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create nub-identity-decrypt branch with both NUB files present</name>
  <files>/home/sandwich/Develop/nubs/NUB-IDENTITY.md, /home/sandwich/Develop/nubs/NUB-CLASS-1.md (git state only — both are existing unchanged files), .planning/phases/137-public-napplet-nubs-amendments-nub-identity-nub-class-1-bundled/137-01-SUMMARY.md</files>
  <read_first>
    - /home/sandwich/Develop/nubs/CLAUDE.md (nubs repo convention — PR-per-NUB pattern, no master commits for specs)
    - git branch -a output at ~/Develop/nubs (verify current state matches planning-time snapshot)
  </read_first>
  <action>
    Execute this exact sequence on `~/Develop/nubs` using absolute paths only (no `cd` per AGENTS.md). The sequence preserves both existing draft branches unchanged and creates a NEW branch containing both files.

    **Step 1 — Confirm starting state:**
    ```bash
    git -C /home/sandwich/Develop/nubs status --short                        # expect clean
    git -C /home/sandwich/Develop/nubs branch --show-current                 # record current branch for restore later
    git -C /home/sandwich/Develop/nubs ls-tree nub-identity -- NUB-IDENTITY.md NUB-CLASS-1.md  # expect only NUB-IDENTITY.md
    git -C /home/sandwich/Develop/nubs ls-tree nub-class-1 -- NUB-IDENTITY.md NUB-CLASS-1.md  # expect only NUB-CLASS-1.md
    ```
    If working tree is dirty OR if the ls-tree results don't match (NUB-IDENTITY.md appears on nub-class-1 etc.) — STOP and report. Don't force through.

    **Step 2 — Fetch latest from origin (informational; no merging from remote expected):**
    ```bash
    git -C /home/sandwich/Develop/nubs fetch --all
    ```

    **Step 3 — Create new branch from `nub-identity`:**
    ```bash
    git -C /home/sandwich/Develop/nubs checkout -b nub-identity-decrypt nub-identity
    ```
    This puts us on `nub-identity-decrypt` with `NUB-IDENTITY.md` present. `NUB-CLASS-1.md` is NOT yet present.

    **Step 4 — Merge `nub-class-1` into `nub-identity-decrypt` to bring in `NUB-CLASS-1.md`:**
    ```bash
    git -C /home/sandwich/Develop/nubs merge --no-ff nub-class-1 -m "chore: bundle nub-identity + nub-class-1 branches for Phase 137 amendment"
    ```
    This is a non-fast-forward merge so the merge commit is explicit in history. Both source branches remain unchanged.

    Expected outcome: zero merge conflicts (the two branches touch disjoint file sets — `nub-identity` adds `NUB-IDENTITY.md` + any README.md changes; `nub-class-1` adds `NUB-CLASS-1.md` + any README.md changes). **If README.md or any other shared file has a conflict**, resolve by preferring the `nub-identity` side for non-NUB-CLASS-1 registry rows and the `nub-class-1` side for NUB-CLASS-1 registry rows — this preserves both rows in the registry table without re-authoring. Commit the merge resolution.

    **Step 5 — Verify both files now present on the amendment branch:**
    ```bash
    ls -la /home/sandwich/Develop/nubs/NUB-IDENTITY.md /home/sandwich/Develop/nubs/NUB-CLASS-1.md
    git -C /home/sandwich/Develop/nubs log --oneline -5
    git -C /home/sandwich/Develop/nubs branch --show-current   # expect: nub-identity-decrypt
    ```

    **Step 6 — Capture merge commit SHA for 137-01-SUMMARY.md:**
    ```bash
    MERGE_SHA=$(git -C /home/sandwich/Develop/nubs rev-parse HEAD)
    echo "MERGE_SHA=$MERGE_SHA"
    ```

    **Step 7 — Write 137-01-SUMMARY.md** at `.planning/phases/137-public-napplet-nubs-amendments-nub-identity-nub-class-1-bundled/137-01-SUMMARY.md` with YAML frontmatter (phase/plan/subsystem/tags/requires/provides/affects/tech-stack/key-files/key-decisions/patterns-established/requirements-completed/duration/completed) + prose body documenting:
    - The starting branch state (master has neither file; two draft branches host them)
    - The merge operation outcome (both files on `nub-identity-decrypt`, merge commit SHA)
    - Zero content changes yet (amendment work lives on waves 2-4)
    - No push / no PR — human gated

    **Step 8 — NO PUSH, NO PR.** Explicitly record in the SUMMARY: `git push origin nub-identity-decrypt` was NOT run; `gh pr create` was NOT run. Per CLASS1-03 + NUB-IDENTITY-07 + `feedback_no_private_refs_commits`, the human opens the PR as the gated shared-state action.

    **Rationale for merge-based approach over rebase:** rebase would rewrite `nub-class-1` commits onto `nub-identity` and produce a linear but divergent history that diverges from the existing draft branches. Merge keeps the existing draft branches as reachable parents of the amendment branch, so the eventual PR reads cleanly as "amendment to both specs" rather than "rewrite of one draft branch with the other's commits appended."
  </action>
  <verify>
    <automated>bash -c 'set -e; git -C /home/sandwich/Develop/nubs rev-parse --abbrev-ref HEAD | grep -qx nub-identity-decrypt && test -f /home/sandwich/Develop/nubs/NUB-IDENTITY.md && test -f /home/sandwich/Develop/nubs/NUB-CLASS-1.md && git -C /home/sandwich/Develop/nubs log --oneline -1 HEAD | grep -qi "bundle" && test -f /home/sandwich/Develop/napplet/.planning/phases/137-public-napplet-nubs-amendments-nub-identity-nub-class-1-bundled/137-01-SUMMARY.md && echo "TASK1_VERIFY_GREEN"'</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/sandwich/Develop/nubs rev-parse --abbrev-ref HEAD` returns `nub-identity-decrypt`
    - Both `/home/sandwich/Develop/nubs/NUB-IDENTITY.md` and `/home/sandwich/Develop/nubs/NUB-CLASS-1.md` exist as regular files
    - `git log --oneline -5` on the new branch shows the merge commit (containing substring "bundle") at HEAD, with `nub-identity` and `nub-class-1` history reachable
    - `git status` is clean (no uncommitted changes)
    - No `git push` was run (check: `git log origin/nub-identity-decrypt..HEAD` either fails because origin/nub-identity-decrypt doesn't exist, OR shows the merge commit as ahead — either confirms no push)
    - No `gh pr create` was run (check: `gh pr list --head nub-identity-decrypt` returns empty)
    - `137-01-SUMMARY.md` exists in phase dir with merge commit SHA recorded
  </acceptance_criteria>
  <done>Branch `nub-identity-decrypt` exists locally on `~/Develop/nubs` with BOTH NUB files present as unchanged source-of-truth copies ready for amendment. Two draft branches (`nub-identity`, `nub-class-1`) are unchanged. No remote push. No PR. Merge commit SHA captured in 137-01-SUMMARY.md.</done>
</task>

</tasks>

<verification>
- `git -C /home/sandwich/Develop/nubs ls-tree nub-identity-decrypt -- NUB-IDENTITY.md NUB-CLASS-1.md` returns both files
- `git -C /home/sandwich/Develop/nubs ls-tree nub-identity -- NUB-IDENTITY.md NUB-CLASS-1.md` returns only NUB-IDENTITY.md (unchanged)
- `git -C /home/sandwich/Develop/nubs ls-tree nub-class-1 -- NUB-IDENTITY.md NUB-CLASS-1.md` returns only NUB-CLASS-1.md (unchanged)
- `git -C /home/sandwich/Develop/nubs branch --list nub-identity-decrypt` confirms the branch exists locally
- `git -C /home/sandwich/Develop/nubs log origin/master..nub-identity-decrypt --oneline | wc -l` > 0 (branch has commits ahead of master)
- `gh pr list --head nub-identity-decrypt --repo napplet/nubs` is empty (no PR opened — human gated)
</verification>

<success_criteria>
- Branch `nub-identity-decrypt` exists on `~/Develop/nubs` with both `NUB-IDENTITY.md` and `NUB-CLASS-1.md` present
- Zero amendment content has been introduced yet — both files are byte-identical to their source-branch originals
- Two source draft branches remain unchanged
- No remote push; no PR opened
- 137-01-SUMMARY.md written with merge commit SHA
</success_criteria>

<output>
After completion, create `.planning/phases/137-public-napplet-nubs-amendments-nub-identity-nub-class-1-bundled/137-01-SUMMARY.md` per standard summary template. Record: merge commit SHA, starting branch state snapshot, both-files-present confirmation, zero-push / zero-PR assertion.
</output>
