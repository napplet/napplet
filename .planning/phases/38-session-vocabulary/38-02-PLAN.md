---
phase: 38-session-vocabulary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shim/src/napplet-keypair.ts
autonomous: true
requirements:
  - SESS-03

# Goal-backward verification
must_haves:
  truths:
    - "napplet-keypair.ts contains a TODO(SEED-001) comment referencing the deterministic keypair design"
    - "loadOrCreateKeypair function name is unchanged"
    - "pnpm --filter @napplet/shim type-check passes"
  artifacts:
    - path: "packages/shim/src/napplet-keypair.ts"
      provides: "SEED-001 TODO comment documenting placeholder keypair design debt"
      contains: "TODO(SEED-001)"
  key_links: []
---

<objective>
Add a `// TODO(SEED-001):` comment to `packages/shim/src/napplet-keypair.ts` documenting the
intentional architectural debt in `loadOrCreateKeypair`. The function always generates a random
keypair (never loads one), which defeats ACL persistence across sessions. The correct design is
captured in SEED-001.

Purpose: SESS-03 (rename to `createEphemeralKeypair`) is deferred because the rename would
permanently encode the wrong behavior. The TODO comment ensures future contributors and agents
understand this is tracked debt, not an oversight. The seed reference makes the fix discoverable.

Output: `napplet-keypair.ts` with SEED-001 TODO block comment added immediately above
`loadOrCreateKeypair`. Function name and signature are unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-session-vocabulary/38-CONTEXT.md

@packages/shim/src/napplet-keypair.ts
@.planning/seeds/SEED-001-deterministic-napplet-keypair.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SEED-001 TODO comment to napplet-keypair.ts</name>
  <files>packages/shim/src/napplet-keypair.ts</files>
  <read_first>packages/shim/src/napplet-keypair.ts, .planning/seeds/SEED-001-deterministic-napplet-keypair.md</read_first>
  <action>
Insert a `// TODO(SEED-001):` block comment immediately before the `export function loadOrCreateKeypair`
declaration. Place it AFTER the existing JSDoc `/** ... */` block and BEFORE the `export function` line.

The exact comment text to insert (3 lines, each a single-line comment):

```ts
// TODO(SEED-001): This placeholder always generates a random keypair.
// The correct design: shell derives a deterministic key from
// SHA256(salt + aggregateHash + dTag + nappletAuthorPubkey) and sends it
// to the napplet via an init message. See .planning/seeds/SEED-001-deterministic-napplet-keypair.md
```

Placement in file — the result should look like:

```ts
/**
 * Generate an ephemeral keypair for a napplet.
 *
 * Always creates a fresh keypair — no IndexedDB or any other persistence.
 * [... rest of existing JSDoc ...]
 *
 * @param _nappType - [...]
 * @returns A fresh ephemeral keypair
 */
// TODO(SEED-001): This placeholder always generates a random keypair.
// The correct design: shell derives a deterministic key from
// SHA256(salt + aggregateHash + dTag + nappletAuthorPubkey) and sends it
// to the napplet via an init message. See .planning/seeds/SEED-001-deterministic-napplet-keypair.md
export function loadOrCreateKeypair(_nappType: string): NappletKeypair {
```

DO NOT:
- Rename `loadOrCreateKeypair` to anything else
- Remove the `_nappType` parameter
- Change any other part of the file
- Add a JSDoc `@todo` tag inside the existing JSDoc block (use standalone `//` comments instead,
  per codebase convention `// TODO:` pattern documented in CONTEXT.md)
  </action>
  <verify>grep -n "TODO(SEED-001)" packages/shim/src/napplet-keypair.ts</verify>
  <acceptance_criteria>
    - "packages/shim/src/napplet-keypair.ts contains 'TODO(SEED-001)'"
    - "packages/shim/src/napplet-keypair.ts contains 'export function loadOrCreateKeypair'"
    - "packages/shim/src/napplet-keypair.ts does NOT contain 'createEphemeralKeypair'"
    - "packages/shim/src/napplet-keypair.ts contains 'SEED-001-deterministic-napplet-keypair.md'"
  </acceptance_criteria>
  <done>TODO(SEED-001) comment added, loadOrCreateKeypair function name unchanged, seed path referenced</done>
</task>

<task type="auto">
  <name>Task 2: Verify shim type-check passes</name>
  <files></files>
  <read_first></read_first>
  <action>
Run the shim type-check to confirm the comment addition did not introduce any issues:

`cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/shim type-check`

This is expected to pass immediately since the change is a comment-only addition.
If it fails, check that the comment was not accidentally inserted inside a string literal or
expression.
  </action>
  <verify>pnpm --filter @napplet/shim type-check exits with code 0</verify>
  <acceptance_criteria>
    - "pnpm --filter @napplet/shim type-check exits with code 0"
  </acceptance_criteria>
  <done>Shim type-check passes cleanly after TODO comment addition</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -n "TODO(SEED-001)" packages/shim/src/napplet-keypair.ts` shows the comment
- [ ] `grep -n "loadOrCreateKeypair" packages/shim/src/napplet-keypair.ts` still shows the unchanged function name
- [ ] `pnpm --filter @napplet/shim type-check` exits 0
- [ ] `grep "createEphemeralKeypair" packages/shim/src/napplet-keypair.ts` returns no hits (function NOT renamed)
</verification>

<success_criteria>
- TODO(SEED-001) comment added above loadOrCreateKeypair
- Function name loadOrCreateKeypair unchanged
- Seed reference path included in comment
- Type-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/38-session-vocabulary/38-02-SUMMARY.md`
</output>
