---
phase: 18-core-types-runtime-dispatch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/types.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - CORE-01
must_haves:
  truths:
    - "ServiceDescriptor interface is defined in packages/core/src/types.ts with name (string), version (string), and description (optional string) fields"
    - "packages/core/src/index.ts exports ServiceDescriptor type"
    - "pnpm build succeeds for @napplet/core with no type errors"
  artifacts:
    - path: "packages/core/src/types.ts"
      provides: "ServiceDescriptor shared protocol type"
      contains: "export interface ServiceDescriptor"
    - path: "packages/core/src/index.ts"
      provides: "ServiceDescriptor export"
      contains: "ServiceDescriptor"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/types.ts"
      via: "type re-export"
      pattern: "from './types.js'"
---

<objective>
Add ServiceDescriptor to @napplet/core as a shared protocol type.

Purpose: ServiceDescriptor describes a service's metadata (name, version, description). It is needed
by runtime (for dispatch/discovery), shell (for providing service implementations), and shim (for
parsing discovery responses). Placing it in @napplet/core — the zero-dependency shared types package —
makes it importable by all downstream packages without circular dependencies.

Output:
- packages/core/src/types.ts — ServiceDescriptor interface added
- packages/core/src/index.ts — ServiceDescriptor type exported
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-core-types-runtime-dispatch/18-CONTEXT.md
@.planning/phases/18-core-types-runtime-dispatch/18-RESEARCH.md

<!-- Source of truth files -->
@packages/core/src/types.ts
@packages/core/src/index.ts
@packages/shell/src/types.ts
</context>

<interfaces>
The ServiceDescriptor interface to add to packages/core/src/types.ts:

```typescript
/**
 * Metadata describing a registered shell service.
 * Services are optional capabilities a shell provides beyond the core protocol
 * (e.g., audio, notifications, clipboard). Napplets discover available services
 * via kind 29010 service discovery events.
 *
 * @example
 * ```ts
 * const descriptor: ServiceDescriptor = {
 *   name: 'audio',
 *   version: '1.0.0',
 *   description: 'Audio playback management and mute control',
 * };
 * ```
 */
export interface ServiceDescriptor {
  /** Unique service identifier (e.g., 'audio', 'notifications', 'clipboard'). */
  name: string;
  /** Semver version of the service implementation. */
  version: string;
  /** Human-readable description of the service. */
  description?: string;
}
```

The existing ServiceDescriptor in packages/shell/src/types.ts (lines 218-225) has the identical shape.
This is a copy, not a move — the shell type will be removed in Plan 03 (shell migration).

The export to add to packages/core/src/index.ts (in the Protocol Types section):

```typescript
export type { NostrEvent, NostrFilter, Capability, ServiceDescriptor } from './types.js';
```

This adds ServiceDescriptor to the existing type export line. Keep ALL_CAPABILITIES on its separate line.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add ServiceDescriptor interface to core/src/types.ts</name>
  <files>packages/core/src/types.ts</files>
  <read_first>
    - packages/core/src/types.ts (current file — see existing section structure and JSDoc patterns)
    - packages/shell/src/types.ts (lines 218-225 — existing ServiceDescriptor definition to replicate exactly)
  </read_first>
  <action>
    Add a new section at the end of packages/core/src/types.ts, after the Capability Types section
    (after the ALL_CAPABILITIES array on line 91).

    Add this exact code block:

    ```
    // ─── Service Types ──────────────────────────────────────────────────────────

    /**
     * Metadata describing a registered shell service.
     * Services are optional capabilities a shell provides beyond the core protocol
     * (e.g., audio, notifications, clipboard). Napplets discover available services
     * via kind 29010 service discovery events.
     *
     * @example
     * ```ts
     * const descriptor: ServiceDescriptor = {
     *   name: 'audio',
     *   version: '1.0.0',
     *   description: 'Audio playback management and mute control',
     * };
     * ```
     */
    export interface ServiceDescriptor {
      /** Unique service identifier (e.g., 'audio', 'notifications', 'clipboard'). */
      name: string;
      /** Semver version of the service implementation. */
      version: string;
      /** Human-readable description of the service. */
      description?: string;
    }
    ```

    The section divider uses the same format as existing sections (e.g., "// --- NIP-01 Types ---").
    The JSDoc follows the same @example pattern as NostrEvent and NostrFilter.
    The field names and types match the existing definition in shell/src/types.ts exactly.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -n "export interface ServiceDescriptor" packages/core/src/types.ts && grep -n "name: string" packages/core/src/types.ts && grep -n "version: string" packages/core/src/types.ts && grep -n "description?: string" packages/core/src/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/types.ts contains `export interface ServiceDescriptor`
    - ServiceDescriptor has `name: string` field
    - ServiceDescriptor has `version: string` field
    - ServiceDescriptor has `description?: string` field (optional)
    - JSDoc comment includes @example block with a ServiceDescriptor literal
    - Section divider comment "Service Types" exists above the interface
  </acceptance_criteria>
  <done>ServiceDescriptor interface added to @napplet/core types</done>
</task>

<task type="auto">
  <name>Task 2: Export ServiceDescriptor from core/src/index.ts</name>
  <files>packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/index.ts (current exports — see exact line to modify)
    - packages/core/src/types.ts (verify ServiceDescriptor export exists after Task 1)
  </read_first>
  <action>
    In packages/core/src/index.ts, modify line 24 to add ServiceDescriptor to the type exports:

    Current line 24:
    ```
    export type { NostrEvent, NostrFilter, Capability } from './types.js';
    ```

    Change to:
    ```
    export type { NostrEvent, NostrFilter, Capability, ServiceDescriptor } from './types.js';
    ```

    This adds ServiceDescriptor to the existing type re-export from types.js.
    Do NOT modify any other lines. Keep ALL_CAPABILITIES on its own line (line 25).
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep "ServiceDescriptor" packages/core/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/index.ts contains `ServiceDescriptor` in the type export line from './types.js'
    - The export line reads: `export type { NostrEvent, NostrFilter, Capability, ServiceDescriptor } from './types.js';`
    - No other lines are modified
  </acceptance_criteria>
  <done>ServiceDescriptor exported from @napplet/core barrel</done>
</task>

<task type="auto">
  <name>Task 3: Verify @napplet/core builds successfully</name>
  <files></files>
  <read_first>
    - packages/core/src/types.ts (confirm ServiceDescriptor is present)
    - packages/core/src/index.ts (confirm export is present)
  </read_first>
  <action>
    Build @napplet/core to verify the new type compiles:

    cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/core build

    If the build fails:
    - Read the error output
    - Fix any TypeScript syntax issues in the ServiceDescriptor definition
    - Re-run build

    Then verify the type is present in the build output:
    grep "ServiceDescriptor" packages/core/dist/types.d.ts
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/core build 2>&1 | tail -3 && grep "ServiceDescriptor" packages/core/dist/types.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter @napplet/core build exits with code 0
    - packages/core/dist/types.d.ts contains ServiceDescriptor
    - No TypeScript errors in build output
  </acceptance_criteria>
  <done>@napplet/core builds cleanly with ServiceDescriptor</done>
</task>

</tasks>

<verification>
The plan is complete when:
1. grep "export interface ServiceDescriptor" packages/core/src/types.ts returns a match
2. grep "ServiceDescriptor" packages/core/src/index.ts returns a match showing the type export
3. pnpm --filter @napplet/core build exits 0
4. grep "ServiceDescriptor" packages/core/dist/types.d.ts shows the type in build output
</verification>

<success_criteria>
- CORE-01: ServiceDescriptor type lives in @napplet/core (importable by shim, runtime, and shell)
- The interface has name (string), version (string), description (optional string)
- @napplet/core builds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-core-types-runtime-dispatch/18-01-SUMMARY.md`
</output>
