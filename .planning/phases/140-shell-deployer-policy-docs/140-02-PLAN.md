---
phase: 140-shell-deployer-policy-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/SHELL-CLASS-POLICY.md
autonomous: true
requirements:
  - POLICY-11
  - POLICY-12
  - POLICY-13
  - POLICY-14
  - POLICY-15
  - POLICY-16

must_haves:
  truths:
    - "Shell deployers can read `specs/SHELL-CLASS-POLICY.md` and implement every NUB-CLASS shell-side MUST without reading the normative NUB spec first."
    - "The document surfaces the cross-NUB invariant (`class === 2` iff `connect.granted === true`) as both prose + audit checkbox + a concrete scenario table."
    - "The wire-timing MUST (`class.assigned` after iframe ready, before napplet branches on class, at-most-one terminal envelope per lifecycle) is explicitly stated and auditable."
    - "The document self-labels non-normative and cites `NUB-CLASS` as its normative source."
    - "Zero-grep hygiene clean: `grep -E '@napplet/|kehto|hyprgate' specs/SHELL-CLASS-POLICY.md` returns 0."
  artifacts:
    - path: "specs/SHELL-CLASS-POLICY.md"
      provides: "Non-normative shell-deployer checklist for NUB-CLASS; 6 required H2 sections + audit checklist + references"
      min_lines: 120
      contains: "non-normative"
  key_links:
    - from: "specs/SHELL-CLASS-POLICY.md"
      to: "NUB-CLASS spec"
      via: "References section + Status banner"
      pattern: "NUB-CLASS"
    - from: "specs/SHELL-CLASS-POLICY.md"
      to: "NUB-CLASS-1.md + NUB-CLASS-2.md"
      via: "class-determination + revocation-UX sections cite the two concrete track members"
      pattern: "NUB-CLASS-[12]"
    - from: "specs/SHELL-CLASS-POLICY.md"
      to: "specs/SHELL-CONNECT-POLICY.md"
      via: "cross-NUB-invariant section names the sibling deployer-policy doc"
      pattern: "SHELL-CONNECT-POLICY"
---

<objective>
Author `specs/SHELL-CLASS-POLICY.md` — a non-normative shell-deployer checklist that mirrors the structural shape of `specs/SHELL-RESOURCE-POLICY.md` (and the sibling `specs/SHELL-CONNECT-POLICY.md` authored in plan 140-01) and surfaces every NUB-CLASS shell-side MUST — class-determination authority, wire timing, cross-NUB invariant, revocation UX — as both prose + audit checkbox.

Purpose: Shell deployers implementing NUB-CLASS need an operator-visible checklist that translates the normative NUB spec's MUSTs into concrete, testable, sign-offable deployment decisions — and that surfaces the cross-NUB invariants (for shells implementing both NUB-CLASS and class-contributing NUBs like NUB-CONNECT) as explicit shell responsibilities rather than leaving them buried in multiple spec prose sections.

Output: One new file at `specs/SHELL-CLASS-POLICY.md`, ~120-180 lines, structured parallel to the sibling policy docs with 6 required top-level H2 sections, an audit checklist enumerating every MUST as a sign-off checkbox, and a References section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/140-shell-deployer-policy-docs/140-CONTEXT.md

<!-- Structural template — mirror this shape -->
@specs/SHELL-RESOURCE-POLICY.md

<!-- Normative source this doc is the companion to -->
@.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md

<!-- Concrete track members that the policy references -->
@.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md
@.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md

<!-- Class-contributing NUB — referenced in the cross-NUB-invariant section -->
@.planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md

<interfaces>
<!-- Citation targets the doc MUST reference -->

Normative spec cited as "NUB-CLASS":
  - Wire message: `class.assigned` (shell → napplet, one terminal envelope per lifecycle)
  - Wire payload: `{ type: 'class.assigned', id: string, class: number }` — class is non-negative integer
  - Runtime surface: `window.napplet.class: number | undefined`
  - Capability: `shell.supports('nub:class')`
  - Sub-track naming: `NUB-CLASS-$N.md` where `$N` is the integer; cite by file name NOT by abstract "Class N"

Concrete track members:
  - NUB-CLASS-1.md — default strict; `connect-src 'none'`; no consent; residual meta-CSP harmless
  - NUB-CLASS-2.md — user-approved explicit-origin; `connect-src <granted-origins>`; prompt at first load; residual meta-CSP MUST refuse-to-serve; revocation UX required

Cross-NUB invariant (shell responsibility per NUB-CLASS Security Considerations):
  - In shells implementing BOTH nub:class AND nub:connect:
    - class === 2 iff connect.granted === true (at the time class.assigned is sent)
    - class === 1 iff connect.granted === false OR no connect tags in manifest OR shell does not implement nub:connect
  - Shells MUST NOT emit disagreeing state

Sibling deployer-policy doc:
  - specs/SHELL-CONNECT-POLICY.md — cross-reference from the cross-NUB-invariant section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author specs/SHELL-CLASS-POLICY.md with all 6 required H2 sections + audit checklist + references</name>
  <files>specs/SHELL-CLASS-POLICY.md</files>

  <read_first>
    BEFORE WRITING ONE WORD, read these in full:
    1. `specs/SHELL-RESOURCE-POLICY.md` — structural template. Mirror the shape: blockquote banner at top → `## Status` → `## Why this exists` → per-policy `## Section Name (MUST|SHOULD|MAY)` H2s → `## Audit Checklist (one-page summary)` → `## References`. ATX headings throughout.
    2. `.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` — normative source. Every MUST in this policy doc MUST trace to NUB-CLASS prose (primarily its Wire Protocol, Security Considerations, and Capability Advertisement sections).
    3. `.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md` and `NUB-CLASS-2.md` — concrete track members. The revocation-UX section needs the reload-vs-refuse behavior from NUB-CLASS-2.md's Revocation Timing; the cross-NUB-invariant section needs both postures to describe the scenario table.
    4. `.planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md` — class-contributing NUB; the cross-NUB-invariant derives from the combination of NUB-CONNECT's `granted` state and NUB-CLASS's `class.assigned` wire.
    5. `.planning/phases/140-shell-deployer-policy-docs/140-CONTEXT.md` — locked section titles and content categories.
    6. (If plan 140-01 has landed) `specs/SHELL-CONNECT-POLICY.md` for cross-referencing style; do NOT copy content, just sibling-cite in References. (If it hasn't landed yet, that's fine — the cross-reference still resolves to the agreed file path.)
  </read_first>

  <action>
    Create `specs/SHELL-CLASS-POLICY.md` (new file) with the exact structure below. All H2 titles are locked; prose is Claude's discretion but MUST surface every MUST item listed.

    ## Required structure (8 H2 sections total: Status + Why + 5 policy sections + Audit + References)

    ### Top-of-file (before first H2)
    - H1 title: `# Shell Class Policy Checklist`
    - Blockquote banner (2 lines): "Shell-implementer guide for v0.29.0+ napplet hosts implementing NUB-CLASS." / "Normative wire shape and MUST/SHOULD language live in NUB-CLASS (napplet/nubs); this document is a deployment checklist."

    ### `## Status`
    - Self-label: "This is a non-normative implementer's guide."
    - Normative source: "The normative spec is **NUB-CLASS** in the `napplet/nubs` repo, with concrete postures defined in `NUB-CLASS-1.md` and `NUB-CLASS-2.md`."
    - One-sentence bridge: sibling deployer-policy docs are `SHELL-CONNECT-POLICY.md` (the class-contributing NUB whose grants drive the Class-2 posture) and `SHELL-RESOURCE-POLICY.md` (the read-only sibling).

    ### `## Why this exists` (POLICY-11 — motivation)
    - Frame: "NUB-CLASS is the shell-to-napplet signal that tells the napplet which security posture the shell is enforcing. The posture itself — CSP shape, sandbox flags, consent state — is applied at frame creation time, before any napplet code runs; the wire envelope is the napplet-side reflection of that enforcement decision, not a substitute for it."
    - State explicitly: "A shell that implements NUB-CLASS incorrectly can cause two distinct silent-failure modes: (a) sending `class.assigned` with a value that disagrees with the shell's actual CSP header — the napplet branches on a lie; (b) in shells implementing both NUB-CLASS and NUB-CONNECT, emitting a state where `class === 2` AND `connect.granted === false` (or vice-versa) — the cross-NUB invariant that napplets rely on breaks."
    - One sentence pointing deployers at the audit checklist.

    ### `## Class-Determination Authority (MUST)` (POLICY-12)
    - State: "The shell is the sole authority on what class a napplet is assigned. Class is determined by:"
      - bullet: "Which class-contributing NUBs are declared in the napplet's NIP-5A manifest."
      - bullet: "Any user-consent outcomes from NUBs that prompt (e.g., NUB-CONNECT's first-load prompt)."
      - bullet: "Any shell-deployment-policy inputs (operator refuses cleartext, operator has denied the napplet globally, etc.)."
    - State: "Napplets MUST NOT attempt to infer their own class from the environment — from the CSP they observe, from the capabilities the shell advertises, from the presence or absence of other NUBs. The `class.assigned` envelope is the only authoritative signal. Shells MUST therefore send the envelope — silence is not the same signal as explicit assignment."
    - State: "Dynamic mid-session re-classification is out of scope for v1. A shell that needs to change posture for an already-running napplet MUST tear down the current iframe and create a new one. The new lifecycle begins with its own `class.assigned` envelope."
    - State: "At most one `class.assigned` envelope per napplet lifecycle. A second envelope is a protocol violation; shells MUST NOT emit one to 'upgrade' or 'downgrade' a class mid-session."

    ### `## Wire Timing (MUST)` (POLICY-13)
    - State: "The `class.assigned` envelope MUST be sent AFTER the iframe signals readiness (shim bootstrap complete) and BEFORE the napplet's own code can meaningfully branch on class."
    - Explain the 'before' side: "Coupling to the shim ready signal is the recommended approach. The shim emits a post-install signal once `window.napplet` is fully populated; the shell listens for that signal and dispatches `class.assigned` in response. This guarantees the envelope arrives before the napplet's first `DOMContentLoaded` handler runs, which in turn guarantees any code path that reads `window.napplet.class` sees a populated value — not the `undefined` race."
    - Explain the 'after' side: "Sending `class.assigned` before iframe ready risks the envelope being dispatched into a dead listener — the shim's handler is registered at install, and any `class.assigned` received before install is lost. Shells MUST NOT pre-send the envelope at iframe creation and hope the shim catches it."
    - Recommend a concrete integration pattern: "A minimal, conformant integration: the shell's iframe-message handler listens for the shim's post-install emission (a ready envelope or a well-known `window.napplet` presence check) and dispatches `class.assigned` synchronously on receipt. Shells implementing both this NUB and class-contributing NUBs MUST NOT send class-contributing NUB state envelopes (e.g., NUB-CONNECT's `<meta name=\"napplet-connect-granted\">` is injected at serve time, not post-facto) before `class.assigned` for lifecycle coherence."

    ### `## Cross-NUB Invariant (MUST — Shell Responsibility)` (POLICY-14)
    - Frame: "NUB-CLASS has no opinion on which integer is 'correct' for a given napplet. It specifies only how that integer is communicated. The correctness question is owned by the class-contributing NUBs — and in a shell implementing BOTH NUB-CLASS AND a class-contributing NUB, the shell MUST not emit a state where the two signals disagree."
    - State the NUB-CONNECT invariant explicitly: "In shells implementing BOTH `nub:class` AND `nub:connect`, the following MUST hold at the time `class.assigned` is sent:"
      - `class === 2 iff connect.granted === true`
      - `class === 1 iff connect.granted === false` (either denied, or no `connect` tags in manifest)
    - `### Scenario Table` — Markdown table enumerating every combination with the shell's required output:

      | Scenario | `nub:connect` advertised? | `connect` tags in manifest? | User decision | Shell MUST emit | `class.assigned` | `connect.granted` |
      |----------|---------------------------|------------------------------|---------------|-----------------|-------------------|-------------------|
      | User approves Class-2 prompt | yes | yes | approved | `connect-src <origins>` CSP header + `<meta name="napplet-connect-granted" content="<origins>">` | `class: 2` | `true` |
      | User denies Class-2 prompt | yes | yes | denied | `connect-src 'none'` CSP header + empty-content meta tag | `class: 1` | `false` |
      | No `connect` tags (vanilla Class-1) | yes | no | n/a | `connect-src 'none'` CSP header + empty-content meta tag OR absent meta | `class: 1` | `false` |
      | Shell does NOT implement `nub:connect` | no | (any) | (any) | shell-baseline CSP; no `napplet-connect-granted` meta | `class: 1` (or absent if `nub:class` also not implemented) | `false` (default) |
      | Shell implements `nub:class` but not `nub:connect` | no | yes | n/a | refuse-to-serve the napplet with a diagnostic ('this shell does not implement NUB-CONNECT; this napplet requires it') OR treat as Class-1 with a deployment warning | `class: 1` | `false` (default) |
      | Shell implements `nub:connect` but not `nub:class` | yes | yes | approved | `connect-src <origins>` CSP + granted meta | (no envelope) | `true` |
      | Future class-contributing NUBs | yes | (varies) | (varies) | posture-specific | `class: N` (per the contributing NUB's track doc) | (varies) |

    - State: "Shells MUST NOT emit a scenario outside this table. A state where `class === 2` AND `connect.granted === false`, or `class === 1` AND `connect.granted === true`, is non-conformant."
    - State: "The invariant is checked at `class.assigned` send time only. Subsequent revocation of a connect grant does not retroactively invalidate the envelope that was correct at send time; see Revocation UX below for the forward-correction path."

    ### `## Revocation UX for Class-2 Napplets (MUST)` (POLICY-15)
    - State: "Revoking a connect grant MUST NOT result in a running Class-2 napplet continuing to see `class === 2` while `connect.granted === false`. NUB-CLASS's at-most-one-envelope rule forbids in-session class re-assignment, so revocation produces one of two conformant outcomes at the shell's choice:"
      - `### Option A: Reload on revocation (recommended)` — Shell tears down the current napplet iframe on revocation. A new iframe is created, which begins its own NUB-CLASS lifecycle with `class.assigned: { class: 1 }`. The napplet observes a page reload and re-bootstraps into the strict posture. This is the lowest-complexity path and the one recommended for most shells.
      - `### Option B: Refuse-to-serve until re-approval` — Shell tears down the current iframe on revocation and does NOT immediately create a new one; instead, it surfaces a re-approval affordance and waits for the user to either re-approve (producing a fresh consent prompt, since the `(dTag, aggregateHash)` is now in the DENIED state) or navigate away. Useful for shells that want to make the revocation explicit to the user at the UX layer.
    - State: "Mid-session dynamic class change is out of v0.29.0 scope. Shells MUST NOT send a second `class.assigned` to an existing iframe to signal revocation — see NUB-CLASS's Security Considerations, At-most-one envelope."
    - Cross-reference: "See `SHELL-CONNECT-POLICY.md`'s Revocation UX section for the shell-CONNECT side of the responsibility: exposing the user-facing revocation affordance, moving revoked grants to DENIED (not deleted), and taking effect at next `(dTag, aggregateHash)` load."

    ### `## Audit Checklist (one-page summary)` — enumerate EVERY MUST as a sign-off checkbox
    - Open with: "Use this as a deployment sign-off. Every item is a MUST unless explicitly marked otherwise."
    - Checkboxes covering:
      * [ ] `shell.supports('nub:class')` returns `true` if and only if this NUB is implemented (POLICY-11)
      * [ ] Shell is the sole authority on class — no napplet-side inference, no environment-sniffing path (POLICY-12)
      * [ ] Class is determined by manifest class-contributing NUBs + user-consent outcomes + deployment policy (POLICY-12)
      * [ ] Exactly one `class.assigned` envelope per napplet lifecycle; shell does NOT emit a second (POLICY-12)
      * [ ] Dynamic mid-session re-classification is NOT implemented (out of scope for v0.29.0) (POLICY-12)
      * [ ] `class.assigned` sent AFTER shim bootstrap complete / iframe ready signal (POLICY-13)
      * [ ] `class.assigned` sent BEFORE napplet code can meaningfully branch on class (POLICY-13)
      * [ ] Integration is coupled to the shim's ready signal (recommended) — shell does NOT pre-send at iframe creation (POLICY-13)
      * [ ] In shells implementing BOTH nub:class AND nub:connect: `class === 2` iff `connect.granted === true` at send time (POLICY-14)
      * [ ] No emitted state outside the scenario table (POLICY-14)
      * [ ] Revocation of a connect grant triggers EITHER iframe reload with `class: 1` (Option A) OR refuse-to-serve until re-approval (Option B) (POLICY-15)
      * [ ] Revocation does NOT attempt mid-session `class.assigned` re-emission (POLICY-15)
      * [ ] SHELL-CONNECT-POLICY audit checklist also completed if implementing both NUBs (POLICY-14, POLICY-15 cross-ref)

    ### `## References`
    - `NUB-CLASS` — parent class-track spec; defines the `class.assigned` envelope, the `window.napplet.class` runtime surface, and the authoring rules for track members. Repo: `napplet/nubs`.
    - `NUB-CLASS-1.md` — default strict posture. Repo: `napplet/nubs`.
    - `NUB-CLASS-2.md` — user-approved explicit-origin posture. Repo: `napplet/nubs`.
    - `NUB-CONNECT` — class-contributing NUB whose grant state drives the cross-NUB invariant for shells implementing both. Repo: `napplet/nubs`.
    - [NIP-5D](./NIP-5D.md) — parent transport spec.
    - [SHELL-CONNECT-POLICY.md](./SHELL-CONNECT-POLICY.md) — sibling deployer-policy doc for NUB-CONNECT; required reading for shells implementing both NUBs.
    - [SHELL-RESOURCE-POLICY.md](./SHELL-RESOURCE-POLICY.md) — sibling deployer-policy doc for the read-only NUB.

    ## Style rules
    - ATX headings throughout. Matches SHELL-RESOURCE-POLICY.md + SHELL-CONNECT-POLICY.md.
    - NO `@napplet/*` references anywhere in the file (zero-grep hygiene per POLICY-16).
    - NO `kehto` or `hyprgate` references.
    - Cite class documents by file name (`NUB-CLASS-2.md`) per NUB-CLASS citation convention, NOT abstract "Class 2" as primary reference.
    - Cite `NUB-CLASS` (the parent) as the bare name; it has no `$N` suffix.
    - When discussing the integer abstractly (not pointing at a specific member), use "the class number" or "the integer carried by `class.assigned`" rather than "Class N".
    - 2-space indentation in code blocks; backtick-delimited inline code; triple-backtick fences with language where useful (`typescript`, `javascript`).
    - Target ~120-180 lines total; the doc is shorter than SHELL-CONNECT-POLICY.md because NUB-CLASS has fewer surface areas.
  </action>

  <acceptance_criteria>
    File exists and contains the expected shape:
    - `test -f specs/SHELL-CLASS-POLICY.md`
    - `wc -l specs/SHELL-CLASS-POLICY.md | awk '{print $1}'` reports at least 110 lines
    - `grep -c "^## " specs/SHELL-CLASS-POLICY.md` returns in [7, 9] (Status, Why this exists, Class-Determination Authority, Wire Timing, Cross-NUB Invariant, Revocation UX, Audit Checklist, References — 8 locked; +/- 1 for any explanatory sub-H2 added): `count=$(grep -c "^## " specs/SHELL-CLASS-POLICY.md); test "$count" -ge 7 && test "$count" -le 9`

    Non-normative self-label + NUB-CLASS citation (POLICY-11):
    - `grep -i "non-normative" specs/SHELL-CLASS-POLICY.md` matches at least once
    - `grep -c "NUB-CLASS" specs/SHELL-CLASS-POLICY.md` matches at least 4 times (Status banner, Why-this-exists, Class-Determination section, References — plus incidental)

    Class-determination authority (POLICY-12):
    - `grep -i "sole authority" specs/SHELL-CLASS-POLICY.md` matches at least once
    - `grep -c "class.assigned" specs/SHELL-CLASS-POLICY.md` matches at least 6 times (prose + table + checklist)
    - `grep -i "at most one\|at-most-one" specs/SHELL-CLASS-POLICY.md` matches at least once
    - `grep -iE "mid.session|mid-session" specs/SHELL-CLASS-POLICY.md` matches at least twice (out-of-scope statement + revocation cross-ref)

    Wire timing (POLICY-13):
    - `grep -iE "iframe ready|iframe-ready|shim bootstrap|bootstrap complete" specs/SHELL-CLASS-POLICY.md` matches at least twice
    - `grep -i "before .* branch" specs/SHELL-CLASS-POLICY.md` matches at least once (or equivalent phrasing capturing the 'before napplet can meaningfully branch' MUST)

    Cross-NUB invariant (POLICY-14):
    - `grep -c "connect.granted" specs/SHELL-CLASS-POLICY.md` matches at least 4 (prose statement, scenario table rows, checklist)
    - `grep -c "class === 2" specs/SHELL-CLASS-POLICY.md` matches at least 2 (invariant statement + checklist) OR equivalent `class: 2` phrasing matches — loosen to: `grep -cE "class === 2|class: 2" specs/SHELL-CLASS-POLICY.md` returns at least 3
    - Scenario table present: `grep -c "^|" specs/SHELL-CLASS-POLICY.md` matches at least 7 (table border + header-sep + at least 5 data rows) — loosen check: `test $(grep -c "^|" specs/SHELL-CLASS-POLICY.md) -ge 7`
    - `grep -i "iff" specs/SHELL-CLASS-POLICY.md` matches at least once (the invariant uses "iff" per REQUIREMENTS.md)

    Revocation UX (POLICY-15):
    - `grep -iE "reload" specs/SHELL-CLASS-POLICY.md` matches at least once
    - `grep -iE "refuse.to.serve|refuse-to-serve" specs/SHELL-CLASS-POLICY.md` matches at least once
    - `grep -i "option a\|option b" specs/SHELL-CLASS-POLICY.md` matches at least twice (both options enumerated)
    - `grep -i "SHELL-CONNECT-POLICY" specs/SHELL-CLASS-POLICY.md` matches at least twice (cross-ref + References)

    Audit checklist (structural):
    - `grep -cE "^- \[ \]" specs/SHELL-CLASS-POLICY.md` returns at least 10 (audit checklist items covering every MUST from POLICY-12..15)
    - The section header `## Audit Checklist` appears: `grep -iE "^## audit checklist" specs/SHELL-CLASS-POLICY.md` matches exactly 1

    Zero-grep hygiene (POLICY-16):
    - `! grep -E '@napplet/|kehto|hyprgate' specs/SHELL-CLASS-POLICY.md` MUST return nonzero exit (zero matches)

    References wiring:
    - `grep "NUB-CLASS-1" specs/SHELL-CLASS-POLICY.md` matches at least once in references
    - `grep "NUB-CLASS-2" specs/SHELL-CLASS-POLICY.md` matches at least once in references + once in scenario table/revocation
    - `grep "NUB-CONNECT" specs/SHELL-CLASS-POLICY.md` matches at least twice (cross-NUB invariant + References)
    - `grep "SHELL-RESOURCE-POLICY" specs/SHELL-CLASS-POLICY.md` matches at least once (References)
    - `grep "NIP-5D" specs/SHELL-CLASS-POLICY.md` matches at least once
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; f=specs/SHELL-CLASS-POLICY.md; test -f "$f"; test $(wc -l < "$f") -ge 110; c=$(grep -c "^## " "$f"); test "$c" -ge 7 && test "$c" -le 9; grep -qi "non-normative" "$f"; test $(grep -c "NUB-CLASS" "$f") -ge 4; grep -qi "sole authority" "$f"; test $(grep -c "class.assigned" "$f") -ge 6; grep -qiE "at most one|at-most-one" "$f"; test $(grep -ciE "mid.session|mid-session" "$f") -ge 2; test $(grep -ciE "iframe ready|iframe-ready|shim bootstrap|bootstrap complete" "$f") -ge 2; test $(grep -c "connect.granted" "$f") -ge 4; test $(grep -cE "class === 2|class: 2" "$f") -ge 3; test $(grep -c "^|" "$f") -ge 7; grep -qi "iff" "$f"; grep -qiE "reload" "$f"; grep -qiE "refuse.to.serve|refuse-to-serve" "$f"; test $(grep -ci "option a\|option b" "$f") -ge 2; test $(grep -c "SHELL-CONNECT-POLICY" "$f") -ge 2; test $(grep -cE "^- \[ \]" "$f") -ge 10; grep -qiE "^## audit checklist" "$f"; ! grep -qE "@napplet/|kehto|hyprgate" "$f"; grep -q "NUB-CLASS-1" "$f"; grep -q "NUB-CLASS-2" "$f"; test $(grep -c "NUB-CONNECT" "$f") -ge 2; grep -q "SHELL-RESOURCE-POLICY" "$f"; grep -q "NIP-5D" "$f"; echo "OK"'</automated>
  </verify>

  <done>
    `specs/SHELL-CLASS-POLICY.md` exists, is ~120-180 lines, mirrors the structural shape of `specs/SHELL-RESOURCE-POLICY.md` (ATX H2 sections + blockquote banner + audit checklist + references), surfaces every POLICY-12..15 MUST as both prose and an audit checkbox, contains the cross-NUB-invariant scenario table with all 7 scenarios, self-labels non-normative, cites NUB-CLASS and the NUB-CLASS-$N track members, and is zero-grep-hygiene clean (no `@napplet/`, no `kehto`, no `hyprgate`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Final audit — grep every POLICY-N coverage check + commit</name>
  <files>specs/SHELL-CLASS-POLICY.md</files>

  <read_first>
    Re-read `specs/SHELL-CLASS-POLICY.md` in full and cross-reference against `.planning/REQUIREMENTS.md` POLICY-11..16 to verify every MUST appears in both the prose AND the audit checklist. If gaps exist, fix them in place before committing.
  </read_first>

  <action>
    Run the full grep-verifiable audit bundle below. If ANY check fails, fix the source file in place (add missing prose, add missing checkbox, fix phrasing) and re-run. Do NOT commit until every check passes.

    ## Audit bundle (all MUST pass)

    Execute as a single bash script and verify exit code 0:

    ```bash
    set -e
    f=specs/SHELL-CLASS-POLICY.md

    # File exists + size + H2 count
    test -f "$f"
    test $(wc -l < "$f") -ge 110
    c=$(grep -c "^## " "$f")
    test "$c" -ge 7 && test "$c" -le 9

    # POLICY-11: non-normative + citation
    grep -qi "non-normative" "$f"
    test $(grep -c "NUB-CLASS" "$f") -ge 4

    # POLICY-12: class-determination authority
    grep -qi "sole authority" "$f"
    test $(grep -c "class.assigned" "$f") -ge 6
    grep -qiE "at most one|at-most-one" "$f"
    test $(grep -ciE "mid.session|mid-session" "$f") -ge 2

    # POLICY-13: wire timing
    test $(grep -ciE "iframe ready|iframe-ready|shim bootstrap|bootstrap complete" "$f") -ge 2

    # POLICY-14: cross-NUB invariant + scenario table
    test $(grep -c "connect.granted" "$f") -ge 4
    test $(grep -cE "class === 2|class: 2" "$f") -ge 3
    test $(grep -c "^|" "$f") -ge 7
    grep -qi "iff" "$f"

    # POLICY-15: revocation UX
    grep -qiE "reload" "$f"
    grep -qiE "refuse.to.serve|refuse-to-serve" "$f"
    test $(grep -ci "option a\|option b" "$f") -ge 2
    test $(grep -c "SHELL-CONNECT-POLICY" "$f") -ge 2

    # Audit checklist structural gate
    test $(grep -cE "^- \[ \]" "$f") -ge 10
    grep -qiE "^## audit checklist" "$f"

    # POLICY-16: zero-grep hygiene
    ! grep -qE "@napplet/|kehto|hyprgate" "$f"

    # References wiring
    grep -q "NUB-CLASS-1" "$f"
    grep -q "NUB-CLASS-2" "$f"
    test $(grep -c "NUB-CONNECT" "$f") -ge 2
    grep -q "SHELL-RESOURCE-POLICY" "$f"
    grep -q "NIP-5D" "$f"

    echo "AUDIT PASSED"
    ```

    After the audit passes, commit using the gsd-tools harness:

    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(140-02): add specs/SHELL-CLASS-POLICY.md shell-deployer checklist" --files specs/SHELL-CLASS-POLICY.md
    ```
  </action>

  <acceptance_criteria>
    - Audit bundle bash script above exits 0 (every check passes).
    - `git log -1 --oneline` shows the new commit with the specified message.
    - `git status --porcelain specs/SHELL-CLASS-POLICY.md` is empty.
    - Zero-grep hygiene final gate: `! grep -qE '@napplet/|kehto|hyprgate' specs/SHELL-CLASS-POLICY.md` returns nonzero exit.
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'set -e; f=specs/SHELL-CLASS-POLICY.md; ! grep -qE "@napplet/|kehto|hyprgate" "$f"; git log -1 --oneline | grep -q "140-02"; test -z "$(git status --porcelain $f)"; echo OK'</automated>
  </verify>

  <done>
    `specs/SHELL-CLASS-POLICY.md` passes the full grep audit bundle covering every POLICY-11..16 requirement; file is committed with message `docs(140-02): add specs/SHELL-CLASS-POLICY.md shell-deployer checklist`; working tree is clean.
  </done>
</task>

</tasks>

<verification>
- [ ] `specs/SHELL-CLASS-POLICY.md` exists and is ~120-180 lines
- [ ] All 6 required H2 content sections present (Status, Why this exists, Class-Determination Authority, Wire Timing, Cross-NUB Invariant, Revocation UX) plus Audit Checklist + References
- [ ] Cross-NUB invariant scenario table present with all 7 scenarios
- [ ] `class === 2 iff connect.granted === true` invariant stated
- [ ] Revocation UX enumerates Option A (reload) and Option B (refuse-to-serve)
- [ ] Audit checklist has >= 10 `- [ ]` checkboxes covering every MUST from POLICY-12..15
- [ ] Self-labels non-normative with citation to NUB-CLASS
- [ ] Zero-grep hygiene: no `@napplet/`, `kehto`, `hyprgate`
- [ ] Cites sibling `SHELL-CONNECT-POLICY.md` at least twice
- [ ] Cites both `NUB-CLASS-1.md` and `NUB-CLASS-2.md` by file name
- [ ] File committed to git with phase-tagged message
</verification>

<success_criteria>
1. `specs/SHELL-CLASS-POLICY.md` is merged into the repo as a non-normative shell-deployer checklist parallel to `specs/SHELL-RESOURCE-POLICY.md` and `specs/SHELL-CONNECT-POLICY.md`.
2. Every POLICY-11..16 REQ from `.planning/REQUIREMENTS.md` is visible in the file via grep.
3. Shell deployers implementing NUB-CLASS (and class-contributing NUBs like NUB-CONNECT) can use the audit checklist to sign off on their deployment and catch cross-NUB invariant violations before they ship.
4. File is citation-safe for cross-referencing from the public `napplet/nubs` repo (no private-package refs).
</success_criteria>

<output>
After completion, create `.planning/phases/140-shell-deployer-policy-docs/140-02-SUMMARY.md` using the standard summary template, capturing:
- Final line count of the authored doc
- H2 section count and titles
- Grep audit results (every POLICY-N ID mapped to its grep check result)
- Zero-grep hygiene confirmation
- Commit hash
</output>
