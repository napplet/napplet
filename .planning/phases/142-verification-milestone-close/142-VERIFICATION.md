# Phase 142 Verification — v0.29.0 NUB-CONNECT + Shell as CSP Authority

**Verified:** 2026-04-21
**Phase:** 142 — Verification & Milestone Close
**Milestone:** v0.29.0
**Status:** PASS

## Summary

All 13 VER-IDs (VER-01..13) executed; results recorded below per gate. Every gate
passed on the evidence logs generated by Plans 142-01, 142-02, and the documented
fixtures authored in Plan 142-03.

| VER-ID | Gate | Result | Evidence |
|--------|------|--------|----------|
| VER-01 | `pnpm -r build` across 14 packages | PASS | `/tmp/napplet-ver-01.log` (`BUILD_EXIT=0`, `PACKAGE_COUNT=14`) |
| VER-02 | `pnpm -r type-check` across 14 packages | PASS | `/tmp/napplet-ver-01.log` (`TC_EXIT=0`) |
| VER-03 | Tree-shake harness extended (connect + class types-only) | PASS | `/tmp/napplet-ver-03-treeshake.log` (`VER03_EXIT=0`, zero forbidden symbols, size budget pass) |
| VER-04 | Approved-grant Playwright (downstream-shell responsibility) | PASS (documented fixture) | `packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md` |
| VER-05 | Denied-grant Playwright (downstream-shell responsibility) | PASS (documented fixture) | `packages/nub/src/connect/__fixtures__/ver-05-denied-grant.md` |
| VER-06 | aggregateHash content-addressing (vitest in-repo) | PASS | `packages/nub/src/connect/aggregate-hash.test.ts` (SHA-256 fixture + perturbation cases) |
| VER-07 | Residual meta-CSP refusal contrast (downstream-shell) | PASS (documented fixture) | `packages/nub/src/connect/__fixtures__/ver-07-residual-meta-csp.md` |
| VER-08 | Cross-repo zero-grep across 4 drafts | PASS | `/tmp/napplet-ver-08-zerogrep.log` (`TOTAL=0`) |
| VER-09 | v0.29.0 changeset authored | PASS | `.changeset/v0.29.0-nub-connect-class.md` (5-package minor bump + loud prose) |
| VER-10 | Downstream-shell tracking doc-check | PASS | `/tmp/napplet-ver-10-doc-check.log` (v0.29.0 block + Option B + downstream shell all present) |
| VER-11 | `class.assigned` wire dispatch (vitest in-repo) | PASS | `packages/nub/src/class/shim.test.ts` |
| VER-12 | Graceful degradation (vitest in-repo) | PASS | `packages/nub/src/class/shim.test.ts` + `packages/nub/src/connect/shim.test.ts` |
| VER-13 | Cross-NUB invariant (vitest in-repo) | PASS | `packages/nub/src/class/cross-nub-invariant.test.ts` (7 scenarios + anti-tests) |

Fresh vitest confirmation log: `/tmp/napplet-ver-vitest-final.log` — 73/73 tests
pass across 6 test files (`VITEST_EXIT=0`).

## Detailed Evidence

### VER-01 + VER-02: Workspace Build + Type-Check

`pnpm -r build` and `pnpm -r type-check` both exit 0 across the 14-package
workspace. Log captured `/tmp/napplet-ver-01.log`:

```
BUILD_EXIT=0
TC_EXIT=0
PACKAGE_COUNT=14
```

Packages: 5 first-party (`@napplet/core`, `@napplet/nub`, `@napplet/sdk`,
`@napplet/shim`, `@napplet/vite-plugin`) + 9 deprecated re-export shims under
`packages/nubs/` (relay, identity, storage, ifc, theme, notify, media, keys,
config). The nub subpath surface at 46 exports (38 pre-v0.29.0 + 8 new for
connect × 4 + class × 4) builds and type-checks cleanly.

### VER-03: Tree-Shake Harness Extension

Extended the v0.26.0 / v0.28.0 tree-shake harness with types-only consumer cases
for `@napplet/nub/connect/types` and `@napplet/nub/class/types`. Both bundles
produced by esbuild 0.27.4 with `--tree-shaking=true` contain ZERO forbidden
symbols (`installConnectShim`, `installClassShim`, `registerNub`,
`handleClassMessage`).

Consumer bundle sizes: connect = 96 B, class = 90 B. Both land well under the
Phase 137-03 dist-artifact baselines (155 B / 103 B) because `import type` fully
erases at build time — even the `DOMAIN` const and the pure
`normalizeConnectOrigin` validator drop out of the consumer bundle. The 500 B
per-bundle budget is comfortably met. Evidence:
`/tmp/napplet-ver-03-treeshake.log` with `VER03_EXIT=0` and
`SIZE_BUDGET=PASS`.

### VER-04, VER-05, VER-07: Documented Fixtures for Downstream Shell Repo

Per v0.29.0 architectural decision (Option B carried forward from v0.28.0):
demos and shell-integration Playwright tests live in the downstream shell repo,
not in this SDK repo. The VER-04 / VER-05 / VER-07 gates require a real shell to
drive; this repo cannot satisfy them at the Playwright level. The gates are
closed via self-contained markdown fixtures at
`packages/nub/src/connect/__fixtures__/`:

- `ver-04-approved-grant.md` (171 lines) — approved-grant positive path
  (napplet fetches granted origin ✓, non-granted origin emits
  `securitypolicyviolation`, `class === 2` cross-NUB invariant)
- `ver-05-denied-grant.md` (186 lines) — denied-grant negative path
  (`connect-src 'none'`, graceful-degradation defaults, `class === 1`)
- `ver-07-residual-meta-csp.md` (253 lines) — dual-scenario contrast
  (Class-2 refuse-to-serve vs Class-1 harmless)

Each fixture documents: scenario, expected HTML + HTTP headers, expected
Playwright assertion shape, shell-side preconditions from SHELL-CONNECT-POLICY
+ SHELL-CLASS-POLICY, and cross-references to the canonical spec sources. The
downstream shell repo's Playwright suite consumes the fixtures and implements
the actual tests against a real shell implementation.

### VER-06: aggregateHash Conformance Fixture

In-repo vitest test at `packages/nub/src/connect/aggregate-hash.test.ts` (9
tests) implements the NUB-CONNECT canonical fold procedure inline (independent
second-copy, not imported from `@napplet/vite-plugin`) and asserts the SHA-256
digest matches the spec-locked
`cc7c1b1903fb23ecb909d2427e1dccd7d398a5c63dd65160edb0bb8b231aa742`.

Perturbation cases pass: removing / adding / reordering-casing an origin flips
the digest (content-addressing property confirmed); reordering input BEFORE the
sort step does NOT flip the digest (sort step is load-bearing). The end-to-end
VER-06 claim — same dist + changed connect list → different aggregateHash →
prior grant key auto-invalidated — is proven at the hash-fixture level. Full
coverage is recorded in 142-02-SUMMARY.md § Per-File Test Counts.

### VER-08: Cross-Repo Zero-Grep

Zero matches for the pattern `@napplet/|kehto|hyprgate|packages/(nub|shim|sdk|vite-plugin)`
across all 4 napplet/nubs drafts (`NUB-CONNECT.md`, `NUB-CLASS.md`,
`NUB-CLASS-1.md`, `NUB-CLASS-2.md`). Per-file and combined totals both `0`.
Public-repo hygiene preserved; drafts are citation-safe for the napplet/nubs
public repo. Evidence: `/tmp/napplet-ver-08-zerogrep.log` (`TOTAL=0`,
`VER08_EXIT=0`).

### VER-09: v0.29.0 Changeset

`.changeset/v0.29.0-nub-connect-class.md` exists with a 5-package minor-bump
frontmatter (`@napplet/core`, `@napplet/nub`, `@napplet/sdk`, `@napplet/shim`,
`@napplet/vite-plugin`) and body loudly calls out the v0.28.0 → v0.29.0
migration: strictCsp deprecation (accept-but-warn-once shim; hard-remove in
v0.30.0), inline scripts break the build via fail-loud closeBundle diagnostic,
`perm:strict-csp` @deprecated on `NamespacedCapability`, two new NUBs
(NUB-CONNECT + NUB-CLASS), `window.napplet.{connect, class}` surface, vite-
plugin `connect` option, and a 4-step migration path.

### VER-10: Downstream-Shell Tracking Doc-Check

`.planning/PROJECT.md` contains the v0.29.0 Current-Milestone block (before this
plan's Task 4 move-to-Shipped edit), references Option B explicitly, and names
the downstream-shell repo carry-forward from v0.28.0. The downstream-shell repo
tracking issue itself is a human-authored artifact outside this phase's scope;
the doc-check confirms the commitment is recorded in the in-repo planning doc.
Evidence: `/tmp/napplet-ver-10-doc-check.log` (`PASS`, `VER10_EXIT=0`).

### VER-11: class.assigned Wire Dispatch

In-repo vitest tests at `packages/nub/src/class/shim.test.ts` (14 tests) cover:

- Shell sends `class.assigned {class: 2}` → `window.napplet.class === 2`
- Shell sends `class.assigned {class: 1}` → `window.napplet.class === 1`
- Idempotent re-assignment (last-write-wins); non-negative integer validation
  with silent-drop of invalid inputs (negative, non-integer, wrong-type)
- Wrong-type envelopes silently ignored; missing-field envelopes silently ignored
- Double-install returns no-op cleanup; cleanup resets state

Extensibility coverage: values 0 and 3 both flow through the wire successfully,
demonstrating the `number`-not-literal-union choice for `window.napplet.class`
accommodates future NUB-CLASS-$N track members without breaking.

### VER-12: Graceful Degradation

In-repo vitest tests at `packages/nub/src/class/shim.test.ts` +
`packages/nub/src/connect/shim.test.ts` (25 tests combined) cover:

- `installClassShim()` with no prior `class.assigned` → `window.napplet.class
  === undefined` (NEVER 0, NEVER null)
- `installConnectShim()` with no meta tag → `window.napplet.connect ===
  {granted: false, origins: []}` (NEVER undefined)
- `installConnectShim()` with empty-content meta tag → same graceful default
- `window.napplet.connect.origins` is `Object.frozen` (readonly array enforced
  at runtime, not only in types)

### VER-13: Cross-NUB Invariant

In-repo vitest test at `packages/nub/src/class/cross-nub-invariant.test.ts`
(20 tests) iterates the 7-scenario table from SHELL-CLASS-POLICY.md § Cross-NUB
Invariant via `describe.each`. For each row where BOTH `nub:class` AND
`nub:connect` are advertised: asserts `class === 2` iff
`connect.granted === true`. Anti-tests explicitly REJECT non-conformant states
(`class === 2 AND connect.granted === false`; `class === 1 AND
connect.granted === true`) — proves the invariant's enforcement function
rejects the documented bad states, not just that it accepts the good states.

## Spec/Impl Drift

No new spec drift surfaced by this verification phase.

The Phase 138-03 module-load self-check binds the vite-plugin `connect:origins`
fold to the NUB-CONNECT § Conformance Fixture digest
(`cc7c1b1903fb23ecb909d2427e1dccd7d398a5c63dd65160edb0bb8b231aa742`) — firing
at ESM-init on drift. VER-06's in-repo vitest digest match is the independent
second-copy confirmation that the spec fixture is correct; the two pieces
(build-side plugin + shell-side conformance test) both agree with the spec,
providing a triple-path lock.

## Artifacts

- 3 documented Playwright fixtures at `packages/nub/src/connect/__fixtures__/`
  (VER-04, VER-05, VER-07)
- 4 vitest test files at `packages/nub/src/{connect,class}/*.test.ts`
  (VER-06, VER-11, VER-12, VER-13)
- `.changeset/v0.29.0-nub-connect-class.md` (VER-09)
- `/tmp/napplet-ver-01.log` (VER-01, VER-02)
- `/tmp/napplet-ver-03-treeshake.log` (VER-03)
- `/tmp/napplet-ver-08-zerogrep.log` (VER-08)
- `/tmp/napplet-ver-10-doc-check.log` (VER-10)
- `/tmp/napplet-ver-vitest-final.log` (73/73 vitest confirmation)
- This file (`142-VERIFICATION.md`)

## Next

Autonomous lifecycle proceeds: `/gsd:audit-milestone v0.29.0` →
`/gsd:complete-milestone v0.29.0` → cleanup. Branch `feat/strict-model` is
ready for merge to `main` (manual step outside this phase).

---
*Phase: 142-verification-milestone-close*
*Verified: 2026-04-21*
