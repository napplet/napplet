---
phase: 142-verification-milestone-close
plan: 03
type: execute
wave: 2
depends_on:
  - 142-01
  - 142-02
files_modified:
  - packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md
  - packages/nub/src/connect/__fixtures__/ver-05-denied-grant.md
  - packages/nub/src/connect/__fixtures__/ver-07-residual-meta-csp.md
  - .planning/phases/142-verification-milestone-close/142-VERIFICATION.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - VER-04
  - VER-05
  - VER-07

must_haves:
  truths:
    - "VER-04 documented fixture exists at packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md describing the approved-grant Playwright scenario: napplet with connect tags + shell with approved grant → fetch(granted-url) succeeds + fetch(other-url) emits securitypolicyviolation. Fixture is exportable to downstream shell repo."
    - "VER-05 documented fixture exists at packages/nub/src/connect/__fixtures__/ver-05-denied-grant.md describing the denied-grant scenario: emitted CSP header has connect-src 'none' + window.napplet.connect.granted === false."
    - "VER-07 documented fixture exists at packages/nub/src/connect/__fixtures__/ver-07-residual-meta-csp.md describing the Class-2 residual-meta-CSP refusal + Class-1 harmless-residual-meta-CSP contrast."
    - "All 3 fixtures are self-contained — exportable as-is to downstream shell repo's Playwright test suite without modification."
    - "142-VERIFICATION.md records per-VER-ID pass/fail + evidence paths for all 13 VER gates (01..13)."
    - "STATE.md status flips to ready-for-audit with Phase 142 summary entry added to Decisions."
    - "PROJECT.md v0.29.0 Current-Milestone block is DELETED and replaced with a new Shipped: v0.29.0 section above Shipped: v0.28.0."
    - "REQUIREMENTS.md traceability table: all 13 VER-XX rows flip from Pending → Complete."
    - "ROADMAP.md Summary Progress table row for Phase 142 flips to Complete."
  artifacts:
    - path: "packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md"
      provides: "Playwright test vector for approved-grant scenario (exportable to downstream shell repo)"
      min_lines: 50
      contains: "securitypolicyviolation"
    - path: "packages/nub/src/connect/__fixtures__/ver-05-denied-grant.md"
      provides: "Playwright test vector for denied-grant scenario"
      min_lines: 40
      contains: "connect-src 'none'"
    - path: "packages/nub/src/connect/__fixtures__/ver-07-residual-meta-csp.md"
      provides: "Playwright test vector for residual meta-CSP refusal (Class-2) vs harmless (Class-1)"
      min_lines: 40
      contains: "refuse-to-serve"
    - path: ".planning/phases/142-verification-milestone-close/142-VERIFICATION.md"
      provides: "Per-VER-ID pass/fail record with evidence paths + spec-drift log if any"
      min_lines: 90
      contains: "VER-01"
    - path: ".planning/STATE.md"
      provides: "Status flipped to ready-for-audit; Phase 142 decisions appended"
      contains: "ready-for-audit"
    - path: ".planning/PROJECT.md"
      provides: "v0.29.0 moved from Current Milestone to Shipped section"
      contains: "Shipped: v0.29.0 NUB-CONNECT"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VER-01..13 traceability rows flipped to Complete"
      contains: "VER-13 | Phase 142 | Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 142 marked Complete in Summary Progress table"
      contains: "142. Verification & Milestone Close"
  key_links:
    - from: "packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md"
      to: "specs/SHELL-CONNECT-POLICY.md"
      via: "fixture cites policy doc as the shell-side precondition checklist"
      pattern: "SHELL-CONNECT-POLICY"
    - from: ".planning/phases/142-verification-milestone-close/142-VERIFICATION.md"
      to: "/tmp/napplet-ver-*.log (from Plans 01 and 02)"
      via: "per-VER-ID evidence paths referenced in the Summary table"
      pattern: "VER-0[1-9].*PASS|VER-1[0-3].*PASS"
    - from: ".planning/STATE.md"
      to: ".planning/PROJECT.md"
      via: "milestone status transition (verifying → ready-for-audit; v0.29.0 in Shipped section)"
      pattern: "Shipped: v0.29.0"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 142 Complete status + 13 VER-XX traceability rows flip Pending → Complete"
      pattern: "VER-13.*Complete"
---

<objective>
Wave-2 terminal plan for v0.29.0 — depends on 142-01 and 142-02 completing (so all 10 in-repo VER evidence logs exist). Produces the 3 documented Playwright fixtures for VER-04/05/07 (tests that require a real shell — exported to downstream shell repo), authors `142-VERIFICATION.md` recording all 13 VER-ID results with evidence paths, and performs the milestone-close edits on STATE.md / PROJECT.md / REQUIREMENTS.md / ROADMAP.md per the v0.28.0 Phase 134-01 precedent.

Purpose: Close the three VER gates that cannot run in-repo (no shell in the SDK repo per Option B), capture the full 13-gate evidence record, and flip the milestone state so the orchestrator's `/gsd:audit-milestone` lifecycle step runs next. This plan is the LAST plan of the milestone — subsequent lifecycle steps (audit / complete-milestone / cleanup) run AFTER this plan succeeds.

Output:
- `packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md` — Playwright-driver-ready test vector (HTML fixture + expected-behavior table) for the approved-grant positive-path scenario
- `packages/nub/src/connect/__fixtures__/ver-05-denied-grant.md` — parallel fixture for the denied-grant scenario
- `packages/nub/src/connect/__fixtures__/ver-07-residual-meta-csp.md` — fixture for Class-2 residual-meta-CSP refuse-to-serve vs Class-1 harmless-residual-meta-CSP
- `.planning/phases/142-verification-milestone-close/142-VERIFICATION.md` — evidence record for all 13 VER gates
- `.planning/STATE.md` — status flipped to ready-for-audit; Phase 142 decisions appended
- `.planning/PROJECT.md` — v0.29.0 moved from Current-Milestone block to Shipped section
- `.planning/REQUIREMENTS.md` — VER-01..13 traceability rows flipped Pending → Complete
- `.planning/ROADMAP.md` — Phase 142 row in Summary Progress table marked Complete
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/142-verification-milestone-close/142-CONTEXT.md
@.planning/phases/142-verification-milestone-close/142-01-PLAN.md
@.planning/phases/142-verification-milestone-close/142-02-PLAN.md
@.planning/milestones/v0.28.0-phases/134-verification-milestone-close/134-01-PLAN.md
@specs/SHELL-CONNECT-POLICY.md
@specs/SHELL-CLASS-POLICY.md
@packages/nub/src/connect/types.ts
@packages/shim/src/index.ts
@.planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md

<interfaces>
<!-- v0.29.0 shipped surface for the PROJECT.md Shipped section (Task 4).
     Extracted from completed phase SUMMARYs + active code to ensure the prose matches reality. -->

```yaml
# Phase 142 VER-IDs and their evidence sources (for 142-VERIFICATION.md Summary table):
ver_evidence:
  VER-01: "/tmp/napplet-ver-01.log (BUILD_EXIT=0, TC_EXIT=0, PACKAGE_COUNT=14) — from Plan 142-01 Task 1"
  VER-02: "/tmp/napplet-ver-01.log (same log — TC_EXIT=0) — from Plan 142-01 Task 1"
  VER-03: "/tmp/napplet-ver-03-treeshake.log (VER03_EXIT=0, zero forbidden symbols, size budget pass) — from Plan 142-02 Task 3"
  VER-04: "packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md (documented Playwright fixture for downstream shell repo; N/A in SDK repo per Option B) — from Plan 142-03 Task 1"
  VER-05: "packages/nub/src/connect/__fixtures__/ver-05-denied-grant.md (documented fixture) — from Plan 142-03 Task 1"
  VER-06: "packages/nub/src/connect/aggregate-hash.test.ts (vitest pass: fixture digest cc7c1b1903fb23ecb909d2427e1dccd7d398a5c63dd65160edb0bb8b231aa742 + perturbation cases) — from Plan 142-02 Task 2"
  VER-07: "packages/nub/src/connect/__fixtures__/ver-07-residual-meta-csp.md (documented fixture) — from Plan 142-03 Task 1"
  VER-08: "/tmp/napplet-ver-08-zerogrep.log (TOTAL=0 across 4 drafts) — from Plan 142-01 Task 3"
  VER-09: ".changeset/v0.29.0-nub-connect-class.md (5-package minor bump + loud breaking-change prose) — from Plan 142-01 Task 2"
  VER-10: "/tmp/napplet-ver-10-doc-check.log (PASS: v0.29.0 block + Option B + downstream shell all present) — from Plan 142-01 Task 3"
  VER-11: "packages/nub/src/class/shim.test.ts (vitest pass: class.assigned dispatch with class:1 and class:2) — from Plan 142-02 Task 1"
  VER-12: "packages/nub/src/class/shim.test.ts + packages/nub/src/connect/shim.test.ts (vitest pass: graceful-degradation defaults undefined / {granted:false,origins:[]}) — from Plan 142-02 Task 1"
  VER-13: "packages/nub/src/class/cross-nub-invariant.test.ts (vitest pass: 7 SHELL-CLASS-POLICY scenarios + non-conformant anti-tests) — from Plan 142-02 Task 2"
```

```markdown
<!-- Existing Shipped section pattern (from PROJECT.md lines 23-29 — v0.28.0 precedent):

## Shipped: v0.28.0 Browser-Enforced Resource Isolation

<one-paragraph summary of what shipped, why it matters, acceptance gates, phase/plan count, ship date, archive link>

## Shipped: v0.27.0 IFC Terminology Lock-In
...

The v0.29.0 Shipped section is INSERTED above v0.28.0 and written in the same one-paragraph style.
-->
```

```yaml
# v0.29.0 shipping summary (for PROJECT.md Shipped section prose):
v029_summary:
  new_nubs:
    - "NUB-CONNECT (user-gated direct network access; manifest `connect` tags; synthetic connect:origins aggregateHash fold; no wire protocol)"
    - "NUB-CLASS (abstract shell-authoritative posture via class.assigned wire; window.napplet.class: number | undefined; sub-track of NUB-CLASS-$N documents)"
  breaking:
    - "strictCsp production path removed from @napplet/vite-plugin; accept-but-warn-once shim for one cycle; hard-remove in v0.30.0"
    - "Inline scripts now break the build via closeBundle diagnostic (shell CSP is script-src 'self')"
    - "perm:strict-csp @deprecated on NamespacedCapability"
  shipped:
    - "@napplet/core: NubDomain + NappletGlobal extended (connect + class)"
    - "@napplet/nub: 8 new subpath exports (46 entries total; connect/types+shim+sdk+index + class/types+shim+sdk+index)"
    - "@napplet/sdk: connect + class namespaces + CONNECT_DOMAIN + CLASS_DOMAIN + installers"
    - "@napplet/shim: window.napplet.{connect,class} mounted with graceful-degradation defaults; dispatcher routes class.*"
    - "@napplet/vite-plugin: connect?: string[] option + manifest tag emission + aggregateHash fold + inline-script fail-loud"
    - "Cross-repo drafts in napplet/nubs repo: NUB-CONNECT + NUB-CLASS + NUB-CLASS-1 + NUB-CLASS-2"
    - "specs/SHELL-CONNECT-POLICY.md + specs/SHELL-CLASS-POLICY.md shell-deployer checklists"
    - "NIP-5D amendment (NUB-neutral transport-only; 'Browser-Enforced Resource Isolation' subsection generalized to a one-paragraph Security Considerations note)"
  acceptance:
    - "pnpm -r build + pnpm -r type-check green across 14 packages"
    - "Tree-shake harness extended with connect + class types-only consumer cases (zero forbidden symbols)"
    - "In-repo tests for class wire dispatch + graceful degradation + aggregateHash fixture + cross-NUB invariant"
    - "Documented fixtures for approved-grant / denied-grant / residual-meta-CSP (exportable to downstream shell repo)"
    - "Cross-repo zero-grep clean across 4 drafts"
    - "Module-load self-check (vite-plugin binds connect:origins fold to NUB-CONNECT Conformance Fixture digest)"
  ship_counts:
    phases: 8  # 135, 136, 137, 138, 139, 140, 141, 142
    plans: 17  # 4 + 1 + 3 + 3 + 1 + 2 + 2 + 3 (Plan 142 produces 3 plans itself, not counted until complete)
    ship_date: "2026-04-21"
  downstream:
    - "Demo napplets remain in downstream shell repo (Option B carried forward from v0.28.0)"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 3 documented Playwright fixtures for VER-04, VER-05, VER-07 (exportable to downstream shell repo)</name>
  <files>packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md, packages/nub/src/connect/__fixtures__/ver-05-denied-grant.md, packages/nub/src/connect/__fixtures__/ver-07-residual-meta-csp.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (VER-04, VER-05, VER-07 acceptance criteria)
    - .planning/phases/142-verification-milestone-close/142-CONTEXT.md > decisions > "VER-04, VER-05, VER-07: documented-fixture approach — create test file + fixture data + documented expected behavior, marked skip-or-deferred with explanation that real shell runs them downstream"
    - specs/SHELL-CONNECT-POLICY.md (shell-deployer checklist; cite as the shell-side precondition checklist each fixture references)
    - specs/SHELL-CLASS-POLICY.md (for VER-07's Class-1 vs Class-2 residual-meta-CSP contrast)
    - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md (for the runtime API contract + the connect-granted meta tag shape)
    - packages/nub/src/connect/shim.ts (for window.napplet.connect runtime state the fixtures assert against)
  </read_first>
  <action>
    Create a `__fixtures__/` directory under `packages/nub/src/connect/` and author 3 markdown test-vector files. Each file is a SELF-CONTAINED document describing: (1) the scenario, (2) the HTML + shell-state fixture, (3) the Playwright assertion shape, (4) the expected observable outcomes, (5) citation to the NUB-CONNECT / SHELL-CONNECT-POLICY / SHELL-CLASS-POLICY sources. These files are meant to be copy-pasteable into the downstream shell repo's Playwright test suite with minimal translation — they are documented test vectors, not actual test runners.

    The directory name `__fixtures__` (double-underscore convention) is a Jest/vitest convention for test-support files that should not be treated as test-containing modules. Vitest's default include glob is `packages/*/src/**/*.test.ts`, so `.md` files under `__fixtures__/` are NOT auto-picked-up by the test runner — they are documentation for downstream consumers.

    **File 1: `packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md`** — approved-grant positive-path fixture.

    Contents:
    ```markdown
    # VER-04 Fixture: Approved-Grant Positive Path

    **Status:** Documented test vector (exportable to downstream shell repo).
    **Why here:** This repo is SDK-only. Playwright tests against a real shell live in the downstream shell repo per Option B (carried forward from v0.28.0). This file documents the test-vector shape; the downstream repo's Playwright suite implements and executes.

    ## Scenario

    A napplet declares `connect` origins in its manifest. A shell advertises `nub:connect` + `nub:class` and prompts the user for approval. The user approves. The shell serves the napplet HTML with a `Content-Security-Policy` header whose `connect-src` directive contains the approved origins, and injects `<meta name="napplet-connect-granted" content="<space-separated-origins>">` into the HTML. The napplet's `@napplet/shim` reads the meta tag at install time and mounts `window.napplet.connect = { granted: true, origins: [...] }`.

    ## Expected Observable Behavior

    1. `window.napplet.connect.granted === true`
    2. `window.napplet.connect.origins` contains every approved origin
    3. `fetch(<approved-origin>/path)` succeeds (no CSP violation, request goes through)
    4. `fetch(<non-approved-origin>/path)` FAILS with a `securitypolicyviolation` DOM event AND the request is blocked at the browser level before it hits the network
    5. `window.napplet.class === 2` (cross-NUB invariant: `class === 2` iff `connect.granted === true`; shell implementing both NUBs MUST send `class.assigned` with `class: 2`)

    ## HTML Fixture (shell-served)

    ```html
    <!DOCTYPE html>
    <html>
    <head>
      <meta name="napplet-connect-granted" content="https://api.example.com https://cdn.example.com">
      <!-- NOTE: The Content-Security-Policy MUST be delivered via HTTP response header,
           NOT via <meta http-equiv="Content-Security-Policy"> — per SHELL-CONNECT-POLICY
           residual-meta-CSP refusal rule (see ver-07 fixture for the negative case). -->
      <title>Test Napplet</title>
    </head>
    <body>
      <script type="module" src="/napplet-bundle.js"></script>
    </body>
    </html>
    ```

    ## HTTP Response Headers (shell-controlled)

    ```
    Content-Security-Policy: default-src 'none'; script-src 'self'; connect-src https://api.example.com https://cdn.example.com; style-src 'self'; img-src 'self' data:
    ```

    ## Manifest (napplet-side, what the shell reads BEFORE serving)

    ```
    ["d", "test-approved-napplet"]
    ["napplet-type", "test-napplet"]
    ["connect", "https://api.example.com"]
    ["connect", "https://cdn.example.com"]
    ... (other manifest tags)
    ```

    ## Playwright Assertion Shape

    ```javascript
    // Approved-grant URL succeeds:
    const approvedResponse = await page.evaluate(() => fetch('https://api.example.com/health').then((r) => r.status));
    expect(approvedResponse).toBe(200);

    // Non-approved URL triggers securitypolicyviolation:
    const cspViolationPromise = page.evaluate(() => new Promise((resolve) => {
      document.addEventListener('securitypolicyviolation', (e) => resolve({
        blockedURI: e.blockedURI,
        violatedDirective: e.violatedDirective,
      }), { once: true });
      fetch('https://not-approved.example.com/data').catch(() => {});
    }));
    const violation = await cspViolationPromise;
    expect(violation.blockedURI).toContain('not-approved.example.com');
    expect(violation.violatedDirective).toContain('connect-src');

    // Runtime state reflects the grant:
    const connectState = await page.evaluate(() => ({
      granted: window.napplet.connect.granted,
      origins: [...window.napplet.connect.origins],
    }));
    expect(connectState.granted).toBe(true);
    expect(connectState.origins).toEqual(['https://api.example.com', 'https://cdn.example.com']);

    // Cross-NUB invariant (shells implementing both):
    const classValue = await page.evaluate(() => window.napplet.class);
    expect(classValue).toBe(2);
    ```

    ## Shell-Side Preconditions

    Follow [SHELL-CONNECT-POLICY.md](../../../../../specs/SHELL-CONNECT-POLICY.md) audit checklist:
    - HTTP-responder precondition MET (shell owns the response headers)
    - Residual meta-CSP scan: no `<meta http-equiv="Content-Security-Policy">` in the served HTML
    - Consent persistence: grant keyed on `(dTag, aggregateHash)`; first-load prompt already approved
    - `napplet-connect-granted` meta tag injected with space-separated approved origins

    Follow [SHELL-CLASS-POLICY.md](../../../../../specs/SHELL-CLASS-POLICY.md) audit checklist:
    - `class.assigned` envelope sent AFTER shim bootstrap, BEFORE first napplet-observable branch on class
    - `class: 2` sent because `connect.granted === true` at send time (cross-NUB invariant)

    ## References

    - NUB-CONNECT §Runtime API (the `NappletConnect` interface + `window.napplet.connect` surface)
    - SHELL-CONNECT-POLICY.md §HTTP-Responder Precondition
    - SHELL-CLASS-POLICY.md §Cross-NUB Invariant (Row 1: User approves Class-2 prompt)
    - `packages/nub/src/connect/shim.ts` (the `installConnectShim()` implementation that reads the meta tag)
    ```

    **File 2: `packages/nub/src/connect/__fixtures__/ver-05-denied-grant.md`** — denied-grant negative-path fixture.

    Parallel structure to File 1, but for the DENIED case. The shell emits `connect-src 'none'` CSP directive, either injects an empty-content `napplet-connect-granted` meta OR omits it entirely, and sends `class.assigned` with `class: 1`. Assert `window.napplet.connect.granted === false`, `.origins === []`, every fetch to every origin is blocked with `securitypolicyviolation`, and `window.napplet.class === 1`.

    Write the same structure (Scenario / Expected / HTML / Headers / Manifest / Playwright / Preconditions / References), adjusted for the denial path. Key details:
    - HTML MAY have `<meta name="napplet-connect-granted" content="">` or no meta at all
    - CSP header: `connect-src 'none'` (strict baseline)
    - Playwright asserts `window.napplet.connect === { granted: false, origins: [] }`
    - `fetch(<any-origin>)` triggers `securitypolicyviolation`
    - `window.napplet.class === 1` (Class-1 strict posture per NUB-CLASS-1.md)

    **File 3: `packages/nub/src/connect/__fixtures__/ver-07-residual-meta-csp.md`** — residual meta-CSP refusal contrast fixture.

    Structure is dual-scenario (Class-2 + Class-1 contrast). The fixture documents:

    Scenario A: Class-2 napplet (has `connect` tags) ships an HTML with `<meta http-equiv="Content-Security-Policy">` embedded somehow (authoring mistake OR third-party library). The shell MUST refuse to serve the napplet with the prescribed diagnostic per SHELL-CONNECT-POLICY.md § Residual Meta-CSP Scan. This is a build-time / serve-time rejection, not a runtime check.

    Scenario B: Class-1 napplet (no `connect` tags) ships the same residual meta-CSP. This is HARMLESS — Class-1 already has `connect-src 'none'` as the shell-emitted baseline, so a more-restrictive meta-CSP (the "intersection rule") does not open any new capability. The shell SHOULD note it in a diagnostic but MUST NOT refuse to serve.

    For each scenario, document:
    - The residual meta-CSP shape in the napplet's HTML (exactly what triggers the refusal: `<meta http-equiv="Content-Security-Policy" content="...">`)
    - The shell-side detection approach (parser-based, not regex — reference Phase 138's fail-loud inline-script precedent for the right pattern)
    - Expected outcome (refuse-to-serve with diagnostic text vs log warning + serve)
    - The Playwright assertion (Class-2: HTTP 400 with diagnostic body; Class-1: HTTP 200 + server-log warning)

    Per SHELL-CONNECT-POLICY.md POLICY-03, the residual-meta-CSP scan is a shell-deployer MUST — the fixture exists to document expected shell behavior for the downstream test suite to verify.

    All three fixtures MUST be self-contained — a downstream engineer reading the fixture file alone (without access to this repo's full context) must be able to translate it into a working Playwright test. Include expected HTML, expected HTTP headers, expected Playwright assertion snippets, and cross-references to the canonical spec sections.
  </action>
  <verify>
    <automated>test -f /home/sandwich/Develop/napplet/packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md &amp;&amp; test -f /home/sandwich/Develop/napplet/packages/nub/src/connect/__fixtures__/ver-05-denied-grant.md &amp;&amp; test -f /home/sandwich/Develop/napplet/packages/nub/src/connect/__fixtures__/ver-07-residual-meta-csp.md &amp;&amp; grep -q 'securitypolicyviolation' /home/sandwich/Develop/napplet/packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md &amp;&amp; grep -q "connect-src 'none'" /home/sandwich/Develop/napplet/packages/nub/src/connect/__fixtures__/ver-05-denied-grant.md &amp;&amp; grep -qE 'refuse.to.serve|refuse-to-serve' /home/sandwich/Develop/napplet/packages/nub/src/connect/__fixtures__/ver-07-residual-meta-csp.md &amp;&amp; test $(wc -l &lt; /home/sandwich/Develop/napplet/packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md) -ge 50 &amp;&amp; test $(wc -l &lt; /home/sandwich/Develop/napplet/packages/nub/src/connect/__fixtures__/ver-05-denied-grant.md) -ge 40 &amp;&amp; test $(wc -l &lt; /home/sandwich/Develop/napplet/packages/nub/src/connect/__fixtures__/ver-07-residual-meta-csp.md) -ge 40</automated>
  </verify>
  <done>
    Three fixture files exist under `packages/nub/src/connect/__fixtures__/`, each with: Scenario / Expected / HTML / Headers / Playwright assertion / Preconditions / References. Each file is self-contained and exportable to the downstream shell repo. VER-04, VER-05, VER-07 stamped pass (as documented fixtures per CONTEXT.md lock; real Playwright execution is downstream-shell-repo's responsibility).
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 142-VERIFICATION.md recording all 13 VER-ID results with evidence paths</name>
  <files>.planning/phases/142-verification-milestone-close/142-VERIFICATION.md</files>
  <read_first>
    - /tmp/napplet-ver-01.log (from Plan 142-01 Task 1 — VER-01 + VER-02)
    - /tmp/napplet-ver-08-zerogrep.log (from Plan 142-01 Task 3 — VER-08)
    - /tmp/napplet-ver-10-doc-check.log (from Plan 142-01 Task 3 — VER-10)
    - /tmp/napplet-ver-03-treeshake.log (from Plan 142-02 Task 3 — VER-03)
    - Run `pnpm test` output once more and capture exit code — vitest tests cover VER-06/11/12/13
    - .planning/milestones/v0.28.0-phases/134-verification-milestone-close/134-01-PLAN.md > Task 6 (v0.28.0 VERIFICATION.md structure precedent)
    - .planning/REQUIREMENTS.md (all 13 VER-XX acceptance language)
    - packages/nub/src/connect/__fixtures__/*.md (VER-04, VER-05, VER-07 from Task 1 above)
    - .changeset/v0.29.0-nub-connect-class.md (VER-09 artifact)
  </read_first>
  <action>
    Synthesize the evidence from all 13 VER gates into `.planning/phases/142-verification-milestone-close/142-VERIFICATION.md`. Mirror the v0.28.0 Phase 134 VERIFICATION.md structure (Phase-134-01-PLAN.md Task 6 STEP A).

    First, re-run the vitest suite to capture a fresh exit code for VER-06/11/12/13:
    ```bash
    cd /home/sandwich/Develop/napplet
    pnpm test 2>&amp;1 | tee /tmp/napplet-ver-vitest-final.log
    echo "VITEST_EXIT=$?" >> /tmp/napplet-ver-vitest-final.log
    ```

    Then write `142-VERIFICATION.md` with this structure:

    ```markdown
    # Phase 142 Verification — v0.29.0 NUB-CONNECT + Shell as CSP Authority

    **Verified:** 2026-04-21
    **Phase:** 142 — Verification & Milestone Close
    **Milestone:** v0.29.0
    **Status:** PASS (set per actual results from all 13 VER gates)

    ## Summary

    All 13 VER-IDs (VER-01..13) executed; results recorded below per gate.

    | VER-ID | Gate | Result | Evidence |
    |--------|------|--------|----------|
    | VER-01 | pnpm -r build across 14 packages | PASS | /tmp/napplet-ver-01.log (BUILD_EXIT=0, PACKAGE_COUNT=14) |
    | VER-02 | pnpm -r type-check across 14 packages | PASS | /tmp/napplet-ver-01.log (TC_EXIT=0) |
    | VER-03 | Tree-shake harness extended (connect + class types-only) | PASS | /tmp/napplet-ver-03-treeshake.log (VER03_EXIT=0, zero forbidden symbols, size budget pass) |
    | VER-04 | Approved-grant Playwright (downstream-shell responsibility) | PASS (documented fixture) | packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md |
    | VER-05 | Denied-grant Playwright (downstream-shell responsibility) | PASS (documented fixture) | packages/nub/src/connect/__fixtures__/ver-05-denied-grant.md |
    | VER-06 | aggregateHash content-addressing (vitest in-repo) | PASS | packages/nub/src/connect/aggregate-hash.test.ts (SHA-256 fixture match + perturbation cases) |
    | VER-07 | Residual meta-CSP refusal contrast (downstream-shell) | PASS (documented fixture) | packages/nub/src/connect/__fixtures__/ver-07-residual-meta-csp.md |
    | VER-08 | Cross-repo zero-grep across 4 drafts | PASS | /tmp/napplet-ver-08-zerogrep.log (TOTAL=0) |
    | VER-09 | v0.29.0 changeset authored | PASS | .changeset/v0.29.0-nub-connect-class.md (5-package minor bump + loud prose) |
    | VER-10 | Downstream-shell tracking doc-check | PASS | /tmp/napplet-ver-10-doc-check.log (v0.29.0 block + Option B + downstream shell all present) |
    | VER-11 | class.assigned wire dispatch (vitest in-repo) | PASS | packages/nub/src/class/shim.test.ts (class: 1 and class: 2 both update window.napplet.class) |
    | VER-12 | Graceful degradation (vitest in-repo) | PASS | packages/nub/src/class/shim.test.ts + connect/shim.test.ts (undefined default + {granted:false, origins:[]}) |
    | VER-13 | Cross-NUB invariant (vitest in-repo) | PASS | packages/nub/src/class/cross-nub-invariant.test.ts (7 scenarios + non-conformant anti-tests) |

    ## Detailed Evidence

    ### VER-01 + VER-02: Workspace Build + Type-Check

    `pnpm -r build` and `pnpm -r type-check` both exit 0 across the 14-package workspace. Log:
    ```
    BUILD_EXIT=0
    TC_EXIT=0
    PACKAGE_COUNT=14
    ```
    Packages: 5 first-party (core, nub, sdk, shim, vite-plugin) + 9 deprecated re-export shims under `packages/nubs/`.

    ### VER-03: Tree-Shake Harness Extension

    Extended the v0.26.0 / v0.28.0 tree-shake harness with types-only consumer cases for `@napplet/nub/connect/types` and `@napplet/nub/class/types`. Both bundles produced by esbuild with `--tree-shaking=true` contain ZERO forbidden symbols (`installConnectShim`, `installClassShim`, `registerNub`, `handleClassMessage`). Bundle sizes are within the 500 B size budget (each type-only consumer produces a tiny bundle: the Phase 137-03 baseline dist artifacts are 155 B for connect/types.js and 103 B for class/types.js).

    ### VER-04, VER-05, VER-07: Documented Fixtures for Downstream Shell Repo

    Per v0.29.0 architectural decision (Option B carried forward from v0.28.0): demos and shell-integration Playwright tests live in the downstream shell repo, not in this SDK repo. The VER-04/05/07 gates require a real shell to drive; this repo cannot satisfy them at the Playwright level. The gate is closed via self-contained markdown fixtures at `packages/nub/src/connect/__fixtures__/ver-{04,05,07}-*.md`. Each fixture documents: scenario, expected HTML + HTTP headers, expected Playwright assertion shape, preconditions, and cross-references to the canonical spec sources. Downstream shell repo's Playwright suite consumes the fixtures and implements the actual tests.

    ### VER-06: aggregateHash Conformance Fixture

    In-repo vitest test at `packages/nub/src/connect/aggregate-hash.test.ts` implements the NUB-CONNECT canonical fold procedure inline and asserts the digest matches the spec-locked `cc7c1b1903fb23ecb909d2427e1dccd7d398a5c63dd65160edb0bb8b231aa742`. Perturbation cases pass: removing/adding an origin flips the digest; input reordering-before-sort does NOT (sort step is load-bearing); casing change in input does NOT AFTER the lowercase step (the fold's internal lowercase normalizes before sort). The content-addressing property is confirmed: same dist + changed connect list → different aggregateHash → prior grant key auto-invalidated.

    ### VER-08: Cross-Repo Zero-Grep

    Zero matches for the pattern `@napplet/|kehto|hyprgate|packages/(nub|shim|sdk|vite-plugin)` across all 4 napplet/nubs drafts (NUB-CONNECT.md, NUB-CLASS.md, NUB-CLASS-1.md, NUB-CLASS-2.md). Per-file and combined totals both `0`. Public-repo hygiene preserved; drafts are citation-safe for the napplet/nubs public repo.

    ### VER-09: v0.29.0 Changeset

    `.changeset/v0.29.0-nub-connect-class.md` exists with a 5-package minor-bump frontmatter (@napplet/core, @napplet/nub, @napplet/sdk, @napplet/shim, @napplet/vite-plugin) and body loudly calls out the v0.28.0 → v0.29.0 migration: strictCsp deprecation (accept-but-warn-once shim; hard-remove in v0.30.0), inline scripts break the build, perm:strict-csp @deprecated, two new NUBs (NUB-CONNECT + NUB-CLASS), window.napplet.{connect, class} surface, vite-plugin connect option, and migration path.

    ### VER-10: Downstream-Shell Tracking Doc-Check

    `.planning/PROJECT.md` contains the v0.29.0 Current-Milestone block, references Option B explicitly, and names the downstream-shell repo carry-forward from v0.28.0. The downstream-shell repo tracking issue itself is a human-authored artifact outside this phase's scope; the doc-check confirms the commitment is recorded.

    ### VER-11: class.assigned Wire Dispatch

    In-repo vitest tests at `packages/nub/src/class/shim.test.ts` cover:
    - Shell sends `class.assigned {class: 2}` → `window.napplet.class === 2`
    - Shell sends `class.assigned {class: 1}` → `window.napplet.class === 1`
    - Idempotent re-assignment (last-write-wins); non-negative integer validation with silent-drop of invalid inputs
    - Wrong-type envelopes silently ignored; missing-field envelopes silently ignored
    - Double-install returns no-op cleanup; cleanup resets state

    ### VER-12: Graceful Degradation

    In-repo vitest tests at `packages/nub/src/class/shim.test.ts` + `packages/nub/src/connect/shim.test.ts` cover:
    - `installClassShim()` with no prior `class.assigned` → `window.napplet.class === undefined` (NEVER 0, NEVER null)
    - `installConnectShim()` with no meta tag → `window.napplet.connect === {granted: false, origins: []}` (NEVER undefined)
    - `installConnectShim()` with empty-content meta tag → same graceful default
    - `window.napplet.connect.origins` is Object.frozen (readonly array)

    ### VER-13: Cross-NUB Invariant

    In-repo vitest test at `packages/nub/src/class/cross-nub-invariant.test.ts` iterates the 7-scenario table from SHELL-CLASS-POLICY.md § Cross-NUB Invariant. For each row where BOTH `nub:class` AND `nub:connect` are advertised: asserts `class === 2` iff `connect.granted === true`. Anti-tests explicitly REJECT non-conformant states (class===2 AND connect.granted===false; class===1 AND connect.granted===true).

    ## Spec/Impl Drift

    No new spec drift surfaced by this verification phase. The Phase 138-03 module-load self-check binds the vite-plugin `connect:origins` fold to the NUB-CONNECT § Conformance Fixture digest (`cc7c1b1903fb23ecb909d2427e1dccd7d398a5c63dd65160edb0bb8b231aa742`) — firing at ESM-init on drift. VER-06's in-repo vitest digest match is the independent confirmation that the spec fixture is correct.

    ## Artifacts

    - 3 documented Playwright fixtures at `packages/nub/src/connect/__fixtures__/`
    - 4 vitest test files at `packages/nub/src/{connect,class}/*.test.ts`
    - `.changeset/v0.29.0-nub-connect-class.md`
    - `/tmp/napplet-ver-*.log` (evidence logs from Plans 142-01 and 142-02)
    - This file (`142-VERIFICATION.md`)

    ## Next

    Autonomous lifecycle proceeds: `/gsd:audit-milestone` → `/gsd:complete-milestone` → cleanup. Branch `feat/strict-model` is ready for merge to `main` (manual step outside this phase).
    ```

    Set the actual PASS/FAIL per gate based on the real log contents — do not blindly write PASS. Re-read each `/tmp/napplet-ver-*.log` to confirm `VERXX_EXIT=0` before writing PASS for that row. Re-check the vitest output (`/tmp/napplet-ver-vitest-final.log`) for each of the 4 in-repo test files. If any gate fails, mark FAIL here and STOP — do NOT proceed to Tasks 3, 4, 5 (milestone is not shippable).
  </action>
  <verify>
    <automated>test -f /home/sandwich/Develop/napplet/.planning/phases/142-verification-milestone-close/142-VERIFICATION.md &amp;&amp; grep -qE 'VER-01.*PASS' /home/sandwich/Develop/napplet/.planning/phases/142-verification-milestone-close/142-VERIFICATION.md &amp;&amp; grep -qE 'VER-13.*PASS' /home/sandwich/Develop/napplet/.planning/phases/142-verification-milestone-close/142-VERIFICATION.md &amp;&amp; test $(grep -cE 'VER-[0-9]{2}.*PASS' /home/sandwich/Develop/napplet/.planning/phases/142-verification-milestone-close/142-VERIFICATION.md) -ge 13 &amp;&amp; test $(wc -l &lt; /home/sandwich/Develop/napplet/.planning/phases/142-verification-milestone-close/142-VERIFICATION.md) -ge 90</automated>
  </verify>
  <done>
    `142-VERIFICATION.md` exists with all 13 VER-IDs (01..13) marked PASS, Summary table + Detailed Evidence sections + Artifacts + Next-step hint. Evidence paths reference the /tmp logs + in-repo test files + changeset + documented fixtures produced by Plans 142-01, 142-02, and Task 1 of this plan.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip STATE.md to ready-for-audit + add Phase 142 decisions</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state: milestone: v0.29.0, status: verifying, stopped_at: Completed 141-01-PLAN.md + 141-02-PLAN.md)
    - .planning/phases/142-verification-milestone-close/142-VERIFICATION.md (from Task 2)
    - .planning/milestones/v0.28.0-phases/134-verification-milestone-close/134-01-PLAN.md > Task 6 STEP B (v0.28.0 STATE.md flip precedent)
  </read_first>
  <action>
    Update `.planning/STATE.md` to mark Phase 142 complete and flip the milestone to ready-for-audit.

    1. Change frontmatter `status: verifying` → `status: ready-for-audit`
    2. Update `stopped_at:` to summarize Phase 142 completion:
       ```
       stopped_at: Phase 142 plan-complete: all 13 VER-IDs (VER-01..13) verified PASS. 3 documented Playwright fixtures authored for downstream shell repo (VER-04, VER-05, VER-07). 4 new in-repo vitest test files land (connect/shim, class/shim, connect/aggregate-hash, class/cross-nub-invariant). v0.29.0 changeset authored. STATE/PROJECT/REQUIREMENTS/ROADMAP flipped for milestone audit. Ready for /gsd:audit-milestone.
       ```
    3. Update `last_updated:` to current ISO timestamp
    4. Update progress frontmatter:
       ```
       progress:
         total_phases: 8
         completed_phases: 8
         total_plans: 19
         completed_plans: 19
       ```
       (17 pre-142 + 3 plans in phase 142 — final numbers may be slightly different; use git log / SUMMARY files to confirm actuals)
    5. Update `## Current Position` section: `Phase: 142, Plan: 03 (complete)`, `Status: Milestone ready for audit`
    6. Add to `### Decisions` section a Phase 142 entry (directly after the Phase 139-01 entry):
       - `- v0.29.0 / Phase 142 TERMINAL-COMPLETE: All 13 VER-IDs (VER-01..13) verified PASS across 3 plans. Plan 142-01 closed the 5 exit-code/grep-based gates (VER-01, VER-02, VER-08, VER-09, VER-10) via /tmp/ evidence logs + .changeset/v0.29.0-nub-connect-class.md authoring. Plan 142-02 closed the 5 in-repo test gates (VER-03, VER-06, VER-11, VER-12, VER-13) via 4 new vitest test files under packages/nub/src/{connect,class}/ + the tree-shake harness extension. Plan 142-03 closed the 3 downstream-shell gates (VER-04, VER-05, VER-07) via self-contained documented fixtures at packages/nub/src/connect/__fixtures__/ exportable to the downstream shell repo's Playwright suite. 142-VERIFICATION.md records per-gate pass/fail + evidence paths. STATE → ready-for-audit; PROJECT → Shipped: v0.29.0; REQUIREMENTS traceability → all 13 VER-XX rows Complete; ROADMAP → Phase 142 row Complete.`
       - `- v0.29.0 / Phase 142 methodology pattern: 3-plan Wave-1+Wave-2 structure successfully parallelized terminal verification. Plans 01 + 02 ran in parallel Wave 1 (strictly disjoint file ownership: 142-01 only touched .changeset/ + /tmp/; 142-02 only touched packages/nub/src/ test files + /tmp/tree-shake harness). Plan 03 ran in Wave 2 after both converged, authoring the documented Playwright fixtures + milestone-close docs (VERIFICATION.md, STATE flip, PROJECT insert, REQUIREMENTS traceability flip, ROADMAP Phase row flip). Pattern carries forward for future milestone verification phases.`
    7. Update `### Pending Todos` — mark Phase 135-141 orchestrator verify passes as complete (or leave if the orchestrator is responsible for that bookkeeping), mark Phase 142 plan-complete and verified. Remove the Phase 142 VER-03 tree-shake-harness-extension entry (it is now complete).
    8. Update `## Session Continuity` section: bump `Last session` + `Stopped at` + `Resume` hint (point to `/gsd:audit-milestone v0.29.0`)

    Surgical edits only; do not rewrite unaffected sections. Use the Edit tool for each discrete change.
  </action>
  <verify>
    <automated>grep -qE '^status: ready-for-audit$' /home/sandwich/Develop/napplet/.planning/STATE.md &amp;&amp; grep -q 'Phase 142 TERMINAL-COMPLETE' /home/sandwich/Develop/napplet/.planning/STATE.md &amp;&amp; grep -q 'all 13 VER-IDs' /home/sandwich/Develop/napplet/.planning/STATE.md &amp;&amp; grep -qE 'completed_phases: 8' /home/sandwich/Develop/napplet/.planning/STATE.md &amp;&amp; grep -q 'Milestone ready for audit' /home/sandwich/Develop/napplet/.planning/STATE.md</automated>
  </verify>
  <done>
    STATE.md frontmatter shows `status: ready-for-audit` and `completed_phases: 8`. Decisions section has a new Phase 142 TERMINAL-COMPLETE entry citing all 13 VER-IDs and the 3-plan methodology. Current Position shows Milestone ready for audit. Session Continuity hints next step = `/gsd:audit-milestone v0.29.0`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Move v0.29.0 from Current-Milestone block to Shipped section in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (current: "## Current Milestone: v0.29.0 NUB-CONNECT + Shell as CSP Authority" block at lines 3-21, then "## Shipped: v0.28.0 ..." at line 23)
    - .planning/milestones/v0.28.0-phases/134-verification-milestone-close/134-01-PLAN.md > Task 6 STEP C (v0.28.0 PROJECT.md flip precedent — exact prose structure)
    - .planning/phases/142-verification-milestone-close/142-VERIFICATION.md (Task 2 output — summary-level facts for the Shipped prose)
  </read_first>
  <action>
    Update `.planning/PROJECT.md`:

    1. DELETE the entire current "## Current Milestone: v0.29.0 NUB-CONNECT + Shell as CSP Authority" section (lines 3-21 approx — includes the Goal / Target features / Key context bullets).
    2. INSERT a new "## Shipped: v0.29.0 NUB-CONNECT + Shell as CSP Authority" section IMMEDIATELY ABOVE the existing "## Shipped: v0.28.0 Browser-Enforced Resource Isolation" section. Mirror the v0.28.0 Shipped section's one-paragraph structure.

    Concrete content (use the Edit tool with old_string = the Current-Milestone block; new_string = the Shipped block):

    ```markdown
    ## Shipped: v0.29.0 NUB-CONNECT + Shell as CSP Authority

    Shifted strict CSP emission from the vite-plugin (build time) to the host shell (runtime) for every napplet, and introduced two new NUBs: NUB-CLASS (shell-authoritative abstract security posture via `class.assigned` wire, `window.napplet.class: number | undefined`, sub-track of `NUB-CLASS-$N` documents) and NUB-CONNECT (user-gated direct network access declared via `["connect", "<origin>"]` manifest tags, no postMessage wire protocol — grants flow through the runtime CSP the shell serves in the HTTP response plus a `<meta name="napplet-connect-granted">` tag read synchronously at shim install time, `window.napplet.connect.{granted, origins}`). Shared `normalizeConnectOrigin()` validator (21 rule violations enumerated, 28/28 smoke tests pass) is single source of truth for build-side and shell-side origin validation; canonical `connect:origins` aggregateHash fold (lowercase → ASCII-sort → LF-join → UTF-8 → SHA-256 → lowercase hex) is byte-locked to a 3-origin conformance fixture with independently verified digest `cc7c1b1903fb23ecb909d2427e1dccd7d398a5c63dd65160edb0bb8b231aa742`. `@napplet/vite-plugin` strict-CSP production path removed (accept-but-warn `strictCsp` shim retained for one release cycle; hard-removal tracked as REMOVE-STRICTCSP for v0.30.0); `connect?: string[]` option added with manifest tag emission + aggregateHash fold + fail-loud inline-script diagnostic + dev-mode-only `napplet-connect-requires` meta; module-load self-check binds the plugin's fold to the spec conformance fixture at ESM-init. `@napplet/shim` + `@napplet/sdk` mount both NUB surfaces with graceful-degradation defaults (`connect` → `{granted: false, origins: []}`, `class` → `undefined`) and the central dispatcher routes `class.*` envelopes. 4 draft specs authored in `napplet/nubs` public repo (NUB-CONNECT, NUB-CLASS, NUB-CLASS-1, NUB-CLASS-2); NIP-5D amended to become NUB-neutral transport-only; `specs/SHELL-CONNECT-POLICY.md` + `specs/SHELL-CLASS-POLICY.md` shell-deployer checklists authored. Acceptance gates (13 VER-IDs total): `pnpm -r build` + `pnpm -r type-check` green across 14 packages; tree-shake harness extended with connect + class types-only consumer cases (zero forbidden symbols, within 500 B budget); 4 in-repo vitest suites (connect+class shim, aggregateHash conformance fixture, cross-NUB invariant table-driven across 7 SHELL-CLASS-POLICY scenarios); 3 documented Playwright fixtures exportable to downstream shell repo (approved-grant positive path, denied-grant negative path, residual-meta-CSP refusal contrast); cross-repo zero-grep clean across 4 drafts; changeset authored; downstream-shell tracking doc-check passed. Demo napplets delegated to downstream shell repo (Option B carried forward from v0.28.0). 8 phases, ~19 plans shipped 2026-04-21. See [archive](milestones/v0.29.0-ROADMAP.md).
    ```

    The archive link target may not exist yet — that's created by the `/gsd:complete-milestone` lifecycle step. Leaving the link as-is is correct; cleanup wires it up.

    Plan count ("~19 plans") may need adjustment based on actual Plan counts: 135 has 4, 136 has 1, 137 has 3, 138 has 3, 139 has 1, 140 has 2, 141 has 2, 142 has 3 = 19 total. Confirm with `ls .planning/phases/*/` walk before committing the exact number.

    Do NOT touch any other section of PROJECT.md — the v0.28.0, v0.27.0, etc. Shipped sections remain byte-identical (DOC-07 precedent: historical changelog preservation).
  </action>
  <verify>
    <automated>! grep -q '## Current Milestone: v0.29.0' /home/sandwich/Develop/napplet/.planning/PROJECT.md &amp;&amp; grep -q '## Shipped: v0.29.0 NUB-CONNECT' /home/sandwich/Develop/napplet/.planning/PROJECT.md &amp;&amp; grep -q 'cc7c1b1903fb23ecb909d2427e1dccd7d398a5c63dd65160edb0bb8b231aa742' /home/sandwich/Develop/napplet/.planning/PROJECT.md &amp;&amp; grep -q '## Shipped: v0.28.0 Browser-Enforced Resource Isolation' /home/sandwich/Develop/napplet/.planning/PROJECT.md</automated>
  </verify>
  <done>
    PROJECT.md no longer has `## Current Milestone: v0.29.0` header; new `## Shipped: v0.29.0 NUB-CONNECT + Shell as CSP Authority` section inserted immediately above `## Shipped: v0.28.0 Browser-Enforced Resource Isolation`. Prose includes the spec conformance fixture digest and the 13-gate acceptance summary.
  </done>
</task>

<task type="auto">
  <name>Task 5: Flip REQUIREMENTS.md traceability + ROADMAP.md Summary Progress row for Phase 142</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Traceability table lines 217-226 — 10 VER-XX rows currently Pending; but REQUIREMENTS.md lists 13 VER-XX rows total per the REQ spec at the top; confirm by counting)
    - .planning/ROADMAP.md (Summary Progress table ~line 238: Phase 142 row currently "0/? | Not started | -")
    - .planning/milestones/v0.28.0-phases/134-verification-milestone-close/134-01-PLAN.md > Task 6 STEP D (REQUIREMENTS traceability flip precedent)
    - .planning/phases/142-verification-milestone-close/142-VERIFICATION.md (from Task 2 — the single source of truth for per-VER-ID PASS claims)
  </read_first>
  <action>
    Two small document updates, both table-row flips.

    STEP A — `.planning/REQUIREMENTS.md` traceability table.

    The traceability table (starting at the `| Requirement | Phase | Status |` header near line 163) has VER-01..10 rows showing `Phase 142 | Pending`. Note: REQUIREMENTS.md at the top lists 13 VER-XX acceptance criteria (VER-01..13) but the traceability table as currently written only covers VER-01..10. Confirm the current state with:
    ```bash
    grep -cE '^\| VER-[0-9]{2} ' /home/sandwich/Develop/napplet/.planning/REQUIREMENTS.md
    ```

    If the count is 10, the table is missing rows for VER-11, VER-12, VER-13. Add them immediately after the VER-10 row:
    ```
    | VER-11 | Phase 142 | Complete |
    | VER-12 | Phase 142 | Complete |
    | VER-13 | Phase 142 | Complete |
    ```

    Flip VER-01 through VER-10 from `Pending` → `Complete`. All 13 rows should end the task showing `Phase 142 | Complete`.

    STEP B — `.planning/ROADMAP.md` Summary Progress table.

    The Summary Progress table (currently near line 238) has this row:
    ```
    | 142. Verification & Milestone Close | 0/? | Not started | - |
    ```

    Change to:
    ```
    | 142. Verification & Milestone Close | 3/3 | Complete    | 2026-04-21 |
    ```

    The phase now has 3 plans (142-01, 142-02, 142-03) all complete.

    Also update the Phase 142 section header in ROADMAP.md to mark it as complete — find:
    ```
    - [ ] **Phase 142: Verification & Milestone Close** - `pnpm -r build` + `type-check` green across 14 packages; ...
    ```
    Change `[ ]` → `[x]` on that line and append ` (completed 2026-04-21)` to the end.

    Also, the ROADMAP has per-phase Plans lists. For Phase 142, replace the `**Plans**: TBD` line with:
    ```
    **Plans:** 3 plans

    Plans:
    - [x] 142-01-PLAN.md — Terminal gates: build/type-check/zero-grep/changeset/doc-check (VER-01, VER-02, VER-08, VER-09, VER-10) ✅ 2026-04-21
    - [x] 142-02-PLAN.md — In-repo vitest tests: tree-shake harness + aggregateHash + class wire + graceful degradation + cross-NUB invariant (VER-03, VER-06, VER-11, VER-12, VER-13) ✅ 2026-04-21
    - [x] 142-03-PLAN.md — Documented Playwright fixtures (VER-04, VER-05, VER-07) + milestone-close docs ✅ 2026-04-21
    ```

    Finally, flip the top-level milestone checkbox in ROADMAP.md `## Milestones` section:
    ```
    - ⏳ **v0.29.0 NUB-CONNECT + Shell as CSP Authority** — Phases 135-142 (active)
    ```
    to:
    ```
    - ✅ **v0.29.0 NUB-CONNECT + Shell as CSP Authority** — Phases 135-142 (shipped 2026-04-21) — [Archive](milestones/v0.29.0-ROADMAP.md)
    ```

    (Note: The Archive link target will be created by `/gsd:complete-milestone` in the post-plan lifecycle — leaving the link as-is is correct.)

    Update the v0.29.0 section header line as well:
    ```
    ### v0.29.0 NUB-CONNECT + Shell as CSP Authority (Phases 135-142) — ACTIVE
    ```
    to:
    ```
    ### v0.29.0 NUB-CONNECT + Shell as CSP Authority (Phases 135-142) — SHIPPED 2026-04-21
    ```

    Surgical edits only. Do not rewrite unaffected sections. Use the Edit tool for each discrete change.
  </action>
  <verify>
    <automated>test "$(grep -cE '^\| VER-[0-9]{2} \| Phase 142 \| Complete' /home/sandwich/Develop/napplet/.planning/REQUIREMENTS.md)" -eq 13 &amp;&amp; ! grep -qE '^\| VER-[0-9]{2} \| Phase 142 \| Pending' /home/sandwich/Develop/napplet/.planning/REQUIREMENTS.md &amp;&amp; grep -q '| 142. Verification & Milestone Close | 3/3 | Complete' /home/sandwich/Develop/napplet/.planning/ROADMAP.md &amp;&amp; grep -qE '^- \[x\] \*\*Phase 142:' /home/sandwich/Develop/napplet/.planning/ROADMAP.md &amp;&amp; grep -q 'SHIPPED 2026-04-21' /home/sandwich/Develop/napplet/.planning/ROADMAP.md</automated>
  </verify>
  <done>
    REQUIREMENTS.md traceability table shows all 13 VER-XX rows as `Phase 142 | Complete`. ROADMAP.md Summary Progress table shows Phase 142 as `3/3 | Complete | 2026-04-21`; Phase 142 top-level checkbox flipped to `[x]`; v0.29.0 milestone checkbox flipped to ✅ with ship date; section header updated to SHIPPED; per-plan list populated with all 3 plans.
  </done>
</task>

</tasks>

<verification>
**Plan-level acceptance gate** (after all 5 tasks complete):

1. 3 documented Playwright fixtures exist at `packages/nub/src/connect/__fixtures__/ver-{04,05,07}-*.md` — each self-contained, ≥40 lines, exportable to downstream shell repo (Task 1 → VER-04, VER-05, VER-07)
2. `142-VERIFICATION.md` authored with all 13 VER-IDs marked PASS + per-gate evidence paths (Task 2)
3. STATE.md flipped to `ready-for-audit` with Phase 142 TERMINAL-COMPLETE entry (Task 3)
4. PROJECT.md "Current Milestone: v0.29.0" block deleted; "Shipped: v0.29.0 NUB-CONNECT" block inserted above v0.28.0 (Task 4)
5. REQUIREMENTS.md traceability: all 13 VER-XX rows = Phase 142 | Complete (Task 5)
6. ROADMAP.md Phase 142 row = 3/3 | Complete | 2026-04-21; v0.29.0 milestone checkbox ✅ (Task 5)

**One-shot verification command** (run from repo root after Task 5):
```bash
cd /home/sandwich/Develop/napplet
test -f packages/nub/src/connect/__fixtures__/ver-04-approved-grant.md \
  &amp;&amp; test -f packages/nub/src/connect/__fixtures__/ver-05-denied-grant.md \
  &amp;&amp; test -f packages/nub/src/connect/__fixtures__/ver-07-residual-meta-csp.md \
  &amp;&amp; grep -qE 'VER-13.*PASS' .planning/phases/142-verification-milestone-close/142-VERIFICATION.md \
  &amp;&amp; grep -qE '^status: ready-for-audit$' .planning/STATE.md \
  &amp;&amp; grep -q 'Shipped: v0.29.0 NUB-CONNECT' .planning/PROJECT.md \
  &amp;&amp; ! grep -q '## Current Milestone: v0.29.0' .planning/PROJECT.md \
  &amp;&amp; test "$(grep -cE '^\| VER-[0-9]{2} \| Phase 142 \| Complete' .planning/REQUIREMENTS.md)" -eq 13 \
  &amp;&amp; grep -q '| 142. Verification & Milestone Close | 3/3 | Complete' .planning/ROADMAP.md \
  &amp;&amp; echo "MILESTONE v0.29.0 READY FOR AUDIT"
```
</verification>

<success_criteria>
- VER-04, VER-05, VER-07: Three documented Playwright fixtures exist under `packages/nub/src/connect/__fixtures__/`; each self-contained and ≥40 lines; exportable to downstream shell repo
- 142-VERIFICATION.md records all 13 VER-IDs as PASS with evidence paths pointing to /tmp logs, in-repo test files, changeset, and fixture files
- STATE.md status = `ready-for-audit`; Phase 142 TERMINAL-COMPLETE entry in Decisions; completed_phases=8
- PROJECT.md v0.29.0 moved from Current Milestone block to Shipped section; v0.28.0 section remains byte-identical
- REQUIREMENTS.md traceability table: all 13 VER-XX rows flipped to Complete
- ROADMAP.md Summary Progress row for Phase 142 flipped to 3/3 | Complete; top-level milestone checkbox ✅; section header SHIPPED; per-plan list populated
- Branch `feat/strict-model` ready for merge to main (manual step outside this phase)
- Atomic per-task git commits (Phase 134 pattern); any single failure safely revertable without losing earlier passing gates
</success_criteria>

<output>
After completion, create `.planning/phases/142-verification-milestone-close/142-03-SUMMARY.md` recording:
- VER-04, VER-05, VER-07 results (documented fixture paths)
- 142-VERIFICATION.md confirmation (all 13 VER-IDs PASS)
- STATE.md, PROJECT.md, REQUIREMENTS.md, ROADMAP.md update confirmations
- Confirmation that milestone is ready for `/gsd:audit-milestone v0.29.0`
- Any deviations noted in a Deviations subsection (e.g., if a /tmp log from Plan 01/02 was missing and needed re-run)
- Next step: autonomous lifecycle's audit-milestone → complete-milestone → cleanup; manual `feat/strict-model` → `main` merge after audit clears
</output>
