---
phase: 17-shell-export-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shell/src/index.ts
  - packages/shell/src/enforce.ts
  - tests/e2e/shell-host.html
  - apps/demo/src/shell-host.ts
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
must_haves:
  truths:
    - "Shell index.ts does not export handleStateRequest, cleanupNappState, nappKeyRegistry, or aclStore"
    - "Shell index.ts re-exports createEnforceGate, resolveCapabilities, formatDenialReason from @napplet/runtime"
    - "packages/shell/src/enforce.ts no longer exists"
    - "tests/e2e/shell-host.html uses createShellBridge and relay.runtime.nappKeyRegistry"
    - "apps/demo/src/shell-host.ts does not import nappKeyRegistry or aclStore from @napplet/shell"
    - "pnpm build && pnpm type-check pass with no errors"
    - "All tests pass"
  artifacts:
    - path: "packages/shell/src/index.ts"
      provides: "Cleaned shell public API"
      contains: "export { createEnforceGate, resolveCapabilities, formatDenialReason } from '@napplet/runtime'"
    - path: "tests/e2e/shell-host.html"
      provides: "Updated e2e test host"
      contains: "createShellBridge"
  key_links:
    - from: "packages/shell/src/index.ts"
      to: "@napplet/runtime"
      via: "re-export of enforce functions"
      pattern: "from '@napplet/runtime'"
    - from: "apps/demo/src/shell-host.ts"
      to: "relay.runtime.nappKeyRegistry"
      via: "bridge.runtime accessor"
      pattern: "relay\\.runtime"
---

<objective>
Remove dead exports from @napplet/shell's public API left over from the pre-runtime refactor.

Purpose: After Phase 14 moved the protocol engine to @napplet/runtime, shell kept re-exporting
functions from stale local modules (enforce.ts duplicate, state-proxy.ts dead path, disconnected
singletons). This phase cleans up the public surface so integrators get correct, live API only.

Output:
- packages/shell/src/index.ts — cleaned export surface
- packages/shell/src/enforce.ts — deleted (duplicate of runtime's enforce.ts)
- tests/e2e/shell-host.html — updated consumers
- apps/demo/src/shell-host.ts — updated consumers
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-shell-export-cleanup/17-CONTEXT.md

<!-- Source of truth files — read before touching anything -->
@packages/shell/src/index.ts
@packages/shell/src/enforce.ts
@packages/runtime/src/index.ts
@apps/demo/src/shell-host.ts
@tests/e2e/shell-host.html
</context>

<interfaces>
<!-- Current shell/src/index.ts exports the executor must understand before editing -->

Lines to REMOVE from packages/shell/src/index.ts:
  Line 45: export { nappKeyRegistry } from './napp-key-registry.js';        (D-09: remove)
  Line 46: export type { PendingUpdate } from './napp-key-registry.js';      (D-09: remove — no external consumer)
  Line 47: export { aclStore } from './acl-store.js';                        (D-10: remove)
  Line 52: export { cleanupNappState } from './state-proxy.js';              (D-01/D-02: remove)
  Line 55: export { createEnforceGate, resolveCapabilities, formatDenialReason } from './enforce.js';  (replace with runtime source)
  Line 56: export type { CapabilityResolution, EnforceResult, EnforceConfig, IdentityResolver, AclChecker } from './enforce.js';  (replace with runtime source)
  Line 64: export { handleStateRequest } from './state-proxy.js';            (D-01: remove)
  Line 65: export { DEFAULT_STATE_QUOTA } from './acl-store.js';             (discretion: remove, no external consumer)

Lines to ADD to packages/shell/src/index.ts:
  export { createEnforceGate, resolveCapabilities, formatDenialReason } from '@napplet/runtime';       (D-05)
  export type { CapabilityResolution, EnforceResult, EnforceConfig, IdentityResolver, AclChecker } from '@napplet/runtime';  (D-06)

Lines to KEEP in packages/shell/src/index.ts (do not touch):
  Line 44: export { originRegistry } from './origin-registry.js';            (D-07: keep)
  Line 48: export { audioManager } from './audio-manager.js';                (D-08: keep)
  Line 49: export type { AudioSource } from './audio-manager.js';            (D-08: keep)
  Line 50: export { manifestCache } from './manifest-cache.js';              (D-11: keep)
  Line 51: export type { ManifestCacheEntry } from './manifest-cache.js';    (D-11: keep)

From packages/runtime/src/index.ts (these are what we re-export):
  export { createEnforceGate, resolveCapabilities, formatDenialReason } from './enforce.js';
  export type { CapabilityResolution, EnforceResult, EnforceConfig, IdentityResolver, AclChecker } from './enforce.js';

Consumer fix — apps/demo/src/shell-host.ts line 9-18:
  REMOVE: nappKeyRegistry, aclStore from the '@napplet/shell' import
  UPDATE: Replace nappKeyRegistry.getPubkey(wid) / nappKeyRegistry.getEntry(pubkey) with relay.runtime.nappKeyRegistry.getPubkey(wid) / relay.runtime.nappKeyRegistry.getEntry(pubkey) (lines ~309-311)
  UPDATE: Replace aclStore.grant / aclStore.revoke / aclStore.check / aclStore.block / aclStore.unblock with relay.runtime.aclState (lines ~370-388)
  NOTE: relay.runtime is available because bootShell() sets the module-level relay variable (line 244)

Consumer fix — tests/e2e/shell-host.html:
  REMOVE: nappKeyRegistry from import line 10 — import { createPseudoRelay, originRegistry, nappKeyRegistry }
  REPLACE: createPseudoRelay → createShellBridge (it was renamed in Phase 14; this file was never updated)
  UPDATE: nappKeyRegistry.isRegistered(windowId) → relay.runtime.nappKeyRegistry.isRegistered(windowId) where relay = createShellBridge(hooks)
  NOTE: shell-host.html needs a relay variable. The relay returned by createShellBridge must be stored and used.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Clean shell/src/index.ts export surface</name>
  <files>packages/shell/src/index.ts</files>
  <read_first>
    - packages/shell/src/index.ts (current export surface — read every line before editing)
    - packages/runtime/src/index.ts (verify enforce exports are present before re-exporting)
    - packages/shell/src/enforce.ts (understand what we are replacing — will be deleted in Task 2)
  </read_first>
  <action>
    Edit packages/shell/src/index.ts with three changes:

    CHANGE 1 — Remove state-proxy dead exports (CLEAN-01, per D-01/D-02):
    Remove these two lines (they are in the "Internal re-exports" section at the bottom):
      export { handleStateRequest } from './state-proxy.js';
      export { cleanupNappState } from './state-proxy.js';
    Also remove from the "Standalone utilities" section:
      export { cleanupNappState } from './state-proxy.js';
    (Note: cleanupNappState appears once on line 52 in standalone utilities section.)
    Remove DEFAULT_STATE_QUOTA line 65 as well (no external consumer, discretion per pre-1.0 clean break).

    CHANGE 2 — Re-point enforce exports to @napplet/runtime (CLEAN-02, per D-04/D-05/D-06):
    Replace the two lines that currently import from './enforce.js':
      export { createEnforceGate, resolveCapabilities, formatDenialReason } from './enforce.js';
      export type { CapabilityResolution, EnforceResult, EnforceConfig, IdentityResolver, AclChecker } from './enforce.js';
    With:
      export { createEnforceGate, resolveCapabilities, formatDenialReason } from '@napplet/runtime';
      export type { CapabilityResolution, EnforceResult, EnforceConfig, IdentityResolver, AclChecker } from '@napplet/runtime';
    Update the comment above from "re-exported from shell's local enforce.ts" to "re-exported from @napplet/runtime for backwards compatibility".

    CHANGE 3 — Remove disconnected singleton exports (CLEAN-03, per D-09/D-10):
    Remove from "Standalone utilities" section:
      export { nappKeyRegistry } from './napp-key-registry.js';
      export type { PendingUpdate } from './napp-key-registry.js';
      export { aclStore } from './acl-store.js';

    KEEP UNTOUCHED: originRegistry, audioManager + AudioSource, manifestCache + ManifestCacheEntry.

    After editing, the "Standalone utilities" section should only contain:
      export { originRegistry } from './origin-registry.js';
      export { audioManager } from './audio-manager.js';
      export type { AudioSource } from './audio-manager.js';
      export { manifestCache } from './manifest-cache.js';
      export type { ManifestCacheEntry } from './manifest-cache.js';
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -n "handleStateRequest\|cleanupNappState\|nappKeyRegistry\|aclStore\|DEFAULT_STATE_QUOTA\|enforce\.js" packages/shell/src/index.ts; echo "--- Should show no matches above ---"; grep -n "createEnforceGate" packages/shell/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/shell/src/index.ts contains zero lines importing from './enforce.js'
    - packages/shell/src/index.ts contains zero lines exporting handleStateRequest, cleanupNappState, nappKeyRegistry, aclStore, DEFAULT_STATE_QUOTA
    - packages/shell/src/index.ts exports createEnforceGate from '@napplet/runtime' (not './enforce.js')
    - packages/shell/src/index.ts exports type CapabilityResolution, EnforceResult, EnforceConfig, IdentityResolver, AclChecker from '@napplet/runtime'
    - originRegistry, audioManager, manifestCache exports are still present and unchanged
  </acceptance_criteria>
  <done>index.ts has no references to enforce.js, no dead exports, and re-exports enforce functions from @napplet/runtime</done>
</task>

<task type="auto">
  <name>Task 2: Delete enforce.ts duplicate and update consumers</name>
  <files>
    packages/shell/src/enforce.ts
    tests/e2e/shell-host.html
    apps/demo/src/shell-host.ts
  </files>
  <read_first>
    - packages/shell/src/enforce.ts (confirm no other shell files import from it before deleting)
    - tests/e2e/shell-host.html (read fully — needs createPseudoRelay rename + nappKeyRegistry fix)
    - apps/demo/src/shell-host.ts (read fully — imports nappKeyRegistry, aclStore to be removed)
  </read_first>
  <action>
    STEP 1 — Verify enforce.ts is safe to delete:
    Before deleting, confirm no shell source file other than index.ts imports from './enforce.js' or './enforce.ts'.
    Run: grep -rn "from.*enforce" packages/shell/src/ (should only show index.ts, which we already fixed in Task 1)
    If any other file imports from enforce.ts, fix that import to use '@napplet/runtime' instead.
    Then delete the file: rm packages/shell/src/enforce.ts

    STEP 2 — Fix tests/e2e/shell-host.html:
    This file has two problems:
    a) It imports `createPseudoRelay` (old pre-rename function) — replace with `createShellBridge`
    b) It imports `nappKeyRegistry` directly — remove from import, use relay.runtime.nappKeyRegistry instead

    The script currently does:
      import { createPseudoRelay, originRegistry, nappKeyRegistry } from '@napplet/shell';
      const relay = createPseudoRelay(hooks);
      get authCompleted() { return nappKeyRegistry.isRegistered(windowId); }

    Change to:
      import { createShellBridge, originRegistry } from '@napplet/shell';
      const relay = createShellBridge(hooks);
      get authCompleted() { return relay.runtime.nappKeyRegistry.isRegistered(windowId); }

    STEP 3 — Fix apps/demo/src/shell-host.ts:
    Remove `nappKeyRegistry` and `aclStore` from the '@napplet/shell' import statement (lines 9-18).
    The import currently reads:
      import {
        createShellBridge,
        originRegistry,
        aclStore,
        nappKeyRegistry,
        type ShellBridge,
        ...
      } from '@napplet/shell';

    Remove `aclStore` and `nappKeyRegistry` from that import.

    Then update the two usages in the file:

    Usage 1 (lines ~309-311 in bootShell function's tap.onMessage callback):
      nappKeyRegistry.getPubkey(wid) → relay.runtime.nappKeyRegistry.getPubkey(wid)
      nappKeyRegistry.getEntry(pubkey) → relay.runtime.nappKeyRegistry.getEntry(pubkey)
    Note: relay is the module-level export declared at line 244: export let relay: ShellBridge;
    It is set in bootShell() before tap.onMessage is called, so relay.runtime is available.

    Usage 2 (toggleCapability function, lines ~364-378):
      aclStore.grant(...) → relay.runtime.aclState.grant(...)
      aclStore.revoke(...) → relay.runtime.aclState.revoke(...)
      aclStore.check(...) → relay.runtime.aclState.check(...)

    Usage 3 (toggleBlock function, lines ~381-390):
      aclStore.block(...) → relay.runtime.aclState.block(...)
      aclStore.unblock(...) → relay.runtime.aclState.unblock(...)

    Note: relay.runtime.aclState is the AclStateContainer. Its methods (grant, revoke, check, block, unblock)
    match the shell's old aclStore methods — same signature.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && ls packages/shell/src/enforce.ts 2>&1; grep -n "createPseudoRelay\|nappKeyRegistry\|aclStore" tests/e2e/shell-host.html apps/demo/src/shell-host.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/shell/src/enforce.ts does not exist (ls returns error)
    - tests/e2e/shell-host.html imports createShellBridge (not createPseudoRelay) from '@napplet/shell'
    - tests/e2e/shell-host.html has no import of nappKeyRegistry from '@napplet/shell'
    - tests/e2e/shell-host.html uses relay.runtime.nappKeyRegistry.isRegistered(windowId)
    - apps/demo/src/shell-host.ts does not import nappKeyRegistry or aclStore from '@napplet/shell'
    - apps/demo/src/shell-host.ts uses relay.runtime.nappKeyRegistry for registry calls
    - apps/demo/src/shell-host.ts uses relay.runtime.aclState for ACL calls
    - No other shell source file imports from './enforce.js' or './enforce.ts'
  </acceptance_criteria>
  <done>enforce.ts deleted, all three consumers updated to use runtime-backed accessors</done>
</task>

<task type="auto">
  <name>Task 3: Verify build, type-check, and test suite</name>
  <files></files>
  <read_first>
    - packages/shell/src/index.ts (confirm final state matches plan)
    - packages/runtime/src/index.ts (confirm all enforce exports are present for re-export)
  </read_first>
  <action>
    Run the full verification sequence in order:

    1. Build all packages:
       cd /home/sandwich/Develop/napplet && pnpm build

    2. Type-check all packages:
       cd /home/sandwich/Develop/napplet && pnpm type-check

    3. Run the full test suite:
       cd /home/sandwich/Develop/napplet && pnpm test

    If pnpm build fails:
    - Read the error output — likely a remaining import of a removed export
    - Fix the import in the failing file
    - Re-run build

    If pnpm type-check fails:
    - Read the TS errors — likely a type import removed from shell that a consumer needs
    - If the type is a pure re-export from shell that no external code actually uses, remove it
    - If it is needed, add a type re-export from the appropriate source (@napplet/runtime or @napplet/core)

    If tests fail:
    - Read the failing test output
    - Likely cause: e2e test harness imports something removed from shell
    - Fix the import in the failing test or harness file
    - Re-run tests
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && pnpm build 2>&1 | tail -5 && pnpm type-check 2>&1 | tail -5 && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - pnpm build exits with code 0, no errors in output
    - pnpm type-check exits with code 0, no TypeScript errors
    - pnpm test exits with code 0, all tests pass (expect ~180 tests green based on Phase 16 baseline)
    - No test failure message about missing exports from @napplet/shell
  </acceptance_criteria>
  <done>All builds green, all tests pass, cleanup is complete</done>
</task>

</tasks>

<verification>
The phase is complete when:
1. grep -n "enforce\.js\|handleStateRequest\|cleanupNappState\|nappKeyRegistry\|aclStore\|DEFAULT_STATE_QUOTA" packages/shell/src/index.ts returns no matches
2. ls packages/shell/src/enforce.ts returns "No such file or directory"
3. grep "from '@napplet/runtime'" packages/shell/src/index.ts shows enforce re-exports
4. grep "createPseudoRelay" tests/e2e/shell-host.html returns no matches
5. pnpm build && pnpm type-check && pnpm test all exit 0
</verification>

<success_criteria>
- CLEAN-01: Shell index.ts has no handleStateRequest or cleanupNappState exports
- CLEAN-02: Shell index.ts exports enforce functions via '@napplet/runtime', local enforce.ts is deleted
- CLEAN-03: Shell index.ts has no nappKeyRegistry or aclStore singleton exports
- All 180 tests pass with no new failures
- pnpm build and pnpm type-check pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/17-shell-export-cleanup/17-01-SUMMARY.md`
</output>
