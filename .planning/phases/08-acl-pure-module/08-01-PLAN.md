---
phase: 08-acl-pure-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/acl/package.json
  - packages/acl/tsconfig.json
  - packages/acl/tsup.config.ts
  - packages/acl/src/types.ts
autonomous: true
requirements: [ACL-03, ACL-04]
user_setup: []
must_haves:
  truths:
    - "@napplet/acl package exists at packages/acl/ with zero dependencies"
    - "tsconfig.json has lib: ['ES2022'] with no DOM or Node types"
    - "AclState type uses Readonly<> at all levels to enforce immutability"
    - "All 10 capability bit constants are exported with correct bit positions"
    - "Identity interface has pubkey, dTag, and hash fields"
  artifacts:
    - path: packages/acl/package.json
      provides: "Zero-dependency ESM package definition"
      contains: '"dependencies": {}'
    - path: packages/acl/tsconfig.json
      provides: "TypeScript config without DOM/Node types"
      contains: '"lib": ["ES2022"]'
    - path: packages/acl/tsup.config.ts
      provides: "ESM-only build config with zero externals"
      contains: "format: ['esm']"
    - path: packages/acl/src/types.ts
      provides: "AclState, Identity types and CAP_* bit constants"
      contains: "CAP_RELAY_READ"
  key_links: []
---

<objective>
Create the @napplet/acl package scaffold and define all types, constants, and interfaces.

Purpose: Establish the physical package boundary that enforces zero dependencies (ACL-04) and define the immutable state model (ACL-03) with bitfield capability constants.
Output: A new packages/acl/ directory with package.json (zero deps), tsconfig (no DOM), tsup config, and src/types.ts with all type definitions and constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-acl-pure-module/08-CONTEXT.md
@.planning/phases/08-acl-pure-module/08-RESEARCH.md

@packages/shell/package.json
@packages/shell/tsconfig.json
@packages/shell/tsup.config.ts
@packages/shell/src/types.ts
@packages/shell/src/acl-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create packages/acl/package.json</name>
  <files>packages/acl/package.json</files>
  <read_first>packages/shell/package.json</read_first>
  <action>
  Create `packages/acl/package.json` with the following exact content:

  ```json
  {
    "name": "@napplet/acl",
    "version": "0.1.0",
    "description": "Pure, WASM-ready ACL module for the napplet protocol — zero dependencies, zero side effects",
    "type": "module",
    "main": "./dist/index.js",
    "module": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
      ".": {
        "types": "./dist/index.d.ts",
        "import": "./dist/index.js"
      }
    },
    "files": ["dist"],
    "sideEffects": false,
    "publishConfig": {
      "access": "public"
    },
    "dependencies": {},
    "devDependencies": {
      "tsup": "^8.5.0",
      "typescript": "^5.9.3"
    },
    "scripts": {
      "build": "tsup",
      "type-check": "tsc --noEmit",
      "test": "vitest run"
    },
    "license": "MIT",
    "repository": {
      "type": "git",
      "url": "git+https://github.com/sandwichfarm/napplet.git",
      "directory": "packages/acl"
    },
    "keywords": ["nostr", "napplet", "acl", "capability", "wasm"]
  }
  ```

  Key constraints:
  - `"dependencies": {}` — zero runtime dependencies (ACL-04)
  - No `"peerDependencies"` — no nostr-tools, no Node APIs
  - `"sideEffects": false` — enables tree-shaking
  - `"type": "module"` — ESM only
  </action>
  <verify>cat packages/acl/package.json | grep -c '"dependencies": {}'</verify>
  <acceptance_criteria>
    - packages/acl/package.json exists
    - `"name"` is `"@napplet/acl"`
    - `"dependencies"` is `{}`  (empty object, zero dependencies)
    - No `"peerDependencies"` key exists OR it is `{}`
    - `"type"` is `"module"`
    - `"sideEffects"` is `false`
  </acceptance_criteria>
  <done>@napplet/acl package.json created with zero dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create packages/acl/tsconfig.json</name>
  <files>packages/acl/tsconfig.json</files>
  <read_first>packages/shell/tsconfig.json, tsconfig.json</read_first>
  <action>
  Create `packages/acl/tsconfig.json` with the following exact content:

  ```json
  {
    "extends": "../../tsconfig.json",
    "compilerOptions": {
      "outDir": "dist",
      "rootDir": "src",
      "lib": ["ES2022"]
    },
    "include": ["src"]
  }
  ```

  Critical difference from shell's tsconfig: `lib` is `["ES2022"]` only. No `"DOM"`, no `"DOM.Iterable"`. This means:
  - Any use of `localStorage`, `window`, `document`, `setTimeout`, `fetch` etc. will cause a TypeScript compilation error
  - Any use of `NodeJS` types (fs, path, process) will cause a TypeScript compilation error
  - Only standard ECMAScript APIs (Map, Set, Array, Promise, etc.) are available

  This is the primary compile-time enforcement of the zero-dependency constraint.
  </action>
  <verify>cat packages/acl/tsconfig.json | grep '"ES2022"'</verify>
  <acceptance_criteria>
    - packages/acl/tsconfig.json exists
    - `"lib"` contains only `["ES2022"]`
    - `"lib"` does NOT contain `"DOM"` or `"DOM.Iterable"`
    - `"extends"` is `"../../tsconfig.json"`
    - `"rootDir"` is `"src"`
  </acceptance_criteria>
  <done>tsconfig.json created with ES2022-only lib (no DOM/Node types)</done>
</task>

<task type="auto">
  <name>Task 3: Create packages/acl/tsup.config.ts</name>
  <files>packages/acl/tsup.config.ts</files>
  <read_first>packages/shell/tsup.config.ts</read_first>
  <action>
  Create `packages/acl/tsup.config.ts` with the following exact content:

  ```typescript
  import { defineConfig } from 'tsup';

  export default defineConfig({
    entry: ['src/index.ts'],
    format: ['esm'],
    dts: true,
    sourcemap: true,
    clean: true,
  });
  ```

  Note: No `external` array needed because there are no dependencies to externalize. The shell's tsup config has `external: ['nostr-tools']` — the ACL module needs nothing.
  </action>
  <verify>cat packages/acl/tsup.config.ts | grep "format: \['esm'\]"</verify>
  <acceptance_criteria>
    - packages/acl/tsup.config.ts exists
    - `format` is `['esm']` (ESM only)
    - `dts` is `true` (generates type declarations)
    - No `external` array (no dependencies)
    - `entry` is `['src/index.ts']`
  </acceptance_criteria>
  <done>tsup.config.ts created with ESM-only output and zero externals</done>
</task>

<task type="auto">
  <name>Task 4: Create packages/acl/src/types.ts with all types and constants</name>
  <files>packages/acl/src/types.ts</files>
  <read_first>packages/shell/src/types.ts, packages/shell/src/acl-store.ts</read_first>
  <action>
  Create `packages/acl/src/types.ts` with the following exact content:

  ```typescript
  /**
   * @napplet/acl — Type definitions and capability bit constants.
   *
   * All types use Readonly<> to enforce immutability at the type level.
   * Capability constants are bitfield values for fast check/grant/revoke.
   */

  // ─── Capability Bit Constants ─────────────────────────────────────────────────

  /** relay:read — subscribe to relay events */
  export const CAP_RELAY_READ    = 1 << 0;   // 1
  /** relay:write — publish events to relays */
  export const CAP_RELAY_WRITE   = 1 << 1;   // 2
  /** cache:read — read from local cache */
  export const CAP_CACHE_READ    = 1 << 2;   // 4
  /** cache:write — write to local cache */
  export const CAP_CACHE_WRITE   = 1 << 3;   // 8
  /** hotkey:forward — forward keyboard shortcuts to shell */
  export const CAP_HOTKEY_FORWARD = 1 << 4;  // 16
  /** sign:event — request event signing */
  export const CAP_SIGN_EVENT    = 1 << 5;   // 32
  /** sign:nip04 — request NIP-04 encrypt/decrypt */
  export const CAP_SIGN_NIP04    = 1 << 6;   // 64
  /** sign:nip44 — request NIP-44 encrypt/decrypt */
  export const CAP_SIGN_NIP44    = 1 << 7;   // 128
  /** state:read — read napplet-scoped state */
  export const CAP_STATE_READ    = 1 << 8;   // 256
  /** state:write — write napplet-scoped state */
  export const CAP_STATE_WRITE   = 1 << 9;   // 512

  /** All capabilities granted (bits 0-9 set) */
  export const CAP_ALL = (1 << 10) - 1;      // 1023

  /** No capabilities granted */
  export const CAP_NONE = 0;

  // ─── Identity ─────────────────────────────────────────────────────────────────

  /**
   * Napplet identity — composite key for ACL lookups.
   *
   * @param pubkey - Ephemeral session public key (hex)
   * @param dTag - Derived tag (deterministic from pubkey + napp type)
   * @param hash - Aggregate hash of napplet build artifacts
   */
  export interface Identity {
    readonly pubkey: string;
    readonly dTag: string;
    readonly hash: string;
  }

  // ─── ACL Entry ────────────────────────────────────────────────────────────────

  /**
   * A single ACL entry for one napplet identity.
   *
   * @param caps - Bitfield of granted capabilities (use CAP_* constants)
   * @param blocked - Orthogonal block flag; when true, all checks fail regardless of caps
   * @param quota - State storage quota in bytes
   */
  export interface AclEntry {
    readonly caps: number;
    readonly blocked: boolean;
    readonly quota: number;
  }

  // ─── ACL State ────────────────────────────────────────────────────────────────

  /**
   * Complete ACL state — immutable data structure.
   *
   * All mutations return a new AclState; the original is never modified.
   *
   * @param defaultPolicy - 'permissive' grants all caps to unknown identities;
   *                        'restrictive' denies all caps to unknown identities
   * @param entries - Map from composite key ('pubkey:dTag:hash') to AclEntry
   */
  export interface AclState {
    readonly defaultPolicy: 'permissive' | 'restrictive';
    readonly entries: Readonly<Record<string, AclEntry>>;
  }

  /** Default state storage quota in bytes (512 KB) */
  export const DEFAULT_QUOTA = 512 * 1024;
  ```

  This file defines:
  - 10 capability bit constants (CAP_RELAY_READ through CAP_STATE_WRITE) matching all capabilities from shell's types.ts
  - CAP_ALL (all bits set) and CAP_NONE (zero) convenience constants
  - Identity interface with readonly fields
  - AclEntry interface with readonly fields (caps bitfield, blocked flag, quota)
  - AclState interface with readonly defaultPolicy and readonly entries Record
  - DEFAULT_QUOTA constant (512 KB, matching shell's DEFAULT_STATE_QUOTA)
  </action>
  <verify>grep -c "CAP_" packages/acl/src/types.ts</verify>
  <acceptance_criteria>
    - packages/acl/src/types.ts exists
    - Contains exactly 10 CAP_* constants: CAP_RELAY_READ, CAP_RELAY_WRITE, CAP_CACHE_READ, CAP_CACHE_WRITE, CAP_HOTKEY_FORWARD, CAP_SIGN_EVENT, CAP_SIGN_NIP04, CAP_SIGN_NIP44, CAP_STATE_READ, CAP_STATE_WRITE
    - Contains CAP_ALL = (1 << 10) - 1 and CAP_NONE = 0
    - AclState interface has `readonly defaultPolicy` and `readonly entries`
    - AclEntry interface has `readonly caps`, `readonly blocked`, `readonly quota`
    - Identity interface has `readonly pubkey`, `readonly dTag`, `readonly hash`
    - DEFAULT_QUOTA = 512 * 1024
    - No imports from any external package
  </acceptance_criteria>
  <done>types.ts created with all types, interfaces, and 10 capability bit constants</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `packages/acl/package.json` exists with zero dependencies
- [ ] `packages/acl/tsconfig.json` exists with `lib: ["ES2022"]` only
- [ ] `packages/acl/tsup.config.ts` exists with ESM format
- [ ] `packages/acl/src/types.ts` exists with all types and constants
- [ ] `grep -r "import" packages/acl/src/types.ts` returns zero external imports
- [ ] `cat packages/acl/package.json | grep -c '"dependencies": {}'` returns 1
</verification>

<success_criteria>
- @napplet/acl package scaffold exists with zero dependencies
- tsconfig enforces no DOM/Node types (ES2022 only)
- All 10 capability bit constants are defined with correct values
- AclState type enforces immutability via Readonly<>
- Identity type matches the composite key model from CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/08-acl-pure-module/08-01-SUMMARY.md`
</output>
