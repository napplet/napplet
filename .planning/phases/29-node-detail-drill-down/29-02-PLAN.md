---
phase: 29-node-detail-drill-down
plan: 02
title: "Add a right-side inspector pane and selected-node interaction model"
wave: 2
depends_on:
  - 01
files_modified:
  - apps/demo/index.html
  - apps/demo/src/main.ts
  - apps/demo/src/flow-animator.ts
  - apps/demo/src/node-details.ts
  - apps/demo/src/node-inspector.ts
  - tests/unit/demo-node-inspector-render.test.ts
autonomous: true
requirements:
  - NODE-01
  - NODE-02
must_haves:
  truths:
    - "Every current node can be selected to open the inspector; drill-down support is not limited to a hand-picked subset"
    - "The inspector is rendered as a right-side pane in the upper workspace and does not cover the bottom debugger"
    - "Selected-node state is managed centrally, but inspector rendering remains modular"
    - "Collapsed nodes and the expanded inspector consume the same node-detail source of truth"
    - "tests/unit/demo-node-inspector-render.test.ts locks layout and selection invariants"
    - "`pnpm --filter @napplet/demo build` stays green after the inspector UI changes"
  artifacts:
    - path: "apps/demo/src/node-inspector.ts"
      provides: "Inspector rendering and selected-node display logic"
      contains: "selectedNodeId"
    - path: "apps/demo/index.html"
      provides: "Upper-workspace layout with right-side inspector region"
      contains: "node-inspector"
    - path: "tests/unit/demo-node-inspector-render.test.ts"
      provides: "Inspector render/layout regression coverage"
      contains: "debugger-section"
  key_links:
    - from: "apps/demo/src/main.ts"
      to: "apps/demo/src/node-details.ts"
      via: "Selected-node detail lookup"
      pattern: "buildNodeDetails"
    - from: "apps/demo/src/node-inspector.ts"
      to: "apps/demo/src/debugger.ts"
      via: "Complementary observability boundary"
      pattern: "recent activity|debugger"
---

# Plan 29-02: Add a Right-Side Inspector Pane and Selected-Node Interaction Model

<objective>
Deliver NODE-02 by adding a consistent selected-node interaction model and a right-side inspector pane that preserves the bottom debugger. This plan turns node details from passive summaries into an actual drill-down workflow.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-node-detail-drill-down/29-CONTEXT.md
@.planning/phases/29-node-detail-drill-down/29-RESEARCH.md
@.planning/phases/29-node-detail-drill-down/29-01-PLAN.md

@apps/demo/index.html
@apps/demo/src/main.ts
@apps/demo/src/node-details.ts
@apps/demo/src/debugger.ts
@apps/demo/src/flow-animator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce selected-node state and universal drill-down affordances</name>
  <files>apps/demo/src/main.ts, apps/demo/src/node-details.ts, apps/demo/index.html</files>
  <read_first>
    - apps/demo/src/main.ts
    - apps/demo/index.html
    - apps/demo/src/node-details.ts
    - .planning/phases/29-node-detail-drill-down/29-CONTEXT.md
  </read_first>
  <action>
    Add a central selected-node state to the demo UI and wire every current node so it can open the inspector consistently.

    Requirements for this task:
    - selection is keyed by stable topology node id
    - every node in the topology can trigger selection
    - selection affordances are consistent across napplet, shell, ACL, runtime, and service nodes
    - there is a clear close/reset path back to "no node selected"

    Do not introduce multi-select, pinning, or compare mode in this phase.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -q "selectedNodeId" apps/demo/src/main.ts && grep -q "drillDownSupported" apps/demo/src/node-details.ts && grep -q "data-node-id" apps/demo/index.html</automated>
  </verify>
  <acceptance_criteria>
    - apps/demo/src/main.ts stores selected-node state keyed by topology node id
    - apps/demo/index.html or equivalent rendered markup exposes a stable per-node selection hook
    - All node roles participate in selection, not just napplets
    - No pinning or multi-select behavior is added
  </acceptance_criteria>
  <done>The demo can select any node for drill-down using one consistent interaction model</done>
</task>

<task type="auto">
  <name>Task 2: Render the inspector as a right-side pane without obscuring the debugger</name>
  <files>apps/demo/index.html, apps/demo/src/node-inspector.ts, apps/demo/src/main.ts</files>
  <read_first>
    - apps/demo/index.html
    - apps/demo/src/main.ts
    - apps/demo/src/debugger.ts
    - .planning/PROJECT.md
    - .planning/phases/29-node-detail-drill-down/29-CONTEXT.md
  </read_first>
  <action>
    Add a new `apps/demo/src/node-inspector.ts` module and the required layout hooks in `apps/demo/index.html` so the upper workspace can render a right-side inspector pane for the selected node.

    The layout must satisfy these exact constraints:
    - the inspector lives on the right side
    - the bottom debugger remains visible and usable
    - opening the inspector resizes the top workspace rather than covering the debugger
    - the inspector can show current state sections for the selected node

    Use the node-detail model as the content source. Keep the inspector modular so later phases can extend specific node sections without rewriting `main.ts`.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && test -f apps/demo/src/node-inspector.ts && grep -q "node-inspector" apps/demo/index.html && grep -q "debugger-section" apps/demo/index.html && pnpm --filter @napplet/demo build</automated>
  </verify>
  <acceptance_criteria>
    - apps/demo/src/node-inspector.ts exists
    - apps/demo/index.html contains a dedicated inspector region
    - The debugger section remains present and outside the inspector pane
    - The inspector displays current-state sections for the selected node
  </acceptance_criteria>
  <done>The demo gains a right-side inspector pane that coexists with the bottom debugger</done>
</task>

<task type="auto">
  <name>Task 3: Add render-level regression coverage for inspector layout and selection</name>
  <files>tests/unit/demo-node-inspector-render.test.ts</files>
  <read_first>
    - apps/demo/index.html
    - apps/demo/src/main.ts
    - apps/demo/src/node-inspector.ts
    - tests/unit/demo-topology-render.test.ts
  </read_first>
  <action>
    Create `tests/unit/demo-node-inspector-render.test.ts` with render-oriented coverage for the new inspector model.

    Assert that:
    - the upper workspace contains a dedicated right-side inspector region
    - the debugger section remains a separate bottom region
    - selection hooks exist for all node roles
    - the render path can represent both no-selection and selected-node states

    This test should protect layout and interaction invariants rather than deep content formatting.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && test -f tests/unit/demo-node-inspector-render.test.ts && grep -q "node-inspector" tests/unit/demo-node-inspector-render.test.ts && grep -q "debugger-section" tests/unit/demo-node-inspector-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/demo-node-inspector-render.test.ts exists
    - The test file asserts the presence of the inspector region and debugger region
    - The test file covers selected and unselected states
    - The test fails if the debugger is folded into the inspector layout
  </acceptance_criteria>
  <done>Inspector layout and selection behavior are locked by render-level coverage</done>
</task>

</tasks>

<verify>
  <summary>
    Build the demo and run the inspector render tests. Confirm every node can open a right-side inspector while the bottom debugger remains visible and functional.
  </summary>
  <automated>
    cd /home/sandwich/Develop/napplet && pnpm vitest run tests/unit/demo-node-inspector-render.test.ts
    cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/demo build
  </automated>
  <manual>
    Open the demo, select shell/ACL/runtime/napplet/service nodes in turn, and confirm the inspector opens on the right without covering the debugger.
  </manual>
</verify>
