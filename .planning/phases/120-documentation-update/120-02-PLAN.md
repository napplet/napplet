---
phase: 120-documentation-update
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - packages/core/README.md
  - packages/shim/README.md
  - packages/sdk/README.md
autonomous: true
requirements:
  - DOC-02

must_haves:
  truths:
    - "Root README no longer references any @napplet/nub-<domain> package name"
    - "Root README no longer references the DEFUNCT @napplet/nub-signer (removed in v0.24.0)"
    - "Root README dep graph shows the 5-package shape: core ← nub ← shim + sdk, with vite-plugin as a build-time leaf"
    - "packages/core/README.md line 353 area references @napplet/nub domain modules, not per-nub packages"
    - "packages/shim/README.md dep-comparison table row shows single @napplet/nub entry"
    - "packages/sdk/README.md peerDep reference uses @napplet/nub/config subpath form"
    - "packages/sdk/README.md type-to-package mapping table uses @napplet/nub/<domain> for all 9 rows"
  artifacts:
    - path: README.md
      provides: "Updated root landing README with single @napplet/nub row + redrawn dep graph + no defunct signer refs"
      contains: "@napplet/nub"
    - path: packages/core/README.md
      provides: "Updated NUB packages integration note"
    - path: packages/shim/README.md
      provides: "Updated Shim vs SDK dependency row"
    - path: packages/sdk/README.md
      provides: "Updated peerDep reference + 9-row type-to-package mapping table"
  key_links:
    - from: "README.md"
      to: "packages/nub/README.md"
      via: "markdown link"
      pattern: "\\[@napplet/nub\\]\\(packages/nub\\)"
    - from: "packages/sdk/README.md line ~296"
      to: "@napplet/nub/<domain> barrel subpaths"
      via: "table column replacement"
      pattern: "@napplet/nub/(relay|identity|storage|ifc|keys|media|notify|config)"
---

<objective>
Update the 4 existing READMEs that currently reference the deprecated `@napplet/nub-<domain>` package names — replace them with the new `@napplet/nub/<domain>` subpath pattern. Additionally, clean up the root README's DEFUNCT `@napplet/nub-signer` references (killed in v0.24.0) and redraw the dep graph to reflect the post-consolidation 5-package topology.

Purpose: Root README is the main landing page on GitHub; stale references there mislead new readers. The three package READMEs are the authoritative per-package docs and each one currently shows old paths in specific load-bearing sections (integration note, dep comparison, type-to-package mapping).

Output: 4 edited markdown files with zero remaining `@napplet/nub-` matches in any of them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-documentation-update/120-CONTEXT.md

# Files being modified
@README.md
@packages/core/README.md
@packages/shim/README.md
@packages/sdk/README.md

# Authoritative subpath list (34 entries)
@packages/nub/package.json

<interfaces>
<!-- Authoritative mapping from deprecated names to canonical subpaths. -->
<!-- Executor uses these for every replacement. -->

Deprecated package → replacement subpath (for prose/table references):
  @napplet/nub-relay     → @napplet/nub/relay
  @napplet/nub-storage   → @napplet/nub/storage
  @napplet/nub-ifc       → @napplet/nub/ifc
  @napplet/nub-keys      → @napplet/nub/keys
  @napplet/nub-theme     → @napplet/nub/theme
  @napplet/nub-media     → @napplet/nub/media
  @napplet/nub-notify    → @napplet/nub/notify
  @napplet/nub-identity  → @napplet/nub/identity
  @napplet/nub-config    → @napplet/nub/config

Defunct (REMOVE entirely — package deleted in v0.24.0):
  @napplet/nub-signer    → (delete all references)

Post-consolidation dep graph shape (for root README):
  5 packages: core, nub, shim, sdk, vite-plugin
  Runtime chain: @napplet/core ← @napplet/nub ← @napplet/shim + @napplet/sdk
  Build-time leaf: @napplet/vite-plugin (depends on nostr-tools; stands alone)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update root README.md — table + dep graph + remove defunct signer refs</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — 89 lines; the 13 `@napplet/nub-` matches span lines 14-36)
    - packages/nub/package.json (for accurate domain list)
  </read_first>
  <action>
    Make 3 coordinated edits to `/home/sandwich/Develop/napplet/README.md`:

    **Edit 1 — Package table (lines 14-18):** Delete the 5 per-nub table rows entirely (lines 14-18 covering `@napplet/nub-relay`, `@napplet/nub-signer`, `@napplet/nub-storage`, `@napplet/nub-ifc`, `@napplet/nub-theme`). Replace them with a single new row placed between the `@napplet/sdk` row (line 13) and the `@napplet/vite-plugin` row (line 19):

    ```
    | [@napplet/nub](packages/nub) | `@napplet/nub` | Consolidated NUB package. 9 domain subpaths (relay, storage, ifc, keys, theme, media, notify, identity, config) with barrel + granular (types/shim/sdk) exports. Tree-shakable (`sideEffects: false`). See [packages/nub/README.md](packages/nub/README.md) for the full subpath reference. |
    ```

    The `@napplet/core` row (line 11), `@napplet/shim` row (line 12), `@napplet/sdk` row (line 13), and `@napplet/vite-plugin` row (line 19) stay unchanged.

    **Edit 2 — Dep graph ASCII block (lines 25-39):** Replace the entire ASCII block (the current 10-box graph including all the `@napplet/nub-<domain>` lines plus the DEFUNCT `@napplet/nub-signer` references on lines 27, 31) with this new 5-package graph:

    ```
    @napplet/shim ──┐
                    ├──► @napplet/nub ──► @napplet/core
    @napplet/sdk  ──┘

    @napplet/vite-plugin  (build-time only, depends on nostr-tools)
    ```

    The ASCII must be inside the existing fenced code block (triple backticks; no language tag, matching the current style).

    **Edit 3 — Nothing else changes.** Leave the "Napplet-Side Communication" diagram (lines 42-57), the "Origin" section (lines 62-63), "Development" (lines 65-71), "Publishing" (lines 73-81), "Related" (lines 83-85), and "License" (lines 87-88) fully intact.

    Verification-driving constraint: after these edits, running `grep -c "@napplet/nub-" README.md` MUST return `0`, and `grep -c "@napplet/nub-signer" README.md` MUST return `0`. `grep -qF "[@napplet/nub](packages/nub)" README.md` MUST succeed.
  </action>
  <verify>
    <automated>
      set -e
      cd /home/sandwich/Develop/napplet
      # Negative: zero deprecated refs
      test $(grep -c "@napplet/nub-" README.md) -eq 0
      # Negative: zero defunct signer refs
      test $(grep -c "@napplet/nub-signer" README.md) -eq 0
      # Positive: new @napplet/nub table row with correct link target
      grep -qF "[@napplet/nub](packages/nub)" README.md
      # Positive: dep graph mentions new topology
      grep -qF "@napplet/nub ──►" README.md || grep -qF "@napplet/nub" README.md
      # Positive: unchanged surrounding sections still present
      grep -qF "## Packages" README.md
      grep -qF "## Architecture" README.md
      grep -qF "## License" README.md
      grep -qF "@napplet/vite-plugin" README.md
      grep -qF "@napplet/core" README.md
      grep -qF "@napplet/shim" README.md
      grep -qF "@napplet/sdk" README.md
      echo "OK: README.md passes"
    </automated>
  </verify>
  <done>
    Root README has single `@napplet/nub` row linking to `packages/nub`, no `@napplet/nub-` matches, no `@napplet/nub-signer` refs, and the dep graph shows the 5-package post-consolidation shape.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update packages/core/README.md line 353 integration note</name>
  <files>packages/core/README.md</files>
  <read_first>
    - packages/core/README.md lines 345-360 (the "Integration Note" section with the sole match on line 353)
  </read_first>
  <action>
    Edit `/home/sandwich/Develop/napplet/packages/core/README.md` line 353.

    Current line 353:
    ```
    - **NUB packages** (`@napplet/nub-relay`, `@napplet/nub-identity`, `@napplet/nub-storage`, `@napplet/nub-ifc`, `@napplet/nub-keys`, `@napplet/nub-media`, `@napplet/nub-notify`, `@napplet/nub-config`): extend `NappletMessage` for their domain-specific message types and call `registerNub` at import time.
    ```

    Replace line 353 with (single-line rewrite referencing the consolidated package + its domain modules):
    ```
    - **`@napplet/nub` domain modules** (`@napplet/nub/relay`, `@napplet/nub/identity`, `@napplet/nub/storage`, `@napplet/nub/ifc`, `@napplet/nub/keys`, `@napplet/nub/media`, `@napplet/nub/notify`, `@napplet/nub/config`): extend `NappletMessage` for their domain-specific message types and call `registerNub` at import time. (The `@napplet/nub/theme` barrel is types-only — no `registerNub` side effect.)
    ```

    Do not modify any other line in this file. The surrounding "Integration Note" bullet above (line 352, mentions `@napplet/shim`, `@napplet/sdk`, `@napplet/vite-plugin`) and the "Protocol Reference" section below (line 355+) stay unchanged.
  </action>
  <verify>
    <automated>
      set -e
      cd /home/sandwich/Develop/napplet
      # Negative: zero deprecated refs in this file
      test $(grep -c "@napplet/nub-" packages/core/README.md) -eq 0
      # Positive: new subpath refs present
      grep -qF "@napplet/nub/relay" packages/core/README.md
      grep -qF "@napplet/nub/identity" packages/core/README.md
      grep -qF "@napplet/nub/config" packages/core/README.md
      # Positive: theme exception called out
      grep -qF "@napplet/nub/theme" packages/core/README.md
      # Positive: surrounding context preserved
      grep -qF "## Integration Note" packages/core/README.md
      grep -qF "## Protocol Reference" packages/core/README.md
      grep -qF "registerNub" packages/core/README.md
      echo "OK: packages/core/README.md passes"
    </automated>
  </verify>
  <done>
    Line 353 of `packages/core/README.md` references `@napplet/nub/<domain>` subpaths for 8 domains that call `registerNub` plus a parenthetical note on theme's types-only status. Zero `@napplet/nub-` matches remain.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update packages/shim/README.md line 426 dep-comparison row</name>
  <files>packages/shim/README.md</files>
  <read_first>
    - packages/shim/README.md lines 415-440 (the "Shim vs SDK" comparison table containing the match on line 426)
  </read_first>
  <action>
    Edit `/home/sandwich/Develop/napplet/packages/shim/README.md` line 426.

    Current line 426 (inside the Shim vs SDK comparison table):
    ```
    | **Dependencies** | `@napplet/nub-relay`, `@napplet/nub-identity`, `@napplet/nub-ifc`, `@napplet/nub-keys`, `@napplet/nub-media`, `@napplet/nub-notify`, `@napplet/nub-config` | `@napplet/core` (types only) |
    ```

    Replace line 426 with a single `@napplet/nub` entry in the shim column (SDK column on the right stays unchanged):
    ```
    | **Dependencies** | `@napplet/nub` (uses `@napplet/nub/<domain>/shim` subpaths internally) | `@napplet/core` (types only) |
    ```

    Do not modify any other line. The surrounding "Shim vs SDK" intro (line 420), table header (lines 422-423), and the "Typical usage" section (line 430+) stay intact.
  </action>
  <verify>
    <automated>
      set -e
      cd /home/sandwich/Develop/napplet
      # Negative: zero deprecated refs
      test $(grep -c "@napplet/nub-" packages/shim/README.md) -eq 0
      # Positive: bare @napplet/nub present on the updated table row
      grep -qF "\`@napplet/nub\`" packages/shim/README.md
      # Positive: granular /shim subpath pattern called out
      grep -qF "@napplet/nub/<domain>/shim" packages/shim/README.md || grep -qF "@napplet/nub/" packages/shim/README.md
      # Positive: table structure preserved
      grep -qF "| **Dependencies** |" packages/shim/README.md
      grep -qF "## Shim vs SDK" packages/shim/README.md
      echo "OK: packages/shim/README.md passes"
    </automated>
  </verify>
  <done>
    Line 426 of `packages/shim/README.md` has a single `@napplet/nub` entry in the shim column, noting that granular `/shim` subpaths are used internally. Zero `@napplet/nub-` matches remain.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update packages/sdk/README.md — peerDep note + type-to-package table</name>
  <files>packages/sdk/README.md</files>
  <read_first>
    - packages/sdk/README.md lines 170-185 (the `FromSchema type inference` subsection containing line 178)
    - packages/sdk/README.md lines 285-310 (the "NUB Message Types" table containing lines 296-303)
  </read_first>
  <action>
    Make 2 coordinated edits to `/home/sandwich/Develop/napplet/packages/sdk/README.md`.

    **Edit 1 — Line 178 (peerDep reference):**

    Current line 178:
    ```
    `json-schema-to-ts` is declared as an optional `peerDependency` of `@napplet/nub-config`. Install it in your napplet to get `FromSchema<typeof schema>` typing for your `config.subscribe` callback -- the `values` parameter is inferred directly from your schema (enums, required fields, defaults all flow through). Authors who skip `json-schema-to-ts` pay no install cost and `config.subscribe` still works with the default `Record<string, unknown>` typing.
    ```

    Replace `@napplet/nub-config` with `@napplet/nub` (the optional peerDep is now declared on the consolidated package, not the deprecated per-nub package). Rewrite the opening clause to:

    ```
    `json-schema-to-ts` is declared as an optional `peerDependency` of `@napplet/nub` (scoped to the `@napplet/nub/config` domain's `FromSchema` typing). Install it in your napplet to get `FromSchema<typeof schema>` typing for your `config.subscribe` callback -- the `values` parameter is inferred directly from your schema (enums, required fields, defaults all flow through). Authors who skip `json-schema-to-ts` pay no install cost and `config.subscribe` still works with the default `Record<string, unknown>` typing.
    ```

    **Edit 2 — Table rows at lines 296-303 (type-to-package mapping):**

    Replace each `@napplet/nub-<domain>` value in the "NUB Package" column with `@napplet/nub/<domain>` (barrel subpath form). The 8 rows must read exactly as follows after the edit (header row on lines 294-295 stays unchanged):

    ```
    | `RelayNubMessage` | `@napplet/nub/relay` | Discriminated union of all relay domain messages |
    | `IdentityNubMessage` | `@napplet/nub/identity` | Discriminated union of all identity domain messages |
    | `StorageNubMessage` | `@napplet/nub/storage` | Discriminated union of all storage domain messages |
    | `IfcNubMessage` | `@napplet/nub/ifc` | Discriminated union of all IFC domain messages |
    | `KeysNubMessage` | `@napplet/nub/keys` | Discriminated union of all keys domain messages |
    | `MediaNubMessage` | `@napplet/nub/media` | Discriminated union of all media domain messages |
    | `NotifyNubMessage` | `@napplet/nub/notify` | Discriminated union of all notify domain messages |
    | `ConfigNubMessage` | `@napplet/nub/config` | Discriminated union of all config domain messages |
    ```

    Consider whether the column header should also change (currently "NUB Package" — acceptable as-is; the values now point at subpaths but are still of the `@napplet/nub` package). Leave the header unchanged unless the executor's reading of line 294 suggests otherwise; if changed, update to "Subpath" or "Source Subpath".

    **Edit 3 — Do NOT touch anything else.** The surrounding "NUB Domain Constants" section (starting line 308 with its import example on line 313) stays unchanged; that example uses `from '@napplet/sdk'` (not the old per-nub packages) and is already correct.
  </action>
  <verify>
    <automated>
      set -e
      cd /home/sandwich/Develop/napplet
      # Negative: zero deprecated refs
      test $(grep -c "@napplet/nub-" packages/sdk/README.md) -eq 0
      # Positive: peerDep note rewritten
      grep -qF "optional \`peerDependency\` of \`@napplet/nub\`" packages/sdk/README.md
      grep -qF "@napplet/nub/config" packages/sdk/README.md
      # Positive: all 8 table rows use subpath form
      grep -qF "\`@napplet/nub/relay\`" packages/sdk/README.md
      grep -qF "\`@napplet/nub/identity\`" packages/sdk/README.md
      grep -qF "\`@napplet/nub/storage\`" packages/sdk/README.md
      grep -qF "\`@napplet/nub/ifc\`" packages/sdk/README.md
      grep -qF "\`@napplet/nub/keys\`" packages/sdk/README.md
      grep -qF "\`@napplet/nub/media\`" packages/sdk/README.md
      grep -qF "\`@napplet/nub/notify\`" packages/sdk/README.md
      grep -qF "\`@napplet/nub/config\`" packages/sdk/README.md
      # Positive: surrounding structure preserved
      grep -qF "FromSchema" packages/sdk/README.md
      grep -qF "RelayNubMessage" packages/sdk/README.md
      grep -qF "## NUB Domain Constants" packages/sdk/README.md
      echo "OK: packages/sdk/README.md passes"
    </automated>
  </verify>
  <done>
    Line ~178 references `@napplet/nub` (consolidated package) as the optional peerDep source and names `@napplet/nub/config` as the subpath. Lines ~296-303 have all 8 table rows using `@napplet/nub/<domain>` barrel subpaths. Zero `@napplet/nub-` matches remain in the file.
  </done>
</task>

</tasks>

<verification>
All 4 tasks share one final cross-file invariant: a combined grep across every modified file returns 0 matches for the deprecated pattern.

```bash
cd /home/sandwich/Develop/napplet
COUNT=$(grep -c "@napplet/nub-" README.md packages/core/README.md packages/shim/README.md packages/sdk/README.md 2>&1 | awk -F: '{sum+=$2} END {print sum}')
test "$COUNT" -eq 0 && echo "OK: no deprecated refs across 4 edited files"
```
</verification>

<success_criteria>
DOC-02 is satisfied: root README + `@napplet/core` README + `@napplet/shim` README + `@napplet/sdk` README all reference `@napplet/nub/<domain>` paths (or the bare `@napplet/nub` package name) in every NUB-related example. No `@napplet/nub-<domain>` references remain. Root README's defunct `@napplet/nub-signer` references are gone. Root README's dep graph shows the 5-package post-consolidation shape.
</success_criteria>

<output>
After completion, create `.planning/phases/120-documentation-update/120-02-SUMMARY.md`
</output>
