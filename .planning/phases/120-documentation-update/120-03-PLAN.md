---
phase: 120-documentation-update
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - DOC-03
  - DOC-04

must_haves:
  truths:
    - "specs/NIP-5D.md contains zero @napplet/nub-<domain> references (confirmed by grep AND by explicit file-content sanity pass)"
    - "skills/build-napplet/SKILL.md contains zero @napplet/nub-<domain> references (confirmed by grep AND by explicit file-content sanity pass)"
    - "The overall Phase 120 success grep across README.md + packages/*/README.md + specs/ + skills/ returns zero matches"
  artifacts: []
  key_links:
    - from: "Phase 120 final grep sweep"
      to: "all 4 DOC-* requirements satisfied"
      via: "grep -rn"
      pattern: "@napplet/nub-"
---

<objective>
Verify DOC-03 and DOC-04 — confirm that `specs/NIP-5D.md` and `skills/build-napplet/SKILL.md` contain zero references to deprecated `@napplet/nub-<domain>` package names, AND run the full phase-level grep sweep proving that across the entire documentation surface (root README + all 4 updated package READMEs + specs/ + skills/) there are zero remaining matches.

Preflight grep already found 0 matches in both files, so this plan is expected to be a no-op on file content. The verification step is still mandatory: (a) as a sanity check against text that grep might miss (e.g., inside fenced code blocks with surrounding context that preflight may have under-counted), (b) to formally satisfy DOC-03 and DOC-04 acceptance criteria, and (c) to run the phase-level cross-file grep that depends on Plans 01 + 02 having shipped.

Purpose: Close out DOC-03 and DOC-04 with evidence rather than assumption, and produce the single grep command output that Phase 121 verification will re-run as its success gate.

Output: No file writes expected under the happy path. If a match is found during the explicit file read, edit the offending line to use `@napplet/nub/<domain>` subpath form (update `files_modified` in the summary accordingly).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-documentation-update/120-CONTEXT.md

# Files being verified
@specs/NIP-5D.md
@skills/build-napplet/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify specs/NIP-5D.md — grep + file-content sanity pass</name>
  <files>specs/NIP-5D.md</files>
  <read_first>
    - specs/NIP-5D.md (full file — read it and scan for any import-example patterns not caught by literal-string grep; particular attention to fenced code blocks)
  </read_first>
  <action>
    Two-step verification:

    **Step A — Grep:**
    ```bash
    cd /home/sandwich/Develop/napplet
    grep -c "@napplet/nub-" specs/NIP-5D.md
    ```
    Expected: `0`. If non-zero, proceed to Step B with specific lines flagged.

    **Step B — File-content sanity pass:**
    Read the full file via the Read tool. Scan all fenced code blocks, ASCII tables, and inline backtick-quoted strings. Look specifically for:
    - `@napplet/nub-<any-word>` patterns (caught by grep above, but verify by eye)
    - `nub-<domain>` substrings not prefixed with `@napplet/` (e.g., references to "nub-relay" without the scope)
    - `import ... from '@napplet/...'` patterns anywhere in code fences
    - Example import blocks in any "Example" or "Usage" section

    **Step C — If any problematic reference is found:**
    Edit the offending line to use `@napplet/nub/<domain>` subpath form. Add the file to the plan's `files_modified` frontmatter in the summary (not in this PLAN file).

    **Step D — If file is already clean (expected):**
    Record the grep output `0` and the byte count / line count of the file as evidence in the plan summary. No file modification is needed.

    Do NOT modify the file under the happy path. This task's default outcome is "read-only verification with evidence capture."
  </action>
  <verify>
    <automated>
      set -e
      cd /home/sandwich/Develop/napplet
      # Primary acceptance: zero deprecated refs in NIP-5D.md
      test $(grep -c "@napplet/nub-" specs/NIP-5D.md) -eq 0
      # Secondary sanity check: file is present and non-empty (validates we actually checked the right file)
      test -s specs/NIP-5D.md
      # Structure sanity: the spec still looks like the spec (title + terminology section present)
      grep -q "^NIP-5D$" specs/NIP-5D.md
      grep -q "## Terminology" specs/NIP-5D.md
      echo "OK: specs/NIP-5D.md verified clean"
    </automated>
  </verify>
  <done>
    `grep -c "@napplet/nub-" specs/NIP-5D.md` returns 0, AND the executor has explicitly read through the file looking for import-pattern examples that grep might have missed. DOC-03 is satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify skills/build-napplet/SKILL.md — grep + file-content sanity pass</name>
  <files>skills/build-napplet/SKILL.md</files>
  <read_first>
    - skills/build-napplet/SKILL.md (full file — scan for any import-example patterns not caught by literal-string grep; particular attention to the `pnpm add` install instructions and all fenced `ts` code blocks)
  </read_first>
  <action>
    Two-step verification identical in pattern to Task 1:

    **Step A — Grep:**
    ```bash
    cd /home/sandwich/Develop/napplet
    grep -c "@napplet/nub-" skills/build-napplet/SKILL.md
    ```
    Expected: `0`.

    **Step B — File-content sanity pass:**
    Read the full file. This skill is an agent-facing usage guide with many code fences. Pay attention to:
    - `pnpm add` commands (install instructions should reference `@napplet/shim`, `@napplet/sdk`, or — if added — `@napplet/nub`; never the deprecated per-nub packages)
    - `import` statements in fenced `ts` code blocks
    - Any prose mentioning package names

    **Step C — If any problematic reference is found:**
    Edit to use `@napplet/nub/<domain>` subpath form. Update `files_modified` in the summary.

    **Step D — If clean (expected):**
    Record grep output `0` as evidence in summary. No file modification.

    Do NOT modify the file under the happy path.
  </action>
  <verify>
    <automated>
      set -e
      cd /home/sandwich/Develop/napplet
      # Primary acceptance: zero deprecated refs in SKILL.md
      test $(grep -c "@napplet/nub-" skills/build-napplet/SKILL.md) -eq 0
      # Secondary sanity: file present and non-empty
      test -s skills/build-napplet/SKILL.md
      # Structure sanity: skill frontmatter + overview section present
      grep -q "^name: build-napplet$" skills/build-napplet/SKILL.md
      grep -q "^## Overview$" skills/build-napplet/SKILL.md
      echo "OK: skills/build-napplet/SKILL.md verified clean"
    </automated>
  </verify>
  <done>
    `grep -c "@napplet/nub-" skills/build-napplet/SKILL.md` returns 0, AND the executor has explicitly read through the file confirming no import examples reference deprecated names. DOC-04 is satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase-level cross-file grep sweep (final gate for Plans 01 + 02 + this plan)</name>
  <files></files>
  <read_first>
    - (none — verification-only task that runs greps across files modified by Plans 01 + 02)
  </read_first>
  <action>
    Run the canonical Phase 120 success grep across all documentation surfaces:

    ```bash
    cd /home/sandwich/Develop/napplet
    grep -rn "@napplet/nub-" README.md packages/core/README.md packages/shim/README.md packages/sdk/README.md packages/vite-plugin/README.md packages/nub/README.md specs/ skills/ 2>&1 | grep -v "^packages/nubs/" || true
    ```

    Expected output: empty (zero lines). If any line appears, it is a Phase 120 failure and must be closed before marking the phase complete.

    **Note on excluded paths:**
    - `packages/nubs/<domain>/README.md` (9 files) are DEPRECATED re-export shim READMEs shipped in Phase 118. They legitimately contain `@napplet/nub-<domain>` as part of their deprecation banner and migration examples. These are explicitly OUT OF SCOPE for Phase 120 (per `120-CONTEXT.md <deferred>` block) — they will be removed in the future REMOVE-01..03 milestone.
    - `.planning/` tree is excluded — it contains historical phase planning docs that reference old names by design.
    - The grep above explicitly filters out `packages/nubs/` lines so deprecated README banners don't pollute the final gate signal.

    This task assumes Plans 01 and 02 have already run (or run in parallel in Wave 1). If the grep surfaces matches that Plans 01/02 were supposed to close, flag them in the plan's return summary so the orchestrator can re-dispatch those plans.
  </action>
  <verify>
    <automated>
      set -e
      cd /home/sandwich/Develop/napplet
      # The canonical Phase 120 success grep. Exit 0 only if zero matches.
      MATCHES=$(grep -rn "@napplet/nub-" README.md packages/core/README.md packages/shim/README.md packages/sdk/README.md packages/vite-plugin/README.md packages/nub/README.md specs/ skills/ 2>&1 | grep -v "^packages/nubs/" | grep -v "No such file" || true)
      if [ -n "$MATCHES" ]; then
        echo "FAIL: found deprecated refs:"
        echo "$MATCHES"
        exit 1
      fi
      echo "OK: Phase 120 final grep sweep clean across docs surface"
    </automated>
  </verify>
  <done>
    The final cross-file grep across root README, all 4 edited package READMEs, the new `packages/nub/README.md`, `packages/vite-plugin/README.md`, `specs/`, and `skills/` returns zero matches for `@napplet/nub-` (excluding `packages/nubs/<domain>/` which is out of scope). Phase 120's primary invariant is satisfied.
  </done>
</task>

</tasks>

<verification>
All 3 tasks' automated checks pass. The gate for the whole phase is Task 3's cross-file grep returning zero lines (after filtering `packages/nubs/<domain>/` — those are Phase-118-owned deprecation shim READMEs out of scope here).
</verification>

<success_criteria>
DOC-03 and DOC-04 satisfied: both `specs/NIP-5D.md` and `skills/build-napplet/SKILL.md` verified free of deprecated references via grep + explicit file-content read. Phase 120's cross-file acceptance grep returns zero matches across the documentation surface.
</success_criteria>

<output>
After completion, create `.planning/phases/120-documentation-update/120-03-SUMMARY.md`. The summary MUST record:
- Whether either file required edits (expected: no)
- The exact output of the Task 3 cross-file grep (expected: empty)
- Any anomalies or deferred items
</output>
