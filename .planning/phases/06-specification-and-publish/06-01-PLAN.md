---
phase: 06-specification-and-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SPEC.md
autonomous: true
requirements: [SPEC-01, SPEC-02, SPEC-03, SPEC-04]
user_setup: []
must_haves:
  truths:
    - "SPEC.md relay URI is 'shell://' not 'hyprgate://shell' or 'napplet://shell'"
    - "SPEC.md meta tag prefix is 'napplet-*' not 'hyprgate-*'"
    - "SPEC.md documents storage keys() response using repeated ['key', name] tags"
    - "SPEC.md documents AUTH failure sends OK false followed by NOTICE about dropped messages"
    - "SPEC.md documents missing type/aggregateHash tags cause AUTH failure (not permissive default)"
    - "SPEC.md documents pre-AUTH queue capped at 50 (configurable)"
    - "SPEC.md documents blocked napp gets CLOSED with 'blocked:' prefix"
    - "SPEC.md documents ACL persistence format"
    - "SPEC.md documents storage quota uses UTF-8 byte count"
    - "SPEC.md documents sender exclusion only for kind 29003 (inter-pane topic events)"
    - "SPEC.md documents NIP-01 error prefix convention for error messages"
    - "SPEC.md documents manifest format with hash computation and NIP-5A reference"
    - "SPEC.md documents all protocol message types with examples"
    - "SPEC.md documents security model including ACL, consent, and isolation boundaries"
  artifacts:
    - path: SPEC.md
      provides: "Refined NIP-5A specification reflecting all implementation learnings"
      contains: "shell://"
  key_links: []
---

<objective>
Refine SPEC.md to reflect all implementation learnings from Phases 1-5.

Purpose: The spec is the canonical protocol reference. Implementation diverged from the original draft in 11 specific areas. This plan applies those changes as targeted updates (not a rewrite) and ensures all protocol message types have examples, the security model is fully documented, and the manifest format with hash computation is specified.
Output: Updated SPEC.md that accurately represents the implemented protocol.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-specification-and-publish/06-CONTEXT.md
@.planning/phases/06-specification-and-publish/06-RESEARCH.md
@.planning/phases/01-wiring-fixes/01-CONTEXT.md
@.planning/phases/03-core-protocol-tests/03-CONTEXT.md
@.planning/phases/04-capability-tests/04-CONTEXT.md

@SPEC.md
@packages/shell/src/pseudo-relay.ts
@packages/shell/src/acl-store.ts
@packages/shell/src/storage-proxy.ts
@packages/shell/src/types.ts
@packages/shim/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AUTH relay URI and meta tag prefix</name>
  <files>SPEC.md</files>
  <read_first>SPEC.md, .planning/phases/01-wiring-fixes/01-CONTEXT.md</read_first>
  <action>
  Search SPEC.md for all occurrences of relay URI and update:

  1. Change any `hyprgate://shell` references to `shell://` (Phase 1 D-01 used `shell://`, but current SPEC.md may already say `napplet://shell` from extraction). Verify the actual current value and update to `shell://`.
  2. Look at Section 2.2 (AUTH response event). The relay tag should be `["relay", "shell://"]`.
  3. Look at Section 2.3 and 2.4. Meta tag names should be `napplet-napp-type` and `napplet-aggregate-hash` (not `hyprgate-*`).
  4. Update any references to `PSEUDO_RELAY_URI` constant value to show `shell://`.

  Also check the `packages/shell/src/types.ts` for the actual constant value to ensure the spec matches implementation.
  </action>
  <verify>grep -c "shell://" SPEC.md && ! grep "hyprgate://" SPEC.md && grep "napplet-napp-type" SPEC.md && grep "napplet-aggregate-hash" SPEC.md</verify>
  <acceptance_criteria>
    - SPEC.md contains `shell://` as the pseudo-relay URI
    - SPEC.md does NOT contain `hyprgate://` anywhere
    - SPEC.md Section 2.3 references `napplet-napp-type` meta tag
    - SPEC.md Section 2.4 references `napplet-aggregate-hash` meta tag
  </acceptance_criteria>
  <done>AUTH relay URI and meta tag prefix updated</done>
</task>

<task type="auto">
  <name>Task 2: Update AUTH failure behavior and missing tag handling</name>
  <files>SPEC.md</files>
  <read_first>SPEC.md, .planning/phases/01-wiring-fixes/01-CONTEXT.md, .planning/phases/03-core-protocol-tests/03-CONTEXT.md</read_first>
  <action>
  Update Section 2 (Authentication Handshake):

  1. **AUTH failure response** (Section 2.2, step 5): Update to document that on failure, shell sends BOTH:
     - `["OK", <event_id>, false, "auth-required: <reason>"]`
     - `["NOTICE", "N queued messages dropped due to auth failure"]`
     This is Phase 1 D-06.

  2. **Missing type tag** (Section 2.3): Update to specify that if `type` tag is missing from AUTH event, AUTH MUST fail (not default to "unknown"). This is Phase 3 D-02. The error message should be: `"auth-required: missing type tag"`.

  3. **Missing aggregateHash tag** (Section 2.4): Update to specify that if `aggregateHash` tag is missing from AUTH event, AUTH MUST fail (not use empty string). The error message should be: `"auth-required: missing aggregateHash tag"`.

  4. **NIP-01 error prefix convention** (Phase 3 D-01): Add a note in Section 2 or a general protocol notes section that error reason strings follow NIP-01 prefix convention:
     - `auth-required:` for pre-auth issues
     - `error:` for validation failures
     - `blocked:` for ACL denials

  5. **Pre-AUTH queue cap** (Phase 3 D-08): Update Section 2.7 to document that the pre-AUTH message queue is capped at 50 messages by default. This cap is configurable both globally (shell-wide) and per-napp. Messages exceeding the cap MUST be rejected with a NOTICE.
  </action>
  <verify>grep "auth-required:" SPEC.md && grep "NOTICE.*dropped" SPEC.md && grep "missing type tag" SPEC.md && grep "50" SPEC.md</verify>
  <acceptance_criteria>
    - SPEC.md Section 2 documents AUTH failure sends OK false + NOTICE about dropped messages
    - SPEC.md documents missing type tag causes AUTH failure with "auth-required: missing type tag"
    - SPEC.md documents missing aggregateHash tag causes AUTH failure
    - SPEC.md documents pre-AUTH queue cap of 50 (configurable)
    - SPEC.md documents NIP-01 error prefix conventions (auth-required:, error:, blocked:)
  </acceptance_criteria>
  <done>AUTH failure behavior and missing tag handling updated</done>
</task>

<task type="auto">
  <name>Task 3: Update message routing and blocked napp behavior</name>
  <files>SPEC.md</files>
  <read_first>SPEC.md, .planning/phases/03-core-protocol-tests/03-CONTEXT.md</read_first>
  <action>
  Update Section 3 (NIP-01 Message Routing):

  1. **Blocked napp CLOSED prefix** (Phase 3 D-09): Update the section on ACL enforcement in routing to specify that when a blocked napp attempts any operation, the shell sends:
     `["CLOSED", <sub_id>, "blocked: capability denied"]`
     The `blocked:` prefix is distinct from `error:` and `auth-required:` so napplets can programmatically identify ACL denials.

  2. **Sender exclusion scope** (Phase 3 D-07): Find the inter-pane delivery section and specify that sender exclusion (sender does not receive their own broadcast) applies ONLY to kind 29003 (inter-pane topic events). Normal relay events (kind 1, etc.) can be delivered back to the sender if they match a subscription filter.

  3. **REQ from blocked napp**: Document that REQ from a blocked napp receives `["CLOSED", sub_id, "blocked: relay:read denied"]`.
  </action>
  <verify>grep "blocked:" SPEC.md && grep "29003" SPEC.md && grep "sender exclusion" SPEC.md</verify>
  <acceptance_criteria>
    - SPEC.md documents blocked napp CLOSED with "blocked: capability denied" prefix
    - SPEC.md documents sender exclusion applies only to kind 29003
    - SPEC.md documents REQ from blocked napp gets CLOSED with "blocked: relay:read denied"
  </acceptance_criteria>
  <done>Message routing and blocked napp behavior updated</done>
</task>

<task type="auto">
  <name>Task 4: Update storage specification</name>
  <files>SPEC.md</files>
  <read_first>SPEC.md, .planning/phases/01-wiring-fixes/01-CONTEXT.md, .planning/phases/04-capability-tests/04-CONTEXT.md, packages/shell/src/storage-proxy.ts</read_first>
  <action>
  Update the storage section of SPEC.md:

  1. **keys() response format** (Phase 1 D-05): Update to specify that the storage `keys()` response uses repeated NIP tags: one `["key", <name>]` tag per key. NOT a comma-joined string. Example:
     ```json
     {
       "kind": 29001,
       "tags": [
         ["key", "theme"],
         ["key", "preferences"],
         ["key", "history"]
       ],
       "content": ""
     }
     ```

  2. **Quota computation** (Phase 4 D-02): Update to specify that storage quota is calculated using UTF-8 byte count:
     `new TextEncoder().encode(key + value).length`
     The default quota is 512KB per napp (scoped by nappType:aggregateHash composite key).

  3. **Quota exceeded error** (Phase 4 D-03): Document that quota exceeded returns a response event with `["error", "quota exceeded: 512KB limit"]` tag. This follows the NIP tag error pattern used throughout the protocol.

  4. **Storage scoping**: Verify the spec documents that storage keys are scoped by `nappType:aggregateHash` composite key, so different napplets and different versions of the same napplet have isolated storage.
  </action>
  <verify>grep "key.*name" SPEC.md && grep "UTF-8" SPEC.md && grep "512KB" SPEC.md && grep "quota exceeded" SPEC.md</verify>
  <acceptance_criteria>
    - SPEC.md documents keys() response uses repeated ["key", name] NIP tags
    - SPEC.md documents quota uses UTF-8 byte count
    - SPEC.md documents 512KB default quota
    - SPEC.md documents quota exceeded error with NIP tag format
    - SPEC.md documents storage scoping by nappType:aggregateHash
  </acceptance_criteria>
  <done>Storage specification updated</done>
</task>

<task type="auto">
  <name>Task 5: Document ACL persistence format and security model</name>
  <files>SPEC.md</files>
  <read_first>SPEC.md, .planning/phases/04-capability-tests/04-CONTEXT.md, packages/shell/src/acl-store.ts</read_first>
  <action>
  Update the ACL/security section of SPEC.md:

  1. **ACL persistence format** (Phase 4 D-04): Add a subsection documenting the ACL persistence format. Read `packages/shell/src/acl-store.ts` to determine the exact localStorage key name and JSON structure. Document:
     - The localStorage key used for ACL storage
     - The JSON schema of the persisted ACL entries
     - That this format is LOCKED as part of the protocol contract
     - That implementations MUST be able to load ACL state persisted by other implementations

  2. **Security model completeness** (SPEC-03): Verify the spec has dedicated documentation for:
     - ACL enforcement (capabilities, grant/revoke/block operations)
     - Consent flow for destructive signing operations
     - Isolation boundaries (no allow-same-origin, storage scoping, relay proxying)
     - The "verify once, trust source" model for post-AUTH messages

  3. **Consent flow**: If not already present, add documentation for the signer consent flow:
     - Non-destructive kinds (kind < 10000 except specified list): auto-signed
     - Destructive kinds: require explicit user consent via shell UI
     - Consent request includes: event template, napp identity, requested capability
     - Shell MUST support approve and deny outcomes
     - Timeout after 30s returns error to napplet

  4. **All capabilities list**: Document the complete capabilities list:
     - `relay:read`, `relay:write`
     - `storage:read`, `storage:write`
     - `sign:event`
     Verify against `packages/shell/src/types.ts` ALL_CAPABILITIES constant.
  </action>
  <verify>grep "persistence" SPEC.md && grep "localStorage" SPEC.md && grep "consent" SPEC.md && grep "relay:read" SPEC.md && grep "relay:write" SPEC.md && grep "storage:read" SPEC.md && grep "sign:event" SPEC.md</verify>
  <acceptance_criteria>
    - SPEC.md documents ACL persistence format with localStorage key and JSON schema
    - SPEC.md documents full security model: ACL, consent, isolation boundaries
    - SPEC.md documents consent flow for destructive signing operations
    - SPEC.md documents all capabilities: relay:read, relay:write, storage:read, storage:write, sign:event
    - SPEC.md has ACL persistence format marked as LOCKED
  </acceptance_criteria>
  <done>ACL persistence and security model documented</done>
</task>

<task type="auto">
  <name>Task 6: Document manifest format and hash computation</name>
  <files>SPEC.md</files>
  <read_first>SPEC.md, .planning/phases/06-specification-and-publish/06-CONTEXT.md, packages/vite-plugin/src/index.ts</read_first>
  <action>
  Update or add a manifest section in SPEC.md:

  1. **Manifest format** (SPEC-04): Document the NIP-5A manifest event (kind 35128):
     - Event structure with tags: `path` tags with `[sha256, relativePath]`, `x` tag with aggregate hash
     - Reference NIP-5A for the full specification
     - Reference nostr-protocol/nips#2287 for the aggregate hash `x` tag (not yet merged)

  2. **Hash computation algorithm**: Document how the aggregate hash is computed:
     ```
     1. Walk all files in the build output directory
     2. For each file, compute SHA-256 of file contents
     3. Create lines: "<sha256hex> <relativePath>\n"
     4. Sort lines lexicographically
     5. Concatenate sorted lines
     6. SHA-256 the concatenated string = aggregate hash
     ```
     This matches the implementation in `packages/vite-plugin/src/index.ts` `computeAggregateHash()`.

  3. **Shell verification model** (CONTEXT D-04, D-05): Document:
     - Shell uses the aggregate hash from the NIP-5A event's `x` tag as part of the ACL composite key
     - The HTML meta tag `napplet-aggregate-hash` is a local convenience for the shim during development
     - The NIP-5A event on relays is the source of truth for production verification
     - ACL composite key is `(pubkey, dTag, aggregateHash)` — changes to any component create a new identity

  4. **Vite plugin is NOT canonical**: Add a note that the `@napplet/vite-plugin` is a development convenience. Production deployment uses community tools (nsyte, etc.) to create and publish NIP-5A manifest events.
  </action>
  <verify>grep "35128" SPEC.md && grep "aggregate hash" SPEC.md && grep "SHA-256" SPEC.md && grep "nips#2287\|nostr-protocol/nips" SPEC.md</verify>
  <acceptance_criteria>
    - SPEC.md documents kind 35128 manifest event format
    - SPEC.md documents aggregate hash computation algorithm (sort, concatenate, SHA-256)
    - SPEC.md references NIP-5A and nostr-protocol/nips#2287
    - SPEC.md documents shell uses x tag from NIP-5A event as ACL composite key component
    - SPEC.md notes vite-plugin is dev convenience, not production deploy tool
  </acceptance_criteria>
  <done>Manifest format and hash computation documented</done>
</task>

<task type="auto">
  <name>Task 7: Add message examples for all protocol message types</name>
  <files>SPEC.md</files>
  <read_first>SPEC.md, packages/shell/src/types.ts</read_first>
  <action>
  Verify SPEC.md has concrete JSON examples for every protocol message type. Check Sections 1.4 and 1.5 tables and add examples where missing:

  **Napplet-to-Shell messages:**
  1. `EVENT` — publish example:
     ```json
     ["EVENT", {"kind": 1, "content": "Hello from napplet!", "tags": [], "created_at": 1711800000, "pubkey": "abc...", "id": "def...", "sig": "ghi..."}]
     ```
  2. `REQ` — subscription example:
     ```json
     ["REQ", "sub-1", {"kinds": [1], "limit": 10}]
     ```
  3. `CLOSE` — close subscription example:
     ```json
     ["CLOSE", "sub-1"]
     ```
  4. `AUTH` — AUTH response example (already in Section 2.2, verify complete)
  5. `COUNT` — count request example:
     ```json
     ["COUNT", "count-1", {"kinds": [1], "authors": ["abc..."]}]
     ```

  **Shell-to-Napplet messages:**
  1. `EVENT` — delivered event example:
     ```json
     ["EVENT", "sub-1", {"kind": 1, "content": "Hello!", ...}]
     ```
  2. `OK` — success and failure examples:
     ```json
     ["OK", "event-id-hex", true, ""]
     ["OK", "event-id-hex", false, "auth-required: complete AUTH first"]
     ```
  3. `EOSE` — end of stored events:
     ```json
     ["EOSE", "sub-1"]
     ```
  4. `CLOSED` — subscription closed (normal and blocked):
     ```json
     ["CLOSED", "sub-1", ""]
     ["CLOSED", "sub-1", "blocked: relay:read denied"]
     ```
  5. `AUTH` — challenge:
     ```json
     ["AUTH", "a1b2c3d4-uuid-challenge"]
     ```
  6. `NOTICE` — human-readable notice:
     ```json
     ["NOTICE", "3 queued messages dropped due to auth failure"]
     ```
  7. `COUNT` — count result:
     ```json
     ["COUNT", "count-1", {"count": 42}]
     ```

  Also check for **inter-pane event** (kind 29003) example and **storage request/response** examples. Add if missing.
  </action>
  <verify>grep -c '```json' SPEC.md | awk '{if ($1 >= 15) print "PASS"; else print "FAIL: need more examples"}'</verify>
  <acceptance_criteria>
    - SPEC.md has JSON examples for all 5 napplet-to-shell message types (EVENT, REQ, CLOSE, AUTH, COUNT)
    - SPEC.md has JSON examples for all 7 shell-to-napplet message types (EVENT, OK, EOSE, CLOSED, AUTH, NOTICE, COUNT)
    - SPEC.md has examples for inter-pane events (kind 29003)
    - SPEC.md has examples for storage request/response messages
    - All examples use correct NIP-01 wire format
  </acceptance_criteria>
  <done>Protocol message examples added for all message types</done>
</task>

<task type="auto">
  <name>Task 8: Final spec review and consistency check</name>
  <files>SPEC.md</files>
  <read_first>SPEC.md</read_first>
  <action>
  Do a complete read of the updated SPEC.md and check for consistency:

  1. **No stale references**: Search for any remaining `hyprgate` references. Search for `napplet://shell` (should be `shell://`). Search for inconsistent relay URIs.
  2. **Section cross-references**: Verify section numbers referenced in other sections still point to the correct content.
  3. **Status markers**: Verify [OPEN] and [LOCKED] markers are consistent — the AUTH handshake section should be [LOCKED], the manifest section should reference [OPEN] status of NIP-5A.
  4. **Protocol version**: Confirm version 2.0.0 is stated.
  5. **Terminology table**: Verify terminology definitions are consistent with usage throughout the document.
  6. **No TODO/FIXME markers**: Search for any TODO, FIXME, TBD, or [OPEN QUESTION] markers that should have been resolved.

  Fix any issues found.
  </action>
  <verify>! grep -i "hyprgate" SPEC.md && ! grep "napplet://shell" SPEC.md && ! grep -i "FIXME\|TODO" SPEC.md && grep "2.0.0" SPEC.md</verify>
  <acceptance_criteria>
    - SPEC.md contains zero occurrences of "hyprgate"
    - SPEC.md contains zero occurrences of "napplet://shell" (should be "shell://")
    - SPEC.md contains zero FIXME or TODO markers
    - SPEC.md states protocol version 2.0.0
    - All section cross-references are valid
  </acceptance_criteria>
  <done>Final spec consistency check complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 11 implementation changes from RESEARCH.md table are reflected in SPEC.md
- [ ] SPEC.md has concrete examples for all 12 protocol message types
- [ ] Security model section covers ACL, consent, isolation boundaries
- [ ] Manifest section covers hash computation and NIP-5A references
- [ ] No stale hyprgate references or incorrect relay URIs remain
- [ ] No FIXME/TODO markers remain
</verification>

<success_criteria>
- SPEC.md accurately reflects the implemented protocol
- All protocol message types have JSON examples
- Security model is fully documented
- Manifest format and hash computation are documented
- Spec is internally consistent with no stale references
</success_criteria>

<output>
After completion, create `.planning/phases/06-specification-and-publish/06-01-SUMMARY.md`
</output>
