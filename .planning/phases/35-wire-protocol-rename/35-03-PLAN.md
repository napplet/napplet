---
plan: 03
phase: 35
title: SPEC.md Combined Pass — INTER-PANE → IPC-PEER, napp → napplet, IPC-* Section
wave: 3
depends_on: []
requirements_addressed: [WIRE-02]
files_modified:
  - SPEC.md
autonomous: true
---

# Plan 35-03: SPEC.md Combined Pass

## Objective

Edit `SPEC.md` once, applying three changes in a single pass per D-08:
1. Replace all `INTER_PANE` / `INTER-PANE` / `inter-pane` occurrences with `IPC_PEER` / `IPC-PEER` (10 sites)
2. Correct standalone `napp` → `napplet` where "napp" means the sandboxed iframe app (not a topic string value or code identifier in a JSON blob)
3. Add an IPC-* namespace subsection in Section 3.8 (Ephemeral Bus Kinds)

This plan is independent of Plans 35-01 and 35-02 (no TypeScript compilation involved) and MAY be executed in any order.

---

## Task 35-03-01: Section heading and section 8.5 — INTER-PANE → IPC-PEER

<read_first>
- `SPEC.md` lines 755-769 — "### 8.5 Inter-Pane Events" section
- `SPEC.md` lines 406, 437, 539, 630, 638, 664, 757, 897, 908, 920 — all INTER_PANE/inter-pane occurrences
</read_first>

<action>
Apply each of the following exact string replacements to `SPEC.md`:

**1. Line 406 — sender exclusion note:**
Find:
```
- **Sender exclusion (kind 29003 only):** For inter-pane topic events (kind 29003), the sender is NOT included in delivery.
```
Replace with:
```
- **Sender exclusion (kind 29003 only):** For IPC-PEER topic events (kind 29003), the sender is NOT included in delivery.
```

**2. Line 437 — BusKind table row:**
Find:
```
| 29003 | INTER_PANE | Inter-napplet messaging and shell commands |
```
Replace with:
```
| 29003 | IPC_PEER | Inter-napplet messaging and shell commands |
```

**3. Line 539 — storage proxy overview:**
Find:
```
All state operations use kind 29003 (INTER_PANE) events with topic-based routing.
```
Replace with:
```
All state operations use kind 29003 (IPC_PEER) events with topic-based routing.
```

**4. Line 630 — audio section heading 6.2:**
Find:
```
### 6.2 Napplet-to-Shell Messages (kind 29003 INTER_PANE)
```
Replace with:
```
### 6.2 Napplet-to-Shell Messages (kind 29003 IPC_PEER)
```

**5. Line 638 — audio section heading 6.3:**
Find:
```
### 6.3 Shell-to-Napplet Messages (kind 29003 INTER_PANE)
```
Replace with:
```
### 6.3 Shell-to-Napplet Messages (kind 29003 IPC_PEER)
```

**6. Line 664 — relay management overview:**
Find:
```
All relay operations use kind 29003 (INTER_PANE) with shell-targeted topics.
```
Replace with:
```
All relay operations use kind 29003 (IPC_PEER) with shell-targeted topics.
```

**7. Line 755 — section heading 8.5:**
Find:
```
### 8.5 Inter-Pane Events
```
Replace with:
```
### 8.5 IPC-PEER Events
```

**8. Line 757 — section 8.5 body:**
Find:
```
Napplets can communicate with each other via free-form topics on kind 29003 (INTER_PANE). Built-in topics include:
```
Replace with:
```
Napplets can communicate with each other via free-form topics on kind 29003 (IPC_PEER). Built-in topics include:
```

**9. Line 897 — service message routing:**
Find:
```
Services receive messages via INTER_PANE events (kind 29003) with topic strings prefixed by the service name. The topic format is:
```
Replace with:
```
Services receive messages via IPC-PEER events (kind 29003) with topic strings prefixed by the service name. The topic format is:
```

**10. Line 908 — service message routing cont.:**
Find:
```
The shell routes INTER_PANE events to service handlers based on the topic prefix. Events with topics that match a registered service's name prefix are dispatched to that service's handler.
```
Replace with:
```
The shell routes IPC-PEER events to service handlers based on the topic prefix. Events with topics that match a registered service's name prefix are dispatched to that service's handler.
```

**11. Line 920 — service lifecycle step 4:**
Find:
```
4. **Interaction:** The napplet sends INTER_PANE events with service-prefixed topics. The shell dispatches these to the appropriate service handler.
```
Replace with:
```
4. **Interaction:** The napplet sends IPC-PEER events with service-prefixed topics. The shell dispatches these to the appropriate service handler.
```
</action>

<acceptance_criteria>
- `grep -c 'INTER_PANE\|INTER-PANE\|inter-pane' SPEC.md` returns 0
- Section heading `### 8.5 IPC-PEER Events` exists in SPEC.md
</acceptance_criteria>

---

## Task 35-03-02: Add IPC-* namespace subsection after the BusKind table (Section 3.8)

<read_first>
- `SPEC.md` lines 432-444 — Section 3.8 Ephemeral Bus Kinds table (ends at line 444 with the Note)
- Line 446 — horizontal rule `---` that follows
</read_first>

<action>
After the existing BusKind table and its Note (after the line `Note: These kind numbers are implementation-specific...`), and before the `---` horizontal rule, insert a new subsection:

Find the exact text:
```
Note: These kind numbers are implementation-specific. Other shell implementations MAY use different ephemeral kind ranges, provided they remain within the 20000-29999 ephemeral range defined by NIP-01.

---
```

Replace with:
```
Note: These kind numbers are implementation-specific. Other shell implementations MAY use different ephemeral kind ranges, provided they remain within the 20000-29999 ephemeral range defined by NIP-01.

#### IPC-* Namespace

The `IPC-*` prefix is reserved for the inter-napplet communication bus. Current and reserved members:

| Constant | Kind | Status | Description |
|----------|------|--------|-------------|
| `IPC_PEER` | 29003 | Current | Directed peer-to-peer IPC between napplets and the shell |
| `IPC_BROADCAST` | TBD | Reserved | Future: broadcast to all napplets |
| `IPC_CHANNEL` | TBD | Reserved | Future: named channel pubsub |

See `@napplet/core` `constants.ts` for the authoritative `BusKind` constant definition.

---
```
</action>

<acceptance_criteria>
- `grep -n 'IPC-\* Namespace' SPEC.md` returns a result (the new section heading exists)
- `grep -n 'IPC_BROADCAST' SPEC.md` returns a result (reserved member documented)
- `grep -n 'IPC_CHANNEL' SPEC.md` returns a result (reserved member documented)
</acceptance_criteria>

---

## Task 35-03-03: Correct standalone napp → napplet in SPEC.md prose

<read_first>
- `SPEC.md` lines 35, 167, 250, 265, 348, 412-415, 535, 599, 604, 606, 620, 702-710, 1041, 1049, 1085, 1089, 1141, 1159, 1162 — targeted corrections
</read_first>

<action>
Apply the following targeted prose corrections. For each, find the exact string and replace it. Do NOT make global search-and-replace — each correction is surgical.

**NOTE on scope:** Only rename standalone `napp` meaning "the sandboxed iframe application". Do NOT rename:
- `napp:audio-muted` — topic string (wire value, left as-is in SPEC.md; Phase 34 changed the code constant but SPEC.md still documents the wire protocol behavior)
- `napp:state-response` — topic string (same reasoning)
- `napp-state:` — localStorage key prefix in Section 5.5 (documents the storage format)
- `napp_type` in JSON blob at line 1162 — this is a JSON key name in a code example, not prose
- `napp publisher` at line 1159 — "napp publisher" is Nostr ecosystem terminology for the entity that publishes napplets; keep as "napplet publisher" only if unambiguous (see below)
- `KIND_NAPP_LISTING` — constant name in a table, not prose
- Section 15.1 heading `Napp Listing Kind` — names the Nostr kind concept, not this SDK's app type; leave for now

Apply these replacements in order:

1. Line 35:
   Find: `A derived identifier combining napp pubkey prefix and napp type`
   Replace: `A derived identifier combining napplet pubkey prefix and napplet type`

2. Line 167 (CLOSED example comment):
   Find: `CLOSED` (subscription closed -- blocked napp):
   Replace: `CLOSED` (subscription closed -- blocked napplet):

3. Line 250:
   Find: `the shell SHOULD verify it against the napp's published NIP-5A manifest on relays`
   Replace: `the shell SHOULD verify it against the napplet's published NIP-5A manifest on relays`

4. Line 265:
   Find: `blocked:` | ACL denials (capability revoked or napp blocked)
   Replace: `blocked:` | ACL denials (capability revoked or napplet blocked)

5. Line 348:
   Find: `to prevent cross-napp subscription ID collisions`
   Replace: `to prevent cross-napplet subscription ID collisions`

6. Line 412 (section header):
   Find: `### 3.6.1 Blocked Napp Behavior`
   Replace: `### 3.6.1 Blocked Napplet Behavior`

7. Line 412 (section body):
   Find: `When a blocked napp attempts any operation:`
   Replace: `When a blocked napplet attempts any operation:`

8. Line 414:
   Find: `- **REQ from blocked napp:** The shell MUST respond with`
   Replace: `- **REQ from blocked napplet:** The shell MUST respond with`

9. Line 415:
   Find: `- **EVENT from blocked napp:** The shell MUST respond with`
   Replace: `- **EVENT from blocked napplet:** The shell MUST respond with`

10. Line 535 (storage proxy overview):
    Find: `scopes data by napp composite identity`
    Replace: `scopes data by napplet composite identity`
    
    Also in the same sentence:
    Find: `When a napp updates (new aggregateHash)`
    Replace: `When a napplet updates (new aggregateHash)`

11. Line 599 (quota enforcement):
    Find: `Each napp identity has a state quota`
    Replace: `Each napplet identity has a state quota`

12. Line 604:
    Find: `"quota exceeded: napp state limit is <N> bytes"`
    Replace: `"quota exceeded: napplet state limit is <N> bytes"`

13. Line 606:
    Find: `The per-napp quota MAY be configured`
    Replace: `The per-napplet quota MAY be configured`

14. Line 620:
    Find: `When a napp's `aggregateHash` changes`
    Replace: `When a napplet's `aggregateHash` changes`

15. Lines 702-710 (window creation content):
    Find: `"class": "<napp class identifier>",`
    Replace: `"class": "<napplet class identifier>",`
    
    Find: `"url": "<optional napp URL>",`
    Replace: `"url": "<optional napplet URL>",`
    
    Find: `- `class` (REQUIRED): Napp class identifier (used for launcher routing)`
    Replace: `- `class` (REQUIRED): Napplet class identifier (used for launcher routing)`
    
    Find: `if absent, the shell resolves the napp URL from the class identifier`
    Replace: `if absent, the shell resolves the napplet URL from the class identifier`

16. Line 1041:
    Find: `the shell fetches the napp's NIP-5A manifest from relays`
    Replace: `the shell fetches the napplet's NIP-5A manifest from relays`

17. Line 1049 (section heading):
    Find: `### 13.7 Napp Update Detection`
    Replace: `### 13.7 Napplet Update Detection`

18. Line 1085:
    Find: `- `stateQuota`: Per-napp state quota in bytes`
    Replace: `- `stateQuota`: Per-napplet state quota in bytes`

19. Line 1089:
    Find: `Each napplet identity has a per-napp state quota.`
    Replace: `Each napplet identity has a per-napplet state quota.`

20. Line 1141:
    Find: `This kind is used for napp directory listings.`
    Replace: `This kind is used for napplet directory listings.`

21. Line 1159:
    Find: `"pubkey": "<napp publisher hex pubkey>",`
    Replace: `"pubkey": "<napplet publisher hex pubkey>",`
</action>

<acceptance_criteria>
- All targeted prose corrections applied
- `grep -n '### 3.6.1 Blocked Napplet Behavior' SPEC.md` returns a result
- `grep -n '### 13.7 Napplet Update Detection' SPEC.md` returns a result
- Topic strings `napp:audio-muted` and `napp:state-response` remain unchanged in SPEC.md (wire format documentation)
- `napp-state:` key format in Section 5.5 remains unchanged
- `napp_type` JSON key in the code block at line 1162 remains unchanged
</acceptance_criteria>

---

## Wave 3 Final Validation

After completing all tasks in this plan, run:

```bash
cd /home/sandwich/Develop/napplet
grep -c 'INTER_PANE\|INTER-PANE\|inter-pane\|Inter-Pane\|Inter-pane' SPEC.md
```
Expected: 0

```bash
grep -n 'IPC-\* Namespace\|IPC_BROADCAST\|IPC_CHANNEL' SPEC.md | head -5
```
Expected: 3 results (new section present)

This satisfies WIRE-02: "SPEC.md and documentation updated — `INTER-PANE` / `inter-pane` replaced with `IPC-PEER` / `ipc-peer` throughout; `IPC-*` namespace documented as the family for future `IPC-BROADCAST` and `IPC-CHANNEL` types".
