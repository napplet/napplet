---
plan: 02
phase: 35
title: Test File Updates — BusKind.IPC_PEER
wave: 2
depends_on: [01]
requirements_addressed: [WIRE-01]
files_modified:
  - packages/core/src/index.test.ts
  - packages/runtime/src/dispatch.test.ts
  - packages/services/src/notification-service.test.ts
autonomous: true
---

# Plan 35-02: Test File Updates — BusKind.IPC_PEER

## Objective

Update the three test files that still reference `BusKind.INTER_PANE` after Plan 35-01 completes. These are pure search-and-replace updates — no behavioral changes to tests, only identifier renaming.

---

## Task 35-02-01: Update packages/core/src/index.test.ts

<read_first>
- `packages/core/src/index.test.ts` — contains `BusKind.INTER_PANE` at lines 23 and 121
</read_first>

<action>
In `packages/core/src/index.test.ts`:

Replace all occurrences of `BusKind.INTER_PANE` with `BusKind.IPC_PEER`.

There are 2 occurrences:
1. Line 23: `expect(typeof BusKind.INTER_PANE).toBe('number');`
   → `expect(typeof BusKind.IPC_PEER).toBe('number');`

2. Line 121: `const _busKindVal: BusKindValue = BusKind.INTER_PANE;`
   → `const _busKindVal: BusKindValue = BusKind.IPC_PEER;`
</action>

<acceptance_criteria>
- `grep 'INTER_PANE' packages/core/src/index.test.ts` returns no results
- Both assertions still test valid properties of BusKind
</acceptance_criteria>

---

## Task 35-02-02: Update packages/runtime/src/dispatch.test.ts

<read_first>
- `packages/runtime/src/dispatch.test.ts` — 9 occurrences of `BusKind.INTER_PANE` at lines 234, 324, 355, 381, 458, 523, 533; 2 inline comments referencing "inter-pane" at lines 230, 233; 1 test description at line 319
</read_first>

<action>
In `packages/runtime/src/dispatch.test.ts`:

**Code replacements (9 occurrences of `BusKind.INTER_PANE`):**
Replace all occurrences of `BusKind.INTER_PANE` with `BusKind.IPC_PEER`.

Sites:
- Line 234: `runtime.handleMessage(WINDOW_ID, ['REQ', 'sub-buf', { kinds: [BusKind.INTER_PANE] }]);`
  → `{ kinds: [BusKind.IPC_PEER] }`
- Line 324: `kind: BusKind.INTER_PANE,` → `kind: BusKind.IPC_PEER,`
- Line 355: `{ kinds: [BusKind.INTER_PANE] }` → `{ kinds: [BusKind.IPC_PEER] }`
- Line 381: `{ kinds: [BusKind.INTER_PANE] }` → `{ kinds: [BusKind.IPC_PEER] }`
- Line 458: `kind: BusKind.INTER_PANE,` → `kind: BusKind.IPC_PEER,`
- Line 523: `{ kinds: [BusKind.INTER_PANE] }` → `{ kinds: [BusKind.IPC_PEER] }`
- Line 533: `expect(deliveredEvent.kind).toBe(BusKind.INTER_PANE)` → `.toBe(BusKind.IPC_PEER)`

**Comment updates:**
- Line 230 comment: `// Inject an inter-pane event into the buffer`
  → `// Inject an IPC-PEER event into the buffer`
- Line 233 comment: `// Subscribe with filter matching inter-pane events`
  → `// Subscribe with filter matching IPC-PEER events`

**Test description update:**
- Line 319: `it('sends OK true for inter-pane events', async () => {`
  → `it('sends OK true for IPC-PEER events', async () => {`
</action>

<acceptance_criteria>
- `grep 'INTER_PANE\|inter-pane' packages/runtime/src/dispatch.test.ts` returns no results
- Test descriptions reference "IPC-PEER" not "inter-pane"
</acceptance_criteria>

---

## Task 35-02-03: Update packages/services/src/notification-service.test.ts

<read_first>
- `packages/services/src/notification-service.test.ts` — `kind: BusKind.INTER_PANE` at line 20 in a test fixture
</read_first>

<action>
In `packages/services/src/notification-service.test.ts`:

Line 20: `kind: BusKind.INTER_PANE,`
→ `kind: BusKind.IPC_PEER,`
</action>

<acceptance_criteria>
- `grep 'INTER_PANE' packages/services/src/notification-service.test.ts` returns no results
</acceptance_criteria>

---

## Wave 2 Final Validation

After completing all tasks in this plan, run:

```bash
cd /home/sandwich/Develop/napplet
pnpm type-check 2>&1 | tail -5
```

Expected: All packages pass type-check with zero errors.

Then run the full negative grep to confirm zero remaining hits across all source files:
```bash
grep -r 'INTER_PANE\|INTER-PANE\|inter-pane' packages/ --include="*.ts" \
  --exclude-dir=node_modules --exclude-dir=dist
```
Expected: zero results.

This satisfies WIRE-01: "`BusKind.INTER_PANE` constant renamed to `BusKind.IPC_PEER` in `@napplet/core`; all consumers updated".
