---
phase: 27-demo-audit-correctness
plan: 02
title: "Make relay, inter-pane, signer, and state failures legible"
wave: 2
depends_on:
  - 01
files_modified:
  - apps/demo/src/acl-panel.ts
  - apps/demo/src/debugger.ts
  - apps/demo/src/sequence-diagram.ts
  - apps/demo/src/main.ts
  - apps/demo/napplets/chat/src/main.ts
  - apps/demo/napplets/bot/src/main.ts
autonomous: true
requirements:
  - DEMO-02
  - DEMO-03
must_haves:
  truths:
    - "apps/demo/src/acl-panel.ts uses path-accurate labels/tooltips instead of 'Read Shell' and 'Write Shell'"
    - "apps/demo/src/debugger.ts displays path-specific status for relay, inter-pane, signer, and state operations"
    - "apps/demo/src/sequence-diagram.ts distinguishes chat->bot inter-pane traffic from relay traffic rather than flattening them"
    - "apps/demo/napplets/chat/src/main.ts reports relay publish failures separately from bot/inter-pane outcomes"
    - "apps/demo/napplets/bot/src/main.ts reports chat-message receipt and response failures without implying relay publish semantics"
    - "A user can tell from the UI/debugger whether a failure came from ACL denial, runtime/service behavior, or demo wording"
  artifacts:
    - path: "apps/demo/src/acl-panel.ts"
      provides: "Corrected ACL wording and path hints"
      contains: "relay publish"
    - path: "apps/demo/src/debugger.ts"
      provides: "Path-aware diagnostics"
      contains: "state-read"
    - path: "apps/demo/src/sequence-diagram.ts"
      provides: "Separated path visualization"
      contains: "inter-pane"
  key_links:
    - from: "apps/demo/src/debugger.ts"
      to: "packages/runtime/src/enforce.ts"
      via: "Capability-path semantics"
      pattern: "relay:write|state:read|sign:event"
    - from: "apps/demo/napplets/chat/src/main.ts"
      to: "apps/demo/napplets/bot/src/main.ts"
      via: "chat:message / bot:response inter-pane flow"
      pattern: "chat:message|bot:response"
---

# Plan 27-02: Make Relay, Inter-Pane, Signer, and State Failures Legible

<objective>
Correct the demo's user-facing diagnosis layer so revoking a capability shows the affected path precisely, without conflating relay publish, inter-pane delivery, signer requests, or state operations. This plan is the main delivery vehicle for DEMO-02 and the visible part of DEMO-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-demo-audit-correctness/27-CONTEXT.md
@.planning/phases/27-demo-audit-correctness/27-RESEARCH.md

@apps/demo/src/acl-panel.ts
@apps/demo/src/debugger.ts
@apps/demo/src/sequence-diagram.ts
@apps/demo/src/main.ts
@apps/demo/src/shell-host.ts
@apps/demo/napplets/chat/src/main.ts
@apps/demo/napplets/bot/src/main.ts
@packages/runtime/src/enforce.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace stale ACL labels with path-accurate copy</name>
  <files>apps/demo/src/acl-panel.ts</files>
  <read_first>
    - apps/demo/src/acl-panel.ts
    - apps/demo/src/shell-host.ts
    - packages/runtime/src/enforce.ts
    - .planning/phases/27-demo-audit-correctness/27-CONTEXT.md
  </read_first>
  <action>
    Update `apps/demo/src/acl-panel.ts` so the capability labels and tooltips stop using the ambiguous "Shell" wording.

    Replace the current labels with wording that describes the actual affected behavior. Use exact phrases along these lines:
    - `relay:read` -> `Relay Subscribe`
    - `relay:write` -> `Relay Publish / Inter-Pane Send`
    - `sign:event` -> `Signer Requests`
    - `state:read` -> `State Read`
    - `state:write` -> `State Write`

    Update button titles and any debugger system messages emitted from ACL interactions so they reference the exact capability and its path meaning, e.g. `REVOKE relay:write (relay publish / inter-pane send) on chat`.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -q "Relay Publish / Inter-Pane Send" apps/demo/src/acl-panel.ts && grep -q "Signer Requests" apps/demo/src/acl-panel.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/demo/src/acl-panel.ts` does not contain `Read Shell` or `Write Shell`
    - `relay:write` is labeled with both relay publish and inter-pane send semantics
    - ACL-triggered system messages include the raw capability string and path meaning
  </acceptance_criteria>
  <done>ACL controls describe the real affected protocol paths</done>
</task>

<task type="auto">
  <name>Task 2: Add path-specific debugger and sequence-diagram classification</name>
  <files>apps/demo/src/debugger.ts, apps/demo/src/sequence-diagram.ts, apps/demo/src/main.ts</files>
  <read_first>
    - apps/demo/src/debugger.ts
    - apps/demo/src/sequence-diagram.ts
    - apps/demo/src/main.ts
    - apps/demo/src/shell-host.ts
    - packages/runtime/src/enforce.ts
  </read_first>
  <action>
    Extend the demo diagnostics so each message is grouped or labeled by an explicit protocol path.

    Update `apps/demo/src/debugger.ts` to classify entries into at least:
    - `relay-publish`
    - `relay-subscribe`
    - `inter-pane-send`
    - `inter-pane-receive`
    - `state-read`
    - `state-write`
    - `signer-request`
    - `signer-response`
    - `auth`

    Use these path names in the visible debugger detail text. Preserve the runtime denial text such as `denied: relay:write`, `denied: state:read`, or `denied: sign:event` instead of replacing it with vague summary text.

    Update `apps/demo/src/sequence-diagram.ts` so chat->bot `chat:message` and bot->chat `bot:response` are rendered as inter-pane traffic, not generic shell traffic. Ensure relay traffic keeps a distinct label so the same user action can visibly produce both an inter-pane success and a relay failure.

    Update `apps/demo/src/main.ts` only as needed to feed the debugger/diagram the extra path metadata or status messaging.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -q "relay-publish" apps/demo/src/debugger.ts && grep -q "inter-pane" apps/demo/src/sequence-diagram.ts && pnpm --filter @napplet/demo build</automated>
  </verify>
  <acceptance_criteria>
    - apps/demo/src/debugger.ts contains the exact path labels listed in this task
    - Debugger detail text preserves raw denial reasons from runtime responses
    - apps/demo/src/sequence-diagram.ts uses inter-pane-specific labels for `chat:message` and `bot:response`
    - `pnpm --filter @napplet/demo build` exits 0
  </acceptance_criteria>
  <done>Debugger and sequence view distinguish the real protocol paths behind a user action</done>
</task>

<task type="auto">
  <name>Task 3: Make chat and bot UI status messages path-aware</name>
  <files>apps/demo/napplets/chat/src/main.ts, apps/demo/napplets/bot/src/main.ts</files>
  <read_first>
    - apps/demo/napplets/chat/src/main.ts
    - apps/demo/napplets/bot/src/main.ts
    - apps/demo/src/debugger.ts
    - packages/runtime/src/enforce.ts
  </read_first>
  <action>
    Update the demo napplet status messaging so chat and bot stop implying a single combined success/failure path.

    In `apps/demo/napplets/chat/src/main.ts`:
    - keep the current `emit('chat:message', ...)` followed by `publish(...)` ordering unless a smaller correctness fix requires otherwise
    - add explicit user-visible/system messages that distinguish:
      - inter-pane send attempted
      - relay publish failed
      - relay subscribe failed
      - state history load/save failed
    - when relay publish fails, the text must not imply the bot failed to receive the message if the inter-pane path already succeeded

    In `apps/demo/napplets/bot/src/main.ts`:
    - log receipt of `chat:message` as inter-pane input
    - log response failures as either inter-pane response failure or signer/state-related failure when known
    - avoid wording that suggests the bot response came from relay publish
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -q "relay publish failed" apps/demo/napplets/chat/src/main.ts && grep -q "inter-pane" apps/demo/napplets/bot/src/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - Chat UI text separates inter-pane and relay outcomes
    - Bot UI text explicitly refers to `chat:message`/inter-pane receipt rather than generic shell receipt
    - No status message claims a full send failure when only relay publish failed
  </acceptance_criteria>
  <done>Chat and bot surfaces now explain the reported ACL confusion instead of masking it</done>
</task>

</tasks>

<verify>
  <summary>
    Build the demo and run at least one end-to-end scenario where chat loses `relay:write`. Confirm the UI shows bot inter-pane behavior separately from relay denial, and that debugger/sequence views preserve the exact denied capability string.
  </summary>
  <automated>
    cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/demo build
    cd /home/sandwich/Develop/napplet && pnpm playwright test tests/e2e/demo-audit-correctness.spec.ts
  </automated>
  <manual>
    Revoke chat `relay:write`, send a message, and confirm the demo shows `chat:message` inter-pane traffic separately from any relay publish denial.
  </manual>
</verify>
