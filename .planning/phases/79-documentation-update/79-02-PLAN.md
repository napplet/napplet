---
phase: 79-documentation-update
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sdk/README.md
  - README.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "@napplet/sdk README documents NUB message type re-exports from nub packages"
    - "@napplet/sdk README documents window.napplet.shell.supports() and the shell namespace"
    - "@napplet/sdk README documents RELAY_DOMAIN, SIGNER_DOMAIN, STORAGE_DOMAIN, IFC_DOMAIN exports"
    - "Root README architecture diagram reflects JSON envelope wire format (not NIP-01)"
    - "Root README packages table includes packages/nubs/ NUB packages"
    - "Root README postMessage diagram shows JSON envelope objects"
  artifacts:
    - path: "packages/sdk/README.md"
      provides: "SDK package API docs with NUB re-exports"
      contains: "RelayNubMessage"
    - path: "README.md"
      provides: "Monorepo root docs with NUB architecture"
      contains: "JSON envelope"
  key_links:
    - from: "packages/sdk/README.md"
      to: "packages/sdk/src/index.ts"
      via: "NUB type re-exports, domain constants"
      pattern: "RELAY_DOMAIN"
    - from: "README.md"
      to: "packages/nubs/"
      via: "NUB packages listed in packages table"
      pattern: "nub-relay"
---

<objective>
Rewrite @napplet/sdk and root README for the JSON envelope + modular NUB architecture introduced in Phases 74-78.

Purpose: The SDK README references "kind 29003" IPC events and NIP-01 subscription language throughout. It does not document the NUB type re-exports (RelayNubMessage, SignerNubMessage, etc.) or domain constants (RELAY_DOMAIN etc.) that were added in Phase 78. The root README's architecture diagram still shows NIP-01 wire format and is missing the packages/nubs/ directory.

Output: Two updated README files. These can be executed in parallel with Plan 01 as they touch different files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Source files to read before writing docs
@packages/sdk/src/index.ts

<interfaces>
<!-- What @napplet/sdk actually exports after Phase 78 -->

From packages/sdk/src/index.ts:

Named object exports:
  relay: { subscribe(), publish(), query() }
  ipc: { emit(), on() }
  services: { list(), has() }
  storage: { getItem(), setItem(), removeItem(), keys() }

Type re-exports from @napplet/core:
  NostrEvent, NostrFilter, ServiceInfo, Subscription, EventTemplate,
  NappletMessage, NubDomain, ShellSupports, NUB_DOMAINS

NUB Message Type re-exports:
  From @napplet/nub-relay: RelayMessage, RelaySubscribeMessage, RelayCloseMessage,
    RelayPublishMessage, RelayQueryMessage, RelayEventMessage, RelayEoseMessage,
    RelayClosedMessage, RelayPublishResultMessage, RelayQueryResultMessage,
    RelayOutboundMessage, RelayInboundMessage, RelayNubMessage

  From @napplet/nub-signer: SignerMessage, SignerGetPublicKeyMessage, SignerSignEventMessage,
    SignerGetRelaysMessage, SignerNip04EncryptMessage, SignerNip04DecryptMessage,
    SignerNip44EncryptMessage, SignerNip44DecryptMessage,
    SignerGetPublicKeyResultMessage, SignerSignEventResultMessage, SignerGetRelaysResultMessage,
    SignerNip04EncryptResultMessage, SignerNip04DecryptResultMessage,
    SignerNip44EncryptResultMessage, SignerNip44DecryptResultMessage,
    SignerRequestMessage, SignerResultMessage, SignerNubMessage

  From @napplet/nub-storage: StorageMessage, StorageGetMessage, StorageSetMessage,
    StorageRemoveMessage, StorageKeysMessage, StorageGetResultMessage, StorageSetResultMessage,
    StorageRemoveResultMessage, StorageKeysResultMessage,
    StorageRequestMessage, StorageResultMessage, StorageNubMessage

  From @napplet/nub-ifc: IfcMessage, IfcEmitMessage, IfcSubscribeMessage,
    IfcSubscribeResultMessage, IfcUnsubscribeMessage, IfcEventMessage,
    IfcChannelOpenMessage, IfcChannelOpenResultMessage, IfcChannelEmitMessage,
    IfcChannelEventMessage, IfcChannelBroadcastMessage, IfcChannelListMessage,
    IfcChannelListResultMessage, IfcChannelCloseMessage, IfcChannelClosedMessage,
    IfcTopicMessage, IfcChannelMessage, IfcOutboundMessage, IfcInboundMessage, IfcNubMessage

Domain constant re-exports:
  RELAY_DOMAIN (from @napplet/nub-relay, value: 'relay')
  SIGNER_DOMAIN (from @napplet/nub-signer, value: 'signer')
  DESTRUCTIVE_KINDS (from @napplet/nub-signer)
  STORAGE_DOMAIN (from @napplet/nub-storage, value: 'storage')
  IFC_DOMAIN (from @napplet/nub-ifc, value: 'ifc')

NUB packages (in packages/nubs/):
  @napplet/nub-relay   — relay domain message types
  @napplet/nub-signer  — signer domain message types
  @napplet/nub-storage — storage domain message types
  @napplet/nub-ifc     — inter-frame communication message types
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite @napplet/sdk README</name>
  <files>packages/sdk/README.md</files>
  <action>
Rewrite packages/sdk/README.md to reflect v0.16.0 state. Key changes:

1. **API Reference — relay section**: Update method descriptions. Remove "NIP-01 subscription" language. Say "relay subscription via JSON envelope" or simply "relay subscription through the shell's relay pool". In the table row for subscribe: "Open a live relay subscription" (not "NIP-01 subscription").

2. **API Reference — ipc section**: Update to reflect JSON envelope transport. emit() sends `{ type: 'ifc.emit', topic, payload }` via shell; on() receives `{ type: 'ifc.event', topic, payload, sender }` from shell. Remove "kind 29003" references. Table description for emit: "Broadcast an IFC event to other napplets" (not "kind 29003 event").

3. **Add shell namespace section** after services (mirrors window.napplet.shell):
```
### `shell`

Shell capability query interface.

| Method | Returns | Description |
|--------|---------|-------------|
| `supports(capability)` | `boolean` | Check whether the shell supports a NUB domain or sandbox permission |
```
Note: The SDK does not currently export a top-level `shell` object — this is accessed via `window.napplet.shell.supports()` directly or through the shim. Add a note: "Access via `window.napplet.shell.supports('relay')` after importing @napplet/shim."

4. **Types section** — expand to include new exports:
```ts
import type {
  // Protocol types (from @napplet/core)
  NostrEvent, NostrFilter, ServiceInfo, Subscription, EventTemplate,
  NappletMessage, NubDomain, ShellSupports,
  // NUB message types (re-exported from NUB packages)
  RelayNubMessage, SignerNubMessage, StorageNubMessage, IfcNubMessage,
} from '@napplet/sdk';
```
Add a table section for NUB message types:
| Type | NUB Package | Description |
|------|-------------|-------------|
| `RelayNubMessage` | `@napplet/nub-relay` | Discriminated union of all relay domain messages |
| `SignerNubMessage` | `@napplet/nub-signer` | Discriminated union of all signer domain messages |
| `StorageNubMessage` | `@napplet/nub-storage` | Discriminated union of all storage domain messages |
| `IfcNubMessage` | `@napplet/nub-ifc` | Discriminated union of all IFC domain messages |

5. **Add NUB Domain Constants section** after Types:
```ts
import { RELAY_DOMAIN, SIGNER_DOMAIN, STORAGE_DOMAIN, IFC_DOMAIN } from '@napplet/sdk';
// Values: 'relay', 'signer', 'storage', 'ifc'
```
These are re-exported from the individual NUB packages. Useful for conditional logic: `if (window.napplet.shell.supports(RELAY_DOMAIN)) { ... }`.

6. **SDK vs Shim table** — no substantive changes needed; the comparison remains accurate.

Do NOT change: Installation, Quick Start code, Runtime Guard section, License. Keep existing prose style.
  </action>
  <verify>
    <automated>grep -n "RelayNubMessage\|RELAY_DOMAIN\|ifc\.emit\|shell\|NUB" packages/sdk/README.md | wc -l</automated>
  </verify>
  <done>
- "RelayNubMessage" appears in Types section
- "RELAY_DOMAIN" appears in domain constants section
- "ifc.emit" appears (replacing "kind 29003") in ipc section
- grep count returns >= 6 lines matching
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Root README</name>
  <files>README.md</files>
  <action>
Rewrite README.md to reflect the v0.16.0 package structure and JSON envelope wire format. Key changes:

1. **Opening paragraph** — update: "The shell and napplet communicate over postMessage using a JSON envelope format (`{ type, ...payload }`) defined by NIP-5D." Remove "NIP-01 wire format" from the description.

2. **Packages table** — update @napplet/core description and add NUB packages:
| Package | npm | Description |
|---------|-----|-------------|
| [@napplet/core](packages/core) | `@napplet/core` | JSON envelope types (NappletMessage, NubDomain), NUB dispatch infrastructure (registerNub, dispatch), protocol constants and Nostr types. Imported by all other packages. |
| [@napplet/shim](packages/shim) | `@napplet/shim` | Side-effect-only window installer. Importing `@napplet/shim` installs the `window.napplet` global and registers with the shell. Sends JSON envelope messages via postMessage. Zero named exports. |
| [@napplet/sdk](packages/sdk) | `@napplet/sdk` | Named TypeScript exports wrapping `window.napplet` for bundler consumers. Provides `relay`, `ipc`, `services`, `storage` objects plus NUB message type re-exports. |
| @napplet/nub-relay | `@napplet/nub-relay` | Relay NUB: typed JSON envelope message definitions for relay proxy operations (subscribe, publish, query). |
| @napplet/nub-signer | `@napplet/nub-signer` | Signer NUB: typed message definitions for NIP-07/NIP-44 signing delegation. |
| @napplet/nub-storage | `@napplet/nub-storage` | Storage NUB: typed message definitions for scoped key-value storage proxy. |
| @napplet/nub-ifc | `@napplet/nub-ifc` | IFC NUB: typed message definitions for inter-frame communication (topic pub/sub and named channels). |
| [@napplet/vite-plugin](packages/vite-plugin) | `@napplet/vite-plugin` | Vite plugin for NIP-5D manifest generation. Computes per-file SHA-256 hashes, signs a kind 35128 manifest event, and injects `requires` meta tags. |

3. **Architecture — Package Dependency Graph** — update:
```
@napplet/shim ──► @napplet/core
                ──► @napplet/nub-signer (types)
                ──► @napplet/nub-ifc    (types)
@napplet/sdk  ──► @napplet/core
                ──► @napplet/nub-relay   (types)
                ──► @napplet/nub-signer  (types)
                ──► @napplet/nub-storage (types)
                ──► @napplet/nub-ifc     (types)

@napplet/nub-relay   ──► @napplet/core
@napplet/nub-signer  ──► @napplet/core
@napplet/nub-storage ──► @napplet/core
@napplet/nub-ifc     ──► @napplet/core

@napplet/vite-plugin  (build-time only, depends on nostr-tools)
```

4. **Architecture — Napplet-Side Communication diagram** — replace NIP-01 wire format description with JSON envelope:
```
Shell (host page)                          Napplet (sandboxed iframe)
──────────────────────────                 ──────────────────────────
@kehto/shell (or any shell)                @napplet/shim
  ShellBridge                                window.napplet.relay (subscribe/publish/query)
  ├── JSON envelope message routing          window.napplet.ipc   (emit/on)
  ├── Identity via message.source            window.napplet.storage (get/set/remove)
  ├── ACL enforcement                        window.napplet.shell.supports(domain)
  ├── NUB dispatch (relay/signer/storage)    window.nostr (NIP-07 proxy)
  └── IFC routing

◄────────── postMessage: { type: 'relay.subscribe', id, filters } ──────────►
◄────────── postMessage: { type: 'relay.event', subId, event }    ──────────►

@napplet/vite-plugin (build time)
  └── NIP-5D manifest generation + requires tag injection
```

5. Keep: Origin section, Development commands, Publishing, Related links, License.

Do NOT add NIP-01 references in the new text.
  </action>
  <verify>
    <automated>grep -n "JSON envelope\|nub-relay\|nub-signer\|nub-storage\|nub-ifc\|NappletMessage\|NUB" README.md | wc -l</automated>
  </verify>
  <done>
- "JSON envelope" appears in opening paragraph and architecture section
- "nub-relay", "nub-signer", "nub-storage", "nub-ifc" appear in packages table
- "NappletMessage" or "NUB" appears in core description
- grep count returns >= 8 lines matching
  </done>
</task>

</tasks>

<verification>
Spot-check grep assertions:
- grep "RelayNubMessage" packages/sdk/README.md returns results
- grep "RELAY_DOMAIN" packages/sdk/README.md returns results
- grep "JSON envelope" README.md returns results
- grep "nub-relay" README.md returns results
- grep "NIP-01 wire format" README.md returns 0 results (replaced by JSON envelope)
</verification>

<success_criteria>
- packages/sdk/README.md: NUB message type re-exports (RelayNubMessage etc.) documented
- packages/sdk/README.md: RELAY_DOMAIN, SIGNER_DOMAIN domain constants documented
- packages/sdk/README.md: IPC section removed "kind 29003" references
- README.md: packages/nubs/ NUB packages listed in packages table
- README.md: architecture diagram shows JSON envelope wire format
- README.md: @napplet/core description updated to mention NappletMessage and NUB dispatch
</success_criteria>

<output>
After completion, create `.planning/phases/79-documentation-update/79-02-SUMMARY.md`
</output>
