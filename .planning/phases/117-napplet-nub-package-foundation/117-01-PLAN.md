---
phase: 117-napplet-nub-package-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/nub/package.json
  - packages/nub/tsconfig.json
  - packages/nub/tsup.config.ts
  - packages/nub/src/.gitkeep
autonomous: true
requirements:
  - PKG-02
  - PKG-03
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04

must_haves:
  truths:
    - "packages/nub/ directory exists with package.json, tsconfig.json, tsup.config.ts"
    - "packages/nub/package.json declares @napplet/core as the sole runtime dep"
    - "packages/nub/package.json carries json-schema-to-ts as optional peerDep (peerDependenciesMeta.json-schema-to-ts.optional: true)"
    - "packages/nub/package.json sets sideEffects: false"
    - "packages/nub/package.json exports map contains all 36 entry points (9 barrels + 27 granular) with types + import conditions"
    - "packages/nub/package.json exports map has NO '.' entry — root import is unresolvable (EXP-04)"
    - "packages/nub/tsup.config.ts entry object enumerates all 36 entries keyed as <domain>/index | <domain>/types | <domain>/shim | <domain>/sdk"
    - "pnpm install picks up the new package (pnpm-workspace.yaml packages/* glob already matches packages/nub)"
  artifacts:
    - path: "packages/nub/package.json"
      provides: "Package metadata + 36-entry exports map (no root '.' export)"
      contains: "\"./relay\": {"
    - path: "packages/nub/tsconfig.json"
      provides: "Extends root tsconfig, rootDir: src, outDir: dist"
      contains: "\"extends\": \"../../tsconfig.json\""
    - path: "packages/nub/tsup.config.ts"
      provides: "36-entry tsup entry object mapping all subpath sources to dist/<domain>/<file>.js"
      contains: "'relay/index':"
  key_links:
    - from: "packages/nub/package.json exports map"
      to: "packages/nub/tsup.config.ts entry keys"
      via: "Each exports key './<domain>/...' expects dist/<domain>/<file>.js; tsup entry key '<domain>/<file>' produces that path"
      pattern: "dist/{relay,storage,ifc,keys,theme,media,notify,identity,config}/(index|types|shim|sdk)\\.(js|d\\.ts)"
    - from: "packages/nub/package.json dependencies"
      to: "@napplet/core workspace link"
      via: "\"@napplet/core\": \"workspace:*\""
      pattern: "\"@napplet/core\": \"workspace:\\*\""
---

<objective>
Scaffold the `@napplet/nub` package skeleton at `packages/nub/` with a fully-declared 36-entry `exports` map, matching tsup entry object, tsconfig extending the repo root, and an empty `src/` placeholder. This plan produces ZERO source code — the source files are copied in Plan 02, and the build is exercised in Plan 03.

Purpose: Establish the load-bearing package shell (metadata + build config + exports contract) that phases 118-121 depend on. The exports map must be complete and correct even though `dist/` does not yet exist — `pnpm install` only validates the package.json `exports` field shape, not runtime resolvability.

Output:
- `packages/nub/package.json` — full metadata, deps, 36-entry exports map, NO root `.` entry
- `packages/nub/tsconfig.json` — extends root, rootDir:src, outDir:dist
- `packages/nub/tsup.config.ts` — 36-entry `entry` object, esm + dts + sourcemap + clean
- `packages/nub/src/.gitkeep` — placeholder so git tracks the directory
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/117-napplet-nub-package-foundation/117-CONTEXT.md

# Reference files — existing NUB package templates to copy metadata shape from
@packages/nubs/relay/package.json
@packages/nubs/relay/tsup.config.ts
@packages/nubs/relay/tsconfig.json
@packages/nubs/config/package.json
@pnpm-workspace.yaml
@tsconfig.json

<interfaces>
<!-- Concrete shapes the executor must produce verbatim. No interpretation. -->

## packages/nub/package.json — complete file

```json
{
  "name": "@napplet/nub",
  "version": "0.2.1",
  "description": "All 9 napplet NUB domains (relay, storage, ifc, keys, theme, media, notify, identity, config) as layered subpath exports",
  "type": "module",
  "exports": {
    "./relay": {
      "types": "./dist/relay/index.d.ts",
      "import": "./dist/relay/index.js"
    },
    "./relay/types": {
      "types": "./dist/relay/types.d.ts",
      "import": "./dist/relay/types.js"
    },
    "./relay/shim": {
      "types": "./dist/relay/shim.d.ts",
      "import": "./dist/relay/shim.js"
    },
    "./relay/sdk": {
      "types": "./dist/relay/sdk.d.ts",
      "import": "./dist/relay/sdk.js"
    },
    "./storage": {
      "types": "./dist/storage/index.d.ts",
      "import": "./dist/storage/index.js"
    },
    "./storage/types": {
      "types": "./dist/storage/types.d.ts",
      "import": "./dist/storage/types.js"
    },
    "./storage/shim": {
      "types": "./dist/storage/shim.d.ts",
      "import": "./dist/storage/shim.js"
    },
    "./storage/sdk": {
      "types": "./dist/storage/sdk.d.ts",
      "import": "./dist/storage/sdk.js"
    },
    "./ifc": {
      "types": "./dist/ifc/index.d.ts",
      "import": "./dist/ifc/index.js"
    },
    "./ifc/types": {
      "types": "./dist/ifc/types.d.ts",
      "import": "./dist/ifc/types.js"
    },
    "./ifc/shim": {
      "types": "./dist/ifc/shim.d.ts",
      "import": "./dist/ifc/shim.js"
    },
    "./ifc/sdk": {
      "types": "./dist/ifc/sdk.d.ts",
      "import": "./dist/ifc/sdk.js"
    },
    "./keys": {
      "types": "./dist/keys/index.d.ts",
      "import": "./dist/keys/index.js"
    },
    "./keys/types": {
      "types": "./dist/keys/types.d.ts",
      "import": "./dist/keys/types.js"
    },
    "./keys/shim": {
      "types": "./dist/keys/shim.d.ts",
      "import": "./dist/keys/shim.js"
    },
    "./keys/sdk": {
      "types": "./dist/keys/sdk.d.ts",
      "import": "./dist/keys/sdk.js"
    },
    "./theme": {
      "types": "./dist/theme/index.d.ts",
      "import": "./dist/theme/index.js"
    },
    "./theme/types": {
      "types": "./dist/theme/types.d.ts",
      "import": "./dist/theme/types.js"
    },
    "./theme/shim": {
      "types": "./dist/theme/shim.d.ts",
      "import": "./dist/theme/shim.js"
    },
    "./theme/sdk": {
      "types": "./dist/theme/sdk.d.ts",
      "import": "./dist/theme/sdk.js"
    },
    "./media": {
      "types": "./dist/media/index.d.ts",
      "import": "./dist/media/index.js"
    },
    "./media/types": {
      "types": "./dist/media/types.d.ts",
      "import": "./dist/media/types.js"
    },
    "./media/shim": {
      "types": "./dist/media/shim.d.ts",
      "import": "./dist/media/shim.js"
    },
    "./media/sdk": {
      "types": "./dist/media/sdk.d.ts",
      "import": "./dist/media/sdk.js"
    },
    "./notify": {
      "types": "./dist/notify/index.d.ts",
      "import": "./dist/notify/index.js"
    },
    "./notify/types": {
      "types": "./dist/notify/types.d.ts",
      "import": "./dist/notify/types.js"
    },
    "./notify/shim": {
      "types": "./dist/notify/shim.d.ts",
      "import": "./dist/notify/shim.js"
    },
    "./notify/sdk": {
      "types": "./dist/notify/sdk.d.ts",
      "import": "./dist/notify/sdk.js"
    },
    "./identity": {
      "types": "./dist/identity/index.d.ts",
      "import": "./dist/identity/index.js"
    },
    "./identity/types": {
      "types": "./dist/identity/types.d.ts",
      "import": "./dist/identity/types.js"
    },
    "./identity/shim": {
      "types": "./dist/identity/shim.d.ts",
      "import": "./dist/identity/shim.js"
    },
    "./identity/sdk": {
      "types": "./dist/identity/sdk.d.ts",
      "import": "./dist/identity/sdk.js"
    },
    "./config": {
      "types": "./dist/config/index.d.ts",
      "import": "./dist/config/index.js"
    },
    "./config/types": {
      "types": "./dist/config/types.d.ts",
      "import": "./dist/config/types.js"
    },
    "./config/shim": {
      "types": "./dist/config/shim.d.ts",
      "import": "./dist/config/shim.js"
    },
    "./config/sdk": {
      "types": "./dist/config/sdk.d.ts",
      "import": "./dist/config/sdk.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@napplet/core": "workspace:*"
  },
  "devDependencies": {
    "@types/json-schema": "^7.0.15",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "json-schema-to-ts": "^3.1.1"
  },
  "peerDependenciesMeta": {
    "json-schema-to-ts": {
      "optional": true
    }
  },
  "scripts": {
    "build": "tsup",
    "type-check": "tsc --noEmit"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sandwichfarm/napplet.git",
    "directory": "packages/nub"
  },
  "keywords": [
    "nostr",
    "napplet",
    "nub"
  ]
}
```

## packages/nub/tsconfig.json — complete file

```json
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["src"]
}
```

## packages/nub/tsup.config.ts — complete file

```ts
import { defineConfig } from 'tsup';

export default defineConfig({
  entry: {
    'relay/index': 'src/relay/index.ts',
    'relay/types': 'src/relay/types.ts',
    'relay/shim': 'src/relay/shim.ts',
    'relay/sdk': 'src/relay/sdk.ts',
    'storage/index': 'src/storage/index.ts',
    'storage/types': 'src/storage/types.ts',
    'storage/shim': 'src/storage/shim.ts',
    'storage/sdk': 'src/storage/sdk.ts',
    'ifc/index': 'src/ifc/index.ts',
    'ifc/types': 'src/ifc/types.ts',
    'ifc/shim': 'src/ifc/shim.ts',
    'ifc/sdk': 'src/ifc/sdk.ts',
    'keys/index': 'src/keys/index.ts',
    'keys/types': 'src/keys/types.ts',
    'keys/shim': 'src/keys/shim.ts',
    'keys/sdk': 'src/keys/sdk.ts',
    'theme/index': 'src/theme/index.ts',
    'theme/types': 'src/theme/types.ts',
    'theme/shim': 'src/theme/shim.ts',
    'theme/sdk': 'src/theme/sdk.ts',
    'media/index': 'src/media/index.ts',
    'media/types': 'src/media/types.ts',
    'media/shim': 'src/media/shim.ts',
    'media/sdk': 'src/media/sdk.ts',
    'notify/index': 'src/notify/index.ts',
    'notify/types': 'src/notify/types.ts',
    'notify/shim': 'src/notify/shim.ts',
    'notify/sdk': 'src/notify/sdk.ts',
    'identity/index': 'src/identity/index.ts',
    'identity/types': 'src/identity/types.ts',
    'identity/shim': 'src/identity/shim.ts',
    'identity/sdk': 'src/identity/sdk.ts',
    'config/index': 'src/config/index.ts',
    'config/types': 'src/config/types.ts',
    'config/shim': 'src/config/shim.ts',
    'config/sdk': 'src/config/sdk.ts',
  },
  format: ['esm'],
  dts: true,
  sourcemap: true,
  clean: true,
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold packages/nub/ with package.json + tsconfig.json + tsup.config.ts + empty src/</name>
  <files>packages/nub/package.json, packages/nub/tsconfig.json, packages/nub/tsup.config.ts, packages/nub/src/.gitkeep</files>
  <read_first>
    - .planning/phases/117-napplet-nub-package-foundation/117-CONTEXT.md (locked decisions and non-negotiables)
    - packages/nubs/relay/package.json (baseline NUB package.json metadata to match)
    - packages/nubs/config/package.json (source of optional peerDep + peerDependenciesMeta pattern)
    - packages/nubs/relay/tsconfig.json (baseline NUB tsconfig extends pattern — note "../../../tsconfig.json" is 3 levels up for nubs/<domain>/; this new package at packages/nub/ is 2 levels up, so use "../../tsconfig.json")
    - packages/nubs/relay/tsup.config.ts (baseline tsup shape — this package will extend with an entry object, not an entry array)
    - pnpm-workspace.yaml (verify packages/* glob covers packages/nub)
    - tsconfig.json (root tsconfig being extended)
  </read_first>
  <action>
Create the directory `packages/nub/` and write FOUR files. Every byte of content below is final — no interpretation needed.

**File 1 — `packages/nub/package.json`:** Write the exact JSON shown in the `<interfaces>` block above under "packages/nub/package.json — complete file". Verbatim. All 36 exports entries. NO `"."` key anywhere in the `exports` object. NO `"main"`, `"module"`, or `"types"` top-level fields (they would imply a root entry point; omitting them reinforces EXP-04). The `exports` map is the SOLE resolution surface.

**File 2 — `packages/nub/tsconfig.json`:** Write the exact JSON shown in the `<interfaces>` block above under "packages/nub/tsconfig.json — complete file". The `extends` path is `"../../tsconfig.json"` (two levels up: packages/nub/ → packages/ → repo root). NOT `"../../../tsconfig.json"` (that's for `packages/nubs/<domain>/` which is three levels deep).

**File 3 — `packages/nub/tsup.config.ts`:** Write the exact TypeScript shown in the `<interfaces>` block above under "packages/nub/tsup.config.ts — complete file". 36 entries total. Each key is `<domain>/<file>` where `<domain>` ∈ {relay, storage, ifc, keys, theme, media, notify, identity, config} and `<file>` ∈ {index, types, shim, sdk}. Each value is `src/<domain>/<file>.ts`. Order: the 9 domains in the order shown (relay, storage, ifc, keys, theme, media, notify, identity, config); for each domain the 4 files in the order index, types, shim, sdk.

**File 4 — `packages/nub/src/.gitkeep`:** Create an empty file. This keeps the directory tracked until Plan 02 populates it. Content: empty (zero bytes).

After writing the four files, run `pnpm install` from the repo root. pnpm must pick up the new workspace package because `pnpm-workspace.yaml` already contains `packages/*`. Expected log line: `+ @napplet/nub@0.2.1` or similar acknowledgement that the new workspace package is recognized.

DO NOT create any `src/<domain>/` subdirectories or `src/<domain>/*.ts` files — Plan 02 does that. DO NOT run `pnpm build` — Plan 03 does that.
  </action>
  <verify>
    <automated>test -f packages/nub/package.json &amp;&amp; test -f packages/nub/tsconfig.json &amp;&amp; test -f packages/nub/tsup.config.ts &amp;&amp; test -f packages/nub/src/.gitkeep &amp;&amp; node -e "const p=require('./packages/nub/package.json'); const e=p.exports; const keys=Object.keys(e); if(keys.length!==36) throw new Error('expected 36 exports, got '+keys.length); if('.' in e) throw new Error('EXP-04 violated: exports has . entry'); const domains=['relay','storage','ifc','keys','theme','media','notify','identity','config']; for(const d of domains){ for(const s of ['','/types','/shim','/sdk']){ const k='./'+d+s; if(!(k in e)) throw new Error('missing export: '+k); const v=e[k]; if(!v.types||!v.import) throw new Error('missing types/import condition: '+k); } } if(p.sideEffects!==false) throw new Error('sideEffects must be false'); if(p.dependencies['@napplet/core']!=='workspace:*') throw new Error('@napplet/core must be workspace:*'); if(p.peerDependencies['json-schema-to-ts']!=='^3.1.1') throw new Error('peerDep json-schema-to-ts missing'); if(p.peerDependenciesMeta['json-schema-to-ts'].optional!==true) throw new Error('json-schema-to-ts must be optional peerDep'); if(Object.keys(p.dependencies).length!==1) throw new Error('sole runtime dep must be @napplet/core'); console.log('OK');" &amp;&amp; pnpm install</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/nub/package.json` exists and parses as valid JSON
    - `packages/nub/package.json` `.name` === `"@napplet/nub"`
    - `packages/nub/package.json` `.version` === `"0.2.1"`
    - `packages/nub/package.json` `.type` === `"module"`
    - `packages/nub/package.json` `.sideEffects` === `false`
    - `packages/nub/package.json` `.dependencies` has exactly ONE key `"@napplet/core"` with value `"workspace:*"`
    - `packages/nub/package.json` `.peerDependencies["json-schema-to-ts"]` === `"^3.1.1"`
    - `packages/nub/package.json` `.peerDependenciesMeta["json-schema-to-ts"].optional` === `true`
    - `packages/nub/package.json` `.exports` contains EXACTLY 36 keys
    - `packages/nub/package.json` `.exports` contains the 9 barrel keys: `./relay`, `./storage`, `./ifc`, `./keys`, `./theme`, `./media`, `./notify`, `./identity`, `./config`
    - `packages/nub/package.json` `.exports` contains the 27 granular keys: `./{domain}/{types,shim,sdk}` for each of the 9 domains
    - `packages/nub/package.json` `.exports` does NOT contain a `"."` key (NEGATIVE assertion for EXP-04)
    - `packages/nub/package.json` does NOT contain top-level `"main"`, `"module"`, or `"types"` fields (would shadow exports)
    - Every export value is an object with BOTH `"types"` and `"import"` keys
    - Every `"types"` value matches `./dist/{domain}/{file}.d.ts` and every `"import"` value matches `./dist/{domain}/{file}.js`
    - File `packages/nub/tsconfig.json` exists and extends `"../../tsconfig.json"` (exactly, NOT three dots)
    - File `packages/nub/tsconfig.json` has `compilerOptions.outDir === "dist"` and `compilerOptions.rootDir === "src"`
    - File `packages/nub/tsup.config.ts` exists, uses `defineConfig` from `tsup`, and has an `entry` object with exactly 36 keys
    - File `packages/nub/tsup.config.ts` has `format: ['esm']`, `dts: true`, `sourcemap: true`, `clean: true`
    - Every tsup entry key matches the pattern `<domain>/<file>` and every entry value matches `src/<domain>/<file>.ts` for the 9 domains × 4 files
    - Directory `packages/nub/src/` exists (tracked via `.gitkeep`)
    - `pnpm install` exits 0 and recognizes `@napplet/nub` as a workspace package
    - No `packages/nub/src/<domain>/` subdirectories exist yet (Plan 02's scope)
  </acceptance_criteria>
  <done>`packages/nub/` exists on disk with the four scaffold files; `pnpm install` completes 0 errors; the `exports` map has exactly 36 entries, zero `.` entries, and every entry has paired `types` + `import` conditions; `tsup.config.ts` enumerates all 36 entry keys; the package is a recognized workspace member.</done>
</task>

</tasks>

<verification>
After this plan, run these checks manually:

1. `test -d packages/nub/src && test -f packages/nub/package.json && test -f packages/nub/tsconfig.json && test -f packages/nub/tsup.config.ts` — all four scaffold files exist
2. `node -e "const p=require('./packages/nub/package.json'); console.log(Object.keys(p.exports).length, '.' in p.exports)"` — must print `36 false`
3. `pnpm install` from repo root — exits 0, lists `@napplet/nub` as workspace package
4. `ls packages/nub/src/` — shows only `.gitkeep` (source population is Plan 02's job)
</verification>

<success_criteria>
- packages/nub/ directory and four scaffold files exist
- package.json declares @napplet/core as the sole runtime dep (PKG-02)
- package.json declares json-schema-to-ts as optional peerDep (PKG-02)
- package.json sets sideEffects: false (PKG-03)
- package.json exports map contains 36 entries (EXP-01: 9 barrels, EXP-02: 27 granular, EXP-03: both conditions present)
- package.json exports map has no "." entry (EXP-04)
- tsup.config.ts entry object enumerates 36 entry keys mirroring the exports map
- pnpm install succeeds and recognizes @napplet/nub as a workspace package
- No source files in packages/nub/src/ yet — Plan 02 adds them
</success_criteria>

<output>
After completion, create `.planning/phases/117-napplet-nub-package-foundation/117-01-SUMMARY.md` noting the four files written, the exact 36 exports-map entry count, the NO-root-export assertion, and confirmation that pnpm picked up the workspace package.
</output>
