---
phase: 59-channel-protocol-design
plan: 02
type: execute
wave: 2
depends_on:
  - 59-01
files_modified:
  - specs/nubs/README.md
  - specs/nubs/TEMPLATE-WORD.md
  - specs/nubs/TEMPLATE-NN.md
autonomous: true
requirements:
  - SIMP-04

must_haves:
  truths:
    - "NUB governance document explains the dual-track system (NUB-WORD interfaces and NUB-NN protocols)"
    - "NUB-WORD template exists for interface proposals"
    - "NUB-NN template exists for message protocol proposals"
    - "Governance follows NIP-style informal process per D-07"
  artifacts:
    - path: "specs/nubs/README.md"
      provides: "NUB governance and overview document"
      contains: "NUB-WORD"
    - path: "specs/nubs/TEMPLATE-WORD.md"
      provides: "Template for NUB-WORD interface proposals"
      contains: "window.napplet"
    - path: "specs/nubs/TEMPLATE-NN.md"
      provides: "Template for NUB-NN message protocol proposals"
      contains: "Event Semantics"
  key_links:
    - from: "specs/nubs/README.md"
      to: "specs/NIP-5D.md"
      via: "Back-reference to NIP-5D"
      pattern: "NIP-5D"
    - from: "specs/nubs/README.md"
      to: "specs/nubs/TEMPLATE-WORD.md"
      via: "Template reference"
      pattern: "TEMPLATE-WORD"
---

<objective>
Create the NUB (Napplet Unified Blueprint) framework documents: governance README, interface proposal template (NUB-WORD), and message protocol proposal template (NUB-NN).

Purpose: Establish the extension mechanism that NIP-5D v2 references. This framework allows interface specs (relay, storage, signer, etc.) and message protocol specs (feed, chat, etc.) to evolve independently of the core NIP.

Output: specs/nubs/ directory with README.md, TEMPLATE-WORD.md, TEMPLATE-NN.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-channel-protocol-design/59-CONTEXT.md
@specs/NIP-5D.md

<interfaces>
<!-- From NIP-5D v2 (created in Plan 01), the NUB Extension Framework section references: -->
<!-- - NUB-WORD (interfaces): NUB-RELAY, NUB-STORAGE, NUB-SIGNER, NUB-NOSTRDB, NUB-IPC, NUB-PIPES -->
<!-- - NUB-NN (message protocols): numbered, competing allowed -->
<!-- - github.com/napplets as the home for NUB proposals -->
<!-- These documents must match what the NIP says. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NUB governance README</name>
  <files>specs/nubs/README.md</files>
  <read_first>specs/NIP-5D.md, .planning/phases/59-channel-protocol-design/59-CONTEXT.md</read_first>
  <action>
Create the directory specs/nubs/ and write specs/nubs/README.md as the NUB governance document.

The document must cover (per D-05, D-06, D-07):

**Title:** "NUB: Napplet Unified Blueprints"

**Overview section (~5 lines):**
NUBs extend [NIP-5D](../NIP-5D.md) with interface and protocol specifications for the napplet ecosystem. The core NIP defines transport, authentication, and security. Everything else -- relay access, storage, signing, IPC, pipes, message protocols -- is a NUB.

**Two Tracks section (~20 lines):**
Per D-05 and D-06:

1. **NUB-WORD (Interface Specs):** Named by a single uppercase word. One canonical spec per name. Defines shell-provided API contracts -- what shows up on `window.napplet.*` or `window.nostr` or `window.nostrdb`. The shell implements these; napplets consume them. Discovery: `shell.supports("NUB-RELAY")`.

   Current interface names:
   | NUB ID | Namespace | Description | Status |
   |--------|-----------|-------------|--------|
   | NUB-RELAY | window.napplet.relay | NIP-01 relay proxy | Draft |
   | NUB-STORAGE | window.napplet.storage | Scoped key-value storage | Draft |
   | NUB-SIGNER | window.nostr | NIP-07 signer proxy | Draft |
   | NUB-NOSTRDB | window.nostrdb | Local event database | Draft |
   | NUB-IPC | window.napplet.ipc | Inter-napplet pub/sub | Draft |
   | NUB-PIPES | window.napplet.pipes | Authenticated point-to-point connections | Draft |

2. **NUB-NN (Message Protocol Specs):** Numbered sequentially (NUB-01, NUB-02, etc.). Multiple competing specs allowed per domain. Defines event semantics -- what napplets agree on with each other. Napplets negotiate via `shell.supports("NUB-RELAY", "NUB-02")`. Example domains: feed rendering, chat, collaborative editing.

**Boundary Rule section (~5 lines):**
Per D-06: An interface (NUB-WORD) is shell-provided AND defines an API surface. A protocol (NUB-NN) is napplet-agreed AND defines event semantics. Both criteria apply. Edge cases are judged pragmatically.

**Governance section (~10 lines):**
Per D-07: NIP-style informal process.
- Fork this repo, add a markdown file following the appropriate template, open a PR.
- Community discusses via PR comments.
- Maintainer (dskvr) merges when the spec makes sense and has at least one implementation.
- No formal stages, review committees, or voting.
- NUB-WORD names are first-come-first-served but must be approved by maintainer.
- NUB-NN numbers are assigned sequentially on merge.

**Templates section (~3 lines):**
- Interface proposals: Use [TEMPLATE-WORD.md](TEMPLATE-WORD.md)
- Protocol proposals: Use [TEMPLATE-NN.md](TEMPLATE-NN.md)

**Reference section (~3 lines):**
- Core protocol: [NIP-5D](../NIP-5D.md)
- Reference implementation: [@napplet packages](https://github.com/sandwichfarm/napplet)
- Reference shell: [hyprgate](https://github.com/sandwichfarm/hyprgate)

Keep the document concise. Target ~80 lines total. This is a governance doc, not a tutorial.
  </action>
  <verify>
    <automated>test -f specs/nubs/README.md && echo "PASS: README exists" && wc -l specs/nubs/README.md</automated>
  </verify>
  <acceptance_criteria>
    - specs/nubs/README.md exists
    - grep "NUB-WORD" specs/nubs/README.md returns a match
    - grep "NUB-NN" specs/nubs/README.md returns a match
    - grep "NUB-RELAY" specs/nubs/README.md returns a match (interface table)
    - grep "NUB-STORAGE" specs/nubs/README.md returns a match
    - grep "NUB-SIGNER" specs/nubs/README.md returns a match
    - grep "NUB-NOSTRDB" specs/nubs/README.md returns a match
    - grep "NUB-IPC" specs/nubs/README.md returns a match
    - grep "NUB-PIPES" specs/nubs/README.md returns a match
    - grep "shell.supports" specs/nubs/README.md returns a match
    - grep "NIP-5D" specs/nubs/README.md returns a match (back-reference)
    - grep "TEMPLATE-WORD" specs/nubs/README.md returns a match
    - grep "TEMPLATE-NN" specs/nubs/README.md returns a match
    - grep "dskvr" specs/nubs/README.md returns a match (maintainer per D-07)
  </acceptance_criteria>
  <done>NUB governance README exists with dual-track explanation, interface table listing all 6 NUB-WORD specs, governance rules, and template references.</done>
</task>

<task type="auto">
  <name>Task 2: Create NUB proposal templates</name>
  <files>specs/nubs/TEMPLATE-WORD.md, specs/nubs/TEMPLATE-NN.md</files>
  <read_first>specs/nubs/README.md, .planning/phases/59-channel-protocol-design/59-CONTEXT.md</read_first>
  <action>
Create two template files that NUB proposal authors fill in.

**specs/nubs/TEMPLATE-WORD.md** (Interface Proposal Template):

```markdown
NUB-{NAME}
==========

{Title}
-------

`draft`

**NUB ID:** NUB-{NAME}
**Namespace:** `window.napplet.{name}` (or `window.nostr`, `window.nostrdb`)
**Discovery:** `shell.supports("NUB-{NAME}")`

## Description

{One paragraph: what this interface provides and why.}

## API Surface

{Method signatures with brief descriptions. Use markdown code blocks for types.}

```
interface {Name} {
  method(param: type): ReturnType;
}
```

## Shell Behavior

{What the shell MUST and MAY do when providing this interface.}

## Event Kinds

{If this interface uses postMessage bus kinds, define them here.}

| Kind | Name | Direction | Description |
|------|------|-----------|-------------|
| {kind} | {NAME} | {napplet->shell / shell->napplet} | {description} |

## Security Considerations

{Interface-specific security requirements.}

## Implementations

- {links to implementations}
```

**specs/nubs/TEMPLATE-NN.md** (Message Protocol Template):

```markdown
NUB-{NN}
========

{Title}
-------

`draft`

**NUB ID:** NUB-{NN}
**Domain:** {e.g., feed rendering, chat, collaborative editing}
**Requires:** {NUB-WORD interfaces needed, e.g., NUB-RELAY, NUB-IPC}
**Discovery:** `shell.supports("NUB-{WORD}", "NUB-{NN}")`

## Description

{One paragraph: what event semantics this protocol defines and for what purpose.}

## Event Semantics

{Define the event kinds, tags, and content structure that participating napplets agree on.}

### Event: {name}

```json
{
  "kind": {kind},
  "tags": [{tag structure}],
  "content": "{content structure}"
}
```

{Behavioral requirements for producers and consumers.}

## Negotiation

{How napplets discover peers that support this protocol.}

## Implementations

- {links to implementations}
```

Both templates should use setext headings (matching NIP convention). Keep them minimal -- 40-60 lines each. The placeholder text in curly braces tells authors what to fill in.
  </action>
  <verify>
    <automated>test -f specs/nubs/TEMPLATE-WORD.md && test -f specs/nubs/TEMPLATE-NN.md && echo "PASS: Both templates exist" && wc -l specs/nubs/TEMPLATE-WORD.md specs/nubs/TEMPLATE-NN.md</automated>
  </verify>
  <acceptance_criteria>
    - specs/nubs/TEMPLATE-WORD.md exists
    - specs/nubs/TEMPLATE-NN.md exists
    - grep "NUB-{NAME}" specs/nubs/TEMPLATE-WORD.md returns a match (placeholder)
    - grep "window.napplet" specs/nubs/TEMPLATE-WORD.md returns a match
    - grep "shell.supports" specs/nubs/TEMPLATE-WORD.md returns a match
    - grep "API Surface" specs/nubs/TEMPLATE-WORD.md returns a match
    - grep "Shell Behavior" specs/nubs/TEMPLATE-WORD.md returns a match
    - grep "NUB-{NN}" specs/nubs/TEMPLATE-NN.md returns a match (placeholder)
    - grep "Event Semantics" specs/nubs/TEMPLATE-NN.md returns a match
    - grep "Negotiation" specs/nubs/TEMPLATE-NN.md returns a match
    - grep "Requires:" specs/nubs/TEMPLATE-NN.md returns a match (interface dependency)
  </acceptance_criteria>
  <done>Both NUB proposal templates exist with appropriate sections. TEMPLATE-WORD covers interface proposals (namespace, API surface, shell behavior, event kinds). TEMPLATE-NN covers message protocol proposals (event semantics, negotiation, interface dependencies).</done>
</task>

</tasks>

<verification>
1. `ls specs/nubs/` shows README.md, TEMPLATE-WORD.md, TEMPLATE-NN.md
2. `grep "NUB-WORD\|NUB-NN" specs/nubs/README.md | wc -l` returns >= 2
3. `grep -l "shell.supports" specs/nubs/README.md specs/nubs/TEMPLATE-WORD.md` returns both files
4. All 6 interface names appear in README.md: NUB-RELAY, NUB-STORAGE, NUB-SIGNER, NUB-NOSTRDB, NUB-IPC, NUB-PIPES
</verification>

<success_criteria>
- NUB governance document exists at specs/nubs/README.md with dual-track explanation
- NUB-WORD template exists for interface proposals
- NUB-NN template exists for message protocol proposals
- All 6 initial NUB-WORD names listed in governance table
- Governance follows NIP-style informal process (D-07)
- Documents reference NIP-5D and each other correctly
</success_criteria>

<output>
After completion, create `.planning/phases/59-channel-protocol-design/59-02-SUMMARY.md`
</output>
