---
phase: 7
plan: "07-02"
title: "Complete storage-to-state rename in tests"
wave: 1
depends_on: []
files_modified:
  - tests/e2e/storage-isolation.spec.ts -> tests/e2e/state-isolation.spec.ts
  - tests/e2e/acl-enforcement.spec.ts
requirements: [STA-01, STA-02, STA-03, STA-04]
autonomous: true
---

<objective>
Complete the storage-to-state rename by: (1) renaming storage-isolation.spec.ts to state-isolation.spec.ts, (2) fixing 2 remaining shell:storage-* topic strings in acl-enforcement.spec.ts, and (3) verifying all tests pass with new names. The source packages already use state:read/state:write and shell:state-* topics — only test files have remaining old references.
</objective>

<tasks>

<task id="07-02-01">
<title>Rename storage-isolation.spec.ts to state-isolation.spec.ts</title>
<read_first>
- tests/e2e/storage-isolation.spec.ts
</read_first>
<action>
Rename the test file:
`git mv tests/e2e/storage-isolation.spec.ts tests/e2e/state-isolation.spec.ts`

No content changes needed inside this file — it already uses `shell:state-*` topics, `state:read`/`state:write` capability strings, and `napp:state-response` topics throughout.

Verify internal content is already correct:
- `grep "shell:storage-" tests/e2e/state-isolation.spec.ts` should return 0 matches
- `grep "storage:read\|storage:write" tests/e2e/state-isolation.spec.ts` should return 0 matches
</action>
<acceptance_criteria>
- `tests/e2e/state-isolation.spec.ts` exists
- `tests/e2e/storage-isolation.spec.ts` does not exist
- `grep -c "shell:storage-" tests/e2e/state-isolation.spec.ts` returns 0
- `grep -c "storage:read\|storage:write" tests/e2e/state-isolation.spec.ts` returns 0
- `grep "shell:state-" tests/e2e/state-isolation.spec.ts` returns multiple matches (confirming content uses new names)
</acceptance_criteria>
</task>

<task id="07-02-02">
<title>Fix remaining shell:storage-* topic strings in acl-enforcement.spec.ts</title>
<read_first>
- tests/e2e/acl-enforcement.spec.ts
</read_first>
<action>
In `tests/e2e/acl-enforcement.spec.ts`, fix 2 remaining old topic strings:

1. Line 227 comment: `// Send a storage-get request` -> `// Send a state-get request`
2. Line 236: `tags: [['t', 'shell:storage-get'], ['key', 'test-key'], ['id', cid]],` -> `tags: [['t', 'shell:state-get'], ['key', 'test-key'], ['id', cid]],`
3. Line 278 comment: `// Send a storage-set request` -> `// Send a state-set request`
4. Line 287: `tags: [['t', 'shell:storage-set'], ['key', 'test-key'], ['value', 'test-val'], ['id', cid]],` -> `tags: [['t', 'shell:state-set'], ['key', 'test-key'], ['value', 'test-val'], ['id', cid]],`

These are the only 2 test data references that still use the old topic names. The ACL capability strings (state:read, state:write) and assertion strings (state:read capability denied, state:write capability denied) are already correct.
</action>
<acceptance_criteria>
- `grep -c "shell:storage-" tests/e2e/acl-enforcement.spec.ts` returns 0
- `grep "shell:state-get" tests/e2e/acl-enforcement.spec.ts` returns at least 1 match
- `grep "shell:state-set" tests/e2e/acl-enforcement.spec.ts` returns at least 1 match
- `grep "state:read capability denied" tests/e2e/acl-enforcement.spec.ts` returns at least 1 match (already correct, verify preserved)
- `grep "state:write capability denied" tests/e2e/acl-enforcement.spec.ts` returns at least 1 match (already correct, verify preserved)
</acceptance_criteria>
</task>

<task id="07-02-03">
<title>Verify no remaining storage references in test files and harness</title>
<read_first>
- tests/e2e/harness/harness.ts
</read_first>
<action>
Run a comprehensive grep to verify no old storage capability or topic strings remain in tests:

1. `grep -r "storage:read\|storage:write" tests/ --include="*.ts"` — must return 0 matches
2. `grep -r "shell:storage-" tests/ --include="*.ts"` — must return 0 matches
3. `grep -r "napp:storage-" tests/ --include="*.ts"` — must return 0 matches (should already use napp:state-response)
4. `ls tests/e2e/storage-isolation.spec.ts` — must not exist

Check the test harness for any remaining storage references:
- `grep "storage" tests/e2e/harness/harness.ts` — only lowercase "storage" in localStorage-related helper names is acceptable (e.g., __getLocalStorageKeys__ is about DOM localStorage, not the capability name)
</action>
<acceptance_criteria>
- `grep -r "storage:read\|storage:write" tests/ --include="*.ts"` returns 0 matches
- `grep -r "shell:storage-" tests/ --include="*.ts"` returns 0 matches
- `grep -r "napp:storage-" tests/ --include="*.ts"` returns 0 matches
- `test ! -f tests/e2e/storage-isolation.spec.ts` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
## Verification Criteria

1. `tests/e2e/state-isolation.spec.ts` exists with all 9 original tests intact
2. `tests/e2e/storage-isolation.spec.ts` does not exist
3. `grep -r "storage:read\|storage:write\|shell:storage-\|napp:storage-" tests/ --include="*.ts"` returns zero matches
4. All tests pass: `pnpm exec playwright test` exits 0 (66 tests expected)
5. ACL-06 and ACL-07 tests specifically pass (they now send shell:state-get and shell:state-set topics)
</verification>

<must_haves>
- All test capability strings use state:read/state:write, not storage:* (STA-01)
- Test file is named state-isolation.spec.ts (STA-02)
- All test topic strings use shell:state-*, not shell:storage-* (STA-03)
- Test harness has no remaining storage capability references (STA-04)
</must_haves>
