---
phase: 133-documentation-demo-coordination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/nub/README.md
  - packages/shim/README.md
  - packages/sdk/README.md
  - packages/vite-plugin/README.md
  - README.md
  - skills/build-napplet/SKILL.md
  - specs/SHELL-RESOURCE-POLICY.md
  - .planning/PROJECT.md
  - .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
  - DOC-06
  - DOC-07
  - DEMO-01

must_haves:
  truths:
    - "A developer landing on packages/nub/README.md sees the new /resource subpath documented alongside the existing 9 domains, with the four-scheme overview (https / blossom / nostr / data) and the single bytes(url) → Blob primitive"
    - "A developer landing on packages/shim/README.md sees window.napplet.resource documented in the API shape table and the wire format section, with bytes() and bytesAsObjectURL() signatures"
    - "A developer landing on packages/sdk/README.md sees the resource named export documented with the RESOURCE_DOMAIN const and resource type re-exports listed alongside the other 9 NUB domains"
    - "A developer landing on packages/vite-plugin/README.md sees strictCsp?: boolean | StrictCspOptions documented with the dev/prod CSP behavior split, the 10-directive baseline, and the build-time pitfalls it catches"
    - "A reader of root README.md understands v0.28.0 ships browser-enforced resource isolation and that resource is now the canonical fetch path"
    - "A cold-reading agent reading skills/build-napplet/SKILL.md learns to call napplet.resource.bytes(url) for any external image / file / blob — and is explicitly warned that <img src=externalUrl> and fetch() are blocked by the iframe CSP"
    - "A shell deployer landing on specs/SHELL-RESOURCE-POLICY.md gets a concrete checklist covering private-IP block ranges, sidecar opt-in, SVG rasterization caps, MIME byte-sniffing, and redirect chain limits with recommended values"
    - "A reader of .planning/PROJECT.md sees an explicit coordination note that v0.28.0 demo napplets live in the downstream shell repo, not this repo"
    - "A reader of the NUB-RESOURCE draft at .planning/phases/132/drafts/ sees the same delegation in an Implementation note"
  artifacts:
    - path: packages/nub/README.md
      provides: "Updated nub package README with /resource subpath section"
      contains_sections:
        - "## 10 Domains"
        - "@napplet/nub/resource"
    - path: packages/shim/README.md
      provides: "Updated shim README with resource NUB integration"
      contains:
        - "window.napplet.resource"
        - "resource.bytes"
    - path: packages/sdk/README.md
      provides: "Updated sdk README with resource namespace + RESOURCE_DOMAIN"
      contains:
        - "RESOURCE_DOMAIN"
        - "resource.bytes"
    - path: packages/vite-plugin/README.md
      provides: "Updated vite-plugin README with strictCsp option section"
      contains:
        - "strictCsp"
        - "10-directive"
    - path: README.md
      provides: "Updated root README with v0.28.0 surface"
      contains:
        - "@napplet/nub/resource"
        - "browser-enforced"
    - path: skills/build-napplet/SKILL.md
      provides: "Updated skill teaching napplet.resource.bytes(url) usage"
      contains:
        - "napplet.resource.bytes"
    - path: specs/SHELL-RESOURCE-POLICY.md
      provides: "Shell-deployer resource policy checklist"
      min_lines: 80
      contains_sections:
        - "# Shell Resource Policy Checklist"
        - "## Private-IP Block List"
        - "## Sidecar Pre-Resolution"
        - "## SVG Rasterization Caps"
        - "## MIME Byte-Sniffing Allowlist"
        - "## Redirect Chain Limits"
    - path: .planning/PROJECT.md
      provides: "Coordination note delegating v0.28.0 demos to downstream shell repo"
      contains:
        - "downstream shell repo"
    - path: .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
      provides: "Implementation note delegating demos to downstream shell repo"
      contains:
        - "Implementation note"
  key_links:
    - from: "packages/nub/README.md"
      to: "@napplet/nub/resource subpath"
      via: "domain table row + barrel/types/shim/sdk paths"
      pattern: "@napplet/nub/resource"
    - from: "packages/vite-plugin/README.md"
      to: "perm:strict-csp capability identifier"
      via: "documented under strictCsp option"
      pattern: "perm:strict-csp"
    - from: "specs/SHELL-RESOURCE-POLICY.md"
      to: "NUB-RESOURCE in napplet/nubs"
      via: "external link + cross-reference"
      pattern: "napplet/nubs"
    - from: "skills/build-napplet/SKILL.md"
      to: "window.napplet.resource API"
      via: "Step section showing bytes(url) usage"
      pattern: "napplet\\.resource\\.bytes"
---

<objective>
Sweep all v0.28.0 user-facing documentation in this repo so it reflects the shipped Browser-Enforced Resource Isolation surface. 5 package READMEs gain new resource-NUB / strict-CSP sections; the napplet-author skill is rewritten to teach `napplet.resource.bytes(url)`; a fresh shell-deployer policy checklist is created at `specs/SHELL-RESOURCE-POLICY.md`; and PROJECT.md + the NUB-RESOURCE draft both get a coordination note explicitly delegating v0.28.0 demo napplets to the downstream shell repo (per Option B scope decision in CONTEXT.md).

Purpose: Phases 125–132 have shipped the entire wire + SDK + spec surface for the resource NUB and strict CSP. Until this phase lands, every README still reads as if v0.27.0 were current — agents reading the docs will write code that the Phase 130 strict CSP will block at runtime, shell deployers have no policy reference, and downstream contributors cannot tell where the v0.28.0 demos live. This is a pure prose sweep; NO source code, NO new specs (those are Phase 132).

Output: 9 file edits / creations, all verifiable by literal-string grep, leaving `pnpm -r type-check` exit 0 (doc-only, no source touched).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/133-documentation-demo-coordination/133-CONTEXT.md

# Phase 132 NUB-RESOURCE draft — load-bearing source of truth for resource NUB prose;
# every claim made about the resource API in this phase must be consistent with the draft.
@.planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md

# Existing READMEs being modified (read first; preserve everything not explicitly changed)
@packages/nub/README.md
@packages/shim/README.md
@packages/sdk/README.md
@packages/vite-plugin/README.md
@README.md
@skills/build-napplet/SKILL.md

# Resource NUB triad — for accurate import paths and signatures (cite, do not modify)
@packages/nub/src/resource/index.ts
@packages/nub/src/resource/types.ts
@packages/nub/src/resource/shim.ts
@packages/nub/src/resource/sdk.ts

# Vite-plugin csp.ts entry — for accurate strictCsp option shape (cite, do not modify)
@packages/vite-plugin/src/csp.ts

<interfaces>
<!-- Authoritative resource NUB shape — extracted from packages/nub/src/resource/. -->
<!-- Tasks below MUST use these exact identifiers and signatures. -->

From packages/nub/src/resource/types.ts (concept; verify exact shape before writing):
```typescript
type ResourceScheme = 'https' | 'blossom' | 'nostr' | 'data';
type ResourceErrorCode =
  | 'not-found'
  | 'blocked-by-policy'
  | 'timeout'
  | 'too-large'
  | 'unsupported-scheme'
  | 'decode-failed'
  | 'network-error'
  | 'quota-exceeded';
interface ResourceBytesMessage  { type: 'resource.bytes'; id: string; url: string; }
interface ResourceCancelMessage { type: 'resource.cancel'; id: string; }
interface ResourceBytesResultMessage { type: 'resource.bytes.result'; id: string; blob: Blob; mime: string; }
interface ResourceBytesErrorMessage  { type: 'resource.bytes.error';  id: string; code: ResourceErrorCode; error?: string; }
interface ResourceSidecarEntry { url: string; blob: Blob; mime: string; }
```

From packages/nub/src/resource/sdk.ts:
```typescript
export const RESOURCE_DOMAIN = 'resource' as const;
export function bytes(url: string, opts?: { signal?: AbortSignal }): Promise<Blob>;
export function bytesAsObjectURL(url: string): { url: string; revoke: () => void };
```

From packages/nub/src/resource/shim.ts:
```typescript
export function installResourceShim(targetWindow: Window): void;
export function hydrateResourceCache(entries: ResourceSidecarEntry[]): void;
```

@napplet/sdk re-exports (from Phase 129 SUMMARY): aliased as `resourceBytes`, `resourceBytesAsObjectURL`,
plus `installResourceShim`, `RESOURCE_DOMAIN`, and 11 type re-exports. `hydrateResourceCache` is NOT re-exported
(relay-shim-internal helper).

window.napplet shape gains exactly one new key:
```typescript
window.napplet.resource = {
  bytes(url: string, opts?: { signal?: AbortSignal }): Promise<Blob>;
  bytesAsObjectURL(url: string): { url: string; revoke: () => void };
};
```

From packages/vite-plugin (Phase 130 — verify exact shape before writing):
```typescript
interface Nip5aManifestOptions {
  nappletType: string;
  requires?: string[];
  configSchema?: JSONSchema7 | string;
  strictCsp?: boolean | StrictCspOptions;   // NEW in v0.28.0
}
interface StrictCspOptions {
  // shape lives in packages/vite-plugin/src/csp.ts — read it for the exact field set
}
```

10-directive baseline (per Phase 130 CSP-06):
- `default-src 'none'`
- `script-src 'nonce-...' 'self'`  (never `'unsafe-inline'`, never `'unsafe-eval'`)
- `connect-src 'none'` (prod) / `connect-src 'self' ws://localhost:* wss://localhost:*` (dev)
- `img-src blob: data:`
- `font-src blob: data:`
- `style-src 'self'`
- `worker-src 'none'`
- `object-src 'none'`
- `base-uri 'none'`
- `form-action 'none'`

Capability identifiers shipped in v0.28.0:
- `nub:resource` (resource NUB available)
- `resource:scheme:<name>` (per-scheme; e.g. `resource:scheme:blossom`)
- `perm:strict-csp` (shell enforces strict CSP — orthogonal to nub:resource)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 5 package READMEs for v0.28.0 resource NUB + strict-CSP surface</name>
  <files>packages/nub/README.md, packages/shim/README.md, packages/sdk/README.md, packages/vite-plugin/README.md, README.md</files>
  <read_first>
    - packages/nub/README.md (existing — preserve everything except the additions described below)
    - packages/shim/README.md
    - packages/sdk/README.md
    - packages/vite-plugin/README.md
    - README.md
    - .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md (source of truth for prose)
    - packages/nub/src/resource/types.ts (verify exact identifier names before writing)
    - packages/nub/src/resource/sdk.ts (verify exact export names)
    - packages/vite-plugin/src/csp.ts (verify exact StrictCspOptions field set)
    - packages/vite-plugin/src/index.ts (verify exact Nip5aManifestOptions shape after CSP-01 landed)
  </read_first>
  <action>
Make 5 surgical edits, one per README. Preserve all existing content byte-stable except where new sections are inserted. Do NOT restructure existing sections; APPEND or INSERT only.

**1A. packages/nub/README.md**

a. Update the H1 lead-in line (currently `> All 9 napplet NUB domains ...`) to read:
   `> All 10 napplet NUB domains (relay, storage, ifc, keys, theme, media, notify, identity, config, resource) as layered subpath exports.`

b. Rename the section heading `## 9 Domains` to `## 10 Domains` and update the prose under it (currently "Each domain is an independent subpath...") to mention 10 instead of 9 where applicable.

c. Insert a new row at the bottom of the domains table (after the `config` row), exactly:
   ```
   | resource | `@napplet/nub/resource` | `@napplet/nub/resource/types` | `@napplet/nub/resource/shim` | `@napplet/nub/resource/sdk` | Sandboxed byte fetching (https/blossom/nostr/data) via `bytes(url) → Blob` |
   ```

d. Update the Tree-Shaking Contract section (currently mentions 34 entry points, 9 barrels, 9 types, 8 shim, 8 sdk):
   - Change "34 entry points" to "38 entry points"
   - Change "9 domain barrels" to "10 domain barrels"
   - Change "9 granular types entries" to "10 granular types entries"
   - Change "8 granular shim entries (theme omitted ...)" to "9 granular shim entries (theme omitted ...)"
   - Change "8 granular sdk entries (theme omitted ...)" to "9 granular sdk entries (theme omitted ...)"

e. Add a new section after the Theme Exception section, before Migration, titled `## Resource NUB (v0.28.0)`. Body:
   ```markdown
   ## Resource NUB (v0.28.0)

   The `resource` domain ships in v0.28.0 alongside the v0.28.0 milestone of browser-enforced
   resource isolation. It defines a single scheme-pluggable byte-fetching primitive:

   ```ts
   import { bytes, bytesAsObjectURL } from '@napplet/nub/resource/sdk';

   // Fetch any URL the shell accepts under its policy. URL space is scheme-pluggable.
   const blob: Blob = await bytes('https://example.com/avatar.png');

   // Synchronous handle for <img src> use; revoke when done.
   const handle = bytesAsObjectURL('blossom:sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855');
   imgEl.src = handle.url;
   // later
   handle.revoke();
   ```

   Four canonical schemes are defined in the spec:
   - `data:` — RFC 2397, decoded inside the napplet shim with zero shell round-trip
   - `https:` — shell-side network fetch under the default resource policy (private-IP block list at DNS time, MIME byte-sniffing, size cap, timeout, rate limit, redirect cap)
   - `blossom:sha256:<hex>` — Blossom hash → bytes; shell verifies hash before delivery
   - `nostr:<bech32>` — single-hop NIP-19 resolution against the shell's relay pool

   Errors arrive as one of 8 typed codes: `not-found`, `blocked-by-policy`, `timeout`,
   `too-large`, `unsupported-scheme`, `decode-failed`, `network-error`, `quota-exceeded`.

   See [NUB-RESOURCE](https://github.com/napplet/nubs) for the normative spec, the
   default shell resource policy, and the SVG rasterization MUSTs.
   ```

f. Add a row to the Migration table (after the `@napplet/nub-config` row): there is NO deprecated `@napplet/nub-resource` package — resource shipped only via `@napplet/nub/resource`. Add a one-line note BELOW the migration table, NOT a new row:
   ```markdown
   *Note: the `resource` domain (v0.28.0) shipped only via `@napplet/nub/resource`. There is no deprecated `@napplet/nub-resource` package.*
   ```

**1B. packages/shim/README.md**

a. In the "How It Works" numbered list, item 3 currently reads "Once registered, `window.napplet` is populated with relay, ifc, storage, keys, media, notify, identity, config, and shell sub-objects". Insert `resource,` between `config,` and `and shell`. New text:
   `Once registered, \`window.napplet\` is populated with relay, ifc, storage, keys, media, notify, identity, config, resource, and shell sub-objects`

b. In the Quick Start code block, ADD a new pair of lines before `// Clean up`:
   ```ts
   // Fetch external bytes via the shell (CSP blocks direct <img src=externalUrl> / fetch())
   const avatarBlob = await window.napplet.resource.bytes('https://example.com/avatar.png');
   const handle = window.napplet.resource.bytesAsObjectURL('blossom:sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855');
   imgEl.src = handle.url;
   // later: handle.revoke();
   ```

c. In the "Outbound (napplet → shell)" wire-format code block, append:
   ```ts
   { type: 'resource.bytes', id: string, url: string }
   { type: 'resource.cancel', id: string }
   ```

d. In the "Inbound (shell → napplet)" wire-format code block, append:
   ```ts
   { type: 'resource.bytes.result', id: string, blob: Blob, mime: string }
   { type: 'resource.bytes.error', id: string, code: 'not-found' | 'blocked-by-policy' | 'timeout' | 'too-large' | 'unsupported-scheme' | 'decode-failed' | 'network-error' | 'quota-exceeded', error?: string }
   ```

e. In the `window.napplet` Shape code block, add a `resource` block after the `config` block:
   ```ts
     resource: {
       bytes(url, opts?): Promise<Blob>;
       bytesAsObjectURL(url): { url: string; revoke: () => void };
     },
   ```

f. Add a new subsection after `### window.napplet.config` and before `### window.napplet.shell`:
   ```markdown
   ### `window.napplet.resource`

   Sandboxed byte fetching. The iframe sandbox (no `allow-same-origin`) plus strict CSP (no `connect-src`) means napplets cannot fetch external URLs directly — `<img src="https://...">`, `fetch()`, and `XMLHttpRequest` are all blocked by the browser. Use `resource.bytes(url)` to fetch any external resource through the shell.

   | Method | Returns | Description |
   |--------|---------|-------------|
   | `bytes(url, opts?)` | `Promise<Blob>` | Fetch bytes for a URL via the shell. `opts.signal` accepts an `AbortSignal` for cancellation. |
   | `bytesAsObjectURL(url)` | `{ url: string; revoke: () => void }` | Synchronous handle whose `url` resolves to a blob URL once the underlying fetch completes. Caller MUST `revoke()` when done. |

   Four canonical schemes: `data:` (decoded in-shim), `https:` (shell-side network with policy), `blossom:sha256:<hex>` (hash-verified), `nostr:<bech32>` (single-hop NIP-19 resolution).

   Errors reject the Promise with one of 8 codes: `not-found`, `blocked-by-policy`, `timeout`, `too-large`, `unsupported-scheme`, `decode-failed`, `network-error`, `quota-exceeded`.

   Capability detection:
   ```ts
   if (window.napplet.shell.supports('nub:resource')) { /* ... */ }
   if (window.napplet.shell.supports('resource:scheme:blossom')) { /* ... */ }
   if (window.napplet.shell.supports('perm:strict-csp')) { /* shell enforces strict CSP */ }
   ```
   ```

g. In the TypeScript Support section, the comment line `// TypeScript knows about window.napplet.relay, .ifc, .storage, .keys, .media, .notify, .identity, .shell` — add `, .resource` between `.identity` and `, .shell`.

**1C. packages/sdk/README.md**

a. In the Quick Start `import` line (currently `import { relay, ifc, storage, keys, media, notify, config, type NostrEvent } from '@napplet/sdk';`), add `resource, ` between `config, ` and `type NostrEvent`:
   `import { relay, ifc, storage, keys, media, notify, config, resource, type NostrEvent } from '@napplet/sdk';`

   NOTE: The actual exported helpers from Phase 129 are `resourceBytes` / `resourceBytesAsObjectURL` (aliased — not bare `bytes`). The `resource` namespace re-export pattern follows the other 9 NUBs. Verify the exact shape in `packages/sdk/src/index.ts` before writing this line; if a `resource` namespace object is NOT exported, replace with `resourceBytes, resourceBytesAsObjectURL,`.

b. ADD a usage block in Quick Start after the `// Deep-link settings UI` block, before `// Clean up`:
   ```ts
   // Fetch external bytes via the shell (the iframe sandbox + strict CSP block direct fetch)
   const avatarBlob = await resource.bytes('https://example.com/avatar.png');
   const handle = resource.bytesAsObjectURL('blossom:sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855');
   imgEl.src = handle.url;
   // handle.revoke() when done
   ```
   (If the actual export is `resourceBytes` instead of `resource.bytes`, write the helper-form: `await resourceBytes('https://...')` etc.)

c. ADD a new section after `### config` and before `### keys` titled `### resource`:
   ```markdown
   ### `resource`

   Sandboxed byte fetching (NUB-RESOURCE). Mirrors `window.napplet.resource`. Required because the iframe sandbox + strict CSP block direct `fetch()` / `<img src=externalUrl>` / `XMLHttpRequest`.

   | Method | Returns | Description |
   |--------|---------|-------------|
   | `bytes(url, opts?)` | `Promise<Blob>` | Fetch bytes through the shell. `opts.signal` accepts an `AbortSignal`. |
   | `bytesAsObjectURL(url)` | `{ url: string; revoke: () => void }` | Synchronous handle whose `url` resolves to a blob URL once the fetch completes. |

   Four canonical schemes: `data:` (in-shim), `https:` (shell-side under policy), `blossom:sha256:<hex>` (hash-verified), `nostr:<bech32>` (single-hop NIP-19).
   ```

d. In the NUB Domain Constants section, update the import line to include `RESOURCE_DOMAIN`:
   ```ts
   import { RELAY_DOMAIN, IDENTITY_DOMAIN, STORAGE_DOMAIN, IFC_DOMAIN, THEME_DOMAIN, KEYS_DOMAIN, MEDIA_DOMAIN, NOTIFY_DOMAIN, CONFIG_DOMAIN, RESOURCE_DOMAIN } from '@napplet/sdk';
   // Values: 'relay', 'identity', 'storage', 'ifc', 'theme', 'keys', 'media', 'notify', 'config', 'resource'
   ```

   Add a capability example block after the existing `nub:config` block:
   ```ts
   if (window.napplet.shell.supports('nub:resource')) {
     // resource.bytes(url) is available; check per-scheme too:
     if (window.napplet.shell.supports('resource:scheme:blossom')) { /* ... */ }
   }
   ```

e. In the NUB Message Types table, append a row:
   ```
   | `ResourceNubMessage` | `@napplet/nub/resource` | Discriminated union of all resource domain messages |
   ```

f. In the API Reference TOC-style section ordering, the `resource` heading SHOULD come after `notify` and before `config` to match the on-wire/window order — but if this requires reordering existing sections, KEEP the existing order and just APPEND the `resource` section at the end of the per-domain headings.

**1D. packages/vite-plugin/README.md**

a. ADD a new top-level subsection after the `#### configSchema` block (and after its `Build-Time Guards` table) and BEFORE the `### Environment Variables` heading. Title: `#### strictCsp`. Body:
   ```markdown
   #### strictCsp (optional, v0.28.0+)

   **Type:** `boolean | StrictCspOptions`

   When set, the plugin emits a strict Content-Security-Policy `<meta http-equiv="Content-Security-Policy">` tag as the **literal first child of `<head>`** in the napplet's `index.html`. Strict CSP turns the iframe sandbox into browser-enforced resource isolation: napplets cannot `fetch()`, cannot `<img src=externalUrl>`, cannot `XMLHttpRequest`. All external bytes must flow through `napplet.resource.bytes(url)`.

   **Quick start:**

   ```ts
   import { defineConfig } from 'vite';
   import { nip5aManifest } from '@napplet/vite-plugin';

   export default defineConfig({
     plugins: [
       nip5aManifest({
         nappletType: 'my-napp',
         strictCsp: true,   // enable v0.28.0 baseline
       }),
     ],
   });
   ```

   **10-directive baseline (when `strictCsp: true`):**

   | Directive | Value (prod) | Value (dev) |
   |-----------|--------------|-------------|
   | `default-src` | `'none'` | `'none'` |
   | `script-src` | `'nonce-<random>' 'self'` | `'nonce-<random>' 'self'` |
   | `style-src` | `'self'` | `'self'` |
   | `img-src` | `blob: data:` | `blob: data:` |
   | `font-src` | `blob: data:` | `blob: data:` |
   | `connect-src` | `'none'` | `'self' ws://localhost:* wss://localhost:*` |
   | `worker-src` | `'none'` | `'none'` |
   | `object-src` | `'none'` | `'none'` |
   | `base-uri` | `'none'` | `'none'` |
   | `form-action` | `'none'` | `'none'` |

   **What the build will REJECT:**

   | Pitfall | What fails the build |
   |---------|----------------------|
   | Pitfall 1 — meta not first head child | Build fails if any `<script>`, `<style>`, or `<link>` precedes the CSP meta in `<head>`. |
   | Pitfall 2 — header-only directives in meta | Build fails if the configured policy contains `frame-ancestors`, `sandbox`, `report-uri`, or `report-to` (these are silently ignored by browsers in meta delivery). |
   | Pitfall 18 — dev relaxation leaks into prod | Build fails if the production manifest contains any of `'unsafe-inline'`, `'unsafe-eval'`, `ws://`, or `wss://`. |
   | Pitfall 19 — single-quote-bearing values | Hand-rolled CSP extractor anchors on `"` delimiters so `'none'` / `'self'` survive correctly. |

   **Capability advertisement:**

   Shells that enforce this CSP advertise it via `shell.supports('perm:strict-csp')`. The capability is **orthogonal to `nub:resource`** — a permissive dev shell can implement the resource NUB without enforcing strict CSP, and a production shell can enforce CSP whether or not the napplet actually uses the resource NUB.

   **Disable for legacy / migration napplets:**

   ```ts
   nip5aManifest({ nappletType: 'my-old-napp', strictCsp: false });   // explicit opt-out
   nip5aManifest({ nappletType: 'my-old-napp' });                     // implicit (default off — verify against src/index.ts)
   ```

   See [NUB-RESOURCE](https://github.com/napplet/nubs) and [NIP-5D Security Considerations](../../specs/NIP-5D.md) for the wire-side contract that strict CSP enforces in tandem with the iframe sandbox model.
   ```

b. In the `Nip5aManifestOptions` interface code block under the API Reference section, ADD a field for `strictCsp` (verify the exact type signature from `packages/vite-plugin/src/csp.ts` and `packages/vite-plugin/src/index.ts` — likely `strictCsp?: boolean | StrictCspOptions;` plus a JSDoc comment). Append after the `configSchema` field:
   ```ts
     /**
      * Emit a strict Content-Security-Policy meta as the first <head> child.
      * Pairs with the resource NUB to enforce browser-level resource isolation.
      * Boolean true uses the 10-directive baseline; pass StrictCspOptions to customize.
      * Disabled by default for back-compat with pre-v0.28.0 napplets.
      */
     strictCsp?: boolean | StrictCspOptions;
   ```

c. In the Protocol Reference section, ADD a bullet:
   ```markdown
   - [NUB-RESOURCE (PR pending)](https://github.com/napplet/nubs) — sandboxed byte fetching primitive that strict CSP enforces against
   ```

**1E. README.md (root)**

a. Add a new section after the existing `## Architecture` block and before `## Origin`. Title: `## v0.28.0 — Browser-Enforced Resource Isolation`. Body:
   ```markdown
   ## v0.28.0 — Browser-Enforced Resource Isolation

   v0.28.0 converts napplet iframe security from ambient trust ("napplets shouldn't fetch directly") to browser-enforced isolation ("napplets cannot fetch directly — the browser blocks it"):

   - **New NUB:** `@napplet/nub/resource` ships a single scheme-pluggable primitive — `napplet.resource.bytes(url) → Blob`. URL space is scheme-pluggable: `https:` (shell-side network under policy), `blossom:sha256:<hex>` (hash-verified), `nostr:<bech32>` (single-hop NIP-19), `data:` (RFC 2397, decoded in-shim).
   - **Strict CSP:** `@napplet/vite-plugin` ships a `strictCsp` option that emits a 10-directive baseline (`default-src 'none'` + `connect-src 'none'` in prod) as the first child of `<head>`. The browser blocks every `fetch()`, `<img src=externalUrl>`, `XMLHttpRequest`, and direct WebSocket; everything network-sourced flows through the resource NUB.
   - **Optional sidecar:** Shells MAY pre-resolve resources referenced by `relay.event` envelopes via an opt-in (default-OFF) `resources?` field; napplet-side `resource.bytes(sidecarUrl)` calls then resolve from cache without a postMessage round-trip.
   - **SVG rasterization:** Shells MUST rasterize SVG to PNG/WebP in a sandboxed Worker with no network — napplets never receive parseable XML.
   - **Specs:** `specs/NIP-5D.md` ships a Security Considerations subsection; NUB-RESOURCE is drafted at `napplet/nubs`; NUB-RELAY/IDENTITY/MEDIA carry coordination amendments.
   - **Shell-deployer guide:** `specs/SHELL-RESOURCE-POLICY.md` checklists the private-IP block list, sidecar opt-in, SVG caps, MIME allowlist, and redirect chain limits.

   Demo napplets exercising the model end-to-end (profile viewer, feed napplet with inline images, scheme-mixed consumer) live in the **downstream shell repo** — this monorepo ships only the wire + SDK surface.
   ```

b. In the Packages table, add a phrase to the `@napplet/nub` description: change "9 domain subpaths" to "10 domain subpaths" and append "Includes the v0.28.0 `resource` NUB for sandboxed byte fetching." to the description.

c. In the Packages table, append to the `@napplet/vite-plugin` description: "v0.28.0+ ships a `strictCsp` option for 10-directive browser-enforced CSP."

d. In the Architecture / Napplet-Side Communication section, in the right-side `@napplet/shim` listing, ADD a line `window.napplet.resource (bytes/bytesAsObjectURL)` between the existing `window.napplet.storage` and `window.napplet.shell.supports(domain)` lines.
  </action>
  <verify>
    <automated>
# All five files contain v0.28.0 surface markers
grep -q "@napplet/nub/resource" packages/nub/README.md
grep -q "10 Domains" packages/nub/README.md
grep -q "window.napplet.resource" packages/shim/README.md
grep -q "resource\\.bytes" packages/shim/README.md
grep -q "RESOURCE_DOMAIN" packages/sdk/README.md
grep -q "resource\\.bytes" packages/sdk/README.md
grep -q "strictCsp" packages/vite-plugin/README.md
grep -q "10-directive" packages/vite-plugin/README.md
grep -q "perm:strict-csp" packages/vite-plugin/README.md
grep -q "@napplet/nub/resource" README.md
grep -q "browser-enforced" README.md
# Public-repo hygiene (zero kehto / hyprgate in any of these files)
! grep -i -q "kehto\\|hyprgate" packages/nub/README.md packages/shim/README.md packages/sdk/README.md packages/vite-plugin/README.md README.md
# Doc-only changes — workspace stays green
pnpm -r type-check
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@napplet/nub/resource" packages/nub/README.md` >= 4 (lead-in, table row, code example, migration note)
    - `grep -q "10 Domains" packages/nub/README.md`
    - `grep -q "Resource NUB (v0.28.0)" packages/nub/README.md`
    - `grep -q "38 entry points" packages/nub/README.md`
    - `grep -q "window.napplet.resource" packages/shim/README.md`
    - `grep -q "resource\\.bytes\\.result" packages/shim/README.md`
    - `grep -q "bytesAsObjectURL" packages/shim/README.md`
    - `grep -q "RESOURCE_DOMAIN" packages/sdk/README.md`
    - `grep -q "ResourceNubMessage" packages/sdk/README.md`
    - `grep -q "strictCsp" packages/vite-plugin/README.md` (>= 3 occurrences)
    - `grep -q "10-directive" packages/vite-plugin/README.md`
    - `grep -q "Pitfall 1" packages/vite-plugin/README.md`
    - `grep -q "perm:strict-csp" packages/vite-plugin/README.md`
    - `grep -q "## v0.28.0" README.md`
    - `grep -q "browser-enforced" README.md`
    - `grep -q "downstream shell repo" README.md`
    - Hygiene: `grep -ri "kehto\\|hyprgate" packages/nub/README.md packages/shim/README.md packages/sdk/README.md packages/vite-plugin/README.md README.md` returns no matches
    - `pnpm -r type-check` exits 0 (no source touched)
  </acceptance_criteria>
  <done>5 READMEs updated with new resource-NUB / strict-CSP sections; existing content otherwise byte-stable. DOC-01..05 satisfied. Workspace stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite skills/build-napplet/SKILL.md to teach napplet.resource.bytes(url) (DOC-06)</name>
  <files>skills/build-napplet/SKILL.md</files>
  <read_first>
    - skills/build-napplet/SKILL.md (existing — note: file is heavily out of date; references `nappletState`, `window.nostr`, `discoverServices`, `subscribe`/`publish`/`query` named exports from `@napplet/shim` that no longer exist)
    - .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md (resource API reference)
    - packages/shim/README.md (current API surface — use as source of truth for accurate window.napplet.* signatures)
  </read_first>
  <action>
The skill file currently teaches APIs that have been removed across 4+ milestones. The minimum surgical update for THIS phase is:
1. Add a Resource NUB step that teaches `napplet.resource.bytes(url)` (DOC-06 hard requirement)
2. Add a Common Pitfall warning that `<img src=externalUrl>` and `fetch()` are blocked by the iframe sandbox + CSP and MUST be replaced with `napplet.resource.bytes(url)`
3. Update the frontmatter `description:` to include resource NUB

Do NOT attempt to fix the rest of the file's drift in this phase — that is out of scope (separate skill-modernization phase). Confine edits to additions consistent with current API.

**2A. Update frontmatter `description:` field.**

Replace the existing description line:
```
description: Use when writing a napplet (sandboxed Nostr iframe app) using @napplet/shim — covers Vite project setup, NIP-5A manifest plugin, subscribe/publish/query relay API, nappStorage, window.nostr NIP-07 proxy, inter-frame events, and service discovery
```

With:
```
description: Use when writing a napplet (sandboxed Nostr iframe app) using @napplet/shim — covers Vite project setup, NIP-5A manifest plugin, subscribe/publish/query relay API, scoped storage, inter-frame events, and the v0.28.0 resource NUB for sandboxed byte fetching (replaces direct fetch / <img src=externalUrl>, both of which the iframe CSP blocks)
```

**2B. Add a new step section.**

Insert a new top-level step section just before the existing `## Common pitfalls` heading. Title: `## Step 10 — Fetch external bytes (resource NUB, v0.28.0+)`. Body (verbatim):

```markdown
## Step 10 — Fetch external bytes (resource NUB, v0.28.0+)

The iframe sandbox (no `allow-same-origin`) plus strict CSP (`connect-src 'none'`) means `fetch()`, `<img src="https://...">`, `XMLHttpRequest`, and `new WebSocket(...)` are all blocked by the browser. Any external bytes — avatars, blossom-served images, NIP-19 resource resolution — flow through the shell via the resource NUB.

```ts
import '@napplet/shim';

// Fetch any URL the shell accepts. Returns a Blob.
const blob: Blob = await window.napplet.resource.bytes('https://example.com/avatar.png');
const objectUrl = URL.createObjectURL(blob);
imgEl.src = objectUrl;
// remember to URL.revokeObjectURL(objectUrl) when done

// Or use the synchronous handle helper:
const handle = window.napplet.resource.bytesAsObjectURL('blossom:sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855');
imgEl.src = handle.url;            // populated once the underlying fetch resolves
// later
handle.revoke();
```

Four canonical schemes are supported:

| Scheme | Example | Resolution |
|--------|---------|------------|
| `data:` | `data:image/png;base64,iVBORw0KGgo...` | Decoded in the napplet shim — zero shell round-trip |
| `https:` | `https://example.com/avatar.png` | Shell-side network fetch under policy (private-IP block at DNS time, MIME byte-sniffing, size cap, timeout, rate limit, redirect cap) |
| `blossom:sha256:<hex>` | `blossom:sha256:e3b0c44...` | Blossom hash → bytes; shell verifies hash before delivery |
| `nostr:<bech32>` | `nostr:nprofile1...` | Single-hop NIP-19 resolution against the shell's relay pool |

**Cancellation:** Pass an `AbortSignal` to cancel an in-flight fetch:

```ts
const ctrl = new AbortController();
const promise = window.napplet.resource.bytes(url, { signal: ctrl.signal });
ctrl.abort();   // sends resource.cancel to the shell; promise rejects with AbortError
```

**Errors:** The Promise rejects with an Error whose `code` is one of:
`not-found`, `blocked-by-policy`, `timeout`, `too-large`, `unsupported-scheme`, `decode-failed`, `network-error`, `quota-exceeded`.

Always branch on `code`, never on the `error` string.

**Capability detection:**

```ts
if (window.napplet.shell.supports('nub:resource')) {
  // resource.bytes(url) is available
}
if (window.napplet.shell.supports('resource:scheme:blossom')) {
  // blossom: scheme is available specifically
}
if (window.napplet.shell.supports('perm:strict-csp')) {
  // shell enforces strict CSP — direct fetch / <img src=externalUrl> WILL be blocked
}
```

SVG inputs are silently rasterized server-side to PNG/WebP — napplets never receive `image/svg+xml` bytes (the shell rasterizes in a sandboxed Worker with no network access). The `mime` returned to the napplet is shell-classified via byte-sniffing, never the upstream `Content-Type` header.
```

**2C. Add new Common Pitfall bullets.**

In the existing `## Common pitfalls` section, append two new bullets at the end of the list:

```markdown
- **Do not call `fetch()`, `<img src="https://...">`, `<link href="https://...">`, `XMLHttpRequest`, or `new WebSocket(...)` from a napplet.** The iframe sandbox + strict CSP (`connect-src 'none'`, `img-src blob: data:`) block all of them at the browser level. Use `window.napplet.resource.bytes(url)` instead — it returns a `Blob` you can pass to `URL.createObjectURL()` for `<img src>` use.
- **Do not use the upstream `Content-Type` for resource MIME decisions.** The shell byte-sniffs the response and delivers a classified `mime` field on the result; the upstream `Content-Type` header is attacker-controlled and never reaches the napplet.
```
  </action>
  <verify>
    <automated>
# Frontmatter description references resource NUB
grep -q "resource NUB" skills/build-napplet/SKILL.md
# Step 10 exists and teaches napplet.resource.bytes
grep -q "## Step 10" skills/build-napplet/SKILL.md
grep -q "napplet\\.resource\\.bytes" skills/build-napplet/SKILL.md
# Pitfall warning explicitly names the blocked APIs
grep -q "Do not call \`fetch()\`" skills/build-napplet/SKILL.md
grep -q "<img src=" skills/build-napplet/SKILL.md
# Hygiene
! grep -i -q "kehto\\|hyprgate" skills/build-napplet/SKILL.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Step 10 — Fetch external bytes" skills/build-napplet/SKILL.md`
    - `grep -c "napplet\\.resource\\.bytes" skills/build-napplet/SKILL.md` >= 3
    - `grep -q "AbortController" skills/build-napplet/SKILL.md`
    - `grep -q "blocked-by-policy" skills/build-napplet/SKILL.md`
    - `grep -q "perm:strict-csp" skills/build-napplet/SKILL.md`
    - `grep -q "Do not call \`fetch" skills/build-napplet/SKILL.md`
    - `grep -q "image/svg+xml" skills/build-napplet/SKILL.md` (rasterization note)
    - `grep -i -q "kehto\\|hyprgate" skills/build-napplet/SKILL.md` returns no matches
    - Per CONTEXT NOTE: `@napplet/*` references in this file are ALLOWED (skill teaches napplet authors how to consume @napplet/shim) — not a hygiene failure
  </acceptance_criteria>
  <done>SKILL.md teaches `napplet.resource.bytes(url)` as the canonical fetch path; pitfall warnings explicitly name `fetch()` / `<img src=externalUrl>` as blocked. DOC-06 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Create specs/SHELL-RESOURCE-POLICY.md (DOC-07)</name>
  <files>specs/SHELL-RESOURCE-POLICY.md</files>
  <read_first>
    - .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md (Default Resource Policy, SVG Handling, Sidecar Pre-Resolution sections — source of truth for normative content)
    - specs/NIP-5D.md (existing in-repo spec; the new file lives alongside it)
    - .planning/phases/133-documentation-demo-coordination/133-CONTEXT.md (locked content for the checklist)
  </read_first>
  <action>
Create a new file at `specs/SHELL-RESOURCE-POLICY.md`. This is a **shell implementer's guide** — NOT a spec. It cross-references NUB-RESOURCE for the normative wire shape and provides a concrete, enumerated checklist for shell deployers to walk through when implementing or auditing a v0.28.0-conformant shell.

Keep prose tight. Each policy area = one section with a short rationale + a checklist of concrete decisions/values. Reference NUB-RESOURCE (in `napplet/nubs`) for the normative MUST/SHOULD/MAY language.

Public-repo hygiene: do NOT mention `@napplet/*` private packages, do NOT mention `kehto`, do NOT mention `hyprgate`. This file may end up in a public-facing context.

Write the file with this exact structure (verbatim, including all sections):

```markdown
# Shell Resource Policy Checklist

> Shell-implementer guide for v0.28.0+ napplet hosts implementing the resource NUB.
> Normative wire shape and MUST/SHOULD language live in [NUB-RESOURCE](https://github.com/napplet/nubs); this document is a deployment checklist.

## Status

This is a non-normative implementer's guide. The normative spec is **NUB-RESOURCE** in the `napplet/nubs` repo. Shell hosts MUST implement the MUSTs in NUB-RESOURCE; this document enumerates the concrete defaults and decisions a deployer needs to make.

## Why this exists

The resource NUB makes the host shell the sole network-fetch path on behalf of every sandboxed napplet. The shell-as-fetch-proxy model is an irreducible attack surface: a naively-implemented shell becomes an SSRF gadget that can probe internal addresses, exfiltrate cloud-metadata credentials, or scan the deployer's intranet on behalf of an attacker-supplied URL. NUB-RESOURCE locks the protocol-level MUSTs needed to neutralize this; this checklist makes the operator-visible decisions explicit.

Shells that violate any of the MUSTs below are non-conformant and SHOULD NOT be deployed in adversarial contexts.


## Private-IP Block List (MUST, at DNS-resolution time)

The single most important policy. Reject URLs whose **resolved IP** falls in any blocked range. Check happens **after** the DNS resolver returns an address and **before** the HTTP connection is opened. Each redirect hop is re-validated independently.

URL-parse-time checks (looking at the literal hostname) are NOT sufficient — an attacker-controlled DNS record can resolve `attacker.example.com` to `127.0.0.1` and bypass any naive check. DNS pinning to the validated address defeats DNS-rebinding and TOCTOU attacks.

### Required ranges

- [ ] `10.0.0.0/8` — RFC1918 private IPv4
- [ ] `172.16.0.0/12` — RFC1918 private IPv4
- [ ] `192.168.0.0/16` — RFC1918 private IPv4
- [ ] `127.0.0.0/8` — IPv4 loopback
- [ ] `::1/128` — IPv6 loopback
- [ ] `169.254.0.0/16` — IPv4 link-local
- [ ] `fe80::/10` — IPv6 link-local
- [ ] `fc00::/7` — IPv6 unique-local
- [ ] `169.254.169.254` (singleton) — Cloud metadata service (AWS, GCP, Azure, DigitalOcean, etc.)

### Implementation requirements

- [ ] Validation runs **after** DNS resolves to an IP, **before** the TCP connection is opened
- [ ] Each redirect hop is re-validated independently against the same list
- [ ] Failed validation emits `code: "blocked-by-policy"` with an error string identifying the matching range (so deployers can debug policy)
- [ ] Additional addresses MAY be allowed behind explicit shell-administrator policy (enterprise on-prem services), but the default for community-deployed shells MUST be restrictive


## Sidecar Pre-Resolution (default OFF)

The NUB-RELAY amendment adds an optional `resources?: ResourceSidecarEntry[]` field on `relay.event` envelopes. When the shell pre-fetches resources referenced by an event and ships the bytes alongside the event, the napplet's subsequent `resource.bytes(url)` calls resolve from cache without a postMessage round-trip.

### Privacy rationale (why default OFF)

Pre-fetching reveals user activity to upstream hosts before the user has chosen to render the event. An avatar URL on every event in a 1000-event timeline becomes 1000 HTTP requests, each one a fingerprint visible to the upstream host operator. Default OFF preserves the user's "I haven't rendered this yet" semantic.

### Checklist

- [ ] Sidecar emission defaults to **OFF** (no pre-resolution unless explicitly opted in)
- [ ] Opt-in is per **shell deployment policy** — not a per-napplet capability the napplet can negotiate
- [ ] Opt-in SHOULD be scoped by a **per-event-kind allowlist** (e.g., enable for kind 0 metadata only; do not pre-fetch resources from arbitrary user-content kinds)
- [ ] Sidecar bytes obey the **same policy** as direct `resource.bytes(url)` calls (private-IP block, MIME byte-sniffing, size cap, SVG rasterization)
- [ ] The `mime` field on each sidecar entry is shell-classified by byte-sniffing — **never** populated from the upstream `Content-Type` header
- [ ] SVG entries appearing in a sidecar are rasterized to PNG/WebP **before** being placed on the wire (the sidecar is not a bypass for SVG rasterization)
- [ ] Operators document any deviation from default-OFF in the shell's user-facing privacy notice


## SVG Rasterization Caps (MUST)

`image/svg+xml` is a parseable XML execution surface — `<script>`, `<foreignObject>`, `<image href>` external references, `<use href>` recursion, DOCTYPE entity expansion (the "billion laughs" pattern), `@font-face src:` URLs. Delivering raw SVG bytes to a sandboxed napplet recreates every attack surface the sandbox was designed to eliminate.

### Required behavior

- [ ] When the byte-sniffer identifies a fetched resource as `image/svg+xml`, the shell rasterizes it to a bitmap (PNG or WebP) **before** delivery
- [ ] The `mime` field on the result envelope for any SVG-source-input is `image/png` or `image/webp` — **never** `image/svg+xml`
- [ ] The rasterizer runs in a **sandboxed Worker** with **no network** access (no `XMLHttpRequest`, no `fetch`, no `WebSocket`, no `EventSource`, no `<img>` external `href` resolution, no `<use href>` external resolution, no `@font-face src:` resolution, no DOCTYPE entity URL resolution)

### Recommended caps (SHOULD, all enforced together)

| Cap | Recommended default | On exceed |
|-----|---------------------|-----------|
| Max input bytes | **5 MiB** | `code: "too-large"` |
| Max output dimensions | **4096 × 4096 pixels** | `code: "too-large"` |
| Wall-clock rasterization budget | **2 seconds** | `code: "timeout"` |

All three caps mitigate distinct attacks:
- Input cap → billion-laughs entity expansion is bounded
- Output cap → recursive-`<use>` rendering bombs are bounded
- Wall-clock cap → `<foreignObject>` script-driven CPU exhaustion is bounded

Relaxing any one cap undermines the others.


## MIME Byte-Sniffing Allowlist (MUST)

The upstream `Content-Type` header is attacker-controlled. A content host can declare `text/html` for what is actually `image/png` (or vice-versa) and coerce a napplet into a confused-render attack.

### Required behavior

- [ ] Classify response bytes via a byte-sniffing strategy ([WHATWG MIME Sniffing Standard](https://mimesniff.spec.whatwg.org/) or equivalent)
- [ ] **Never** pass through the upstream `Content-Type` header to the napplet
- [ ] Enforce a **scheme-appropriate MIME allowlist**; bytes whose sniffed MIME falls outside the allowlist are rejected with `code: "blocked-by-policy"`

### Recommended baseline allowlist (image-rendering shells)

- [ ] `image/png`
- [ ] `image/jpeg`
- [ ] `image/webp`
- [ ] `image/gif`
- [ ] `image/svg+xml` — only acceptable as **input** to the rasterizer; **never** delivered to the napplet as `image/svg+xml`

Shells delivering non-image bytes (e.g., `application/json` for `nostr:` resolution) extend the allowlist per scheme. Maintain one allowlist per scheme rather than a global union — the threat model differs per scheme.


## Redirect Chain Limits (SHOULD, with per-hop re-validation)

Public hosts can 302 to internal addresses. Without per-hop re-validation, a redirect chain trivially bypasses the private-IP block list.

### Recommended values

- [ ] Cap redirect chain at **5 hops**
- [ ] **Each hop is re-validated independently** against the private-IP block list (DNS pinning per hop)
- [ ] Excess hops or a redirect to a blocked address emits `code: "blocked-by-policy"`


## Recommended Operational Caps (SHOULD)

These mitigate resource-exhaustion attacks against the shell itself.

| Cap | Recommended default | On exceed |
|-----|---------------------|-----------|
| Per-response size | **10 MiB** | `code: "too-large"` |
| Per-URL fetch timeout (wall-clock) | **30 seconds** | `code: "timeout"` |
| Per-napplet concurrent in-flight `resource.bytes` calls | **10** | `code: "blocked-by-policy"` |
| Per-napplet `resource.bytes` rate limit (sliding window) | **60 calls / minute** | `code: "blocked-by-policy"` |
| Per-napplet outstanding-Blob quota | **~50 MiB** | `code: "quota-exceeded"` |

Community-deployed shells SHOULD NOT raise the response size cap above ~50 MiB without explicit operator opt-in.


## Single-Flight Cache (SHOULD)

Coalesce concurrent same-URL fetches.

- [ ] Cache keyed on the URL string as supplied by the napplet (byte-equal — this NUB does not mandate canonicalization)
- [ ] N concurrent calls for the same URL share **one** in-flight fetch and resolve with the same `Blob` reference
- [ ] Cache scope partitioned per `(dTag, aggregateHash)` per NIP-5D — napplets MUST NOT see another napplet's cached resources
- [ ] Aborted entries are removed from the in-flight map for retryability


## Scheme Whitelist (MUST)

Only the canonical schemes plus shell-administrator opt-ins are dispatched. Smuggling-prone schemes are never enabled by default.

### Canonical schemes

- [ ] `data:` (RFC 2397) — decoded in-shim with zero shell round-trip; size cap still applies on the decoded `Blob`
- [ ] `https:` — full Default Resource Policy applies
- [ ] `blossom:sha256:<hex>` — shell verifies hash against the URL's declared digest **before** delivery; mismatch → `code: "decode-failed"`
- [ ] `nostr:<bech32>` — single-hop NIP-19 resolution; recursive resolution is **not** the shell's job

### Never enable by default

- [ ] `file:` — local filesystem read; trivial sandbox escape
- [ ] `gopher:`, `dict:`, `ftp:`, `tftp:` — protocol smuggling vectors
- [ ] `http:` (cleartext) — opt-in only behind explicit shell-administrator policy (e.g., enterprise on-prem services)

Unknown schemes emit `code: "unsupported-scheme"`.


## Capability Advertisement

Shells advertise resource-NUB conformance via the standard capability query API:

- [ ] `shell.supports('nub:resource')` returns `true`
- [ ] `shell.supports('resource:scheme:<name>')` returns `true` for each supported scheme (e.g., `resource:scheme:blossom`)
- [ ] `shell.supports('perm:strict-csp')` returns `true` if the shell enforces strict CSP on napplet iframes (orthogonal to `nub:resource` — a permissive dev shell can implement the resource NUB without enforcing strict CSP)


## Audit Checklist (one-page summary)

Use this as a deployment sign-off:

- [ ] Private-IP block list enforced at DNS-resolution time, all 9 ranges covered
- [ ] Each redirect hop independently DNS-pinned and re-validated
- [ ] MIME byte-sniffing replaces upstream `Content-Type` for the value delivered to the napplet
- [ ] SVG rasterization runs in a sandboxed Worker with no network; raw `image/svg+xml` bytes never reach the napplet
- [ ] SVG caps (5 MiB input / 4096×4096 output / 2s wall-clock) all enforced together
- [ ] Sidecar pre-resolution defaults OFF; opt-in per shell deployment policy + per-event-kind allowlist
- [ ] Sidecar bytes obey the same MIME/SVG/size policy as direct calls
- [ ] Single-flight cache scoped per `(dTag, aggregateHash)`
- [ ] Scheme dispatch is a whitelist; smuggling-prone schemes blocked
- [ ] Capability advertisement (`nub:resource`, `resource:scheme:*`, optionally `perm:strict-csp`) wired through `shell.supports()`
- [ ] Resource bytes treated as observable (cleartext over postMessage); deployers document this in user-facing notice if relevant


## References

- [NUB-RESOURCE](https://github.com/napplet/nubs) — normative spec for the resource NUB (wire shape, MUST/SHOULD/MAY contract)
- [NIP-5D](./NIP-5D.md) — napplet-shell protocol; Security Considerations subsection covers strict-CSP posture and `sandbox="allow-scripts"` reaffirmation
- [WHATWG MIME Sniffing Standard](https://mimesniff.spec.whatwg.org/) — recommended byte-sniffing reference
```
  </action>
  <verify>
    <automated>
# File exists with required structure
test -f specs/SHELL-RESOURCE-POLICY.md
grep -q "^# Shell Resource Policy Checklist" specs/SHELL-RESOURCE-POLICY.md
grep -q "^## Private-IP Block List" specs/SHELL-RESOURCE-POLICY.md
grep -q "^## Sidecar Pre-Resolution" specs/SHELL-RESOURCE-POLICY.md
grep -q "^## SVG Rasterization Caps" specs/SHELL-RESOURCE-POLICY.md
grep -q "^## MIME Byte-Sniffing Allowlist" specs/SHELL-RESOURCE-POLICY.md
grep -q "^## Redirect Chain Limits" specs/SHELL-RESOURCE-POLICY.md
# Critical normative content
grep -q "169\\.254\\.169\\.254" specs/SHELL-RESOURCE-POLICY.md
grep -q "RFC1918" specs/SHELL-RESOURCE-POLICY.md
grep -q "DNS-resolution time" specs/SHELL-RESOURCE-POLICY.md
grep -q "default OFF" specs/SHELL-RESOURCE-POLICY.md
grep -q "sandboxed Worker" specs/SHELL-RESOURCE-POLICY.md
grep -q "WHATWG MIME Sniffing" specs/SHELL-RESOURCE-POLICY.md
# Hygiene: no @napplet/* private package refs (this is a public-facing spec doc)
! grep -q "@napplet/" specs/SHELL-RESOURCE-POLICY.md
! grep -i -q "kehto\\|hyprgate" specs/SHELL-RESOURCE-POLICY.md
# Min lines threshold
test "$(wc -l < specs/SHELL-RESOURCE-POLICY.md)" -ge 80
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at `specs/SHELL-RESOURCE-POLICY.md`
    - Contains all 6 required H2 sections (Private-IP, Sidecar, SVG, MIME, Redirect, plus Audit Checklist)
    - Mentions all 9 private-IP ranges including `169.254.169.254` cloud metadata singleton
    - Documents `default OFF` for sidecar with privacy rationale
    - SVG caps table includes 5 MiB / 4096×4096 / 2s
    - Lists smuggling-prone schemes to block (`file:`, `gopher:`, `dict:`, `ftp:`, `tftp:`)
    - Cross-references NUB-RESOURCE (in `napplet/nubs`) and NIP-5D
    - Hygiene: ZERO `@napplet/` references (this is a public-facing spec doc)
    - Hygiene: ZERO kehto/hyprgate references
    - File is at least 80 lines
  </acceptance_criteria>
  <done>specs/SHELL-RESOURCE-POLICY.md exists with all five required policy areas + audit checklist + scheme whitelist. Cross-references NUB-RESOURCE for normative shape. DOC-07 satisfied.</done>
</task>

<task type="auto">
  <name>Task 4: Demo coordination notes (DEMO-01) — PROJECT.md + NUB-RESOURCE draft</name>
  <files>.planning/PROJECT.md, .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md</files>
  <read_first>
    - .planning/PROJECT.md (existing — preserve byte-stable except where the new note is appended)
    - .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md (existing draft — append to its existing structure)
    - .planning/phases/133-documentation-demo-coordination/133-CONTEXT.md (locked Option B scope decision)
  </read_first>
  <action>
Two surgical edits delegating v0.28.0 demo napplets to the downstream shell repo per the locked Option B scope decision in CONTEXT.md.

**4A. .planning/PROJECT.md**

Find the existing `## Current Milestone: v0.28.0 Browser-Enforced Resource Isolation` section. After the `**Key context:**` bullet list and BEFORE the next `## Shipped:` section, INSERT a new subsection. The new subsection (verbatim):

```markdown
**Demo coordination (v0.28.0):**

This monorepo ships only the **wire + SDK surface** for v0.28.0 — `@napplet/nub/resource`, the `@napplet/shim` integration, the `@napplet/sdk` re-exports, the `@napplet/vite-plugin` `strictCsp` option, the `specs/NIP-5D.md` Security Considerations amendment, and the four cross-repo PRs to `napplet/nubs`. **Demo napplets** exercising the model end-to-end (profile viewer, feed napplet with inline images, scheme-mixed consumer) are explicitly delegated to the **downstream shell repo** for v0.28.0 (Option B per milestone scoping). The downstream shell hosts the demo napplets because demos require a complete shell implementation including the resource NUB handler, default policy enforcement, SVG rasterizer, and strict CSP — all of which are shell-side responsibilities living outside this monorepo since the v0.13.0 extraction. Direct contributors to v0.28.0 demo work to the downstream shell repo, not here.
```

**4B. .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md**

Find the existing `## Implementations` section near the bottom of the file (currently reads `## Implementations\n\n- (none yet)`). REPLACE this section (heading + body) with the following expanded version. Heading stays the same; body grows:

```markdown
## Implementations

- (none yet)

## Implementation Note

Reference implementations of NUB-RESOURCE — both the napplet-side primitive and the shell-side resource handler with the default policy, SVG rasterizer, and sidecar plumbing — live in the **downstream shell repo** alongside v0.28.0's first set of demo napplets (profile viewer, feed napplet with inline images, scheme-mixed consumer). The napplet-side SDK surface (`window.napplet.resource.bytes` / `bytesAsObjectURL`) is shipped via the napplet protocol SDK monorepo and is consumed by any conformant shell implementation; the shell-side handler implementation (network policy enforcement, MIME byte-sniffing, SVG rasterization in a sandboxed Worker, optional sidecar pre-resolution) is shell-specific and is not part of this spec.

Shell implementers SHOULD consult the downstream shell repo for a working reference implementation of the default resource policy described in this NUB. The protocol surface defined here is implementation-agnostic; any shell may host it.
```

DO NOT modify any other section of NUB-RESOURCE.md. Preserve byte-stability everywhere except the `## Implementations` section and the new `## Implementation Note` section.
  </action>
  <verify>
    <automated>
# PROJECT.md got the coordination note
grep -q "Demo coordination (v0.28.0)" .planning/PROJECT.md
grep -q "downstream shell repo" .planning/PROJECT.md
grep -q "Option B" .planning/PROJECT.md
# NUB-RESOURCE got the Implementation Note section
grep -q "^## Implementation Note" .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
grep -q "downstream shell repo" .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
# Hygiene: NUB-RESOURCE draft is destined for napplet/nubs (PUBLIC) — must NOT mention @napplet/* private packages
! grep -q "@napplet/" .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
! grep -i -q "kehto\\|hyprgate" .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
# PROJECT.md is a planning doc; @napplet/* refs are fine here, but kehto/hyprgate still banned
! grep -i -q "kehto\\|hyprgate" .planning/PROJECT.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Demo coordination (v0.28.0)" .planning/PROJECT.md`
    - `grep -q "downstream shell repo" .planning/PROJECT.md` (in the v0.28.0 section, not just elsewhere)
    - `grep -q "Option B" .planning/PROJECT.md`
    - `grep -q "^## Implementation Note" .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md`
    - `grep -q "downstream shell repo" .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md`
    - Hygiene: NUB-RESOURCE.md still has ZERO `@napplet/` references (public-repo destination)
    - Hygiene: ZERO kehto/hyprgate in either file
    - All other sections of both files byte-stable (no incidental edits)
  </acceptance_criteria>
  <done>PROJECT.md contains the v0.28.0 demo coordination note; NUB-RESOURCE draft contains an Implementation Note both delegating demo napplets to the downstream shell repo per Option B. DEMO-01 satisfied.</done>
</task>

<task type="auto">
  <name>Task 5: Final hygiene + workspace verification gate</name>
  <files></files>
  <read_first>
    - All 9 files modified/created in Tasks 1–4
  </read_first>
  <action>
Run the final hygiene + acceptance gate before declaring this plan complete. NO source code changes; this is a verification-only step that catches any leakage missed in per-task verifies.

Steps:

1. **Hygiene sweep across the entire scope of this phase.** From the repo root, run:
   ```bash
   grep -ri "kehto\|hyprgate" \
     packages/nub/README.md \
     packages/shim/README.md \
     packages/sdk/README.md \
     packages/vite-plugin/README.md \
     README.md \
     skills/build-napplet/SKILL.md \
     specs/SHELL-RESOURCE-POLICY.md \
     .planning/PROJECT.md \
     .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
   ```
   MUST return zero matches. If any match, fix the offending file before proceeding.

2. **`@napplet/` reference audit on PUBLIC-destined files only.** The NUB-RESOURCE draft is destined for `napplet/nubs` (PUBLIC) and the new `specs/SHELL-RESOURCE-POLICY.md` may end up in a public-facing context. Both MUST be `@napplet/`-clean:
   ```bash
   grep -n "@napplet/" specs/SHELL-RESOURCE-POLICY.md \
     .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
   ```
   MUST return zero matches.

   Per CONTEXT NOTE: `@napplet/*` references in the 5 package READMEs, the root README, the build-napplet SKILL.md, and PROJECT.md are EXPECTED and NOT a hygiene failure (these are first-party docs that intentionally reference the @napplet/* packages they document).

3. **Workspace type-check stays green.** Run:
   ```bash
   pnpm -r type-check
   ```
   MUST exit 0. (Doc-only changes; no source touched. If this fails, an earlier task accidentally edited source code — investigate the diff.)

4. **Requirement coverage spot-check.** Confirm each REQ-ID is addressed:
   - DOC-01 → packages/nub/README.md updated (Task 1A)
   - DOC-02 → packages/shim/README.md updated (Task 1B)
   - DOC-03 → packages/sdk/README.md updated (Task 1C)
   - DOC-04 → packages/vite-plugin/README.md updated (Task 1D)
   - DOC-05 → README.md (root) updated (Task 1E)
   - DOC-06 → skills/build-napplet/SKILL.md updated (Task 2)
   - DOC-07 → specs/SHELL-RESOURCE-POLICY.md created (Task 3)
   - DEMO-01 → PROJECT.md + NUB-RESOURCE draft notes (Task 4)
  </action>
  <verify>
    <automated>
# Hygiene sweep across all 9 modified files
! grep -ri "kehto\|hyprgate" \
  packages/nub/README.md \
  packages/shim/README.md \
  packages/sdk/README.md \
  packages/vite-plugin/README.md \
  README.md \
  skills/build-napplet/SKILL.md \
  specs/SHELL-RESOURCE-POLICY.md \
  .planning/PROJECT.md \
  .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
# Public-repo files must be @napplet/-clean
! grep -q "@napplet/" specs/SHELL-RESOURCE-POLICY.md
! grep -q "@napplet/" .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
# Workspace stays green
pnpm -r type-check
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -ri "kehto\\|hyprgate"` across all 9 modified files returns 0 matches
    - `grep -q "@napplet/" specs/SHELL-RESOURCE-POLICY.md` returns false (zero matches)
    - `grep -q "@napplet/" .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md` returns false (zero matches)
    - `pnpm -r type-check` exits 0 across all 14 workspace packages
    - All 8 REQ-IDs (DOC-01..07, DEMO-01) traced to a concrete artifact above
  </acceptance_criteria>
  <done>Hygiene gates green, workspace type-check green, all 8 REQ-IDs satisfied. Phase 133 ready for verification.</done>
</task>

</tasks>

<verification>
After all 5 tasks complete, the following SHOULD be observable:

1. `grep -l "@napplet/nub/resource" packages/nub/README.md packages/shim/README.md packages/sdk/README.md README.md` returns at least these 4 files
2. `grep -l "strictCsp" packages/vite-plugin/README.md` confirms the new option section exists
3. `grep -q "napplet\\.resource\\.bytes" skills/build-napplet/SKILL.md` confirms napplet-author guidance updated
4. `test -f specs/SHELL-RESOURCE-POLICY.md` confirms shell-deployer doc exists
5. `grep -q "downstream shell repo" .planning/PROJECT.md .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md` confirms demo coordination notes landed in both places
6. `pnpm -r type-check` exits 0
7. `grep -ri "kehto\\|hyprgate" packages/ specs/ skills/ README.md .planning/PROJECT.md .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md` returns zero matches

The phase is observably complete when a fresh agent reading the docs would write `napplet.resource.bytes(url)` instead of `<img src=externalUrl>`, a shell deployer has a concrete checklist for v0.28.0 conformance, and direct contributors know to take v0.28.0 demo work to the downstream shell repo.
</verification>

<success_criteria>
- All 8 REQ-IDs (DOC-01, DOC-02, DOC-03, DOC-04, DOC-05, DOC-06, DOC-07, DEMO-01) addressed by concrete file edits
- 5 package READMEs (nub, shim, sdk, vite-plugin, root) reflect v0.28.0 resource-NUB + strict-CSP surface; existing content otherwise byte-stable
- skills/build-napplet/SKILL.md teaches `napplet.resource.bytes(url)` as the canonical fetch path with explicit pitfall warnings against `fetch()` / `<img src=externalUrl>`
- specs/SHELL-RESOURCE-POLICY.md exists with all five required policy areas (private-IP, sidecar, SVG, MIME, redirect) + scheme whitelist + audit checklist
- PROJECT.md and NUB-RESOURCE draft both contain coordination notes delegating v0.28.0 demos to the downstream shell repo
- Public-repo hygiene clean: zero `@napplet/*` in public-destined files (NUB-RESOURCE draft, SHELL-RESOURCE-POLICY.md); zero `kehto`/`hyprgate` anywhere across the 9 modified files
- `pnpm -r type-check` exits 0 across all 14 workspace packages (doc-only changes)
</success_criteria>

<output>
After completion, create `.planning/phases/133-documentation-demo-coordination/133-01-SUMMARY.md` summarizing:
- Files modified and lines changed per file
- REQ-ID → file mapping for traceability
- Any deviations from this plan and the rationale
- Hygiene gate results (kehto/hyprgate sweep, @napplet/ public-doc audit)
- `pnpm -r type-check` result
</output>
