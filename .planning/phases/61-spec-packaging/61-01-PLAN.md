---
phase: 61-spec-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SPEC.md
  - RUNTIME-SPEC.md
  - CLAUDE.md
  - packages/acl/README.md
  - packages/core/README.md
  - packages/runtime/README.md
  - packages/services/README.md
  - packages/shell/README.md
  - packages/shim/README.md
  - packages/sdk/README.md
  - packages/vite-plugin/README.md
  - packages/core/src/constants.ts
  - packages/runtime/src/service-discovery.ts
  - packages/shim/src/discovery-shim.ts
  - packages/shim/src/napplet-keypair.ts
  - specs/NIP-5D.md
autonomous: true
requirements: [PKG-01, PKG-02, PKG-03]

must_haves:
  truths:
    - "SPEC.md no longer exists; RUNTIME-SPEC.md exists with internal-reference header"
    - "All package READMEs and source comments reference RUNTIME-SPEC.md, not SPEC.md"
    - "NIP-5D has a References section listing referenced NIPs"
    - "NIP-5D Implementations section lists @napplet/shim, @napplet/shell, and hyprgate with links"
  artifacts:
    - path: "RUNTIME-SPEC.md"
      provides: "Internal runtime protocol reference (renamed from SPEC.md)"
      contains: "internal runtime reference"
    - path: "specs/NIP-5D.md"
      provides: "Final NIP-5D v2 spec"
      contains: "## References"
  key_links:
    - from: "packages/*/README.md"
      to: "RUNTIME-SPEC.md"
      via: "markdown link"
      pattern: "RUNTIME-SPEC\\.md"
    - from: "specs/NIP-5D.md"
      to: "https://github.com/sandwichfarm/napplet"
      via: "Implementations section link"
      pattern: "sandwichfarm/napplet"
---

<objective>
Rename SPEC.md to RUNTIME-SPEC.md, update all cross-references, and finalize NIP-5D v2 format.

Purpose: Complete the spec packaging phase -- position RUNTIME-SPEC.md as internal reference and ensure NIP-5D v2 meets nostr-protocol/nips conventions for submission.
Output: RUNTIME-SPEC.md with header, updated READMEs and source comments, NIP-5D v2 with References section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-spec-packaging/61-CONTEXT.md
@specs/NIP-5D.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename SPEC.md to RUNTIME-SPEC.md and update all references</name>
  <files>
    SPEC.md, RUNTIME-SPEC.md, CLAUDE.md,
    packages/acl/README.md, packages/core/README.md, packages/runtime/README.md,
    packages/services/README.md, packages/shell/README.md, packages/shim/README.md,
    packages/sdk/README.md, packages/vite-plugin/README.md,
    packages/core/src/constants.ts, packages/runtime/src/service-discovery.ts,
    packages/shim/src/discovery-shim.ts, packages/shim/src/napplet-keypair.ts
  </files>
  <read_first>
    SPEC.md (first 20 lines only -- need to see current header),
    CLAUDE.md,
    packages/acl/README.md (last 10 lines -- Protocol Reference section),
    packages/core/src/constants.ts (line 58 area),
    packages/runtime/src/service-discovery.ts (lines 1-45),
    packages/shim/src/discovery-shim.ts (line 36 area),
    packages/shim/src/napplet-keypair.ts (lines 1-10)
  </read_first>
  <action>
Per D-01: Rename SPEC.md to RUNTIME-SPEC.md and update all internal references.

**Step 1: git mv**
Run `git mv SPEC.md RUNTIME-SPEC.md` to rename with history preservation.

**Step 2: Add internal-reference header to RUNTIME-SPEC.md**
Insert at the very top of the file (before the existing NIP-XXXX heading):

```
> **Internal Runtime Reference** -- This document describes the @napplet SDK's
> internal protocol implementation. It is NOT the NIP standard. For the
> authoritative protocol specification, see [NIP-5D](specs/NIP-5D.md).

---

```

That is: a blockquote note, a blank line, a horizontal rule, and a blank line before the existing content.

**Step 3: Update 8 package READMEs**
In each of these files, replace:
`- [Napplet Shell Protocol Specification](../../SPEC.md)`
with:
`- [Napplet Runtime Reference](../../RUNTIME-SPEC.md)`

Files: packages/acl/README.md, packages/core/README.md, packages/runtime/README.md, packages/services/README.md, packages/shell/README.md, packages/shim/README.md, packages/sdk/README.md, packages/vite-plugin/README.md

**Step 4: Update source file comments**
- `packages/core/src/constants.ts` line ~58: change `SPEC.md` to `RUNTIME-SPEC.md`
- `packages/runtime/src/service-discovery.ts` lines ~6 and ~40: change `SPEC.md` to `RUNTIME-SPEC.md`
- `packages/shim/src/discovery-shim.ts` line ~36: change `SPEC.md` to `RUNTIME-SPEC.md`
- `packages/shim/src/napplet-keypair.ts` line ~4: change `SPEC.md` to `RUNTIME-SPEC.md`

**Step 5: Verify CLAUDE.md**
CLAUDE.md has no literal "SPEC.md" references (already verified). The "Relationship to hyprgate" section mentions "The NIP specification draft lives in hyprgate" which is about the old hyprgate spec, not this file. No changes needed to CLAUDE.md.

**Step 6: Verify no remaining SPEC.md references in non-.planning, non-.claude/worktrees paths**
Run: `grep -r "SPEC\.md" --include="*.md" --include="*.ts" --include="*.js" . | grep -v ".planning/" | grep -v ".claude/worktrees/" | grep -v "RUNTIME-SPEC.md" | grep -v "node_modules/"`
Should return 0 lines.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && test -f RUNTIME-SPEC.md && test ! -f SPEC.md && grep -c "internal runtime reference\|Internal Runtime Reference" RUNTIME-SPEC.md | grep -q "[1-9]" && grep -rl "SPEC\.md" packages/ --include="*.md" --include="*.ts" | grep -v "node_modules" | grep -v "RUNTIME-SPEC" | wc -l | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - SPEC.md does not exist at repo root
    - RUNTIME-SPEC.md exists at repo root
    - RUNTIME-SPEC.md first line contains "Internal Runtime Reference"
    - RUNTIME-SPEC.md contains link to specs/NIP-5D.md
    - All 8 package READMEs link to ../../RUNTIME-SPEC.md not ../../SPEC.md
    - All 4 source files reference RUNTIME-SPEC.md in comments
    - `grep -r "SPEC\.md" packages/ --include="*.md" --include="*.ts" | grep -v RUNTIME` returns 0 lines
  </acceptance_criteria>
  <done>SPEC.md renamed to RUNTIME-SPEC.md with internal-reference header; all 12 cross-references updated</done>
</task>

<task type="auto">
  <name>Task 2: Finalize NIP-5D v2 format -- add References section and verify conventions</name>
  <files>specs/NIP-5D.md</files>
  <read_first>specs/NIP-5D.md</read_first>
  <action>
Per D-02 and D-03: Finalize NIP-5D v2 to match nostr-protocol/nips conventions.

**Audit current format against conventions:**
1. Setext headings for title -- ALREADY CORRECT (lines 1-2: `NIP-5D\n======`, lines 4-5: subtitle with `---`)
2. Draft/optional badges -- ALREADY CORRECT (line 7: `` `draft` `optional` ``)
3. Event Kinds table -- ALREADY CORRECT (lines 179-184)
4. Implementations section -- ALREADY CORRECT per D-03 (lines 188-191 list @napplet/shim, @napplet/shell, hyprgate)

**One format gap to fix: add References section.**

Insert a `## References` section between `## Implementations` and the end of the file. The References section collects all NIPs cited in the document body. Based on inline references in the current text:

```markdown
## References

- [NIP-01](01.md) -- Basic protocol flow (events, subscriptions, filters)
- [NIP-42](42.md) -- Authentication of clients to relays
- [NIP-45](45.md) -- Event counts
- [NIP-5A](5A.md) -- Napplet manifest format and aggregate hash
```

Note: Use relative links (`01.md`, `42.md`, etc.) per nostr-protocol/nips convention -- these resolve within the nips repo. NIP-07 is referenced in Security Considerations but only for context (not a protocol dependency), so include it:

```markdown
- [NIP-07](07.md) -- `window.nostr` signer capability
```

Final References section (5 entries, alphabetical by number):

```markdown
## References

- [NIP-01](01.md) -- Basic protocol flow
- [NIP-07](07.md) -- `window.nostr` signer capability
- [NIP-42](42.md) -- Authentication of clients to relays
- [NIP-45](45.md) -- Event counts
- [NIP-5A](5A.md) -- Napplet manifest format and aggregate hash
```

Place this section BEFORE `## Implementations` (References is content; Implementations is metadata -- follows NIP convention where implementations come last).

**Also verify:** The Implementations section per D-03 lists:
- `@napplet/shim` and `@napplet/shell` with link to github.com/sandwichfarm/napplet
- `hyprgate` with link to github.com/sandwichfarm/hyprgate

Current text (lines 189-191):
```
- [@napplet/shim](https://github.com/sandwichfarm/napplet) -- napplet-side SDK
- [@napplet/shell](https://github.com/sandwichfarm/napplet) -- shell runtime
- [hyprgate](https://github.com/sandwichfarm/hyprgate) -- reference shell implementation
```

This matches D-03 exactly. No change needed.

**Final line count check:** Should remain under 200 lines (currently 191 + ~8 for References = ~199).
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -q "## References" specs/NIP-5D.md && grep -q "NIP-01" specs/NIP-5D.md && grep -q "NIP-42" specs/NIP-5D.md && grep -q "NIP-5A" specs/NIP-5D.md && grep -q "## Implementations" specs/NIP-5D.md && grep -q "sandwichfarm/napplet" specs/NIP-5D.md && grep -q "sandwichfarm/hyprgate" specs/NIP-5D.md && wc -l < specs/NIP-5D.md | awk '{if ($1 <= 210) print "PASS"; else print "FAIL: " $1 " lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - specs/NIP-5D.md contains `## References` section
    - References section lists NIP-01, NIP-07, NIP-42, NIP-45, NIP-5A with relative links (01.md, 07.md, etc.)
    - `## References` appears before `## Implementations`
    - Implementations section lists @napplet/shim, @napplet/shell, hyprgate with correct links
    - File is under 210 lines total
    - Setext headings present (first line is `NIP-5D`, second line is `======`)
    - Draft badge present (`` `draft` `optional` ``)
  </acceptance_criteria>
  <done>NIP-5D v2 has References section with all cited NIPs, Implementations section verified per D-03, file under 210 lines, all nostr-protocol/nips conventions met</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `test ! -f SPEC.md` -- old file gone
2. `head -1 RUNTIME-SPEC.md` -- shows internal reference header
3. `grep -c "SPEC\.md" packages/*/README.md | grep -v ":0$"` -- returns nothing (all updated)
4. `grep "## References" specs/NIP-5D.md` -- section exists
5. `grep "## Implementations" specs/NIP-5D.md` -- section exists
6. `wc -l specs/NIP-5D.md` -- under 210
7. `pnpm type-check` -- no breakage from comment changes
</verification>

<success_criteria>
- SPEC.md renamed to RUNTIME-SPEC.md with internal-reference header
- All 8 package READMEs and 4 source files reference RUNTIME-SPEC.md
- NIP-5D v2 has References section, Implementations section, setext headings, draft badge
- NIP-5D under 210 lines
- No remaining SPEC.md references in packages/ directory
- Type-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/61-spec-packaging/61-01-SUMMARY.md`
</output>
