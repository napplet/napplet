---
phase: 28-architecture-topology-view
plan: 01
title: "Build a host-backed layered topology model and layout"
wave: 1
depends_on: []
files_modified:
  - apps/demo/index.html
  - apps/demo/src/main.ts
  - apps/demo/src/shell-host.ts
  - apps/demo/src/topology.ts
  - tests/unit/demo-topology-model.test.ts
  - tests/unit/demo-topology-render.test.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
must_haves:
  truths:
    - "The main flow renders napplets, shell, ACL, runtime, and each real service as distinct topology nodes instead of a single `shell-box`"
    - "The topology is generated from demo host/runtime truth and does not assume exactly two napplets or a fixed service list"
    - "ACL is represented as its own checkpoint/layer between napplets and runtime-facing behavior"
    - "Runtime is the direct parent of service nodes; services are not grouped under a fake extra container"
    - "tests/unit/demo-topology-model.test.ts locks required node ids, roles, and hierarchy relationships"
    - "tests/unit/demo-topology-render.test.ts or equivalent DOM coverage verifies the layered architecture view renders the required nodes"
    - "`pnpm --filter @napplet/demo build` succeeds after the topology-layout changes"
  artifacts:
    - path: "apps/demo/src/topology.ts"
      provides: "UI-facing topology model derived from demo host/runtime state"
      contains: "runtime"
    - path: "apps/demo/index.html"
      provides: "Layered architecture view skeleton"
      contains: "topology"
    - path: "tests/unit/demo-topology-model.test.ts"
      provides: "Topology-model regression coverage"
      contains: "topology-node-runtime"
  key_links:
    - from: "apps/demo/src/topology.ts"
      to: "apps/demo/src/shell-host.ts"
      via: "Host/service source of truth"
      pattern: "DEMO_PROTOCOL_PATHS|DEMO_SIGNER_MODE|registerService"
    - from: "apps/demo/src/topology.ts"
      to: "packages/runtime/src/runtime.ts"
      via: "Runtime-to-service relationships"
      pattern: "serviceRegistry|registerService"
---

# Plan 28-01: Build a Host-Backed Layered Topology Model and Layout

<objective>
Replace the demo's fixed three-column scene with a layered architecture view generated from demo host/runtime data, so the main topology teaches napplets, shell, ACL, runtime, and real services as separate nodes at a glance. This is the structural delivery for both ARCH-01 and ARCH-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-architecture-topology-view/28-CONTEXT.md
@.planning/phases/28-architecture-topology-view/28-RESEARCH.md

@apps/demo/index.html
@apps/demo/src/main.ts
@apps/demo/src/shell-host.ts
@apps/demo/src/flow-animator.ts
@packages/runtime/src/runtime.ts
@packages/runtime/src/enforce.ts
@packages/services/src/signer-service.ts
@packages/services/src/notification-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export a topology model that matches the real host architecture</name>
  <files>apps/demo/src/shell-host.ts, apps/demo/src/topology.ts, apps/demo/src/main.ts</files>
  <read_first>
    - apps/demo/src/shell-host.ts
    - apps/demo/src/main.ts
    - packages/runtime/src/runtime.ts
    - packages/runtime/src/enforce.ts
    - .planning/phases/28-architecture-topology-view/28-CONTEXT.md
    - .planning/phases/28-architecture-topology-view/28-RESEARCH.md
  </read_first>
  <action>
    Add a new `apps/demo/src/topology.ts` module that exports a concrete topology model for the main architecture view.

    The topology model must define explicit node ids for:
    - `topology-node-shell`
    - `topology-node-acl`
    - `topology-node-runtime`
    - one node per napplet using a stable prefix such as `topology-node-napplet-<name>`
    - one node per real service using a stable prefix such as `topology-node-service-<name>`

    The model must also define explicit parent-child or edge relationships so these facts are mechanically true:
    - napplet nodes sit in a shared napplet layer
    - shell is separate from ACL
    - ACL is separate from runtime
    - runtime is the direct parent of each service node
    - services shown come only from the demo's real wired services, with `signer` present today and future services able to appear from the same model

    Update `apps/demo/src/shell-host.ts` to export whatever topology inputs are needed from real host/runtime state, such as the registered service names and the current napplet list. Update `apps/demo/src/main.ts` to build the topology from this model rather than relying on hard-coded chat/shell/bot assumptions.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -q "topology-node-runtime" apps/demo/src/topology.ts && grep -q "topology-node-acl" apps/demo/src/topology.ts && grep -q "topology-node-service-" apps/demo/src/topology.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/demo/src/topology.ts exists
    - apps/demo/src/topology.ts contains the exact node id strings `topology-node-shell`, `topology-node-acl`, and `topology-node-runtime`
    - apps/demo/src/topology.ts contains a stable service-node prefix such as `topology-node-service-`
    - apps/demo/src/main.ts imports and uses the topology model
    - apps/demo/src/shell-host.ts exports topology inputs derived from real demo host/runtime state instead of a UI-only placeholder list
  </acceptance_criteria>
  <done>The demo exposes a single topology truth source that matches the layered architecture</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the main flow markup into a layered architecture view</name>
  <files>apps/demo/index.html, apps/demo/src/main.ts</files>
  <read_first>
    - apps/demo/index.html
    - apps/demo/src/main.ts
    - apps/demo/src/topology.ts
    - apps/demo/src/flow-animator.ts
    - .planning/phases/28-architecture-topology-view/28-CONTEXT.md
  </read_first>
  <action>
    Replace the current flow-area structure in `apps/demo/index.html` so the main scene is no longer `chat -> shell/acl -> bot`.

    Render the layout as a top-to-bottom layered stack with these named regions:
    - a napplet layer that can render multiple napplet cards
    - a shell node region
    - an ACL checkpoint region
    - a runtime node region
    - a service branch region attached directly to runtime

    Remove the old single center heading `shell / acl` and replace it with separate labels for `shell`, `acl`, and `runtime`. Keep the bottom debugger section intact. The napplet cards still need containers for the existing chat and bot iframes, but the architecture layout must no longer assume those are the only napplets that can exist.

    Add stable DOM ids or `data-*` attributes for the new topology nodes and edges so animation code can target them without inferring layout structure from CSS classes.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && ! grep -q "shell / acl" apps/demo/index.html && grep -q "topology-node-runtime" apps/demo/index.html && grep -q "topology-node-acl" apps/demo/index.html</automated>
  </verify>
  <acceptance_criteria>
    - apps/demo/index.html no longer contains the exact text `shell / acl`
    - apps/demo/index.html contains separate runtime and ACL topology node ids
    - The flow area markup contains a distinct napplet region and a distinct service region
    - The bottom debugger section remains present in apps/demo/index.html
    - apps/demo/src/main.ts still mounts the chat and bot napplets into the new structure
  </acceptance_criteria>
  <done>The main demo view teaches the layered architecture structurally instead of by implication</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for topology structure and rendering</name>
  <files>tests/unit/demo-topology-model.test.ts, tests/unit/demo-topology-render.test.ts</files>
  <read_first>
    - tests/unit/demo-host-audit.test.ts
    - apps/demo/src/topology.ts
    - apps/demo/index.html
    - apps/demo/src/main.ts
  </read_first>
  <action>
    Create two focused tests:

    1. `tests/unit/demo-topology-model.test.ts`
    Assert that the topology model includes:
    - `topology-node-shell`
    - `topology-node-acl`
    - `topology-node-runtime`
    - at least one `topology-node-napplet-...`
    - at least one `topology-node-service-...`

    Also assert that ACL is not the parent of runtime and that runtime is the direct parent of each service node.

    2. `tests/unit/demo-topology-render.test.ts`
    Add DOM-oriented coverage for the rendered architecture skeleton or generated markup, verifying that the main view contains separate shell, ACL, runtime, napplet, and service regions and no longer renders the old merged `shell / acl` label.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && test -f tests/unit/demo-topology-model.test.ts && test -f tests/unit/demo-topology-render.test.ts && grep -q "topology-node-runtime" tests/unit/demo-topology-model.test.ts && grep -q "shell / acl" tests/unit/demo-topology-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/demo-topology-model.test.ts exists
    - tests/unit/demo-topology-render.test.ts exists
    - The model test asserts the exact node ids `topology-node-shell`, `topology-node-acl`, and `topology-node-runtime`
    - The render test checks that the old merged label `shell / acl` is no longer part of the architecture view
  </acceptance_criteria>
  <done>Topology structure is protected by unit-level regression coverage</done>
</task>

</tasks>

<verify>
  <summary>
    Build the demo and run the new topology model/render tests. Confirm the main scene now has a layered architecture skeleton and that the topology model exposes runtime-direct service branching with ACL as a separate checkpoint.
  </summary>
  <automated>
    cd /home/sandwich/Develop/napplet && pnpm vitest run tests/unit/demo-topology-model.test.ts tests/unit/demo-topology-render.test.ts
    cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/demo build
  </automated>
  <manual>
    Open the demo and verify the first read is napplets -> shell -> ACL -> runtime -> services, not chat -> shell/acl -> bot.
  </manual>
</verify>

