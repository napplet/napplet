---
phase: 86-decision-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/SPEC-GAPS.md]
autonomous: false
requirements: [DECIDE-01]

must_haves:
  truths:
    - "Every gap item (GAP-01 through GAP-09, including GAP-02a/b/c sub-items) has a recorded decision"
    - "Each decision is one of: drop, amend-spec, defer, or keep-shell-only"
    - "The spec author has seen evidence for each gap before deciding"
    - "A summary of decisions by category exists at the top of SPEC-GAPS.md"
  artifacts:
    - path: ".planning/SPEC-GAPS.md"
      provides: "Updated gap inventory with decisions recorded per entry and summary table"
      contains: "Decision:"
  key_links:
    - from: ".planning/SPEC-GAPS.md"
      to: "Phase 87+ planning"
      via: "Decisions determine what gets dropped, amended, deferred"
      pattern: "Decision:.*?(drop|amend-spec|defer|keep-shell-only)"
---

<objective>
Present the complete SPEC-GAPS.md inventory to the spec author and capture a drop-or-amend decision for every gap item.

Purpose: The gap inventory from Phase 84 documents 10 items (GAP-01, GAP-02a, GAP-02b, GAP-02c, GAP-03, GAP-04, GAP-05, GAP-06, GAP-07, GAP-09) that exist in the codebase without spec backing. The spec author must decide the fate of each item before any code changes or spec amendments can proceed.

Output: SPEC-GAPS.md updated with a "Decision" field on every entry and a summary table of decisions by category.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/SPEC-GAPS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Present gap inventory and capture decisions</name>
  <files>.planning/SPEC-GAPS.md</files>
  <decision>The spec author must decide the fate of each unspecified code item in the SDK.</decision>
  <context>
Phase 84 produced a gap inventory (SPEC-GAPS.md) documenting 10 items across 7 GAP IDs that exist in the codebase without NIP-5D or NUB spec backing. Each item needs an explicit decision before the milestone can close.

Decision options per item (per D-02):
- `drop` — Remove from code entirely (delete the export/file)
- `amend-spec` — Add to NIP-5D or create/update a NUB spec to cover it
- `defer` — Keep in code for now, revisit in a future milestone
- `keep-shell-only` — Acknowledged as shell implementation concern, not SDK/spec (may still be removed from @napplet/core but is not a spec gap)
  </context>

  <action>
Read `.planning/SPEC-GAPS.md` fully. Then present each gap item to the user using the following procedure:

**For each gap item (in this order: GAP-01, GAP-02a, GAP-02b, GAP-02c, GAP-03, GAP-04, GAP-05, GAP-06, GAP-07, GAP-09):**

1. Present a concise summary of the gap:
   - ID and name
   - Current category from Phase 84 (shell-only, future-nub, superseded, unknown)
   - What it is (1-2 sentences)
   - Where it lives (file path and line)
   - Key evidence of no spec backing (1-2 sentences)
   - Phase 84's recommendation reasoning (1-2 sentences)
   - Any cross-references to other gap items

2. Ask the user for their decision using the four options: `drop`, `amend-spec`, `defer`, `keep-shell-only`

3. If the user provides additional reasoning or notes, capture that too.

**IMPORTANT for GAP-02c (16 unknown topics):** This item contains 16 individual topics spanning 5 sub-domains (shell:config, keybinds, wm, relay:scoped, audio). The user may want to give different decisions for different sub-groups. Ask whether they want to decide per-sub-group or for the entire GAP-02c as one.

**After capturing ALL decisions**, update `.planning/SPEC-GAPS.md` by adding a `### Decision` subsection to each gap section (after the existing content), formatted as:

```markdown
### Decision

**Verdict:** `{drop|amend-spec|defer|keep-shell-only}`
**Notes:** {Any reasoning or notes from the spec author, or "—" if none}
```

For GAP-02c, if the user gave per-sub-group decisions, record each sub-group decision separately within the GAP-02c Decision section.

Also update the Summary Table at the top of SPEC-GAPS.md to include a `Decision` column.

Per D-01: Present evidence before asking for verdict.
Per D-03: Record decisions in SPEC-GAPS.md itself.
  </action>

  <how-to-verify>
    The executor will present each gap and ask for a decision via interactive prompts. The user responds with their verdict for each item.
  </how-to-verify>

  <resume-signal>All decisions captured and recorded in SPEC-GAPS.md</resume-signal>

  <verify>
    <automated>grep -c "Verdict:" .planning/SPEC-GAPS.md</automated>
  </verify>

  <done>Every gap entry (GAP-01, GAP-02a, GAP-02b, GAP-02c, GAP-03, GAP-04, GAP-05, GAP-06, GAP-07, GAP-09) has a recorded Decision subsection with a verdict of drop, amend-spec, defer, or keep-shell-only. The Summary Table includes a Decision column.</done>
</task>

<task type="auto">
  <name>Task 2: Write decision summary and verify completeness</name>
  <files>.planning/SPEC-GAPS.md</files>
  <action>
After all decisions are recorded in SPEC-GAPS.md (from Task 1), add a new top-level section `## Decision Summary` immediately after the Summary Table. This section should contain:

1. **Counts by verdict:**
   - How many items got `drop`
   - How many items got `amend-spec`
   - How many items got `defer`
   - How many items got `keep-shell-only`

2. **Action items table** listing only the items that require follow-up work:

```markdown
| Verdict | Items | Next Action |
|---------|-------|-------------|
| drop | {list of GAP IDs} | Delete from codebase in next phase |
| amend-spec | {list of GAP IDs} | Add to NIP-5D or create NUB spec |
| defer | {list of GAP IDs} | Carry forward to future milestone |
| keep-shell-only | {list of GAP IDs} | Move to shell repo or document as shell concern |
```

Only include rows for verdicts that have at least one item.

3. **Verify completeness**: Ensure every gap ID has exactly one Decision subsection. If any are missing, flag them clearly.

Per D-04: Updated SPEC-GAPS.md with decisions per entry.
Per D-05: Summary of decisions by category.
  </action>

  <verify>
    <automated>grep -c "Verdict:.*\(drop\|amend-spec\|defer\|keep-shell-only\)" .planning/SPEC-GAPS.md | xargs test 10 -le</automated>
  </verify>

  <done>SPEC-GAPS.md contains a Decision Summary section with counts by verdict and an action items table. Every gap ID (10 items) has a recorded decision. The file is ready to drive Phase 87+ planning.</done>
</task>

</tasks>

<verification>
1. `grep -c "### Decision" .planning/SPEC-GAPS.md` returns at least 10 (one per gap item)
2. `grep "Verdict:" .planning/SPEC-GAPS.md` shows only valid verdicts (drop, amend-spec, defer, keep-shell-only)
3. `grep "## Decision Summary" .planning/SPEC-GAPS.md` returns a match
4. Every GAP ID from the Summary Table has a corresponding Decision subsection
</verification>

<success_criteria>
- Every gap item (10 entries) has a recorded decision with one of: drop, amend-spec, defer, keep-shell-only
- The spec author has reviewed evidence for each gap before deciding
- A Decision Summary section exists with counts by verdict and an action items table
- SPEC-GAPS.md is self-contained: it documents both the gaps and their resolutions
</success_criteria>

<output>
After completion, create `.planning/phases/86-decision-gate/86-01-SUMMARY.md`
</output>
