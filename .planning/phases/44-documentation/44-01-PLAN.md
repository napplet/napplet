---
phase: 44
plan: 1
title: "Update SPEC.md for shim/SDK split"
wave: 1
depends_on: []
requirements_addressed: [ECO-03]
files_modified:
  - SPEC.md
autonomous: true
---

# Plan 44-01: Update SPEC.md for Shim/SDK Split

## Objective

Make surgical updates to SPEC.md so that the specification reflects the new `window.napplet` namespaced shape and the two-package ecosystem (`@napplet/shim` + `@napplet/sdk`). Per CONTEXT decision D-01, no new dedicated section is added -- only existing sections are updated where they reference old API patterns.

## Tasks

<task id="44-01-01">
<title>Update Section 4.1 to reference both shim and SDK</title>

<read_first>
- SPEC.md (lines 460-470 — Section 4.1 Overview)
- .planning/phases/44-documentation/44-CONTEXT.md
- .planning/phases/41-shim-restructure/41-CONTEXT.md
</read_first>

<action>
In `SPEC.md`, find the sentence on line 466:

```
The napplet-side SDK (`@napplet/shim`) provides a `window.nostr`-compatible interface that transparently bridges to the shell via postMessage.
```

Replace with:

```
The napplet-side packages provide convenience layers over the raw wire protocol. `@napplet/shim` is a side-effect-only module that installs a `window.napplet` global with namespaced sub-objects (`relay`, `ipc`, `services`, `storage`) and a `window.nostr`-compatible NIP-07 interface. `@napplet/sdk` provides the same API surface as named TypeScript exports for bundler-consuming developers. Both delegate to the shell via postMessage.
```

This is a single-sentence replacement that introduces both packages and the `window.napplet` shape without adding a new section.
</action>

<acceptance_criteria>
- `grep "side-effect-only module" SPEC.md` returns 1 match in Section 4.1
- `grep "@napplet/sdk" SPEC.md` returns at least 1 match (Section 4.1)
- `grep "window.napplet" SPEC.md` returns at least 1 match
- `grep "relay.*ipc.*services.*storage" SPEC.md` returns at least 1 match documenting the sub-objects
- The old text `The napplet-side SDK (\`@napplet/shim\`) provides` no longer appears in SPEC.md
</acceptance_criteria>
</task>

<task id="44-01-02">
<title>Add convenience-layer note to Section 16.1</title>

<read_first>
- SPEC.md (lines 1247-1306 — Section 16.1 Minimal Napplet)
- .planning/phases/44-documentation/44-CONTEXT.md
- .planning/phases/44-documentation/44-RESEARCH.md
</read_first>

<action>
Section 16.1 shows raw wire protocol code (generating keypair, handling AUTH, sending REQ/EVENT via postMessage). This is correct and should NOT be replaced -- it documents the protocol layer. Add a note after the code block (after line 1305's closing triple backtick) that directs developers to the convenience packages.

After the closing ``` of the Section 16.1 code block (the line that closes the `publishNote` function example), insert:

```markdown

> **Note:** The example above demonstrates the raw wire protocol. In practice, napplet developers use `@napplet/shim` (installs `window.napplet` global with `relay`, `ipc`, `services`, `storage` sub-objects) or `@napplet/sdk` (same API as named TypeScript imports). See the package READMEs for the convenience API.
```

This is a blockquote note, consistent with SPEC.md's existing style of using blockquotes for implementation guidance (see Section 2.6 notes pattern).
</action>

<acceptance_criteria>
- `SPEC.md` contains the text `The example above demonstrates the raw wire protocol` after the Section 16.1 code block
- `grep "window.napplet.*relay.*ipc.*services.*storage" SPEC.md` returns at least 1 match in the new note
- `grep "@napplet/sdk.*named TypeScript imports" SPEC.md` returns 1 match
- The raw wire protocol code in Section 16.1 is unchanged (still contains `window.parent.postMessage`, `generateKeypair`, etc.)
- The note appears BEFORE Section 16.2 (Minimal Shell)
</acceptance_criteria>
</task>

<task id="44-01-03">
<title>Update Section 17.1 package list</title>

<read_first>
- SPEC.md (lines 1380-1390 — Section 17.1 Reference Implementation)
- .planning/phases/44-documentation/44-CONTEXT.md
</read_first>

<action>
In `SPEC.md`, find the package list in Section 17.1 (around lines 1384-1388):

```markdown
- **@napplet/shim** (napplet-side SDK): `packages/shim/src/` (relay-shim.ts, index.ts, state-shim.ts, keyboard-shim.ts)
- **@napplet/shell** (shell-side runtime): `packages/shell/src/` (shell-bridge.ts, state-proxy.ts, acl-store.ts, audio-manager.ts)
- **@napplet/vite-plugin** (dev tooling): `packages/vite-plugin/src/` (NIP-5A manifest generation)
```

Replace with:

```markdown
- **@napplet/shim** (window installer): `packages/shim/src/` — side-effect-only module that installs `window.napplet` global and `window.nostr` NIP-07 bridge
- **@napplet/sdk** (developer API): `packages/sdk/src/` — named TypeScript exports wrapping `window.napplet` for bundler consumers
- **@napplet/core** (shared types): `packages/core/src/` — protocol types, constants, and bus kind definitions
- **@napplet/runtime** (protocol engine): `packages/runtime/src/` — framework-agnostic NIP-01 message router, AUTH handler, ACL enforcement
- **@napplet/acl** (access control): `packages/acl/src/` — pure, zero-dep ACL module with bitfield capabilities
- **@napplet/services** (service handlers): `packages/services/src/` — audio and notification ServiceHandler implementations
- **@napplet/shell** (browser adapter): `packages/shell/src/` — browser-specific shell adapter over runtime
- **@napplet/vite-plugin** (dev tooling): `packages/vite-plugin/src/` — NIP-5A manifest generation at build time
```

This updates the old 3-package list to the full 8-package list, with accurate one-line descriptions reflecting the v0.8.0 architecture. The shim description now says "window installer" instead of "napplet-side SDK". The SDK is listed as a separate entry.
</action>

<acceptance_criteria>
- `grep "@napplet/sdk" SPEC.md` returns at least 2 matches (Section 4.1 + Section 17.1)
- `grep "@napplet/core" SPEC.md` returns at least 1 match in Section 17.1
- `grep "@napplet/runtime" SPEC.md` returns at least 1 match in Section 17.1
- `grep "@napplet/acl" SPEC.md` returns at least 1 match in Section 17.1
- `grep "@napplet/services" SPEC.md` returns at least 1 match in Section 17.1
- `grep "window installer" SPEC.md` returns 1 match (shim description)
- The old text `(napplet-side SDK)` no longer appears in SPEC.md
- The package list contains exactly 8 bullet points
</acceptance_criteria>
</task>

## Verification

```bash
# Verify Section 4.1 updated
grep "side-effect-only module" SPEC.md
# Expected: 1 match

# Verify window.napplet documented
grep "window.napplet" SPEC.md
# Expected: at least 2 matches (Section 4.1 + Section 16.1 note)

# Verify SDK referenced
grep "@napplet/sdk" SPEC.md
# Expected: at least 2 matches

# Verify old shim description gone
grep "napplet-side SDK" SPEC.md
# Expected: 0 matches

# Verify Section 17.1 has all packages
grep -c "@napplet/" SPEC.md
# Expected: at least 10 (packages referenced across multiple sections)

# Verify raw wire protocol example preserved
grep "window.parent.postMessage" SPEC.md
# Expected: at least 2 matches (Section 16.1 napplet + Section 16.2 shell)
```

## must_haves

- [ ] Section 4.1 mentions both `@napplet/shim` and `@napplet/sdk` with their distinct roles
- [ ] `window.napplet` with `relay`, `ipc`, `services`, `storage` sub-objects is documented in SPEC.md
- [ ] Section 16.1 raw wire protocol example is preserved unchanged
- [ ] Section 16.1 has a note directing developers to convenience packages
- [ ] Section 17.1 lists all 8 current @napplet packages including @napplet/sdk
