---
phase: 40-remaining-rename-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shim/src/napplet-keypair.ts
  - packages/shim/src/index.ts
  - packages/shim/src/keyboard-shim.ts
  - packages/vite-plugin/src/index.ts
  - apps/demo/napplets/chat/vite.config.ts
  - apps/demo/napplets/bot/vite.config.ts
  - tests/fixtures/napplets/publish-napplet/vite.config.ts
  - tests/fixtures/napplets/auth-napplet/vite.config.ts
autonomous: true
requirements:
  - SESS-03
  - TERM-01

must_haves:
  truths:
    - "loadOrCreateKeypair does not exist anywhere in packages/"
    - "createEphemeralKeypair takes no parameters"
    - "Nip5aManifestOptions.nappletType is the public API field name"
    - "All vite.config.ts consumers use nappletType instead of nappType"
    - "pnpm type-check passes with zero errors"
  artifacts:
    - path: "packages/shim/src/napplet-keypair.ts"
      provides: "createEphemeralKeypair() function with no params"
      contains: "export function createEphemeralKeypair"
    - path: "packages/vite-plugin/src/index.ts"
      provides: "Nip5aManifestOptions with nappletType field"
      contains: "nappletType: string"
  key_links:
    - from: "packages/shim/src/index.ts"
      to: "packages/shim/src/napplet-keypair.ts"
      via: "import { createEphemeralKeypair }"
      pattern: "import.*createEphemeralKeypair.*from.*napplet-keypair"
    - from: "apps/demo/napplets/chat/vite.config.ts"
      to: "packages/vite-plugin/src/index.ts"
      via: "nappletType option"
      pattern: "nappletType:"
---

<objective>
Rename the two remaining TypeScript identifier gaps from the v0.7.0 audit:
1. `loadOrCreateKeypair(_nappType)` -> `createEphemeralKeypair()` in @napplet/shim (SESS-03)
2. `Nip5aManifestOptions.nappType` -> `nappletType` in @napplet/vite-plugin and all consumers (TERM-01)

Purpose: Complete the ontology rename so no `napp[^l]` TypeScript identifiers remain in production code.
Output: All TypeScript files updated, type-check green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-remaining-rename-gaps/40-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename loadOrCreateKeypair to createEphemeralKeypair (SESS-03)</name>
  <files>
    packages/shim/src/napplet-keypair.ts
    packages/shim/src/index.ts
    packages/shim/src/keyboard-shim.ts
  </files>
  <read_first>
    packages/shim/src/napplet-keypair.ts
    packages/shim/src/index.ts
    packages/shim/src/keyboard-shim.ts
  </read_first>
  <action>
In `packages/shim/src/napplet-keypair.ts`:
- Rename the function from `loadOrCreateKeypair` to `createEphemeralKeypair`
- Remove the `_nappType: string` parameter entirely (the function takes no arguments)
- Update the JSDoc: remove the `@param _nappType` line. Keep the rest of the doc comment as-is.
- Keep the `TODO(SEED-001)` comment block above the function as-is (it documents a future design direction)

The function signature changes from:
```typescript
export function loadOrCreateKeypair(_nappType: string): NappletKeypair {
```
to:
```typescript
export function createEphemeralKeypair(): NappletKeypair {
```

In `packages/shim/src/index.ts`:
- Line 7: Change `import { loadOrCreateKeypair } from './napplet-keypair.js';` to `import { createEphemeralKeypair } from './napplet-keypair.js';`
- Line 228: Change `keypair = loadOrCreateKeypair(nappletType);` to `keypair = createEphemeralKeypair();`
- Line 344: Change `keypair = loadOrCreateKeypair(nappletType);` to `keypair = createEphemeralKeypair();`

In `packages/shim/src/keyboard-shim.ts`:
- Line 9: Change comment from `// keypair is set by index.ts after loadOrCreateKeypair via setKeyboardShimKeypair()` to `// keypair is set by index.ts after createEphemeralKeypair via setKeyboardShimKeypair()`
- Line 14: Change comment from `* Called by index.ts immediately after loadOrCreateKeypair() resolves and also inside` to `* Called by index.ts immediately after createEphemeralKeypair() resolves and also inside`
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -r 'loadOrCreateKeypair' packages/ | grep -v node_modules | grep -v dist | wc -l | xargs test 0 -eq && echo "PASS: zero loadOrCreateKeypair hits" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r 'loadOrCreateKeypair' packages/` returns 0 hits (excluding node_modules/dist)
    - `packages/shim/src/napplet-keypair.ts` contains `export function createEphemeralKeypair(): NappletKeypair`
    - `packages/shim/src/index.ts` contains `import { createEphemeralKeypair } from './napplet-keypair.js'`
    - `packages/shim/src/index.ts` contains `keypair = createEphemeralKeypair();` (two occurrences, no arguments)
    - `packages/shim/src/keyboard-shim.ts` line 9 contains `createEphemeralKeypair` instead of `loadOrCreateKeypair`
  </acceptance_criteria>
  <done>loadOrCreateKeypair is fully replaced by createEphemeralKeypair with no parameters across all 3 shim files</done>
</task>

<task type="auto">
  <name>Task 2: Rename nappType to nappletType in vite-plugin and consumers (TERM-01)</name>
  <files>
    packages/vite-plugin/src/index.ts
    apps/demo/napplets/chat/vite.config.ts
    apps/demo/napplets/bot/vite.config.ts
    tests/fixtures/napplets/publish-napplet/vite.config.ts
    tests/fixtures/napplets/auth-napplet/vite.config.ts
    tests/fixtures/napplets/auth-napplet/src/main.ts
  </files>
  <read_first>
    packages/vite-plugin/src/index.ts
    apps/demo/napplets/chat/vite.config.ts
    apps/demo/napplets/bot/vite.config.ts
    tests/fixtures/napplets/publish-napplet/vite.config.ts
    tests/fixtures/napplets/auth-napplet/vite.config.ts
    tests/fixtures/napplets/auth-napplet/src/main.ts
  </read_first>
  <action>
In `packages/vite-plugin/src/index.ts` -- replace all 9 occurrences of `nappType` with `nappletType`:
- Line 22: `nappType: string;` -> `nappletType: string;`
- Line 63: JSDoc `nappType` -> `nappletType` in the @param description
- Line 91: `content: options.nappType,` -> `content: options.nappletType,`
- Line 100: `content: options.nappType,` -> `content: options.nappletType,`
- Line 156: `['d', options.nappType],` -> `['d', options.nappletType],`
- Line 186: `options.nappType` in console.log -> `options.nappletType`
- Line 193: `options.nappType` in console.log -> `options.nappletType`
- Line 205: `options.nappType` in console.log -> `options.nappletType`
- Line 209: `options.nappType` in console.log -> `options.nappletType`

Also update the JSDoc for the interface field (line 21) from:
`/** Napplet type/dtag identifier (e.g., 'feed', 'chat'). Used as the NIP-5A 'd' tag and injected as napplet-type meta attribute. */`
to:
`/** Napplet type/dtag identifier (e.g., 'feed', 'chat'). Used as the NIP-5A 'd' tag and injected as napplet-type meta attribute. */`
(This JSDoc is already correct -- no change needed to the description text, only the field name below it.)

In `apps/demo/napplets/chat/vite.config.ts` line 8:
- `nappType: 'demo-chat',` -> `nappletType: 'demo-chat',`

In `apps/demo/napplets/bot/vite.config.ts` line 8:
- `nappType: 'demo-bot',` -> `nappletType: 'demo-bot',`

In `tests/fixtures/napplets/publish-napplet/vite.config.ts` line 8:
- `nappType: 'publish-test',` -> `nappletType: 'publish-test',`

In `tests/fixtures/napplets/auth-napplet/vite.config.ts` line 8:
- `nappType: 'auth-test',` -> `nappletType: 'auth-test',`

In `tests/fixtures/napplets/auth-napplet/src/main.ts` line 5:
- Comment `* 1. Read nappType from <meta name="napplet-napp-type">` -> `* 1. Read nappletType from <meta name="napplet-napp-type">`

Do NOT rename `nappType` in `tests/helpers/auth-event-builder.ts` -- that is out of scope per the audit.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep 'nappType' packages/vite-plugin/src/index.ts | wc -l | xargs test 0 -eq && echo "PASS: zero nappType in vite-plugin" || echo "FAIL"; pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'nappType' packages/vite-plugin/src/index.ts` returns 0 hits
    - `packages/vite-plugin/src/index.ts` contains `nappletType: string;` in the interface
    - `apps/demo/napplets/chat/vite.config.ts` contains `nappletType: 'demo-chat'`
    - `apps/demo/napplets/bot/vite.config.ts` contains `nappletType: 'demo-bot'`
    - `tests/fixtures/napplets/publish-napplet/vite.config.ts` contains `nappletType: 'publish-test'`
    - `tests/fixtures/napplets/auth-napplet/vite.config.ts` contains `nappletType: 'auth-test'`
    - `pnpm type-check` passes with zero errors
  </acceptance_criteria>
  <done>nappType field is fully replaced by nappletType in vite-plugin public API and all 4 consumer config files; type-check green</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# SESS-03: No loadOrCreateKeypair anywhere
grep -r 'loadOrCreateKeypair' packages/ | grep -v node_modules | grep -v dist
# Expected: 0 hits

# TERM-01: No nappType in vite-plugin
grep 'nappType' packages/vite-plugin/src/index.ts
# Expected: 0 hits

# Full type check
pnpm type-check
# Expected: 0 errors
```
</verification>

<success_criteria>
- `createEphemeralKeypair()` is the sole function name in shim; `loadOrCreateKeypair` returns zero hits
- `Nip5aManifestOptions.nappletType` is the public API field; `nappType` returns zero hits in vite-plugin
- `pnpm type-check` passes across all packages
</success_criteria>

<output>
After completion, create `.planning/phases/40-remaining-rename-gaps/40-01-SUMMARY.md`
</output>
