---
phase: 68-audit-clean
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - RUNTIME-SPEC.md
  - skills/add-service/SKILL.md
  - skills/integrate-shell/SKILL.md
  - skills/build-napplet/SKILL.md
  - specs/nubs/NUB-STORAGE.md
  - specs/nubs/NUB-NOSTRDB.md
  - specs/nubs/NUB-PIPES.md
  - specs/nubs/NUB-IPC.md
  - specs/nubs/NUB-RELAY.md
  - specs/nubs/NUB-SIGNER.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]

must_haves:
  truths:
    - "RUNTIME-SPEC.md Section 17.1 lists only the 4 current @napplet packages plus notes that runtime/acl/services/shell moved to @kehto"
    - "skills/ files reference @kehto/shell and @kehto/services instead of @napplet/shell and @napplet/services"
    - "specs/nubs/ files reference @kehto package paths instead of @napplet/shell or @napplet/runtime paths"
    - "grep -r '@napplet/shell\\|@napplet/runtime\\|@napplet/acl\\|@napplet/services' skills/ specs/nubs/ RUNTIME-SPEC.md returns no matches"
  artifacts:
    - path: "RUNTIME-SPEC.md"
      provides: "Section 17.1 Reference Implementation with accurate package listing"
      contains: "@kehto/shell"
    - path: "skills/integrate-shell/SKILL.md"
      provides: "Shell integration guide pointing to @kehto/shell"
      contains: "@kehto/shell"
    - path: "skills/add-service/SKILL.md"
      provides: "Service handler guide pointing to @kehto/shell"
      contains: "@kehto/shell"
  key_links:
    - from: "skills/"
      to: "@kehto packages"
      via: "import statements in code examples"
      pattern: "@kehto/shell"
    - from: "specs/nubs/"
      to: "@kehto packages"
      via: "reference implementation links"
      pattern: "@kehto/"
---

<objective>
Update all documentation files that reference extracted packages (@napplet/shell, @napplet/runtime, @napplet/acl, @napplet/services) to point to their new @kehto equivalents.

Purpose: Eliminate stale documentation references so developers reading these files are directed to the correct packages.
Output: RUNTIME-SPEC.md, 3 skills files, and 6 specs/nubs files updated with accurate @kehto references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-audit-clean/68-CONTEXT.md

@RUNTIME-SPEC.md (lines 1490-1505 — Section 17.1 Reference Implementation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update RUNTIME-SPEC.md and specs/nubs references</name>
  <files>RUNTIME-SPEC.md, specs/nubs/NUB-STORAGE.md, specs/nubs/NUB-NOSTRDB.md, specs/nubs/NUB-PIPES.md, specs/nubs/NUB-IPC.md, specs/nubs/NUB-RELAY.md, specs/nubs/NUB-SIGNER.md</files>
  <action>
**RUNTIME-SPEC.md (DOC-01):**

1. Read lines 1490-1505 of RUNTIME-SPEC.md (Section 17.1 Reference Implementation).

2. Replace the package listing (lines 1493-1502) with an updated version that:
   - Lists the 4 current @napplet packages: @napplet/shim, @napplet/sdk, @napplet/core, @napplet/vite-plugin (keep existing descriptions)
   - Adds a note that @napplet/runtime, @napplet/acl, @napplet/services, and @napplet/shell have moved to the [kehto](https://github.com/sandwichfarm/kehto) monorepo as @kehto/* packages
   - Keep the hyprgate reference line unchanged

3. Search the rest of RUNTIME-SPEC.md for any other `@napplet/shell`, `@napplet/runtime`, `@napplet/acl`, or `@napplet/services` references. If found elsewhere, update those too.

**specs/nubs/ (DOC-03):**

4. Update each of the 6 nubs files that reference extracted packages. The specific changes:

   - `NUB-STORAGE.md` line 106: `@napplet/shell` reference with `packages/runtime/src/state-handler.ts` path → update to `@kehto/shell` with `packages/runtime/src/state-handler.ts` (the path is within the kehto repo now)
   
   - `NUB-NOSTRDB.md` line 93: `@napplet/shell` with shell-side NIPDB routing → update to `@kehto/shell` (shell-side routing is in kehto)
   
   - `NUB-PIPES.md` line 369: `@napplet/runtime` reference → update to `@kehto/runtime`
   
   - `NUB-IPC.md` line 105: `@napplet/shell` with shell-side IPC routing → update to `@kehto/shell`
   
   - `NUB-RELAY.md` line 115: `@napplet/shell` with `packages/runtime/src/shell-bridge.ts` → update to `@kehto/shell`
   
   - `NUB-SIGNER.md` line 84: `@napplet/shell` with `packages/runtime/src/signer-handler.ts` → update to `@kehto/shell`

   For all nubs files: Update the GitHub link from `https://github.com/sandwichfarm/napplet` to `https://github.com/sandwichfarm/kehto` where the reference points to shell/runtime code. Keep `napplet` links where they reference shim/core/sdk/vite-plugin code.

5. After all edits, verify no `@napplet/shell`, `@napplet/runtime`, `@napplet/acl`, or `@napplet/services` references remain in specs/nubs/ or RUNTIME-SPEC.md.
  </action>
  <verify>
    <automated>! grep -rq '@napplet/\(shell\|runtime\|acl\|services\)' RUNTIME-SPEC.md specs/nubs/</automated>
  </verify>
  <done>RUNTIME-SPEC.md Section 17.1 lists only @napplet/core, @napplet/shim, @napplet/sdk, @napplet/vite-plugin with a note about @kehto migration. All 6 specs/nubs files reference @kehto/shell or @kehto/runtime instead of @napplet equivalents. Zero grep matches for extracted package names in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Update skills files to reference @kehto packages</name>
  <files>skills/add-service/SKILL.md, skills/integrate-shell/SKILL.md, skills/build-napplet/SKILL.md</files>
  <action>
**skills/integrate-shell/SKILL.md (DOC-02):**

1. Read the full file. This skill teaches how to integrate @napplet/shell into a host app.

2. Replace ALL occurrences of `@napplet/shell` with `@kehto/shell` in:
   - The frontmatter description (line 3)
   - The title and overview text (lines 6, 10)
   - Install command: `pnpm add @napplet/shell nostr-tools` → `pnpm add @kehto/shell nostr-tools` (line 22)
   - Import statements: `from '@napplet/shell'` → `from '@kehto/shell'` (lines 30, 127)
   - Any other body text references

3. If the skill references `ShellHooks` — this type was renamed to `ShellAdapter` in v0.7.0 Phase 37 but kept a deprecated alias. Since kehto inherits the latest code, update `ShellHooks` → `ShellAdapter` if present.

**skills/add-service/SKILL.md (DOC-02):**

4. Read the full file. This skill teaches how to add a ServiceHandler.

5. Replace ALL occurrences:
   - `@napplet/shell` → `@kehto/shell` in import statements and prerequisite text (lines 14, 22, 38, 39, 40, 122)
   - `@napplet/services` → `@kehto/services` if referenced (line 166 area — check for createAudioService import)

6. Update the reference to `packages/services/src/audio-service.ts` if present — this path is now in the kehto repo.

**skills/build-napplet/SKILL.md (DOC-02):**

7. Read the full file. This skill teaches how to build a napplet using @napplet/shim.

8. Line 16 references "A host shell running `@napplet/shell`" → update to "A host shell running `@kehto/shell`". This is the only extracted-package reference in this file.

9. All references to `@napplet/shim`, `@napplet/sdk`, and `@napplet/vite-plugin` are CORRECT (these are current packages) — do NOT change them.

10. After all edits, verify no `@napplet/shell`, `@napplet/runtime`, `@napplet/acl`, or `@napplet/services` references remain in skills/.
  </action>
  <verify>
    <automated>! grep -rq '@napplet/\(shell\|runtime\|acl\|services\)' skills/</automated>
  </verify>
  <done>All 3 skills files reference @kehto/shell and @kehto/services instead of @napplet equivalents. References to @napplet/shim, @napplet/sdk, @napplet/vite-plugin, and @napplet/core are preserved (those are current packages). Zero grep matches for extracted package names in skills/.</done>
</task>

</tasks>

<verification>
- `! grep -rq '@napplet/\(shell\|runtime\|acl\|services\)' RUNTIME-SPEC.md` — spec has no stale refs
- `! grep -rq '@napplet/\(shell\|runtime\|acl\|services\)' skills/` — skills have no stale refs
- `! grep -rq '@napplet/\(shell\|runtime\|acl\|services\)' specs/nubs/` — nubs have no stale refs
- All docs still reference @napplet/core, @napplet/shim, @napplet/sdk, @napplet/vite-plugin correctly
</verification>

<success_criteria>
1. RUNTIME-SPEC.md references only the 4 current @napplet packages, notes @kehto migration
2. All 3 skills files point to @kehto/shell and @kehto/services
3. All 6 specs/nubs files point to @kehto/shell or @kehto/runtime
4. `grep -r '@napplet/\(shell\|runtime\|acl\|services\)' RUNTIME-SPEC.md skills/ specs/nubs/` returns zero matches
5. References to @napplet/core, @napplet/shim, @napplet/sdk, @napplet/vite-plugin remain intact
</success_criteria>

<output>
After completion, create `.planning/phases/68-audit-clean/68-02-SUMMARY.md`
</output>
