---
plan: 22.1-01
title: "Signer service — extract handleSignerRequest as ServiceHandler"
status: complete
completed_at: "2026-03-31"
commit: b016591
---

# Summary: Plan 22.1-01 — Signer Service

## What Was Done

Created `packages/services/src/signer-service.ts` with `createSignerService()` factory. Exports `SignerServiceOptions` type and the factory function. Barrel export updated in `packages/services/src/index.ts`.

Added vitest config and devDependency to `@napplet/services` (was missing). 18 unit tests written and passing.

## Artifacts Created

- `packages/services/src/signer-service.ts` — ServiceHandler implementation wrapping NIP-07 signer methods
- `packages/services/src/signer-service.test.ts` — 18 unit tests covering all signer methods, error cases, consent gating
- `packages/services/vitest.config.ts` — vitest config for services package
- `packages/services/src/index.ts` — updated barrel export

## Verification

- `pnpm build` — all 14 tasks successful
- `pnpm --filter @napplet/services type-check` — passes
- `pnpm --filter @napplet/services test:unit` — 18/18 tests pass
- `createSignerService` and `SignerServiceOptions` exported from `packages/services/dist/index.d.ts`

## Key Decisions

- Signer service sends both `['EVENT', '__signer__', responseEvent]` and `['OK', event.id, true, '']` via send callback (matching plan spec)
- Consent gating uses default kinds [0, 3, 5, 10002], configurable via `consentKinds` option
- `onWindowDestroyed` is a no-op (signer has no per-window state)
