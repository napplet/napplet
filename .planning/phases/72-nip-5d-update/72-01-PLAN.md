---
phase: 72-nip-5d-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/NIP-5D.md
autonomous: true
requirements: [DOC-02]

must_haves:
  truths:
    - "NIP-5D does not list AUTH as a required protocol step"
    - "NIP-5D does not reference REGISTER or IDENTITY verbs"
    - "NIP-5D describes identity as shell-assigned via MessageEvent.source at iframe creation"
    - "NIP-5D wire format tables contain only the active verbs (EVENT, REQ, CLOSE, COUNT for napplet-to-shell; EVENT, OK, EOSE, CLOSED, NOTICE, COUNT for shell-to-napplet)"
    - "NIP-5D is internally consistent with no dangling references to removed handshake, keypair, signing, or NIP-42"
  artifacts:
    - path: "specs/NIP-5D.md"
      provides: "Updated NIP-5D specification for v3.0.0 simplified wire protocol"
      contains: "MessageEvent.source"
  key_links:
    - from: "specs/NIP-5D.md"
      to: "Phase 70-71 protocol changes"
      via: "Spec reflects code reality"
      pattern: "MessageEvent\\.source"
---

<objective>
Update NIP-5D to reflect the simplified wire protocol established in Phases 70-71. The spec must accurately describe a protocol where napplets send plain NIP-01 messages without signing, and the shell identifies senders via MessageEvent.source at iframe creation time. The AUTH handshake, REGISTER/IDENTITY verbs, delegated keys, and NIP-42 references must all be removed.

Purpose: NIP-5D is the external protocol spec. It must match the v3.0.0 protocol reality so developers building against it implement the correct (simplified) wire protocol.
Output: Updated specs/NIP-5D.md with no AUTH/handshake references and accurate identity model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-nip-5d-update/72-CONTEXT.md
@.planning/phases/70-core-protocol-types/70-01-SUMMARY.md
@.planning/phases/71-shim-simplification/71-01-SUMMARY.md

<interfaces>
<!-- Current NIP-5D structure at specs/NIP-5D.md (142 lines) -->
<!-- Sections to update/rewrite: -->
<!-- Line 14-21: Terminology table — remove "Delegated key" row -->
<!-- Line 23-35: Transport — update "unregistered sources" language -->
<!-- Line 37-59: Wire Format tables — remove REGISTER, IDENTITY, AUTH verbs -->
<!-- Line 60-61: Note about new verbs — remove REGISTER/IDENTITY mention -->
<!-- Line 63-91: Authentication section — COMPLETE REWRITE to shell-assigned identity -->
<!-- Line 93-103: Extension Discovery — remove "post-AUTH" references -->
<!-- Line 115-124: Security Considerations — remove AUTH/handshake/NIP-42 references -->
<!-- Line 128-131: Event Kinds — remove kind 22242 AUTH -->
<!-- Line 135-140: References — remove NIP-42 reference -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite NIP-5D for simplified wire protocol</name>
  <files>specs/NIP-5D.md</files>
  <read_first>specs/NIP-5D.md, .planning/phases/70-core-protocol-types/70-01-SUMMARY.md, .planning/phases/71-shim-simplification/71-01-SUMMARY.md</read_first>
  <action>
Rewrite specs/NIP-5D.md to reflect the v3.0.0 simplified wire protocol. Make these specific changes:

**Terminology table:**
- Remove the "Delegated key" row entirely (no more deterministic secp256k1 keypair derivation)
- Remove the "Composite key" row (the concept still exists internally but the spec should not reference it as a wire protocol concept)
- Keep: Shell, Napplet, dTag, Aggregate hash, NUB

**Transport section:**
- Replace "Messages from unregistered sources MUST be silently dropped" with "Messages from unknown sources (iframes not created by the shell) MUST be silently dropped" — the concept is the same but avoids implying a REGISTER verb
- Add a sentence: "The shell maps each iframe's Window reference to its napplet identity (dTag, aggregateHash) at iframe creation time."

**Wire Format — Napplet-to-Shell table:**
- Remove the REGISTER row
- Remove the AUTH row
- Keep: EVENT, REQ, CLOSE, COUNT (4 verbs)

**Wire Format — Shell-to-Napplet table:**
- Remove the IDENTITY row
- Remove the AUTH row
- Keep: EVENT, OK, EOSE, CLOSED, NOTICE, COUNT (6 verbs)

**Wire Format note (after tables):**
- Change from "REQ, EVENT, CLOSE, EOSE, OK, CLOSED, COUNT, NOTICE follow NIP-01 semantics. REGISTER and IDENTITY are new verbs." to "All verbs follow [NIP-01](01.md) relay semantics. The shell acts as a virtual relay to each napplet."

**Authentication section — COMPLETE REWRITE:**
Replace the entire "## Authentication" section (lines 63-91) with a new "## Identity" section:

```
## Identity

The shell assigns napplet identity at iframe creation time. No handshake is required.

When the shell creates a napplet iframe, it maps the iframe's `Window` reference to the napplet's `(dTag, aggregateHash)` tuple. The shell derives an internal identity pubkey for each napplet using `HMAC-SHA256(shellSecret, dTag + aggregateHash)` where `shellSecret` is a 32-byte random value persisted per shell instance. Same dTag + same aggregateHash + same shell = same identity.

Napplets send unsigned event templates (kind, created_at, tags, content) without id, pubkey, or sig fields. The shell stamps inbound messages with the napplet's derived pubkey internally. Events signed with a user's key ([NIP-07](07.md)/NIP-46) go through the shell's signer proxy, not the napplet's identity.

The shell MUST verify `MessageEvent.source` on every inbound message. Messages from Window references not mapped to a napplet identity MUST be silently dropped.

The shell SHOULD check `requires` tags from the [NIP-5A](5A.md) manifest against available capabilities at iframe creation time.
```

**Extension Discovery section:**
- Change "post-AUTH" to "at load time" in the sentence about manifest requires tags: "The shell checks these during post-AUTH" becomes "The shell checks these at napplet load time"

**Security Considerations:**
- Remove item 3 ("AUTH handshake: one-time Schnorr signature via NIP-42...")
- Replace with: "Identity binding: the shell maps `MessageEvent.source` to napplet identity at iframe creation. No per-message or one-time signature verification is needed — the browser's `MessageEvent.source` is unforgeable within the same browsing context."
- Remove the NIP-42 reference from item 4 about delegated key confinement
- Update item 4 to: "Internal identity confinement: events stamped with shell-derived napplet pubkeys MUST NOT be published to external relays; the user's signer ([NIP-07](07.md)/NIP-46) is the only source of externally published events."
- Renumber items after removals to be sequential (1, 2, 3, 4, 5)

**Event Kinds table:**
- Remove the kind 22242 AUTH row entirely
- Replace with: "Interface-specific event kinds are defined in their respective NUB proposals." (already present as a note — just remove the table since it's now empty and keep the note)

**References:**
- Remove NIP-42 reference line
- Keep: NIP-01, NIP-07, NIP-45, NIP-5A
  </action>
  <verify>
    <automated>grep -cE "AUTH|REGISTER|IDENTITY|handshake|delegated key|22242|NIP-42|Schnorr" specs/NIP-5D.md | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "AUTH" specs/NIP-5D.md returns 0
    - grep -c "REGISTER" specs/NIP-5D.md returns 0
    - grep -c "IDENTITY" specs/NIP-5D.md returns 0
    - grep -c "handshake" specs/NIP-5D.md returns 0 (case-insensitive)
    - grep -c "delegated key" specs/NIP-5D.md returns 0 (case-insensitive)
    - grep -c "22242" specs/NIP-5D.md returns 0
    - grep -c "NIP-42" specs/NIP-5D.md returns 0
    - grep -c "Schnorr" specs/NIP-5D.md returns 0
    - grep -c "MessageEvent.source" specs/NIP-5D.md returns at least 3
    - grep -c "## Identity" specs/NIP-5D.md returns 1
    - grep -c "unsigned event templates" specs/NIP-5D.md returns at least 1
    - grep -c "iframe creation" specs/NIP-5D.md returns at least 1
    - The file has no section named "## Authentication"
  </acceptance_criteria>
  <done>NIP-5D contains zero references to AUTH, REGISTER, IDENTITY, handshake, delegated keys, kind 22242, NIP-42, or Schnorr signatures. New Identity section describes shell-assigned identity via MessageEvent.source. Wire format tables list only active verbs.</done>
</task>

<task type="auto">
  <name>Task 2: Verify internal consistency of updated NIP-5D</name>
  <files>specs/NIP-5D.md</files>
  <read_first>specs/NIP-5D.md</read_first>
  <action>
Read the updated NIP-5D from top to bottom and verify internal consistency. Check for:

1. **No dangling verb references**: Every verb mentioned in prose appears in the Wire Format tables and vice versa
2. **No stale cross-references**: All NIP references in the References section are actually cited in the body text
3. **No orphaned terminology**: Every term in the Terminology table is used at least once in the spec body
4. **MUST/SHOULD/MAY consistency**: All normative keywords are used correctly per RFC 2119
5. **Section flow**: Terminology -> Transport -> Wire Format -> Identity -> Extension Discovery -> NUB Extension Framework -> Security Considerations -> Event Kinds -> References
6. **No empty sections**: If Event Kinds table is gone, ensure the section still has meaningful content or is removed

Fix any inconsistencies found. If the terminology table still references "Composite key" but it is no longer used, remove it. If any verb is mentioned in prose but not in a table, add or remove as appropriate.
  </action>
  <verify>
    <automated>grep -cE "AUTH|REGISTER|IDENTITY|handshake|delegated key|22242|NIP-42|Schnorr" specs/NIP-5D.md | xargs test 0 -eq && echo "CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - Every NIP cited in References (NIP-01, NIP-07, NIP-45, NIP-5A) appears at least once in the body
    - Every term in the Terminology table is used in the body
    - No verb appears in prose that is not in the Wire Format tables
    - grep -ciE "AUTH|REGISTER|IDENTITY|handshake|delegated.key|22242|NIP-42|Schnorr" specs/NIP-5D.md returns 0
  </acceptance_criteria>
  <done>NIP-5D is internally consistent — all cross-references valid, all terminology used, all verbs documented, no stale remnants from old protocol</done>
</task>

</tasks>

<verification>
1. `grep -ciE "AUTH|REGISTER|IDENTITY|handshake|delegated.key|22242|NIP-42|Schnorr" specs/NIP-5D.md` returns 0
2. `grep -c "MessageEvent.source" specs/NIP-5D.md` returns >= 3
3. `grep -c "## Identity" specs/NIP-5D.md` returns 1
4. `grep -c "## Authentication" specs/NIP-5D.md` returns 0
5. `grep -c "unsigned event templates" specs/NIP-5D.md` returns >= 1
6. Every NIP in the References section is cited in the body
</verification>

<success_criteria>
NIP-5D accurately describes the v3.0.0 simplified wire protocol: napplets send plain NIP-01 messages, shell identifies senders via MessageEvent.source at iframe creation, no AUTH handshake, no napplet-side signing. The spec is internally consistent with no stale references.
</success_criteria>

<output>
After completion, create `.planning/phases/72-nip-5d-update/72-01-SUMMARY.md`
</output>
