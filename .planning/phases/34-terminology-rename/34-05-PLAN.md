---
plan: 05
phase: 34
title: Test Updates and Documentation
wave: 4
depends_on: [01, 02, 03, 04]
requirements_addressed: [TERM-01, TERM-04, TERM-05]
files_modified:
  - packages/core/src/index.test.ts
  - packages/runtime/src/dispatch.test.ts
  - README.md
  - packages/core/README.md
  - packages/services/README.md
  - packages/shell/README.md
  - packages/shim/README.md
  - packages/vite-plugin/README.md
  - skills/build-napplet/SKILL.md
  - SPEC.md
autonomous: true
---

# Plan 34-05: Test Updates and Documentation

## Objective

Update the test files that assert topic string values (now changed to `napplet:*`), update all README files, and update SPEC.md where it says "napp" meaning "napplet". This is the final wave — after this plan, `pnpm type-check` passes, the grep gate returns zero, and all documentation is correct.

---

## Task 34-05-01: Update test files

<read_first>
- `packages/core/src/index.test.ts` — line ~100: `expect(TOPICS.STATE_RESPONSE).toBe('napp:state-response')`
- `packages/runtime/src/dispatch.test.ts` — uses `runtime.nappKeyRegistry`, has `'test-napp'` type strings in test data
- `packages/runtime/src/discovery.test.ts` — uses `['type', 'test-napp']` in test fixtures
</read_first>

<action>
In `packages/core/src/index.test.ts`:

1. Update the topic assertion:
   Current: `expect(TOPICS.STATE_RESPONSE).toBe('napp:state-response');`
   Replace with: `expect(TOPICS.STATE_RESPONSE).toBe('napplet:state-response');`

2. Check for any `AUDIO_MUTED` assertion similarly:
   If there is: `expect(TOPICS.AUDIO_MUTED).toBe('napp:audio-muted');`
   Replace with: `expect(TOPICS.AUDIO_MUTED).toBe('napplet:audio-muted');`

In `packages/runtime/src/dispatch.test.ts`:

1. Update the comment reference to `nappKeyRegistry`:
   Current: `// After auth, the nappKeyRegistry should have the entry`
   Replace with: `// After auth, the sessionRegistry should have the entry`

2. Update the access to `runtime.nappKeyRegistry`:
   Current (line ~106): `const pubkey = runtime.nappKeyRegistry.getPubkey(WINDOW_ID);`
   Keep as-is — `nappKeyRegistry` is still exported as a property name on `Runtime` (we kept the property name for backward compat in Plan 34-03). No change needed to the test code here.

3. The test-data strings `'test-napp'` and `'test-napp-2'` in `TEST_DTAG` and `['type', 'test-napp']`:
   These are test fixture values (napplet type identifiers used in test events), NOT TypeScript identifiers. Per the research, "napp" in test data strings that represent Nostr event tag values can stay — they don't violate the `napp[^l]` identifier rule. However, if we want the grep gate to pass for ALL production TypeScript (excluding test data), we should distinguish.
   
   The success criterion is: `grep -r 'napp[^l]' packages/ | grep -v test | wc -l` → 0. Test files with `'test-napp'` string literals are acceptable per CONTEXT.md D-13 ("excluding test snapshots referencing old names").
   
   DECISION: Leave `'test-napp'` and `'test-napp-2'` in test fixtures as-is — they are example napplet type strings, not identifiers. The grep gate excludes test files.
   
   But update the comment on line 21:
   Current: `const TEST_DTAG = parseInt(TEST_PUBKEY.slice(0, 8), 16).toString(36) + 'test-napp';`
   Leave the VALUE as-is (it's a computed string used as a Nostr d-tag). Add or update the comment if it says "napp" to say "napplet type".

In `packages/runtime/src/discovery.test.ts`:
- The `['type', 'test-napp']` is a test fixture string value — leave as-is per above decision.
</action>

<acceptance_criteria>
- `packages/core/src/index.test.ts` contains `toBe('napplet:state-response')`
- `packages/core/src/index.test.ts` does NOT contain `toBe('napp:state-response')`
- `pnpm type-check` exits 0
</acceptance_criteria>

---

## Task 34-05-02: Update packages/shell/README.md

<read_first>
- `packages/shell/README.md` — contains `nappKeyRegistry`, `cleanupNappState`, `napp identity`, `napp key entries`, etc.
</read_first>

<action>
In `packages/shell/README.md`:

1. Update all references to `nappKeyRegistry` → add note about `sessionRegistry` being canonical with `nappKeyRegistry` deprecated:
   - Table entry: `| \`nappKeyRegistry\` | Manage ephemeral napp key entries after AUTH |`
     Replace with: `| \`sessionRegistry\` | Manage napplet session entries after AUTH (\`nappKeyRegistry\` is deprecated alias) |`

2. Update code example that imports `nappKeyRegistry`:
   Current: `import { originRegistry, manifestCache, aclStore, audioManager, nappKeyRegistry } from '@napplet/shell';`
   Replace with: `import { originRegistry, manifestCache, aclStore, audioManager, sessionRegistry } from '@napplet/shell';`
   
   Current: `nappKeyRegistry,`
   Replace with: `sessionRegistry,`

3. Update `cleanupNappState` reference:
   Current: `| \`cleanupNappState\` | Remove all state for a specific napp identity |`
   Replace with: `| \`cleanupNappState\` | Remove all state for a specific napplet identity |`
   
   (Function name unchanged in Phase 34 — only JSDoc/text description changes.)

4. Update all inline text that says "napp identity" → "napplet identity", "napp key entries" → "napplet session entries", "napp messages" → "napplet messages" where referring to the iframe app.
</action>

<acceptance_criteria>
- `packages/shell/README.md` contains `sessionRegistry` in usage examples
- `packages/shell/README.md` does NOT contain `nappKeyRegistry` without a "deprecated" note alongside it
</acceptance_criteria>

---

## Task 34-05-03: Update packages/shim/README.md

<read_first>
- `packages/shim/README.md` — extensive `nappState` usage; section header `### nappState`; method docs like `nappState.getItem`, etc.
</read_first>

<action>
In `packages/shim/README.md`:

1. Update the quick-start import example:
   Current: `import { subscribe, publish, on, nappState, discoverServices, hasService } from '@napplet/shim';`
   Replace with: `import { subscribe, publish, on, nappletState, discoverServices, hasService } from '@napplet/shim';`

2. Update the usage steps:
   Current step 3: `Once authenticated, use \`subscribe\`, \`publish\`, \`query\`, \`emit\`, \`on\`, and \`nappState\` to interact...`
   Replace: `nappState` → `nappletState`

3. Update code examples:
   Current: `await nappState.setItem('theme', 'dark');`
   Replace: `nappState` → `nappletState` throughout all code blocks.

4. Update the section header:
   Current: `### nappState`
   Replace with: `### nappletState`
   Add note: `> **Note:** \`nappState\` and \`nappStorage\` are deprecated aliases for \`nappletState\`. They will be removed in v0.9.0.`

5. Update method headings:
   Current: `#### nappState.getItem(...)`, `#### nappState.setItem(...)`, etc.
   Replace: `nappState.` → `nappletState.`

6. In the API table or reference section, update `nappState` → `nappletState` as the primary name, mention `nappState`/`nappStorage` as deprecated aliases.
</action>

<acceptance_criteria>
- `packages/shim/README.md` contains `nappletState` as the primary API name
- `packages/shim/README.md` mentions `nappState` only in a deprecation notice context
- `packages/shim/README.md` section heading `### nappletState` exists
</acceptance_criteria>

---

## Task 34-05-04: Update packages/core/README.md

<read_first>
- `packages/core/README.md` — contains `TOPICS.STATE_RESPONSE // 'napp:state-response'` and `TOPICS.AUDIO_MUTED // 'napp:audio-muted'` in example code blocks
</read_first>

<action>
In `packages/core/README.md`:

1. Update the topic constants examples:
   Current: `TOPICS.STATE_RESPONSE           // 'napp:state-response'`
   Replace with: `TOPICS.STATE_RESPONSE           // 'napplet:state-response'`
   
   Current: `TOPICS.AUDIO_MUTED              // 'napp:audio-muted'`
   Replace with: `TOPICS.AUDIO_MUTED              // 'napplet:audio-muted'`

2. Update any prose that says "napp state" or "napp audio" → "napplet state" / "napplet audio".
</action>

<acceptance_criteria>
- `packages/core/README.md` does NOT contain `'napp:state-response'`
- `packages/core/README.md` does NOT contain `'napp:audio-muted'`
- `packages/core/README.md` contains `'napplet:state-response'` and `'napplet:audio-muted'`
</acceptance_criteria>

---

## Task 34-05-05: Update packages/services/README.md

<read_first>
- `packages/services/README.md` — contains `nappClass` in AudioSource table, `napp:audio-muted` topic in message table
</read_first>

<action>
In `packages/services/README.md`:

1. Update `nappClass` in interface/table references:
   Current: `| \`nappClass\` | ... |` (in AudioSource description)
   Replace: `nappClass` → `nappletClass`
   
   Current: `nappClass: 'music-player'` (in example content payloads)
   Replace: `nappClass` → `nappletClass`

2. Update the audio topic in message table:
   Current: `| \`napp:audio-muted\` | shell → napplet | Mute state notification sent back to napplet (content: \`{ muted }\`) |`
   Replace: `napp:audio-muted` → `napplet:audio-muted`

3. Update the `AudioSource` type description in the type table:
   Current: `| \`AudioSource\` | Active audio source — \`windowId\`, \`nappClass\`, \`title\`, \`muted\` |`
   Replace: `nappClass` → `nappletClass`
</action>

<acceptance_criteria>
- `packages/services/README.md` does NOT contain `napp:audio-muted`
- `packages/services/README.md` does NOT contain `nappClass` (without "napplet")
- `packages/services/README.md` contains `napplet:audio-muted` and `nappletClass`
</acceptance_criteria>

---

## Task 34-05-06: Update packages/vite-plugin/README.md

<read_first>
- `packages/vite-plugin/README.md` — contains `napplet-napp-type` meta attribute references, `nappType` option docs, example HTML with `<meta name="napplet-napp-type" content="my-napp">`
</read_first>

<action>
In `packages/vite-plugin/README.md`:

1. Update the meta attribute references:
   Current: `<meta name="napplet-napp-type" content="my-napp">`
   Replace with: `<meta name="napplet-type" content="my-napp">`
   
   Add a note below: `> **Backward compatibility:** The plugin also injects \`<meta name="napplet-napp-type">\` for one release cycle. This will be removed in v0.9.0.`

2. Update the `nappType` option description:
   Current: `#### nappType (required)`
   Keep the heading as `nappType` (the option name is unchanged).
   
   Current: `The napp type identifier (e.g., \`'feed'\`, \`'chat'\`, \`'profile'\`).`
   Replace with: `The napplet type identifier (e.g., \`'feed'\`, \`'chat'\`, \`'profile'\`).`
   
   Current: `Injected as the \`content\` of the \`<meta name="napplet-napp-type">\` tag`
   Replace with: `Injected as the \`content\` of the \`<meta name="napplet-type">\` tag`

3. Update the output example HTML:
   Current line ~125: `<meta name="napplet-napp-type" content="my-music-app">`
   Replace with: `<meta name="napplet-type" content="my-music-app">`

4. Update the `Nip5aManifestOptions` type reference table:
   Current: `nappType: string;` — keep as-is (option name unchanged)

5. The `nappType: 'my-napp'` in code examples — keep as-is (it's the option value, not the meta attribute name).
</action>

<acceptance_criteria>
- `packages/vite-plugin/README.md` primary HTML example shows `<meta name="napplet-type">`
- `packages/vite-plugin/README.md` has a backward compat note about `napplet-napp-type`
- `packages/vite-plugin/README.md` option description says "napplet type identifier"
</acceptance_criteria>

---

## Task 34-05-07: Update root README.md and skills/build-napplet/SKILL.md

<read_first>
- `README.md` (root) — has `nappState` in API table and architecture diagram
- `skills/build-napplet/SKILL.md` — mentions `nappStorage` and `nappType`
</read_first>

<action>
In `README.md` (root):

1. Update the package table entry for @napplet/shim:
   Current: `...provides \`subscribe\`, \`publish\`, \`query\`, \`emit\`, \`on\`, \`nappState\`, and service discovery...`
   Replace: `nappState` → `nappletState`

2. Update the architecture ASCII diagram:
   Current line: `│     ├── NIP-01 message routing           nappState (proxied storage)`
   Replace: `nappState` → `nappletState`

In `skills/build-napplet/SKILL.md`:

1. Update the `nappStorage` reference in Step 6:
   Current: `## Step 6 — Use nappStorage`
   Replace with: `## Step 6 — Use nappletState`
   
   Current: `Import \`nappStorage\` from \`@napplet/shim\`.`
   Replace with: `Import \`nappletState\` from \`@napplet/shim\`. (\`nappStorage\` and \`nappState\` are deprecated aliases — use \`nappletState\` for new code.)`
   
   Current: `Storage is scoped by \`nappType:aggregateHash\``
   Replace: `nappType` → `nappletType` (referring to the type identifier concept, not the option name)

2. Review other mentions of `nappType` option in the SKILL.md — if referring to the vite-plugin option, keep as `nappType` (the option name is unchanged). If referring to the concept of "what type of napplet", say "nappletType".
</action>

<acceptance_criteria>
- `README.md` contains `nappletState` in the shim package description
- `skills/build-napplet/SKILL.md` section heading `## Step 6 — Use nappletState` exists
- `skills/build-napplet/SKILL.md` does NOT mention `nappStorage` without a deprecation note
</acceptance_criteria>

---

## Task 34-05-08: Update SPEC.md (TERM-04)

<read_first>
- `SPEC.md` (root) — Per CONTEXT.md D-07, Phase 34 DOES NOT touch SPEC.md; Phase 35 will do a combined pass.
- CONTEXT.md D-12 confirms SPEC.md is excluded from Phase 34 scope.
- REQUIREMENTS.md TERM-04 is "SPEC.md corrected — all uses of 'napp' where 'napplet' is intended replaced throughout."
</read_first>

<action>
SKIP. SPEC.md is explicitly excluded from Phase 34 scope per CONTEXT.md D-07/D-12. TERM-04 will be addressed in Phase 35 as part of the combined SPEC.md pass (napp→napplet + INTER_PANE→IPC-PEER).

Add a note at the top of this task indicating the skip reason so the executor does not accidentally edit SPEC.md.

No action required for SPEC.md in this plan.
</action>

<acceptance_criteria>
- No changes made to `SPEC.md`
- The `requirements_addressed` field for this plan includes `TERM-04` but notes it is deferred to Phase 35 per CONTEXT.md D-07
</acceptance_criteria>

---

## Final Phase Verification

After all plans in Phase 34 are complete:

```bash
cd /home/sandwich/Develop/napplet

# Gate 1: No napp[^l] identifiers in production TypeScript
grep -r 'napp[^l]' packages/ --include="*.ts" --exclude-dir=node_modules --exclude-dir=dist | grep -v '\.test\.' | wc -l
# Expected: 0

# Gate 2: Type-check passes
pnpm type-check 2>&1 | tail -5
# Expected: 0 errors

# Gate 3: Build passes
pnpm build 2>&1 | tail -5
# Expected: success

# Gate 4: Canonical exports present
grep 'nappletState' packages/shim/src/index.ts | wc -l
# Expected: >= 1

grep 'sessionRegistry' packages/shell/src/session-registry.ts | wc -l
# Expected: >= 1

grep 'napplet-state:' packages/runtime/src/state-handler.ts | wc -l
# Expected: >= 1

grep 'napplet:state-response' packages/core/src/topics.ts | wc -l
# Expected: 1
```

---

## Requirements Coverage Summary

| Requirement | Covered By | Status |
|-------------|-----------|--------|
| TERM-01 | Plans 01-04 (type renames, file renames, consumer updates) | ✓ |
| TERM-02 | Plan 03 (state-handler.ts dual-read migration) | ✓ |
| TERM-03 | Plans 04 (shim meta attr) + 04 (vite-plugin injection) | ✓ |
| TERM-04 | Deferred to Phase 35 per CONTEXT.md D-07 | Deferred |
| TERM-05 | Plan 05 (README + skills updates) | ✓ |
