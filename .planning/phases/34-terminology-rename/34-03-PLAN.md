---
plan: 03
phase: 34
title: Runtime and Shell Consumer Updates
wave: 3
depends_on: [01, 02]
requirements_addressed: [TERM-01, TERM-02]
files_modified:
  - packages/runtime/src/runtime.ts
  - packages/runtime/src/state-handler.ts
  - packages/runtime/src/event-buffer.ts
  - packages/runtime/src/enforce.ts
  - packages/runtime/src/index.ts
  - packages/runtime/src/manifest-cache.ts
  - packages/shell/src/state-proxy.ts
  - packages/shell/src/shell-bridge.ts
  - packages/shell/src/hooks-adapter.ts
  - packages/shell/src/index.ts
  - packages/shell/src/audio-manager.ts
  - packages/shell/src/acl-store.ts
  - packages/shell/src/manifest-cache.ts
  - packages/shell/src/origin-registry.ts
autonomous: true
---

# Plan 34-03: Runtime and Shell Consumer Updates

## Objective

Update all consumers in the `runtime` and `shell` packages to use the new type names, file paths, and topic strings. After this plan, `pnpm type-check` should pass for these two packages.

---

## Task 34-03-01: Update runtime/src/runtime.ts

<read_first>
- `packages/runtime/src/runtime.ts` — primary file; imports `createNappKeyRegistry` + `NappKeyRegistry` from `./napp-key-registry.js`; uses local vars `nappKeyRegistry`, `nappPubkey`, `nappEntries`, `nappInfoMap`, `nappType`, `nappEntry`
- `packages/runtime/src/session-registry.ts` — new file created in 34-02; exports `createSessionRegistry`, `SessionRegistry`
</read_first>

<action>
In `packages/runtime/src/runtime.ts`:

1. Update the import of napp-key-registry:
   Current: `import { createNappKeyRegistry } from './napp-key-registry.js';`
   Replace with: `import { createSessionRegistry } from './session-registry.js';`
   
   Current: `import type { NappKeyRegistry } from './napp-key-registry.js';`
   Replace with: `import type { SessionRegistry } from './session-registry.js';`

2. Update the type annotation on `nappKeyRegistry` property of `Runtime` interface (line ~111):
   Current:
   ```ts
   /** Access the identity registry (for shell adapter to read napp state). */
   readonly nappKeyRegistry: NappKeyRegistry;
   ```
   Replace with:
   ```ts
   /** Access the identity registry (for shell adapter to read napplet session state). */
   readonly nappKeyRegistry: SessionRegistry;
   ```
   NOTE: Keep the property name as `nappKeyRegistry` — it is a public API surface that consumers use; renaming it would be a breaking change. The type changes to `SessionRegistry`.

3. Inside `createRuntime()` function body (line ~170):
   Current: `const nappKeyRegistry = createNappKeyRegistry(hooks.onPendingUpdate);`
   Replace with: `const nappKeyRegistry = createSessionRegistry(hooks.onPendingUpdate);`

4. Update local variable names inside `createRuntime()` that are scoped only to the function:

   a. Search for `nappPubkey` (line ~388):
      Current: `const nappPubkey = nappKeyRegistry.getPubkey(windowId);`
      Replace with: `const nappletPubkey = nappKeyRegistry.getPubkey(windowId);`
      Update all usages of `nappPubkey` → `nappletPubkey` within the same block scope.
   
   b. Search for `nappEntry` (local var, line ~390):
      Current: `const nappEntry = nappKeyRegistry.getEntry(nappPubkey);`
      Replace with: `const nappletEntry = nappKeyRegistry.getEntry(nappletPubkey);`
      Update all usages of `nappEntry` → `nappletEntry` within the same block scope.
   
   c. Search for `nappEntries` (line ~735):
      Current: `const nappEntries = nappKeyRegistry.getAllEntries();`
      Replace with: `const nappletEntries = nappKeyRegistry.getAllEntries();`
      Update all usages of `nappEntries` in the same block scope.
   
   d. Search for `nappInfoMap` (line ~736):
      Current: `const nappInfoMap: Record<string, { type: string; registeredAt: number }> = {};`
      Replace with: `const nappletInfoMap: Record<string, { type: string; registeredAt: number }> = {};`
      Update all usages of `nappInfoMap` → `nappletInfoMap` in the same block scope.
   
   e. Search for `const nappType = typeTag[1];` (line ~246):
      Replace with: `const nappletType = typeTag[1];`
      Update all usages of `nappType` → `nappletType` within the same block scope (lines 247, 263, 266, 275, etc.).
      
      IMPORTANT: Only rename LOCAL VARIABLE usages of `nappType` that refer to the parsed AUTH event type field. Do NOT rename `options.nappType` or any external parameter named `nappType`.

5. Update the JSDoc and comment lines in runtime.ts that say "napp" where napplet is meant:
   - `* @returns A Runtime instance ready to handle napp messages` → `* @returns A Runtime instance ready to handle napplet messages`
   - `/** Access the identity registry (for shell adapter to read napp state). */` → already updated above
   - Any comment that says "napp's pubkey" or "napp reconnects" → update to "napplet"

6. Find where `nappKeyRegistry` is returned in the object at the end:
   Current: `get nappKeyRegistry() { return nappKeyRegistry; },`
   Keep the property name `nappKeyRegistry` but this is fine since the local var name is still `nappKeyRegistry`.
</action>

<acceptance_criteria>
- `packages/runtime/src/runtime.ts` does NOT contain `createNappKeyRegistry` (uses `createSessionRegistry` instead)
- `packages/runtime/src/runtime.ts` does NOT contain `from './napp-key-registry.js'`
- `packages/runtime/src/runtime.ts` contains `from './session-registry.js'`
- `packages/runtime/src/runtime.ts` contains `createSessionRegistry`
- `packages/runtime/src/runtime.ts` does NOT contain `const nappPubkey` or `const nappEntries` or `const nappInfoMap` or `const nappType = typeTag`
- `pnpm --filter @napplet/runtime type-check` exits 0 (after this task)
</acceptance_criteria>

---

## Task 34-03-02: Update runtime/src/state-handler.ts (TERM-02: localStorage prefix migration)

<read_first>
- `packages/runtime/src/state-handler.ts` — imports `NappKeyRegistry` from `./napp-key-registry.js`; has `napp-state:` prefix strings; has hardcoded `'napp:state-response'` tag (line 41)
- `packages/runtime/src/session-registry.ts` — new file; exports `SessionRegistry`
- `packages/core/src/topics.ts` — `STATE_RESPONSE` now equals `'napplet:state-response'`
</read_first>

<action>
In `packages/runtime/src/state-handler.ts`:

1. Update the import:
   Current: `import type { NappKeyRegistry } from './napp-key-registry.js';`
   Replace with: `import type { SessionRegistry } from './session-registry.js';`

2. Add import for TOPICS if not already imported (check file head):
   Add or ensure: `import { TOPICS } from '@napplet/core';`

3. Update the hardcoded topic string tag (line ~41):
   Current:
   ```ts
   tags: [['t', 'napp:state-response'], ['id', correlationId], ...tags],
   ```
   Replace with:
   ```ts
   tags: [['t', TOPICS.STATE_RESPONSE], ['id', correlationId], ...tags],
   ```

4. Update the function parameter type in `handleStateRequest`:
   Current: `nappKeyRegistry: NappKeyRegistry,`
   Replace with: `sessionRegistry: SessionRegistry,`
   
   Update all usages of `nappKeyRegistry` within `handleStateRequest` body → `sessionRegistry`.
   
   NOTE: `handleStateRequest` is called from `runtime.ts` with `nappKeyRegistry` variable — the call site in runtime.ts must pass `nappKeyRegistry` (the local var). The parameter name changes to `sessionRegistry` in state-handler.ts only.

5. Update localStorage prefix — `makeKey` helper function:
   Current: `return \`napp-state:${pubkey}:${dTag}:${aggregateHash}:${userKey}\`;`
   Replace with: `return \`napplet-state:${pubkey}:${dTag}:${aggregateHash}:${userKey}\`;`

6. Update prefix in `handleStateRequest` GET operation — add dual-read fallback:
   Find the location where state is read for GET operation. Currently it likely does:
   ```ts
   const prefix = `napp-state:${pubkey}:${dTag}:${aggregateHash}:`;
   ```
   Replace with:
   ```ts
   const prefix = `napplet-state:${pubkey}:${dTag}:${aggregateHash}:`;
   ```
   
   For the specific GET value lookup (storage-get case), add dual-read fallback:
   Find the place where a single key is read (storage-get handler):
   ```ts
   // Dual-read: try new prefix first, fall back to old for migration
   const newStorageKey = makeKey(pubkey, dTag, aggregateHash, userKey);
   const oldStorageKey = `napp-state:${pubkey}:${dTag}:${aggregateHash}:${userKey}`;
   const value = statePersistence.getItem(newStorageKey) ?? statePersistence.getItem(oldStorageKey) ?? null;
   ```
   
   This replaces any single `statePersistence.getItem(makeKey(...))` call in the GET path.

7. Update all remaining `napp-state:` prefix strings:
   - Line ~87: `const prefix = \`napp-state:...\`` → `const prefix = \`napplet-state:...\``
   - Line ~118 (in quote-lookup section): same pattern → `napplet-state:`
   - Line ~132: comment `// Strip the prefix to return user-facing key names (e.g., 'k1' not 'napp-state:pk:dTag:hash:k1')` → update example to use `napplet-state:`
   - Line ~158: `const prefix = \`napp-state:...\`` in `cleanupNappState` → `napplet-state:`

8. Update JSDoc lines:
   - `* @param nappKeyRegistry - Identity registry for looking up napp identity` → `* @param sessionRegistry - Identity registry for looking up napplet session identity`
   - `* Remove all state entries for a napp identity.` → `* Remove all state entries for a napplet identity.`
   - `* Used during napp cleanup when a window is closed.` → `* Used during napplet cleanup when a window is closed.`
   - `* @param pubkey - The napp's pubkey` → `* @param pubkey - The napplet's pubkey` (all occurrences)
</action>

<acceptance_criteria>
- `packages/runtime/src/state-handler.ts` does NOT contain `'napp-state:'` (all replaced with `'napplet-state:'`)
- `packages/runtime/src/state-handler.ts` does NOT contain `from './napp-key-registry.js'`
- `packages/runtime/src/state-handler.ts` does NOT contain `'napp:state-response'` (now uses `TOPICS.STATE_RESPONSE`)
- `packages/runtime/src/state-handler.ts` contains `napplet-state:` prefix strings
- `packages/runtime/src/state-handler.ts` contains dual-read fallback: `statePersistence.getItem(oldStorageKey)` with old prefix `napp-state:`
- `packages/runtime/src/state-handler.ts` contains `SessionRegistry`
</acceptance_criteria>

---

## Task 34-03-03: Update runtime/src/event-buffer.ts

<read_first>
- `packages/runtime/src/event-buffer.ts` — imports `NappKeyRegistry` from `./napp-key-registry.js`; uses it as a parameter type in `deliverToSubscribers`
</read_first>

<action>
In `packages/runtime/src/event-buffer.ts`:

1. Update the import:
   Current: `import type { NappKeyRegistry } from './napp-key-registry.js';`
   Replace with: `import type { SessionRegistry } from './session-registry.js';`

2. Update the function parameter type (line ~88):
   Current:
   ```ts
   nappKeyRegistry: NappKeyRegistry,
   ```
   Replace with:
   ```ts
   sessionRegistry: SessionRegistry,
   ```

3. Update usages of the parameter in the function body:
   Current: `const recipientPubkey = nappKeyRegistry.getPubkey(sub.windowId);`
   Replace with: `const recipientPubkey = sessionRegistry.getPubkey(sub.windowId);`

4. Update JSDoc:
   Current: `* @param nappKeyRegistry - Identity registry for looking up napp pubkeys`
   Replace with: `* @param sessionRegistry - Identity registry for looking up napplet pubkeys`

5. In `runtime.ts`, update the call to `deliverToSubscribers` to pass `nappKeyRegistry` (the local var in runtime.ts) as the `sessionRegistry` parameter — this is just argument position, no name change needed at the call site since it's positional.
</action>

<acceptance_criteria>
- `packages/runtime/src/event-buffer.ts` does NOT contain `from './napp-key-registry.js'`
- `packages/runtime/src/event-buffer.ts` contains `SessionRegistry`
- `packages/runtime/src/event-buffer.ts` does NOT contain `nappKeyRegistry` as a local parameter name
</acceptance_criteria>

---

## Task 34-03-04: Update runtime/src/enforce.ts and runtime/src/manifest-cache.ts

<read_first>
- `packages/runtime/src/enforce.ts` — comment-only reference to `nappKeyRegistry` at line ~120; no code change needed, only JSDoc
- `packages/runtime/src/manifest-cache.ts` — JSDoc says "napp manifest entries", "napp updates"
</read_first>

<action>
In `packages/runtime/src/enforce.ts`:
1. Find the comment (line ~120):
   Current: `* Provided by nappKeyRegistry at runtime.`
   Replace with: `* Provided by sessionRegistry at runtime.`
   
2. Other JSDoc that says "napp" → update to "napplet" where it means the iframe app.

In `packages/runtime/src/manifest-cache.ts`:
1. Update JSDoc:
   Current: `* Caches NIP-5A manifest data (aggregate hashes) per napp identity.`
   Replace with: `* Caches NIP-5A manifest data (aggregate hashes) per napplet identity.`
   
   Current: `* Cache for verified napp manifest entries.`
   Replace with: `* Cache for verified napplet manifest entries.`
   
   Current: `* Used to detect napp updates (aggregateHash changes) across sessions.`
   Replace with: `* Used to detect napplet updates (aggregateHash changes) across sessions.`
</action>

<acceptance_criteria>
- `packages/runtime/src/enforce.ts` does NOT contain `* Provided by nappKeyRegistry at runtime.`
- `packages/runtime/src/manifest-cache.ts` does NOT contain `* Cache for verified napp manifest entries.`
</acceptance_criteria>

---

## Task 34-03-05: Update runtime/src/index.ts exports

<read_first>
- `packages/runtime/src/index.ts` — current exports of `createNappKeyRegistry`, `NappKeyRegistry`, `NappKeyEntry` from napp-key-registry.js and types.js
</read_first>

<action>
In `packages/runtime/src/index.ts`:

1. Update the `NappKeyRegistry` export section:
   Current:
   ```ts
   // ─── NappKeyRegistry ──────────────────────────────────────────────────────
   export { createNappKeyRegistry } from './napp-key-registry.js';
   export type { NappKeyRegistry } from './napp-key-registry.js';
   ```
   Replace with:
   ```ts
   // ─── SessionRegistry ──────────────────────────────────────────────────────
   export { createSessionRegistry, createNappKeyRegistry } from './session-registry.js';
   export type { SessionRegistry, NappKeyRegistry } from './session-registry.js';
   ```

2. Update the `NappKeyEntry` export in the types block:
   Current: `NappKeyEntry,`
   Replace with: `SessionEntry,`
   And add `NappKeyEntry,` on the next line (the deprecated alias is already in types.ts, so it will be re-exported automatically via `SessionEntry`):
   
   Actually: Since `NappKeyEntry` is now a `type` alias in types.ts (`export type NappKeyEntry = SessionEntry`), it will be re-exported automatically if you include it in the export list. So add both:
   ```ts
   SessionEntry,
   NappKeyEntry,  // @deprecated — use SessionEntry
   ```

3. The cleanupNappState export:
   Current: `export { handleStateRequest, cleanupNappState } from './state-handler.js';`
   Keep as-is (function name not changing in this phase).
</action>

<acceptance_criteria>
- `packages/runtime/src/index.ts` contains `export { createSessionRegistry, createNappKeyRegistry } from './session-registry.js';`
- `packages/runtime/src/index.ts` does NOT contain `from './napp-key-registry.js'`
- `packages/runtime/src/index.ts` exports `SessionEntry`
</acceptance_criteria>

---

## Task 34-03-06: Update shell/src/state-proxy.ts (dead code, prefix update only)

<read_first>
- `packages/shell/src/state-proxy.ts` — dead code since v0.3.0; still has `napp-state:` prefix strings (5 occurrences); imports `nappKeyRegistry` from `./napp-key-registry.js`
</read_first>

<action>
In `packages/shell/src/state-proxy.ts`:

1. Update the import:
   Current: `import { nappKeyRegistry } from './napp-key-registry.js';`
   Replace with: `import { sessionRegistry } from './session-registry.js';`

2. Update all usages of `nappKeyRegistry` → `sessionRegistry` in the file body.

3. Update all `napp-state:` prefix string literals → `napplet-state:`:
   - Line ~15: `` return `napp-state:${...}` `` → `` return `napplet-state:${...}` ``
   - Line ~21: `` const prefix = `napp-state:${...}` `` → `napplet-state:`
   - Line ~23: `` `napp-state:${pubkey}:${dTag}:${aggregateHash}:${excludeUserKey}` `` → `napplet-state:`
   - Line ~118: `` const prefix = `napp-state:${...}` `` → `napplet-state:`
   - Line ~142: `` const prefix = `napp-state:${...}` `` → `napplet-state:`

4. Update JSDoc:
   - `* State proxy — shell-side handler for napp state requests.` → `* State proxy — shell-side handler for napplet state requests.`
   - `* This proxy stores napp state in the shell's localStorage, scoped by` → `* This proxy stores napplet state in the shell's localStorage, scoped by`
   - `* napp identity` → `* napplet identity`
   - `* quota exceeded: napp state limit is` → `* quota exceeded: napplet state limit is`
   - `* Remove all state entries for a napp identity from localStorage.` → `* Remove all state entries for a napplet identity from localStorage.`
   - All `@param pubkey - The napp's pubkey` → `The napplet's pubkey`
</action>

<acceptance_criteria>
- `packages/shell/src/state-proxy.ts` does NOT contain `'napp-state:'` prefix string
- `packages/shell/src/state-proxy.ts` does NOT contain `from './napp-key-registry.js'`
- `packages/shell/src/state-proxy.ts` contains `sessionRegistry` (imported from `./session-registry.js`)
</acceptance_criteria>

---

## Task 34-03-07: Update shell/src/shell-bridge.ts and shell/src/hooks-adapter.ts

<read_first>
- `packages/shell/src/shell-bridge.ts` — imports `nappKeyRegistry` from `./napp-key-registry.js`; uses in object spread
- `packages/shell/src/hooks-adapter.ts` — imports type `nappKeyRegistry as NappKeyRegistryType` from `./napp-key-registry.js`; has `nappKeyRegistry: typeof NappKeyRegistryType` in `BrowserDeps`
</read_first>

<action>
In `packages/shell/src/shell-bridge.ts`:
1. Update import:
   Current: `import { nappKeyRegistry } from './napp-key-registry.js';`
   Replace with: `import { sessionRegistry, nappKeyRegistry } from './session-registry.js';`
   
   NOTE: Import BOTH so the `nappKeyRegistry` spread at line ~143 still works (it's spread into the BrowserDeps object which uses `nappKeyRegistry` key name). Alternatively, rename the spread key — but since `BrowserDeps` interface uses `nappKeyRegistry` key, keep spreading as `nappKeyRegistry`:
   
   Actually the simplest fix: change the spread to use the deprecated alias:
   Current line ~143: `nappKeyRegistry,` (in object passed to adaptHooks)
   This still works since `nappKeyRegistry` is exported as deprecated alias from `./session-registry.js`.

2. Update JSDoc:
   - `* Provides direct access to the runtime's nappKeyRegistry, aclState,` → `* Provides direct access to the runtime's sessionRegistry, aclState,`
   - `* bridge.sendChallenge('napp-window-1');` → `* bridge.sendChallenge('napplet-window-1');` (example)
   - `* @returns A ShellBridge instance ready to handle napp messages` → `* @returns A ShellBridge instance ready to handle napplet messages`

In `packages/shell/src/hooks-adapter.ts`:
1. Update the import:
   Current: `import type { nappKeyRegistry as NappKeyRegistryType } from './napp-key-registry.js';`
   Replace with: `import type { sessionRegistry as SessionRegistryType } from './session-registry.js';`

2. Update the `BrowserDeps` interface:
   Current: `nappKeyRegistry: typeof NappKeyRegistryType;`
   Replace with: `nappKeyRegistry: typeof SessionRegistryType;`
   
   NOTE: Keep the property name `nappKeyRegistry` in `BrowserDeps` — it's used as a key name throughout the adapter and shell-bridge.

3. Update JSDoc example:
   Current: `originRegistry, manifestCache, aclStore, audioManager, nappKeyRegistry,`
   This line in the example can stay — it's showing parameter destructuring and `nappKeyRegistry` still works via the alias.
</action>

<acceptance_criteria>
- `packages/shell/src/shell-bridge.ts` does NOT contain `from './napp-key-registry.js'`
- `packages/shell/src/hooks-adapter.ts` does NOT contain `from './napp-key-registry.js'`
- `packages/shell/src/hooks-adapter.ts` contains `from './session-registry.js'`
</acceptance_criteria>

---

## Task 34-03-08: Update shell/src/index.ts exports

<read_first>
- `packages/shell/src/index.ts` — exports `NappKeyEntry`, imports from `./napp-key-registry.js` (if any direct export), re-exports types from `./types.js`
</read_first>

<action>
In `packages/shell/src/index.ts`:

1. Find any import/export that references `./napp-key-registry.js`:
   If any line exports `nappKeyRegistry` singleton directly from the old file path, update to import from `./session-registry.js`.
   
   Check: `grep 'napp-key-registry\|nappKeyRegistry' packages/shell/src/index.ts`
   
   Current exports include:
   ```ts
   export type {
     ...
     NappKeyEntry,
     ...
   } from './types.js';
   ```
   
   Since `NappKeyEntry` is now a deprecated alias in `./types.js`, it still exports fine. No change needed to the type re-export.
   
   If there is a value export of `nappKeyRegistry` singleton:
   Find any line like: `export { nappKeyRegistry } from './napp-key-registry.js';`
   Replace with: `export { sessionRegistry, nappKeyRegistry } from './session-registry.js';`
   And add `sessionRegistry` to the public exports list.

2. Add `SessionEntry` to the exported types from `./types.js` (alongside the deprecated `NappKeyEntry` alias):
   In the type export block, add `SessionEntry,` if not already present.

3. Check if shell/index.ts currently has an explicit `nappKeyRegistry` export — it likely exports it from napp-key-registry.ts. Update that export to session-registry.ts.
</action>

<acceptance_criteria>
- `packages/shell/src/index.ts` does NOT import from `./napp-key-registry.js`
- `packages/shell/src/index.ts` exports `sessionRegistry` (canonical) and `nappKeyRegistry` (deprecated alias)
</acceptance_criteria>

---

## Task 34-03-09: Update shell/src/audio-manager.ts

<read_first>
- `packages/shell/src/audio-manager.ts` — has `nappClass: string` field in `AudioSource`, `register()` method taking `nappClass` param, and hardcoded `'napp:audio-muted'` topic string (line ~104)
- `packages/core/src/topics.ts` — `AUDIO_MUTED` now equals `'napplet:audio-muted'`
</read_first>

<action>
In `packages/shell/src/audio-manager.ts`:

1. Add import for TOPICS if not present:
   Check if `TOPICS` is imported. If not:
   ```ts
   import { TOPICS } from '@napplet/core';
   ```

2. Rename `nappClass` → `nappletClass` in the `AudioSource` interface:
   Current:
   ```ts
   nappClass: string;
   ```
   Replace with:
   ```ts
   nappletClass: string;
   ```

3. Update the `register()` method signature and body:
   Current: `register(windowId: string, nappClass: string, title: string): void {`
   Replace with: `register(windowId: string, nappletClass: string, title: string): void {`
   
   Current: `sources.set(windowId, { windowId, nappClass, title, muted: false });`
   Replace with: `sources.set(windowId, { windowId, nappletClass, title, muted: false });`

4. Update the hardcoded topic string:
   Current: `tags: [['t', 'napp:audio-muted']],`
   Replace with: `tags: [['t', TOPICS.AUDIO_MUTED]],`

5. Update JSDoc:
   - `* windowId: 'win-1', nappClass: 'music-player',` (in example block) → `* windowId: 'win-1', nappletClass: 'music-player',`
   - `* @param nappClass - The napp class/type (e.g., 'music-player')` → `* @param nappletClass - The napplet class/type (e.g., 'music-player')`
</action>

<acceptance_criteria>
- `packages/shell/src/audio-manager.ts` contains `nappletClass: string;` (not `nappClass`)
- `packages/shell/src/audio-manager.ts` does NOT contain `'napp:audio-muted'`
- `packages/shell/src/audio-manager.ts` contains `TOPICS.AUDIO_MUTED`
</acceptance_criteria>

---

## Task 34-03-10: Update shell JSDoc-only files (acl-store.ts, manifest-cache.ts, origin-registry.ts)

<read_first>
- `packages/shell/src/acl-store.ts` — JSDoc comments only; no code identifiers to rename
- `packages/shell/src/manifest-cache.ts` — JSDoc comments only
- `packages/shell/src/origin-registry.ts` — comments reference "napp iframes", "napp window"
</read_first>

<action>
In `packages/shell/src/acl-store.ts`, update all JSDoc lines that say "napp" where "napplet" is meant:
- `* ACL Store — composite-keyed capability registry for napp identity.` → `* ACL Store — composite-keyed capability registry for napplet identity.`
- `* Default state quota in bytes (512 KB) per napp identity.` → `* per napplet identity.`
- `* ACL store — manages capability grants, revocations, and blocks for napp identities.` → `* for napplet identities.`
- All `* Check if a napp identity has a specific capability.` → `* Check if a napplet identity has a specific capability.`
- All `* @param pubkey - The napp's pubkey` → `* The napplet's pubkey`
- All `* @param dTag - The napp's dTag` → `* The napplet's dTag`
- All `* @param aggregateHash - The napp's build hash` → `* The napplet's build hash`
- All `* Grant/Revoke/Block/Unblock a capability to/from a napp identity.` → `napplet identity`
- `* that ties permissions to a specific napp build.` → `* that ties permissions to a specific napplet build.`

In `packages/shell/src/manifest-cache.ts`, update JSDoc:
- `* Manifest cache — persists verified NIP-5A aggregate hashes per napp identity.` → `* per napplet identity.`
- `* A cached manifest entry for a verified napp build.` → `* for a verified napplet build.`
- `* Cache for verified napp manifest entries.` → `* Cache for verified napplet manifest entries.`
- `* Used to detect napp updates (aggregateHash changes) across sessions.` → `* to detect napplet updates`
- All `* @param pubkey - The napp's pubkey` → `* The napplet's pubkey`

In `packages/shell/src/origin-registry.ts`, update comments:
- `* napp iframes` → `* napplet iframes`
- `* napp window` → `* napplet window`
- Example: `originRegistry.register(iframe.contentWindow, 'napp-1');` — keep as-is (it's a string value example, not an identifier)
</action>

<acceptance_criteria>
- `packages/shell/src/acl-store.ts` does NOT contain `* ACL Store — composite-keyed capability registry for napp identity.`
- `packages/shell/src/manifest-cache.ts` does NOT contain `* Cache for verified napp manifest entries.`
- `packages/shell/src/origin-registry.ts` does NOT contain `napp iframes`
</acceptance_criteria>

---

## Wave 3 Verification (after this plan)

```bash
cd /home/sandwich/Develop/napplet
pnpm type-check 2>&1 | tail -10
# Expected: Should pass or show only shim/services/vite-plugin errors (those are fixed in 34-04)
```
