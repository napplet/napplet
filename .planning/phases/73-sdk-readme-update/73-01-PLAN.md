---
phase: 73-sdk-readme-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shim/README.md
  - packages/sdk/README.md
  - packages/core/README.md
  - packages/vite-plugin/README.md
  - README.md
autonomous: true
requirements: [DOC-03]

must_haves:
  truths:
    - "@napplet/shim README describes the shim as a zero-crypto window installer with no mention of keypairs, signing, nostr-tools, or AUTH"
    - "@napplet/sdk README no longer references napplet-side signing, ephemeral keys, or nostr-tools"
    - "@napplet/core README reflects removed handshake types (no AUTH_KIND, no BusKind.REGISTRATION, PROTOCOL_VERSION is 3.0.0)"
    - "Root monorepo README describes identity as shell-assigned via message.source, not AUTH handshake"
    - "No README links to deleted RUNTIME-SPEC.md -- all protocol references updated"
  artifacts:
    - path: "packages/shim/README.md"
      provides: "Shim documentation with zero-crypto messaging"
      contains: "zero-crypto"
    - path: "packages/sdk/README.md"
      provides: "SDK documentation without signing references"
    - path: "packages/core/README.md"
      provides: "Core documentation reflecting v0.15.0 type surface"
      contains: "PROTOCOL_VERSION"
    - path: "README.md"
      provides: "Root README with shell-assigned identity model"
      contains: "message.source"
  key_links:
    - from: "packages/shim/README.md"
      to: "specs/NIP-5D.md"
      via: "Protocol Reference link"
      pattern: "NIP-5D"
    - from: "packages/core/README.md"
      to: "specs/NIP-5D.md"
      via: "Protocol Reference link"
      pattern: "NIP-5D"
---

<objective>
Update all package READMEs and the root README to reflect the v0.15.0 protocol simplification: napplets send unsigned messages, the shell assigns identity via message.source, there is no AUTH handshake, no nostr-tools dependency in the shim, and RUNTIME-SPEC.md has been moved to kehto.

Purpose: Developers reading these READMEs should see accurate documentation reflecting the current no-crypto wire protocol. Stale references to AUTH, keypairs, signing, nostr-tools, and deleted files must be eliminated.
Output: 5 updated README files with accurate v0.15.0 content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/shim/README.md
@packages/sdk/README.md
@packages/core/README.md
@packages/vite-plugin/README.md
@README.md
@packages/core/src/constants.ts
@packages/core/src/index.ts
@packages/shim/package.json
@packages/sdk/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update shim, sdk, and core README files</name>
  <files>packages/shim/README.md, packages/sdk/README.md, packages/core/README.md</files>
  <read_first>
    - packages/shim/README.md
    - packages/sdk/README.md
    - packages/core/README.md
    - packages/core/src/constants.ts
    - packages/core/src/index.ts
    - packages/shim/package.json
    - packages/sdk/package.json
  </read_first>
  <action>
Update packages/shim/README.md:
- Replace Prerequisites: remove "nostr-tools as a peer dependency" bullet entirely. The only prerequisite is a shell host.
- Replace "How It Works" step 2: was "The shim generates an ephemeral keypair and completes the NIP-42 AUTH handshake with the shell". Change to: "The shim registers with the shell via postMessage -- the shell assigns identity based on the iframe's message.source Window reference"
- Replace "How It Works" step 3: was "Once authenticated, window.napplet is populated...". Change to: "Once registered, window.napplet is populated..."
- Replace Installation command: change `npm install @napplet/shim nostr-tools` to `npm install @napplet/shim`
- In the Shim vs SDK comparison table: change shim's "What it does" from "Installs window.napplet global + AUTH handshake" to "Installs window.napplet global + shell registration". Change SDK vs Shim table similarly -- remove "nostr-tools (peer)" from Dependencies row, change to "None (types from @napplet/core)". Remove "starts AUTH" from side effects row -- change to "Yes -- installs globals, registers with shell"
- Replace Protocol Reference section: remove the RUNTIME-SPEC.md link (file no longer exists in this repo -- moved to kehto). Remove the NIP-42 link (no longer relevant). Keep NIP-01 link. Add NIP-5D link: `[NIP-5D](../../specs/NIP-5D.md) -- Napplet-shell protocol specification`
- Add a short sentence in the opening description or How It Works clarifying this is a zero-crypto package: "No cryptographic dependencies -- the shim sends plain NIP-01 messages and the shell handles identity."

Update packages/sdk/README.md:
- Replace Installation command: change `npm install @napplet/sdk @napplet/shim nostr-tools` to `npm install @napplet/sdk @napplet/shim`
- In the SDK vs Shim comparison table: change shim "What it does" from "Installs window.napplet + AUTH handshake" to "Installs window.napplet + shell registration". Change Dependencies row for shim from "nostr-tools (peer)" to "None (types from @napplet/core)". Change Side effects row from "Yes -- installs globals, starts AUTH" to "Yes -- installs globals, registers with shell"
- Replace Protocol Reference section: remove RUNTIME-SPEC.md link. Add: `[NIP-5D](../../specs/NIP-5D.md) -- Napplet-shell protocol specification`. Keep @napplet/shim and @napplet/core links.

Update packages/core/README.md:
- Replace Quick Start example: remove `AUTH_KIND` from the import list. Current code shows `BusKind, AUTH_KIND, SHELL_BRIDGE_URI` -- change to `BusKind, SHELL_BRIDGE_URI` (AUTH_KIND no longer exported).
- In Protocol Constants table: remove the AUTH_KIND row entirely (no longer exported). Update PROTOCOL_VERSION value from `'2.0.0'` to `'3.0.0'`.
- In Bus Event Kinds table: remove `BusKind.REGISTRATION | 29000 | Napplet registration events` row entirely (no longer in BusKind enum). Note: BusKind.INTER_PANE was renamed to BusKind.IPC_PEER -- update the table row from `BusKind.INTER_PANE | 29003 | Inter-pane pubsub events between napplets` to `BusKind.IPC_PEER | 29003 | Inter-napplet IPC peer events`.
- Replace Protocol Reference section: remove RUNTIME-SPEC.md link. Add: `[NIP-5D](../../specs/NIP-5D.md) -- Napplet-shell protocol specification`. Keep NIP-01 link.
  </action>
  <verify>
    <automated>grep -c "nostr-tools" packages/shim/README.md | xargs test 0 -eq && grep -c "AUTH" packages/shim/README.md | xargs test 0 -eq && grep -c "nostr-tools" packages/sdk/README.md | xargs test 0 -eq && grep -c "AUTH_KIND" packages/core/README.md | xargs test 0 -eq && grep -c "REGISTRATION" packages/core/README.md | xargs test 0 -eq && grep -c "RUNTIME-SPEC" packages/shim/README.md packages/sdk/README.md packages/core/README.md | grep -v ":0$" | wc -l | xargs test 0 -eq && echo "PASS"</automated>
  </verify>
  <done>
    - packages/shim/README.md has zero mentions of nostr-tools, AUTH, keypair, ephemeral, handshake, signing, or NIP-42
    - packages/sdk/README.md has zero mentions of nostr-tools, AUTH handshake, or signing
    - packages/core/README.md has no AUTH_KIND, no BusKind.REGISTRATION, PROTOCOL_VERSION shows 3.0.0, BusKind.IPC_PEER replaces INTER_PANE
    - No README references RUNTIME-SPEC.md (deleted file)
    - All three READMEs link to specs/NIP-5D.md instead
  </done>
</task>

<task type="auto">
  <name>Task 2: Update root README and vite-plugin README</name>
  <files>README.md, packages/vite-plugin/README.md</files>
  <read_first>
    - README.md
    - packages/vite-plugin/README.md
  </read_first>
  <action>
Update README.md (root):
- In the Packages table, shim description: change "Importing @napplet/shim installs the window.napplet global and completes the NIP-42 AUTH handshake. No named exports." to "Importing @napplet/shim installs the window.napplet global and registers with the shell. Zero crypto dependencies. No named exports."
- In the Architecture section "Napplet-Side Communication" ASCII diagram: remove the "NIP-42 AUTH handshake" line from the shell side. Replace it with "Identity via message.source". The shell side should list: ShellBridge, NIP-01 message routing, Identity via message.source, ACL enforcement, Service dispatch (kind 29010), Signer + storage proxy. Remove "NIP-42 AUTH handshake" from the napplet side (if present) and from the postMessage description.
- In the Origin section: remove the RUNTIME-SPEC.md link (file deleted from this repo). Change "the detailed [Runtime Reference](RUNTIME-SPEC.md)" to reference kehto: "the runtime spec lives in [@kehto](https://github.com/sandwichfarm/kehto)". Keep the NIP-5D spec link.
- In Related section: check if RUNTIME-SPEC.md is referenced -- if so, remove that bullet.

Update packages/vite-plugin/README.md:
- Replace Protocol Reference section: remove RUNTIME-SPEC.md link. Add: `[NIP-5D](../../specs/NIP-5D.md) -- Napplet-shell protocol specification`. Keep NIP-5A and Aggregate Hash PR links.
- In "Dev Mode" subsection: the text says "The napplet shim reads these tags during AUTH to include in the handshake." Change to: "The shell reads these tags during napplet registration to resolve the aggregate hash for ACL scoping."
  </action>
  <verify>
    <automated>grep -c "NIP-42" README.md | xargs test 0 -eq && grep -c "AUTH handshake" README.md | xargs test 0 -eq && grep -c "RUNTIME-SPEC" README.md packages/vite-plugin/README.md | grep -v ":0$" | wc -l | xargs test 0 -eq && grep -c "message.source" README.md | xargs test 1 -le && echo "PASS"</automated>
  </verify>
  <done>
    - Root README describes identity as "message.source" based, no AUTH handshake or NIP-42 references
    - Root README no longer links to RUNTIME-SPEC.md
    - Root README shim package description says "Zero crypto dependencies"
    - packages/vite-plugin/README.md no longer links to RUNTIME-SPEC.md
    - packages/vite-plugin/README.md dev mode description updated to reflect shell registration (not AUTH handshake)
  </done>
</task>

</tasks>

<verification>
Run against all 5 READMEs:
1. `grep -rn "AUTH" packages/shim/README.md packages/sdk/README.md` -- must return nothing
2. `grep -rn "nostr-tools" packages/shim/README.md packages/sdk/README.md` -- must return nothing
3. `grep -rn "AUTH_KIND\|REGISTRATION\|INTER_PANE" packages/core/README.md` -- must return nothing
4. `grep -rn "RUNTIME-SPEC" packages/*/README.md README.md` -- must return nothing
5. `grep -rn "NIP-42" packages/*/README.md README.md` -- must return nothing
6. `grep -c "message.source" README.md` -- must be >= 1
7. `grep -c "3.0.0" packages/core/README.md` -- must be >= 1
8. `pnpm build` -- must still pass (no code changes, just docs)
</verification>

<success_criteria>
All 5 READMEs accurately reflect v0.15.0 protocol simplification:
- Zero mentions of nostr-tools as a dependency of shim or sdk
- Zero mentions of AUTH handshake, NIP-42, ephemeral keypairs, or napplet-side signing
- Core README shows current exports (no AUTH_KIND, no BusKind.REGISTRATION, PROTOCOL_VERSION 3.0.0)
- Root README explains identity as shell-assigned via message.source
- All RUNTIME-SPEC.md links replaced with NIP-5D or kehto references
- No broken links to deleted files
</success_criteria>

<output>
After completion, create `.planning/phases/73-sdk-readme-update/73-01-SUMMARY.md`
</output>
