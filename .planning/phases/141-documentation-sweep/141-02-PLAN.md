---
phase: 141-documentation-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/vite-plugin/README.md
autonomous: true
requirements:
  - DOC-03
  - DOC-07

must_haves:
  truths:
    - "A reader of packages/vite-plugin/README.md no longer sees strictCsp documented as an active feature — the former `strictCsp (optional, v0.28.0+)` section is replaced by a short `strictCsp (deprecated, v0.29.0)` note explaining the option is accepted-but-warns, has no effect on emitted HTML, and is scheduled for hard-remove in v0.30.0"
    - "A reader of packages/vite-plugin/README.md sees a new `connect?: string[]` option documented — with accept/reject origin rules, the aggregate-hash fold rationale, and the one-`[\"connect\", \"<origin>\"]`-tag-per-origin manifest emission"
    - "A reader of packages/vite-plugin/README.md sees the fail-loud inline-script diagnostic documented — Vite build fails on any `<script>` element in `dist/index.html` without a `src` attribute, with the error referencing the shell's baseline `script-src 'self'` posture"
    - "A reader of packages/vite-plugin/README.md sees the cleartext build-time warning documented — http:/ws: origins in `connect` emit an informational console log citing browser mixed-content reality (silent failure from HTTPS shells except localhost / 127.0.0.1)"
    - "A reader of packages/vite-plugin/README.md sees the dev-mode-only `napplet-connect-requires` meta tag documented as DISTINCT from the shell-authoritative `napplet-connect-granted` name"
    - "A reader of packages/vite-plugin/README.md sees the `Nip5aManifestOptions` interface in the API Reference section reflect the v0.29.0 shape: `connect?: string[]` added, `strictCsp?: unknown` (deprecated) stays as a type-only compatibility field"
    - "The existing `## Getting Started` / `## Installation` / `## Quick Start` / `### configSchema` / `### requires` / `### Build-Time Guards` / `## Service Dependencies` / `## How It Works` / `### Environment Variables` sections in packages/vite-plugin/README.md are byte-identical to their pre-edit form except where the specific edits in this plan replace them (DOC-07 precedent)"
  artifacts:
    - path: packages/vite-plugin/README.md
      provides: "Updated vite-plugin README with strictCsp-deprecated + connect option + inline-script diagnostic + cleartext warning + dev-mode connect-requires meta"
      contains:
        - "connect?: string[]"
        - "napplet-connect-requires"
        - "napplet-connect-granted"
        - "mixed-content"
        - "script-src 'self'"
        - "deprecated"
        - "connect:origins"
  key_links:
    - from: "packages/vite-plugin/README.md"
      to: "@napplet/nub/connect/types.normalizeConnectOrigin"
      via: "cite as shared build-side + shell-side validator"
      pattern: "normalizeConnectOrigin"
    - from: "packages/vite-plugin/README.md"
      to: "NUB-CONNECT aggregateHash fold procedure"
      via: "connect:origins synthetic xTag entry prose"
      pattern: "connect:origins"
    - from: "packages/vite-plugin/README.md"
      to: "shell as sole runtime CSP authority (v0.29.0 architectural shift)"
      via: "strictCsp deprecation note + inline-script diagnostic rationale"
      pattern: "shell.*runtime CSP authority"
---

<objective>
Rewrite `packages/vite-plugin/README.md` for the v0.29.0 surgery: the former `strictCsp` section becomes a short deprecated-with-pointer note, a new `connect?: string[]` option section is added, a new build-time diagnostics section documents the inline-script hard-error + cleartext warning + dev-mode connect-requires meta, and the `Nip5aManifestOptions` interface in the API Reference reflects the v0.29.0 shape.

Purpose: Phase 138 (vite-plugin surgery) dropped the production strict-CSP machinery (the shell is now the sole runtime CSP authority) and added the `connect` option + inline-script diagnostic + cleartext warning + dev-mode `napplet-connect-requires` meta. The v0.28.0 README currently still documents `strictCsp` as an active 10-directive CSP emitter — napplet authors reading it will configure a feature that now only emits a deprecation warning and has zero effect on the built HTML. This plan fixes the prose to match the shipped code surface, preserving every historical / unrelated section byte-identical (DOC-07 precedent).

Output: One file edit (`packages/vite-plugin/README.md`), verifiable by literal-string grep; all unrelated sections (Environment Variables, Service Dependencies, How It Works, configSchema, requires, Build-Time Guards) byte-identical; `pnpm -r type-check` exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/141-documentation-sweep/141-CONTEXT.md

# Source-of-truth specs — cite, do not modify
@.planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md
@.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md
@specs/SHELL-CONNECT-POLICY.md

# Existing file being modified — read first; preserve byte-identical except for targeted edits
@packages/vite-plugin/README.md

# Phase 138 runtime source — authoritative prose source for option shape, error prefixes,
# accept/reject rules, aggregate-hash fold, and meta-tag names
@packages/vite-plugin/src/index.ts
@packages/nub/src/connect/types.ts

# Phase 138 summary + prior SUMMARY (if it exists) — what was ACTUALLY shipped
@.planning/phases/138-vite-plugin-strict-csp/138-01-SUMMARY.md
@.planning/phases/138-vite-plugin-strict-csp/138-02-SUMMARY.md
@.planning/phases/138-vite-plugin-strict-csp/138-03-SUMMARY.md

<interfaces>
<!-- Authoritative v0.29.0 vite-plugin shape — extracted from
     packages/vite-plugin/src/index.ts after Phase 138. Use these
     IDENTIFIERS and ACCEPT/REJECT rules exactly. -->

From packages/vite-plugin/src/index.ts:
```typescript
export interface Nip5aManifestOptions {
  nappletType: string;
  requires?: string[];
  configSchema?: JSONSchema7 | string;

  /**
   * @deprecated v0.29.0: The shell is now the sole runtime CSP authority.
   * This option is accepted-but-warns (one console.warn per build) and has
   * NO effect on the emitted HTML. Scheduled for hard-remove in v0.30.0
   * (tracked as REMOVE-STRICTCSP in the v0.29.0 requirements).
   */
  strictCsp?: unknown;

  /**
   * Origins the napplet needs direct browser-level network access to.
   * Each origin is validated via `normalizeConnectOrigin` from
   * `@napplet/nub/connect/types`, emitted as a `["connect", "<origin>"]`
   * manifest tag, and folded into `aggregateHash` via a synthetic
   * `connect:origins` xTag so any origin change auto-invalidates prior
   * grants. See NUB-CONNECT for the authoritative origin format.
   */
  connect?: string[];
}
```

Origin accept/reject rules (from normalizeConnectOrigin in packages/nub/src/connect/types.ts):
- Accepted schemes: `https`, `wss`, `http`, `ws` (closed set)
- Accepted hosts: ASCII DNS labels (already-Punycode for IDN), IPv4 literals (including 127.0.0.1 + RFC-1918 private ranges)
- Reject: uppercase scheme or host; wildcard (`*`); path (`/...`); query (`?...`); fragment (`#...`); default port (`:443` on https/wss, `:80` on http/ws); non-Punycode non-ASCII; IPv6 literals (bracketed or post-port-strip)
- Error prefix: `[@napplet/nub/connect]` — the vite-plugin re-prefixes its own diagnostics with `[nip5a-manifest]`

Manifest tag emission (from Phase 138 summary):
- One `["connect", "<origin>"]` tag per origin, in AUTHOR-DECLARED ORDER
- Placed BETWEEN `manifestXTags` and `configTags`
- Origins re-sorted ASCII-ascending for the `connect:origins` fold (determinism); author-declared order preserved for manifest tags (readability per NUB-CONNECT §Manifest Tag Shape)

aggregateHash fold:
- Canonical procedure: lowercase → ASCII-ascending sort → LF-join (no trailing newline) → UTF-8 → SHA-256 → lowercase hex
- Synthetic xTag entry `[<hex-digest>, 'connect:origins']` pushed into xTags BEFORE `computeAggregateHash`
- Filtered OUT of the public `['x', ...]` tag projection via `SYNTHETIC_XTAG_PATHS` (covers both `config:schema` from v0.25.0 and `connect:origins` from v0.29.0)
- Normative conformance fixture: 3 origins (`https://api.example.com`, `https://xn--caf-dma.example.com`, `wss://events.example.com`), SHA-256 = `cc7c1b1903fb23ecb909d2427e1dccd7d398a5c63dd65160edb0bb8b231aa742`

Inline-script diagnostic (Phase 138 `assertNoInlineScripts`):
- Runs in `closeBundle` after `dist/index.html` is written
- Scans for `<script>` elements without a non-empty `src` attribute
- Allow-list: `<script type="application/json">`, `<script type="application/ld+json">`, `<script type="importmap">`, `<script type="speculationrules">`, and HTML comments (stripped before scan)
- Hard-errors (throws, fails the build) with message referencing `script-src 'self'` as the shell's baseline CSP posture

Cleartext informational warning:
- Emitted in `configResolved` when any `http:` or `ws:` origin appears in `connect`
- One `console.log` with `[nip5a-manifest]` prefix
- Explains browser mixed-content reality: HTTPS shells silently drop cleartext fetches except for `localhost` / `127.0.0.1` secure-context exceptions

Dev-mode-only `napplet-connect-requires` meta tag (VITE-10):
- Injected ONLY in dev mode (`vite serve`), NOT in production builds
- Distinct name from the shell-authoritative `napplet-connect-granted` (which the shell injects at serve time on approval, and is what the napplet shim reads)
- `content` is the ASCII-sorted list of normalized origins, space-separated
- Used by shell-less local-preview workflows to simulate a granted state
- Plugins MUST NEVER emit `napplet-connect-granted` — that name is reserved for the shell

Baseline commit hash for DOC-07 diff-check:
- 741f9c8 (docs(133-01): update 5 READMEs for v0.28.0 resource NUB + strict CSP surface) — state of packages/vite-plugin/README.md BEFORE this phase
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the `strictCsp` section into a short `@deprecated` note + add new `connect` option section + new Build-Time Diagnostics section + update Nip5aManifestOptions interface</name>
  <files>packages/vite-plugin/README.md</files>
  <read_first>
    - packages/vite-plugin/README.md (existing — read ALL sections to establish which are preserved byte-identical; specifically identify line ranges for the current `#### strictCsp` section so the replacement lands exactly there)
    - packages/vite-plugin/src/index.ts (verify the EXACT current shape of `Nip5aManifestOptions`; verify the plugin's prefix string for normalized-origin rejections is `[nip5a-manifest]` not `[@napplet/vite-plugin]`; verify `assertNoInlineScripts` allow-list)
    - packages/nub/src/connect/types.ts (verify `normalizeConnectOrigin` accept/reject rule set and error prefix)
    - .planning/phases/138-vite-plugin-strict-csp/138-02-SUMMARY.md (Phase 138-02 shipped the connect option + diagnostic + cleartext warn; cross-reference before writing prose)
    - .planning/phases/138-vite-plugin-strict-csp/138-01-SUMMARY.md (Phase 138-01 shipped the strictCsp deprecation — confirms the option is `strictCsp?: unknown` and emits one `console.warn` per build)
    - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md (authoritative source for origin format + manifest tag shape prose)
  </read_first>
  <action>
ONE surgical edit to `packages/vite-plugin/README.md`. Four targeted changes, landing in this order in the file. Preserve every other section byte-identical.

═══

**1A. Replace the entire `#### strictCsp (optional, v0.28.0+)` section with a short deprecated-pointer note.**

Locate the section starting with `#### strictCsp (optional, v0.28.0+)` and extending through the end of its "Disable for legacy / migration napplets:" subsection (the line `nip5aManifest({ nappletType: 'my-old-napp' });                     // default (off — strict CSP requires explicit opt-in)` plus its closing code fence and the trailing paragraph `See [NUB-RESOURCE](https://github.com/napplet/nubs) and [NIP-5D Security Considerations](../../specs/NIP-5D.md) for the wire-side contract that strict CSP enforces in tandem with the iframe sandbox model.`). This entire block is replaced.

Replacement content:

```markdown
#### strictCsp (deprecated in v0.29.0)

**Type:** `unknown` (accepted-but-warns)

As of v0.29.0, the shell is the sole runtime CSP authority — every napplet receives its Content-Security-Policy from the HTTP response header the shell serves with the napplet's HTML, not from a meta tag emitted by the build. The `strictCsp` option is therefore **deprecated and has no effect on the emitted HTML**.

For back-compat with pre-v0.29.0 `vite.config.ts` files, the option is still accepted at the type level (typed as `unknown`) and emits exactly one `console.warn` per build from `configResolved`:

```
[nip5a-manifest] `strictCsp` is deprecated and has no effect (v0.29.0+). The shell is now the sole runtime CSP authority. Remove this option from your vite.config.ts to silence this warning. Scheduled for hard-remove in v0.30.0 (REMOVE-STRICTCSP).
```

Migration:

- Drop the `strictCsp` key from your `vite.config.ts`. Napplets ship only the HTML + assets; the shell emits the CSP.
- If your napplet ships inline `<script>` elements (without `src`), fix them before v0.30.0 — the build now hard-fails on inline scripts to mirror the shell's baseline `script-src 'self'` posture. See [Build-Time Diagnostics](#build-time-diagnostics) below.
- If your napplet needs direct network access (previously implicit under a permissive dev CSP, now explicit), declare the origins via the new [`connect`](#connect-optional-v0290) option and let the shell prompt the user.

See [NUB-CLASS-2.md](https://github.com/napplet/nubs) for the shell-side posture that replaces the old in-page strict-CSP baseline, and [`specs/SHELL-CONNECT-POLICY.md`](../../specs/SHELL-CONNECT-POLICY.md) for the deployer-side checklist covering HTTP-responder precondition, residual meta-CSP refusal, and grant-persistence key.
```

═══

**1B. Insert a new `#### connect (optional, v0.29.0+)` section IMMEDIATELY AFTER the deprecated-strictCsp section (and BEFORE the next existing `### Environment Variables` section).**

New section content:

````markdown
#### connect (optional, v0.29.0+)

**Type:** `string[]`

Declares the origins the napplet needs direct browser-level network access to (`fetch`, `WebSocket`, `SSE`, `EventSource`). Each origin is validated by the shared `normalizeConnectOrigin` function from `@napplet/nub/connect/types` (the single source of truth used by both the build tool and shell implementations), emitted as a `["connect", "<origin>"]` tag in the signed NIP-5A manifest, and folded into `aggregateHash` via a synthetic `connect:origins` xTag so any origin-list change auto-invalidates prior user grants keyed on `(dTag, aggregateHash)`.

**Quick start:**

```ts
// vite.config.ts
import { defineConfig } from 'vite';
import { nip5aManifest } from '@napplet/vite-plugin';

export default defineConfig({
  plugins: [
    nip5aManifest({
      nappletType: 'my-napplet',
      connect: [
        'https://api.example.com',
        'wss://events.example.com',
        'https://xn--caf-dma.example.com',   // café.example.com in Punycode
      ],
    }),
  ],
});
```

**Origin format (accept / reject rules):**

| Rule | Behaviour |
|------|-----------|
| Scheme | MUST be one of `https`, `wss`, `http`, `ws`. Closed set. Anything else rejected. |
| Lowercase | Scheme AND host MUST be lowercase on the wire. Uppercase anywhere rejected. |
| Host | ASCII DNS labels or already-Punycode IDN (`xn--…`). Non-Punycode non-ASCII rejected — use Punycode in the config string. |
| IPv4 literal | Accepted (including `127.0.0.1`, `10.x`, `192.168.x`, `172.16.x`). |
| IPv6 literal | Rejected in v1 (both bracketed `[::1]` and bare). |
| Port | Explicit non-default OK (e.g. `:8443`). Default port (`:443` on https/wss, `:80` on http/ws) MUST be omitted — explicit default-port forms rejected for aggregateHash determinism. |
| Path / query / fragment | Rejected. Origin = scheme + host + optional non-default port. `https://foo.com/api`, `https://foo.com?x=1`, `https://foo.com#x` all invalid. |
| Wildcard | Rejected. Each subdomain is a separate origin. |

Rejected inputs throw at `configResolved` with a diagnostic prefixed `[nip5a-manifest]`, failing the Vite build before `dist/` is written.

**Manifest tag emission:**

One `["connect", "<origin>"]` tag per origin, emitted in **author-declared order** (as supplied in the config array). Tags are placed between the existing `manifestXTags` (file hashes) and `configTags` (config schema) in the signed kind 35128 manifest event.

**aggregateHash fold:**

Origins are fed through a canonical fold procedure (lowercase → ASCII-ascending sort → LF-join with no trailing newline → UTF-8 → SHA-256 → lowercase hex) and the resulting digest is pushed as a synthetic xTag entry `[<hex-digest>, 'connect:origins']` into the manifest's xTag array BEFORE `computeAggregateHash`. The synthetic entry participates in `aggregateHash` but is filtered out of the public `['x', ...]` tag projection via a shared `SYNTHETIC_XTAG_PATHS` set (which also covers the v0.25.0 `config:schema` fold).

Any addition, removal, or reorder-after-normalization of the origin set produces a different `aggregateHash`, which auto-invalidates any prior user grant keyed on `(dTag, aggregateHash)` — guaranteeing the shell re-prompts on a supply-chain origin change.

**Module-load conformance guardrail:**

At ESM import time, the plugin self-checks its fold procedure against the normative NUB-CONNECT conformance fixture (3 origins → SHA-256 `cc7c1b1903fb23ecb909d2427e1dccd7d398a5c63dd65160edb0bb8b231aa742`). A drift in the fold procedure (even a one-byte change to the sort or separator) fires a fatal at plugin-import time, preventing the build from shipping a hash incompatible with shells.

**See also:**
- [NUB-CONNECT](https://github.com/napplet/nubs) — normative spec (origin format, aggregateHash fold, conformance fixture)
- [`specs/SHELL-CONNECT-POLICY.md`](../../specs/SHELL-CONNECT-POLICY.md) — shell-deployer checklist (HTTP-responder precondition, residual meta-CSP refusal, grant persistence, consent prompt MUSTs, revocation UX)
- [NUB-CLASS-2.md](https://github.com/napplet/nubs) — posture triggered by presence of `connect` tags (CSP shape, shell responsibilities at serve time)
````

═══

**1C. Insert a new `## Build-Time Diagnostics` top-level section AFTER the existing `## Service Dependencies` section and BEFORE the existing `## How It Works` section.**

New section content:

````markdown
## Build-Time Diagnostics

v0.29.0 adds two build-time safeguards and one informational warning, all enforced in `closeBundle` or `configResolved` so misconfiguration fails loud before `dist/` reaches a shell.

### Inline-script fail-loud (new in v0.29.0)

The plugin scans `dist/index.html` after build for any `<script>` element without a non-empty `src` attribute. Such elements are hard-errors — the build throws and exits non-zero with a diagnostic referencing the shell's baseline `script-src 'self'` posture.

Allowed script variants (not flagged):
- `<script src="..."></script>` — external module
- `<script type="application/json">…</script>` — JSON data island
- `<script type="application/ld+json">…</script>` — JSON-LD data island
- `<script type="importmap">…</script>` — import map
- `<script type="speculationrules">…</script>` — speculation rules
- HTML comments (stripped before scan)

Rejected:
- `<script>console.log("hi")</script>` — inline JS without `src`
- `<script type="module">/* inline */</script>` — inline module

**Why:** The shell is now the sole runtime CSP authority. Every conformant shell serves napplet HTML with a CSP that includes `script-src 'self'` (or tighter, via `'nonce-…'`). Inline `<script>` without `src` violates that policy, so shipping inline JS guarantees the napplet will be partially non-functional at runtime. Surfacing this at build time converts a silent runtime failure into a loud build failure.

**Fixing:** Move inline JS into a `.js` module under `src/` and import it. For build-time state that needs to reach runtime code (feature flags, config defaults), use a `<script type="application/json" id="data">…</script>` data island and read it at runtime via `document.getElementById('data').textContent`.

### Cleartext origin warning (new in v0.29.0)

When `connect` contains any `http:` or `ws:` origin, the plugin emits one informational `console.log` during `configResolved` explaining browser mixed-content reality:

```
[nip5a-manifest] cleartext origin declared in `connect`: http://… / ws://…
HTTPS shells silently drop fetches to http: / ws: origins (except localhost / 127.0.0.1 secure-context exceptions). Approved grants will produce no traffic when the shell is served over https. Prefer https: / wss: end-to-end, or check shell.supports('connect:scheme:http') at runtime.
```

This is **informational, not an error** — operator policy may explicitly permit cleartext for localhost development or for explicit-opt-out-of-TLS deployments. Shells advertising `shell.supports('connect:scheme:http') === false` refuse cleartext entirely; check at runtime before depending on cleartext grants.

### Dev-mode-only `napplet-connect-requires` meta (new in v0.29.0)

In dev mode (`vite serve`), the plugin injects a `<meta name="napplet-connect-requires" content="<origins>">` tag into the served HTML's `<head>` when `connect` is non-empty. The `content` is the ASCII-sorted origin list, space-separated. This tag is useful for shell-less local-preview workflows that want to simulate a granted state without running a full shell.

**IMPORTANT:** The tag name is `napplet-connect-requires` — deliberately distinct from the shell-authoritative `napplet-connect-granted`, which is what the napplet shim reads at install time and is injected ONLY by a real shell at HTTP-serve time on grant approval. The plugin MUST NEVER emit `napplet-connect-granted`; the plugin has no authority to represent a user's consent decision.

The `-requires` tag is NOT injected in production builds (`vite build`). Production HTML contains neither tag; grants flow through the shell's HTTP response at serve time.
````

═══

**1D. Update the `### Nip5aManifestOptions` interface code block in the API Reference section.**

Locate the `### Nip5aManifestOptions` subsection and its TypeScript code block (currently shows `strictCsp?: boolean | StrictCspOptions;`). Replace the entire code block with:

```ts
interface Nip5aManifestOptions {
  /** Napplet type/dtag (e.g., 'feed', 'chat') */
  nappletType: string;

  /** Service dependencies this napplet requires (e.g., ['audio', 'notifications']). Optional. */
  requires?: string[];

  /**
   * JSON Schema (draft-07+) describing the napplet's config surface (NUB-CONFIG).
   * May be an inline object or a path string (resolved relative to the Vite
   * project root). Falls through to `config.schema.json` then `napplet.config.*`
   * discovery when omitted.
   */
  configSchema?: JSONSchema7 | string;

  /**
   * Origins the napplet needs direct browser-level network access to (v0.29.0+).
   * Each origin validated via `normalizeConnectOrigin` from
   * `@napplet/nub/connect/types`, emitted as `["connect", "<origin>"]`
   * manifest tags, folded into `aggregateHash` via a synthetic `connect:origins`
   * xTag so any origin change auto-invalidates prior grants.
   * See NUB-CONNECT for the authoritative origin format.
   */
  connect?: string[];

  /**
   * @deprecated v0.29.0: The shell is now the sole runtime CSP authority.
   * This option is accepted-but-warns (one console.warn per build) and has
   * NO effect on the emitted HTML. Scheduled for hard-remove in v0.30.0
   * (REMOVE-STRICTCSP). Remove this key from vite.config.ts to silence.
   */
  strictCsp?: unknown;
}
```

═══

**1E. Update the `## Protocol Reference` section at the bottom of the file.** Add two new bullets BETWEEN the existing `[NUB-CONFIG spec (PR #13)]...` line and the existing `[NIP-5D](../../specs/NIP-5D.md) ...` line (preserve all other bullets byte-identical):

```markdown
- [NUB-CONNECT (drafts)](https://github.com/napplet/nubs) -- user-gated direct network access: origin format, manifest tag shape, canonical `connect:origins` aggregateHash fold, runtime API
- [NUB-CLASS (drafts)](https://github.com/napplet/nubs) + [`NUB-CLASS-1.md`](https://github.com/napplet/nubs) + [`NUB-CLASS-2.md`](https://github.com/napplet/nubs) -- napplet class track and the two v0.29.0 posture members (strict baseline + user-approved explicit-origin)
- [`specs/SHELL-CONNECT-POLICY.md`](../../specs/SHELL-CONNECT-POLICY.md) + [`specs/SHELL-CLASS-POLICY.md`](../../specs/SHELL-CLASS-POLICY.md) -- shell-deployer checklists
```

Also update the existing `[NUB-RESOURCE (PR pending)]...` line: change `(PR pending)` to `(drafts)` since NUB-RESOURCE is no longer a pending PR in v0.29.0.
  </action>
  <verify>
    <automated>
grep -q "connect?: string\[\]" packages/vite-plugin/README.md &amp;&amp; \
grep -q "napplet-connect-requires" packages/vite-plugin/README.md &amp;&amp; \
grep -q "napplet-connect-granted" packages/vite-plugin/README.md &amp;&amp; \
grep -q "mixed-content" packages/vite-plugin/README.md &amp;&amp; \
grep -qE "script-src\s+'self'" packages/vite-plugin/README.md &amp;&amp; \
grep -q "deprecated" packages/vite-plugin/README.md &amp;&amp; \
grep -q "connect:origins" packages/vite-plugin/README.md &amp;&amp; \
grep -q "strictCsp (deprecated" packages/vite-plugin/README.md &amp;&amp; \
grep -q "Build-Time Diagnostics" packages/vite-plugin/README.md &amp;&amp; \
grep -q "cc7c1b1903fb23ecb909d2427e1dccd7d398a5c63dd65160edb0bb8b231aa742" packages/vite-plugin/README.md &amp;&amp; \
! grep -q "strictCsp: true" packages/vite-plugin/README.md &amp;&amp; \
! grep -q "10-directive baseline" packages/vite-plugin/README.md &amp;&amp; \
! grep -q "perm:strict-csp" packages/vite-plugin/README.md
    </automated>
    DOC-07 check (historical preservation): Run
    ```
    git diff 741f9c8 -- packages/vite-plugin/README.md | grep -E "^-[^-]" | grep -iE "(configSchema|requires\?:|Service Dependencies|Environment Variables|VITE_DEV_PRIVKEY_HEX|Build-Time Guards|## How It Works|Dev Mode|Build Mode \(closeBundle\)|## Installation|## Quick Start|## Getting Started)"
    ```
    MUST return zero matches (all unrelated sections byte-identical). The ONLY sections that should show DELETIONS in the diff are the old `#### strictCsp (optional, v0.28.0+)` section (replaced by the deprecation note), the `Nip5aManifestOptions` code block (replaced), and possibly one line in Protocol Reference (the NUB-RESOURCE `(PR pending)` → `(drafts)` swap).

    Zero-grep hygiene for removed v0.28.0 terminology:
    ```
    ! grep -iE "StrictCspOptions|buildBaselineCsp|validateStrictCspOptions|assertMetaIsFirstHeadChild|'nonce-<random>'|Pitfall 1|Pitfall 2|Pitfall 18|Pitfall 19|first child of <head>" packages/vite-plugin/README.md
    ```
    MUST exit 0 (these v0.28.0 strict-CSP terms are fully removed from active documentation).
  </verify>
  <done>
- The former `#### strictCsp (optional, v0.28.0+)` section is replaced by a short `#### strictCsp (deprecated in v0.29.0)` note with migration pointers to `connect` + Build-Time Diagnostics.
- A new `#### connect (optional, v0.29.0+)` section is present with accept/reject rules table, manifest tag emission description, aggregateHash fold explanation, and the normative conformance-fixture digest.
- A new `## Build-Time Diagnostics` top-level section is present covering inline-script fail-loud, cleartext warning, and dev-mode `napplet-connect-requires` meta.
- `Nip5aManifestOptions` interface in API Reference reflects v0.29.0 shape (connect added, strictCsp typed as `unknown` + `@deprecated`).
- Protocol Reference bullets updated with NUB-CONNECT, NUB-CLASS, and SHELL-CONNECT-POLICY / SHELL-CLASS-POLICY pointers.
- All unrelated sections (Getting Started, Installation, Quick Start, configSchema, requires, Build-Time Guards, Environment Variables, Service Dependencies, How It Works — Dev Mode / Build Mode) are byte-identical per `git diff 741f9c8`.
- `pnpm -r type-check` exits 0.
  </done>
</task>

</tasks>

<verification>
Phase-level doc-sweep checks for this plan:

1. **DOC-03 covered:** The `connect` option, inline-script diagnostic, cleartext warning, dev-mode connect-requires meta, and `Nip5aManifestOptions` interface are all documented; the old strictCsp section no longer documents strictCsp as an active feature.

2. **DOC-07 (historical preservation):** `git diff 741f9c8 -- packages/vite-plugin/README.md | grep -E "^-[^-]" | grep -iE "(configSchema|requires\?:|Service Dependencies|Environment Variables|VITE_DEV_PRIVKEY_HEX|Build-Time Guards|## How It Works)"` MUST return zero matches.

3. **Zero-grep hygiene (v0.28.0 strict-CSP terminology removed):** grep confirms no remaining references to `StrictCspOptions`, `buildBaselineCsp`, `10-directive baseline`, `perm:strict-csp`, `'nonce-<random>'`, or "Pitfall 1/2/18/19" in active documentation.

4. **Type-check green:** `pnpm -r type-check` exits 0 (doc-only edit, no source touched).
</verification>

<success_criteria>
- 1 file edited (packages/vite-plugin/README.md).
- Every grep in the per-task `<verify>` block passes.
- `git diff 741f9c8 -- packages/vite-plugin/README.md` shows DELETIONS ONLY inside the old `#### strictCsp (optional, v0.28.0+)` section, the `Nip5aManifestOptions` code block, and (optionally) one line in Protocol Reference; ZERO deletions elsewhere.
- Zero references to v0.28.0 strict-CSP terminology as active features remain.
- `pnpm -r type-check` exits 0.
- Both REQ-IDs in the `requirements` frontmatter are addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/141-documentation-sweep/141-02-SUMMARY.md`.
</output>
