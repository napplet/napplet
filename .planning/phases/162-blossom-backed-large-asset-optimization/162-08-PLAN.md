---
phase: 162-blossom-backed-large-asset-optimization
plan: "08"
type: execute
wave: 5
depends_on:
  - 162-05
  - 162-07
files_modified:
  - packages/vite-plugin/package.json
  - pnpm-lock.yaml
  - packages/vite-plugin/src/optimizer/node-services.ts
  - packages/vite-plugin/src/optimizer/node-services.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-05/D-07: Node services reuse protected nbunksec or visibly pair by QR/paste while keeping all signer material out of Vite output, logs, cache, and config."
    - "D-09 through D-15/D-29: lazy Node relay, DNS, and HTTP adapters satisfy the shared verified discovery/upload contracts and abort deterministically."
    - "No terminal, credential, DNS, relay, or HTTP side effect occurs at module evaluation or before an over-target artifact asks for live services."
    - "Audited Vite-package dependency and lock changes are minimal, while known unrelated root `package.json` and root `deno.lock` changes remain untouched and unstaged."
  artifacts:
    - path: packages/vite-plugin/src/optimizer/node-services.ts
      provides: "Node terminal/QR, process/filesystem, relay, DNS, fetch, clock, and safe-logger adapters"
    - path: packages/vite-plugin/src/optimizer/node-services.test.ts
      provides: "Lazy side-effect, pairing, protected-store, cancellation, resolver, fetch, and redaction tests"
    - path: packages/vite-plugin/package.json
      provides: "Audited @napplet/build-tools, qrcode, and @types/qrcode consumer dependencies"
  key_links:
    - from: packages/vite-plugin/src/optimizer/node-services.ts
      to: packages/build-tools/src/index.ts
      via: "Node implementations satisfy shared signer, store, discovery, network-policy, and Blossom interfaces"
      pattern: "@napplet/build-tools"
    - from: packages/vite-plugin/src/optimizer/node-services.ts
      to: OS credential providers
      via: "shared createPlatformSecretStore receives Node process/filesystem adapters"
      pattern: "createPlatformSecretStore"
---

<objective>
Install audited consumer dependencies and implement fully fakeable Node platform adapters per D-05, D-07, D-09 through D-15, D-27, and D-29.

Purpose: Isolate package/lock and platform side effects from live Vite orchestration so both can reach independent green checkpoints with a small file budget.
Output: Audited Vite dependency resolution and tested lazy Node services, with no live pipeline mutation.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-CONTEXT.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-RESEARCH.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-05-SUMMARY.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-07-SUMMARY.md
@packages/cli/src/nostr-connect.ts
@packages/cli/src/key-store.ts
@packages/vite-plugin/package.json
</context>

<interfaces>
- `createNodeOptimizationServices(options: NodeOptimizationOptions): OptimizationServices`
- `NodeOptimizationOptions` accepts injected terminal, QR renderer, protected store, process/filesystem, relay client, DNS resolver, fetch, clock, abort signal, and safe logger.
- Production defaults are created lazily; tests supply all fakes and never access real stdin, credentials, DNS, network, clock, or environment.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add audited Node consumer dependencies under a lockfile guard</name>
  <files>packages/vite-plugin/package.json, pnpm-lock.yaml</files>
  <precondition>`pnpm-lock.yaml` and `packages/vite-plugin/package.json` have no unrelated overlapping edits; root `package.json` and root `deno.lock` dirty hunks are classified as unowned.</precondition>
  <action>Before editing, record status plus explicit diffs/hashes for the two owned files and the known unowned root `package.json`/`deno.lock`. Reconfirm `qrcode` and `@types/qrcode` identities and exact versions are `[OK]` in the research Package Legitimacy Audit, then add `@napplet/build-tools` and only those audited QR dependencies to the Vite package. Make a recoverable copy of `pnpm-lock.yaml`, run a narrow lock-only update, and audit every generated hunk. Accept only expected build-tools/qrcode importer and resolution changes; restore and halt on unrelated dependency churn or any root-file change. Stage only the Vite package and pnpm lock, inspect the staged names/content, and prove root `package.json` and root `deno.lock` remain byte-identical and unstaged before the atomic dependency commit.</action>
  <reversibility rating="reversible">Package and lock edits are copied before generation and restored on any unexpected hunk.</reversibility>
  <verify>
    <automated>pnpm --filter @napplet/vite-plugin install --lockfile-only &amp;&amp; git diff --check -- packages/vite-plugin/package.json pnpm-lock.yaml</automated>
  </verify>
  <done>The Vite package resolves only audited dependencies, its generated lock diff is minimal, and known unrelated root dirty files remain untouched and unstaged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lazy fakeable Node optimization services</name>
  <files>packages/vite-plugin/src/optimizer/node-services.ts, packages/vite-plugin/src/optimizer/node-services.test.ts</files>
  <behavior>
    - Test 1 (D-05/D-07): stored-session reconnect avoids prompting; otherwise interactive QR/text plus pasted bunker input use shared first-success pairing and protected persistence.
    - Test 2: noninteractive input, cancellation, missing credential backend, protected-store error, signer timeout, DNS failure, and HTTP abort return typed redaction-safe unavailable/failure results.
    - Test 3: module import and service factory creation perform no terminal, credential, process, filesystem, relay, DNS, fetch, clock, or environment side effect.
    - Test 4: injected fakes satisfy every platform boundary and abort all pending work deterministically.
  </behavior>
  <action>Write the Node adapter tests first, then implement narrow adapters over `@napplet/build-tools`: terminal/QR and pasted input, process/filesystem for the shared protected store, relay transport, DNS resolution, fetch, monotonic clock, abort, and safe logger. Reuse the same storage key and provider behavior as the CLI; never create a plaintext plugin config or cache. Return an explicit unavailable result when pairing cannot be performed noninteractively. Keep every production side effect behind lazy method invocation so importing the module or constructing the plugin remains pure. Commit the red tests, adapter implementation, and cleanup/refactor at explicit green checkpoints.</action>
  <reversibility rating="reversible">All platform behavior remains behind shared injected contracts and can change without altering public Vite configuration or protocol output.</reversibility>
  <verify>
    <automated>pnpm --filter @napplet/vite-plugin test:unit -- optimizer/node-services.test.ts &amp;&amp; pnpm --filter @napplet/vite-plugin type-check</automated>
  </verify>
  <done>Node services reconnect or pair, supply verified discovery/upload dependencies, remain fully fakeable/lazy, clean up every abort path, and expose no secret-bearing output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node process → terminal/credential store | Pairing and reusable signer material cross platform-specific interfaces. |
| Node process → relay/DNS/HTTP | Untrusted events, addresses, redirects, and response bodies enter shared validation. |
| Package metadata → pnpm lock | Dependency generation can absorb unrelated dirty workspace state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-38 | Information Disclosure | Node terminal/store/logger | high | mitigate | Task 2 uses protected shared storage and redaction tests across every adapter error/status path. |
| T-162-39 | Elevation of Privilege | Node signer adapter | high | mitigate | Task 2 delegates to least-authority BuildSigner and exposes no extra signing API. |
| T-162-40 | Elevation of Privilege | DNS/fetch adapter | high | mitigate | Task 2 supplies resolution/redirect data to the shared public-HTTPS policy before bytes or authorization leave process. |
| T-162-41 | Denial of Service | lazy platform resources | high | mitigate | Task 2 bounds and aborts terminal, relay, DNS, and HTTP work and proves import-time purity. |
| T-162-SC | Tampering | dependency/lock generation | high | mitigate | Task 1 permits only audited identities, recoverable narrow generation, explicit hunk audit, and explicit-path staging. |
</threat_model>

<verification>
- Inspect dependency legitimacy and generated lock evidence.
- `pnpm --filter @napplet/vite-plugin test:unit -- optimizer/node-services.test.ts`
- `pnpm --filter @napplet/vite-plugin type-check`
- Prove the known root `package.json` and root `deno.lock` dirty hunks are unchanged and unstaged.
</verification>

<success_criteria>
- The plan changes only four files and reaches separate dependency and Node-adapter green commits.
- Node side effects are lazy, fakeable, abortable, least-authority, and secret-safe.
- Package/lock generation is audited without absorbing unrelated dirty state.
</success_criteria>

<output>
Create `.planning/phases/162-blossom-backed-large-asset-optimization/162-08-SUMMARY.md` when done.
</output>
