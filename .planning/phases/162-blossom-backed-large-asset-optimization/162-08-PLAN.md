---
phase: 162-blossom-backed-large-asset-optimization
plan: "08"
type: execute
wave: 5
depends_on:
  - 162-05
  - 162-07
files_modified:
  - packages/vite-plugin/src/optimizer/large-fixture.ts
  - packages/vite-plugin/src/optimizer/large-fixture.test.ts
  - packages/vite-plugin/README.md
  - packages/build-tools/README.md
  - packages/cli/README.md
  - apps/docs/packages/vite-plugin.md
  - README.md
  - scripts/check-build-secret-leaks.mjs
  - scripts/check-build-secret-leaks.test.mjs
  - .changeset/tidy-blossom-assets.md
  - .planning/phases/162-blossom-backed-large-asset-optimization/162-DEMO.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-22/D-23: a deterministic generated fixture with more than 50 MiB of total candidate assets, each individually modest, proves trigger size, selection order, verified uploads, final size, and exact recovery through fake NAP-RESOURCE."
    - "D-23/D-24: durable demo evidence and user/package documentation show supported reference forms, nonfatal outcomes, signer/discovery flow, private mapping boundaries, canonical links, and the non-portability of one 50 MiB Blob or streaming claim."
    - "D-25: targeted Node and Deno suites, full build/type/unit gates, docs/link checks, secret scan, changesets, and the pinned AI-slop gate pass before the branch is pushed and a PR is opened."
    - "The demo proves multiple bounded assets totaling 50 MiB+, not a new NAP-RESOURCE streaming/range/progress capability or a 50 MiB single-Blob portability promise."
  artifacts:
    - path: packages/vite-plugin/src/optimizer/large-fixture.test.ts
      provides: "Generated 50 MiB+ end-to-end build, fake relay/signer/Blossom, and fake NAP-RESOURCE demonstration"
    - path: .planning/phases/162-blossom-backed-large-asset-optimization/162-DEMO.md
      provides: "Recorded commands, sizes, selection/upload/recovery evidence, security scans, full gates, and known whole-Blob boundary"
    - path: packages/vite-plugin/README.md
      provides: "Public configuration, behavior, browser-support matrix, failure semantics, and canonical protocol links"
    - path: .changeset/tidy-blossom-assets.md
      provides: "Release intent for the Vite feature, shared build tooling, and corrected CLI network implementation"
  key_links:
    - from: packages/vite-plugin/src/optimizer/large-fixture.test.ts
      to: packages/vite-plugin/src/index.ts
      via: "the generated Vite build exercises the public plugin option and complete automatic path"
      pattern: "largeAssetOptimization"
    - from: packages/vite-plugin/src/optimizer/large-fixture.test.ts
      to: fake NIP-46/relay/Blossom/NAP services
      via: "verified events, authorization, exact descriptors, and recovered bytes cross every external boundary locally"
      pattern: "createFixtureServices"
    - from: packages/vite-plugin/README.md
      to: canonical NIP/NAP/BUD sources
      via: "all normative behavior links to living external documents and private build behavior is labeled non-normative"
      pattern: "non-normative"
---

<objective>
Prove the completed optimization with a deterministic 50 MiB+ multi-asset demonstration, synchronize documentation and release metadata, pass every repository gate, and ship the branch per D-22 through D-25.

Purpose: Turn unit-level architecture and security evidence into a reproducible end-to-end result that users can understand and maintainers can safely release.
Output: Generated large fixture, recorded demo evidence, synchronized docs, changeset, secret scanner, complete green gates, pushed branch, and open PR.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-CONTEXT.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-VALIDATION.md
@.planning/phases/162-blossom-backed-large-asset-optimization/COVERAGE.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-05-SUMMARY.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-07-SUMMARY.md
@packages/vite-plugin/README.md
@packages/cli/README.md
@apps/docs/packages/vite-plugin.md
@.github/workflows/ai-slop.yml
@.aislop/config.yml
</context>

<interfaces>
Fixture evidence contract:
- Generate at test runtime at least seven deterministic assets, no one asset greater than 10 MiB, with total candidate bytes greater than 50 MiB.
- Fake directory relays deliver signed newest/older/forged kind-10002 vectors; fake write relays deliver signed newest/older/forged kind-10063 vectors.
- Fake NIP-46 signer accepts only kind 24242; fake Blossom servers verify authorization, exact bytes, and descriptor integrity; fake NAP-RESOURCE returns uploaded whole Blobs.
- Evidence records pre-optimization rendered bytes, deterministic selected order, per-server verified descriptors, final `/index.html` bytes, removed/preserved assets, aggregate hash, and byte-for-byte recovered content.

Documentation contract:
- Canonical references link externally to NIP-5A, NIP-5D, NIP-46, NIP-65, the accepted/proposed NAP-RESOURCE text, and relevant Blossom BUD documents.
- Every private loader/mapping, URL-hardening, threshold, and supported-browser statement is labeled implementation behavior, not protocol law.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Demonstrate more than 50 MiB across bounded assets end to end</name>
  <files>packages/vite-plugin/src/optimizer/large-fixture.ts, packages/vite-plugin/src/optimizer/large-fixture.test.ts</files>
  <behavior>
    - Test 1 (D-22): a reproducible generated set totaling more than 50 MiB, with every member at most 10 MiB, produces a would-be inline artifact above target without committing binary fixtures.
    - Test 2 (D-23): signed fake relay events prove corrected directory → write/unmarked relay → ordered server discovery; fake signer and Blossom endpoints prove exact authorized uploads and matching descriptors.
    - Test 3 (D-23): final `/index.html` is below 2 MiB for the demonstration set, selection order matches the deterministic planner, selected files are gone, aggregate hash verifies, and all asset bytes recover exactly through fake `resource.bytesMany`.
    - Test 4: forged/stale relay events, one secondary upload failure, and a corrupt NAP Blob fail or report according to policy without corrupting the local artifact.
    - Test 5 (D-24): a separately generated individual Blob over the supported per-asset bound is preserved/ineligible with an explicit whole-Blob portability reason, not presented as a streaming success.
  </behavior>
  <action>Build a runtime-only deterministic fixture generator that produces at least seven individually modest assets totaling over 50 MiB, with supported JavaScript/media/CSS references and no checked-in large binary. Drive the public plugin through fake but cryptographically valid NIP-46, kind-10002, kind-10063, BUD HEAD/PUT/auth/descriptor, and NAP-RESOURCE services. Capture structured evidence for trigger/render sizes, candidate order, selected assets, hashes, server results, final bytes, aggregate hash, and exact recovered content per D-22/D-23. Include older and forged discovery events and partial/corrupt service cases. Add a distinct over-limit single-Blob case that documents the whole-Blob portability boundary per D-24. Keep the full test locally bounded and under the repository's targeted-test time budget. Commit the generator and green demonstration separately.</action>
  <reversibility rating="reversible">All service effects occur in local fakes and all large bytes are generated in temporary directories removed by test teardown.</reversibility>
  <verify>
    <automated>pnpm --filter @napplet/vite-plugin test:unit -- optimizer/large-fixture.test.ts</automated>
  </verify>
  <done>The generated fixture proves more than 50 MiB of bounded assets optimize below target and recover byte-for-byte across every real layer and fake external boundary, while the single-Blob boundary is explicit and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize public docs and release intent with canonical boundaries</name>
  <files>packages/vite-plugin/README.md, packages/build-tools/README.md, packages/cli/README.md, apps/docs/packages/vite-plugin.md, README.md</files>
  <action>Document the automatic/disabled option, exact 2 MiB trigger and nonfatal target semantics, protected QR/paste signer reuse, corrected kind-10002 write/unmarked relay to kind-10063 discovery flow, exact-byte canonical Blossom URI behavior, direct-secondary policy, private mapping/loader status, supported/ineligible browser reference matrix, whole-Blob resource limits, security bounds, report fields, and recovery behavior per D-24. Link every normative statement to the living canonical NIP/NAP/BUD source and label threshold, URL hardening, selection policy, private table/loader, and browser support as non-normative tool behavior. State plainly that one 50 MiB Blob, streaming, ranges, progress, BUD-04 mirror, BUD-10 URI syntax, deletion, listing, media transforms, payment, report, and other opted-out operations are not delivered. Add `@napplet/build-tools` to the root package overview and document its public APIs with examples. Update CLI text for shared behavior without narrowing existing commands. Keep Markdown paragraphs physically unwrapped per repository convention and run stale-string/canonical-link checks before the docs commit.</action>
  <verify>
    <automated>pnpm check:links &amp;&amp; pnpm --filter @napplet/vite-plugin build &amp;&amp; pnpm --filter @napplet/build-tools build</automated>
  </verify>
  <done>All public docs agree with implementation and canonical sources, clearly distinguish protocol from tooling, enumerate supported/ineligible forms and opted-out operations, and pass links/package builds.</done>
</task>

<task type="auto">
  <name>Task 3: Record evidence, run all gates, version shipped packages, and open the PR</name>
  <files>scripts/check-build-secret-leaks.mjs, scripts/check-build-secret-leaks.test.mjs, .changeset/tidy-blossom-assets.md, .planning/phases/162-blossom-backed-large-asset-optimization/162-DEMO.md</files>
  <action>Create a focused scanner with synthetic positive/negative tests for actual nbunksec/bunker encodings, private-key encodings, Blossom Authorization headers, credential-bearing URLs, and accidental secret serialization in generated artifacts, logs, reports, config/cache-facing diffs, and fixture output; permit documented source-code identifiers without permitting secret-shaped values. Add one changeset covering the shipped Vite feature, new shared build-tools package, and CLI behavior correction with semver appropriate to their 0.x public impact. Run the generated demonstration, targeted plugin/build-tools/CLI suites, existing Deno regression suite, full `pnpm build`, `pnpm type-check`, `pnpm -r test:unit`, docs/link/JSR/release-tooling checks, the secret scanner, `git diff --check`, and the repository's pinned AI-slop 0.12.0 workflow-equivalent scan; restore touched-file quality to 100/100 and do not alter `.aislop/config.yml`. Record exact commands, versions, pass/fail results, trigger/final sizes, selected assets, verified servers/descriptors, recovery hashes, secret-scan result, support boundary, and any nonfatal exhaustion evidence in `162-DEMO.md`. Preserve unrelated dirty files and stage this phase's paths explicitly. Commit each green checkpoint with Conventional Commits and the required agent co-author trailer, push the current feature branch, and create or update the GitHub PR with what/why, canonical boundaries, threat mitigations, verification evidence, changesets, and out-of-scope operations.</action>
  <reversibility rating="costly">The changeset and commits are reversible before merge; pushing and opening a PR are outward-facing but authorized closing actions and remain reviewable without merging or publishing.</reversibility>
  <verify>
    <automated>node --test scripts/check-build-secret-leaks.test.mjs &amp;&amp; node scripts/check-build-secret-leaks.mjs &amp;&amp; deno check packages/build-tools/src/index.ts &amp;&amp; deno test --allow-all packages/cli/tests &amp;&amp; pnpm build &amp;&amp; pnpm type-check &amp;&amp; pnpm -r test:unit &amp;&amp; pnpm check:links &amp;&amp; pnpm check:jsr &amp;&amp; pnpm test:release-tooling &amp;&amp; npx --yes aislop@0.12.0 scan --changes --base origin/main &amp;&amp; git diff --check</automated>
  </verify>
  <done>All targeted/full/security/docs/release/slop gates are green, the demo evidence and changeset are committed without unrelated files, the branch is pushed, and an open PR contains the complete verification and boundary record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated fixture → production plugin | Large deterministic input exercises real selection, transaction, and loader code. |
| Fake external services → demonstration evidence | Signed events, auth, descriptors, and recovered bytes must faithfully model canonical external operations. |
| Source/build/log/config → secret scanner | Potential secret-shaped material crosses into persistent artifacts and review output. |
| Local branch → GitHub PR | Commits, changesets, evidence, and any unrelated working-tree state can become externally visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-43 | Tampering | generated large fixture | high | mitigate | Task 1 records deterministic seeds, exact input hashes, signed event vectors, upload descriptors, final aggregate hash, and recovered-byte hashes. |
| T-162-44 | Denial of Service | 50 MiB+ demonstration | high | mitigate | Task 1 splits total bytes across individually bounded assets, uses temporary generated files, bounds service concurrency, and tears down all fakes/resources. |
| T-162-45 | Information Disclosure | demo/log/config/cache/PR | high | mitigate | Task 3 adds a tested secret scanner, records redacted evidence only, stages explicit paths, and scans all generated and outward-facing artifacts before push. |
| T-162-46 | Spoofing | fake relay/signer services | high | mitigate | Task 1 uses cryptographically valid and invalid vectors and requires production verification paths rather than bypass flags. |
| T-162-47 | Repudiation | release evidence | medium | mitigate | Task 3 records exact commands/results in DEMO.md, atomic commits, changeset intent, and PR verification details. |
| T-162-48 | Elevation of Privilege | documentation as protocol authority | medium | mitigate | Task 2 links living canonical sources and labels every optimization/loading/security policy as non-normative implementation behavior. |
| T-162-SC | Tampering | released dependency graph | high | mitigate | Task 3 runs lock/package legitimacy, build, JSR, release-tooling, changeset, and pinned AI-slop gates before push. |
</threat_model>

<verification>
- `pnpm --filter @napplet/vite-plugin test:unit -- optimizer/large-fixture.test.ts`
- `pnpm --filter @napplet/build-tools test:unit && deno check packages/build-tools/src/index.ts`
- `deno test --allow-all packages/cli/tests`
- `node --test scripts/check-build-secret-leaks.test.mjs && node scripts/check-build-secret-leaks.mjs`
- `pnpm build && pnpm type-check && pnpm -r test:unit`
- `pnpm check:links && pnpm check:jsr && pnpm test:release-tooling`
- `npx --yes aislop@0.12.0 scan --changes --base origin/main` with 100/100 for touched code.
- `git diff --check`, explicit-path commit inspection, push, and PR URL capture in `162-DEMO.md`.
</verification>

<success_criteria>
- A generated 50 MiB+ bounded multi-asset fixture proves the complete feature and final artifact below target.
- Byte recovery, descriptor integrity, aggregate hash, security failures, and nonfatal cases have durable evidence.
- Docs, package APIs, canonical links, support boundaries, and changesets are synchronized.
- Targeted and full Node/Deno/release/security/slop gates pass.
- Only phase-owned files ship on a pushed feature branch with an open reviewable PR.
</success_criteria>

<output>
Create `.planning/phases/162-blossom-backed-large-asset-optimization/162-08-SUMMARY.md` when done.
</output>
