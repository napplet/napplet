---
phase: 162-blossom-backed-large-asset-optimization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/vite-plugin/src/index.ts
  - packages/vite-plugin/src/manifest.ts
  - packages/vite-plugin/src/optimizer/pipeline.ts
  - packages/vite-plugin/src/optimizer/loader.ts
  - packages/vite-plugin/src/optimizer/pipeline.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-01/D-02: an artifact whose would-be inline /index.html size is greater than 2 * 1024 * 1024 bytes is evaluated using retained Vite asset boundaries and deterministic byte-descending, path-tied ordering."
    - "D-03/D-04: each selection is measured by rendering the actual candidate HTML; selection stops below the optimization target, while exhaustion above the target is reported without failing a conformant build."
    - "D-14/D-15/D-16/D-17: exact emitted bytes produce lowercase SHA-256 descriptors, fake authenticated upload results become only blossom:sha256:<64-lowerhex> URIs in a private deterministic table, and final committed output contains neither selected asset files nor stale bytes."
    - "D-18/D-20/D-21: selected files remain recoverable until upload, rewritten artifact, private loader, and fake resource byte recovery all verify; the runtime rejects bytes that do not match the recorded hash."
    - "No requirement-probe predicates exist because Phase 162 has no requirement IDs; the complete decision and canonical-boundary predicates are carried here instead."
  artifacts:
    - path: packages/vite-plugin/src/optimizer/pipeline.ts
      provides: "Production optimization planner, pure renderer, transactional commit coordinator, report types, and dependency-injected upload/resource seams"
    - path: packages/vite-plugin/src/optimizer/loader.ts
      provides: "Private deterministic mapping serializer and one-path resource loader used by the tracer"
    - path: packages/vite-plugin/src/optimizer/pipeline.test.ts
      provides: "Generated-byte tracer tests for threshold, ordering, upload, rewrite, rollback, and fake NAP-RESOURCE recovery"
  key_links:
    - from: packages/vite-plugin/src/index.ts
      to: packages/vite-plugin/src/optimizer/pipeline.ts
      via: "closeBundle passes retained Rollup output and a dependency-injected fake/live service boundary to optimizeSingleFileArtifact"
      pattern: "optimizeSingleFileArtifact"
    - from: packages/vite-plugin/src/optimizer/pipeline.ts
      to: packages/vite-plugin/src/manifest.ts
      via: "the manifest aggregate hash is calculated only from the verified committed index.html"
      pattern: "writeBundleManifest"
    - from: packages/vite-plugin/src/optimizer/loader.ts
      to: window.napplet.resource.bytes
      via: "the private loader resolves only canonical Blossom resource URIs and verifies returned Blob bytes"
      pattern: "resource\.bytes"
---

<objective>
Prove one production-quality end-to-end large-asset path from retained Vite output through deterministic selection, fake authenticated upload, private metadata and URI rewrite, transactional commit, and fake NAP-RESOURCE byte recovery per D-01 through D-04 and D-14 through D-21.

Purpose: Establish the architectural seam and failure semantics before live signer, discovery, and HTTP adapters are connected, without inventing any NIP-5A, NIP-5D, Blossom, or NAP-RESOURCE surface.
Output: A tested optimization pipeline and private loader skeleton that later plans expand without changing the artifact, upload, or runtime trust boundaries.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-CONTEXT.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-RESEARCH.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-PATTERNS.md
@.planning/phases/162-blossom-backed-large-asset-optimization/COVERAGE.md
@.planning/spikes/001-blossom-build-optimization/prototype.mjs
@packages/vite-plugin/src/index.ts
@packages/vite-plugin/src/html.ts
@packages/vite-plugin/src/manifest.ts
@packages/vite-plugin/src/types.ts
@packages/sdk/src/resource.ts
</context>

<interfaces>
Existing entry points to preserve:
- `singleFileBuildConfig(): Plugin` in `packages/vite-plugin/src/index.ts`
- `inlineSingleFileBuildAssets(outDir: string, htmlFileName?: string): void` in `packages/vite-plugin/src/html.ts`
- `writeBundleManifest(outDir: string, options: Nip5aManifestOptions): Promise<void>` in `packages/vite-plugin/src/manifest.ts`
- `resourceBytes(uri: string): Promise<Blob>` and `resourceBytesMany(uris: string[]): Promise<Blob[]>` in `packages/sdk/src/resource.ts`

New internal contracts produced by this plan:
- `optimizeSingleFileArtifact(input: OptimizeArtifactInput, services: OptimizationServices): Promise<OptimizationReport>`
- `planExternalAssets(input: RetainedBuild): OptimizationPlan`
- `renderOptimizedHtml(input: RenderInput): RenderedArtifact`
- `commitOptimizedArtifact(input: CommitInput): Promise<void>`
- `renderPrivateResourceTable(entries: ResourceTableEntry[]): string`
- `renderResourceLoader(table: ResourceTableEntry[]): string`
- `ResourceTableEntry = { source: string; uri: string; sha256: string; bytes: number; mime: string }`

`OptimizationServices` is private build-tool plumbing. Its fake upload method must accept exact bytes plus a short-lived authorization result and return a verified Blossom descriptor; it is not wire protocol.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one retained asset through verified upload, rewrite, commit, and byte recovery</name>
  <files>packages/vite-plugin/src/index.ts, packages/vite-plugin/src/manifest.ts, packages/vite-plugin/src/optimizer/pipeline.ts, packages/vite-plugin/src/optimizer/loader.ts, packages/vite-plugin/src/optimizer/pipeline.test.ts</files>
  <behavior>
    - Test 1 (D-01): 2,097,152 rendered bytes do not trigger optimization; 2,097,153 rendered bytes do.
    - Test 2 (D-02/D-03): retained assets are considered by byte length descending and normalized path ascending for ties, and the candidate HTML is actually rendered and measured after each addition.
    - Test 3 (D-04): eligible-asset exhaustion above 2 MiB returns a visible nonfatal `target-not-reached` report with final measured bytes.
    - Test 4 (D-14/D-15): exact selected bytes are hashed to lowercase SHA-256, fake upload receives those exact bytes and a fake short-lived authorization, and the only public URI emitted is `blossom:sha256:<64 lowercase hexadecimal>`.
    - Test 5 (D-16/D-17): the private table is stable under input permutation, records source identity, canonical URI, digest, byte count, and MIME type, and selected source files are absent only after verified commit.
    - Test 6 (D-18): upload failure, descriptor mismatch, renderer failure, or final verification failure preserves/restores every emitted file byte-for-byte and prevents manifest hashing of a partial artifact.
    - Test 7 (D-19/D-20/D-21): the injected loader recovers one Blob through fake `resource.bytes`, accepts matching bytes, and rejects a digest or length mismatch without using a raw network path.
  </behavior>
  <action>Start by adding the failing generated-byte tests above, then replace the current destructive inline/delete flow with a pure retained-output model. Configure Vite internally to retain asset boundaries, collect exact output bytes and MIME data, compute the would-be inline HTML, and evaluate only when it is greater than `2 * 1024 * 1024` per D-01. Implement deterministic selection and render-after-each-selection per D-02/D-03; keep the 2 MiB value an optimization target and return a structured nonfatal exhaustion report per D-04. Add dependency-injected fake upload and resource services so the tracer proves an authenticated request boundary without live credentials or network access. Hash the exact selected bytes and accept a returned descriptor only when its lowercase SHA-256 and size match; serialize a deterministic private table and emit only the canonical NAP-RESOURCE URI per D-14 through D-17. The table and loader are signed-artifact implementation plumbing, never manifest metadata, a NIP-5D field, or a shell requirement. Stage removals in a recoverable quarantine or equivalent reversible transaction, render and verify the final HTML plus fake byte recovery, then delete the quarantined files and invoke manifest hashing; on any error restore the original output per D-18. Use the existing whole-Blob `resource.bytes` path for the one tracer consumer and validate length and digest at the runtime trust boundary per D-19/D-21. Preserve the current build path when optimization is not triggered. Commit the red test, green implementation, and any refactor as separate atomic commits.</action>
  <reversibility rating="reversible">The new path is opt-out capable and the original emitted files remain recoverable until the verified commit boundary.</reversibility>
  <verify>
    <automated>pnpm --filter @napplet/vite-plugin test:unit -- optimizer/pipeline.test.ts</automated>
  </verify>
  <done>A generated retained asset crosses every phase layer, is uploaded through the fake authenticated seam, becomes a canonical URI in private metadata, is recovered and hash-checked through fake NAP-RESOURCE, and all injected failures leave the original emitted artifact intact; the targeted suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rollup output → optimizer | Build filenames, bytes, MIME data, and reference shapes enter deterministic selection and rewrite logic. |
| Optimizer → upload service | Exact selected bytes and short-lived authorization cross into an external-service adapter seam. |
| Signed artifact → napplet runtime | Private mapping data drives resource retrieval inside an opaque-origin iframe. |
| Runtime resource API → consumer | A whole Blob returned by the shell crosses back into the napplet and must match signed metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-01 | Tampering | Upload descriptor and private resource table | high | mitigate | Task 1 hashes exact emitted bytes, requires descriptor hash/size equality, serializes stable metadata, and tests digest substitution. |
| T-162-02 | Tampering | Destructive artifact commit | high | mitigate | Task 1 quarantines selected files, verifies upload/rewrite/recovery, writes the final manifest last, and tests restoration at every injected failure. |
| T-162-03 | Tampering | Runtime Blob substitution | high | mitigate | Task 1 validates whole-Blob length and SHA-256 against signed private metadata before exposing bytes to the consumer. |
| T-162-04 | Denial of Service | Candidate rendering and Blob verification | high | mitigate | Task 1 uses deterministic one-at-a-time rendering, bounded candidate sets from emitted output, and whole-Blob limits inherited from the selected asset policy; generated tests exercise bounded failure. |
| T-162-05 | Information Disclosure | Build logs and optimization report | medium | mitigate | Task 1 reports paths, hashes, counts, and status only; the service interface contains no long-lived signer secret and tests snapshots for stable redacted output. |
| T-162-06 | Elevation of Privilege | Private loader treated as protocol authority | medium | mitigate | Task 1 keeps mapping and loader fields out of NIP-5A metadata and shell messages; tests assert the existing resource methods are the sole runtime boundary. |
</threat_model>

<verification>
- `pnpm --filter @napplet/vite-plugin test:unit -- optimizer/pipeline.test.ts`
- `pnpm --filter @napplet/vite-plugin type-check`
- Inspect the generated tracer artifact to confirm the manifest has no private mapping fields and the only resource URI is `blossom:sha256:<lowercase-hash>`.
</verification>

<success_criteria>
- Threshold, deterministic selection, render measurement, and nonfatal exhaustion implement D-01 through D-04 exactly.
- The tracer crosses build, upload seam, private rewrite, commit, manifest-ordering, runtime retrieval, and hash verification layers.
- Failure injection proves no partial artifact or selected-file deletion survives a failed transaction.
- No new manifest tag, NIP-5D message, NAP action, or shell obligation is introduced.
</success_criteria>

<output>
Create `.planning/phases/162-blossom-backed-large-asset-optimization/162-01-SUMMARY.md` when done.
</output>
