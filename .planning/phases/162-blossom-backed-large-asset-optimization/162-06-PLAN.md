---
phase: 162-blossom-backed-large-asset-optimization
plan: "06"
type: execute
wave: 5
depends_on:
  - 162-04
  - 162-05
files_modified:
  - packages/cli/src/suggestions.ts
  - packages/cli/tests/suggestions_test.ts
  - packages/cli/src/blossom-upload.ts
  - packages/cli/src/deploy-network.ts
  - packages/cli/tests/deploy_network_test.ts
  - packages/cli/tests/deploy_plan_test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-09 through D-13: CLI relay and Blossom suggestions use the shared verified two-stage discovery direction, including write/unmarked kind-10002 relays and ordered kind-10063 servers."
    - "D-14/D-28/D-29: CLI Blossom upload behavior uses the same exact-byte, authorization, descriptor, direct-secondary, timeout, redirect, retry, and failure semantics as the Vite integration will use."
    - "Existing CLI suggestions and deployment flows remain compatible while duplicate discovery/upload protocol code is removed."
    - "Any current CLI behavior that contradicts the living NIP-65 or Blossom BUD documents is corrected and captured by a regression vector rather than preserved as precedent."
  artifacts:
    - path: packages/cli/src/suggestions.ts
      provides: "Deno relay adapter and CLI presentation over shared verified Blossom discovery"
    - path: packages/cli/src/blossom-upload.ts
      provides: "Deno fetch/platform adapter over shared exact-byte Blossom upload"
    - path: packages/cli/src/deploy-network.ts
      provides: "Deployment network orchestration consuming shared typed discovery/upload results"
    - path: packages/cli/tests/deploy_network_test.ts
      provides: "CLI regression coverage for discovery direction, server order, auth, partial upload, and visible fallback"
  key_links:
    - from: packages/cli/src/suggestions.ts
      to: packages/build-tools/src/discovery.ts
      via: "CLI supplies its Deno relay client and presents found/no-list results"
      pattern: "discoverBlossomServers"
    - from: packages/cli/src/blossom-upload.ts
      to: packages/build-tools/src/blossom.ts
      via: "CLI delegates exact-byte HEAD/PUT and descriptor verification"
      pattern: "uploadExactBlobs"
    - from: packages/cli/src/deploy-network.ts
      to: packages/cli/src/deploy-signer.ts
      via: "the shared BuildSigner signs narrowly scoped upload authorization"
      pattern: "BuildSigner"
---

<objective>
Move CLI discovery and Blossom upload onto the shared canonical services while preserving its deployment experience and correcting any stale relay-direction assumptions per D-09 through D-15 and D-28/D-29.

Purpose: Demonstrate that the shared network code is a true extraction from established CLI capability, not a second implementation, before the Vite plugin consumes it.
Output: Thin CLI relay/fetch adapters and green suggestions/deployment regressions against `@napplet/build-tools`.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-CONTEXT.md
@.planning/phases/162-blossom-backed-large-asset-optimization/COVERAGE.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-04-SUMMARY.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-05-SUMMARY.md
@packages/cli/src/suggestions.ts
@packages/cli/src/blossom-upload.ts
@packages/cli/src/deploy-network.ts
@packages/cli/tests/suggestions_test.ts
@packages/cli/tests/deploy_network_test.ts
</context>

<interfaces>
Consume from `@napplet/build-tools`:
- `discoverBlossomServers(...)`, `BlossomDiscoveryResult`
- `headBlob(...)`, `uploadBlob(...)`, `uploadExactBlobs(...)`, `UploadBatchResult`
- `NetworkPolicy`, `BuildSigner`, and injected relay/fetch/resolver/clock interfaces

Preserve CLI entry points and output contracts from:
- suggestion resolution in `packages/cli/src/suggestions.ts`
- deployment upload orchestration in `packages/cli/src/blossom-upload.ts` and `packages/cli/src/deploy-network.ts`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route CLI suggestions through verified two-stage discovery</name>
  <files>packages/cli/src/suggestions.ts, packages/cli/tests/suggestions_test.ts</files>
  <behavior>
    - Test 1 (D-09/D-10): CLI starts from the bounded directory list, obtains the user's verified newest kind 10002, and uses only write/unmarked relays for kind-10063 lookup.
    - Test 2 (D-11/D-12): read-only relays and every kind-10002 `r` URL are absent from Blossom results; normalized servers retain kind-10063 order.
    - Test 3 (D-13): no valid server list produces an explicit user-visible result and requires the existing deliberate CLI choice path instead of substituting a server.
  </behavior>
  <action>Replace the CLI's internal suggestion traversal with a Deno relay adapter passed to `discoverBlossomServers`. Update tests first to encode the corrected direction: public directory relays → newest verified user kind 10002 → write/unmarked relays → newest verified user kind 10063. Preserve the CLI's presentation layer and deliberate manual/no-list decision path, but delete duplicated signature, newest-event, tag, normalization, and ordering logic after all callers use the shared result. Include `wss://purplepag.es` in the shared default through the package, not as a CLI-only exception. Flag any missing canonical behavior rather than inventing a suggestion tag or message.</action>
  <verify>
    <automated>deno test --allow-env --allow-read --allow-net packages/cli/tests/suggestions_test.ts</automated>
  </verify>
  <done>CLI suggestions follow the corrected verified two-stage path, ordered kind-10063 results and visible no-list behavior pass, and no duplicate discovery parser remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route CLI deployment uploads through the shared Blossom client</name>
  <files>packages/cli/src/blossom-upload.ts, packages/cli/src/deploy-network.ts, packages/cli/tests/deploy_network_test.ts, packages/cli/tests/deploy_plan_test.ts</files>
  <behavior>
    - Test 1 (D-14): deployment uploads exact input bytes with a short-lived kind-24242 authorization and accepts only a hash/size-matching descriptor.
    - Test 2 (D-28): primary and configured secondary servers receive deterministic direct uploads, while no BUD-04 mirror request is made.
    - Test 3 (D-29): timeout, retry, redirect, body, concurrency, and partial-batch limits from the shared client propagate as redaction-safe CLI status and a failed deployment step.
    - Test 4: existing dry-run, local signing, remote signing, and non-network deployment-plan behavior stays green.
  </behavior>
  <action>Turn `blossom-upload.ts` into the Deno fetch/resolver adapter for `uploadExactBlobs`, and wire `deploy-network.ts` to shared typed discovery, signer, and upload results. Begin with regression tests for exact bytes, descriptor mismatch, direct secondary uploads, unsafe redirects, retry bounds, and partial failures. Preserve existing deploy planning and dry-run behavior while replacing duplicate BUD request/auth/descriptor code. Ensure errors and status show safe server origins and hashes only, never authorization content or signer/session values. Commit network adapter migration and the full deploy-plan compatibility pass separately.</action>
  <verify>
    <automated>deno test --allow-all packages/cli/tests/deploy_network_test.ts packages/cli/tests/deploy_plan_test.ts</automated>
  </verify>
  <done>CLI deployment uses one shared Blossom implementation, all canonical and hardening vectors pass, dry-run/local/remote branches remain compatible, and partial upload can never be reported as success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deno relay adapter → shared discovery | CLI transport data becomes verified relay/server-list results. |
| CLI options → shared network policy | User/configured server inputs enter URL, DNS, and redirect validation. |
| Shared upload result → CLI deployment plan | Verified or failed upload evidence determines whether deployment can continue. |
| CLI output → operator/log capture | Status and diagnostics can disclose server credentials or signer material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-27 | Spoofing | CLI relay adapter | high | mitigate | Task 1 defers author/kind/signature/newest validation to the shared service and tests forged/stale vectors at the CLI boundary. |
| T-162-28 | Elevation of Privilege | configured/deferred server URL | high | mitigate | Task 2 routes every URL, DNS resolution, and redirect through shared public-HTTPS policy before authorization or bytes are sent. |
| T-162-29 | Tampering | deployment upload result | high | mitigate | Task 2 propagates only verified hash/size/server evidence and treats every partial or ambiguous batch as failed. |
| T-162-30 | Information Disclosure | CLI network diagnostics | high | mitigate | Task 2 snapshots redacted output and excludes credentials, auth events, response bodies, bunker/session values, and DNS details. |
| T-162-31 | Repudiation | protocol extraction compatibility | medium | mitigate | Both tasks run targeted old/new regression suites and commit suggestion and upload migrations independently. |
</threat_model>

<verification>
- `deno test --allow-env --allow-read --allow-net packages/cli/tests/suggestions_test.ts`
- `deno test --allow-all packages/cli/tests/deploy_network_test.ts packages/cli/tests/deploy_plan_test.ts`
- `deno test --allow-all packages/cli/tests`
- Run the repository secret scan over CLI test output and modified source.
</verification>

<success_criteria>
- CLI discovery and upload consume the same shared services as the planned Vite integration.
- The corrected kind-10002/write-relay/kind-10063 flow is regression-tested at the CLI boundary.
- Existing deployment and suggestion capabilities remain available.
- No duplicate NIP/BUD parsing or request construction survives in the migrated CLI paths.
</success_criteria>

<output>
Create `.planning/phases/162-blossom-backed-large-asset-optimization/162-06-SUMMARY.md` when done.
</output>
