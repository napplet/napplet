---
phase: 162-blossom-backed-large-asset-optimization
plan: "03"
type: execute
wave: 3
depends_on:
  - 162-02
files_modified:
  - packages/build-tools/src/index.ts
  - packages/build-tools/src/secret-store.ts
  - packages/build-tools/src/secret-store.test.ts
  - packages/build-tools/src/terminal.ts
  - packages/build-tools/src/terminal.test.ts
  - packages/cli/deno.json
  - pnpm-lock.yaml
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-05: QR/deep-link and pasted-bunker paths race to one verified session, cancel losing work, and expose only safe terminal status."
    - "D-07: verified nbunksec is persisted/reused through macOS Keychain, Linux Secret Service, Windows Credential Manager, or the deliberate file fallback without entering artifacts, logs, cache, or config."
    - "D-08/D-27: terminal and protected-store coordination extend the neutral package, and npm plus CLI-local Deno resolution consume it without a package cycle or root workspace metadata changes."
    - "Known unrelated dirty root `package.json` and root `deno.lock` hunks remain present, unmodified, unstaged, and absent from every plan commit."
  artifacts:
    - path: packages/build-tools/src/secret-store.ts
      provides: "Protected platform-store selection and verified-session persistence over injected process/filesystem adapters"
    - path: packages/build-tools/src/terminal.ts
      provides: "Abortable nostrconnect QR/paste pairing coordinator"
    - path: packages/cli/deno.json
      provides: "CLI-local source mapping for @napplet/build-tools without changing either Deno workspace lock"
    - path: pnpm-lock.yaml
      provides: "Audited generated workspace resolution containing only expected @napplet/build-tools dependency hunks"
  key_links:
    - from: packages/build-tools/src/terminal.ts
      to: packages/build-tools/src/signer.ts
      via: "pairBuildSigner races the two terminal entry paths and returns one verified BuildSignerSession"
      pattern: "pairBuildSigner"
    - from: packages/build-tools/src/terminal.ts
      to: packages/build-tools/src/secret-store.ts
      via: "session material is saved only after successful verified pairing"
      pattern: "SecretStore"
    - from: packages/cli/deno.json
      to: packages/build-tools/src/index.ts
      via: "a CLI-local relative import mapping is checked with --no-lock, preserving root and legacy CLI locks"
      pattern: "@napplet/build-tools"
---

<objective>
Add QR/paste pairing, protected session persistence, and guarded npm/CLI-local Deno workspace integration per D-05, D-07, D-08, and D-27.

Purpose: Complete the shared signer service while isolating lock generation from known unrelated dirty root files and proving only expected dependency hunks can enter an atomic commit.
Output: Tested pairing/store coordination, CLI-local Deno resolution, minimal audited lock updates, and dirty-state evidence.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-CONTEXT.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-RESEARCH.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-02-SUMMARY.md
@pnpm-workspace.yaml
@packages/cli/deno.json
@packages/cli/deno.lock
@packages/cli/src/key-store.ts
@packages/cli/src/nostr-connect.ts
</context>

<interfaces>
Extend `@napplet/build-tools` with:
- `pairBuildSigner(options: PairBuildSignerOptions): Promise<BuildSignerSession>`
- `reconnectBuildSigner(options: ReconnectBuildSignerOptions): Promise<BuildSignerSession | undefined>`
- `createPlatformSecretStore(options: PlatformSecretStoreOptions): Promise<SecretStore>`
- `TerminalAdapter.showQr(value: string): Promise<void>`, `readLine(prompt: string, signal: AbortSignal): Promise<string>`, and `writeStatus(message: SafeStatus): void`

Dirty-state contract:
- Root `package.json` is already covered by a known unrelated workspace edit and is never in this plan's file set.
- Root `deno.lock` contains unrelated generated workspace-member hunks and is never regenerated, edited, restored, staged, or committed by this plan.
- Existing `packages/*` in `pnpm-workspace.yaml` discovers the new package without a root metadata edit.
- Deno's official workspace rules make `lock` workspace-only: only one lock file/resolver may exist per workspace, so `packages/cli/deno.lock` cannot receive an isolated member update. The root `deno.lock` and legacy `packages/cli/deno.lock` both remain byte-identical and outside this plan's staging path. [CITED: https://docs.deno.com/runtime/fundamentals/workspaces/]
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coordinate protected session persistence across platform stores</name>
  <files>packages/build-tools/src/secret-store.ts, packages/build-tools/src/secret-store.test.ts, packages/build-tools/src/index.ts</files>
  <behavior>
    - Test 1 (D-07): reconnect reads the stable secret key before pairing and returns undefined for a missing record without mutating storage.
    - Test 2: macOS Keychain, Linux Secret Service, Windows Credential Manager, and deliberate file fallback preserve existing CLI behavior through injected process/filesystem operations.
    - Test 3: a session is stored only after verified connection; parse, store, or reconnect failure never replaces the prior valid value.
    - Test 4: nbunksec, client key, bunker URI, authorization, command arguments, and secret-store response data remain absent from status, errors, JSON, inspection, and test output.
  </behavior>
  <action>Write the failing store vectors, then move the existing platform behavior behind the neutral `ProcessAdapter`/`FileSystemAdapter` and implement `createPlatformSecretStore` plus reconnect/save coordination per D-07. Preserve OS provider selection, key names, and the deliberate file fallback; do not downgrade a protected provider to plaintext on an unexpected provider error. Parse and validate stored nbunksec before connecting, bind the expected remote pubkey/relays, and mutate storage only after a verified pair. Export the implementation with public JSDoc and commit its red/green checkpoints without changing workspace or lock files.</action>
  <verify>
    <automated>deno test --no-lock packages/build-tools/src/secret-store.test.ts</automated>
  </verify>
  <done>All platform-store and session lifecycle vectors pass, previous valid material survives failures, and no secret-bearing output exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Race QR/deep-link pairing against pasted bunker input</name>
  <files>packages/build-tools/src/terminal.ts, packages/build-tools/src/terminal.test.ts, packages/build-tools/src/index.ts</files>
  <behavior>
    - Test 1 (D-05): the QR/deep-link and pasted bunker paths race; first verified success wins and cancels the losing prompt/relay work.
    - Test 2: failure in one path leaves the other active, while cancellation, dual failure, or timeout closes all prompts/subscriptions and stores nothing.
    - Test 3 (D-07): successful pairing stores one opaque nbunksec through SecretStore and every terminal/error snapshot remains secret-free.
    - Test 4 (D-27): fake terminal, relay, store, process/filesystem, and clock adapters run without evaluating Node- or Deno-specific globals.
  </behavior>
  <action>Write the pairing race tests first, then implement `pairBuildSigner` and `reconnectBuildSigner` over the existing signer/store contracts per D-05/D-07. Generate a nostrconnect URI for an injected QR renderer, concurrently accept pasted bunker input, and use one AbortController hierarchy for first-success cancellation and cleanup. Persist only a verified session and expose safe pairing state plus abbreviated public identifiers. Keep QR rendering, stdin, OS commands, and filesystem access injected. Export the APIs with JSDoc and commit the red/green/refactor checkpoints before workspace integration.</action>
  <verify>
    <automated>deno test --no-lock packages/build-tools/src/terminal.test.ts packages/build-tools/src/secret-store.test.ts</automated>
  </verify>
  <done>Both pairing entry paths, cancellation, protected persistence, and redaction pass with platform-neutral fakes.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate npm and CLI-local Deno resolution under a dirty-state guard</name>
  <files>packages/cli/deno.json, pnpm-lock.yaml</files>
  <precondition>`git status --short` and explicit diffs prove `pnpm-lock.yaml` and `packages/cli/deno.json` have no unrelated overlapping edits; known root `package.json` and root `deno.lock` changes are classified as unowned, and the legacy `packages/cli/deno.lock` is byte-identical and outside the file set.</precondition>
  <action>Before any edit, record `git status --short`, `git diff -- package.json deno.lock pnpm-lock.yaml packages/cli/deno.json packages/cli/deno.lock`, and `shasum -a 256 package.json deno.lock packages/cli/deno.lock`, then classify the two known unowned root files and legacy CLI lock. Abort on an unrelated hunk in either owned path; do not claim or manufacture a clean tree. Confirm `pnpm-workspace.yaml` already includes `packages/*`, so root `package.json` needs no edit. Add a CLI-local relative source mapping for `@napplet/build-tools`; official Deno workspace semantics require `--no-lock` validation because lock configuration is workspace-only and only one resolver/lock may exist. Use `mktemp -d` for recoverable copies of the two owned generated/config files, run `pnpm install --lockfile-only --filter @napplet/build-tools`, then audit every generated hunk. Accept only the new build-tools workspace importer/dependency entries and CLI-local source mapping; restore from temporary copies and halt on broad reformatting, workspace-member import, root-file change, or unrelated dependency churn. Recheck root and legacy CLI lock hashes/diffs. Stage only `packages/cli/deno.json` and `pnpm-lock.yaml`, inspect `git diff --cached --name-only` and `git diff --cached`, and commit only after proving no unowned hunk is staged. Record the dirty-state evidence and commands in the plan summary.</action>
  <reversibility rating="reversible">All generated files are copied before regeneration and restored on unexpected output; root dirty files are never targeted.</reversibility>
  <verify>
    <automated>pnpm --filter @napplet/build-tools build &amp;&amp; pnpm --filter @napplet/build-tools type-check &amp;&amp; deno check --no-lock --config packages/cli/deno.json packages/build-tools/src/index.ts &amp;&amp; deno eval --no-lock --config packages/cli/deno.json 'import { pairBuildSigner } from "@napplet/build-tools"; if (typeof pairBuildSigner !== "function") throw new Error("missing build-tools mapping");' &amp;&amp; git diff --cached --name-only | node -e "let d='';process.stdin.on('data',c=&gt;d+=c);process.stdin.on('end',()=&gt;{const ok=new Set(['packages/cli/deno.json','pnpm-lock.yaml']);const bad=d.trim().split(/\n/).filter(Boolean).filter(x=&gt;!ok.has(x));if(bad.length){console.error(bad.join('\n'));process.exit(1)}})"</automated>
  </verify>
  <done>The package builds in pnpm and imports through CLI-local Deno config using `--no-lock`, generated diffs contain only expected owned hunks, and root `package.json`, root `deno.lock`, and legacy CLI `deno.lock` remain byte-identical, unstaged, and uncommitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terminal → pairing coordinator | QR values and pasted bunker URIs enter from an operator-visible channel. |
| Process → OS credential provider | Reusable signer material crosses into protected platform persistence. |
| Package metadata → generated locks | Dependency tooling can absorb unrelated dirty workspace state into generated files. |
| Git index → plan commit | Owned and unowned hunks coexist in one working tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-11 | Information Disclosure | nbunksec, bunker URI, client key | high | mitigate | Tasks 1–2 use protected stores/redacted values and snapshot every terminal, error, process, and serialization surface. |
| T-162-12 | Tampering | stored session | high | mitigate | Task 1 validates identity/relay material and writes only after verified pairing, preserving prior valid state on failure. |
| T-162-13 | Denial of Service | pairing race | medium | mitigate | Task 2 bounds timeouts and uses first-success abort cleanup for prompt and relay work. |
| T-162-14 | Tampering | generated lock and git index | high | mitigate | Task 3 snapshots owned files, audits minimal generated hunks, rechecks unowned hashes, stages explicit paths, and aborts on overlap. |
| T-162-SC | Tampering | package resolution | high | mitigate | Task 3 accepts only audited existing dependency identities and expected workspace resolution entries. |
</threat_model>

<verification>
- `deno test --no-lock packages/build-tools/src/secret-store.test.ts packages/build-tools/src/terminal.test.ts`
- `pnpm --filter @napplet/build-tools build && pnpm --filter @napplet/build-tools type-check`
- `deno check --no-lock --config packages/cli/deno.json packages/build-tools/src/index.ts`
- Inspect recorded before/after hashes and diffs proving root `package.json`, root `deno.lock`, and legacy CLI `deno.lock` were unchanged and unstaged.
- Inspect the explicit-path staged diff before the atomic workspace integration commit.
</verification>

<success_criteria>
- Pairing and protected persistence extend the isolated signer package with complete tests.
- Existing workspace globs and a CLI-local Deno mapping integrate the package without root metadata or Deno lock changes.
- Generated lock changes are minimal, recoverable, and audited.
- Known unrelated dirty root changes never enter a phase commit.
</success_criteria>

<output>
Create `.planning/phases/162-blossom-backed-large-asset-optimization/162-03-SUMMARY.md` when done.
</output>
