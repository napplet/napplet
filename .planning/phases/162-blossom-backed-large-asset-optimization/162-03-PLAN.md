---
phase: 162-blossom-backed-large-asset-optimization
plan: "03"
type: execute
wave: 3
depends_on:
  - 162-02
files_modified:
  - packages/cli/src/nostr-connect.ts
  - packages/cli/src/nostr-connect-pool.ts
  - packages/cli/tests/nostr_connect_test.ts
  - packages/cli/src/key-store.ts
  - packages/cli/src/deploy-signer.ts
  - packages/cli/tests/key_store_test.ts
  - packages/cli/tests/deploy_signer_test.ts
  - packages/cli/deno.json
  - packages/cli/deno.lock
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-05/D-08: the existing CLI preserves its QR/deep-link and pasted-bunker experience while delegating the pairing state machine to `@napplet/build-tools`."
    - "D-07: the CLI's current macOS Keychain, Linux Secret Service, Windows Credential Manager, and deliberate file-store fallback behavior remains intact through the shared SecretStore contract."
    - "Existing CLI NIP-46 and deploy-signer regression behavior stays green, including cancellation, timeout, stored-session reuse, noninteractive operation, and secret-redacted output."
    - "The extraction removes duplicate protocol logic instead of changing documented CLI commands or narrowing intended package behavior."
  artifacts:
    - path: packages/cli/src/nostr-connect.ts
      provides: "Deno terminal and QR adapters over shared NIP-46 pairing orchestration"
    - path: packages/cli/src/key-store.ts
      provides: "Existing OS credential implementations adapted to the shared SecretStore interface"
    - path: packages/cli/src/deploy-signer.ts
      provides: "Deploy signer selection and reconnect path backed by the shared BuildSigner contract"
    - path: packages/cli/tests/nostr_connect_test.ts
      provides: "CLI-facing compatibility and cleanup regression coverage after extraction"
  key_links:
    - from: packages/cli/src/nostr-connect.ts
      to: packages/build-tools/src/terminal.ts
      via: "CLI supplies Deno prompt/QR/relay adapters to pairBuildSigner"
      pattern: "pairBuildSigner"
    - from: packages/cli/src/key-store.ts
      to: packages/build-tools/src/secret-store.ts
      via: "platform credential providers satisfy SecretStore without exposing raw values"
      pattern: "SecretStore"
    - from: packages/cli/src/deploy-signer.ts
      to: packages/build-tools/src/signer.ts
      via: "deployment signs through BuildSigner and reconnectBuildSigner"
      pattern: "BuildSigner"
---

<objective>
Move the CLI onto the shared build-signer contracts without changing its user-visible pairing, protected persistence, or deploy behavior per D-05, D-07, and D-08.

Purpose: Prove the extraction is compatible with the established Deno CLI and leave one authoritative NIP-46 pairing implementation for the later Vite adapter.
Output: Thin CLI platform adapters, shared signer wiring, and the complete existing CLI regression suite running against them.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-CONTEXT.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-PATTERNS.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-02-SUMMARY.md
@packages/cli/src/nostr-connect.ts
@packages/cli/src/nostr-connect-pool.ts
@packages/cli/src/key-store.ts
@packages/cli/src/deploy-signer.ts
@packages/cli/tests/nostr_connect_test.ts
@packages/cli/tests/key_store_test.ts
@packages/cli/tests/deploy_signer_test.ts
</context>

<interfaces>
Consume from `@napplet/build-tools`:
- `pairBuildSigner(options): Promise<BuildSignerSession>`
- `reconnectBuildSigner(options): Promise<BuildSignerSession | undefined>`
- `BuildSigner`, `SecretStore`, `TerminalAdapter`, and relay/clock adapter types

Preserve CLI-facing behavior and exported call sites from:
- `connectRemoteSigner(...)` in `packages/cli/src/nostr-connect.ts`
- `KeyStoreProvider` and `createKeyStore(...)` in `packages/cli/src/key-store.ts`
- deploy-signer selection in `packages/cli/src/deploy-signer.ts`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace CLI pairing internals with shared signer adapters</name>
  <files>packages/cli/src/nostr-connect.ts, packages/cli/src/nostr-connect-pool.ts, packages/cli/tests/nostr_connect_test.ts</files>
  <behavior>
    - Test 1 (D-05): existing QR output, nostrconnect deep-link text, and pasted bunker input all remain available and feed the shared first-success pairing coordinator.
    - Test 2: Deno relay-pool subscription, prompt, and QR resources close when pairing succeeds, is cancelled, times out, or rejects.
    - Test 3: public CLI status remains stable while nbunksec, client secret, raw bunker URI, and kind-24133 payloads never appear in output or error snapshots.
  </behavior>
  <action>Lock the current CLI user-facing behavior with failing compatibility tests, then turn `nostr-connect.ts` and `nostr-connect-pool.ts` into Deno adapters for the shared terminal, relay, and clock contracts per D-05/D-08. Preserve the existing QR rendering and pasted-input prompts, output wording where tests establish it as public behavior, noninteractive injected paths, and abort cleanup. Delete the duplicated connection/request state machine once all call sites use `pairBuildSigner` or `reconnectBuildSigner`; retain only Deno-specific relay and terminal glue. Treat any mismatch between the shared package and published NIP-46 as a canonical gap to flag, not an excuse for a CLI-only message or permission.</action>
  <verify>
    <automated>deno test --allow-env --allow-read packages/cli/tests/nostr_connect_test.ts</automated>
  </verify>
  <done>The CLI pairing experience is unchanged, all lifecycle and redaction regressions pass, and no second NIP-46 state machine remains in the CLI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adapt protected key stores and deploy signing to BuildSigner</name>
  <files>packages/cli/src/key-store.ts, packages/cli/src/deploy-signer.ts, packages/cli/tests/key_store_test.ts, packages/cli/tests/deploy_signer_test.ts, packages/cli/deno.json</files>
  <behavior>
    - Test 1 (D-07): macOS, Linux, and Windows provider selection plus the deliberate file-store fallback satisfy SecretStore get/set/delete semantics and preserve existing key names and migration behavior.
    - Test 2: stored-session reconnect is attempted before pairing, an invalid stored session is handled without printing it, and the protected value is replaced only after a successful pair.
    - Test 3 (D-06): deploy signing passes only kind 24242 templates to BuildSigner and rejects an altered event, wrong signer, or wrong kind before returning an authorization header.
    - Test 4: existing local-key and no-signing deploy choices continue to behave exactly as before.
  </behavior>
  <action>Adapt the existing `KeyStoreProvider` entry point to instantiate the shared platform `SecretStore` behavior without weakening macOS Keychain, Linux Secret Service, Windows Credential Manager, or the deliberate file-store fallback. Wire deploy-signer selection to `BuildSigner`, keep existing local-key and unsigned branches intact, and make remote signing validate the returned kind-24242 event before it is used. Update the Deno import map/workspace reference to consume `@napplet/build-tools` from the workspace. Remove now-unused duplicate key-store and signer helpers only after both targeted suites and all callers pass. Commit the key-store adapter and deploy-signer migration at separate green checkpoints.</action>
  <reversibility rating="reversible">The CLI preserves its entry points and platform providers, so the adapter layer can be reverted without migrating stored user data.</reversibility>
  <verify>
    <automated>deno test --allow-env --allow-read --allow-write --allow-run packages/cli/tests/key_store_test.ts packages/cli/tests/deploy_signer_test.ts</automated>
  </verify>
  <done>Protected stored-session reuse, least-authority deploy signing, local signing, and unsigned flows all pass their existing plus new regression vectors through the shared interfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the full Deno CLI remains compatible after extraction</name>
  <files>packages/cli/deno.lock</files>
  <action>Refresh only the CLI lock resolution required by the workspace import and run the existing CLI regression suite, including discovery, suggestions, deploy-network, deploy-plan, manifest, resolver-free, output, and CLI command tests. Investigate any failure as a compatibility defect in the extraction; do not remove an intended export, command path, or protocol-faithful vector to make the suite green. Run a secret scan over CLI source, test output, build output, Deno cache-facing configuration, and the lock diff, then commit the compatibility proof separately.</action>
  <verify>
    <automated>deno test --allow-all packages/cli/tests</automated>
  </verify>
  <done>The complete existing Deno CLI regression suite passes against the shared build-tools package, lock resolution is intentional, and the secret scan finds no signer material.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI terminal → shared coordinator | Untrusted pasted input and operator-visible QR/status values cross into shared pairing logic. |
| Deno relay pool → BuildSigner | Relay messages and lifecycle events cross from a runtime-specific transport. |
| CLI process → OS credential provider | Reusable signer material crosses into platform-protected persistence. |
| BuildSigner → deploy authorization | A signed kind-24242 event becomes authority for an HTTP request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-12 | Information Disclosure | CLI output, errors, and Deno config/cache | high | mitigate | Tasks 1–3 preserve redaction tests and scan source, output, config, build artifacts, and lock changes for sensitive signer material. |
| T-162-13 | Elevation of Privilege | deploy-signer remote path | high | mitigate | Task 2 sends only kind 24242 templates and revalidates kind, signature, and signer identity before authorization use. |
| T-162-14 | Spoofing | Deno relay-pool adapter | high | mitigate | Task 1 delegates correlated verification to BuildSigner and proves every pool/subscription is aborted on completion or failure. |
| T-162-15 | Tampering | OS secret-store migration | high | mitigate | Task 2 preserves existing provider keys, selection, and fallback semantics and mutates storage only after verified pairing. |
| T-162-16 | Repudiation | CLI compatibility migration | medium | mitigate | Task 3 runs the complete pre-existing Deno suite and records atomic commits for pairing, persistence, and lock changes. |
</threat_model>

<verification>
- `deno test --allow-env --allow-read packages/cli/tests/nostr_connect_test.ts`
- `deno test --allow-env --allow-read --allow-write --allow-run packages/cli/tests/key_store_test.ts packages/cli/tests/deploy_signer_test.ts`
- `deno test --allow-all packages/cli/tests`
- Run the repository secret scan over `packages/cli`, generated output, config, and lock diffs.
</verification>

<success_criteria>
- CLI pairing, stored-session reuse, and deploy signing now consume shared contracts.
- The complete Deno regression suite remains green with no narrowed commands or exports.
- Secret material remains confined to memory and the existing protected store.
- No CLI-private NIP-46 behavior is retained or introduced.
</success_criteria>

<output>
Create `.planning/phases/162-blossom-backed-large-asset-optimization/162-03-SUMMARY.md` when done.
</output>
