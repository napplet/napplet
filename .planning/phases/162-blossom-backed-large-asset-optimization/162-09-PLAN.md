---
phase: 162-blossom-backed-large-asset-optimization
plan: "09"
type: execute
wave: 6
depends_on:
  - 162-08
files_modified:
  - packages/vite-plugin/src/types.ts
  - packages/vite-plugin/src/index.ts
  - packages/vite-plugin/src/html.ts
  - packages/vite-plugin/src/optimizer/pipeline.ts
  - packages/vite-plugin/src/optimizer/pipeline.test.ts
  - packages/vite-plugin/src/requirements.ts
  - packages/vite-plugin/src/manifest.ts
  - packages/vite-plugin/src/index.test.ts
  - packages/vite-plugin/src/optimizer/security.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-01 through D-15/D-26: `largeAssetOptimization?: 'auto' | false` invokes live services only after exact over-target measurement and preserves a valid inline build on every unavailable, incompatible, or failed external path."
    - "D-16 through D-21: committed offloads use only canonical private resource mapping/loading, and selected files are removed only after verified upload, rewrite, runtime integrity, and atomic commit."
    - "The existing NIP-5A `['requires','resource']` tag appears exactly once when `committedResourceCount > 0` and is absent for disabled, at-target, ineligible, exhausted, unavailable, upload-failed, and rolled-back outcomes."
    - "Private mapping fields never become manifest tags; the aggregate hash is calculated only after the final committed artifact and its effective resource requirement are established."
    - "Build-tool SSRF, signing-scope, secret, transaction, and callback hardening remains explicitly non-normative and test-backed."
  artifacts:
    - path: packages/vite-plugin/src/index.ts
      provides: "Automatic live optimization orchestration and safe report propagation"
    - path: packages/vite-plugin/src/requirements.ts
      provides: "Deterministic effective manifest requirement merge including conditional resource capability"
    - path: packages/vite-plugin/src/manifest.ts
      provides: "Final manifest/tag emission and aggregate hashing after committed artifact verification"
    - path: packages/vite-plugin/src/optimizer/security.test.ts
      provides: "High-severity live-path, conditional-requirement, rollback, and leakage regression matrix"
  key_links:
    - from: packages/vite-plugin/src/index.ts
      to: packages/vite-plugin/src/optimizer/pipeline.ts
      via: "closeBundle invokes the live path only after actual over-target retained-artifact measurement"
      pattern: "largeAssetOptimization"
    - from: packages/vite-plugin/src/optimizer/pipeline.ts
      to: packages/vite-plugin/src/requirements.ts
      via: "committedResourceCount controls the effective resource requirement and no pre-commit candidate count can add it"
      pattern: "committedResourceCount"
    - from: packages/vite-plugin/src/requirements.ts
      to: packages/vite-plugin/src/manifest.ts
      via: "effective requirements are deduplicated before final manifest serialization and hashing"
      pattern: "requires"
    - from: packages/vite-plugin/src/manifest.ts
      to: final index.html
      via: "aggregate hash calculation runs after conditional tags and optimized artifact commit"
      pattern: "aggregate"
---

<objective>
Wire live Node services into automatic Vite optimization and seal the artifact with an exact conditional resource requirement and post-commit aggregate hash per D-01 through D-21 and D-26.

Purpose: Complete production orchestration while proving the manifest advertises the existing resource capability exactly when the signed artifact actually depends on it.
Output: Public option, live pipeline, conditional requirement merge, atomic final manifest, and complete security regressions in a nine-file plan.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-CONTEXT.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-RESEARCH.md
@.planning/phases/162-blossom-backed-large-asset-optimization/COVERAGE.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-07-SUMMARY.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-08-SUMMARY.md
@packages/vite-plugin/src/index.ts
@packages/vite-plugin/src/types.ts
@packages/vite-plugin/src/html.ts
@packages/vite-plugin/src/requirements.ts
@packages/vite-plugin/src/manifest.ts
</context>

<interfaces>
- `largeAssetOptimization?: 'auto' | false` on `Nip5aManifestOptions`, default `'auto'`, with no threshold override.
- `OptimizationReport.committedResourceCount` is zero until a selected-resource transaction commits and equals the exact private-table resource count after commit.
- `effectiveRequirements(configured: string[], committedResourceCount: number): string[]` preserves configured requirements, deduplicates deterministically, and includes `resource` iff the committed count is positive.
- Manifest serialization uses the existing `['requires', '<capability>']` tag form; this plan adds no new tag schema.
- Mapping source identity, URI, hash, bytes, MIME, and loader metadata stay private JavaScript inside signed `/index.html`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the lazy live services into automatic optimization</name>
  <files>packages/vite-plugin/src/types.ts, packages/vite-plugin/src/index.ts, packages/vite-plugin/src/html.ts, packages/vite-plugin/src/optimizer/pipeline.ts, packages/vite-plugin/src/optimizer/pipeline.test.ts</files>
  <behavior>
    - Test 1 (D-01/D-26): disabled and at-or-below target builds perform no terminal, credential, relay, DNS, or HTTP work; an over-target eligible build enters the injected live path.
    - Test 2 (D-05 through D-15): stored/pair signer → verified kind-10002 → write/unmarked relays → ordered verified kind-10063 → exact authorized upload → matching descriptors succeeds end to end.
    - Test 3 (D-04/D-13): ineligible/exhausted, no TTY/signer/list, unsafe server, timeout, descriptor mismatch, or partial upload yields a visible nonfatal report, zero committed resources, and the original artifact.
    - Test 4: existing `experimental.renderBuiltUrl` behavior is safely composed or produces a visible preserve-inline result and is never silently replaced.
  </behavior>
  <action>Finalize the `'auto' | false` public option per D-26 with exact fixed threshold behavior. Preserve Vite boundaries and controlled generated references; detect and test an existing `experimental.renderBuiltUrl` callback. After actual over-target measurement, lazily create the Plan 08 Node services, reuse/pair the signer, run the corrected two-stage discovery, and upload deterministic selected assets. Feed only verified descriptors into the private rewrite/transaction. Return `committedResourceCount = 0` for every no-op/fallback/failure and the exact committed table size only after successful transaction. Preserve the inline artifact instead of failing on unavailable external prerequisites. Commit option/config and live orchestration at separate green checkpoints.</action>
  <verify>
    <automated>pnpm --filter @napplet/vite-plugin test:unit -- optimizer/pipeline.test.ts optimizer/node-services.test.ts</automated>
  </verify>
  <done>External services are lazy, the complete injected live path succeeds, every unsafe/unavailable/failed path preserves inline output, and committedResourceCount accurately distinguishes effective resource dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit resource requirement iff offloads commit, then hash the final artifact</name>
  <files>packages/vite-plugin/src/requirements.ts, packages/vite-plugin/src/manifest.ts, packages/vite-plugin/src/index.test.ts, packages/vite-plugin/src/optimizer/security.test.ts</files>
  <behavior>
    - Test 1: one or more committed offloads produce exactly one `['requires','resource']` tag while preserving and deduplicating existing configured requirement tags.
    - Test 2: disabled, at-target, unsupported/ineligible, target-exhausted, no-signer, no-server-list, unsafe-server, upload-failed, descriptor-mismatched, and rolled-back builds contain no effective resource requirement.
    - Test 3 (D-16/D-17): source identity, URI, hash, bytes, MIME, loader keys, and all private mapping data remain inside signed artifact code and never appear as manifest tags or fields.
    - Test 4 (D-18): failure between upload, rewrite, temp write, atomic replacement, resource verification, selected-file removal, requirement merge, and manifest write restores originals and leaves no resource tag or partial manifest.
    - Test 5: the final NIP-5A aggregate hash verifies against the committed optimized `/index.html` after the effective requirement is established; no pre-commit path can calculate/persist the final hash.
    - Test 6: the complete high-severity matrix covers secret disclosure, overbroad signing, forged/stale discovery, SSRF/rebinding, descriptor substitution, resource substitution/exhaustion, destructive commit failure, and callback conflict.
  </behavior>
  <action>Write the conditional requirement regressions first. Extend the existing requirement merge helper, not the protocol schema: add `resource` exactly when the pipeline reports `committedResourceCount > 0`, preserve existing configured requirements, and deduplicate deterministic tag output. Exercise every no-op/fallback/failure outcome with an absent resource tag. Keep private mapping/loader metadata wholly outside manifest tags. Order the transaction so exact upload, final rewrite, private loader/hash verification, atomic file replacement, selected-file removal, and effective requirement merge all succeed before `writeBundleManifest` persists the aggregate hash. On every injected failure, restore the final inline artifact, omit the resource tag, and persist the aggregate hash only after restoration is verified. Keep uploaded remote blobs as safely reported additive evidence because BUD-12 delete authority is explicitly out of scope. Add every ASVS L1 high-severity regression and secret-output scan fixture, then commit requirement behavior and transaction/security proof separately.</action>
  <reversibility rating="costly">Local artifacts remain recoverable until verification; successful remote uploads are additive and intentionally not deleted.</reversibility>
  <verify>
    <automated>pnpm --filter @napplet/vite-plugin test:unit -- index.test.ts optimizer/security.test.ts optimizer/pipeline.test.ts</automated>
  </verify>
  <done>The resource requirement is present exactly for committed offloads, absent for every other outcome, private metadata never enters tags, the final aggregate hash verifies post-commit output, and every high-severity regression passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite config/output → optimizer | User callbacks and retained bytes enter controlled eligibility, rendering, and live orchestration. |
| Shared services → filesystem transaction | Verified external evidence authorizes private rewrite and eventual local deletion. |
| Pipeline result → manifest requirements | A candidate or failed attempt must never advertise an effective resource dependency. |
| Final artifact → aggregate hash | Manifest/hash evidence must describe exactly the committed bytes and tags. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-42 | Information Disclosure | live reports/artifact/manifest | high | mitigate | Task 2 scans logs, errors, mapping, tags, manifest, temp output, and cache/config-facing artifacts for signer/auth secrets. |
| T-162-43 | Elevation of Privilege | live signing/network path | high | mitigate | Task 1 uses least-authority signer and shared verified discovery/SSRF policy; Task 2 exercises full attack regressions. |
| T-162-44 | Tampering | descriptor/mapping/runtime Blob | high | mitigate | Tasks 1–2 bind exact bytes, descriptor, private metadata, canonical URI, and recovered Blob to one digest/length. |
| T-162-45 | Tampering | resource requirement and aggregate hash | high | mitigate | Task 2 derives the tag only from committedResourceCount and writes the aggregate hash only after final requirement/artifact commit. |
| T-162-46 | Tampering | destructive filesystem transaction | high | mitigate | Task 2 preserves originals, atomically replaces verified output, removes selected files late, and restores on every failure. |
| T-162-47 | Denial of Service | automatic network/resource path | high | mitigate | Task 1 triggers lazily and uses shared bounds; all exhaustion/unavailability is nonfatal and resource count stays zero. |
| T-162-48 | Tampering | existing Vite callback | medium | mitigate | Task 1 proves safe composition or visible inline preservation and never silently overwrites the callback. |
</threat_model>

<verification>
- `pnpm --filter @napplet/vite-plugin test:unit -- optimizer/pipeline.test.ts optimizer/node-services.test.ts index.test.ts optimizer/security.test.ts`
- `pnpm --filter @napplet/vite-plugin build && pnpm --filter @napplet/vite-plugin type-check`
- Inspect generated manifest fixtures for exact conditional `['requires','resource']`, private-field absence, and post-commit aggregate hash verification.
- Run the focused secret scan over source, logs, reports, fixtures, output, manifest, and temporary files.
</verification>

<success_criteria>
- The plan changes nine files and reaches separate orchestration and manifest/security green commits.
- Live external behavior is lazy and every non-success preserves a valid inline artifact.
- `['requires','resource']` is present exactly when at least one resource is committed and absent otherwise.
- Private mapping remains non-protocol artifact plumbing and final aggregate hashing occurs last.
</success_criteria>

<output>
Create `.planning/phases/162-blossom-backed-large-asset-optimization/162-09-SUMMARY.md` when done.
</output>
