---
phase: 162-blossom-backed-large-asset-optimization
plan: "11"
type: execute
wave: 8
depends_on:
  - 162-10
files_modified:
  - scripts/check-build-secret-leaks.mjs
  - scripts/check-build-secret-leaks.test.mjs
  - .changeset/tidy-blossom-assets.md
  - .planning/phases/162-blossom-backed-large-asset-optimization/162-DEMO.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-23/D-25: durable evidence records generated 50 MiB+ sizes, deterministic selection, signed discovery/upload, descriptors, effective resource requirement, aggregate hash, and byte-identical recovery."
    - "D-24/D-25: changesets describe every changed shipped package and all targeted/full Node/Deno/docs/release/secret/slop gates pass before outward actions."
    - "Secrets are absent from artifacts, logs, reports, config/cache-facing diffs, test output, commit content, and PR body."
    - "Known unrelated root `package.json`, root `deno.lock`, caches, generated CLI binaries, and workshop files remain unmodified by this plan, unstaged, and absent from commits/PR."
    - "The branch is pushed and the PR is created or updated only after Plans 01–10 provide durable green commits and this plan's full gate evidence is recorded."
  artifacts:
    - path: scripts/check-build-secret-leaks.mjs
      provides: "Focused scanner for actual signer, authorization, private-key, and credential-shaped values in outward-facing artifacts"
    - path: .changeset/tidy-blossom-assets.md
      provides: "0.x release intent for Vite optimization, shared build tools, and CLI behavior correction"
    - path: .planning/phases/162-blossom-backed-large-asset-optimization/162-DEMO.md
      provides: "Exact final commands, versions, sizes, hashes, upload/recovery/security evidence, gate results, and PR URL"
  key_links:
    - from: scripts/check-build-secret-leaks.mjs
      to: generated output/log/report/config paths
      via: "the final gate scans outward-facing values while permitting non-secret source identifiers"
      pattern: "check-build-secret-leaks"
    - from: .planning/phases/162-blossom-backed-large-asset-optimization/162-DEMO.md
      to: packages/vite-plugin/src/optimizer/large-fixture.test.ts
      via: "recorded measurements and hashes come from the reproducible generated demonstration"
      pattern: "50 MiB"
---

<objective>
Scan outward-facing artifacts, record final evidence, pass every repository/release gate, version shipped packages, and open the PR per D-23 through D-25.

Purpose: Ensure outward actions happen only after durable demonstration/docs commits and cannot absorb secrets or unrelated dirty worktree state.
Output: Tested secret scanner, changeset, final evidence record, green full gates, explicit-path commits, pushed branch, and open PR.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-CONTEXT.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-VALIDATION.md
@.planning/phases/162-blossom-backed-large-asset-optimization/COVERAGE.md
@.planning/phases/162-blossom-backed-large-asset-optimization/162-10-SUMMARY.md
@.github/workflows/ai-slop.yml
@.aislop/config.yml
</context>

<interfaces>
Final evidence must record:
- exact tool/runtime versions and commands;
- generated candidate bytes, would-be inline bytes, deterministic selected order, final HTML bytes, removed/preserved assets;
- verified kind-10002/kind-10063 IDs, normalized server origins, upload descriptor hashes/sizes, conditional `['requires','resource']`, aggregate hash, and recovered byte hashes;
- explicit single-Blob/whole-Blob limitation and all nonfatal outcomes;
- targeted/full/secret/docs/JSR/release/AI-slop results and PR URL.

Dirty-state contract:
- Before either task, classify `git status`, unstaged/staged diffs, and known unowned root/generated paths.
- Stage only named files by explicit path and inspect staged names/content before each commit.
- Push commits only; untracked/unstaged unrelated files are never included.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a focused outward-artifact secret scanner</name>
  <files>scripts/check-build-secret-leaks.mjs, scripts/check-build-secret-leaks.test.mjs</files>
  <behavior>
    - Test 1: actual nbunksec/bunker encodings, Nostr private keys, Blossom Authorization headers, credential-bearing URLs, and serialized redacted-secret backing values are detected.
    - Test 2: documented source identifiers, canonical examples with unmistakably invalid values, hashes, public keys, and safe redacted sentinel values do not produce false positives.
    - Test 3: generated HTML/manifest/temp files, captured logs/reports/test output, config/cache-facing diffs, evidence markdown, staged diff, and PR-body input are all scannable with bounded file/byte handling.
    - Test 4: unreadable, oversized, binary, or traversal targets fail closed with safe path-only diagnostics and never echo suspected secret content.
  </behavior>
  <action>Write synthetic positive/negative tests first, then implement a bounded scanner for actual secret-shaped values in generated and outward-facing paths. Keep diagnostics to safe path/rule/count data; never echo the matching value. Permit protocol/source identifiers without permitting valid-looking secret encodings. Make target roots explicit, reject traversal, cap file/aggregate bytes, and cover staged diff/PR-body inputs. Run it against current generated fixture output and captured logs before the atomic scanner commit; stage only the two script paths and inspect the staged diff.</action>
  <reversibility rating="reversible">The scanner is additive tooling with isolated tests and no modification of build or protocol behavior.</reversibility>
  <verify>
    <automated>node --test scripts/check-build-secret-leaks.test.mjs &amp;&amp; node scripts/check-build-secret-leaks.mjs</automated>
  </verify>
  <done>Synthetic and real outward-artifact scans pass, unsafe inputs fail closed without echoing matches, and the scanner commit contains only its two files.</done>
</task>

<task type="auto">
  <name>Task 2: Record evidence, pass full gates, version packages, and then open the PR</name>
  <files>.changeset/tidy-blossom-assets.md, .planning/phases/162-blossom-backed-large-asset-optimization/162-DEMO.md</files>
  <precondition>Plans 01–10 have durable green commits; current status/diffs classify all remaining dirty paths and show no unrelated overlap in the changeset or DEMO.md.</precondition>
  <action>Add one changeset covering `@napplet/vite-plugin`, `@napplet/build-tools`, and `@napplet/cli` with semver appropriate to each 0.x shipped impact. Run the generated demonstration, targeted Vite/build-tools/CLI suites, complete existing Deno regressions, full build/type/unit gates, docs/link/JSR/release-tooling checks, the focused secret scanner, `git diff --check`, and pinned AI-slop 0.12.0 changed-file scan; restore touched code to 100/100 without changing `.aislop/config.yml`. Record exact commands/versions/results plus all evidence listed in `<interfaces>` in `162-DEMO.md`. Re-run status and explicit diffs for known unrelated root `package.json`, root `deno.lock`, caches, CLI binaries, and workshop files; prove they remain unstaged and absent from the commit. Stage only the changeset and DEMO.md, scan and inspect the staged diff, then make the final Conventional Commit with agent co-author trailer. Only after that green commit, push the current feature branch and create or update the GitHub PR with canonical boundaries, threat mitigations, verification results, changesets, out-of-scope operations, and no secret-bearing content. Record the PR URL without amending or adding unrelated work.</action>
  <reversibility rating="costly">Changeset/evidence commits can be reverted before merge; push and PR creation are authorized reviewable outward actions and do not publish or merge packages.</reversibility>
  <verify>
    <automated>node --test scripts/check-build-secret-leaks.test.mjs &amp;&amp; node scripts/check-build-secret-leaks.mjs &amp;&amp; deno check --config packages/cli/deno.json packages/build-tools/src/index.ts &amp;&amp; deno test --allow-all packages/cli/tests &amp;&amp; pnpm build &amp;&amp; pnpm type-check &amp;&amp; pnpm -r test:unit &amp;&amp; pnpm check:links &amp;&amp; pnpm check:jsr &amp;&amp; pnpm test:release-tooling &amp;&amp; npx --yes aislop@0.12.0 scan --changes --base origin/main &amp;&amp; git diff --check</automated>
  </verify>
  <done>Full evidence and changesets are committed after every gate passes, unrelated dirty paths remain unstaged/uncommitted, the branch is pushed, and an open PR contains the safe complete verification record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated output/log/config → scanner | High-value signer and authorization material could persist in outward-facing files. |
| Dirty worktree → git index | Owned release evidence coexists with unrelated root/generated changes. |
| Local commits → remote branch/PR | Committed evidence and PR prose become externally visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-53 | Information Disclosure | artifacts/logs/evidence/PR | high | mitigate | Tasks 1–2 use a tested non-echoing scanner before commits and again before outward actions. |
| T-162-54 | Tampering | dirty worktree staging | high | mitigate | Task 2 classifies diffs, stages explicit paths, audits staged names/content, and proves known unrelated paths remain unstaged. |
| T-162-55 | Repudiation | release evidence | medium | mitigate | Task 2 records exact commands, versions, results, hashes, commit, and PR URL in durable evidence. |
| T-162-56 | Denial of Service | secret/full-repo scans | medium | mitigate | Task 1 bounds paths/files/bytes and Task 2 uses targeted gates before the intentional full suite. |
| T-162-SC | Tampering | released dependency graph | high | mitigate | Task 2 runs package legitimacy, lock, build, JSR, release-tooling, changeset, and pinned AI-slop gates before push. |
</threat_model>

<verification>
- `node --test scripts/check-build-secret-leaks.test.mjs && node scripts/check-build-secret-leaks.mjs`
- `deno test --allow-all packages/cli/tests`
- `pnpm build && pnpm type-check && pnpm -r test:unit`
- `pnpm check:links && pnpm check:jsr && pnpm test:release-tooling`
- `npx --yes aislop@0.12.0 scan --changes --base origin/main` at 100/100 for touched code.
- Inspect explicit-path staged diffs, final status, pushed commit set, and PR URL.
</verification>

<success_criteria>
- Secret scanning and release evidence are isolated from demonstration/docs work.
- Every full gate passes and is recorded before push/PR.
- Changesets cover all changed shipped packages.
- Unrelated dirty files remain untouched, unstaged, and outside commits/PR.
- The pushed branch has an open reviewable PR with complete safe evidence.
</success_criteria>

<output>
Create `.planning/phases/162-blossom-backed-large-asset-optimization/162-11-SUMMARY.md` when done.
</output>
