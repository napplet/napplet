---
phase: 33-polish-demo-ui-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/demo/src/topology.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "All topology edges render with 90-degree rectilinear paths instead of curves"
    - "Edge routing is orthogonal even as nodes move or inspector resizes"
    - "Leader Line curve option is configured consistently across all edges"
  artifacts:
    - path: "apps/demo/src/topology.ts"
      provides: "Leader Line BASE_OPTIONS with curve: 0 configuration"
      must_contain: "curve: 0"
    - path: "apps/demo/src/topology.ts"
      provides: "Edge initialization code using BASE_OPTIONS"
      must_contain: "new LeaderLine.*BASE_OPTIONS"
  key_links:
    - from: "BASE_OPTIONS constant"
      to: "LeaderLine edge creation"
      via: "spread operator in edge constructor"
      pattern: "const BASE_OPTIONS.*curve.*new LeaderLine.*BASE_OPTIONS"
---

<objective>
Configure Leader Line topology edges to render with orthogonal 90-degree rectilinear routing instead of Bezier curves.

Purpose: Current edges render with smooth curves that make message flow direction ambiguous. Orthogonal routing (90-degree paths) makes the topology more readable and visually consistent with standard node graph UIs.

Output: Updated BASE_OPTIONS constant in topology.ts with `curve: 0` setting, forcing all Leader Line edges to render as rectilinear paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-polish-demo-ui-layout/33-CONTEXT.md
@.planning/phases/33-polish-demo-ui-layout/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add curve: 0 to Leader Line BASE_OPTIONS</name>
  <read_first>
    - apps/demo/src/topology.ts (to locate BASE_OPTIONS constant and understand current configuration)
    - Lines around 204-209 where BASE_OPTIONS is defined
    - All places where BASE_OPTIONS is used in LeaderLine constructor calls
  </read_first>
  <action>
Update the BASE_OPTIONS constant in apps/demo/src/topology.ts to include `curve: 0`:

Current state (approximately lines 204-209):
```typescript
const BASE_OPTIONS = {
  color: COLOR_RESTING,
  size: 2,
  endPlug: 'arrow2',
  endPlugSize: 1.5,
};
```

New state (add `curve: 0` after size property):
```typescript
const BASE_OPTIONS = {
  color: COLOR_RESTING,
  size: 2,
  curve: 0,                      // Force orthogonal 90-degree routing
  endPlug: 'arrow2',
  endPlugSize: 1.5,
};
```

This change:
- Per D-03 (user decision): Leader Line edges will be configured with `curve: 0` to force orthogonal rectilinear routing instead of curves
- Per D-05 (user decision): Path resolution uses Leader Line's built-in orthogonal routing (no custom algorithm needed)
- Applies to ALL edges created with `...BASE_OPTIONS` spread in LeaderLine constructors (both forward and reverse edge pairs)
- curve: 0 is a native Leader Line feature; no additional dependencies needed

Implementation notes:
- The curve option takes numeric values: 1 (default, Bezier curves) or 0 (orthogonal/rectilinear)
- Add the property after the `size: 2` line to keep properties grouped logically (rendering options together)
- No conflicts expected with other BASE_OPTIONS properties (color, endPlug, endPlugSize)
  </action>
  <verify>
File apps/demo/src/topology.ts contains BASE_OPTIONS constant with `curve: 0` property (grep confirms pattern `const BASE_OPTIONS.*curve: 0`)
  </verify>
  <done>
BASE_OPTIONS updated with `curve: 0` configuration. All Leader Line edges created via BASE_OPTIONS will render as orthogonal 90-degree rectilinear paths instead of curves
  </done>
</task>

</tasks>

<verification>
After executing this plan:
1. Open apps/demo/src/topology.ts and confirm BASE_OPTIONS includes `curve: 0`
2. Run TypeScript type-check: `pnpm type-check` (verify no type errors)
3. Run `pnpm dev` from apps/demo directory to start Vite dev server
4. Manually verify in browser: All topology edges render as straight 90-degree orthogonal paths
5. Test edge repositioning: Resize the inspector pane or move nodes — edges should update their orthogonal paths smoothly
</verification>

<success_criteria>
- [ ] BASE_OPTIONS constant in apps/demo/src/topology.ts contains `curve: 0` property
- [ ] TypeScript compilation succeeds (`pnpm type-check` exits 0)
- [ ] Vite dev server builds without errors
- [ ] Visual verification: All edges in the topology view render as orthogonal 90-degree paths (not curves)
</success_criteria>

<output>
After completion, create `.planning/phases/33-polish-demo-ui-layout/33-02-SUMMARY.md`
</output>
