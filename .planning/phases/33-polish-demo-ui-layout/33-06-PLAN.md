---
phase: 33-polish-demo-ui-layout
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/demo/src/topology.ts
autonomous: true
gap_closure: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "All topology edges render as orthogonal 90-degree rectilinear lines"
    - "No curved or diagonal edges appear between nodes"
    - "Edges follow cardinal directions (horizontal then vertical, or vice versa)"
  artifacts:
    - path: "apps/demo/src/topology.ts"
      provides: "BASE_OPTIONS with correct path property for orthogonal routing"
      must_contain: "path: 'grid'"
      must_not_contain: "curve: 0"
  key_links:
    - from: "BASE_OPTIONS"
      to: "LeaderLine instances"
      via: "Spread operator passes path: 'grid' to all edges"
      pattern: "BASE_OPTIONS.*path.*grid"
---

<objective>
Replace invalid `curve: 0` property with correct `path: 'grid'` in BASE_OPTIONS to enable 90-degree orthogonal edge routing.

Purpose: Leader Line's orthogonal routing requires the `path: 'grid'` property. The current code uses `curve: 0`, which is invalid and has no effect, causing edges to render as curves/diagonals instead of rectilinear paths.

Output: BASE_OPTIONS updated so all topology edges render with orthogonal 90-degree routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-polish-demo-ui-layout/33-CONTEXT.md
@.planning/phases/33-polish-demo-ui-layout/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace curve: 0 with path: 'grid' in BASE_OPTIONS</name>
  <files>apps/demo/src/topology.ts</files>
  <action>
In apps/demo/src/topology.ts, locate the initTopologyEdges function (around line 190) and the BASE_OPTIONS object (around line 194).

Current code (INCORRECT):
```typescript
const BASE_OPTIONS = {
  color: COLOR_RESTING,
  size: 2,
  curve: 0,              // ← WRONG: Invalid property, has no effect
  endPlug: 'arrow2',
  endPlugSize: 1.5,
};
```

Fix: Replace `curve: 0` with `path: 'grid'`

Expected result (CORRECT):
```typescript
const BASE_OPTIONS = {
  color: COLOR_RESTING,
  size: 2,
  path: 'grid',          // ← CORRECT: Enables orthogonal 90-degree routing
  endPlug: 'arrow2',
  endPlugSize: 1.5,
};
```

Implementation:
- Delete the line `curve: 0,`
- Add the line `path: 'grid',` in the same position or immediately after `size: 2,`
- Preserve all other properties in BASE_OPTIONS
- No changes to the LeaderLine instantiation calls (lines 208-220)
  </action>
  <verify>
- Run: `grep -n "path: 'grid'" apps/demo/src/topology.ts` — must return 1 match at BASE_OPTIONS line
- Run: `grep -n "curve: 0" apps/demo/src/topology.ts` — must return 0 matches (property removed)
- Run: `pnpm type-check` — must pass with no TypeScript errors
- Run: `pnpm dev` from apps/demo directory to build and start Vite dev server
  </verify>
  <done>
BASE_OPTIONS now contains `path: 'grid'` instead of `curve: 0`. Topology edges render as orthogonal 90-degree rectilinear lines. No curved or diagonal edges visible.
  </done>
</task>

</tasks>

<verification>
After executing this plan:
1. Open apps/demo/src/topology.ts and navigate to initTopologyEdges function (line ~190)
2. Verify BASE_OPTIONS object (line ~194) contains `path: 'grid'` and no `curve: 0` property
3. Run `pnpm type-check` to ensure TypeScript compilation succeeds
4. Start Vite dev server: `pnpm dev` from apps/demo directory
5. Manually verify in browser:
   - Open the demo topology diagram
   - All edges between nodes should render as 90-degree orthogonal paths (no curves, no diagonals)
   - Edges should follow cardinal directions (horizontal/vertical segments)
6. Verify no broken edges or missing connections
</verification>

<success_criteria>
- [ ] BASE_OPTIONS in topology.ts contains `path: 'grid'` property
- [ ] `curve: 0` property removed from BASE_OPTIONS
- [ ] TypeScript compilation succeeds (`pnpm type-check` exits 0)
- [ ] Vite dev server builds without errors
- [ ] Visual verification: All topology edges render as 90-degree orthogonal rectilinear paths (executor judgment)
</success_criteria>

<output>
After completion, create `.planning/phases/33-polish-demo-ui-layout/33-06-SUMMARY.md`
</output>
