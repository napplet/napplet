---
phase: 134-verification-milestone-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
  - .planning/phases/134-verification-milestone-close/134-VERIFICATION.md
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - VER-01
  - VER-02
  - VER-03
  - VER-04
  - VER-05
  - VER-06
  - VER-07

must_haves:
  truths:
    - "pnpm -r build and pnpm -r type-check both exit 0 across all 14 workspace packages"
    - "A spec-conformant CSP-block simulation positively asserts a securitypolicyviolation event for a blocked URL (not just absence-of-request)"
    - "SVG bomb / <foreignObject> / recursive-<use> rejection is provable from the NUB-RESOURCE spec text (caps + sandboxed-Worker MUSTs present)"
    - "N=10 concurrent resource.bytes(sameUrl) calls against the built dist with stubbed postMessage produce exactly 1 outbound resource.bytes envelope and 10 same-Blob resolutions"
    - "NUB-RELAY-AMENDMENT.md spec text contains explicit default-OFF + privacy rationale + per-event-kind allowlist guidance"
    - "grep -c '@napplet/' across all 4 napplet/nubs draft files returns 0:0:0:0"
    - "A consumer that imports only @napplet/nub/relay/types produces a tree-shaken bundle whose source contains zero references to installResourceShim / hydrateResourceCache / resource.bytes"
    - "NUB-RESOURCE.md draft uses error: ResourceErrorCode (matching shipped types.ts), not code:"
    - ".planning/STATE.md status flips to 'Milestone ready for audit' with all 7 VER-IDs marked verified"
    - ".planning/PROJECT.md gains a 'Shipped: v0.28.0 Browser-Enforced Resource Isolation' section"
    - ".planning/phases/134-verification-milestone-close/134-VERIFICATION.md exists and records pass/fail per VER-ID"
  artifacts:
    - path: ".planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md"
      provides: "Spec corrected: error:/message? matches shipped TypeScript types.ts"
      contains: "resource.bytes.error"
    - path: ".planning/phases/134-verification-milestone-close/134-VERIFICATION.md"
      provides: "Per-VER-ID pass/fail record + commands run + evidence"
      min_lines: 80
    - path: ".planning/STATE.md"
      provides: "Status flipped to milestone-audit-ready"
      contains: "Milestone ready for audit"
    - path: ".planning/PROJECT.md"
      provides: "v0.28.0 marked Shipped"
      contains: "Shipped: v0.28.0"
  key_links:
    - from: ".planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md"
      to: "packages/nub/src/resource/types.ts"
      via: "wire envelope shape (resource.bytes.error: id + error + message?)"
      pattern: "error.*ResourceErrorCode|`error`|error\\?:"
    - from: ".planning/STATE.md"
      to: ".planning/PROJECT.md"
      via: "milestone status transition (verifying → ready for audit; v0.28.0 in Shipped section)"
      pattern: "Shipped: v0.28.0"
    - from: ".planning/phases/134-verification-milestone-close/134-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "VER-01..07 traceability rows flip to Complete"
      pattern: "VER-0[1-7].*Complete|✓"
---

<objective>
Mechanical proof that v0.28.0 Browser-Enforced Resource Isolation is shippable. Run all 7 VER gates (VER-01..07), repair the single line of TS-vs-spec drift surfaced by Phase 133 (NUB-RESOURCE.md `code:` → `error:`/`message?:` to match the canonical shipped `packages/nub/src/resource/types.ts`), record pass/fail evidence in `134-VERIFICATION.md`, and flip STATE/PROJECT to "ready for audit" / "Shipped: v0.28.0". This is the LAST plan of the milestone — the autonomous workflow's audit/complete/cleanup steps run after this plan succeeds.

Purpose: Close the v0.28.0 milestone with audit-grade evidence. Every gate is grep-/exit-code-verifiable; every test fixture lives in `/tmp` per AGENTS.md no-pollution rule; the spec drift is a one-line surgical fix scoped to the local draft (cross-repo PR has not been opened yet).

Output:
- Corrected `.planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md` (spec table + 4 inline references + Error Codes table use `error:` / `message?:` instead of `code:` / `error?:`)
- New `.planning/phases/134-verification-milestone-close/134-VERIFICATION.md` recording pass/fail + commands + evidence per VER-ID
- Updated `.planning/STATE.md` (status: "Milestone ready for audit"; all phase 134 plans complete; VER-01..07 marked verified)
- Updated `.planning/PROJECT.md` (new "Shipped: v0.28.0 Browser-Enforced Resource Isolation" section + remove "Current Milestone" header for v0.28.0)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/134-verification-milestone-close/134-CONTEXT.md
@.planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
@.planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RELAY-AMENDMENT.md
@.planning/phases/132-cross-repo-nubs-prs/drafts/NUB-IDENTITY-AMENDMENT.md
@.planning/phases/132-cross-repo-nubs-prs/drafts/NUB-MEDIA-AMENDMENT.md
@packages/nub/src/resource/types.ts
@packages/nub/src/resource/shim.ts
@packages/nub/src/relay/shim.ts
@packages/nub/tsup.config.ts
@packages/nub/package.json
@.planning/codebase/TESTING.md
@.planning/phases/126-resource-nub-scaffold/126-01-SUMMARY.md
@.planning/phases/127-nub-relay-sidecar-amendment/127-01-SUMMARY.md
@.planning/phases/128-central-shim-integration/128-01-SUMMARY.md
@.planning/phases/130-vite-plugin-strict-csp/130-01-SUMMARY.md

<interfaces>
<!-- Canonical shipped envelope shape (from packages/nub/src/resource/types.ts).
     This is the SOURCE OF TRUTH for the NUB-RESOURCE spec correction in Task 4. -->

```typescript
// packages/nub/src/resource/types.ts (Phase 126; canonical, do NOT edit)
export interface ResourceBytesErrorMessage extends ResourceMessage {
  type: 'resource.bytes.error';
  id: string;
  error: ResourceErrorCode;   // <-- field name is `error`, NOT `code`
  message?: string;            // <-- optional human-readable detail is `message`, NOT `error`
}

export type ResourceErrorCode =
  | 'not-found' | 'blocked-by-policy' | 'timeout' | 'too-large'
  | 'unsupported-scheme' | 'decode-failed' | 'network-error' | 'quota-exceeded';

// Result envelope (success path):
export interface ResourceBytesResultMessage extends ResourceMessage {
  type: 'resource.bytes.result';
  id: string;
  blob: Blob;
  mime: string;
}

// Sidecar entry (consumed by hydrateResourceCache):
export interface ResourceSidecarEntry {
  url: string;
  blob: Blob;
  mime: string;
}
```

```typescript
// packages/nub/src/resource/shim.ts public API (Phase 126; canonical)
export function bytes(url: string, opts?: { signal?: AbortSignal }): Promise<Blob>;
export function bytesAsObjectURL(url: string): { url: string; revoke: () => void };
export function hydrateResourceCache(entries?: ResourceSidecarEntry[]): void;
export function handleResourceMessage(msg: { type: string; [key: string]: unknown }): void;
export function installResourceShim(): () => void;
```

```yaml
# Workspace packages (14 total) — for VER-01 acceptance:
# 5 first-party:    packages/{core,nub,sdk,shim,vite-plugin}
# 9 deprecated re-export shims: packages/nubs/{config,identity,ifc,keys,media,notify,relay,storage,theme}
```

```bash
# Built dist artifacts available for VER-04 / VER-07 (after VER-01 confirms green):
# packages/nub/dist/resource/{shim,types,sdk,index}.{js,d.ts,js.map}
# packages/nub/dist/relay/{shim,types,sdk,index}.{js,d.ts,js.map}
# Phase 127 caveat: tsup chunk-splits relay shim runtime; literal grep on dist/relay/shim.js
# for hydrateResourceCache returns 0 (the call lives in a shared chunk like chunk-RHDDLJ3D.js).
# VER-04 single-flight test imports the built bytes() function directly and stubs window.parent.postMessage.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VER-01 — workspace build + type-check gating gate</name>
  <files>/tmp/napplet-ver-01.log</files>
  <read_first>
    - .planning/REQUIREMENTS.md (VER-01 acceptance)
    - .planning/STATE.md (last known green: Phase 130/133 reported pnpm -r type-check green)
    - packages/nub/package.json (workspace package count cross-check)
  </read_first>
  <action>
    Run the workspace build and type-check, capture exit codes, and persist evidence to `/tmp/napplet-ver-01.log` (per AGENTS.md no-home-dir-pollution rule).

    Concrete commands (run sequentially; both MUST exit 0):
    ```bash
    cd /home/sandwich/Develop/napplet
    pnpm -r build      2>&1 | tee /tmp/napplet-ver-01-build.log
    BUILD_EXIT=${PIPESTATUS[0]}
    pnpm -r type-check 2>&1 | tee /tmp/napplet-ver-01-typecheck.log
    TC_EXIT=${PIPESTATUS[0]}
    echo "BUILD_EXIT=$BUILD_EXIT" >  /tmp/napplet-ver-01.log
    echo "TC_EXIT=$TC_EXIT"        >> /tmp/napplet-ver-01.log
    echo "PACKAGE_COUNT=$(ls -d packages/*/ packages/nubs/*/ | wc -l)" >> /tmp/napplet-ver-01.log
    ```

    If either exit is non-zero: STOP. Do NOT proceed to subsequent tasks. Report the failure in the SUMMARY and surface as a blocker. The whole milestone is non-shippable if VER-01 fails.

    Why VER-01 is gating: VER-02..07 build on the dist output (VER-04, VER-07) and on source/spec consistency (VER-03, VER-05, VER-06). A red workspace makes all downstream gates either invalid or impossible.

    Decision reference: per CONTEXT.md > decisions > "Order of test execution (recommend VER-01 first as gating; if it fails the rest don't matter)".
  </action>
  <verify>
    <automated>grep -E 'BUILD_EXIT=0' /tmp/napplet-ver-01.log &amp;&amp; grep -E 'TC_EXIT=0' /tmp/napplet-ver-01.log &amp;&amp; test "$(grep PACKAGE_COUNT /tmp/napplet-ver-01.log | cut -d= -f2)" -eq 14</automated>
  </verify>
  <done>
    `/tmp/napplet-ver-01.log` exists with `BUILD_EXIT=0`, `TC_EXIT=0`, `PACKAGE_COUNT=14`. VER-01 stamped pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: VER-02 + VER-03 — CSP-block positive-assertion simulation + SVG-rejection spec-conformance</name>
  <files>/tmp/napplet-ver-02-csp.mjs, /tmp/napplet-ver-02-csp.log, /tmp/napplet-ver-03-svg.sh, /tmp/napplet-ver-03-svg.log</files>
  <read_first>
    - .planning/REQUIREMENTS.md (VER-02, VER-03 acceptance)
    - .planning/phases/134-verification-milestone-close/134-CONTEXT.md > decisions > "Verification Strategy" (spec-conformant simulation acceptable; full Playwright is downstream-shell territory)
    - .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md (lines 165–187 — SVG MUST/SHOULD)
    - .planning/phases/130-vite-plugin-strict-csp/130-01-SUMMARY.md (Phase 130 already proved 7 CSP gates; this re-executes positive-block simulation as standalone evidence)
  </read_first>
  <action>
    Two combined sub-gates, both Node-side, both writing logs to `/tmp/`.

    SUB-GATE A — VER-02 (CSP positive blocking assertion via Playwright simulation):

    Per CONTEXT.md, the verification strategy LOCKS that "spec-conformant simulation" is acceptable evidence; full Playwright integration is downstream-shell territory. Construct a Node script that uses Playwright (already system-installed per AGENTS.md, browsers at `~/.cache/ms-playwright/`, NODE_PATH=/usr/lib/node_modules) to:

    1. Load a `data:text/html,...` page whose `<head>` first child is a `<meta http-equiv="Content-Security-Policy" content="default-src 'none'; img-src 'self'">`.
    2. Inject an `<img src="https://example.com/blocked.png">` AFTER the meta.
    3. Listen on BOTH `page.on('console')` and `page.on('requestfailed')`.
    4. ASSERT (positive blocking pattern per Pitfall 21): a console message of type `error` containing the substring `Content Security Policy` AND a `requestfailed` event whose URL is `https://example.com/blocked.png` were both observed.

    Write `/tmp/napplet-ver-02-csp.mjs`:
    ```javascript
    import { chromium } from '/usr/lib/node_modules/playwright';

    const html = `<!DOCTYPE html><html><head>` +
      `<meta http-equiv="Content-Security-Policy" content="default-src 'none'; img-src 'self'">` +
      `</head><body><img src="https://example.com/blocked.png"></body></html>`;
    const dataUrl = 'data:text/html;charset=utf-8,' + encodeURIComponent(html);

    const browser = await chromium.launch({ headless: true, args: ['--ozone-platform=wayland'] });
    const ctx = await browser.newContext();
    const page = await ctx.newPage();

    let cspViolation = false;
    let requestFailedForBlocked = false;
    page.on('console', (m) => {
      if (m.type() === 'error' && /Content Security Policy/i.test(m.text())) cspViolation = true;
    });
    page.on('requestfailed', (r) => {
      if (r.url() === 'https://example.com/blocked.png') requestFailedForBlocked = true;
    });

    await page.goto(dataUrl, { waitUntil: 'networkidle' }).catch(() => {});
    await page.waitForTimeout(500);

    const pass = cspViolation && requestFailedForBlocked;
    console.log(JSON.stringify({ cspViolation, requestFailedForBlocked, pass }));
    await browser.close();
    process.exit(pass ? 0 : 1);
    ```

    Run:
    ```bash
    cd /home/sandwich/Develop/napplet
    NODE_PATH=/usr/lib/node_modules node /tmp/napplet-ver-02-csp.mjs > /tmp/napplet-ver-02-csp.log 2>&1
    echo "VER02_EXIT=$?" >> /tmp/napplet-ver-02-csp.log
    ```

    SUB-GATE B — VER-03 (SVG-rejection spec conformance):

    Per CONTEXT.md, real rasterization is shell-side; the spec-conformance check verifies NUB-RESOURCE.md locks the three SVG MUSTs + the three SHOULD caps. Write `/tmp/napplet-ver-03-svg.sh`:

    ```bash
    #!/usr/bin/env bash
    set -e
    DRAFT=/home/sandwich/Develop/napplet/.planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md

    # Three MUST clauses (rasterize, no svg+xml delivery, sandboxed Worker no network):
    grep -q 'shell MUST rasterize' "$DRAFT" || { echo 'FAIL: missing shell-side rasterization MUST'; exit 1; }
    grep -q 'MUST NOT deliver raw .image/svg+xml' "$DRAFT" || { echo 'FAIL: missing svg+xml MUST NOT'; exit 1; }
    grep -q 'sandboxed Worker' "$DRAFT" || { echo 'FAIL: missing sandboxed Worker MUST'; exit 1; }
    grep -q 'no network' "$DRAFT" || { echo 'FAIL: missing no-network MUST'; exit 1; }

    # Three SHOULD caps (input bytes, output dimensions, wall-clock):
    grep -q 'Max input bytes' "$DRAFT" || { echo 'FAIL: missing max input bytes cap'; exit 1; }
    grep -q 'Max output dimensions' "$DRAFT" || { echo 'FAIL: missing max output dimensions cap'; exit 1; }
    grep -q 'Wall-clock rasterization budget' "$DRAFT" || { echo 'FAIL: missing wall-clock cap'; exit 1; }

    # Three named attack vectors must be cited (foreignObject, recursive use, billion laughs):
    grep -q 'foreignObject' "$DRAFT" || { echo 'FAIL: missing foreignObject mention'; exit 1; }
    grep -q 'recursive' "$DRAFT" || { echo 'FAIL: missing recursive mention (recursive use)'; exit 1; }
    grep -q -i 'billion.laughs\|entity.expansion' "$DRAFT" || { echo 'FAIL: missing billion-laughs / entity expansion mention'; exit 1; }

    echo PASS
    ```

    Run:
    ```bash
    chmod +x /tmp/napplet-ver-03-svg.sh
    /tmp/napplet-ver-03-svg.sh > /tmp/napplet-ver-03-svg.log 2>&1
    echo "VER03_EXIT=$?" >> /tmp/napplet-ver-03-svg.log
    ```

    Both sub-gates MUST pass before this task is done. If Playwright fails to launch (e.g., missing browser deps unrelated to napplet), document in the SUMMARY but DO NOT skip the gate — investigate per AGENTS.md (browsers are pacman-installed, no `npx playwright install`).
  </action>
  <verify>
    <automated>grep -q '"pass":true' /tmp/napplet-ver-02-csp.log &amp;&amp; grep -q 'VER02_EXIT=0' /tmp/napplet-ver-02-csp.log &amp;&amp; grep -q '^PASS$' /tmp/napplet-ver-03-svg.log &amp;&amp; grep -q 'VER03_EXIT=0' /tmp/napplet-ver-03-svg.log</automated>
  </verify>
  <done>
    `/tmp/napplet-ver-02-csp.log` reports `pass:true` (both `cspViolation` and `requestFailedForBlocked` were observed for the same URL); `/tmp/napplet-ver-03-svg.log` reports PASS (3 SVG MUSTs + 3 SHOULD caps + 3 named attack vectors all present in NUB-RESOURCE.md). VER-02 and VER-03 stamped pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: VER-04 + VER-05 — single-flight cache stampede + sidecar default-OFF spec conformance</name>
  <files>/tmp/napplet-ver-04-stampede.mjs, /tmp/napplet-ver-04-stampede.log, /tmp/napplet-ver-05-sidecar.sh, /tmp/napplet-ver-05-sidecar.log</files>
  <read_first>
    - .planning/REQUIREMENTS.md (VER-04, VER-05 acceptance)
    - .planning/phases/134-verification-milestone-close/134-CONTEXT.md > decisions > Verification Strategy (VER-05 LOCKED as spec-conformance test, NOT runtime test — see &lt;deep_work_rules&gt; in the planning prompt: shell decides whether to send sidecar; napplet hydrates whatever arrives; "default OFF" is shell policy locked in NUB-RELAY-AMENDMENT.md)
    - packages/nub/src/resource/shim.ts (the bytes() function single-flight cache + sendBytesRequest postMessage call site at line ~67)
    - packages/nub/dist/resource/shim.js (built ESM artifact — Task 1 confirmed this exists and is fresh)
    - .planning/phases/127-nub-relay-sidecar-amendment/127-01-SUMMARY.md (Phase 127 already proved single-flight implicitly; this is standalone re-execution for milestone evidence)
    - .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RELAY-AMENDMENT.md (must contain default-OFF + privacy + per-event-kind allowlist)
  </read_first>
  <action>
    Two combined sub-gates: VER-04 is a runtime test against the built dist; VER-05 is a spec-conformance grep (per CONTEXT decision and the planning prompt's deep_work_rules clarification — VER-05 tests SPEC default-OFF, not runtime hydration).

    SUB-GATE A — VER-04 (single-flight cache stampede, runtime test):

    Write `/tmp/napplet-ver-04-stampede.mjs` that imports the built `bytes()` from `packages/nub/dist/resource/shim.js`, stubs `globalThis.window.parent.postMessage`, fires N=10 concurrent `bytes(sameUrl)` calls, and asserts EXACTLY 1 outbound `resource.bytes` envelope was observed AND all 10 callers receive a Promise that, once the stub responds, resolves to the SAME Blob reference (Object.is identity).

    ```javascript
    import { bytes, handleResourceMessage } from '/home/sandwich/Develop/napplet/packages/nub/dist/resource/shim.js';

    // Minimal jsdom-free shim of window.parent for postMessage interception.
    const sent = [];
    globalThis.window = { parent: { postMessage: (msg) => sent.push(msg) } };
    // crypto.randomUUID is available in Node 18+ as globalThis.crypto.randomUUID

    const URL_UNDER_TEST = 'https://example.com/avatar.png';
    const N = 10;

    const promises = Array.from({ length: N }, () => bytes(URL_UNDER_TEST));

    // Confirm: exactly 1 envelope dispatched (single-flight dedup)
    const envelopeCount = sent.filter((m) => m.type === 'resource.bytes' &amp;&amp; m.url === URL_UNDER_TEST).length;
    if (envelopeCount !== 1) {
      console.log(JSON.stringify({ pass: false, envelopeCount, expected: 1 }));
      process.exit(1);
    }

    // Synthesize a single result envelope; all 10 promises must resolve to the same Blob.
    const id = sent.find((m) => m.type === 'resource.bytes').id;
    const blob = new Blob([new Uint8Array([1, 2, 3])], { type: 'image/png' });
    handleResourceMessage({ type: 'resource.bytes.result', id, blob, mime: 'image/png' });

    const results = await Promise.all(promises);
    const allSameBlob = results.every((r) => Object.is(r, blob));
    const pass = envelopeCount === 1 &amp;&amp; allSameBlob &amp;&amp; results.length === N;
    console.log(JSON.stringify({ pass, envelopeCount, allSameBlob, resultCount: results.length }));
    process.exit(pass ? 0 : 1);
    ```

    Run:
    ```bash
    cd /home/sandwich/Develop/napplet
    node /tmp/napplet-ver-04-stampede.mjs > /tmp/napplet-ver-04-stampede.log 2>&1
    echo "VER04_EXIT=$?" >> /tmp/napplet-ver-04-stampede.log
    ```

    SUB-GATE B — VER-05 (sidecar default-OFF spec conformance):

    Per CONTEXT.md and the planning prompt: this is a SPEC test, not a runtime test. The implementation hydrates whatever the shell sends; the "default OFF" gate lives in `NUB-RELAY-AMENDMENT.md`. Verify the spec text contains all three required pieces (default-OFF + privacy rationale + per-event-kind allowlist).

    Write `/tmp/napplet-ver-05-sidecar.sh`:
    ```bash
    #!/usr/bin/env bash
    set -e
    DRAFT=/home/sandwich/Develop/napplet/.planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RELAY-AMENDMENT.md

    # 1) Default-OFF posture documented:
    grep -q -i 'default.off\|default off\|defaults to off\|opt.in' "$DRAFT" \
      || { echo 'FAIL: NUB-RELAY-AMENDMENT.md missing default-OFF / opt-in language'; exit 1; }

    # 2) Privacy rationale present (must reference upstream visibility / fingerprinting / pre-fetching):
    grep -q -E -i 'privacy|fingerprint|pre.fetch|reveals user activity|upstream host' "$DRAFT" \
      || { echo 'FAIL: NUB-RELAY-AMENDMENT.md missing privacy rationale'; exit 1; }

    # 3) Per-event-kind allowlist guidance:
    grep -q -E -i 'event.kind|kind.*allowlist|per.event|allowlist' "$DRAFT" \
      || { echo 'FAIL: NUB-RELAY-AMENDMENT.md missing per-event-kind allowlist guidance'; exit 1; }

    echo PASS
    ```

    Run:
    ```bash
    chmod +x /tmp/napplet-ver-05-sidecar.sh
    /tmp/napplet-ver-05-sidecar.sh > /tmp/napplet-ver-05-sidecar.log 2>&1
    echo "VER05_EXIT=$?" >> /tmp/napplet-ver-05-sidecar.log
    ```
  </action>
  <verify>
    <automated>grep -q '"pass":true' /tmp/napplet-ver-04-stampede.log &amp;&amp; grep -q '"envelopeCount":1' /tmp/napplet-ver-04-stampede.log &amp;&amp; grep -q '"allSameBlob":true' /tmp/napplet-ver-04-stampede.log &amp;&amp; grep -q 'VER04_EXIT=0' /tmp/napplet-ver-04-stampede.log &amp;&amp; grep -q '^PASS$' /tmp/napplet-ver-05-sidecar.log &amp;&amp; grep -q 'VER05_EXIT=0' /tmp/napplet-ver-05-sidecar.log</automated>
  </verify>
  <done>
    `/tmp/napplet-ver-04-stampede.log` reports `pass:true, envelopeCount:1, allSameBlob:true` for N=10. `/tmp/napplet-ver-05-sidecar.log` reports PASS (default-OFF + privacy rationale + per-event-kind allowlist all present in NUB-RELAY-AMENDMENT.md). VER-04 and VER-05 stamped pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Spec drift fix + VER-06 — NUB-RESOURCE.md `code:` → `error:` correction, then cross-repo zero-grep</name>
  <files>.planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md, /tmp/napplet-ver-06-zerogrep.log</files>
  <read_first>
    - packages/nub/src/resource/types.ts (CANONICAL — `error: ResourceErrorCode` + `message?: string` on ResourceBytesErrorMessage)
    - .planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md (current state — uses `code:` and `error?:`)
    - .planning/STATE.md > decisions > "Phase 133: TS-vs-spec error envelope drift surfaced for future resolution" (the canonical implementation is the wire shape; spec follows impl)
    - .planning/phases/134-verification-milestone-close/134-CONTEXT.md > decisions > "Spec/Impl Alignment (LOCKED — from Phase 133 surfacing)"
    - .planning/REQUIREMENTS.md (VER-06 acceptance: zero `@napplet/` across all 4 drafts)
  </read_first>
  <action>
    PRE-WORK — Spec drift correction (single-line surgical fix scoped to local draft; cross-repo PR has not been opened yet, so this is in-repo only):

    The shipped TypeScript in `packages/nub/src/resource/types.ts` uses `error: ResourceErrorCode` + `message?: string` on `ResourceBytesErrorMessage`. The spec draft uses `code: ResourceErrorCode` + `error?: string`. Per the locked Phase 133 decision and CONTEXT.md, the implementation is canonical; the spec follows.

    Edit `.planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md`. Concrete substitutions (use the Edit tool — these are exact strings present in the draft):

    1. Wire Protocol table row (around line 54):
       FROM: `| `resource.bytes.error`  | shell -> napplet | `id`, `code`, `error?`   |`
       TO:   `| `resource.bytes.error`  | shell -> napplet | `id`, `error`, `message?` |`

    2. Key design notes paragraph (around line 62):
       FROM: ``programmatic dispatch MUST be driven by the `code` field (one of the eight enumerated codes), never by string-matching `error`.``
       TO:   ``programmatic dispatch MUST be driven by the `error` field (one of the eight enumerated codes), never by string-matching `message`.``

    3. Error response example (around line 87):
       FROM: `<- { "type": "resource.bytes.error", "id": "r3", "code": "blocked-by-policy", "error": "resolved IP 10.0.0.5 is in the RFC1918 private range" }`
       TO:   `<- { "type": "resource.bytes.error", "id": "r3", "error": "blocked-by-policy", "message": "resolved IP 10.0.0.5 is in the RFC1918 private range" }`

    4. Scheme `data:` paragraph (around line 99):
       FROM: ``Unknown schemes MUST result in `code: "unsupported-scheme"`.``
       TO:   ``Unknown schemes MUST result in `error: "unsupported-scheme"`.``

    5. `nostr:` not-found (around line 123):
       FROM: ``the result MUST be `code: "not-found"`.``
       TO:   ``the result MUST be `error: "not-found"`.``

    6. Private-IP rejection (around line 141):
       FROM: ``Rejection MUST emit `code: "blocked-by-policy"`. The error string SHOULD identify which range matched``
       TO:   ``Rejection MUST emit `error: "blocked-by-policy"`. The `message` string SHOULD identify which range matched``

    7. MIME byte-sniffing paragraph (around line 147):
       FROM: ``Bytes whose sniffed MIME falls outside the allowlist MUST be rejected with `code: "blocked-by-policy"` rather than delivered as an unknown MIME.``
       TO:   ``Bytes whose sniffed MIME falls outside the allowlist MUST be rejected with `error: "blocked-by-policy"` rather than delivered as an unknown MIME.``

    8. Response size cap (around line 151):
       FROM: ``Excess MUST result in `code: "too-large"`.``
       TO:   ``Excess MUST result in `error: "too-large"`.``

    9. Fetch timeout (around line 155):
       FROM: ``Timeout MUST result in `code: "timeout"`.``
       TO:   ``Timeout MUST result in `error: "timeout"`.``

    10. Per-napplet rate limit (around line 159):
        FROM: ``Rate-limit and concurrency-limit rejections MUST emit `code: "blocked-by-policy"` with an error string identifying which limit was hit.``
        TO:   ``Rate-limit and concurrency-limit rejections MUST emit `error: "blocked-by-policy"` with a `message` string identifying which limit was hit.``

    11. Redirect chain cap (around line 163):
        FROM: ``Excess hops or a redirect to a blocked address MUST result in `code: "blocked-by-policy"`.``
        TO:   ``Excess hops or a redirect to a blocked address MUST result in `error: "blocked-by-policy"`.``

    12. SVG rasterization caps table (around lines 183–185): replace ALL THREE table cells:
        FROM: `| `code: "too-large"` |` (×2 occurrences) and `| `code: "timeout"` |` (×1)
        TO:   `| `error: "too-large"` |` (×2) and `| `error: "timeout"` |` (×1)

    13. Whitelist scheme dispatch row (around line 219):
        FROM: ``Unknown schemes MUST return `code: "unsupported-scheme"`.``
        TO:   ``Unknown schemes MUST return `error: "unsupported-scheme"`.``

    14. Verify Blossom hash row (around line 224):
        FROM: ``Mismatch MUST result in `code: "decode-failed"`.``
        TO:   ``Mismatch MUST result in `error: "decode-failed"`.``

    15. Response size cap row (around line 233):
        FROM: ``Excess MUST result in `code: "too-large"`.``
        TO:   ``Excess MUST result in `error: "too-large"`.``

    16. Fetch timeout row (around line 234):
        FROM: ``Excess MUST result in `code: "timeout"`.``
        TO:   ``Excess MUST result in `error: "timeout"`.``

    17. Per-napplet concurrency row (around line 235):
        FROM: ``Excess MUST result in `code: "blocked-by-policy"`.``
        TO:   ``Excess MUST result in `error: "blocked-by-policy"`.``

    18. Per-napplet outstanding-Blob quota row (around line 237):
        FROM: ``over-quota MUST emit `code: "quota-exceeded"`.``
        TO:   ``over-quota MUST emit `error: "quota-exceeded"`.``

    Sanity check after editing:
    ```bash
    cd /home/sandwich/Develop/napplet
    DRAFT=.planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
    # Should be 0: no remaining `code:` references in error-envelope context.
    # Allowed false positive: the word "code" might appear in unrelated prose; verify each remaining hit.
    grep -nE 'code:\s*"(not-found|blocked-by-policy|timeout|too-large|unsupported-scheme|decode-failed|network-error|quota-exceeded)"' "$DRAFT"
    # Above MUST return exit code 1 (no matches).
    ```

    Then VER-06 — cross-repo zero-grep sweep across all 4 drafts:
    ```bash
    cd /home/sandwich/Develop/napplet
    DRAFTS_DIR=.planning/phases/132-cross-repo-nubs-prs/drafts
    {
      echo "=== VER-06 cross-repo zero-grep sweep ==="
      for f in "$DRAFTS_DIR"/NUB-RESOURCE.md "$DRAFTS_DIR"/NUB-RELAY-AMENDMENT.md "$DRAFTS_DIR"/NUB-IDENTITY-AMENDMENT.md "$DRAFTS_DIR"/NUB-MEDIA-AMENDMENT.md; do
        COUNT=$(grep -c '@napplet/' "$f" || true)
        echo "$(basename "$f"): $COUNT"
      done
      echo "=== combined ==="
      TOTAL=$(grep -c '@napplet/' "$DRAFTS_DIR"/*.md | awk -F: '{s+=$2} END {print s}')
      echo "TOTAL=$TOTAL"
    } > /tmp/napplet-ver-06-zerogrep.log 2>&1
    grep -q 'TOTAL=0' /tmp/napplet-ver-06-zerogrep.log && echo "VER06_EXIT=0" >> /tmp/napplet-ver-06-zerogrep.log || echo "VER06_EXIT=1" >> /tmp/napplet-ver-06-zerogrep.log
    ```

    Hygiene note: kehto / hyprgate are project-instructed banned terms (per global memory); these never appear in nubs drafts but if they did, that would be a separate issue. VER-06 scope is strictly `@napplet/` private-package references.
  </action>
  <verify>
    <automated>! grep -nE 'code:\s*"(not-found|blocked-by-policy|timeout|too-large|unsupported-scheme|decode-failed|network-error|quota-exceeded)"' /home/sandwich/Develop/napplet/.planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md &amp;&amp; grep -q 'TOTAL=0' /tmp/napplet-ver-06-zerogrep.log &amp;&amp; grep -q 'VER06_EXIT=0' /tmp/napplet-ver-06-zerogrep.log &amp;&amp; grep -q 'NUB-RESOURCE.md: 0' /tmp/napplet-ver-06-zerogrep.log &amp;&amp; grep -q 'NUB-RELAY-AMENDMENT.md: 0' /tmp/napplet-ver-06-zerogrep.log &amp;&amp; grep -q 'NUB-IDENTITY-AMENDMENT.md: 0' /tmp/napplet-ver-06-zerogrep.log &amp;&amp; grep -q 'NUB-MEDIA-AMENDMENT.md: 0' /tmp/napplet-ver-06-zerogrep.log</automated>
  </verify>
  <done>
    NUB-RESOURCE.md spec uses `error:` / `message?:` matching shipped types.ts (zero remaining `code: "<errorname>"` hits in error-envelope context). All 4 drafts report 0 `@napplet/` references; VER-06 stamped pass.
  </done>
</task>

<task type="auto">
  <name>Task 5: VER-07 — tree-shake bundle test (relay-types-only consumer pays zero bytes for resource code)</name>
  <files>/tmp/napplet-ver-07-treeshake/, /tmp/napplet-ver-07-treeshake.log</files>
  <read_first>
    - .planning/REQUIREMENTS.md (VER-07 acceptance)
    - packages/nub/package.json (sideEffects: false; subpath exports for /relay/types and /resource/shim)
    - packages/nub/dist/resource/shim.js (built artifact whose code MUST be absent from the tree-shaken bundle)
    - .planning/phases/121-verification-and-sign-off/*.md (v0.26.0 39-byte tree-shake precedent — the methodology to mirror)
  </read_first>
  <action>
    Build a minimal consumer fixture in `/tmp/napplet-ver-07-treeshake/` that imports ONLY `@napplet/nub/relay/types` and ASSERT the bundled output contains zero references to resource-shim symbols.

    Setup:
    ```bash
    mkdir -p /tmp/napplet-ver-07-treeshake
    cd /tmp/napplet-ver-07-treeshake

    cat > package.json <<'EOF'
    {
      "name": "ver-07-treeshake",
      "version": "0.0.0",
      "type": "module",
      "private": true,
      "dependencies": {
        "@napplet/nub": "file:/home/sandwich/Develop/napplet/packages/nub"
      },
      "devDependencies": {
        "esbuild": "*"
      }
    }
    EOF

    # Minimal type-only consumer: imports a single relay type.
    cat > entry.ts <<'EOF'
    import type { RelayEventMessage } from '@napplet/nub/relay/types';
    // Force the type to "exist" at runtime via a no-op runtime function so esbuild
    // emits something. The TYPES erase; the bundle should be ~empty.
    export function makeStub(): RelayEventMessage | null { return null; }
    EOF
    ```

    Install + bundle. The system has pnpm globally; use `npm install --prefix` to avoid touching the workspace. If npm has issues, fall back to a direct `node --experimental-default-type=module` esbuild invocation using the system esbuild if available.

    ```bash
    cd /tmp/napplet-ver-07-treeshake
    # Use the workspace's pnpm to reach esbuild — but install LOCAL to /tmp to avoid pnpm lockfile churn.
    # esbuild is the most portable bundler available; pacman or npm install --no-save it locally.
    if command -v esbuild >/dev/null; then
      EBUILD="esbuild"
    else
      # Pull esbuild without polluting the workspace (no-save, prefix to /tmp)
      npm install --prefix /tmp/napplet-ver-07-treeshake --no-save esbuild >/dev/null 2>&1
      EBUILD="/tmp/napplet-ver-07-treeshake/node_modules/.bin/esbuild"
    fi

    # First, link in @napplet/nub (file: protocol — copy dist locally to make it real)
    mkdir -p node_modules/@napplet/nub
    cp -r /home/sandwich/Develop/napplet/packages/nub/dist node_modules/@napplet/nub/dist
    cp /home/sandwich/Develop/napplet/packages/nub/package.json node_modules/@napplet/nub/package.json
    # @napplet/core dep transitively (only if any types reference it):
    mkdir -p node_modules/@napplet/core
    cp -r /home/sandwich/Develop/napplet/packages/core/dist node_modules/@napplet/core/dist 2>/dev/null || true
    cp /home/sandwich/Develop/napplet/packages/core/package.json node_modules/@napplet/core/package.json 2>/dev/null || true

    "$EBUILD" entry.ts \
      --bundle \
      --format=esm \
      --platform=neutral \
      --tree-shaking=true \
      --outfile=bundle.js 2> esbuild.err

    {
      echo "=== VER-07 tree-shake bundle inspection ==="
      echo "BUNDLE_SIZE=$(wc -c < bundle.js)"
      echo "--- bundle.js (full content if small):"
      cat bundle.js
      echo "--- forbidden-symbol search (MUST all be absent from a relay-types-only bundle) ---"
      for sym in installResourceShim hydrateResourceCache 'resource\.bytes' bytesAsObjectURL handleResourceMessage; do
        COUNT=$(grep -c "$sym" bundle.js || true)
        echo "$sym: $COUNT"
      done
    } > /tmp/napplet-ver-07-treeshake.log 2>&1

    # Pass criterion: every forbidden symbol must report COUNT 0.
    if grep -E '^(installResourceShim|hydrateResourceCache|resource\\.bytes|bytesAsObjectURL|handleResourceMessage): [1-9]' /tmp/napplet-ver-07-treeshake.log >/dev/null; then
      echo "VER07_EXIT=1" >> /tmp/napplet-ver-07-treeshake.log
    else
      echo "VER07_EXIT=0" >> /tmp/napplet-ver-07-treeshake.log
    fi
    ```

    Pattern reference: this mirrors the v0.26.0 39-byte tree-shake test (Phase 121 precedent). The expected bundle size is similarly tiny (a no-op function returning null + an empty type erasure). The spec acceptance is "zero bytes from `@napplet/nub/resource`", verified by symbol-grep — bundle size is informational, not the gate. (Compiler/minifier choices vary the byte count; symbol absence is the load-bearing signal.)

    Cleanup is deferred to outside this plan — `/tmp/napplet-ver-07-treeshake/` lives until the next reboot per AGENTS.md temp-dir rules.
  </action>
  <verify>
    <automated>grep -q 'VER07_EXIT=0' /tmp/napplet-ver-07-treeshake.log &amp;&amp; grep -q 'installResourceShim: 0' /tmp/napplet-ver-07-treeshake.log &amp;&amp; grep -q 'hydrateResourceCache: 0' /tmp/napplet-ver-07-treeshake.log &amp;&amp; grep -q 'bytesAsObjectURL: 0' /tmp/napplet-ver-07-treeshake.log &amp;&amp; grep -q 'handleResourceMessage: 0' /tmp/napplet-ver-07-treeshake.log</automated>
  </verify>
  <done>
    `/tmp/napplet-ver-07-treeshake/bundle.js` exists; symbol-grep confirms ZERO occurrences of `installResourceShim`, `hydrateResourceCache`, `resource.bytes`, `bytesAsObjectURL`, `handleResourceMessage` in the bundle. VER-07 stamped pass.
  </done>
</task>

<task type="auto">
  <name>Task 6: Milestone close — VERIFICATION.md, STATE.md "ready for audit", PROJECT.md "Shipped: v0.28.0"</name>
  <files>.planning/phases/134-verification-milestone-close/134-VERIFICATION.md, .planning/STATE.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - All 7 task logs (`/tmp/napplet-ver-0[1-7]*.log`) — synthesize evidence into VERIFICATION.md
    - .planning/STATE.md (current — milestone: v0.28.0; status: verifying)
    - .planning/PROJECT.md (current — "Current Milestone: v0.28.0..." section that must move to "Shipped: v0.28.0")
    - .planning/REQUIREMENTS.md (Traceability table — flip VER-01..07 from Pending to Complete)
    - .planning/PROJECT.md > "Shipped: v0.27.0 IFC Terminology Lock-In" — pattern to mirror for v0.28.0 Shipped section
  </read_first>
  <action>
    Synthesize and ship the milestone-close artifacts.

    STEP A — Create `.planning/phases/134-verification-milestone-close/134-VERIFICATION.md` with this structure:

    ```markdown
    # Phase 134 Verification — v0.28.0 Browser-Enforced Resource Isolation

    **Verified:** 2026-04-20
    **Phase:** 134 — Verification & Milestone Close
    **Milestone:** v0.28.0
    **Status:** PASS / FAIL (set per actual results)

    ## Summary

    All 7 VER-IDs (VER-01..07) executed; results recorded below per gate.

    | VER-ID | Gate | Result | Evidence |
    |--------|------|--------|----------|
    | VER-01 | pnpm -r build + type-check across 14 packages | PASS | /tmp/napplet-ver-01.log (BUILD_EXIT=0, TC_EXIT=0, PACKAGE_COUNT=14) |
    | VER-02 | CSP positive-blocking simulation | PASS | /tmp/napplet-ver-02-csp.log (cspViolation=true + requestFailedForBlocked=true) |
    | VER-03 | SVG rejection spec conformance | PASS | /tmp/napplet-ver-03-svg.log (3 MUSTs + 3 SHOULD caps + 3 attack vectors verified in NUB-RESOURCE.md) |
    | VER-04 | Single-flight cache stampede (N=10) | PASS | /tmp/napplet-ver-04-stampede.log (envelopeCount=1, allSameBlob=true, resultCount=10) |
    | VER-05 | Sidecar default-OFF spec conformance | PASS | /tmp/napplet-ver-05-sidecar.log (default-OFF + privacy + per-event-kind allowlist verified in NUB-RELAY-AMENDMENT.md) |
    | VER-06 | Cross-repo zero-grep across 4 drafts | PASS | /tmp/napplet-ver-06-zerogrep.log (TOTAL=0) |
    | VER-07 | Tree-shake (relay-types-only consumer) | PASS | /tmp/napplet-ver-07-treeshake.log (zero references to installResourceShim/hydrateResourceCache/bytesAsObjectURL/handleResourceMessage) |

    ## Spec Drift Resolved

    Per Phase 133 surfacing: `.planning/phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md` updated to use `error: ResourceErrorCode` + `message?: string` (matching shipped `packages/nub/src/resource/types.ts`). 18 substitutions across the spec table, key design notes, examples, scheme prose, policy section, SVG caps table, and Shell Guarantees table. Cross-repo PR has not been opened yet, so the correction is in-repo only — no upstream sync required.

    ## Artifacts

    - Spec correction commit on `feat/strict-model` branch
    - VERIFICATION.md (this file)
    - STATE.md updated: status → "Milestone ready for audit"
    - PROJECT.md updated: v0.28.0 moved from "Current Milestone" to "Shipped: v0.28.0"
    - REQUIREMENTS.md updated: VER-01..07 traceability rows flipped Pending → Complete

    ## Next

    Autonomous lifecycle proceeds: `/gsd:audit-milestone` → `/gsd:complete-milestone` → cleanup. Branch `feat/strict-model` is ready for merge to `main` (manual step outside this phase).
    ```

    Set the actual PASS/FAIL per gate based on the real log contents — do not blindly write PASS. If any gate failed in Tasks 1–5, mark FAIL here and STOP (do not proceed to STEP B/C/D — milestone is not shippable).

    STEP B — Update `.planning/STATE.md`:

    1. Change frontmatter `status: verifying` → `status: ready-for-audit`
    2. Update frontmatter `stopped_at:` to summarize Phase 134 completion: "Phase 134 plan-complete: all 7 VER-IDs (VER-01..07) verified PASS. NUB-RESOURCE.md spec drift resolved (code → error/message?). VERIFICATION.md authored. STATE/PROJECT flipped for milestone audit. Ready for `/gsd:audit-milestone`."
    3. Update frontmatter `last_updated:` to current ISO timestamp
    4. Update progress: `completed_phases: 10`, `completed_plans: 10`, `percent: 100`
    5. Add to Decisions section a Phase 134 entry:
       - "Phase 134: Spec/impl drift resolved — NUB-RESOURCE.md draft now uses `error: ResourceErrorCode` + `message?: string` per shipped `types.ts`. 18 substitutions across spec table, key notes, examples, scheme prose, policy section, SVG caps table, and Shell Guarantees table. Cross-repo PR not yet opened, so correction is in-repo only."
       - "Phase 134: Verification methodology pattern — Node-side spec-conformant simulations + grep-verifiable logs in /tmp/ (per AGENTS.md no-pollution) acceptable evidence for milestone close when full integration tests are downstream-shell territory. VER-02 (Playwright CSP positive-block), VER-04 (single-flight stampede against built dist with stubbed postMessage), VER-07 (tree-shake bundle inspection) are runtime tests; VER-03, VER-05 are spec-conformance greps; VER-01, VER-06 are exit-code/grep gates."
    6. Update Pending Todos: mark Phase 126–133 verified, mark Phase 134 plan-complete and verified.
    7. Update Session Continuity: stopped_at + Resume hint pointing to `/gsd:audit-milestone v0.28.0`.

    STEP C — Update `.planning/PROJECT.md`:

    1. DELETE the entire current "## Current Milestone: v0.28.0..." section (lines 3–24 approx — the goal/target/key context block).
    2. INSERT a new "## Shipped: v0.28.0 Browser-Enforced Resource Isolation" section immediately ABOVE the existing "## Shipped: v0.27.0 IFC Terminology Lock-In" section. Mirror the v0.27.0 section's structure (one paragraph summarizing what shipped + a sentence on acceptance gates + phase/plan count + ship date + archive link placeholder). Concrete content:

       ```markdown
       ## Shipped: v0.28.0 Browser-Enforced Resource Isolation

       Converted napplet iframe security from ambient trust to browser-enforced isolation. Single new NUB (`resource`) with `resource.bytes(url) → Blob` primitive, scheme-pluggable URL space (4 canonical schemes: `https:`, `blossom:`, `nostr:`, `data:`). `data:` decoded inline (zero shell round-trip); other schemes route via `postMessage` envelopes through the host shell. Single-flight cache (N concurrent same-URL calls share 1 fetch), AbortSignal cancellation with `resource.cancel` envelope, `bytesAsObjectURL(url)` lifecycle helper. Optional sidecar pre-resolution on `relay.event` envelopes (`resources?: ResourceSidecarEntry[]`) with default-OFF privacy posture per NUB-RELAY amendment. Strict CSP enforcement at the iframe boundary via `@napplet/vite-plugin` `strictCsp` option: 10-directive baseline, first-`<head>`-child meta injection, header-only directive rejection, dev/prod connect-src split, nonce-based scripts. NIP-5D Security Considerations subsection added in-repo; 4 cross-repo draft PRs authored for `napplet/nubs` (NUB-RESOURCE new spec; NUB-RELAY/IDENTITY/MEDIA amendments). Acceptance gates: `pnpm -r build` + `pnpm -r type-check` green across all 14 packages; CSP positive-blocking Playwright simulation; single-flight stampede against built dist; sidecar default-OFF + SVG-bomb spec conformance; cross-repo zero-grep clean; tree-shake bundle (zero resource-shim symbols in relay-types-only consumer). Demo napplets explicitly delegated to downstream shell repo (Option B). 10 phases, 10 plans shipped 2026-04-20. See [archive](milestones/v0.28.0-ROADMAP.md).
       ```

    Note: the archive link target may not exist yet — that's created by `/gsd:complete-milestone` lifecycle step. Leaving the link as-is is correct; cleanup wires it up.

    STEP D — Update `.planning/REQUIREMENTS.md`:

    Flip the VER-01..07 traceability rows from Pending → Complete (a 7-row edit to the table starting at line 210).

    Decision reference: per CONTEXT.md > "Milestone Close Activities" — these 4 file updates ARE the close. The actual `/gsd:audit-milestone` and `/gsd:complete-milestone` workflows run AFTER this plan in the autonomous lifecycle.
  </action>
  <verify>
    <automated>test -f /home/sandwich/Develop/napplet/.planning/phases/134-verification-milestone-close/134-VERIFICATION.md &amp;&amp; grep -q 'VER-01.*PASS' /home/sandwich/Develop/napplet/.planning/phases/134-verification-milestone-close/134-VERIFICATION.md &amp;&amp; grep -q 'VER-07.*PASS' /home/sandwich/Develop/napplet/.planning/phases/134-verification-milestone-close/134-VERIFICATION.md &amp;&amp; grep -q 'status: ready-for-audit' /home/sandwich/Develop/napplet/.planning/STATE.md &amp;&amp; grep -q 'Shipped: v0.28.0 Browser-Enforced Resource Isolation' /home/sandwich/Develop/napplet/.planning/PROJECT.md &amp;&amp; ! grep -q '## Current Milestone: v0.28.0' /home/sandwich/Develop/napplet/.planning/PROJECT.md &amp;&amp; test "$(grep -cE '^\| VER-0[1-7] \| Phase 134 \| Complete' /home/sandwich/Develop/napplet/.planning/REQUIREMENTS.md)" -eq 7</automated>
  </verify>
  <done>
    `134-VERIFICATION.md` exists with all 7 VER-IDs marked PASS. STATE.md status flipped to `ready-for-audit` with Phase 134 decisions appended. PROJECT.md no longer has "Current Milestone: v0.28.0" header; new "Shipped: v0.28.0 Browser-Enforced Resource Isolation" section in place above the v0.27.0 section. REQUIREMENTS.md traceability table shows all 7 VER-IDs as Complete. Milestone is ready for the autonomous lifecycle's `/gsd:audit-milestone` step.
  </done>
</task>

</tasks>

<verification>
**Phase-level acceptance gate** (after all 6 tasks complete):

1. `pnpm -r build` + `pnpm -r type-check` exit 0 across all 14 workspace packages (Task 1)
2. CSP positive blocking observed by both `console` and `requestfailed` for the same URL (Task 2)
3. SVG rejection spec text contains the 3 MUSTs + 3 SHOULD caps + 3 named attack vectors (Task 2)
4. Single-flight cache: N=10 concurrent calls produce 1 envelope and all 10 callers receive the same Blob reference (Task 3)
5. Sidecar default-OFF: NUB-RELAY-AMENDMENT.md contains default-OFF + privacy rationale + per-event-kind allowlist (Task 3)
6. NUB-RESOURCE.md spec uses `error:` / `message?:` matching shipped types.ts (Task 4)
7. Cross-repo zero-grep: 0 `@napplet/` references in any of the 4 drafts (Task 4)
8. Tree-shake: relay-types-only consumer bundle has 0 references to resource-shim symbols (Task 5)
9. VERIFICATION.md authored, STATE.md "ready-for-audit", PROJECT.md "Shipped: v0.28.0", REQUIREMENTS.md traceability flipped (Task 6)

**One-shot verification command** (run from repo root after Task 6):
```bash
cd /home/sandwich/Develop/napplet
grep -q 'BUILD_EXIT=0' /tmp/napplet-ver-01.log \
  &amp;&amp; grep -q '"pass":true' /tmp/napplet-ver-02-csp.log \
  &amp;&amp; grep -q '^PASS$' /tmp/napplet-ver-03-svg.log \
  &amp;&amp; grep -q '"pass":true' /tmp/napplet-ver-04-stampede.log \
  &amp;&amp; grep -q '^PASS$' /tmp/napplet-ver-05-sidecar.log \
  &amp;&amp; grep -q 'TOTAL=0' /tmp/napplet-ver-06-zerogrep.log \
  &amp;&amp; grep -q 'VER07_EXIT=0' /tmp/napplet-ver-07-treeshake.log \
  &amp;&amp; grep -q 'status: ready-for-audit' .planning/STATE.md \
  &amp;&amp; grep -q 'Shipped: v0.28.0 Browser-Enforced Resource Isolation' .planning/PROJECT.md \
  &amp;&amp; echo "MILESTONE v0.28.0 READY FOR AUDIT"
```
</verification>

<success_criteria>
- All 7 VER-IDs (VER-01..07) report PASS with /tmp log evidence
- NUB-RESOURCE.md spec drift resolved (18 substitutions; in-repo only — no cross-repo PR exists yet)
- 134-VERIFICATION.md authored with per-gate evidence + spec-drift note + next-step hint
- STATE.md flipped to `status: ready-for-audit` with Phase 134 decisions captured
- PROJECT.md "Current Milestone: v0.28.0" section deleted; "Shipped: v0.28.0 Browser-Enforced Resource Isolation" section inserted above v0.27.0
- REQUIREMENTS.md traceability table: VER-01..07 rows show Phase 134 / Complete
- All 6 task logs in `/tmp/` per AGENTS.md no-home-dir-pollution rule
- Atomic per-task git commits (Phase 133 pattern locked); the per-task commit boundary is what makes any single failure safely revertable without losing earlier passing gates
- Branch `feat/strict-model` ready for merge to main (manual step outside this phase, per CONTEXT.md)
</success_criteria>

<output>
After completion, create `.planning/phases/134-verification-milestone-close/134-01-SUMMARY.md` recording:
- All 7 VER-ID results with /tmp log paths
- Spec-drift resolution (18 substitutions in NUB-RESOURCE.md)
- STATE/PROJECT/REQUIREMENTS updates applied
- Confirmation that milestone is ready for `/gsd:audit-milestone v0.28.0`
- Any deviations from plan (if e.g. Playwright launch required workaround) noted in a Deviations subsection
- Next step: autonomous lifecycle's audit-milestone → complete-milestone → cleanup; manual `feat/strict-model` → `main` merge after audit clears
</output>
