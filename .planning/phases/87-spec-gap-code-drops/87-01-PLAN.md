---
phase: 87-spec-gap-code-drops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/types.ts
  - packages/core/src/topics.ts
  - packages/core/src/constants.ts
  - packages/core/src/index.ts
  - packages/core/src/index.test.ts
autonomous: true
requirements:
  - DROP-01
  - DROP-02
  - DROP-03
  - DROP-04
  - DROP-05
  - DROP-06
  - DROP-07
  - DROP-08
  - DROP-09

must_haves:
  truths:
    - "`Capability` type and `ALL_CAPABILITIES` constant do not exist anywhere in packages/core/src/"
    - "13 dropped TOPICS entries (7 superseded + 3 config + 3 scoped relay) do not exist in topics.ts"
    - "15 deferred TOPICS entries (5 future NUB + 6 keybinds + 1 wm + 4 audio) remain in topics.ts"
    - "`SHELL_BRIDGE_URI`, `REPLAY_WINDOW_SECONDS`, and `PROTOCOL_VERSION` do not exist anywhere in packages/core/src/"
    - "constants.ts file does not exist (all 3 constants deleted, file removed)"
    - "index.test.ts contains no tests referencing deleted exports"
    - "`pnpm build && pnpm type-check` passes with zero errors"
  artifacts:
    - path: "packages/core/src/types.ts"
      provides: "NostrEvent, NostrFilter, Subscription, EventTemplate, NappletGlobal types only"
      contains: "export interface NostrEvent"
    - path: "packages/core/src/topics.ts"
      provides: "15 deferred TOPICS entries with auto-narrowed TopicKey/TopicValue types"
      contains: "STREAM_CHANNEL_SWITCH"
    - path: "packages/core/src/index.ts"
      provides: "Barrel exports without Capability, ALL_CAPABILITIES, constants.ts imports, or deleted TOPICS references"
      exports: ["NostrEvent", "NostrFilter", "NappletMessage", "NubDomain", "TOPICS", "TopicKey", "TopicValue"]
    - path: "packages/core/src/index.test.ts"
      provides: "Tests for remaining exports only"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/types.js"
      via: "re-export (no Capability or ALL_CAPABILITIES)"
      pattern: "export type.*NostrEvent.*NostrFilter.*Subscription.*EventTemplate.*NappletGlobal"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/topics.js"
      via: "re-export (unchanged)"
      pattern: "export.*TOPICS.*from.*topics"
    - from: "packages/core/src/index.ts"
      to: "NO constants.js import"
      via: "entire constants section removed"
      pattern: "^(?!.*constants\\.js)"
---

<objective>
Delete all 7 "drop" verdict artifacts from the v0.18.0 spec gap audit, so @napplet/core exports only spec-backed types, constants, and infrastructure.

Purpose: Remove unspecced code -- ACL capability strings, superseded/config/scoped-relay TOPICS, and 3 dead constants -- that were identified in Phase 84 and marked for deletion in Phase 86.
Output: Clean packages/core/src/ with only spec-backed artifacts, passing build and type-check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/SPEC-GAPS.md
@.planning/phases/87-spec-gap-code-drops/87-CONTEXT.md

@packages/core/src/types.ts
@packages/core/src/topics.ts
@packages/core/src/constants.ts
@packages/core/src/index.ts
@packages/core/src/index.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Capability type, ALL_CAPABILITIES, and constants.ts</name>
  <files>packages/core/src/types.ts, packages/core/src/constants.ts</files>
  <action>
**types.ts (DROP-01):** Delete the entire Capability Types section (lines 49-93):
- Delete the section comment `// --- Capability Types ---`
- Delete the `Capability` type union (lines 63-73)
- Delete the `ALL_CAPABILITIES` constant (lines 82-93)
- Delete all JSDoc comments associated with both (lines 51-62 and 75-81)
- Keep everything else intact: the file header, `import type { NappletGlobalShell }` import, NIP-01 Types section (NostrEvent, NostrFilter), and Shim API Types section (Subscription, EventTemplate, NappletGlobal)

**constants.ts (DROP-05, DROP-06, DROP-07):** Delete the entire file. All 3 constants are being dropped:
- `PROTOCOL_VERSION` (line 19) -- NIP-5D is versionless per Nostr convention
- `SHELL_BRIDGE_URI` (line 29) -- AUTH was removed in v0.15.0
- `REPLAY_WINDOW_SECONDS` (line 38) -- shell-only replay policy

Run `rm packages/core/src/constants.ts` to delete the file.
  </action>
  <verify>
    <automated>test ! -f packages/core/src/constants.ts && ! grep -q 'Capability' packages/core/src/types.ts && ! grep -q 'ALL_CAPABILITIES' packages/core/src/types.ts && echo "PASS"</automated>
  </verify>
  <done>types.ts contains only NostrEvent, NostrFilter, Subscription, EventTemplate, NappletGlobal. constants.ts file no longer exists.</done>
</task>

<task type="auto">
  <name>Task 2: Delete 13 TOPICS entries and update index.ts exports</name>
  <files>packages/core/src/topics.ts, packages/core/src/index.ts</files>
  <action>
**topics.ts (DROP-02, DROP-03, DROP-04):** Delete exactly 13 entries from the TOPICS object, keeping 15 deferred entries.

Delete these entries and their section comments:

Superseded (DROP-02) -- delete entire "Auth and Identity" and "State Operations" sections:
- `AUTH_IDENTITY_CHANGED: 'auth:identity-changed'`
- `STATE_GET: 'shell:state-get'`
- `STATE_SET: 'shell:state-set'`
- `STATE_REMOVE: 'shell:state-remove'`
- `STATE_CLEAR: 'shell:state-clear'`
- `STATE_KEYS: 'shell:state-keys'`
- `STATE_RESPONSE: 'napplet:state-response'`

Config (DROP-03) -- delete entire "Shell Config" section:
- `SHELL_CONFIG_GET: 'shell:config-get'`
- `SHELL_CONFIG_UPDATE: 'shell:config-update'`
- `SHELL_CONFIG_CURRENT: 'shell:config-current'`

Scoped relay (DROP-04) -- delete entire "Relay Scoping" section:
- `RELAY_SCOPED_CONNECT: 'shell:relay-scoped-connect'`
- `RELAY_SCOPED_CLOSE: 'shell:relay-scoped-close'`
- `RELAY_SCOPED_PUBLISH: 'shell:relay-scoped-publish'`

Keep these 15 entries (deferred, not dropped):
- Stream and Content (3): STREAM_CHANNEL_SWITCH, STREAM_CURRENT_CONTEXT_GET, STREAM_CURRENT_CONTEXT
- Profile (1): PROFILE_OPEN
- Keybinds (6): KEYBINDS_GET, KEYBINDS_ALL, KEYBINDS_UPDATE, KEYBINDS_RESET, KEYBINDS_CAPTURE_START, KEYBINDS_CAPTURE_END
- Window Manager (1): WM_FOCUSED_WINDOW_CHANGED
- Chat (1): CHAT_OPEN_DM
- Audio (4): AUDIO_REGISTER, AUDIO_UNREGISTER, AUDIO_STATE_CHANGED, AUDIO_MUTED (note: keep "napplet:" prefix direction section comment)

Update the module-level JSDoc: remove the `@example` block that references `TOPICS.AUTH_IDENTITY_CHANGED` and `TOPICS.STATE_GET` since those no longer exist. Replace with an example using a retained topic like `TOPICS.PROFILE_OPEN`.

TopicKey and TopicValue types auto-narrow from `typeof TOPICS` -- no changes needed to those type definitions.

**index.ts (DROP-01 through DROP-07):** Make these changes:

1. Remove the entire "Protocol Constants" section (lines 47-53):
   ```ts
   // --- Protocol Constants ---
   export {
     PROTOCOL_VERSION,
     SHELL_BRIDGE_URI,
     REPLAY_WINDOW_SECONDS,
   } from './constants.js';
   ```

2. In the "Protocol Types" section, remove `Capability` from the type export and `ALL_CAPABILITIES` from the value export:
   - Change the type export from `export type { NostrEvent, NostrFilter, Capability, Subscription, EventTemplate, NappletGlobal }` to `export type { NostrEvent, NostrFilter, Subscription, EventTemplate, NappletGlobal }`
   - Delete the `export { ALL_CAPABILITIES } from './types.js';` line

3. Update the module-level `@example` JSDoc block (lines 12-19) to remove references to `Capability`, `SHELL_BRIDGE_URI`, `PROTOCOL_VERSION`, `ALL_CAPABILITIES`. The example should show only the remaining exports.
  </action>
  <verify>
    <automated>node -e "const t = require('fs').readFileSync('packages/core/src/topics.ts','utf8'); const keys = t.match(/^\s+\w+:/gm); console.log('TOPICS entries:', keys.length); console.assert(keys.length === 15, 'Expected 15, got ' + keys.length);" && ! grep -q 'constants.js' packages/core/src/index.ts && ! grep -q 'ALL_CAPABILITIES' packages/core/src/index.ts && ! grep -q 'Capability' packages/core/src/index.ts && echo "PASS"</automated>
  </verify>
  <done>topics.ts has exactly 15 entries (no superseded/config/scoped-relay). index.ts has no imports from constants.js, no Capability type export, no ALL_CAPABILITIES export.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests and verify clean build</name>
  <files>packages/core/src/index.test.ts</files>
  <action>
**index.test.ts (DROP-08):** Remove all tests referencing deleted exports:

1. Remove these imports from the top-level import statement:
   - `SHELL_BRIDGE_URI`
   - `PROTOCOL_VERSION`
   - `ALL_CAPABILITIES`
   - `REPLAY_WINDOW_SECONDS`
   - From the type import: `Capability`
   Keep: `TOPICS` in the value import; `NostrEvent`, `NostrFilter`, `TopicKey`, `TopicValue`, `NamespacedCapability`, `ShellSupports` in the type import.

2. Delete the entire `describe('protocol constants', ...)` block (lines 14-29) -- all 3 tests reference deleted constants.

3. Delete the entire `describe('capability constants', ...)` block (lines 31-46) -- references deleted ALL_CAPABILITIES.

4. In the `describe('TOPICS', ...)` block:
   - Keep the first test `'exports TOPICS object with shell command keys'` (lines 49-54) -- it uses `Object.values(TOPICS)` generically and still works.
   - Delete the `'includes state operation topics'` test (lines 56-63) -- references deleted STATE_* topics.
   - Delete the `'includes auth and relay topics'` test (lines 65-69) -- references deleted AUTH_IDENTITY_CHANGED and RELAY_SCOPED_* topics.
   - Keep the `'includes audio topics'` test (lines 71-74) -- audio topics are deferred, not dropped.

5. In the `describe('type-level exports (compile check)', ...)` block:
   - Remove `const _cap: Capability = 'relay:read';` (line 82) -- Capability type is deleted.
   - Change `const _topicKey: TopicKey = 'STATE_GET';` to `const _topicKey: TopicKey = 'PROFILE_OPEN';` -- STATE_GET no longer exists.
   - Change `const _topicVal: TopicValue = TOPICS.STATE_GET;` to `const _topicVal: TopicValue = TOPICS.PROFILE_OPEN;` -- same reason.

6. Keep the entire `describe('namespaced capability types', ...)` block (lines 89-132) unchanged -- it tests NamespacedCapability and ShellSupports which are NOT being deleted.

**Build verification (DROP-09):** After all edits, run:
- `cd packages/core && pnpm test:unit` -- unit tests pass
- `pnpm build` -- turborepo build passes across all packages
- `pnpm type-check` -- TypeScript type-check passes across all packages
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && cd packages/core && pnpm test:unit && cd ../.. && pnpm build && pnpm type-check</automated>
  </verify>
  <done>All unit tests pass with no references to deleted exports. `pnpm build && pnpm type-check` exits with zero errors. @napplet/core exports only spec-backed artifacts.</done>
</task>

</tasks>

<verification>
After all tasks complete, verify end-to-end:

1. `grep -r 'Capability\|ALL_CAPABILITIES' packages/core/src/ --include='*.ts'` returns no matches (except NamespacedCapability which is unrelated)
2. `grep -r 'SHELL_BRIDGE_URI\|REPLAY_WINDOW_SECONDS\|PROTOCOL_VERSION' packages/core/src/ --include='*.ts'` returns no matches
3. `grep -r 'STATE_GET\|STATE_SET\|STATE_REMOVE\|STATE_CLEAR\|STATE_KEYS\|STATE_RESPONSE\|AUTH_IDENTITY_CHANGED' packages/core/src/ --include='*.ts'` returns no matches
4. `grep -r 'SHELL_CONFIG_GET\|SHELL_CONFIG_UPDATE\|SHELL_CONFIG_CURRENT\|RELAY_SCOPED_CONNECT\|RELAY_SCOPED_CLOSE\|RELAY_SCOPED_PUBLISH' packages/core/src/ --include='*.ts'` returns no matches
5. `test ! -f packages/core/src/constants.ts` -- file deleted
6. `pnpm build && pnpm type-check` -- clean build
</verification>

<success_criteria>
- Capability type and ALL_CAPABILITIES constant do not exist in packages/core/src/
- 13 dropped TOPICS entries removed; 15 deferred entries remain
- constants.ts file deleted entirely (was emptied by removing all 3 constants)
- index.ts has no imports from constants.js, no Capability/ALL_CAPABILITIES exports
- index.test.ts has no tests referencing deleted exports, all remaining tests pass
- pnpm build && pnpm type-check pass with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/87-spec-gap-code-drops/87-01-SUMMARY.md`
</output>
