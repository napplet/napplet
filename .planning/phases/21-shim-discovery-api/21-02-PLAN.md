---
phase: 21-shim-discovery-api
plan: 02
title: "Install window.napplet global and add public API exports"
wave: 2
depends_on: [21-01]
files_modified:
  - packages/shim/src/index.ts
autonomous: true
requirements: [SHIM-01, SHIM-02, SHIM-03, SHIM-04]
must_haves:
  truths:
    - "window.napplet object is installed during shim initialization in index.ts"
    - "window.napplet.discoverServices is a function"
    - "window.napplet.hasService is a function"
    - "window.napplet.hasServiceVersion is a function"
    - "ServiceInfo type is exported from @napplet/shim public API"
    - "discoverServices, hasService, hasServiceVersion are exported from @napplet/shim public API"
    - "pnpm --filter @napplet/shim build succeeds with no type errors"
    - "pnpm type-check passes for the shim package"
  artifacts:
    - path: "packages/shim/src/index.ts"
      provides: "Updated shim entry point with window.napplet installation"
      contains: "window.napplet"
    - path: "packages/shim/src/index.ts"
      provides: "Public API re-exports for discovery"
      contains: "discoverServices"
  key_links:
    - from: "packages/shim/src/index.ts"
      to: "packages/shim/src/discovery-shim.ts"
      via: "import { discoverServices, hasService, hasServiceVersion }"
      pattern: "import.*discovery-shim"
---

# Plan 21-02: Install window.napplet Global and Add Public API Exports

<objective>
Wire the discovery-shim.ts module (from Plan 21-01) into the shim's initialization and public API surface. Install `window.napplet` as a new global object during shim startup (same pattern as `window.nostr` installation at lines 259-289), and re-export the discovery functions and ServiceInfo type from the shim's barrel export. After this plan, napplet code can access discovery via both `window.napplet.discoverServices()` (no imports) and direct `import { discoverServices } from '@napplet/shim'`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-shim-discovery-api/21-CONTEXT.md

<!-- Source of truth files -->
@packages/shim/src/index.ts
@packages/shim/src/discovery-shim.ts (created by Plan 21-01)
</context>

<interfaces>
### window.napplet global shape

```typescript
interface NappletGlobal {
  discoverServices(): Promise<ServiceInfo[]>;
  hasService(name: string): Promise<boolean>;
  hasServiceVersion(name: string, version: string): Promise<boolean>;
}
```

Installed on `window` during shim initialization, following the same pattern as `window.nostr` (lines 259-289 of index.ts).

### Public API exports (added to index.ts)

```typescript
export { discoverServices, hasService, hasServiceVersion } from './discovery-shim.js';
export type { ServiceInfo } from './discovery-shim.js';
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add discovery imports and public API exports to index.ts</name>
  <files>packages/shim/src/index.ts</files>
  <read_first>
    - packages/shim/src/index.ts (full file — understand current imports, exports, and initialization sections)
    - packages/shim/src/discovery-shim.ts (created by Plan 21-01 — confirm exports)
  </read_first>
  <action>
    Edit packages/shim/src/index.ts to add the discovery-shim integration. Three changes:

    **Change 1: Add import (top of file, after other imports)**

    After the existing import block (around line 12, after `import type { NostrEvent } from './types.js';`), add:

    ```typescript
    import { discoverServices, hasService, hasServiceVersion } from './discovery-shim.js';
    ```

    **Change 2: Add public API exports (in the "Public API exports" section, around line 16-20)**

    After the existing exports (after `export { nappState, nappStorage } from './state-shim.js';` at line 23), add:

    ```typescript
    // Service discovery API (window.napplet)
    export { discoverServices, hasService, hasServiceVersion } from './discovery-shim.js';
    export type { ServiceInfo } from './discovery-shim.js';
    ```

    **Change 3: Install window.napplet global (in the "Initialize" section at the bottom, before the keypair initialization block)**

    Before the existing `// Initialize keypair eagerly` block (around line 306), add:

    ```typescript
    // Install window.napplet discovery API
    (window as unknown as { napplet: unknown }).napplet = {
      discoverServices,
      hasService,
      hasServiceVersion,
    };
    ```

    This follows the exact same casting pattern used for `window.nostr` at line 259:
    `(window as unknown as { nostr: unknown }).nostr = { ... };`

    The window.napplet object is installed at load time (not lazily) per established shim pattern — all globals are installed during initialization.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep "import.*discoverServices.*discovery-shim" packages/shim/src/index.ts && grep "export.*discoverServices.*discovery-shim" packages/shim/src/index.ts && grep "export type.*ServiceInfo.*discovery-shim" packages/shim/src/index.ts && grep "window.*napplet.*discoverServices" packages/shim/src/index.ts && grep "window.*napplet.*hasService" packages/shim/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - index.ts imports discoverServices, hasService, hasServiceVersion from './discovery-shim.js'
    - index.ts re-exports discoverServices, hasService, hasServiceVersion as public API
    - index.ts re-exports ServiceInfo type from discovery-shim
    - window.napplet object is installed with discoverServices, hasService, hasServiceVersion methods
    - window.napplet installation uses same cast pattern as window.nostr: `(window as unknown as { napplet: unknown }).napplet`
    - window.napplet is installed in the Initialize section (bottom of file), not at the top
  </acceptance_criteria>
  <done>window.napplet global installed and public API exports added</done>
</task>

<task type="auto">
  <name>Task 2: Verify build and type-check</name>
  <files></files>
  <read_first>
    - packages/shim/src/index.ts (after Task 1 edits)
    - packages/shim/src/discovery-shim.ts (from Plan 21-01)
  </read_first>
  <action>
    Run the following commands to verify the shim package builds and type-checks correctly with the new discovery module:

    1. `cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/shim build`
       Expected: Exit code 0, produces dist/index.js and dist/index.d.ts

    2. `cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/shim type-check`
       Expected: Exit code 0, no type errors

    3. Verify discovery exports appear in the built output:
       `grep "discoverServices" packages/shim/dist/index.js`
       `grep "ServiceInfo" packages/shim/dist/index.d.ts`

    If build fails:
    - Check that discovery-shim.ts imports use `.js` extension (ESM convention)
    - Check that BusKind.SERVICE_DISCOVERY exists in @napplet/core constants
    - Check that query() is correctly imported from relay-shim.ts

    If type-check fails:
    - Check ServiceInfo interface field types match NostrEvent tag parsing (string values)
    - Check that NostrEvent type imported from types.ts has the expected `tags` shape
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/shim build 2>&1 | tail -5 && pnpm --filter @napplet/shim type-check 2>&1 | tail -5 && grep "discoverServices" packages/shim/dist/index.js && grep "ServiceInfo" packages/shim/dist/index.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter @napplet/shim build exits with code 0
    - pnpm --filter @napplet/shim type-check exits with code 0 (no type errors)
    - packages/shim/dist/index.js contains discoverServices function
    - packages/shim/dist/index.d.ts contains ServiceInfo type export
    - packages/shim/dist/index.d.ts contains discoverServices, hasService, hasServiceVersion function exports
  </acceptance_criteria>
  <done>Shim package builds and type-checks with discovery API</done>
</task>

</tasks>

<verification>
The plan is complete when:
1. `grep "window.*napplet" packages/shim/src/index.ts` confirms global installation
2. `grep "export.*discoverServices" packages/shim/src/index.ts` confirms public re-export
3. `grep "export type.*ServiceInfo" packages/shim/src/index.ts` confirms type re-export
4. `pnpm --filter @napplet/shim build` exits 0
5. `pnpm --filter @napplet/shim type-check` exits 0
6. `grep "ServiceInfo" packages/shim/dist/index.d.ts` confirms type in build output
7. `pnpm build` (full turborepo build) exits 0 — no cross-package breakage
</verification>

<success_criteria>
- SHIM-01: Napplet can call discoverServices() and receive typed ServiceInfo[]
- SHIM-02: Napplet can call hasService(name) for availability check
- SHIM-03: Napplet can call hasServiceVersion(name, version) for version check
- SHIM-04: Discovery API accessible via window.napplet global — no imports needed
- D-01: window.napplet is cleanly separated from window.nostr and window.nostrdb
- Build passes with zero type errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-shim-discovery-api/21-02-SUMMARY.md`
</output>
