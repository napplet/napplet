---
plan: 02
phase: 36
title: "Delete packages/shell/src/state-proxy.ts dead code + full validation — TYPE-02"
wave: 2
depends_on: ["36-01"]
requirements_addressed: [TYPE-02]
files_modified:
  - packages/shell/src/state-proxy.ts
autonomous: true
must_haves:
  truths:
    - "packages/shell/src/state-proxy.ts does not exist (deleted)"
    - "pnpm type-check passes with zero errors across all 7 packages"
    - "All existing tests pass (Playwright e2e + vitest unit/integration)"
    - "ConsentRequest is defined only in @napplet/runtime (TYPE-01 confirmed from plan 36-01)"
  artifacts:
    - path: "packages/runtime/src/state-handler.ts"
      provides: "Runtime-owned state handler (the live replacement for shell/state-proxy.ts)"
      contains: "handleStateRequest"
  key_links:
    - from: "packages/runtime/src/runtime.ts"
      to: "packages/runtime/src/state-handler.ts"
      via: "import { handleStateRequest } from './state-handler.js'"
      pattern: "handleStateRequest.*from.*state-handler"
---

# Plan 36-02: Delete packages/shell/src/state-proxy.ts dead code + full validation — TYPE-02

## Objective

Verify that `packages/shell/src/state-proxy.ts` has zero import references across all packages, then delete it. Follow with full type-check and test suite validation to confirm the phase is complete.

Purpose: Dead code removal. `state-proxy.ts` was the pre-runtime state handler — extracted into `@napplet/runtime/src/state-handler.ts` during Phase 13. The shell version was never cleaned up and has zero imports since Phase 14. Deleting it eliminates a maintenance liability and a confusing second copy of state handling logic.
Output: File deleted; `pnpm type-check` and `pnpm test` pass.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-type-correctness/36-CONTEXT.md
@.planning/phases/36-type-correctness/36-RESEARCH.md

@packages/shell/src/state-proxy.ts
@packages/runtime/src/state-handler.ts
@packages/runtime/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 36-02-01: Verify zero imports of state-proxy.ts before deletion</name>
  <files></files>
  <read_first>
    - `packages/shell/src/state-proxy.ts` — file to delete; verify its exports and what would break if anything imports it
  </read_first>
  <action>
Before deleting, run an exhaustive grep to confirm state-proxy.ts has zero import references:

```bash
grep -rn "state-proxy" /home/sandwich/Develop/napplet/packages/ \
  --include="*.ts" \
  --exclude-dir=node_modules \
  --exclude-dir=dist
```

Expected result: zero output (no lines returned).

Also verify the runtime's state-handler.ts is the live implementation (not state-proxy.ts):

```bash
grep -rn "handleStateRequest\|cleanupNappState" /home/sandwich/Develop/napplet/packages/ \
  --include="*.ts" \
  --exclude-dir=node_modules \
  --exclude-dir=dist
```

Expected result: All hits in `packages/runtime/src/` only — `state-handler.ts` (definitions), `runtime.ts` (usage), `index.ts` (export). The hit in `packages/shell/src/state-proxy.ts` is the dead code itself (not an import of it).

**If zero import hits are confirmed:** Proceed to Task 36-02-02 (deletion).

**If import hits ARE found:** STOP. Do not delete. Write a blocker to `.planning/STATE.md`:
```
BLOCKER: state-proxy.ts has live imports at: [list grep hits]
Resolution: Update those importers to use @napplet/runtime state-handler instead, then re-run this plan.
```
  </action>
  <verify>
    grep -rn "state-proxy" /home/sandwich/Develop/napplet/packages/ --include="*.ts" --exclude-dir=node_modules --exclude-dir=dist
  </verify>
  <acceptance_criteria>
    - `grep -rn "state-proxy" packages/ --include="*.ts"` returns zero results (excluding node_modules, dist)
    - OR: Blocker written to STATE.md if imports found (stop here)
  </acceptance_criteria>
  <done>Zero import hits confirmed; safe to delete state-proxy.ts</done>
</task>

<task type="auto">
  <name>Task 36-02-02: Delete packages/shell/src/state-proxy.ts</name>
  <files>packages/shell/src/state-proxy.ts</files>
  <read_first>
    - `packages/shell/src/state-proxy.ts` — the file being deleted; read to confirm it is the correct file (not accidentally something else with the same name)
  </read_first>
  <action>
Delete `packages/shell/src/state-proxy.ts`:

```bash
rm /home/sandwich/Develop/napplet/packages/shell/src/state-proxy.ts
```

This file exports `handleStateRequest` and `cleanupNappState`. Both are dead exports — nothing imports this file. The runtime's `state-handler.ts` is the live implementation, used by `runtime.ts` and exported from `runtime/index.ts`.

After deletion, verify:
```bash
ls /home/sandwich/Develop/napplet/packages/shell/src/
```

Expected: `state-proxy.ts` is NOT in the listing.
  </action>
  <verify>
    ls /home/sandwich/Develop/napplet/packages/shell/src/ | grep state-proxy
  </verify>
  <acceptance_criteria>
    - `ls packages/shell/src/` does NOT contain `state-proxy.ts`
    - The file is gone from the filesystem
  </acceptance_criteria>
  <done>state-proxy.ts deleted</done>
</task>

<task type="auto">
  <name>Task 36-02-03: Type-check all packages after deletion</name>
  <files></files>
  <read_first></read_first>
  <action>
Run the type-checker across all packages to confirm the deletion introduced no errors:

```bash
cd /home/sandwich/Develop/napplet
pnpm type-check 2>&1
```

Expected: All packages pass with zero errors.

Common issues to watch for (should not happen given zero imports, but check):
- Any import of `state-proxy` that the pre-deletion grep missed (unlikely — would have been caught)
- Build cache referencing the deleted file (run `pnpm build` first if type-check references stale dist files)
  </action>
  <verify>
    pnpm type-check 2>&1 | grep -E "error TS|✓|passed|0 errors" | tail -20
  </verify>
  <acceptance_criteria>
    - `pnpm type-check` exits with code 0
    - No errors referencing state-proxy, state-handler, or any file modified in this phase
  </acceptance_criteria>
  <done>pnpm type-check passes with zero errors after state-proxy.ts deletion</done>
</task>

<task type="auto">
  <name>Task 36-02-04: Run full test suite to confirm no regressions</name>
  <files></files>
  <read_first></read_first>
  <action>
Run the full test suite to confirm zero regressions from all Phase 36 changes (TYPE-01 from plan 36-01 + TYPE-02 from this plan):

```bash
cd /home/sandwich/Develop/napplet
pnpm build && pnpm test 2>&1 | tail -40
```

Expected: All tests pass. The test suite includes Playwright e2e tests and vitest unit/integration tests.

If tests fail with a non-Phase-36 cause (e.g., a Phase 35 issue or pre-existing failure), note in the summary but do not block — Phase 36 changes are additive-removal only and cannot break existing behavior.
  </action>
  <verify>
    pnpm test 2>&1 | grep -E "passed|failed|error" | tail -10
  </verify>
  <acceptance_criteria>
    - All existing tests pass (zero new failures introduced by Phase 36 changes)
    - `pnpm build` succeeds
    - `pnpm test` exits with no failures attributable to TYPE-01 or TYPE-02 changes
  </acceptance_criteria>
  <done>Full test suite passes after Phase 36 changes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ls packages/shell/src/ | grep state-proxy` returns nothing (file deleted)
- [ ] `grep -rn "state-proxy" packages/ --include="*.ts"` returns zero results
- [ ] `pnpm type-check` passes with zero errors
- [ ] `pnpm test` passes with zero new failures
- [ ] Phase 36 success criteria from ROADMAP.md all satisfied:
  - [ ] ConsentRequest defined only in @napplet/runtime (TYPE-01)
  - [ ] shell/state-proxy.ts removed (TYPE-02)
  - [ ] pnpm type-check passes and all tests pass
</verification>

<success_criteria>
- All four tasks completed
- state-proxy.ts deleted with zero import regressions (TYPE-02 requirement met)
- pnpm type-check passes across all 7 packages
- Full test suite passes
- Phase 36 complete — all v0.7.0 TYPE requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/36-type-correctness/36-02-SUMMARY.md`
</output>
