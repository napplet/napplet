---
plan: 01
phase: 35
title: Rename BusKind.INTER_PANE → BusKind.IPC_PEER — Core and All Consumers
wave: 1
depends_on: []
requirements_addressed: [WIRE-01]
files_modified:
  - packages/core/src/constants.ts
  - packages/core/src/topics.ts
  - packages/runtime/src/runtime.ts
  - packages/runtime/src/enforce.ts
  - packages/runtime/src/service-dispatch.ts
  - packages/runtime/src/state-handler.ts
  - packages/services/src/audio-service.ts
  - packages/services/src/notification-service.ts
  - packages/shell/src/audio-manager.ts
  - packages/shell/src/state-proxy.ts
  - packages/shim/src/index.ts
autonomous: true
---

# Plan 35-01: Rename BusKind.INTER_PANE → BusKind.IPC_PEER — Core and All Consumers

## Objective

Perform the core constant rename in `packages/core/src/constants.ts` and update every production consumer in the same pass. After this plan, `pnpm type-check` must pass — zero references to `BusKind.INTER_PANE` remain in production code.

No deprecated alias is shipped (D-01). The numeric value `29003` is unchanged.

---

## Task 35-01-01: Rename constant in packages/core/src/constants.ts

<read_first>
- `packages/core/src/constants.ts` — current content; `BusKind.INTER_PANE` defined at line 57; JSDoc example references `BusKind.INTER_PANE` at line 50
</read_first>

<action>
In `packages/core/src/constants.ts`:

1. Replace the JSDoc block comment above `BusKind`:
   ```
   /**
    * Bus event kinds for the napplet-shell inter-process protocol.
    * All bus kinds are in the 29000-29999 ephemeral range.
    * Ephemeral events are auto-discarded by real relays per NIP-01 spec —
    * perfect for bus traffic that should never persist beyond the ShellBridge.
    * @example
    * ```ts
    * if (event.kind === BusKind.INTER_PANE) { // handle inter-pane message }
    * ```
    */
   ```
   Replace with:
   ```
   /**
    * Bus event kinds for the napplet-shell inter-process protocol.
    * All bus kinds are in the 29000-29999 ephemeral range.
    * Ephemeral events are auto-discarded by real relays per NIP-01 spec —
    * perfect for bus traffic that should never persist beyond the ShellBridge.
    *
    * ### IPC-* Namespace
    * The `IPC-*` prefix is reserved for the inter-napplet communication bus.
    *
    * | Constant | Kind | Status | Description |
    * |----------|------|--------|-------------|
    * | `IPC_PEER` | 29003 | Current | Directed peer-to-peer IPC between napplets and the shell |
    * | `IPC_BROADCAST` | TBD | Reserved | Future: broadcast to all napplets |
    * | `IPC_CHANNEL` | TBD | Reserved | Future: named channel pubsub |
    *
    * See SPEC.md for full IPC-* namespace semantics.
    *
    * @example
    * ```ts
    * if (event.kind === BusKind.IPC_PEER) { // handle IPC-PEER message }
    * ```
    */
   ```

2. Inside the `BusKind` object, rename the key:
   ```
   INTER_PANE: 29003,
   ```
   Replace with:
   ```
   IPC_PEER: 29003,
   ```
   The numeric value 29003 is unchanged.

No other changes to this file.
</action>

<acceptance_criteria>
- `packages/core/src/constants.ts` contains `IPC_PEER: 29003`
- `packages/core/src/constants.ts` does NOT contain `INTER_PANE`
- `packages/core/src/constants.ts` contains the IPC-* namespace table in JSDoc
- `grep 'INTER_PANE' packages/core/src/constants.ts` returns no results
</acceptance_criteria>

---

## Task 35-01-02: Update prose in packages/core/src/topics.ts

<read_first>
- `packages/core/src/topics.ts` — current content; header JSDoc references "inter-pane event bus" and "INTER_PANE events" at lines 2-5 and 10
</read_first>

<action>
In `packages/core/src/topics.ts`, update the file header comment block only (lines 1-7 approximately):

1. Line 2: `* @napplet/core — Topic constants for the napplet inter-pane event bus.`
   → `* @napplet/core — Topic constants for the napplet IPC-PEER event bus.`

2. Lines 4-5:
   ```
   * These constants define the topic strings used in inter-pane
   * INTER_PANE events for shell commands, state operations, audio,
   ```
   → 
   ```
   * These constants define the topic strings used in IPC-PEER
   * events for shell commands, state operations, audio,
   ```

3. Line 10: `* Built-in topic constants for the napplet shell inter-pane protocol.`
   → `* Built-in topic constants for the napplet shell IPC-PEER protocol.`

NOTE: Do not change any TOPICS constant keys or values. No code changes, prose only.
</action>

<acceptance_criteria>
- `grep 'inter-pane\|INTER_PANE' packages/core/src/topics.ts` returns no results
- All TOPICS constant keys and values are unchanged
</acceptance_criteria>

---

## Task 35-01-03: Update packages/runtime/src/runtime.ts

<read_first>
- `packages/runtime/src/runtime.ts` — contains `case BusKind.INTER_PANE:` at line 466 and synthetic event objects with `kind: BusKind.INTER_PANE` at lines 724, 832, 881
</read_first>

<action>
In `packages/runtime/src/runtime.ts`, replace all occurrences of `BusKind.INTER_PANE` with `BusKind.IPC_PEER`.

There are 4 occurrences:
- `case BusKind.INTER_PANE:` — rename to `case BusKind.IPC_PEER:`
- Three `kind: BusKind.INTER_PANE,` in synthetic event object literals — rename each to `kind: BusKind.IPC_PEER,`

No comment or JSDoc changes needed in this file.
</action>

<acceptance_criteria>
- `grep 'INTER_PANE' packages/runtime/src/runtime.ts` returns no results
- `pnpm type-check` passes (or only fails on files not yet updated in this task set)
</acceptance_criteria>

---

## Task 35-01-04: Update packages/runtime/src/enforce.ts

<read_first>
- `packages/runtime/src/enforce.ts` — `if (event.kind === BusKind.INTER_PANE)` at line 75; inline comment "Non-state inter-pane messages" at line 90
</read_first>

<action>
In `packages/runtime/src/enforce.ts`:

1. Line 75: `if (event.kind === BusKind.INTER_PANE) {`
   → `if (event.kind === BusKind.IPC_PEER) {`

2. Line 90 comment: `// Non-state inter-pane messages (including audio, shell commands)`
   → `// Non-state IPC-PEER messages (including audio, shell commands)`
</action>

<acceptance_criteria>
- `grep 'INTER_PANE\|inter-pane' packages/runtime/src/enforce.ts` returns no results
</acceptance_criteria>

---

## Task 35-01-05: Update packages/runtime/src/service-dispatch.ts

<read_first>
- `packages/runtime/src/service-dispatch.ts` — comment-only changes in file header and JSDoc; 3 occurrences at lines 4, 13, 20
</read_first>

<action>
In `packages/runtime/src/service-dispatch.ts`, update the three JSDoc/comment occurrences:

1. Line 4 (file header): `* Routes INTER_PANE events to the correct ServiceHandler based on the topic`
   → `* Routes IPC-PEER events to the correct ServiceHandler based on the topic`

2. Line 13 (function JSDoc): `* Route an INTER_PANE event to the matching service handler by topic prefix.`
   → `* Route an IPC-PEER event to the matching service handler by topic prefix.`

3. Line 20 (`@param` JSDoc): `* @param event - The INTER_PANE event to route`
   → `* @param event - The IPC-PEER event to route`

No code changes (no BusKind references in this file's executable lines).
</action>

<acceptance_criteria>
- `grep 'INTER_PANE' packages/runtime/src/service-dispatch.ts` returns no results
</acceptance_criteria>

---

## Task 35-01-06: Update packages/runtime/src/state-handler.ts

<read_first>
- `packages/runtime/src/state-handler.ts` — `kind: BusKind.INTER_PANE` at line 38
</read_first>

<action>
In `packages/runtime/src/state-handler.ts`:

Line 38: `kind: BusKind.INTER_PANE,`
→ `kind: BusKind.IPC_PEER,`
</action>

<acceptance_criteria>
- `grep 'INTER_PANE' packages/runtime/src/state-handler.ts` returns no results
</acceptance_criteria>

---

## Task 35-01-07: Update packages/services/src/audio-service.ts

<read_first>
- `packages/services/src/audio-service.ts` — occurrences at lines 74 (JSDoc), 81 (`kind:`), 170 (comment), 171 (`if` check)
</read_first>

<action>
In `packages/services/src/audio-service.ts`:

1. Line 74 JSDoc: `* Create a synthetic INTER_PANE event to send back to a napplet.`
   → `* Create a synthetic IPC-PEER event to send back to a napplet.`

2. Line 81: `kind: BusKind.INTER_PANE,`
   → `kind: BusKind.IPC_PEER,`

3. Line 170 comment: `// Only handle INTER_PANE events with audio:* topics`
   → `// Only handle IPC-PEER events with audio:* topics`

4. Line 171: `if (event.kind !== BusKind.INTER_PANE) return;`
   → `if (event.kind !== BusKind.IPC_PEER) return;`
</action>

<acceptance_criteria>
- `grep 'INTER_PANE' packages/services/src/audio-service.ts` returns no results
</acceptance_criteria>

---

## Task 35-01-08: Update packages/services/src/notification-service.ts

<read_first>
- `packages/services/src/notification-service.ts` — occurrences at lines 101 (JSDoc), 108 (`kind:`), 257 (comment), 258 (`if` check)
</read_first>

<action>
In `packages/services/src/notification-service.ts`:

1. Line 101 JSDoc: `* Create a synthetic INTER_PANE event to send back to a napplet.`
   → `* Create a synthetic IPC-PEER event to send back to a napplet.`

2. Line 108: `kind: BusKind.INTER_PANE,`
   → `kind: BusKind.IPC_PEER,`

3. Line 257 comment: `// Only handle INTER_PANE events with notifications:* topics`
   → `// Only handle IPC-PEER events with notifications:* topics`

4. Line 258: `if (event.kind !== BusKind.INTER_PANE) return;`
   → `if (event.kind !== BusKind.IPC_PEER) return;`
</action>

<acceptance_criteria>
- `grep 'INTER_PANE' packages/services/src/notification-service.ts` returns no results
</acceptance_criteria>

---

## Task 35-01-09: Update packages/shell/src/audio-manager.ts

<read_first>
- `packages/shell/src/audio-manager.ts` — `kind: BusKind.INTER_PANE` at line 102
</read_first>

<action>
In `packages/shell/src/audio-manager.ts`:

Line 102: `kind: BusKind.INTER_PANE,`
→ `kind: BusKind.IPC_PEER,`
</action>

<acceptance_criteria>
- `grep 'INTER_PANE' packages/shell/src/audio-manager.ts` returns no results
</acceptance_criteria>

---

## Task 35-01-10: Update packages/shell/src/state-proxy.ts

<read_first>
- `packages/shell/src/state-proxy.ts` — `kind: BusKind.INTER_PANE` at line 38
</read_first>

<action>
In `packages/shell/src/state-proxy.ts`:

Line 38: `kind: BusKind.INTER_PANE,`
→ `kind: BusKind.IPC_PEER,`
</action>

<acceptance_criteria>
- `grep 'INTER_PANE' packages/shell/src/state-proxy.ts` returns no results
</acceptance_criteria>

---

## Task 35-01-11: Update packages/shim/src/index.ts

<read_first>
- `packages/shim/src/index.ts` — occurrences at lines 31 (JSDoc), 50 (`BusKind.INTER_PANE`), 54 (JSDoc), 56 (JSDoc), 77 (`BusKind.INTER_PANE`), 87 (inline comment)
</read_first>

<action>
In `packages/shim/src/index.ts`:

1. Line 31 JSDoc: `* Broadcast an inter-pane event to other napps via the shell.`
   → `* Broadcast an IPC-PEER event to other napplets via the shell.`

2. Line 50: `sendEvent(BusKind.INTER_PANE, [['t', topic], ...extraTags], content);`
   → `sendEvent(BusKind.IPC_PEER, [['t', topic], ...extraTags], content);`

3. Line 54 JSDoc: `* Subscribe to inter-pane events on a specific topic.`
   → `* Subscribe to IPC-PEER events on a specific topic.`

4. Line 56 JSDoc: `* Thin wrapper around subscribe() that filters by inter-pane event kind`
   → `* Thin wrapper around subscribe() that filters by IPC-PEER event kind`

5. Line 77: `{ kinds: [BusKind.INTER_PANE], '#t': [topic] },`
   → `{ kinds: [BusKind.IPC_PEER], '#t': [topic] },`

6. Line 87 inline comment: `/* EOSE — no action needed for inter-pane subscriptions */`
   → `/* EOSE — no action needed for IPC-PEER subscriptions */`
</action>

<acceptance_criteria>
- `grep 'INTER_PANE\|inter-pane' packages/shim/src/index.ts` returns no results
</acceptance_criteria>

---

## Wave 1 Final Validation

After completing all tasks in this plan, run:

```bash
cd /home/sandwich/Develop/napplet
pnpm type-check 2>&1 | tail -10
```

Expected: All packages pass type-check (tests may still have failures — those are fixed in Plan 35-02).

Also run the negative grep to confirm no remaining hits in production files:
```bash
grep -r 'INTER_PANE\|INTER-PANE\|inter-pane' packages/ --include="*.ts" \
  --exclude-dir=node_modules --exclude-dir=dist \
  | grep -v '\.test\.ts'
```
Expected: zero results.
