---
phase: 38-session-vocabulary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/runtime/src/runtime.ts
  - packages/runtime/src/dispatch.test.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-02

# Goal-backward verification
must_haves:
  truths:
    - "grep -rn 'nappKeyRegistry' packages/runtime/ returns zero hits"
    - "runtime.sessionRegistry is the public accessor for the session identity registry"
    - "pnpm type-check passes with zero errors"
    - "pnpm --filter @napplet/runtime test:unit passes"
  artifacts:
    - path: "packages/runtime/src/runtime.ts"
      provides: "Runtime interface with sessionRegistry property (not nappKeyRegistry)"
      contains: "readonly sessionRegistry: SessionRegistry"
    - path: "packages/runtime/src/dispatch.test.ts"
      provides: "Dispatch tests using updated sessionRegistry accessor"
      contains: "runtime.sessionRegistry.getPubkey"
  key_links:
    - from: "packages/runtime/src/runtime.ts"
      to: "packages/runtime/src/session-registry.ts"
      via: "createSessionRegistry() assigned to const sessionRegistry"
      pattern: "const sessionRegistry = createSessionRegistry"
---

<objective>
Rename the `nappKeyRegistry` local variable and public interface property to `sessionRegistry` throughout `packages/runtime/src/runtime.ts`, and update the one test file consumer (`dispatch.test.ts`).

Purpose: Complete the Phase 34 rename map for the runtime layer — `nappKeyRegistry` was listed as a target but left unexecuted. The rename aligns the internal variable name with the already-renamed types (`SessionRegistry`, `SessionEntry`) and the established Phase 35 clean-rename precedent.

Output: `runtime.ts` with `sessionRegistry` throughout (20 occurrences), `dispatch.test.ts` updated (1 occurrence). Type-check and unit tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-session-vocabulary/38-CONTEXT.md
@.planning/phases/38-session-vocabulary/38-RESEARCH.md

@packages/runtime/src/runtime.ts
@packages/runtime/src/dispatch.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename nappKeyRegistry → sessionRegistry in runtime.ts</name>
  <files>packages/runtime/src/runtime.ts</files>
  <read_first>packages/runtime/src/runtime.ts</read_first>
  <action>
Perform a clean, complete rename of `nappKeyRegistry` → `sessionRegistry` across ALL occurrences
in `packages/runtime/src/runtime.ts`. This covers:

1. The `Runtime` interface property at line ~111:
   - OLD: `readonly nappKeyRegistry: SessionRegistry;`
   - NEW: `readonly sessionRegistry: SessionRegistry;`
   - Update the JSDoc comment above it: change "nappKeyRegistry" to "sessionRegistry" if present

2. The local variable declaration at line ~170:
   - OLD: `const nappKeyRegistry = createSessionRegistry(hooks.onPendingUpdate);`
   - NEW: `const sessionRegistry = createSessionRegistry(hooks.onPendingUpdate);`

3. All 18 references to the local variable throughout the function body (lines ~179, 187, 275,
   281, 282, 288, 308, 388, 390, 434, 469, 483, 511, 627, 735, 755, 855)

4. The public getter at line ~956:
   - OLD: `get nappKeyRegistry() { return nappKeyRegistry; }`
   - NEW: `get sessionRegistry() { return sessionRegistry; }`

Use a global search-and-replace within the file. Total occurrences: 20.

DO NOT touch:
- `createSessionRegistry` (function name — remains unchanged)
- `SessionRegistry` type import/usage (type name — already renamed in Phase 34)
- Any files in `packages/shell/` (those are Phase 34 deprecated aliases — must stay)
  </action>
  <verify>grep -n "nappKeyRegistry" packages/runtime/src/runtime.ts | wc -l  # must print 0</verify>
  <acceptance_criteria>
    - "packages/runtime/src/runtime.ts contains 'readonly sessionRegistry: SessionRegistry'"
    - "packages/runtime/src/runtime.ts contains 'const sessionRegistry = createSessionRegistry'"
    - "packages/runtime/src/runtime.ts contains 'get sessionRegistry()'"
    - "grep -c 'nappKeyRegistry' packages/runtime/src/runtime.ts returns 0"
  </acceptance_criteria>
  <done>All 20 occurrences of nappKeyRegistry replaced with sessionRegistry in runtime.ts, zero nappKeyRegistry hits remain</done>
</task>

<task type="auto">
  <name>Task 2: Update dispatch.test.ts to use sessionRegistry</name>
  <files>packages/runtime/src/dispatch.test.ts</files>
  <read_first>packages/runtime/src/dispatch.test.ts</read_first>
  <action>
Update the one occurrence of `runtime.nappKeyRegistry` in `dispatch.test.ts` to
`runtime.sessionRegistry`.

Line ~106 (inside the 'accepts valid AUTH response and registers napplet identity' test):
  - OLD: `const pubkey = runtime.nappKeyRegistry.getPubkey(WINDOW_ID);`
  - NEW: `const pubkey = runtime.sessionRegistry.getPubkey(WINDOW_ID);`

The comment on line ~105 says `// After auth, the sessionRegistry should have the entry` —
this comment is already correct and does NOT need changing.
  </action>
  <verify>grep -n "nappKeyRegistry" packages/runtime/src/dispatch.test.ts | wc -l  # must print 0</verify>
  <acceptance_criteria>
    - "packages/runtime/src/dispatch.test.ts contains 'runtime.sessionRegistry.getPubkey'"
    - "grep -c 'nappKeyRegistry' packages/runtime/src/dispatch.test.ts returns 0"
  </acceptance_criteria>
  <done>dispatch.test.ts updated to runtime.sessionRegistry with zero nappKeyRegistry hits remaining</done>
</task>

<task type="auto">
  <name>Task 3: Verify type-check and unit tests pass</name>
  <files></files>
  <read_first></read_first>
  <action>
Run the following verification commands in sequence:

1. Build the runtime package:
   `cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/runtime build`

2. Type-check the runtime package:
   `cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/runtime type-check`

3. Run the runtime unit tests:
   `cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/runtime test:unit`

4. Confirm zero nappKeyRegistry hits remain in runtime package:
   `grep -rn "nappKeyRegistry" /home/sandwich/Develop/napplet/packages/runtime/ --include="*.ts"`

If type-check fails, read the error output carefully. Likely causes:
- A missed occurrence of nappKeyRegistry in runtime.ts
- A shell file that somehow imports the runtime property by old name (should not be the case)

The shell's deprecated alias exports (`packages/shell/src/session-registry.ts:170` and
`packages/shell/src/index.ts:44`) still export `nappKeyRegistry` as a deprecated const — this is
intentional and expected, and does NOT indicate a problem.
  </action>
  <verify>grep -rn "nappKeyRegistry" packages/runtime/ --include="*.ts" | wc -l  # must print 0</verify>
  <acceptance_criteria>
    - "pnpm --filter @napplet/runtime build exits with code 0"
    - "pnpm --filter @napplet/runtime type-check exits with code 0"
    - "pnpm --filter @napplet/runtime test:unit exits with code 0"
    - "grep -rn nappKeyRegistry packages/runtime/ returns 0 hits"
  </acceptance_criteria>
  <done>All verification passes: build, type-check, and unit tests green. Zero nappKeyRegistry hits in runtime package.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -rn "nappKeyRegistry" packages/runtime/` returns zero hits
- [ ] `pnpm --filter @napplet/runtime type-check` exits 0
- [ ] `pnpm --filter @napplet/runtime test:unit` exits 0
- [ ] `packages/shell/src/session-registry.ts` line 170 still has `export const nappKeyRegistry = sessionRegistry` (deprecated alias, must be preserved)
</verification>

<success_criteria>
- All 20 occurrences of nappKeyRegistry renamed to sessionRegistry in runtime.ts
- dispatch.test.ts updated (1 occurrence)
- TypeScript type-check passes
- Runtime unit tests pass
- Shell deprecated aliases are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/38-session-vocabulary/38-01-SUMMARY.md`
</output>
