---
phase: 39-documentation-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/topics.ts
autonomous: true
requirements: [DOC-01, DOC-02]

must_haves:
  truths:
    - "TOPICS const JSDoc contains a '## Topic Prefix Conventions' section with a three-row markdown table"
    - "nappStorage and nappState in state-shim.ts both have @deprecated JSDoc pointing to nappletState"
  artifacts:
    - path: "packages/core/src/topics.ts"
      provides: "Topic prefix direction semantics documented in TOPICS const JSDoc"
      contains: "Topic Prefix Conventions"
    - path: "packages/shim/src/state-shim.ts"
      provides: "Deprecated aliases for nappState and nappStorage"
      contains: "@deprecated Use nappletState"
  key_links: []
---

<objective>
Add topic prefix direction semantics to the TOPICS const JSDoc in packages/core/src/topics.ts, and verify that nappStorage/nappState deprecated annotations are already present in packages/shim/src/state-shim.ts.

Purpose: Document the implicit protocol convention that shell:* means napplet→shell, napplet:* means shell→napplet, and {service}:* is bidirectional. This is the only thing new contributors need to understand the message-direction contract.

Output: Updated packages/core/src/topics.ts with "## Topic Prefix Conventions" section in TOPICS JSDoc. No change to state-shim.ts (verified already complete).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/topics.ts
@packages/shim/src/state-shim.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Topic Prefix Conventions section to TOPICS JSDoc</name>
  <files>packages/core/src/topics.ts</files>
  <read_first>packages/core/src/topics.ts</read_first>
  <action>
Insert a "## Topic Prefix Conventions" section into the existing TOPICS const JSDoc block, placed after the closing ``` of the @example block and before the closing */ of the JSDoc.

The existing JSDoc ends with:
```
 * ```
 */
export const TOPICS = {
```

Replace the closing ` * ``` */` with:

```
 * ```
 *
 * ## Topic Prefix Conventions
 *
 * Topic strings follow a prefix convention that signals message direction:
 *
 * | Prefix | Direction | Meaning |
 * |--------|-----------|---------|
 * | `shell:*` | napplet → shell | Commands sent by a napplet to the shell |
 * | `napplet:*` | shell → napplet | Responses/notifications sent by shell to napplet |
 * | `{service}:*` | bidirectional | Service-scoped messages; direction is per-topic within the prefix |
 *
 * Examples of `{service}:*` prefixes: `auth:*`, `stream:*`, `profile:*`, `wm:*`,
 * `keybinds:*`, `chat:*`, `audio:*`.
 */
```

No other changes to the file — do not alter any topic values, types, or section comments.
  </action>
  <verify>grep -n "Topic Prefix Conventions" packages/core/src/topics.ts</verify>
  <acceptance_criteria>
    - packages/core/src/topics.ts contains the exact string "## Topic Prefix Conventions"
    - packages/core/src/topics.ts contains "napplet → shell"
    - packages/core/src/topics.ts contains "shell → napplet"
    - packages/core/src/topics.ts contains "bidirectional"
    - packages/core/src/topics.ts contains "auth:*" in the examples line
    - The TOPICS const object itself is unchanged: AUTH_IDENTITY_CHANGED, STATE_GET, AUDIO_MUTED etc. still present
    - TypeScript build passes: run `pnpm --filter @napplet/core build` (or `pnpm build` from root) with no errors
  </acceptance_criteria>
  <done>TOPICS const JSDoc documents prefix direction semantics with three-row table and service prefix examples</done>
</task>

<task type="auto">
  <name>Task 2: Verify @deprecated annotations on nappStorage and nappState (DOC-02)</name>
  <files>packages/shim/src/state-shim.ts</files>
  <read_first>packages/shim/src/state-shim.ts</read_first>
  <action>
Read packages/shim/src/state-shim.ts and confirm that both nappState and nappStorage exports have @deprecated JSDoc.

Expected state (lines approximately 168-178):
```ts
/**
 * @deprecated Use nappletState. Will be removed in v0.9.0.
 * @see nappletState
 */
export const nappState = nappletState;

/**
 * @deprecated Use nappletState. Will be removed in v0.9.0.
 * @see nappletState
 */
export const nappStorage = nappletState;
```

If BOTH annotations are present exactly as above: no edits required. DOC-02 is already complete.

If either annotation is missing or differs: add/correct it to match the expected text above. The @deprecated message must read "Use nappletState. Will be removed in v0.9.0." and include @see nappletState.
  </action>
  <verify>grep -A3 "export const nappState\|export const nappStorage" packages/shim/src/state-shim.ts</verify>
  <acceptance_criteria>
    - packages/shim/src/state-shim.ts contains "@deprecated Use nappletState. Will be removed in v0.9.0." (appears twice, once for each alias)
    - packages/shim/src/state-shim.ts contains "@see nappletState" (appears at least twice)
    - Both nappState and nappStorage are exported as aliases of nappletState
  </acceptance_criteria>
  <done>nappStorage and nappState deprecated aliases confirmed present with correct JSDoc</done>
</task>

</tasks>

<verification>
- [ ] grep "Topic Prefix Conventions" packages/core/src/topics.ts returns a match
- [ ] grep "napplet → shell" packages/core/src/topics.ts returns a match
- [ ] grep "@deprecated Use nappletState" packages/shim/src/state-shim.ts returns 2 matches
- [ ] pnpm --filter @napplet/core type-check passes (no TypeScript errors)
- [ ] pnpm --filter @napplet/shim type-check passes (no TypeScript errors)
</verification>

<success_criteria>
- All tasks completed
- DOC-01: TOPICS const JSDoc contains "## Topic Prefix Conventions" section with three-row direction table and {service}:* examples
- DOC-02: nappStorage and nappState both confirmed @deprecated with removal schedule v0.9.0
- No runtime behavior changes — pure JSDoc additions
- TypeScript build clean
</success_criteria>

<output>
After completion, create `.planning/phases/39-documentation-pass/39-01-SUMMARY.md`
</output>
