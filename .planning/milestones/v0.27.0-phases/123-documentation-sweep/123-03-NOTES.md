# Phase 123 Plan 03 — Active Planning Sweep NOTES

**Purpose:** Document the terminology-swap scope and the deliberate historical-preservation exceptions so Phase 124 inherits the trade-off transparently.

## What this plan rewrote

| File | Edits | Scope |
|---|---|---|
| .planning/codebase/ARCHITECTURE.md | 5 | Current-tense API-surface + routing descriptions (`inter-pane` → `inter-frame`) |
| .planning/codebase/STRUCTURE.md | 2 | Current-tense pubsub API list (`inter-pane` → `inter-frame`) |
| .planning/codebase/INTEGRATIONS.md | 3 | Current-tense integration descriptions (`inter-pane` → `inter-frame`; line 168 `INTER_PANE` historical constant preserved) |
| .planning/codebase/CONCERNS.md | 6 | Current-tense fragile-areas + scaling-limits (`inter-pane` → `inter-frame`) |
| .planning/codebase/TESTING.md | 2 | Transport-layer descriptions (generic `IPC` → accurate `postMessage`) on lines 83, 152 |
| .planning/research/ARCHITECTURE.md | 1 | NappletGlobal interface code-fence snippet (`ipc:` → `ifc:`, column alignment preserved) |
| .planning/research/FEATURES-CHANNELS.md | 9 | Pub/sub comparison-table current-API cells (2 edits, plan-specified) + extended sweep of 7 additional current-tense `IPC` references (deviation: see Rule 2 note below) |
| **Total** | **28** | Terminology / transport-accuracy only |

## What this plan PRESERVED (historical-preservation map)

Files left byte-identical to pre-phase state:

- `.planning/PROJECT.md` — already clean at milestone kickoff. Historical Validated/Shipped bullets left as dated record of past milestones (Phase 35 `BusKind.INTER_PANE` rename, Phase 41 `window.napplet.ipc` shipping at v0.8.0, Phase 45 IPC terminology cleanup quick-task, Phase 84 IPC gap investigation, v0.1.0 `inter-pane` test-coverage bullet).
- `.planning/STATE.md` — already clean at milestone kickoff.
- `.planning/ROADMAP.md` — already clean at milestone kickoff. Phase 45 "IPC terminology cleanup" note on line 39 is a historical quick-task reference.
- `.planning/research/ONTOLOGY.md` — dated 2026-03-29 ontology investigation (line 94 argues `INTER_PANE → IPC_PEER` is a good rename; line 404 discusses the same argument; line 387 table row is a dated bus-kind message-type assessment; line 462 external "Android Binder IPC" citation).
- `.planning/research/SDK_NAMING_PATTERNS.md` — external "Electron IPC Tutorial" citation on line 436.
- `.planning/research/SUMMARY.md` — 2026-04-05 executive summary; single `inter-napplet communication` reference inside dated research framing paragraph (line 10).
- `.planning/SPEC-GAPS.md` — GAP-09 `REMOVED — Spec-Backed` entry dated Phase 84 / 2026-04-08; contains `inter-napplet communication` in dated investigation prose (lines 543, 615).
- `.planning/codebase/INTEGRATIONS.md` line 168 — `29003: INTER_PANE` message-kind description; `INTER_PANE` is the historical constant name (renamed to `IPC_PEER` in v0.7.0 Phase 35; subsequently swept at the source level in v0.27.0 Phase 122).

**Note:** `.planning/codebase/TESTING.md` is NOT preserved — its two generic `IPC` references (lines 83, 152) were rewritten to `postMessage`, the accurate transport term. `.planning/research/FEATURES-CHANNELS.md` is NOT preserved — all 9 current-tense `IPC` references were rewritten (2 plan-specified + 7 deviation). See "What this plan rewrote" above.

## Deviation record

**Rule 2 auto-add (missing critical work):** The original plan specified only 2 edits to `.planning/research/FEATURES-CHANNELS.md` (lines 132 + 140) based on the planner's narrow `read_first` scope (lines 130-145). The plan's Task 3 verification grep, however, listed this file in the zero-grep acceptance set — meaning the planner intended full-file cleanliness but their scoped read missed the other 7 current-tense `IPC` references scattered across the file (lines 3, 17, 18, 20, 30, 34, 50, 177, 185).

**Resolution applied (no user permission needed — Rule 2 inline fix):**
- 3 references to the existing napplet pub/sub system (kind 29003 context — "existing NIP-01 IPC", "current IPC is too heavy", "Current kind 29003 IPC costs", "Current IPC (kind 29003)" section heading) rewritten to `IFC` — matches the line 140 Status row the plan DID rewrite.
- 5 references using `IPC` as a generic transport-category shorthand ("authenticated IPC channels", "multiplexing over raw IPC", "anti-pattern for IPC", "generic IPC", "WebRTC DataChannel for local IPC") rewritten to `postMessage` / "iframe messaging" — applies the TESTING.md lines 83 + 152 precedent (plan-sanctioned: generic `IPC` as catch-all transport term → accurate `postMessage`).

Historical preservation still honored — none of the dated-research files on the preservation list were touched by this deviation.

**Commit trail:**
- Task 1 (codebase sweep, 18 edits, 5 files): `13ba291`
- Task 2 plan-specified (research sweep, 3 edits, 2 files): `a78394d`
- Task 2 deviation extension (FEATURES-CHANNELS.md, 7 additional edits): `ef91495`
- Task 3 (this NOTES.md + SUMMARY.md + state updates): pending

## Phase 124 trade-off

When Phase 124 runs its repo-wide zero-grep across `packages/`, `specs/`, `skills/`, `README.md`, and active `.planning/` docs, it WILL find the following residual matches that this plan deliberately preserved:

| File | Residual match | Justification |
|---|---|---|
| .planning/PROJECT.md (multiple lines) | `ipc`, `INTER_PANE`, `Inter-pane` | Historical Shipped/Validated bullets describing past milestones (Phase 35 rename event, Phase 41 v0.8.0 shipping, Phase 45 quick-task, Phase 84 gap investigation, v0.1.0 test-coverage) |
| .planning/research/ONTOLOGY.md lines 94, 387, 404, 462 | `IPC`, `INTER_PANE`, `IPC_PEER`, "Binder IPC" | Dated 2026-03-29 ontology investigation + external ecosystem citation |
| .planning/research/SDK_NAMING_PATTERNS.md line 436 | `IPC` | External "Electron IPC Tutorial" citation |
| .planning/research/SUMMARY.md line 10 | `inter-napplet` | Dated 2026-04-05 executive summary paragraph |
| .planning/SPEC-GAPS.md lines 543, 615 | `inter-napplet` | Dated Phase 84 / 2026-04-08 gap-inventory entry marked `REMOVED — Spec-Backed` |
| .planning/codebase/INTEGRATIONS.md line 168 | `INTER_PANE` | Historical wire-kind constant name |

**Phase 124 MUST** either:
- **(a) Refine its acceptance grep to EXCLUDE these explicitly-dated paths/lines (recommended):** path-based exclusion for `.planning/research/ONTOLOGY.md`, `.planning/research/SDK_NAMING_PATTERNS.md`, `.planning/research/SUMMARY.md`, `.planning/SPEC-GAPS.md`, plus line-specific exclusions for `.planning/PROJECT.md` Shipped/Validated sections + `.planning/codebase/INTEGRATIONS.md` line 168.
- **(b) Re-open this decision** via a Plan 03 revision and direct a further rewrite.

Option (a) is consistent with 123-CONTEXT.md's "dated investigation notes, rewrite conservatively (terminology only, not historical context)" rule and is recommended. This preserves the historical fidelity of research docs describing the IFC rename's own provenance.

**What Phase 124 will NOT find in the residuals (cleaned in this plan):**
- `.planning/codebase/TESTING.md` — both generic `IPC` references rewritten to `postMessage` (transport-accurate). Zero `\bIPC\b` remaining in this file.
- `.planning/research/FEATURES-CHANNELS.md` — all 9 current-tense `IPC` references rewritten (IFC where it's the napplet pub/sub system; postMessage where it's generic transport shorthand). Zero residual.

## Final acceptance grep (Phase 123 scope)

The current-state-API surface is now IFC-clean (and transport-accurate) across:
- Root README.md (Plan 01)
- packages/core/README.md (Plan 01)
- packages/shim/README.md (Plan 01)
- packages/sdk/README.md (Plan 01)
- skills/build-napplet/SKILL.md (Plan 02)
- .planning/codebase/ARCHITECTURE.md (Plan 03 Task 1)
- .planning/codebase/STRUCTURE.md (Plan 03 Task 1)
- .planning/codebase/INTEGRATIONS.md except line 168 (Plan 03 Task 1)
- .planning/codebase/CONCERNS.md (Plan 03 Task 1)
- .planning/codebase/TESTING.md (Plan 03 Task 1) — now zero `\bIPC\b` matches
- .planning/research/ARCHITECTURE.md (Plan 03 Task 2)
- .planning/research/FEATURES-CHANNELS.md (Plan 03 Task 2 + deviation extension)

The current-surface zero-grep for `\bipc\b`, `\bIPC\b`, `IPC-PEER`, `IPC_PEER`, `inter-pane`, `inter-napplet`, `Inter-napplet`, `Inter-pane`, `INTER_PANE`, `Inter-Pane` passes across all files above with exit 1 (no matches). INTEGRATIONS.md line 168 is the only documented residual match, flagged for Phase 124 exclusion.

### Verified grep run (2026-04-19)

```bash
$ grep -lE "\bipc\b|\bIPC\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane" \
    README.md \
    packages/core/README.md \
    packages/shim/README.md \
    packages/sdk/README.md \
    skills/build-napplet/SKILL.md \
    .planning/codebase/ARCHITECTURE.md \
    .planning/codebase/STRUCTURE.md \
    .planning/codebase/CONCERNS.md \
    .planning/codebase/TESTING.md \
    .planning/research/ARCHITECTURE.md \
    .planning/research/FEATURES-CHANNELS.md
(no output)
$ echo $?
1

$ grep -nE "\bipc\b|\bIPC\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane" .planning/codebase/INTEGRATIONS.md
168:- 29003: INTER_PANE (napplet-to-napplet events)
```

Preserved files confirmed byte-identical (all `git diff --exit-code` exit 0):
- `.planning/PROJECT.md`
- `.planning/STATE.md`
- `.planning/ROADMAP.md`
- `.planning/research/ONTOLOGY.md`
- `.planning/research/SDK_NAMING_PATTERNS.md`
- `.planning/research/SUMMARY.md`
- `.planning/SPEC-GAPS.md`

---

*Phase: 123-documentation-sweep*
*Plan: 03*
*Purpose: Phase 124 inheritance record*
*Completed: 2026-04-19*
