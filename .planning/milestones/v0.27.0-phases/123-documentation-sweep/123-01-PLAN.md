---
phase: 123-documentation-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - packages/core/README.md
  - packages/shim/README.md
  - packages/sdk/README.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "A reader opening root README.md sees zero `ipc` / `IPC-PEER` / `inter-pane` / `inter-napplet` in the package table, architecture diagram, or prose — historical changelog lines (none in root README) are preserved."
    - "A reader opening packages/core/README.md sees `ipc.on()` rephrased to `ifc.on()` in JSDoc references to Subscription."
    - "A reader opening packages/shim/README.md sees every `window.napplet.ipc` → `window.napplet.ifc`, every `ipc:` key in the shape block → `ifc:`, every section heading `### window.napplet.ipc` → `### window.napplet.ifc`, `inter-pane` prose → `inter-frame`, and `ipcSub` sample variable → `ifcSub`."
    - "A reader opening packages/sdk/README.md sees the `### ipc` section heading become `### ifc`, every `import { ... ipc ... } from '@napplet/sdk'` swapped to `ifc`, `ipc.emit/on(...)` → `ifc.emit/on(...)`, `ipcSub` → `ifcSub`, and `Inter-napplet communication` → `Inter-frame communication`."
    - "Running `grep -nE '\\bipc\\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane' README.md packages/core/README.md packages/shim/README.md packages/sdk/README.md` returns zero matches."
  artifacts:
    - path: "README.md"
      provides: "Root README with IFC-only terminology in package table + architecture diagram"
      contains: "window.napplet.ifc"
    - path: "packages/core/README.md"
      provides: "Core README Subscription JSDoc referencing ifc.on()"
      contains: "ifc.on()"
    - path: "packages/shim/README.md"
      provides: "Shim README with ifc namespace in shape block + section heading"
      contains: "window.napplet.ifc"
    - path: "packages/sdk/README.md"
      provides: "SDK README with ifc named-export section + inter-frame phrasing"
      contains: "### `ifc`"
  key_links:
    - from: "packages/shim/README.md `### window.napplet.ifc`"
      to: "the shape-block `ifc: { emit, on }` a few lines above"
      via: "section heading + shape-block naming consistency"
      pattern: "### `window\\.napplet\\.ifc`"
    - from: "packages/sdk/README.md Quick Start import"
      to: "API Reference `### ifc` heading"
      via: "named-export consistency — import matches section name"
      pattern: "import \\{[^}]*\\bifc\\b[^}]*\\} from '@napplet/sdk'"
---

<objective>
Sweep the four published package READMEs (root + core + shim + sdk) to eliminate `ipc`, `IPC-PEER`, `inter-pane`, and `inter-napplet` from all current-tense prose, code fences, tables, and section headings — replacing with `ifc`, `IFC-PEER`, `inter-frame`, and matching Phase 122's canonical tokens. No historical changelog lines are touched (the four READMEs in scope contain no `## Changelog` / `## History` / `Shipped: vX.Y.Z` sections describing past IPC-era decisions, so there is nothing to preserve — but the rule stays enforced by acceptance grep).

**Purpose:** A developer onboarding via npm package pages (the four READMEs Phase 122's source rename shipped against) reads IFC-correct documentation end-to-end. Without this sweep, the source says `ifc` but the docs say `ipc`, breaking the cold-read contract.

**Output:** Four READMEs, IFC-clean, matching the source API surface shipped in Phase 122 (commit `bd2fe98` and earlier task commits).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-source-rename/122-01-SUMMARY.md
@.planning/phases/123-documentation-sweep/123-CONTEXT.md

<interfaces>
<!-- Canonical tokens established in Phase 122. Use these EXACTLY. -->
<!-- Source: packages/core/src/types.ts, packages/shim/src/index.ts, packages/sdk/src/index.ts, packages/nub/src/ifc/sdk.ts -->

Runtime API:
- `window.napplet.ifc`                       (replaces `window.napplet.ipc`)
- `window.napplet.ifc.emit(topic, payload)`  (method signature unchanged)
- `window.napplet.ifc.on(topic, callback)`   (returns Subscription)

SDK named export:
- `import { ifc } from '@napplet/sdk'`       (replaces `import { ipc }`)
- `ifc.emit(...)` / `ifc.on(...)`            (same method signatures)

Prose terminology:
- "Inter-frame" / "inter-frame communication" / "inter-frame events"
  (replaces "Inter-pane", "inter-pane", "Inter-napplet", "inter-napplet")
- "IFC-PEER" (replaces "IPC-PEER" / "IPC_PEER") — though IFC-PEER does not currently appear in these READMEs, the substitution is defined here for completeness if grep finds it.

Sample variable naming (common in code fences):
- `ifcSub` replaces `ipcSub`

Section heading convention:
- `### window.napplet.ifc`       (replaces `### window.napplet.ipc`)
- `### ifc`                      (replaces `### ipc` in sdk/README.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep root README.md and packages/core/README.md</name>
  <read_first>
    - README.md (root — full file, 76 lines)
    - packages/core/README.md (full file, 362 lines — reference only)
    - .planning/phases/123-documentation-sweep/123-CONTEXT.md (rewrite-rules section)
  </read_first>
  <files>README.md, packages/core/README.md</files>
  <action>
    Two surgical token-swaps across two files. Zero structural rewrites.

    **File 1 — README.md (root):**

    1. Line 13 — the `@napplet/sdk` row in the Packages table. Swap the object list verbatim:
       - BEFORE: `Provides \`relay\`, \`ipc\`, \`services\`, \`storage\` objects plus NUB message type re-exports.`
       - AFTER:  `Provides \`relay\`, \`ifc\`, \`services\`, \`storage\` objects plus NUB message type re-exports.`
       (Only `ipc` → `ifc`. Preserve every backtick, comma, and the "services" entry even though services was killed in v0.22.0 — that's an out-of-scope accuracy issue; this phase is terminology-only per 123-CONTEXT.md Deferred Ideas.)

    2. Line 34 — the ASCII architecture diagram. Swap the routing column:
       - BEFORE: `  ├── JSON envelope message routing          window.napplet.ipc   (emit/on)`
       - AFTER:  `  ├── JSON envelope message routing          window.napplet.ifc   (emit/on)`
       (Only `.ipc` → `.ifc`. Preserve the tree-drawing character `├──`, preserve the 3-space gap before `(emit/on)` — the diagram is column-aligned; `ipc` and `ifc` are both 3 chars so alignment is preserved byte-for-byte.)

    **File 2 — packages/core/README.md:**

    1. Line 272 — the `#### Subscription` JSDoc prose. Swap the method reference:
       - BEFORE: `Handle returned by \`relay.subscribe()\` and \`ipc.on()\`.`
       - AFTER:  `Handle returned by \`relay.subscribe()\` and \`ifc.on()\`.`
       (Only `ipc.on()` → `ifc.on()`. Preserve the period, preserve the `and` conjunction.)

    **No other edits to either file.** Do NOT touch:
    - README.md line 38 ("IFC routing") — already IFC-correct.
    - packages/core/README.md domain-table row for `ifc` (line 75) — already IFC-correct.
    - packages/core/README.md NubDomain union (line 67) — already IFC-correct.
    - packages/core/README.md topic-constants note about "IFC topic-based routing" (line 307+) — already IFC-correct.

    Per DOC-01 / 123-CONTEXT.md: terminology-only swap. No rewrites of nearby context, no grammar changes, no restructuring.
  </action>
  <verify>
    <automated>
      # Both files IFC-clean for the phase terminology set:
      grep -nE "\bipc\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane" README.md packages/core/README.md
      # Expected: zero matches, exit code 1.

      # Positive assertion: the two specific replacements landed exactly.
      grep -cF "window.napplet.ifc" README.md
      # Expected: count >= 1 (the diagram line).
      grep -cF "\`ifc\`, \`services\`" README.md
      # Expected: count == 1 (the package table row, verifying backtick preservation).
      grep -cF "\`ifc.on()\`" packages/core/README.md
      # Expected: count == 1 (the Subscription JSDoc).
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "\bipc\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane" README.md packages/core/README.md` returns zero matches.
    - README.md line 13 contains the exact substring `` `relay`, `ifc`, `services`, `storage` `` (backticks preserved).
    - README.md line 34 contains the exact substring `window.napplet.ifc   (emit/on)` (three spaces preserved; column alignment byte-stable).
    - packages/core/README.md line 272 contains the exact substring `` Handle returned by `relay.subscribe()` and `ifc.on()`. ``
    - Total diff across both files: exactly three single-line changes (2 in root, 1 in core). `git diff --stat README.md packages/core/README.md` reports `2 files changed, 3 insertions(+), 3 deletions(-)`.
    - No historical "Shipped:" changelog bullets touched (neither file contains such sections — preservation is vacuous but the grep above would catch any accidental drift into archived wording).
  </acceptance_criteria>
  <done>
    Root README's package table + architecture diagram, and core README's Subscription JSDoc, all use `ifc` terminology. Phase 124's zero-grep acceptance passes for these two files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep packages/shim/README.md</name>
  <read_first>
    - packages/shim/README.md (full file, 444 lines — highest IPC density of the 4 READMEs)
    - .planning/phases/123-documentation-sweep/123-CONTEXT.md (rewrite-rules + specifics sections)
    - packages/shim/src/index.ts (NOT to modify — reference only to confirm installer shape matches README)
  </read_first>
  <files>packages/shim/README.md</files>
  <action>
    Nine scoped edits across one file. Each edit is a literal token-swap on a specific line. No structural changes.

    **Edit 1 — Line 15 (How It Works bullet 3, under `### How It Works`):**
    - BEFORE: `3. Once registered, \`window.napplet\` is populated with relay, ipc, storage, keys, media, notify, identity, config, and shell sub-objects`
    - AFTER:  `3. Once registered, \`window.napplet\` is populated with relay, ifc, storage, keys, media, notify, identity, config, and shell sub-objects`
    (Per 123-CONTEXT.md specifics: "relay, ipc, storage, ..." → "relay, ifc, storage, ..." — order preserved, commas preserved.)

    **Edit 2 — Lines 45-48 (Quick Start code fence, `## Quick Start`):**
    - BEFORE:
      ```
      // Listen for inter-pane events from other napplets
      const ipcSub = window.napplet.ipc.on('profile:open', (payload) => {
        console.log('Profile requested:', payload);
      });
      ```
    - AFTER:
      ```
      // Listen for inter-frame events from other napplets
      const ifcSub = window.napplet.ifc.on('profile:open', (payload) => {
        console.log('Profile requested:', payload);
      });
      ```
    Three swaps: `inter-pane` → `inter-frame` (comment), `ipcSub` → `ifcSub` (variable declaration), `window.napplet.ipc.on` → `window.napplet.ifc.on` (call site).

    **Edit 3 — Line 108 (Quick Start code fence, cleanup block):**
    - BEFORE: `ipcSub.close();`
    - AFTER:  `ifcSub.close();`
    (Matches the variable-name swap from Edit 2. Single-line change.)

    **Edit 4 — Line 233 (shape block under `## \`window.napplet\` Shape`):**
    - BEFORE: `  ipc: {`
    - AFTER:  `  ifc: {`
    (Property name in the shape documentation literal. Preserve the two-space indent.)

    **Edit 5 — Line 304 (API Reference section heading):**
    - BEFORE: `### \`window.napplet.ipc\``
    - AFTER:  `### \`window.napplet.ifc\``
    (Heading. Per 123-CONTEXT.md specifics: table row rename.)

    **Edit 6 — Line 306 (prose under that heading):**
    - BEFORE: `Inter-pane communication between napplets via the shell.`
    - AFTER:  `Inter-frame communication between napplets via the shell.`
    (Prose terminology swap.)

    **Edit 7 — Line 310 (the method table row body under the `### \`window.napplet.ifc\`` section):**
    - BEFORE: `| \`emit(topic, extraTags?, content?)\` | \`void\` | Send an \`ifc.emit\` JSON envelope to the shell for delivery to matching topic subscribers. |`
    - AFTER:  (no change — this line already uses `ifc.emit` correctly since the wire format shipped with IFC domain in v0.16.0+)
    **Verify line 310 already says `ifc.emit` and `ifc.event`. Do NOT edit.** The edit target is the heading (Edit 5) and prose (Edit 6), not the already-correct table.

    (Re-check: lines 311-312 also already use `ifc.event`. No change.)

    **Edit 8 — Line 408 (TypeScript Support code fence):**
    - BEFORE: `// TypeScript knows about window.napplet.relay, .ipc, .storage, .keys, .media, .notify, .identity, .shell`
    - AFTER:  `// TypeScript knows about window.napplet.relay, .ifc, .storage, .keys, .media, .notify, .identity, .shell`
    (Comment inside code fence. Swap `.ipc` → `.ifc`. Order preserved.)

    **Edit 9 — Line 424 (Shim vs SDK table row):**
    - BEFORE: `| **Import style** | \`import '@napplet/shim'\` (side-effect) | \`import { relay, ipc } from '@napplet/sdk'\` |`
    - AFTER:  `| **Import style** | \`import '@napplet/shim'\` (side-effect) | \`import { relay, ifc } from '@napplet/sdk'\` |`
    (Table cell. Swap `ipc` → `ifc`. Preserve pipe delimiters and backticks.)

    **Edit 10 — Line 428 (Shim vs SDK table, Named exports row):**
    - BEFORE: `| **Named exports** | None | \`relay\`, \`ipc\`, \`storage\`, \`keys\`, \`identity\`, plus types |`
    - AFTER:  `| **Named exports** | None | \`relay\`, \`ifc\`, \`storage\`, \`keys\`, \`identity\`, plus types |`
    (Table cell. Same swap pattern.)

    **Edit 11 — Line 434 (Typical usage code fence, end of "Shim vs SDK" section):**
    - BEFORE: `import { relay, ipc, storage, keys, identity } from '@napplet/sdk';`
    - AFTER:  `import { relay, ifc, storage, keys, identity } from '@napplet/sdk';`
    (Code fence import. Swap `ipc` → `ifc`. Preserve order.)

    **Total:** 10 concrete edits on 9 distinct lines (Edits 2-3 share the `ipcSub` var name, appearing on 2 different lines; the quick-start comment + declaration on 45-46 count as 2 swaps on 2 lines within Edit 2). Renumbering for clarity: this task produces exactly 10 line-level diffs.

    **No other changes.** Do NOT touch:
    - Line 139-140 (Wire Format outbound IFC messages) — already `ifc.emit/subscribe/unsubscribe`.
    - Line 192 (Wire Format inbound) — already `ifc.event`.
    - Lines 310-312 (API Reference method table under `### \`window.napplet.ifc\``) — already IFC-clean post Edit 5.
    - No historical / changelog sections exist in this file.

    Per DOC-01 / 123-CONTEXT.md: strict terminology swap, no structural or grammatical rewrites.
  </action>
  <verify>
    <automated>
      # Full file IFC-clean:
      grep -nE "\bipc\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane" packages/shim/README.md
      # Expected: zero matches, exit 1.

      # Positive assertions for each scoped edit:
      grep -cF "const ifcSub = window.napplet.ifc.on(" packages/shim/README.md
      # Expected: count == 1 (Edit 2 landed).

      grep -cF "ifcSub.close()" packages/shim/README.md
      # Expected: count == 1 (Edit 3 landed).

      grep -cF "### \`window.napplet.ifc\`" packages/shim/README.md
      # Expected: count == 1 (Edit 5 — section heading).

      grep -cF "Inter-frame communication between napplets" packages/shim/README.md
      # Expected: count == 1 (Edit 6 — prose).

      grep -cF "import { relay, ifc } from '@napplet/sdk'" packages/shim/README.md
      # Expected: count == 1 (Edit 9 — Shim vs SDK row).

      grep -cF "import { relay, ifc, storage, keys, identity } from '@napplet/sdk'" packages/shim/README.md
      # Expected: count == 1 (Edit 11 — Typical usage).

      # Negative assertion: ensure historical-looking IFC-correct lines are NOT broken.
      grep -cF "window.napplet.ifc not installed" packages/shim/README.md
      # (This string is not expected to appear; just a probe — count ok either way.)
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "\bipc\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane" packages/shim/README.md` returns zero matches.
    - Line 15 reads `...populated with relay, ifc, storage, keys, media, notify, identity, config, and shell sub-objects` (order + punctuation byte-stable).
    - Line 46 reads `const ifcSub = window.napplet.ifc.on('profile:open', (payload) => {` (variable + call-site both renamed).
    - Line 108 reads `ifcSub.close();` (cleanup block renamed).
    - Line 233 reads `  ifc: {` (shape block property name, two-space indent preserved).
    - Line 304 reads `` ### `window.napplet.ifc` `` (section heading).
    - Line 306 reads `Inter-frame communication between napplets via the shell.`
    - Line 408 comment contains `.ifc,` (not `.ipc,`).
    - Lines 424, 428, 434 all reference `ifc` (not `ipc`) in their import-list substrings.
    - No edits to the Wire Format outbound/inbound message-type code fences (they already used IFC correctly pre-phase).
    - `git diff packages/shim/README.md | grep -c "^-"` == 10 (ten line-deletions, matching ten edits).
  </acceptance_criteria>
  <done>
    packages/shim/README.md is fully IFC-clean across Quick Start code, shape block, API Reference heading, prose, TypeScript section, and Shim-vs-SDK comparison table. Phase 124's zero-grep acceptance passes for this file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sweep packages/sdk/README.md</name>
  <read_first>
    - packages/sdk/README.md (full file, 372 lines)
    - .planning/phases/123-documentation-sweep/123-CONTEXT.md (rewrite-rules + specifics sections — in particular "the heading `### ipc` becomes `### ifc`")
    - packages/sdk/src/index.ts (NOT to modify — reference only to confirm exported name matches README)
  </read_first>
  <files>packages/sdk/README.md</files>
  <action>
    Seven scoped edits across one file. Each edit is a literal token-swap on a specific line. No structural changes.

    **Edit 1 — Line 15 (How It Works bullet 2, under `### How It Works`):**
    - BEFORE: `2. Import named exports from \`@napplet/sdk\` -- \`relay\`, \`ipc\`, \`storage\`, \`keys\``
    - AFTER:  `2. Import named exports from \`@napplet/sdk\` -- \`relay\`, \`ifc\`, \`storage\`, \`keys\``
    (Per 123-CONTEXT.md specifics. Preserve double-hyphen `--` and backticks.)

    **Edit 2 — Line 29 (Quick Start import statement):**
    - BEFORE: `import { relay, ipc, storage, keys, media, notify, config, type NostrEvent } from '@napplet/sdk';`
    - AFTER:  `import { relay, ifc, storage, keys, media, notify, config, type NostrEvent } from '@napplet/sdk';`
    (Named-import list. Swap `ipc` → `ifc`. Preserve full list order and `type` keyword.)

    **Edit 3 — Lines 46-48 (Quick Start code fence body, inter-pane messaging block):**
    - BEFORE:
      ```
      // Inter-pane messaging
      ipc.emit('chat:message', [], JSON.stringify({ text: 'hi' }));
      const ipcSub = ipc.on('bot:response', (payload) => {
        console.log('Bot says:', payload);
      });
      ```
    - AFTER:
      ```
      // Inter-frame messaging
      ifc.emit('chat:message', [], JSON.stringify({ text: 'hi' }));
      const ifcSub = ifc.on('bot:response', (payload) => {
        console.log('Bot says:', payload);
      });
      ```
    Four swaps: `Inter-pane` → `Inter-frame` (comment), `ipc.emit` → `ifc.emit` (call), `ipcSub` → `ifcSub` (var), `ipc.on` → `ifc.on` (call).

    **Edit 4 — Line 93 (Quick Start cleanup block):**
    - BEFORE: `ipcSub.close();`
    - AFTER:  `ifcSub.close();`
    (Matches var name from Edit 3.)

    **Edit 5 — Line 110 (API Reference section heading):**
    - BEFORE: `### \`ipc\``
    - AFTER:  `### \`ifc\``
    (Per 123-CONTEXT.md specifics: "the heading `### ipc` becomes `### ifc`". Exact token-swap; preserve backticks.)

    **Edit 6 — Line 112 (prose under the `### \`ifc\`` heading):**
    - BEFORE: `Inter-napplet communication between napplets. Mirrors \`window.napplet.ipc\`.`
    - AFTER:  `Inter-frame communication between napplets. Mirrors \`window.napplet.ifc\`.`
    (Two swaps in one line: "Inter-napplet" → "Inter-frame" (prose), `window.napplet.ipc` → `window.napplet.ifc` (mirror reference). Preserve the period between sentences.)

    **Edit 7 — Line 358 (SDK vs Shim section, Typical usage code fence):**
    - BEFORE: `import { relay, ipc, storage, keys, media, notify } from '@napplet/sdk';  // optional: typed API`
    - AFTER:  `import { relay, ifc, storage, keys, media, notify } from '@napplet/sdk';  // optional: typed API`
    (Named-import list. Swap `ipc` → `ifc`. Preserve trailing comment with two-space gap.)

    **No other changes.** Do NOT touch:
    - Line 114 (prose in the `### \`ifc\`` section: "Messages are sent as JSON envelope objects (`{ type: 'ifc.emit', topic, payload }`)...") — already IFC-correct.
    - Line 115 (continuation: "(`{ type: 'ifc.event', topic, payload, sender }`).") — already IFC-correct.
    - Lines 119-120 (method table: "Broadcast an IFC event..." / "Subscribe to IFC events...") — already IFC-correct.
    - Line 271 (NUB Message Types section: "IfcNubMessage") — already IFC-correct.
    - Line 299 (table row for IfcNubMessage) — already IFC-correct.
    - Line 314 (`IFC_DOMAIN` constant in the NUB Domain Constants list) — already IFC-correct.

    Per DOC-01 / 123-CONTEXT.md: strict terminology swap. No structural or grammatical rewrites beyond the documented 7 edits.
  </action>
  <verify>
    <automated>
      # Full file IFC-clean:
      grep -nE "\bipc\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane" packages/sdk/README.md
      # Expected: zero matches, exit 1.

      # Positive assertions for each edit:
      grep -cF "import { relay, ifc, storage, keys, media, notify, config, type NostrEvent }" packages/sdk/README.md
      # Expected: count == 1 (Edit 2 — Quick Start import).

      grep -cF "// Inter-frame messaging" packages/sdk/README.md
      # Expected: count == 1 (Edit 3 — comment).

      grep -cF "const ifcSub = ifc.on(" packages/sdk/README.md
      # Expected: count == 1 (Edit 3 — var + call).

      grep -cF "ifcSub.close()" packages/sdk/README.md
      # Expected: count == 1 (Edit 4 — cleanup).

      grep -cF "### \`ifc\`" packages/sdk/README.md
      # Expected: count == 1 (Edit 5 — section heading).

      grep -cF "Inter-frame communication between napplets. Mirrors \`window.napplet.ifc\`." packages/sdk/README.md
      # Expected: count == 1 (Edit 6 — prose).

      grep -cF "import { relay, ifc, storage, keys, media, notify } from '@napplet/sdk';" packages/sdk/README.md
      # Expected: count == 1 (Edit 7 — SDK vs Shim).

      # Negative: ensure already-correct IFC strings are preserved (not accidentally rewritten):
      grep -cF "'ifc.emit'" packages/sdk/README.md
      # Expected: count >= 1 (line 114 JSON envelope example preserved).
      grep -cF "IfcNubMessage" packages/sdk/README.md
      # Expected: count >= 2 (type union references preserved).
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "\bipc\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane" packages/sdk/README.md` returns zero matches.
    - Line 15 reads `...\`@napplet/sdk\` -- \`relay\`, \`ifc\`, \`storage\`, \`keys\``.
    - Line 29 contains `relay, ifc, storage, keys, media, notify, config, type NostrEvent` as a comma-separated import list.
    - Line 46 reads `// Inter-frame messaging`.
    - Line 47 reads `ifc.emit('chat:message', [], JSON.stringify({ text: 'hi' }));`.
    - Line 48 reads `const ifcSub = ifc.on('bot:response', (payload) => {`.
    - Line 93 reads `ifcSub.close();`.
    - Line 110 reads `` ### `ifc` ``.
    - Line 112 reads `` Inter-frame communication between napplets. Mirrors `window.napplet.ifc`. ``.
    - Line 358 contains `import { relay, ifc, storage, keys, media, notify } from '@napplet/sdk';`.
    - Lines 114-115 (JSON envelope example), lines 119-120 (table body), line 271 (IfcNubMessage), line 299 (table row for IfcNubMessage), and line 314 (`IFC_DOMAIN` constant) are byte-identical to pre-edit state.
    - `git diff packages/sdk/README.md | grep -c "^-"` == 7 (seven line-deletions, matching seven edits; Edit 3 has 3 touched lines but each is a single-line diff).
  </acceptance_criteria>
  <done>
    packages/sdk/README.md is fully IFC-clean across Quick Start, API Reference section heading + prose, and SDK-vs-Shim comparison. The `### ifc` heading matches the exported `const ifc` from packages/sdk/src/index.ts. Phase 124's zero-grep acceptance passes for this file.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. **Zero-leakage grep across all four READMEs (the phase gate for DOC-01):**
   ```bash
   grep -nE "\bipc\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane" \
     README.md \
     packages/core/README.md \
     packages/shim/README.md \
     packages/sdk/README.md
   ```
   Expected: zero matches, exit 1.

2. **IFC-correctness positive assertion (spot-check anchors):**
   ```bash
   grep -cF "window.napplet.ifc" README.md packages/shim/README.md
   # Expected: both files >= 1.

   grep -cF "### \`ifc\`" packages/sdk/README.md
   grep -cF "### \`window.napplet.ifc\`" packages/shim/README.md
   # Expected: 1 each.
   ```

3. **Source-contract consistency (no changes to packages/*/src/ — Phase 122 territory):**
   ```bash
   git diff --name-only packages/*/src/
   # Expected: empty (no source files modified by this plan).
   ```

4. **Historical changelog preservation:** None of the four in-scope READMEs contains a `## Changelog` or `Shipped: vX.Y.Z` section with historical IPC_PEER references. Preservation check is vacuously satisfied; the phase gate above enforces no accidental drift.

5. **Commit trail:** Three commits land on main in order: Task 1 → Task 2 → Task 3. Each commit touches only the files declared in its `<files>` field. No `.planning/` files touched (that's Plan 03's job).
</verification>

<success_criteria>
- All four READMEs pass the zero-leakage grep for `\bipc\b`, `IPC-PEER`, `IPC_PEER`, `inter-pane`, `inter-napplet`, and their case variants.
- The `### ifc` section heading in packages/sdk/README.md and the `### \`window.napplet.ifc\`` heading in packages/shim/README.md both resolve to sections whose code samples, prose, and table rows all reference the IFC names.
- The Shim-vs-SDK comparison tables in both packages/shim/README.md (lines 424-428) and packages/sdk/README.md (line 358) show `ifc` as the named SDK export.
- Root README package table (line 13) and architecture diagram (line 34) both use `ifc`.
- packages/core/README.md Subscription JSDoc (line 272) references `ifc.on()`.
- DOC-01 requirement satisfied.
- No source files (packages/*/src/) touched.
- No `.planning/` files touched.
- No skills/ files touched (Plan 02's job).
</success_criteria>

<output>
After completion, create `.planning/phases/123-documentation-sweep/123-01-SUMMARY.md` documenting:
- Files modified (4 READMEs)
- Total line-level diff count (3 + 10 + 7 = 20 edits across 4 files)
- Final zero-leakage grep evidence
- Confirmation that no source (packages/*/src/), skills/, or .planning/ files were touched
- Acknowledgment that Plan 02 (skills) runs parallel in Wave 1 and Plan 03 (active planning) runs in Wave 2
</output>
