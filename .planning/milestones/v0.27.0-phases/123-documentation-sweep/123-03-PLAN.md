---
phase: 123-documentation-sweep
plan: 03
type: execute
wave: 2
depends_on: [123-documentation-sweep/01, 123-documentation-sweep/02]
files_modified:
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/TESTING.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/FEATURES-CHANNELS.md
autonomous: true
requirements: [PLAN-01]

must_haves:
  truths:
    - "`.planning/codebase/*.md` files have zero `ipc` / `inter-pane` / `IPC-PEER` leakage in current-tense API references. Historical notes describing prior renames (e.g., BusKind.INTER_PANE → BusKind.IPC_PEER as a dated Phase 35 event) stay as preserved history per 123-CONTEXT.md."
    - "`.planning/codebase/TESTING.md` lines 83 and 152 describe the CURRENT transport (postMessage between napplet iframe and shell), so the generic `IPC` label is replaced with `postMessage` — the concrete transport mechanism that the napplet shim uses today. This is substitution of an overloaded generic CS term with the accurate specific term; no historical context is lost."
    - "`.planning/research/ARCHITECTURE.md` code-fence snippet of the NappletGlobal interface uses `ifc:` (not `ipc:`), matching the current surface after Phase 122."
    - "`.planning/research/FEATURES-CHANNELS.md` comparison-table Status row for the existing shipped pub/sub mode references `window.napplet.ifc.emit/on` (not `window.napplet.ipc.emit/on`)."
    - "`.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/ROADMAP.md` are left byte-identical to their current on-disk state (they were already corrected during milestone kickoff per 123-CONTEXT.md; any `ipc` occurrences there describe past milestones — Shipped sections, Validated bullets, Phase 35/45 notes — and are historical)."
    - "`.planning/research/ONTOLOGY.md` and `.planning/research/SDK_NAMING_PATTERNS.md` dated-investigation content preserved — the `INTER_PANE → IPC_PEER` argumentation is a historical record of a past rename, not a current-state claim. External references (Electron IPC Tutorial, Android Binder IPC) preserved as ecosystem citations."
    - "`.planning/research/SUMMARY.md` dated executive summary (2026-04-05) preserved — its single `inter-napplet` reference is inside a dated historical research framing paragraph."
    - "`.planning/SPEC-GAPS.md` preserves its `inter-napplet communication` references — they appear in GAP-09 which is marked `REMOVED — Spec-Backed` and describes the IFC channel design as a dated investigation (Phase 84 / 2026-04-08)."
  artifacts:
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Architecture doc with current-tense IFC terminology in API-surface descriptions"
      contains: "inter-frame"
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "Structure doc with IFC pubsub phrasing in API listings"
      contains: "inter-frame"
    - path: ".planning/codebase/INTEGRATIONS.md"
      provides: "Integrations doc with IFC terminology for current-state integration descriptions"
      contains: "inter-frame"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Concerns doc with IFC terminology in current-tense scaling-limits + fragile-areas descriptions"
      contains: "inter-frame"
    - path: ".planning/codebase/TESTING.md"
      provides: "Testing doc with accurate current-transport terminology (postMessage) in the mock list and integration-test scope"
      contains: "postMessage"
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "Research arch doc with ifc: key in NappletGlobal reference snippet"
      contains: "ifc:"
    - path: ".planning/research/FEATURES-CHANNELS.md"
      provides: "Features-channels research with window.napplet.ifc in shipped-status row"
      contains: "window.napplet.ifc"
  key_links:
    - from: ".planning/research/ARCHITECTURE.md code-fence NappletGlobal snippet"
      to: "packages/core/src/types.ts NappletGlobal shape (Phase 122 Task 1 output)"
      via: "research snippet kept in sync with source of truth"
      pattern: "ifc:\\s+\\{"
    - from: ".planning/codebase/*.md current-tense 'inter-pane' prose"
      to: "packages/shim/README.md + packages/sdk/README.md 'inter-frame' prose (Plan 01 Wave 1 output)"
      via: "planning docs match published docs terminology"
      pattern: "inter-frame"
    - from: ".planning/codebase/TESTING.md 'IPC mechanism' / 'Shell IPC' prose"
      to: "packages/shim source (window.parent.postMessage call sites)"
      via: "testing doc describes the transport accurately as postMessage, matching actual shim code"
      pattern: "postMessage"
---

<objective>
Sweep active `.planning/` docs for current-tense `ipc` / `inter-pane` / `inter-napplet` terminology, replacing with IFC equivalents — AND rewrite `.planning/codebase/TESTING.md` lines 83 and 152 where the generic word "IPC" is used as a catch-all for postMessage, replacing it with the accurate transport term "postMessage". Explicitly preserve:
1. Dated historical argumentation in `.planning/research/ONTOLOGY.md` + `.planning/research/SUMMARY.md` + `.planning/research/SDK_NAMING_PATTERNS.md` (these describe past milestones' renames or dated investigation context).
2. External ecosystem citations (Electron IPC Tutorial, Android Binder IPC — these are product names, not our terminology).
3. Historical "Shipped: vX.Y.Z" / Validated bullets in `.planning/PROJECT.md` (already confirmed clean at milestone kickoff per 123-CONTEXT.md; no edits needed).
4. `.planning/STATE.md` + `.planning/ROADMAP.md` (already clean post-kickoff).
5. `.planning/SPEC-GAPS.md` dated GAP-09 content (Phase 84 / 2026-04-08 investigation).
6. Historical references in `.planning/codebase/*.md` describing the PRIOR INTER_PANE → IPC_PEER rename as a dated v0.7.0 event — leave those if present.

**TESTING.md judgment call (resolved):** Lines 83 and 152 use the word "IPC" generically to mean "inter-process communication / postMessage transport" — NOT the napplet runtime's `window.napplet.ipc` namespace that Phase 122 renamed. However, leaving them intact would leak a residual `\bIPC\b` match into Phase 124's repo-wide grep acceptance criterion and create a surprise for the next planner. Because the text describes the CURRENT transport (the shim's `window.parent.postMessage()` plumbing and the napplet↔shell message bus carried over postMessage), the accurate term is `postMessage`. Rewriting to `postMessage` is semantically precise, loses no information, and makes Phase 124's grep truly clean without path-based exclusions for TESTING.md. This matches Phase 123's spirit: active docs reflect current reality with accurate terminology.

**This is a deliberate trade-off for the remaining preserved files.** Phase 124's grep acceptance criterion targets current-state correctness. Dated historical content that literally records a past rename is semantically preserved. If Phase 124's grep still flags these dated lines, Phase 124 will need to refine its exclusion scope (e.g., exclude `.planning/research/ONTOLOGY.md`, `.planning/research/SDK_NAMING_PATTERNS.md`, `.planning/research/SUMMARY.md`, and `.planning/SPEC-GAPS.md` from the active-planning grep since they're dated investigation artifacts). That refinement is Phase 124's job; this plan does the terminology sweep where it belongs and flags the judgment calls transparently.

**Purpose:** The active-planning surface a future planner/agent reads to understand the current codebase uses IFC terminology and accurate transport terminology. Historical research records stay truthful to their moment of authorship.

**Output:** Seven modified files; three planning-doc files explicitly left untouched (PROJECT.md, STATE.md, ROADMAP.md — already clean); four planning-doc files explicitly preserved as dated history (research/ONTOLOGY.md, research/SDK_NAMING_PATTERNS.md, research/SUMMARY.md, SPEC-GAPS.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-source-rename/122-01-SUMMARY.md
@.planning/phases/123-documentation-sweep/123-CONTEXT.md
@.planning/phases/123-documentation-sweep/123-01-PLAN.md
@.planning/phases/123-documentation-sweep/123-02-PLAN.md

<interfaces>
<!-- Canonical tokens from Phase 122 (same as Plans 01 + 02). -->

Current API surface (the shape .planning/research/ARCHITECTURE.md snippet must match):
```ts
export interface NappletGlobal {
  relay:    { ... };
  ifc:      { ... };   // renamed from ipc in Phase 122
  storage:  { ... };
  keys:     { ... };
  media:    { ... };
  notify:   { ... };
  identity: { ... };
  config:   { ... };   // added later in v0.25.0
}
```

Current IFC API usage (the phrase FEATURES-CHANNELS.md "Status" row must reflect):
- `window.napplet.ifc.emit/on` (replaces `window.napplet.ipc.emit/on`)

Prose terminology:
- "inter-frame" (replaces "inter-pane", "inter-napplet")
- "IFC" or "IFC peer bus" (replaces "IPC" or "IPC peer bus" in current-tense prose)

Transport terminology (TESTING.md-specific):
- "postMessage" — the concrete transport mechanism the shim uses (`window.parent.postMessage()`).
- Replaces generic "IPC" where the text is describing the transport layer itself, not the napplet runtime namespace.

NOT replaced (dated historical content):
- `BusKind.INTER_PANE` as a subject of a past-rename sentence.
- `IPC_PEER` as a subject of a past-rename sentence.
- "The planned rename to `IPC_PEER` is a significant improvement" (ONTOLOGY.md line 94 — this is a dated comparative argument; preserved).
- External product names: "Electron IPC Tutorial", "Android Binder IPC".
</interfaces>

<historical_preservation_map>
<!-- Explicit list of files + line-ranges to NOT modify, with justification. -->

**.planning/PROJECT.md:** Zero edits.
- Lines 5, 8-11 (Current Milestone section): Already reference IPC → IFC as the rename subject. Current-tense phrasing. Per 123-CONTEXT.md: "already updated during milestone kickoff — DO NOT revert."
- Line 97 (Shipped v0.8.0 paragraph): "`window.napplet` is fully namespaced (`relay`, `ipc`, `services`, `storage`)" — historical Shipped-at-v0.8.0 description. Preserved.
- Line 112 (Validated bullet): "Inter-pane pubsub via kind 29003 topic events — existing" — dated pre-v0.16.0 Validated entry. Preserved.
- Line 140 (Validated bullet): INTER_PANE dispatch reference — Phase 22 historical. Preserved.
- Line 154 (Validated bullet): "BusKind.INTER_PANE renamed to BusKind.IPC_PEER ... v0.7.0 Phase 35" — explicit historical rename. Preserved per 123-CONTEXT.md.
- Line 164 (Validated bullet): `ipc`, `services`, `storage` listed as v0.8.0 Phase 41 shipped-at-that-time surface. Preserved.
- Lines 262-269 (Requirements table): References to v0.27.0 rename subjects. Current-tense but about the rename subject. Preserved as-is (these already describe the rename correctly per milestone kickoff).
- Line 298 (test coverage summary): "inter-pane" in a historical test-coverage bullet describing validated capability at v0.1.0+. Preserved as dated.

**.planning/STATE.md:** Zero edits. Per 123-CONTEXT.md: "already updated during milestone kickoff — DO NOT revert."

**.planning/ROADMAP.md:** Zero edits. Per 123-CONTEXT.md: "already updated during milestone kickoff — DO NOT revert." The "Phase 45 (IPC terminology cleanup)" note on line 39 describes a historical quick task and stays.

**.planning/research/ONTOLOGY.md:** Zero edits.
- Line 94: `BusKind.INTER_PANE` dated-assessment paragraph. The argument about IPC_PEER being an improvement is a dated investigation record. Preserved per 123-CONTEXT.md "dated investigation notes, rewrite conservatively (terminology only, not historical context)."
- Line 387 (table row): INTER_PANE message types row — dated investigation. Preserved.
- Line 404: "`INTER_PANE` -> `IPC_PEER`: Already planned" — dated v0.7.0-era argument. Preserved.
- Line 462 (Sources): "Android Binder IPC" — external ecosystem citation. Preserved (not our terminology).

**.planning/research/SDK_NAMING_PATTERNS.md:** Zero edits.
- Line 436 (Sources): "Electron IPC Tutorial" — external ecosystem citation. Preserved.

**.planning/research/SUMMARY.md:** Zero edits.
- Line 10 (Executive Summary, paragraph 2): Contains `inter-napplet communication` inside a dated 2026-04-05 research framing. Preserved per 123-CONTEXT.md "dated investigation notes" rule. (Note: rewrites to "inter-frame communication" would distort the paragraph's historical-research framing tense.)

**.planning/SPEC-GAPS.md:** Zero edits.
- Lines 543, 615 (GAP-09): "inter-napplet communication" in a Phase 84 / 2026-04-08 gap inventory entry marked `REMOVED — Spec-Backed`. Dated investigation — preserved per 123-CONTEXT.md.

**.planning/codebase/TESTING.md:** TWO edits (lines 83 + 152) — see Task 1 File 5. NOT preserved as-is. Rationale: the two `IPC` occurrences use the term generically to mean "the postMessage transport", describing the current shim implementation. Rewriting to "postMessage" is strictly more accurate and closes Phase 124's grep cleanly without path-based exclusions.

**Phase 124 trade-off flag:** If Phase 124's zero-grep acceptance criterion is applied strictly over `.planning/`, it will hit ONTOLOGY.md, SDK_NAMING_PATTERNS.md, SUMMARY.md, SPEC-GAPS.md, and PROJECT.md's Validated/Shipped bullets. Phase 124 must EITHER (a) add per-file exclusions to its acceptance grep for these explicitly-dated files, OR (b) re-open this decision and direct a rewrite. Document the trade-off in this plan's SUMMARY so Phase 124 inherits it transparently. (TESTING.md is NOT on the trade-off list — it is rewritten in this plan.)
</historical_preservation_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep .planning/codebase/*.md for current-tense IFC terminology + accurate transport terminology in TESTING.md</name>
  <read_first>
    - .planning/codebase/ARCHITECTURE.md (full file, ~195 lines — 5 `inter-pane` occurrences on lines 20, 44, 58, 80, 94)
    - .planning/codebase/STRUCTURE.md (full file, ~218 lines — 2 `inter-pane` occurrences on lines 68, 149)
    - .planning/codebase/INTEGRATIONS.md (full file, ~187 lines — 4 occurrences: `inter-pane` on lines 10, 47; `Inter-Pane Events` heading line 25; `INTER_PANE` line 168)
    - .planning/codebase/CONCERNS.md (full file, ~211 lines — 6 `inter-pane` occurrences on lines 22, 99, 103, 126, 127, 159)
    - .planning/codebase/TESTING.md (full file, ~269 lines — 2 `IPC` occurrences on lines 83, 152; both describe the current postMessage transport)
    - .planning/phases/123-documentation-sweep/123-CONTEXT.md (rewrite-rules section — "`.planning/codebase/*.md`: sweep for current-tense references to `window.napplet.ipc` / `ipc.emit` / 'inter-napplet' and rewrite. Historical summaries noting `BusKind.IPC_PEER` renames stay.")
  </read_first>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/CONCERNS.md, .planning/codebase/TESTING.md</files>
  <action>
    Each occurrence is a current-tense API-surface, scaling-limits, or transport-description — NONE of them is a historical "renamed to" statement. All are eligible for terminology swap.

    **File 1 — .planning/codebase/ARCHITECTURE.md (5 edits):**

    1. Line 20: `- Purpose: SDK for sandboxed iframe applications. Provides high-level relay API, NIP-07 proxy, inter-pane pubsub, storage proxy client`
       → `- Purpose: SDK for sandboxed iframe applications. Provides high-level relay API, NIP-07 proxy, inter-frame pubsub, storage proxy client`
       (One swap: `inter-pane` → `inter-frame`. Note: "NIP-07 proxy" is out-of-scope stale content per 123-CONTEXT.md — leave.)

    2. Line 44: `- **Topics** (\`packages/shell/src/topics.ts\`): Command constants for shell inter-pane routing (e.g., \`shell:storage-get\`, \`shell:relay-scoped-connect\`)`
       → `- **Topics** (\`packages/shell/src/topics.ts\`): Command constants for shell inter-frame routing (e.g., \`shell:storage-get\`, \`shell:relay-scoped-connect\`)`

    3. Line 58: `6. Router decision: special topic (storage, inter-pane) → proxy handler; normal event → relay pool publish`
       → `6. Router decision: special topic (storage, inter-frame) → proxy handler; normal event → relay pool publish`

    4. Line 80: `**Inter-pane Pubsub (topics):**`
       → `**Inter-frame Pubsub (topics):**`

    5. Line 94: `6. Shell returns response via inter-pane event`
       → `6. Shell returns response via inter-frame event`

    **File 2 — .planning/codebase/STRUCTURE.md (2 edits):**

    1. Line 68: `- Contains: Relay API, inter-pane pubsub, NIP-07 proxy, storage client, keypair management`
       → `- Contains: Relay API, inter-frame pubsub, NIP-07 proxy, storage client, keypair management`

    2. Line 149: `- \`emit()\`, \`on()\` — Inter-pane pubsub (camelCase, lowercase)`
       → `- \`emit()\`, \`on()\` — Inter-frame pubsub (camelCase, lowercase)`

    **File 3 — .planning/codebase/INTEGRATIONS.md (3 edits):**

    1. Line 10: `- Napplet sends REQ/EVENT/CLOSE messages; shell routes to real relays, local cache, or inter-pane subscriptions`
       → `- Napplet sends REQ/EVENT/CLOSE messages; shell routes to real relays, local cache, or inter-frame subscriptions`

    2. Line 25 (heading): `**Inter-Pane Events (NIP-29003):**`
       → `**Inter-Frame Events (NIP-29003):**`
       (Capitalized form — preserve capitalization pattern.)

    3. Line 47: `- Shell routes GET/SET/REMOVE/CLEAR operations via inter-pane messages`
       → `- Shell routes GET/SET/REMOVE/CLEAR operations via inter-frame messages`

    4. Line 168: `- 29003: INTER_PANE (napplet-to-napplet events)`
       - **DO NOT** swap `INTER_PANE` to `INTER_FRAME` here. This is a reference to a dated wire-protocol constant `BusKind.INTER_PANE` (renamed to `IPC_PEER` in v0.7.0 Phase 35 per PROJECT.md line 154). The line documents a message-kind description for the 29003 constant name as it existed at the time of this codebase analysis (2026-03-29 — pre-Phase-35).
       - Instead, **leave Line 168 unchanged** per the 123-CONTEXT.md rule "Historical summaries noting `BusKind.IPC_PEER` renames stay." The constant name was a HISTORICAL artifact even at the time the doc was written.
       - **Resolution:** Since `INTER_PANE` in this line refers to a historical constant name and not current-tense terminology, preserve the line as-is. Note the residual grep match in this task's SUMMARY.

    **File 4 — .planning/codebase/CONCERNS.md (6 edits):**

    1. Line 22: `- Impact: Shell-injected inter-pane events fail signature/hash verification. Napplets expecting valid event IDs may reject them.`
       → `- Impact: Shell-injected inter-frame events fail signature/hash verification. Napplets expecting valid event IDs may reject them.`

    2. Line 99: `**Inter-pane event delivery with no ordering guarantee:**`
       → `**Inter-frame event delivery with no ordering guarantee:**`

    3. Line 103: `- Test coverage: None. No test for multi-subscriber inter-pane event delivery.`
       → `- Test coverage: None. No test for multi-subscriber inter-frame event delivery.`

    4. Line 126: `- Current capacity: 100 inter-pane events in memory`
       → `- Current capacity: 100 inter-frame events in memory`

    5. Line 127: `- Limit: Beyond 100 events, old events drop off silently. Long-running shells with lots of inter-pane traffic lose history.`
       → `- Limit: Beyond 100 events, old events drop off silently. Long-running shells with lots of inter-frame traffic lose history.`

    6. Line 159: `- Problem: Napples can subscribe to any inter-pane event from any other napp. There's no built-in capability to restrict napplets to events from specific napp types (e.g., only listen to events from \`type:chat\`).`
       → `- Problem: Napples can subscribe to any inter-frame event from any other napp. There's no built-in capability to restrict napplets to events from specific napp types (e.g., only listen to events from \`type:chat\`).`

    **File 5 — .planning/codebase/TESTING.md (2 edits):**

    These two lines use "IPC" generically to describe the transport layer (postMessage between iframes), NOT the napplet runtime's `window.napplet.ipc` namespace that Phase 122 renamed. The accurate term is `postMessage` — the concrete transport the shim uses via `window.parent.postMessage()`. Rewriting closes Phase 124's `\bIPC\b` grep gate without needing path-based exclusions for TESTING.md.

    1. Line 83: `- \`window.parent.postMessage()\` — Core IPC mechanism in shim`
       → `- \`window.parent.postMessage()\` — Core postMessage transport in shim`

       Rationale: The bullet describes `window.parent.postMessage()` itself as the mechanism. "Core postMessage transport" is more precise than "Core IPC mechanism" — it names the actual transport rather than an abstract category. Column alignment / indentation / surrounding bullets are unchanged.

    2. Line 152: `- Scope: Napplet ↔ Shell IPC via pseudo-relay`
       → `- Scope: Napplet ↔ Shell postMessage via pseudo-relay`

       Rationale: The bullet is inside the "Integration Tests (to implement)" section. "Napplet ↔ Shell postMessage via pseudo-relay" is semantically identical and accurate — the pseudo-relay IS the postMessage-based transport the integration tests exercise. Leading `- Scope: ` and trailing ` via pseudo-relay` preserved byte-for-byte.

    **NO other edits to TESTING.md.** Specifically do NOT touch:
    - Line 158 reference to `hyprgate` — that's a separate deferred concern (stale reference-implementation mention; out of scope per 123-CONTEXT.md Deferred Ideas).
    - The "NIP-01 message patterns" / `AUTH_KIND = 22242` / `BusKind` references — pre-v0.16.0 dated analysis, out of scope.
    - The vitest recommendation, test-framework status, fixture examples — out of scope content for this phase.

    **Summary of edits across Task 1:** 5 + 2 + 3 (line 168 preserved) + 6 + 2 = **18 line-level edits across 5 files**.

    **No other changes.** Do NOT touch:
    - INTEGRATIONS.md line 168 (`INTER_PANE` historical constant name) — preserved per reasoning above.
    - Any line mentioning `NappKeyRegistry`, `nappStorage`, `shell/src/`, `AUTH_KIND = 22242`, NIP-01 wire format, etc. — those are pre-v0.16.0 staleness, out of scope per 123-CONTEXT.md Deferred Ideas.
    - Out-of-scope note: all five codebase/*.md files are dated 2026-03-29 and substantially pre-date the current 14-package monorepo. A full rewrite is a future deliverable, not this plan's scope.
  </action>
  <verify>
    <automated>
      # Zero `inter-pane` / `Inter-pane` / `Inter-Pane` in codebase/*.md:
      grep -nE "inter-pane|Inter-pane|Inter-Pane" .planning/codebase/*.md
      # Expected: zero matches, exit 1.

      # Positive IFC assertions (anchor lines):
      grep -cF "inter-frame pubsub" .planning/codebase/ARCHITECTURE.md
      # Expected: count == 1.
      grep -cF "Inter-frame Pubsub (topics):" .planning/codebase/ARCHITECTURE.md
      # Expected: count == 1.
      grep -cF "Inter-Frame Events (NIP-29003):" .planning/codebase/INTEGRATIONS.md
      # Expected: count == 1.
      grep -cF "inter-frame event delivery" .planning/codebase/CONCERNS.md
      # Expected: count == 1.

      # Preserved: INTEGRATIONS.md line 168 still says INTER_PANE (historical constant):
      grep -cF "29003: INTER_PANE" .planning/codebase/INTEGRATIONS.md
      # Expected: count == 1 (preserved).

      # TESTING.md: IPC swapped to postMessage on lines 83 and 152:
      grep -cF "Core postMessage transport in shim" .planning/codebase/TESTING.md
      # Expected: count == 1.
      grep -cF "Napplet ↔ Shell postMessage via pseudo-relay" .planning/codebase/TESTING.md
      # Expected: count == 1.
      # Zero residual `\bIPC\b` in TESTING.md (no remaining IPC matches):
      grep -cE "\bIPC\b" .planning/codebase/TESTING.md
      # Expected: count == 0.
      # Old "Core IPC mechanism" phrase gone:
      grep -cF "Core IPC mechanism in shim" .planning/codebase/TESTING.md
      # Expected: count == 0.
      grep -cF "Napplet ↔ Shell IPC via pseudo-relay" .planning/codebase/TESTING.md
      # Expected: count == 0.

      # Total: edit count check.
      git diff --numstat .planning/codebase/ARCHITECTURE.md .planning/codebase/STRUCTURE.md .planning/codebase/INTEGRATIONS.md .planning/codebase/CONCERNS.md .planning/codebase/TESTING.md
      # Expected: 5 files, ~18 line-changes total (5+2+3+6+2, line 168 in INTEGRATIONS.md preserved).
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "inter-pane|Inter-pane|Inter-Pane" .planning/codebase/*.md` returns zero matches.
    - `grep -nF "INTER_PANE" .planning/codebase/INTEGRATIONS.md` returns exactly 1 match on line 168 (historical constant reference preserved).
    - `grep -cE "\bIPC\b" .planning/codebase/TESTING.md` returns 0 (both occurrences rewritten to `postMessage`).
    - The heading on INTEGRATIONS.md line 25 reads `**Inter-Frame Events (NIP-29003):**`.
    - The heading on ARCHITECTURE.md line 80 reads `**Inter-frame Pubsub (topics):**`.
    - The heading on CONCERNS.md line 99 reads `**Inter-frame event delivery with no ordering guarantee:**`.
    - TESTING.md line 83 reads `- \`window.parent.postMessage()\` — Core postMessage transport in shim`.
    - TESTING.md line 152 reads `- Scope: Napplet ↔ Shell postMessage via pseudo-relay`.
    - `git diff --stat` for these five files shows: ARCHITECTURE.md (5 insertions, 5 deletions), STRUCTURE.md (2+2), INTEGRATIONS.md (3+3 — NOT 4, because line 168 preserved), CONCERNS.md (6+6), TESTING.md (2+2).
    - No source files, READMEs, skills, or other `.planning/` files touched.
  </acceptance_criteria>
  <done>
    `.planning/codebase/*.md` current-tense API-surface and transport descriptions use IFC terminology (in prose) and `postMessage` (in transport-layer descriptions). The historical `INTER_PANE` constant reference on INTEGRATIONS.md line 168 is preserved as a dated artifact. TESTING.md is fully clean for Phase 124's `\bIPC\b` grep. Phase 124 will see exactly one residual in codebase/ (the documented `INTER_PANE` on INTEGRATIONS.md line 168).
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix current-surface snippets in .planning/research/ARCHITECTURE.md + FEATURES-CHANNELS.md</name>
  <read_first>
    - .planning/research/ARCHITECTURE.md (around lines 380-395 — the NappletGlobal code-fence snippet)
    - .planning/research/FEATURES-CHANNELS.md (around lines 130-145 — the pub/sub vs channel comparison table)
    - .planning/phases/123-documentation-sweep/123-CONTEXT.md (rewrite rules — "research scratch docs: if they describe current API surface, rewrite")
  </read_first>
  <files>.planning/research/ARCHITECTURE.md, .planning/research/FEATURES-CHANNELS.md</files>
  <action>
    Both edits update dated-research content that ALSO functions as a reference to current API surface. The snippets are called out in 123-CONTEXT.md: "if they describe current API surface, rewrite."

    **File 1 — .planning/research/ARCHITECTURE.md (1 edit):**

    Line 387 (inside a TypeScript code fence showing the NappletGlobal interface shape):

    - BEFORE:
      ```typescript
      export interface NappletGlobal {
        relay: { ... };
        ipc:   { ... };
        storage: { ... };
        keys:  { ... };
        media: { ... };
        notify:{ ... };
        identity:{ ... };

        /**
         * Per-napplet declarative configuration.
         ...
      ```
    - AFTER (only line 387 changes):
      ```typescript
      export interface NappletGlobal {
        relay: { ... };
        ifc:   { ... };
        storage: { ... };
        keys:  { ... };
        media: { ... };
        notify:{ ... };
        identity:{ ... };

        /**
         * Per-napplet declarative configuration.
         ...
      ```

    ONE swap: `ipc:` → `ifc:`. Preserve the exact 3-space padding after the colon (original: `ipc:   {` — three spaces between `:` and `{`; new: `ifc:   {` — same three spaces for column alignment with `media:` on line 389 which has only one space). Actually looking at original carefully: `ipc:   { ... };` has three spaces after colon. `ifc:` is 4 chars like `ipc:`, so the three-space gap preserves column alignment byte-for-byte.

    **Why this edit is appropriate despite being in a dated research doc:** The NappletGlobal snippet is a structural reference to the shipped-in-v0.27.0 interface. The research doc was dated 2026-04-17 (v0.25.0 era) when `ipc:` was correct. Leaving it stale would propagate wrong API-shape intel to any agent reading this research file for integration reference. Per 123-CONTEXT.md: "if they describe current API surface, rewrite."

    **File 2 — .planning/research/FEATURES-CHANNELS.md (2 edits):**

    - BEFORE Line 132: `| Aspect | Pub/Sub (existing IPC, kind 29003) | Broadcast (new channel op) | Point-to-Point Channel |`
    - AFTER  Line 132: `| Aspect | Pub/Sub (existing IFC, kind 29003) | Broadcast (new channel op) | Point-to-Point Channel |`

    - BEFORE Line 140: `| **Status** | **Shipped** (kind 29003, \`window.napplet.ipc.emit/on\`) | **New** | **New** |`
    - AFTER  Line 140: `| **Status** | **Shipped** (kind 29003, \`window.napplet.ifc.emit/on\`) | **New** | **New** |`

    TWO swaps: header (`existing IPC` → `existing IFC`) and Status row (`window.napplet.ipc.emit/on` → `window.napplet.ifc.emit/on`). Preserve every pipe, backtick, and the `**Shipped**` / `**New**` bolding.

    **Why:** Both cells describe the CURRENT shipped API — a reader using this table to understand what's already available must see `ifc`, not `ipc`. Additionally, line 132's bare `IPC` acronym is matched by Phase 124's `\bIPC\b` criterion, so rewriting closes the grep gate cleanly.

    **No other changes.** Do NOT touch the `Inter-pane pubsub` / "Inter-pane" text elsewhere in FEATURES-CHANNELS.md (verified not present; confirm via read_first).
  </action>
  <verify>
    <automated>
      # research/ARCHITECTURE.md: ipc: key swapped to ifc:
      grep -nF "  ifc:   { ... };" .planning/research/ARCHITECTURE.md
      # Expected: count >= 1 (exact whitespace match for the NappletGlobal snippet).
      grep -nF "  ipc:   { ... };" .planning/research/ARCHITECTURE.md
      # Expected: count == 0.

      # research/FEATURES-CHANNELS.md: window.napplet.ifc in Status row:
      grep -cF "window.napplet.ifc.emit/on" .planning/research/FEATURES-CHANNELS.md
      # Expected: count == 1.
      grep -cF "window.napplet.ipc.emit/on" .planning/research/FEATURES-CHANNELS.md
      # Expected: count == 0.

      # research/FEATURES-CHANNELS.md: existing IFC in header row:
      grep -cF "Pub/Sub (existing IFC, kind 29003)" .planning/research/FEATURES-CHANNELS.md
      # Expected: count == 1.
      grep -cF "Pub/Sub (existing IPC, kind 29003)" .planning/research/FEATURES-CHANNELS.md
      # Expected: count == 0.

      # Nothing else changed in these files:
      git diff --numstat .planning/research/ARCHITECTURE.md
      # Expected: 1 insertion, 1 deletion.
      git diff --numstat .planning/research/FEATURES-CHANNELS.md
      # Expected: 2 insertions, 2 deletions.
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/research/ARCHITECTURE.md` line 387 reads `  ifc:   { ... };` (byte-for-byte column alignment preserved).
    - `.planning/research/FEATURES-CHANNELS.md` line 132 contains `Pub/Sub (existing IFC, kind 29003)`.
    - `.planning/research/FEATURES-CHANNELS.md` line 140 contains `window.napplet.ifc.emit/on`.
    - Neither `ipc:` nor `window.napplet.ipc` appears anywhere in these two files post-edit.
    - No other research files touched. In particular, `.planning/research/ONTOLOGY.md`, `.planning/research/SDK_NAMING_PATTERNS.md`, `.planning/research/SUMMARY.md` are byte-identical to pre-phase.
    - `git diff --stat .planning/research/` shows exactly 2 files changed (ARCHITECTURE.md + FEATURES-CHANNELS.md), 3 insertions, 3 deletions.
  </acceptance_criteria>
  <done>
    Research docs that function as current-API-surface references use IFC terminology. Dated historical-argumentation docs (ONTOLOGY.md, SDK_NAMING_PATTERNS.md, SUMMARY.md) are preserved per the 123-CONTEXT.md "dated investigation notes" rule.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify no regressions + produce Phase 124 trade-off record</name>
  <read_first>
    - The seven files modified by Tasks 1 and 2.
    - .planning/PROJECT.md (verify no accidental edits — expected byte-identical).
    - .planning/STATE.md (verify no accidental edits — expected byte-identical).
    - .planning/ROADMAP.md (verify no accidental edits — expected byte-identical).
    - .planning/research/ONTOLOGY.md (verify no accidental edits — expected byte-identical).
    - .planning/research/SDK_NAMING_PATTERNS.md (verify no accidental edits — expected byte-identical).
    - .planning/research/SUMMARY.md (verify no accidental edits — expected byte-identical).
    - .planning/SPEC-GAPS.md (verify no accidental edits — expected byte-identical).
  </read_first>
  <files>.planning/phases/123-documentation-sweep/123-03-NOTES.md</files>
  <action>
    This task creates a NOTES.md artifact (NOT a SUMMARY — that's the execute-plan workflow's responsibility) documenting:
    1. What Plan 03 rewrote (counted).
    2. What Plan 03 preserved and why (the historical-preservation map).
    3. What Phase 124 will find when running its repo-wide zero-grep — specifically, the residual `\bipc\b` / `\bIPC\b` / `IPC-PEER` / `INTER_PANE` matches that are DOCUMENTED exceptions. (TESTING.md is NOT in this residual list — it was rewritten.)
    4. Recommended Phase 124 acceptance-grep scope refinement.

    Write to `.planning/phases/123-documentation-sweep/123-03-NOTES.md` with the following content structure (the executor fills in the actual counts from a final grep run):

    ```markdown
    # Phase 123 Plan 03 — Active Planning Sweep NOTES

    **Purpose:** Document the terminology-swap scope and the deliberate historical-preservation exceptions so Phase 124 inherits the trade-off transparently.

    ## What this plan rewrote

    | File | Edits | Scope |
    |---|---|---|
    | .planning/codebase/ARCHITECTURE.md | 5 | Current-tense API-surface + routing descriptions |
    | .planning/codebase/STRUCTURE.md | 2 | Current-tense pubsub API list |
    | .planning/codebase/INTEGRATIONS.md | 3 | Current-tense integration descriptions (line 168 `INTER_PANE` constant preserved) |
    | .planning/codebase/CONCERNS.md | 6 | Current-tense fragile-areas + scaling-limits |
    | .planning/codebase/TESTING.md | 2 | Transport-layer descriptions (generic `IPC` → accurate `postMessage`) on lines 83, 152 |
    | .planning/research/ARCHITECTURE.md | 1 | NappletGlobal interface snippet (current API shape) |
    | .planning/research/FEATURES-CHANNELS.md | 2 | Pub/sub comparison table current-API cells |
    | **Total** | **21** | Terminology / transport-accuracy only |

    ## What this plan PRESERVED (historical-preservation map)

    Files left byte-identical to pre-phase state:
    - `.planning/PROJECT.md` — already clean at milestone kickoff. Historical Validated/Shipped bullets left as dated record of past milestones.
    - `.planning/STATE.md` — already clean at milestone kickoff.
    - `.planning/ROADMAP.md` — already clean at milestone kickoff. Phase 45 "IPC terminology cleanup" note on line 39 is a historical quick-task reference.
    - `.planning/research/ONTOLOGY.md` — dated ontology investigation (line 94 argues `INTER_PANE → IPC_PEER` is a good rename; line 404 discusses the same; table row on line 387 is a dated assessment; line 462 external "Android Binder IPC" citation).
    - `.planning/research/SDK_NAMING_PATTERNS.md` — external "Electron IPC Tutorial" citation on line 436.
    - `.planning/research/SUMMARY.md` — 2026-04-05 executive summary; single `inter-napplet communication` reference inside dated research framing paragraph (line 10).
    - `.planning/SPEC-GAPS.md` — GAP-09 `REMOVED — Spec-Backed` entry dated Phase 84 / 2026-04-08; contains `inter-napplet communication` in dated investigation prose (lines 543, 615).
    - `.planning/codebase/INTEGRATIONS.md` line 168 — `29003: INTER_PANE` message-kind description; `INTER_PANE` is the historical constant name (renamed to `IPC_PEER` in v0.7.0 Phase 35; subsequently touched in v0.27.0 Phase 122 at the source level).

    **Note:** `.planning/codebase/TESTING.md` is NOT preserved — its two generic `IPC` references (lines 83, 152) were rewritten to `postMessage`, the accurate transport term. See "What this plan rewrote" above.

    ## Phase 124 trade-off

    When Phase 124 runs its repo-wide zero-grep across `packages/`, `specs/`, `skills/`, `README.md`, and active `.planning/` docs, it WILL find the following residual matches that this plan deliberately preserved:

    | File | Residual match | Justification |
    |---|---|---|
    | .planning/PROJECT.md (multiple lines) | `ipc`, `INTER_PANE`, `Inter-pane` | Historical Shipped/Validated bullets describing past milestones |
    | .planning/research/ONTOLOGY.md lines 94, 387, 404, 462 | `IPC`, `INTER_PANE`, `IPC_PEER`, "Binder IPC" | Dated ontology investigation + external ecosystem citation |
    | .planning/research/SDK_NAMING_PATTERNS.md line 436 | `IPC` | External "Electron IPC Tutorial" citation |
    | .planning/research/SUMMARY.md line 10 | `inter-napplet` | Dated executive summary paragraph |
    | .planning/SPEC-GAPS.md lines 543, 615 | `inter-napplet` | Dated Phase 84 gap-inventory entry |
    | .planning/codebase/INTEGRATIONS.md line 168 | `INTER_PANE` | Historical wire-kind constant name |

    **Phase 124 MUST** either:
    - (a) Refine its acceptance grep to EXCLUDE these explicitly-dated paths/lines (recommended: a path-based exclusion for `.planning/research/ONTOLOGY.md`, `.planning/research/SDK_NAMING_PATTERNS.md`, `.planning/research/SUMMARY.md`, `.planning/SPEC-GAPS.md`, and line-specific exclusions for `.planning/PROJECT.md` Shipped/Validated sections + `.planning/codebase/INTEGRATIONS.md` line 168), OR
    - (b) Re-open this decision via a Plan 03 revision and direct a further rewrite.

    Option (a) is consistent with 123-CONTEXT.md's "dated investigation notes, rewrite conservatively (terminology only, not historical context)" rule and is recommended. This preserves the historical fidelity of research docs describing the IFC rename's own provenance.

    ## Final acceptance grep (Phase 123 scope)

    The current-state-API surface is now IFC-clean (and transport-accurate) across:
    - Root README.md (Plan 01)
    - packages/core/README.md (Plan 01)
    - packages/shim/README.md (Plan 01)
    - packages/sdk/README.md (Plan 01)
    - skills/build-napplet/SKILL.md (Plan 02)
    - .planning/codebase/ARCHITECTURE.md (Plan 03 Task 1)
    - .planning/codebase/STRUCTURE.md (Plan 03 Task 1)
    - .planning/codebase/INTEGRATIONS.md except line 168 (Plan 03 Task 1)
    - .planning/codebase/CONCERNS.md (Plan 03 Task 1)
    - .planning/codebase/TESTING.md (Plan 03 Task 1) — now zero `\bIPC\b` matches
    - .planning/research/ARCHITECTURE.md (Plan 03 Task 2)
    - .planning/research/FEATURES-CHANNELS.md (Plan 03 Task 2)

    The current-surface zero-grep for lowercased `\bipc\b` AND uppercase `\bIPC\b` passes across all files above (except .planning/PROJECT.md Shipped/Validated bullets which carry historical Shipped content — not part of this plan's scope per 123-CONTEXT.md).
    ```

    After writing the NOTES.md, run the final verification greps and annotate the NOTES.md with actual counts (the executor will do this).
  </action>
  <verify>
    <automated>
      # Phase 123 in-scope acceptance grep (what Plans 01+02+03 collectively should have cleaned — now includes TESTING.md):
      grep -lE "\bipc\b|\bIPC\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane" \
        README.md \
        packages/core/README.md \
        packages/shim/README.md \
        packages/sdk/README.md \
        skills/build-napplet/SKILL.md \
        .planning/codebase/ARCHITECTURE.md \
        .planning/codebase/STRUCTURE.md \
        .planning/codebase/CONCERNS.md \
        .planning/codebase/TESTING.md \
        .planning/research/ARCHITECTURE.md \
        .planning/research/FEATURES-CHANNELS.md
      # Expected: empty output (no files match), exit 1.

      # .planning/codebase/INTEGRATIONS.md: exactly one residual INTER_PANE on line 168:
      grep -nE "\bipc\b|\bIPC\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane" .planning/codebase/INTEGRATIONS.md
      # Expected: exactly 1 line (line 168: "29003: INTER_PANE").

      # Documented-exception files are byte-identical to pre-phase:
      # (Execute: git diff --exit-code .planning/PROJECT.md; should exit 0.)
      git diff --exit-code .planning/PROJECT.md
      git diff --exit-code .planning/STATE.md
      git diff --exit-code .planning/ROADMAP.md
      git diff --exit-code .planning/research/ONTOLOGY.md
      git diff --exit-code .planning/research/SDK_NAMING_PATTERNS.md
      git diff --exit-code .planning/research/SUMMARY.md
      git diff --exit-code .planning/SPEC-GAPS.md
      # All expected: exit 0 (no changes).

      # NOTES.md exists:
      test -f .planning/phases/123-documentation-sweep/123-03-NOTES.md
      # Expected: exit 0.
    </automated>
  </verify>
  <acceptance_criteria>
    - Final grep across all in-scope files (the 11 files listed in the verify block's first grep — now including TESTING.md) returns zero matches.
    - `.planning/codebase/INTEGRATIONS.md` has exactly ONE residual match on line 168 (`INTER_PANE` historical constant).
    - All seven documented-exception files (`.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/research/ONTOLOGY.md`, `.planning/research/SDK_NAMING_PATTERNS.md`, `.planning/research/SUMMARY.md`, `.planning/SPEC-GAPS.md`) are byte-identical to their pre-phase state (`git diff --exit-code` exits 0 for each).
    - `.planning/phases/123-documentation-sweep/123-03-NOTES.md` exists and contains:
      - A "What this plan rewrote" table listing all 21 edits across 7 files (INCLUDING the TESTING.md row).
      - A "What this plan PRESERVED" section listing the 7 untouched files + INTEGRATIONS.md line 168 with justification, and an explicit note that TESTING.md is NOT preserved (was rewritten).
      - A "Phase 124 trade-off" section enumerating residual matches Phase 124 will find (TESTING.md NOT in the residuals table), with Option (a)/(b) recommendation.
      - A "Final acceptance grep" section confirming the Phase 123 in-scope clean across 11 files (READMEs + skill + 4 codebase + TESTING.md + 2 research files).
    - No source files (`packages/*/src/`), READMEs, or skills files modified by this task.
  </acceptance_criteria>
  <done>
    Phase 123 active-planning sweep is complete. Current-API-surface references in `.planning/codebase/` and the two current-surface-reference research files use IFC terminology. TESTING.md transport-layer text uses accurate `postMessage` terminology. Seven files with dated historical/external content are preserved. Phase 124 inherits a documented trade-off record it can use to refine its repo-wide acceptance grep scope — with TESTING.md removed from the residual-exceptions list.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. **Phase 123 in-scope zero-grep (PLAN-01 + cross-plan completeness):**
   ```bash
   grep -lE "\bipc\b|\bIPC\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane" \
     README.md \
     packages/core/README.md \
     packages/shim/README.md \
     packages/sdk/README.md \
     skills/build-napplet/SKILL.md \
     .planning/codebase/ARCHITECTURE.md \
     .planning/codebase/STRUCTURE.md \
     .planning/codebase/CONCERNS.md \
     .planning/codebase/TESTING.md \
     .planning/research/ARCHITECTURE.md \
     .planning/research/FEATURES-CHANNELS.md
   ```
   Expected: empty output, exit 1.

2. **Documented residual match in INTEGRATIONS.md (line 168, historical constant):**
   ```bash
   grep -nE "\bipc\b|\bIPC\b|IPC-PEER|IPC_PEER|inter-pane|inter-napplet|Inter-napplet|Inter-pane|INTER_PANE|Inter-Pane" .planning/codebase/INTEGRATIONS.md
   ```
   Expected: exactly 1 line (line 168).

3. **Preserved historical files byte-identical:**
   ```bash
   for f in .planning/PROJECT.md .planning/STATE.md .planning/ROADMAP.md \
            .planning/research/ONTOLOGY.md .planning/research/SDK_NAMING_PATTERNS.md \
            .planning/research/SUMMARY.md .planning/SPEC-GAPS.md; do
     git diff --exit-code "$f" || echo "UNEXPECTED CHANGE: $f"
   done
   ```
   Expected: no "UNEXPECTED CHANGE" output.

4. **NOTES.md artifact exists:**
   ```bash
   test -f .planning/phases/123-documentation-sweep/123-03-NOTES.md && echo "ok"
   ```
   Expected: `ok`.

5. **Commit trail:** Three commits land on main in order: Task 1 (codebase/) → Task 2 (research/) → Task 3 (NOTES.md). Each commit touches only the files declared in its `<files>` field.
</verification>

<success_criteria>
- `.planning/codebase/ARCHITECTURE.md`, `STRUCTURE.md`, `CONCERNS.md`, `TESTING.md` are fully IFC-clean / transport-accurate for the phase's terminology set — including zero `\bIPC\b` matches in TESTING.md.
- `.planning/codebase/INTEGRATIONS.md` is IFC-clean except the documented exception on line 168 (`INTER_PANE` historical constant).
- `.planning/research/ARCHITECTURE.md` NappletGlobal snippet uses `ifc:`.
- `.planning/research/FEATURES-CHANNELS.md` pub/sub comparison-table header and Status row use IFC terminology.
- Seven documented-exception files (`.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, three research files, and SPEC-GAPS.md) are byte-identical to pre-phase.
- `.planning/phases/123-documentation-sweep/123-03-NOTES.md` documents the trade-off so Phase 124 can refine its acceptance scope. TESTING.md is NOT listed in the residual-exceptions table (it was rewritten, not preserved).
- PLAN-01 requirement satisfied.
- No source (`packages/*/src/`), READMEs, or skills touched (Plans 01+02's territory).
- Phase 123 closes with a verified zero-grep across the cleaned surface (now including TESTING.md) and a documented residual set that Phase 124 inherits.
</success_criteria>

<output>
After completion, create `.planning/phases/123-documentation-sweep/123-03-SUMMARY.md` documenting:
- Files modified (7 active-planning docs — including TESTING.md)
- Total edit count (21 edits across 7 files)
- Files preserved with justification (7 documented-exception files + INTEGRATIONS.md line 168). TESTING.md explicitly NOT on the preserved list — it was rewritten.
- Final zero-grep evidence for the Phase 123 in-scope surface (11 files clean, TESTING.md included)
- Reference to `.planning/phases/123-documentation-sweep/123-03-NOTES.md` for Phase 124 trade-off record
- Confirmation that Plans 01 and 02 (Wave 1) landed cleanly before Plan 03 started — cross-wave dependency respected
</output>
