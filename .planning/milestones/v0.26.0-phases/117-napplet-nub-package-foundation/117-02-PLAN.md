---
phase: 117-napplet-nub-package-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "117-01"
files_modified:
  - packages/nub/src/relay/index.ts
  - packages/nub/src/relay/types.ts
  - packages/nub/src/relay/shim.ts
  - packages/nub/src/relay/sdk.ts
  - packages/nub/src/storage/index.ts
  - packages/nub/src/storage/types.ts
  - packages/nub/src/storage/shim.ts
  - packages/nub/src/storage/sdk.ts
  - packages/nub/src/ifc/index.ts
  - packages/nub/src/ifc/types.ts
  - packages/nub/src/ifc/shim.ts
  - packages/nub/src/ifc/sdk.ts
  - packages/nub/src/keys/index.ts
  - packages/nub/src/keys/types.ts
  - packages/nub/src/keys/shim.ts
  - packages/nub/src/keys/sdk.ts
  - packages/nub/src/theme/index.ts
  - packages/nub/src/theme/types.ts
  - packages/nub/src/theme/shim.ts
  - packages/nub/src/theme/sdk.ts
  - packages/nub/src/media/index.ts
  - packages/nub/src/media/types.ts
  - packages/nub/src/media/shim.ts
  - packages/nub/src/media/sdk.ts
  - packages/nub/src/notify/index.ts
  - packages/nub/src/notify/types.ts
  - packages/nub/src/notify/shim.ts
  - packages/nub/src/notify/sdk.ts
  - packages/nub/src/identity/index.ts
  - packages/nub/src/identity/types.ts
  - packages/nub/src/identity/shim.ts
  - packages/nub/src/identity/sdk.ts
  - packages/nub/src/config/index.ts
  - packages/nub/src/config/types.ts
  - packages/nub/src/config/shim.ts
  - packages/nub/src/config/sdk.ts
autonomous: true
requirements:
  - PKG-01

must_haves:
  truths:
    - "packages/nub/src/<domain>/ exists for all 9 domains (relay, storage, ifc, keys, theme, media, notify, identity, config)"
    - "Each domain directory contains four files: index.ts, types.ts, shim.ts, sdk.ts — 36 source files total"
    - "Each file's content is byte-identical to its source at packages/nubs/<domain>/src/<file>.ts"
    - "The 8 barrel files (all except config/index.ts) preserve their trailing registerNub(DOMAIN, ...) side-effect call unchanged"
    - "config/index.ts preserves its side-effect-free barrel shape (no registerNub call) matching the existing packages/nubs/config/src/index.ts"
    - "Every <domain>/types.ts file contains only type/constant declarations — no runtime imports from @napplet/core beyond `import type` and the DOMAIN const export"
    - "Existing packages/nubs/<domain>/ directories are UNTOUCHED — this is a copy, not a move"
  artifacts:
    - path: "packages/nub/src/relay/index.ts"
      provides: "Relay domain barrel with registerNub side effect"
      contains: "registerNub(DOMAIN"
    - path: "packages/nub/src/config/index.ts"
      provides: "Config domain barrel — NO registerNub call (side-effect-free per existing pattern)"
      contains: "export { DOMAIN }"
    - path: "packages/nub/src/relay/types.ts"
      provides: "Relay message type definitions"
      contains: "export interface RelaySubscribeMessage"
    - path: "packages/nub/src/storage/types.ts"
      provides: "Storage message type definitions"
      contains: "DOMAIN"
    - path: "packages/nub/src/ifc/types.ts"
      provides: "IFC message type definitions"
      contains: "DOMAIN"
    - path: "packages/nub/src/keys/types.ts"
      provides: "Keys message type definitions"
      contains: "DOMAIN"
    - path: "packages/nub/src/theme/types.ts"
      provides: "Theme message type definitions"
      contains: "DOMAIN"
    - path: "packages/nub/src/media/types.ts"
      provides: "Media message type definitions"
      contains: "DOMAIN"
    - path: "packages/nub/src/notify/types.ts"
      provides: "Notify message type definitions"
      contains: "DOMAIN"
    - path: "packages/nub/src/identity/types.ts"
      provides: "Identity message type definitions"
      contains: "DOMAIN"
    - path: "packages/nub/src/config/types.ts"
      provides: "Config message type definitions"
      contains: "DOMAIN"
  key_links:
    - from: "packages/nub/src/<domain>/index.ts (for 8 of 9)"
      to: "@napplet/core registerNub"
      via: "import { registerNub } from '@napplet/core' at barrel bottom + registerNub(DOMAIN, ...) call"
      pattern: "import \\{ registerNub \\} from '@napplet/core'"
    - from: "packages/nub/src/<domain>/index.ts"
      to: "./types.js, ./shim.js, ./sdk.js"
      via: "explicit type + value re-exports"
      pattern: "export.*from './(types|shim|sdk)\\.js'"
    - from: "packages/nub/src/<domain>/shim.ts"
      to: "./types.js"
      via: "import type statements"
      pattern: "import type .* from './types\\.js'"
---

<objective>
Populate `packages/nub/src/` with a verbatim copy of all 9 NUB domains' current source. Each of the 9 existing `packages/nubs/<domain>/src/{index,types,shim,sdk}.ts` files is copied byte-for-byte into `packages/nub/src/<domain>/{index,types,shim,sdk}.ts`. Zero logic changes, zero import rewrites (none are needed — existing NUB source only imports from `@napplet/core`, never from sibling `@napplet/nub-<otherdomain>` packages).

Purpose: Move the authoritative source into the consolidated package so Plan 03's build has something to compile. The existing `packages/nubs/<domain>/` packages remain intact and unmodified — they continue to build and ship as-is this phase. Phase 118 will convert them into re-export shims.

Output: 36 new TypeScript source files under `packages/nub/src/` across 9 domain subdirectories, each byte-identical to its counterpart under `packages/nubs/<domain>/src/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-napplet-nub-package-foundation/117-CONTEXT.md
@.planning/phases/117-napplet-nub-package-foundation/117-01-SUMMARY.md

# Sources — these are COPIED, not moved. All 9 domains × 4 files = 36 source files.
@packages/nubs/relay/src/index.ts
@packages/nubs/relay/src/types.ts
@packages/nubs/relay/src/shim.ts
@packages/nubs/relay/src/sdk.ts
@packages/nubs/storage/src/index.ts
@packages/nubs/storage/src/types.ts
@packages/nubs/storage/src/shim.ts
@packages/nubs/storage/src/sdk.ts
@packages/nubs/ifc/src/index.ts
@packages/nubs/ifc/src/types.ts
@packages/nubs/ifc/src/shim.ts
@packages/nubs/ifc/src/sdk.ts
@packages/nubs/keys/src/index.ts
@packages/nubs/keys/src/types.ts
@packages/nubs/keys/src/shim.ts
@packages/nubs/keys/src/sdk.ts
@packages/nubs/theme/src/index.ts
@packages/nubs/theme/src/types.ts
@packages/nubs/theme/src/shim.ts
@packages/nubs/theme/src/sdk.ts
@packages/nubs/media/src/index.ts
@packages/nubs/media/src/types.ts
@packages/nubs/media/src/shim.ts
@packages/nubs/media/src/sdk.ts
@packages/nubs/notify/src/index.ts
@packages/nubs/notify/src/types.ts
@packages/nubs/notify/src/shim.ts
@packages/nubs/notify/src/sdk.ts
@packages/nubs/identity/src/index.ts
@packages/nubs/identity/src/types.ts
@packages/nubs/identity/src/shim.ts
@packages/nubs/identity/src/sdk.ts
@packages/nubs/config/src/index.ts
@packages/nubs/config/src/types.ts
@packages/nubs/config/src/shim.ts
@packages/nubs/config/src/sdk.ts

<interfaces>
<!-- Key facts about the source files, pre-extracted so the executor does not need to introspect. -->

## Side-effect registration status per domain

| Domain   | index.ts calls registerNub? | Notes                                                                                         |
|----------|-----------------------------|-----------------------------------------------------------------------------------------------|
| relay    | YES                         | `registerNub(DOMAIN, ...)` at bottom of file; import { registerNub } from '@napplet/core'     |
| storage  | YES                         | same shape                                                                                    |
| ifc      | YES                         | same shape                                                                                    |
| keys     | YES                         | same shape                                                                                    |
| theme    | YES                         | same shape                                                                                    |
| media    | YES                         | same shape                                                                                    |
| notify   | YES                         | same shape                                                                                    |
| identity | YES                         | same shape                                                                                    |
| config   | NO                          | barrel is side-effect-free — central shim handles registration. Preserve this exactly.        |

## Import-path discipline

- Existing NUB source files use relative imports like `./types.js`, `./shim.js`, `./sdk.js` inside their own domain. These are ALREADY correct for the new location — no rewrite.
- Existing NUB source files import from `@napplet/core`. This stays `@napplet/core` — still resolvable from the new package (@napplet/nub has @napplet/core as runtime dep per Plan 01).
- NO existing NUB source file imports from any `@napplet/nub-<otherdomain>` package. Verified via grep during planning. Therefore ZERO import rewrites are needed.

## File docstring identifiers

Existing files carry `@napplet/nub-<domain>` in their opening JSDoc. These docstring mentions are informational references, not import paths. They do NOT need to be rewritten in this phase — Plan 02 is a byte-identical copy. (Phase 120 handles doc updates.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy all 9 domains' source trees into packages/nub/src/&lt;domain&gt;/</name>
  <files>packages/nub/src/relay/index.ts, packages/nub/src/relay/types.ts, packages/nub/src/relay/shim.ts, packages/nub/src/relay/sdk.ts, packages/nub/src/storage/index.ts, packages/nub/src/storage/types.ts, packages/nub/src/storage/shim.ts, packages/nub/src/storage/sdk.ts, packages/nub/src/ifc/index.ts, packages/nub/src/ifc/types.ts, packages/nub/src/ifc/shim.ts, packages/nub/src/ifc/sdk.ts, packages/nub/src/keys/index.ts, packages/nub/src/keys/types.ts, packages/nub/src/keys/shim.ts, packages/nub/src/keys/sdk.ts, packages/nub/src/theme/index.ts, packages/nub/src/theme/types.ts, packages/nub/src/theme/shim.ts, packages/nub/src/theme/sdk.ts, packages/nub/src/media/index.ts, packages/nub/src/media/types.ts, packages/nub/src/media/shim.ts, packages/nub/src/media/sdk.ts, packages/nub/src/notify/index.ts, packages/nub/src/notify/types.ts, packages/nub/src/notify/shim.ts, packages/nub/src/notify/sdk.ts, packages/nub/src/identity/index.ts, packages/nub/src/identity/types.ts, packages/nub/src/identity/shim.ts, packages/nub/src/identity/sdk.ts, packages/nub/src/config/index.ts, packages/nub/src/config/types.ts, packages/nub/src/config/shim.ts, packages/nub/src/config/sdk.ts</files>
  <read_first>
    - packages/nubs/relay/src/index.ts, types.ts, shim.ts, sdk.ts
    - packages/nubs/storage/src/index.ts, types.ts, shim.ts, sdk.ts
    - packages/nubs/ifc/src/index.ts, types.ts, shim.ts, sdk.ts
    - packages/nubs/keys/src/index.ts, types.ts, shim.ts, sdk.ts
    - packages/nubs/theme/src/index.ts, types.ts, shim.ts, sdk.ts
    - packages/nubs/media/src/index.ts, types.ts, shim.ts, sdk.ts
    - packages/nubs/notify/src/index.ts, types.ts, shim.ts, sdk.ts
    - packages/nubs/identity/src/index.ts, types.ts, shim.ts, sdk.ts
    - packages/nubs/config/src/index.ts, types.ts, shim.ts, sdk.ts
    - .planning/phases/117-napplet-nub-package-foundation/117-CONTEXT.md (verify the "copy, not move" decision and side-effect preservation rule)
  </read_first>
  <action>
Copy the source trees verbatim. This is a mechanical, byte-identical copy. The most reliable way is `cp -R` since it preserves bytes exactly:

```bash
# Create the 9 domain subdirectories under packages/nub/src/
mkdir -p packages/nub/src/relay packages/nub/src/storage packages/nub/src/ifc \
         packages/nub/src/keys packages/nub/src/theme packages/nub/src/media \
         packages/nub/src/notify packages/nub/src/identity packages/nub/src/config

# Copy each of the 4 source files for each of the 9 domains
for d in relay storage ifc keys theme media notify identity config; do
  cp packages/nubs/$d/src/index.ts packages/nub/src/$d/index.ts
  cp packages/nubs/$d/src/types.ts packages/nub/src/$d/types.ts
  cp packages/nubs/$d/src/shim.ts  packages/nub/src/$d/shim.ts
  cp packages/nubs/$d/src/sdk.ts   packages/nub/src/$d/sdk.ts
done

# Remove the .gitkeep placeholder from Plan 01 now that real source exists
rm -f packages/nub/src/.gitkeep
```

DO NOT edit the copied files. DO NOT change import paths. DO NOT touch `packages/nubs/<domain>/*` — those remain intact and continue to build from their own sources this phase.

DO NOT rewrite the `@napplet/nub-<domain>` strings in JSDoc comments — those are informational file docstrings, not import paths, and phase 120 handles doc updates. The `import`/`export` statements already use relative paths (`./types.js`) or `@napplet/core` and need no changes.

The config domain's `index.ts` has NO `registerNub` call — that's intentional per the existing pattern (central shim handles config registration). Preserve that exactly. The 8 other domains' `index.ts` files DO call `registerNub(DOMAIN, ...)` at the bottom — preserve those calls.

After copying, verify byte equality with `diff -q`:

```bash
for d in relay storage ifc keys theme media notify identity config; do
  for f in index.ts types.ts shim.ts sdk.ts; do
    diff -q "packages/nubs/$d/src/$f" "packages/nub/src/$d/$f" || echo "MISMATCH: $d/$f"
  done
done
# Expected: zero output lines (all files byte-identical)
```
  </action>
  <verify>
    <automated>test -d packages/nub/src/relay &amp;&amp; test -d packages/nub/src/storage &amp;&amp; test -d packages/nub/src/ifc &amp;&amp; test -d packages/nub/src/keys &amp;&amp; test -d packages/nub/src/theme &amp;&amp; test -d packages/nub/src/media &amp;&amp; test -d packages/nub/src/notify &amp;&amp; test -d packages/nub/src/identity &amp;&amp; test -d packages/nub/src/config &amp;&amp; for d in relay storage ifc keys theme media notify identity config; do for f in index.ts types.ts shim.ts sdk.ts; do test -f "packages/nub/src/$d/$f" || { echo "MISSING: packages/nub/src/$d/$f"; exit 1; }; diff -q "packages/nubs/$d/src/$f" "packages/nub/src/$d/$f" || { echo "MISMATCH: $d/$f"; exit 1; }; done; done &amp;&amp; for d in relay storage ifc keys theme media notify identity; do grep -q "registerNub(DOMAIN" "packages/nub/src/$d/index.ts" || { echo "missing registerNub in $d/index.ts"; exit 1; }; done &amp;&amp; if grep -q "registerNub" packages/nub/src/config/index.ts; then echo "config/index.ts must NOT contain registerNub"; exit 1; fi &amp;&amp; ! test -f packages/nub/src/.gitkeep &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 9 directories exist: `packages/nub/src/{relay,storage,ifc,keys,theme,media,notify,identity,config}/`
    - Each of the 9 directories contains exactly these 4 files: `index.ts`, `types.ts`, `shim.ts`, `sdk.ts`
    - Total new source file count under `packages/nub/src/`: exactly 36 `.ts` files (0 other files)
    - For every `<domain>` in {relay, storage, ifc, keys, theme, media, notify, identity, config} and every `<file>` in {index, types, shim, sdk}: `diff -q packages/nubs/<domain>/src/<file>.ts packages/nub/src/<domain>/<file>.ts` exits 0 (byte-identical)
    - `grep -l "registerNub(DOMAIN" packages/nub/src/*/index.ts | sort` lists exactly these 8 files: `packages/nub/src/{ifc,identity,keys,media,notify,relay,storage,theme}/index.ts`
    - `packages/nub/src/config/index.ts` does NOT contain the string `registerNub` (NEGATIVE assertion — preserves existing side-effect-free config barrel)
    - `packages/nub/src/config/index.ts` DOES contain `export { DOMAIN } from './types.js';`
    - `packages/nub/src/.gitkeep` no longer exists (removed now that real source populates the directory)
    - `packages/nubs/<domain>/src/*` files are UNTOUCHED — `git diff packages/nubs/` shows zero changes
    - No `@napplet/nub-<otherdomain>` imports exist in the copied source: `grep -r "@napplet/nub-" packages/nub/src/` returns zero matches (only `@napplet/core` imports are allowed; verified via grep)
    - All relative imports resolve locally: every `./types.js`, `./shim.js`, `./sdk.js` reference sits next to its target in the same domain directory
  </acceptance_criteria>
  <done>`packages/nub/src/` contains all 9 domain directories populated with 36 byte-identical copies of the corresponding `packages/nubs/<domain>/src/` sources. The 8 domains that register with core still do so; config remains side-effect-free. Existing `packages/nubs/` source trees are unchanged. The `.gitkeep` placeholder from Plan 01 is removed.</done>
</task>

</tasks>

<verification>
Run the `diff -q` loop from the action block — it must produce zero output. Additionally:

1. `find packages/nub/src -type f -name '*.ts' | wc -l` — must print `36`
2. `grep -rc "registerNub(DOMAIN" packages/nub/src/ | grep -c ':1$'` — must print `8` (eight index.ts files carry the call)
3. `grep -L "registerNub" packages/nub/src/*/index.ts` — must list exactly `packages/nub/src/config/index.ts`
4. `git diff --stat packages/nubs/ | wc -l` — must print `0` (existing NUB packages untouched)
</verification>

<success_criteria>
- packages/nub/src/ has 9 domain directories, 36 source files total (PKG-01)
- Every file byte-identical to its source in packages/nubs/<domain>/src/
- 8 of 9 barrel index.ts files preserve registerNub(DOMAIN, ...) side effect
- config/index.ts remains side-effect-free (matches existing config NUB pattern)
- packages/nubs/ sources are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/117-napplet-nub-package-foundation/117-02-SUMMARY.md` noting: 36 files copied across 9 domains, byte-equality verified via diff -q, 8 barrels preserve registerNub side effects, config/index.ts is side-effect-free, and no edits were made to packages/nubs/.
</output>
