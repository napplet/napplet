---
phase: 60-channel-protocol-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/nubs/NUB-RELAY.md
  - specs/nubs/NUB-STORAGE.md
  - specs/nubs/NUB-SIGNER.md
autonomous: true
requirements:
  - NUB-02

must_haves:
  truths:
    - "NUB-RELAY.md follows TEMPLATE-WORD.md structure with all sections filled"
    - "NUB-STORAGE.md documents the async localStorage-like API surface"
    - "NUB-SIGNER.md is minimal, referencing NIP-07 for the interface contract"
    - "All three specs use setext headings and draft badge"
    - "All three specs include Discovery, API Surface, Shell Behavior, Event Kinds, Security, and Implementations sections"
  artifacts:
    - path: "specs/nubs/NUB-RELAY.md"
      provides: "NUB-RELAY interface specification"
      contains: "window.napplet.relay"
    - path: "specs/nubs/NUB-STORAGE.md"
      provides: "NUB-STORAGE interface specification"
      contains: "window.napplet.storage"
    - path: "specs/nubs/NUB-SIGNER.md"
      provides: "NUB-SIGNER interface specification"
      contains: "window.nostr"
  key_links:
    - from: "specs/nubs/NUB-RELAY.md"
      to: "specs/NIP-5D.md"
      via: "NUB-RELAY discovery identifier"
      pattern: "shell.supports.*NUB-RELAY"
---

<objective>
Write three NUB-WORD interface specs: NUB-RELAY (NIP-01 relay proxy), NUB-STORAGE (scoped key-value storage), and NUB-SIGNER (NIP-07 signer proxy). Each follows the TEMPLATE-WORD.md format. NUB-RELAY and NUB-STORAGE get full treatment; NUB-SIGNER is minimal since it delegates to NIP-07.

Purpose: Document the three most fundamental shell-provided capabilities as standalone NUB interface specs, extracted from NIP-5D v1 content and existing shim code.

Output: Three new files in specs/nubs/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-channel-protocol-implementation/60-CONTEXT.md
@specs/nubs/README.md
@specs/nubs/TEMPLATE-WORD.md
@specs/NIP-5D.md
@packages/shim/src/relay-shim.ts
@packages/shim/src/state-shim.ts
@packages/shim/src/index.ts
@packages/core/src/constants.ts
@packages/core/src/topics.ts

<interfaces>
<!-- Source APIs from shim code -->

NUB-RELAY API (from relay-shim.ts):
  subscribe(filters, onEvent, onEose, options?): Subscription
  publish(template, options?): Promise<NostrEvent>
  query(filters): Promise<NostrEvent[]>
  Namespace: window.napplet.relay
  Bus kinds used: None directly (uses NIP-01 REQ/EVENT/CLOSE verbs)
  Scoped relay: kind 29001 with topic shell:relay-scoped-connect / shell:relay-scoped-close / shell:relay-scoped-publish

NUB-STORAGE API (from state-shim.ts):
  getItem(key): Promise<string | null>
  setItem(key, value): Promise<void>
  removeItem(key): Promise<void>
  keys(): Promise<string[]>
  Namespace: window.napplet.storage
  Bus kinds used: IPC_PEER (29003) with topics shell:state-get, shell:state-set, shell:state-remove, shell:state-keys, napplet:state-response
  Quota: 512 KB per napplet

NUB-SIGNER API (from index.ts window.nostr):
  getPublicKey(): Promise<string>
  signEvent(event): Promise<object>
  getRelays(): Promise<Record<string, object>>
  nip04.encrypt/decrypt
  nip44.encrypt/decrypt
  Namespace: window.nostr
  Bus kinds used: SIGNER_REQUEST (29001), SIGNER_RESPONSE (29002)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write NUB-RELAY.md</name>
  <files>specs/nubs/NUB-RELAY.md</files>
  <read_first>specs/nubs/TEMPLATE-WORD.md, specs/nubs/README.md, specs/NIP-5D.md, packages/shim/src/relay-shim.ts, packages/core/src/constants.ts</read_first>
  <action>
Create specs/nubs/NUB-RELAY.md following TEMPLATE-WORD.md structure. Content:

**Header:**
```
NUB-RELAY
=========

NIP-01 Relay Proxy
------------------

`draft`

**NUB ID:** NUB-RELAY
**Namespace:** `window.napplet.relay`
**Discovery:** `shell.supports("NUB-RELAY")`
```

**Description:** One paragraph explaining that NUB-RELAY provides napplets with NIP-01 relay access through the shell. Sandboxed iframes cannot open WebSocket connections directly. The shell acts as a relay proxy, forwarding REQ/EVENT/CLOSE messages to its connected relay pool and delivering matching events back to the napplet.

**API Surface:**
```typescript
interface NappletRelay {
  subscribe(
    filters: NostrFilter | NostrFilter[],
    onEvent: (event: NostrEvent) => void,
    onEose: () => void,
    options?: { relay?: string; group?: string },
  ): Subscription;

  publish(
    template: EventTemplate,
    options?: { relay?: boolean },
  ): Promise<NostrEvent>;

  query(
    filters: NostrFilter | NostrFilter[],
  ): Promise<NostrEvent[]>;
}

interface Subscription {
  close(): void;
}
```

Brief description of each method:
- `subscribe()` opens a live NIP-01 subscription. The shell queries local cache and connected relays. Returns a handle with `close()`.
- `publish()` signs the event template via `window.nostr.signEvent()` (NUB-SIGNER) then posts to the shell for relay broadcast. Requires NUB-SIGNER.
- `query()` is a convenience wrapper: subscribes, collects events until EOSE, resolves the Promise.
- `options.relay` / `options.group` enable NIP-29 scoped relay subscriptions.

**Shell Behavior:**
- The shell MUST forward `["REQ", subId, ...filters]` to its relay pool and deliver matching events as `["EVENT", subId, event]`.
- The shell MUST send `["EOSE", subId]` when stored events are exhausted.
- The shell MUST honor `["CLOSE", subId]` by tearing down the subscription.
- The shell MUST send `["OK", eventId, bool, msg]` after processing EVENT messages.
- The shell MAY enforce ACL checks on relay:read and relay:write capabilities before processing REQ or EVENT.
- The shell MAY support scoped relay connections via kind 29001 events with topic `shell:relay-scoped-connect`.

**Event Kinds:** This interface primarily uses NIP-01 wire format verbs (REQ, EVENT, CLOSE, EOSE, OK, CLOSED, NOTICE) which are not custom event kinds. For scoped relay operations, it uses:

| Kind | Name | Direction | Description |
|------|------|-----------|-------------|
| 29001 | Scoped relay command | napplet -> shell | Connect/close/publish to a specific relay (topic: `shell:relay-scoped-connect`, `shell:relay-scoped-close`, `shell:relay-scoped-publish`) |

**Security Considerations:**
- The shell controls which relays the napplet can access. Napplets cannot open arbitrary WebSocket connections.
- The shell MAY filter or reject subscriptions based on ACL capabilities (`relay:read`, `relay:write`).
- Event signatures are verified by the shell before relay broadcast.
- Scoped relay URLs SHOULD be validated by the shell to prevent SSRF-like abuse.

**Implementations:**
- [@napplet/shim](https://github.com/sandwichfarm/napplet) (napplet-side)
- [@napplet/shell](https://github.com/sandwichfarm/napplet) (shell-side)
  </action>
  <verify>
    <automated>test -f specs/nubs/NUB-RELAY.md && grep -c "NUB-RELAY" specs/nubs/NUB-RELAY.md</automated>
  </verify>
  <acceptance_criteria>
    - specs/nubs/NUB-RELAY.md exists
    - grep "NUB-RELAY" specs/nubs/NUB-RELAY.md matches the header (setext heading)
    - grep "window.napplet.relay" specs/nubs/NUB-RELAY.md matches (namespace)
    - grep "shell.supports" specs/nubs/NUB-RELAY.md matches (discovery)
    - grep "subscribe" specs/nubs/NUB-RELAY.md matches (API method)
    - grep "publish" specs/nubs/NUB-RELAY.md matches (API method)
    - grep "query" specs/nubs/NUB-RELAY.md matches (API method)
    - grep "## Shell Behavior" specs/nubs/NUB-RELAY.md matches (section present)
    - grep "## Security Considerations" specs/nubs/NUB-RELAY.md matches (section present)
    - grep "## Implementations" specs/nubs/NUB-RELAY.md matches (section present)
    - grep "draft" specs/nubs/NUB-RELAY.md matches (status badge)
  </acceptance_criteria>
  <done>NUB-RELAY.md created with full API surface (subscribe, publish, query), shell behavior requirements, scoped relay event kinds, security considerations, and implementation links.</done>
</task>

<task type="auto">
  <name>Task 2: Write NUB-STORAGE.md</name>
  <files>specs/nubs/NUB-STORAGE.md</files>
  <read_first>specs/nubs/TEMPLATE-WORD.md, packages/shim/src/state-shim.ts, packages/core/src/topics.ts, packages/core/src/constants.ts</read_first>
  <action>
Create specs/nubs/NUB-STORAGE.md following TEMPLATE-WORD.md structure. Content:

**Header:**
```
NUB-STORAGE
============

Scoped Key-Value Storage
------------------------

`draft`

**NUB ID:** NUB-STORAGE
**Namespace:** `window.napplet.storage`
**Discovery:** `shell.supports("NUB-STORAGE")`
```

**Description:** One paragraph explaining that NUB-STORAGE provides napplets with an async localStorage-like API. Without `allow-same-origin`, iframes have opaque origins and cannot access localStorage directly. This interface routes storage operations through the shell, which scopes data by napplet identity (composite key: dTag + aggregateHash) to enforce isolation between napplets.

**API Surface:**
```typescript
interface NappletStorage {
  getItem(key: string): Promise<string | null>;
  setItem(key: string, value: string): Promise<void>;
  removeItem(key: string): Promise<void>;
  keys(): Promise<string[]>;
}
```

Brief description:
- `getItem()` retrieves a stored value. Returns `null` if key does not exist (matching localStorage semantics).
- `setItem()` stores a key-value pair. Throws if the napplet exceeds its storage quota.
- `removeItem()` deletes a stored key.
- `keys()` lists all keys stored by this napplet.

All methods are async because they cross the postMessage boundary.

**Shell Behavior:**
- The shell MUST scope storage by composite key `(dTag, aggregateHash)`. Different napplet types and different versions of the same napplet MUST have isolated storage.
- The shell MUST enforce a per-napplet storage quota. The reference implementation uses 512 KB.
- The shell MUST persist storage to the host's localStorage (or equivalent) so data survives page reloads.
- The shell MUST respond to storage requests within a reasonable timeout. The reference implementation uses 5 seconds.
- The shell MUST return structured error responses for quota violations and invalid requests.

**Event Kinds:** Storage operations use IPC-PEER events (kind 29003) with topic tags for routing:

| Kind | Name | Direction | Description |
|------|------|-----------|-------------|
| 29003 | Storage request | napplet -> shell | `t` tag: `shell:state-get`, `shell:state-set`, `shell:state-remove`, `shell:state-keys`. Includes `id` tag for correlation. |
| 29003 | Storage response | shell -> napplet | `t` tag: `napplet:state-response`. Includes `id` tag matching request, plus `value`/`found`/`error` tags. |

**Security Considerations:**
- Storage is scoped by composite key. A napplet MUST NOT be able to read or write another napplet's data.
- The shell MUST validate storage key prefixes to prevent scope escape.
- Storage quota enforcement prevents a single napplet from consuming unbounded host storage.
- Storage values are strings only. The shell SHOULD NOT attempt to parse or execute stored content.

**Implementations:**
- [@napplet/shim](https://github.com/sandwichfarm/napplet) (napplet-side)
- [@napplet/shell](https://github.com/sandwichfarm/napplet) (shell-side)
  </action>
  <verify>
    <automated>test -f specs/nubs/NUB-STORAGE.md && grep -c "NUB-STORAGE" specs/nubs/NUB-STORAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - specs/nubs/NUB-STORAGE.md exists
    - grep "NUB-STORAGE" specs/nubs/NUB-STORAGE.md matches header
    - grep "window.napplet.storage" specs/nubs/NUB-STORAGE.md matches (namespace)
    - grep "shell.supports" specs/nubs/NUB-STORAGE.md matches (discovery)
    - grep "getItem" specs/nubs/NUB-STORAGE.md matches (API method)
    - grep "setItem" specs/nubs/NUB-STORAGE.md matches (API method)
    - grep "removeItem" specs/nubs/NUB-STORAGE.md matches (API method)
    - grep "keys" specs/nubs/NUB-STORAGE.md matches (API method)
    - grep "29003" specs/nubs/NUB-STORAGE.md matches (event kind)
    - grep "shell:state-get" specs/nubs/NUB-STORAGE.md matches (topic)
    - grep "512" specs/nubs/NUB-STORAGE.md matches (quota reference)
    - grep "draft" specs/nubs/NUB-STORAGE.md matches (status badge)
  </acceptance_criteria>
  <done>NUB-STORAGE.md created with async localStorage-like API surface, composite-key scoping, quota enforcement, IPC-PEER event routing, and security isolation requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Write NUB-SIGNER.md</name>
  <files>specs/nubs/NUB-SIGNER.md</files>
  <read_first>specs/nubs/TEMPLATE-WORD.md, packages/shim/src/index.ts, packages/core/src/constants.ts</read_first>
  <action>
Create specs/nubs/NUB-SIGNER.md following TEMPLATE-WORD.md structure. This is the minimal spec per D-01 since it delegates to NIP-07. Content:

**Header:**
```
NUB-SIGNER
===========

NIP-07 Signer Proxy
--------------------

`draft`

**NUB ID:** NUB-SIGNER
**Namespace:** `window.nostr`
**Discovery:** `shell.supports("NUB-SIGNER")`
```

**Description:** One paragraph explaining that NUB-SIGNER provides a NIP-07 `window.nostr` interface inside sandboxed iframes. Napplets cannot access the host's signer extension directly. The shell proxies all signing requests to the user's actual signer (NIP-07 browser extension or NIP-46 remote signer) and returns results transparently. The interface contract is defined by NIP-07.

**API Surface:**
```typescript
// NIP-07 interface (see NIP-07 for full specification)
interface WindowNostr {
  getPublicKey(): Promise<string>;
  signEvent(event: EventTemplate): Promise<NostrEvent>;
  getRelays?(): Promise<Record<string, { read: boolean; write: boolean }>>;
  nip04?: {
    encrypt(pubkey: string, plaintext: string): Promise<string>;
    decrypt(pubkey: string, ciphertext: string): Promise<string>;
  };
  nip44?: {
    encrypt(pubkey: string, plaintext: string): Promise<string>;
    decrypt(pubkey: string, ciphertext: string): Promise<string>;
  };
}
```

Note: This is the standard NIP-07 interface. NUB-SIGNER does not extend or modify it.

**Shell Behavior:**
- The shell MUST install `window.nostr` in the napplet iframe before AUTH completes (it is available immediately on shim load).
- The shell MUST proxy `signEvent()` calls to the user's actual signer and return the signed event.
- The shell MUST proxy `getPublicKey()` to return the user's public key (not the delegated session key).
- The shell MAY enforce ACL checks on `sign:event`, `sign:nip04`, `sign:nip44` capabilities.
- The shell SHOULD require user consent before signing destructive event kinds (0: metadata, 3: contacts, 5: deletion, 10002: relay list).
- The shell MUST NOT expose the user's private key material to the napplet.

**Event Kinds:**

| Kind | Name | Direction | Description |
|------|------|-----------|-------------|
| 29001 | Signer request | napplet -> shell | Method + params in tags, correlation `id` tag |
| 29002 | Signer response | shell -> napplet | Result or error in tags, matching `id` tag |

**Security Considerations:**
- The signer proxy is the most security-sensitive interface. The shell stands between untrusted napplet code and the user's signing keys.
- Destructive kinds (0, 3, 5, 10002) SHOULD trigger user consent prompts.
- The shell SHOULD rate-limit signing requests to prevent abuse.
- Events signed via the proxy are signed by the user's real key and will be published to external relays. The napplet cannot distinguish proxy-signed events from locally-signed events.
- NIP-04 and NIP-44 encryption/decryption expose plaintext to the napplet. The shell MAY gate these behind explicit ACL capabilities.

**Implementations:**
- [@napplet/shim](https://github.com/sandwichfarm/napplet) (napplet-side)
- [@napplet/shell](https://github.com/sandwichfarm/napplet) (shell-side)
  </action>
  <verify>
    <automated>test -f specs/nubs/NUB-SIGNER.md && grep -c "NUB-SIGNER" specs/nubs/NUB-SIGNER.md</automated>
  </verify>
  <acceptance_criteria>
    - specs/nubs/NUB-SIGNER.md exists
    - grep "NUB-SIGNER" specs/nubs/NUB-SIGNER.md matches header
    - grep "window.nostr" specs/nubs/NUB-SIGNER.md matches (namespace)
    - grep "NIP-07" specs/nubs/NUB-SIGNER.md matches (NIP reference)
    - grep "shell.supports" specs/nubs/NUB-SIGNER.md matches (discovery)
    - grep "signEvent" specs/nubs/NUB-SIGNER.md matches (API method)
    - grep "getPublicKey" specs/nubs/NUB-SIGNER.md matches (API method)
    - grep "29001" specs/nubs/NUB-SIGNER.md matches (request kind)
    - grep "29002" specs/nubs/NUB-SIGNER.md matches (response kind)
    - grep "draft" specs/nubs/NUB-SIGNER.md matches (status badge)
    - wc -l < specs/nubs/NUB-SIGNER.md shows fewer lines than NUB-RELAY.md (minimal per D-01)
  </acceptance_criteria>
  <done>NUB-SIGNER.md created as minimal spec delegating to NIP-07, with signer proxy shell behavior, consent requirements for destructive kinds, and request/response event kinds.</done>
</task>

</tasks>

<verification>
1. `ls specs/nubs/NUB-RELAY.md specs/nubs/NUB-STORAGE.md specs/nubs/NUB-SIGNER.md` succeeds (all three exist)
2. `grep "draft" specs/nubs/NUB-RELAY.md specs/nubs/NUB-STORAGE.md specs/nubs/NUB-SIGNER.md` returns matches for all three
3. `grep "shell.supports" specs/nubs/NUB-RELAY.md specs/nubs/NUB-STORAGE.md specs/nubs/NUB-SIGNER.md` returns matches for all three
4. `grep "## API Surface" specs/nubs/NUB-RELAY.md specs/nubs/NUB-STORAGE.md specs/nubs/NUB-SIGNER.md` returns matches for all three
5. `grep "## Implementations" specs/nubs/NUB-RELAY.md specs/nubs/NUB-STORAGE.md specs/nubs/NUB-SIGNER.md` returns matches for all three
</verification>

<success_criteria>
- NUB-RELAY.md covers subscribe/publish/query API with NIP-01 forwarding and scoped relay support
- NUB-STORAGE.md covers async getItem/setItem/removeItem/keys API with composite-key scoping and quota
- NUB-SIGNER.md is minimal, delegating to NIP-07 with signer proxy behavior and consent requirements
- All three follow TEMPLATE-WORD.md structure with setext headings and draft badge
- All three include Discovery, API Surface, Shell Behavior, Event Kinds, Security, Implementations
</success_criteria>

<output>
After completion, create `.planning/phases/60-channel-protocol-implementation/60-01-SUMMARY.md`
</output>
