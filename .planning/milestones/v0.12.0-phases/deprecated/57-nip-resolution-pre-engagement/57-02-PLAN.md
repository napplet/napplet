---
phase: 57
plan: 2
title: Stakeholder Scope Outline and Engagement Preparation
wave: 1
depends_on: []
files_modified:
  - .planning/phases/57-nip-resolution-pre-engagement/SCOPE-OUTLINE.md (new)
  - .planning/phases/57-nip-resolution-pre-engagement/ENGAGEMENT-LOG.md (new)
autonomous: true
requirements_addressed: [RES-02]
---

# Plan 57-02: Stakeholder Scope Outline and Engagement Preparation

<objective>
Create a concise scope outline document that distinguishes NIP-5D from NIP-5A and NIP-5B, suitable for sharing with stakeholders (hzrd149, arthurfranca, fiatjaf) via nostr DMs or GitHub comments. Also create an engagement log template for the user to record outreach and feedback.
</objective>

## Tasks

<task id="57-02-01">
<title>Draft the NIP-5D scope outline for stakeholder review</title>
<read_first>
- .planning/phases/57-nip-resolution-pre-engagement/57-CONTEXT.md (decisions D-02, D-03, D-04, D-05)
- .planning/phases/57-nip-resolution-pre-engagement/57-RESEARCH.md (Section 3: Stakeholder Analysis, three-layer narrative)
- .planning/research/PITFALLS.md (Pitfall 2: defining "what is a napp", Pitfall 3: competing with NIP-5B)
- .planning/research/SUMMARY.md (Executive Summary for protocol scope)
</read_first>
<action>
Create `.planning/phases/57-nip-resolution-pre-engagement/SCOPE-OUTLINE.md` with a concise outline that can be copy-pasted into a nostr DM or GitHub comment. The document should have two sections: a short version (DM-length, under 150 words) and a longer version (GitHub comment length, under 300 words).

```markdown
# NIP-5D Scope Outline: Nostr Web Applets

**Purpose:** Share with stakeholders to distinguish NIP-5D scope from NIP-5A/5B before spec writing.

---

## Short Version (for nostr DMs)

I'm drafting NIP-5D — "Nostr Web Applets" — a spec for the postMessage communication protocol between sandboxed iframe apps and a host shell.

NIP-5D fills the runtime gap in the NIP-5x family:
- **NIP-5A** = hosting (static web assets via Nostr events + blossom)
- **NIP-5B** = discovery (embeddable app listings)
- **NIP-5D** = runtime communication (what happens after the iframe loads)

The spec covers:
- AUTH handshake (NIP-42 challenge-response with ephemeral keypairs)
- Capability discovery (kind 29010 service advertisement)
- Standard optional capabilities: relay proxy, signer proxy (NIP-07), storage, IPC pub/sub
- NIP-5A manifest integration (requires tags for capability declaration)
- Security model (iframe sandbox without allow-same-origin, postMessage authentication)

It does NOT define what makes something an "app" or compete with NIP-5B's app listing. A napplet is simply any iframe that speaks this protocol.

The spec references NIP-5A aggregate hashes (PR#2287) for version identity. It's designed to be terse (<500 lines) and focuses only on the observable wire format.

Reference implementation: @napplet/* SDK packages (github.com/sandwichfarm/napplet)

Would appreciate your thoughts on scope and positioning before I start the spec draft.

---

## Long Version (for GitHub comments)

### What is NIP-5D?

NIP-5D defines a postMessage-based wire protocol for communication between sandboxed iframe applications ("napplets") and a host shell. It extends the NIP-5x family:

| Layer | NIP | What it defines |
|-------|-----|-----------------|
| Hosting | NIP-5A | How static web assets are stored and served |
| Discovery | NIP-5B | How embeddable apps are listed and found |
| **Runtime** | **NIP-5D** | How embedded apps communicate with the host |

### What NIP-5D covers

**MUST (mandatory for all implementations):**
- postMessage transport with NIP-01 wire format (REQ/EVENT/CLOSE/EOSE/NOTICE/CLOSED)
- AUTH handshake: REGISTER/IDENTITY/AUTH flow using NIP-42 challenge-response with ephemeral session keypairs
- Service/capability discovery via kind 29010

**MAY (optional capabilities, independently implementable):**
- Relay proxy: transparent relay access through the host
- NIP-07 signer proxy: `window.nostr` delegated to host signer
- Scoped storage: isolated key-value store per napplet version
- IPC pub/sub: inter-napplet event messaging
- Channels: point-to-point named communication pipes (planned)

### What NIP-5D does NOT cover

- App listing or discovery (that's NIP-5B)
- File hosting or serving (that's NIP-5A)
- What makes something a "nostr app" — NIP-5D imposes no classification
- Runtime internals (ACL engines, service registries, session management)

### NIP-5A dependency

NIP-5D references aggregate hashes from NIP-5A (PR#2287) for version-specific napplet identity. The `requires` tag in NIP-5A manifests (kind 35128) declares which capabilities a napplet needs.

### Implementation

Reference SDK: `@napplet/shim` + `@napplet/shell` (github.com/sandwichfarm/napplet)
Reference shell: hyprgate (github.com/sandwichfarm/hyprgate)

---

*This outline is for pre-engagement only. The full spec draft will follow in Phase 58.*
```

Key constraints on this document:
- Short version must be under 150 words (fit in a nostr DM)
- Long version must be under 300 words
- Must NOT define "what is a napp" (Pitfall 2 from PITFALLS.md)
- Must clearly position NIP-5D as complementary to NIP-5B (Pitfall 3)
- Must mention the aggregate hash dependency on PR#2287
- Must include the three-layer table (5A/5B/5D)
</action>
<acceptance_criteria>
- File `.planning/phases/57-nip-resolution-pre-engagement/SCOPE-OUTLINE.md` exists
- Contains "Short Version" section with word count under 150 words (check: `sed -n '/## Short Version/,/## Long Version/p' SCOPE-OUTLINE.md | wc -w`)
- Contains "Long Version" section with word count under 300 words
- Contains the three-layer table with NIP-5A, NIP-5B, NIP-5D rows
- Contains "MUST" and "MAY" capability split
- Does NOT contain phrases like "a napplet is defined as" or "what makes something an app"
- References PR#2287 aggregate hash dependency
- References github.com/sandwichfarm/napplet as implementation
</acceptance_criteria>
</task>

<task id="57-02-02">
<title>Create engagement log template for tracking stakeholder outreach</title>
<read_first>
- .planning/phases/57-nip-resolution-pre-engagement/57-CONTEXT.md (decisions D-02, D-03)
</read_first>
<action>
Create `.planning/phases/57-nip-resolution-pre-engagement/ENGAGEMENT-LOG.md` as a template the user will fill in after conducting outreach:

```markdown
# NIP-5D Stakeholder Engagement Log

**Phase:** 57 — NIP Resolution & Pre-Engagement
**Target:** At least 2 of 3 stakeholders receive scope outline before spec writing

## Engagement Status

| Stakeholder | Contacted | Channel | Date | Response | Notes |
|-------------|-----------|---------|------|----------|-------|
| hzrd149 | [ ] | | | | NIP-5A author, PR#2287 aggregate hash |
| arthurfranca | [ ] | | | | NIP-5B author, PR#2282 |
| fiatjaf | [ ] | | | | NIP-5C Scrolls, nips repo maintainer |

**Channels:** nostr DM, GitHub comment, GitHub PR, other

## Outline Shared

- [ ] Short version (nostr DM) — see SCOPE-OUTLINE.md
- [ ] Long version (GitHub comment) — see SCOPE-OUTLINE.md

## Feedback Received

### hzrd149
*Pending outreach*

### arthurfranca
*Pending outreach*

### fiatjaf
*Pending outreach*

## Completion Criteria

- [x] Scope outline document created (SCOPE-OUTLINE.md)
- [ ] At least 2 of 3 stakeholders contacted with outline
- [ ] Any feedback documented above

---

*Updated by: user (human outreach per CONTEXT.md D-02)*
```

This file is a template that the user fills in. Claude does not perform the outreach (per CONTEXT.md decision D-02).
</action>
<acceptance_criteria>
- File `.planning/phases/57-nip-resolution-pre-engagement/ENGAGEMENT-LOG.md` exists
- Contains rows for all three stakeholders: hzrd149, arthurfranca, fiatjaf
- Contains checkbox columns for tracking contact status
- Contains "Feedback Received" sections for each stakeholder
- Contains completion criteria referencing "at least 2 of 3"
- Does NOT contain any simulated or fabricated outreach results
</acceptance_criteria>
</task>

## Verification

```bash
# Scope outline exists
test -f .planning/phases/57-nip-resolution-pre-engagement/SCOPE-OUTLINE.md && echo "PASS: outline exists"

# Engagement log exists
test -f .planning/phases/57-nip-resolution-pre-engagement/ENGAGEMENT-LOG.md && echo "PASS: engagement log exists"

# Three-layer table present in outline
grep -q "NIP-5A\|NIP-5B\|NIP-5D" .planning/phases/57-nip-resolution-pre-engagement/SCOPE-OUTLINE.md && echo "PASS: NIP-5x references present"

# No definitional overreach
grep -qi "napplet is defined as\|what makes something" .planning/phases/57-nip-resolution-pre-engagement/SCOPE-OUTLINE.md && echo "FAIL: definitional language found" || echo "PASS: no definitional overreach"

# All three stakeholders in engagement log
grep -c "hzrd149\|arthurfranca\|fiatjaf" .planning/phases/57-nip-resolution-pre-engagement/ENGAGEMENT-LOG.md | grep -qE "^[3-9]" && echo "PASS: all stakeholders listed"

# Aggregate hash reference
grep -q "PR#2287\|aggregate hash" .planning/phases/57-nip-resolution-pre-engagement/SCOPE-OUTLINE.md && echo "PASS: PR#2287 referenced"
```

### must_haves
- [ ] Scope outline is concise enough for a nostr DM (short version under 150 words)
- [ ] Three-layer positioning (5A=hosting, 5B=discovery, 5D=runtime) is clearly presented
- [ ] MUST/MAY capability split is explicit in the outline
- [ ] Engagement log template lists all three target stakeholders with tracking fields
- [ ] No definitional language about "what is a napp" (avoids Pitfall 2)
