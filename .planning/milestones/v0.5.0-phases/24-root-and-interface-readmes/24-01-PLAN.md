---
plan: 24-01
phase: 24
wave: 1
title: Update Root README.md
depends_on: []
files_modified:
  - README.md
requirements_addressed:
  - README-01
autonomous: true
---

# Plan 24-01: Update Root README.md

## Objective

Update the root `README.md` to accurately describe the full 7-package v0.4.0 SDK. Replace the stale 3-package table and outdated architecture diagram. Remove `createPseudoRelay` references and update to `createShellBridge`/`ShellBridge`.

## Context

The current `README.md` lists only 3 packages (`@napplet/shim`, `@napplet/shell`, `@napplet/vite-plugin`) and references `createPseudoRelay` in the architecture diagram. The v0.4.0 SDK has 7 packages total.

## Tasks

### Task 1: Update the packages table to all 7 packages

<read_first>
- `README.md` — current root README (the file being modified)
- `packages/shim/src/index.ts` — shim public API exports
- `packages/shell/src/index.ts` — shell public API exports
- `packages/vite-plugin/src/index.ts` — vite-plugin API
- `packages/acl/src/index.ts` — acl public exports
- `packages/core/src/index.ts` — core public exports
- `packages/runtime/src/index.ts` — runtime public exports (if exists)
- `packages/services/src/index.ts` — services public exports (if exists)
</read_first>

<action>
Replace the current 3-row packages table:

```markdown
| Package | npm | Description |
|---------|-----|-------------|
| [@napplet/shim](packages/shim) | `@napplet/shim` | Napplet SDK. Provides `subscribe`, `publish`, `query`, `emit`, `on`, `nappStorage` — the full API a napplet uses to talk to its host shell. |
| [@napplet/shell](packages/shell) | `@napplet/shell` | Shell runtime. `createPseudoRelay(hooks)` factory that handles NIP-01 routing, AUTH handshake, ACL enforcement, signer proxy, storage proxy, and audio management. Framework-agnostic. |
| [@napplet/vite-plugin](packages/vite-plugin) | `@napplet/vite-plugin` | Vite plugin for NIP-5A manifest generation. Computes per-file SHA-256 hashes and signs a kind 35128 manifest event at build time. |
```

With the complete 7-row table:

```markdown
| Package | npm | Description |
|---------|-----|-------------|
| [@napplet/shim](packages/shim) | `@napplet/shim` | Napplet SDK. Provides `subscribe`, `publish`, `query`, `emit`, `on`, `nappState`, and service discovery — everything a napplet needs to communicate with its host shell. |
| [@napplet/shell](packages/shell) | `@napplet/shell` | Shell runtime browser adapter. `createShellBridge(hooks)` factory — handles postMessage routing, NIP-42 AUTH, ACL enforcement, signer proxy, storage proxy, and service dispatch. |
| [@napplet/acl](packages/acl) | `@napplet/acl` | Pure capability enforcement module. Bitfield-based ACL with `check()`, `grant()`, `revoke()`, `block()` — zero browser dependencies. |
| [@napplet/core](packages/core) | `@napplet/core` | Shared protocol types and constants. `NostrEvent`, `NostrFilter`, `BusKind`, `ServiceDescriptor`, topic constants — imported by all other packages. |
| [@napplet/runtime](packages/runtime) | `@napplet/runtime` | Environment-agnostic protocol engine. `createRuntime(hooks)` with `RuntimeHooks` interface — all NIP-01 routing, AUTH, subscription management, service dispatch. |
| [@napplet/services](packages/services) | `@napplet/services` | Concrete service implementations. `createAudioService()`, `createNotificationService()` — ready-to-use `ServiceHandler` instances for `RuntimeHooks.services`. |
| [@napplet/vite-plugin](packages/vite-plugin) | `@napplet/vite-plugin` | Vite plugin for NIP-5A manifest generation. Computes per-file SHA-256 hashes, signs a kind 35128 manifest event at build time, and injects `requires` meta tags. |
```
</action>

<acceptance_criteria>
- `README.md` contains `@napplet/acl` in the packages table
- `README.md` contains `@napplet/core` in the packages table
- `README.md` contains `@napplet/runtime` in the packages table
- `README.md` contains `@napplet/services` in the packages table
- `README.md` does NOT contain `createPseudoRelay` anywhere
- `README.md` does NOT contain the old 3-row table (grep for `createPseudoRelay` returns no matches)
- `grep -c "| \[@napplet" README.md` returns 7
</acceptance_criteria>

### Task 2: Replace the architecture diagram with two diagrams

<read_first>
- `README.md` — current root README (to see the existing diagram style and box-drawing chars)
</read_first>

<action>
Replace the existing single architecture diagram block:

```
## Architecture

```
Shell (host page)                    Napplet (sandboxed iframe)
─────────────────                    ─────────────────────────
@napplet/shell                       @napplet/shim
  createPseudoRelay(hooks)             subscribe / publish / query
  ├── NIP-01 message routing           emit / on (inter-pane events)
  ├── NIP-42 AUTH handshake            nappStorage (proxied localStorage)
  ├── Signer proxy (NIP-07/46)         window.nostr (NIP-07 proxy)
  ├── Storage proxy (scoped)
  ├── ACL enforcement          ◄── postMessage (NIP-01 wire format) ──►
  └── Audio management

@napplet/vite-plugin (build time)
  └── NIP-5A manifest generation
```
```

With two new diagrams:

```markdown
## Architecture

### Package Dependency Graph

```
@napplet/shim       @napplet/services
      │                    │
      ▼                    ▼
@napplet/core ──► @napplet/runtime ──► @napplet/shell
      ▲
@napplet/acl ────────────────────────► @napplet/runtime

@napplet/vite-plugin  (build-time only, depends on nostr-tools)
```

### Runtime Communication Flow

```
Shell (host page)                          Napplet (sandboxed iframe)
──────────────────────────                 ──────────────────────────
@napplet/shell                             @napplet/shim
  createShellBridge(hooks)                   subscribe / publish / query
  ├── @napplet/runtime (engine)              emit / on (inter-pane events)
  │     ├── NIP-01 message routing           nappState (proxied storage)
  │     ├── NIP-42 AUTH handshake            window.nostr (NIP-07 proxy)
  │     ├── ACL enforcement                  window.napplet.discoverServices()
  │     ├── Service dispatch (kind 29010)
  │     └── Signer + storage proxy
  └── @napplet/acl (capability checks)

◄────────────── postMessage (NIP-01 wire format) ──────────────►

@napplet/vite-plugin (build time)
  └── NIP-5A manifest generation + requires tag injection
```
```
</action>

<acceptance_criteria>
- `README.md` contains the text `### Package Dependency Graph`
- `README.md` contains the text `### Runtime Communication Flow`
- `README.md` contains `createShellBridge(hooks)` in the architecture section
- `README.md` does NOT contain `createPseudoRelay` in the architecture section
- `README.md` contains `@napplet/runtime` in the runtime communication flow diagram
- `README.md` contains `discoverServices` or `window.napplet` in the diagram
</acceptance_criteria>

### Task 3: Update the prose description to remove stale references

<read_first>
- `README.md` — current root README (to see existing prose paragraphs)
</read_first>

<action>
Find the prose paragraph at the top (intro paragraph) that reads:
```
The shell and napplet communicate over `postMessage` using the NIP-01 wire format. The napplet never touches `localStorage`, relay connections, or signing keys directly — the shell proxies everything through a pseudo-relay.
```

Update to:
```
The shell and napplet communicate over `postMessage` using the NIP-01 wire format. The napplet never touches `localStorage`, relay connections, or signing keys directly — the shell proxies everything through a `ShellBridge`.
```

Also check the "Related" section for any mention of `createPseudoRelay` or `PseudoRelay` and remove/update those references.
</action>

<acceptance_criteria>
- `grep -i "pseudo-relay\|pseudorelay\|createPseudoRelay" README.md` returns no matches
- `README.md` contains `ShellBridge` at least once in the prose/intro
</acceptance_criteria>

## Verification

After all tasks complete, run:

```bash
# Check 7 packages listed
grep -c "| \[@napplet" README.md
# Expected: 7

# Check no stale API names
grep -i "createPseudoRelay\|pseudo-relay\|pseudorelay" README.md
# Expected: no output

# Check both diagrams present
grep -c "### Package Dependency Graph\|### Runtime Communication Flow" README.md
# Expected: 2

# Check correct factory name
grep "createShellBridge" README.md
# Expected: at least one match
```

## must_haves

- All 7 packages listed in the packages table with accurate descriptions
- Both architecture diagrams present (dependency graph + runtime flow)
- Zero occurrences of `createPseudoRelay` anywhere in `README.md`
- `createShellBridge` and `ShellBridge` used as the canonical API name
