---
phase: 7
plan: "07-01"
title: "Rename pseudo-relay to ShellBridge"
wave: 1
depends_on: []
files_modified:
  - packages/shell/src/pseudo-relay.ts -> packages/shell/src/shell-bridge.ts
  - packages/shell/src/index.ts
  - packages/shell/src/types.ts
  - packages/shell/src/napp-key-registry.ts
  - packages/shell/src/origin-registry.ts
  - packages/shim/src/types.ts
  - packages/shim/src/index.ts
  - packages/shim/src/relay-shim.ts
  - packages/shim/src/nipdb-shim.ts
  - apps/demo/src/shell-host.ts
  - tests/e2e/harness/harness.ts
  - tests/helpers/message-tap.ts
  - tests/e2e/inter-pane.spec.ts
  - tests/e2e/replay.spec.ts
  - SPEC.md
  - packages/shell/README.md
  - CLAUDE.md
requirements: [REN-01, REN-02, REN-03, REN-04, REN-05, REN-06, REN-07, REN-08]
autonomous: true
---

<objective>
Rename all occurrences of pseudo-relay/PseudoRelay/createPseudoRelay/PSEUDO_RELAY_URI to ShellBridge/createShellBridge/SHELL_BRIDGE_URI across the entire codebase. File rename pseudo-relay.ts to shell-bridge.ts. Hard cut with no deprecated aliases (pre-v1, no external consumers).
</objective>

<tasks>

<task id="07-01-01">
<title>Rename pseudo-relay.ts to shell-bridge.ts and update all symbols inside</title>
<read_first>
- packages/shell/src/pseudo-relay.ts
- packages/shell/src/types.ts
</read_first>
<action>
1. Rename file: `git mv packages/shell/src/pseudo-relay.ts packages/shell/src/shell-bridge.ts`

2. In `packages/shell/src/shell-bridge.ts`, make these replacements:
   - Line 2 JSDoc: `pseudo-relay.ts — NIP-01 pseudo-relay message handler.` -> `shell-bridge.ts — NIP-01 ShellBridge message handler.`
   - Line 4 JSDoc: `Factory function that creates a framework-agnostic pseudo-relay instance.` -> `Factory function that creates a framework-agnostic ShellBridge instance.`
   - Line 13 import: `PSEUDO_RELAY_URI` -> `SHELL_BRIDGE_URI`
   - Line 25: `export interface PseudoRelay {` -> `export interface ShellBridge {`
   - Line 39 JSDoc: `Create a pseudo-relay instance with dependency injection via hooks.` -> `Create a ShellBridge instance with dependency injection via hooks.`
   - Line 42 JSDoc: `@returns A PseudoRelay instance ready to handle napp messages.` -> `@returns A ShellBridge instance ready to handle napp messages.`
   - Line 44: `export function createPseudoRelay(hooks: ShellHooks): PseudoRelay {` -> `export function createShellBridge(hooks: ShellHooks): ShellBridge {`
   - Line 164: `PSEUDO_RELAY_URI` -> `SHELL_BRIDGE_URI`
   - Line 607: `const relay: PseudoRelay = {` -> `const relay: ShellBridge = {`
</action>
<acceptance_criteria>
- `packages/shell/src/shell-bridge.ts` exists and `packages/shell/src/pseudo-relay.ts` does not exist
- `grep -c "PseudoRelay" packages/shell/src/shell-bridge.ts` returns 0
- `grep -c "createPseudoRelay" packages/shell/src/shell-bridge.ts` returns 0
- `grep -c "PSEUDO_RELAY_URI" packages/shell/src/shell-bridge.ts` returns 0
- `grep -c "pseudo-relay" packages/shell/src/shell-bridge.ts` returns 0
- `grep -c "ShellBridge" packages/shell/src/shell-bridge.ts` returns at least 4
- `grep -c "createShellBridge" packages/shell/src/shell-bridge.ts` returns at least 1
- `grep -c "SHELL_BRIDGE_URI" packages/shell/src/shell-bridge.ts` returns at least 1
</acceptance_criteria>
</task>

<task id="07-01-02">
<title>Update shell package types.ts constant name</title>
<read_first>
- packages/shell/src/types.ts
</read_first>
<action>
In `packages/shell/src/types.ts`, line 8:
- `export const PSEUDO_RELAY_URI = 'napplet://shell' as const;` -> `export const SHELL_BRIDGE_URI = 'napplet://shell' as const;`

The value `'napplet://shell'` stays the same — only the constant name changes.
</action>
<acceptance_criteria>
- `grep "SHELL_BRIDGE_URI" packages/shell/src/types.ts` matches `export const SHELL_BRIDGE_URI = 'napplet://shell' as const;`
- `grep "PSEUDO_RELAY_URI" packages/shell/src/types.ts` returns no matches
</acceptance_criteria>
</task>

<task id="07-01-03">
<title>Update shell package barrel export (index.ts)</title>
<read_first>
- packages/shell/src/index.ts
</read_first>
<action>
In `packages/shell/src/index.ts`:
- Line 5: `export { createPseudoRelay } from './pseudo-relay.js';` -> `export { createShellBridge } from './shell-bridge.js';`
- Line 6: `export type { PseudoRelay } from './pseudo-relay.js';` -> `export type { ShellBridge } from './shell-bridge.js';`
- Line 42: `export { BusKind, AUTH_KIND, PSEUDO_RELAY_URI, PROTOCOL_VERSION, ALL_CAPABILITIES, DESTRUCTIVE_KINDS } from './types.js';` -> `export { BusKind, AUTH_KIND, SHELL_BRIDGE_URI, PROTOCOL_VERSION, ALL_CAPABILITIES, DESTRUCTIVE_KINDS } from './types.js';`
</action>
<acceptance_criteria>
- `grep "createShellBridge" packages/shell/src/index.ts` matches
- `grep "ShellBridge" packages/shell/src/index.ts` matches (type export)
- `grep "SHELL_BRIDGE_URI" packages/shell/src/index.ts` matches
- `grep "shell-bridge.js" packages/shell/src/index.ts` matches
- `grep "pseudo-relay" packages/shell/src/index.ts` returns no matches
- `grep "PSEUDO_RELAY_URI" packages/shell/src/index.ts` returns no matches
- `grep "createPseudoRelay" packages/shell/src/index.ts` returns no matches
</acceptance_criteria>
</task>

<task id="07-01-04">
<title>Update shell package comment-only references</title>
<read_first>
- packages/shell/src/napp-key-registry.ts
- packages/shell/src/origin-registry.ts
</read_first>
<action>
In `packages/shell/src/napp-key-registry.ts`, line 4:
- `the pseudo-relay registers the napp's` -> `the ShellBridge registers the napp's`

In `packages/shell/src/origin-registry.ts`, line 4:
- `Used by the pseudo-relay to validate` -> `Used by the ShellBridge to validate`
</action>
<acceptance_criteria>
- `grep "pseudo-relay" packages/shell/src/napp-key-registry.ts` returns no matches
- `grep "pseudo-relay" packages/shell/src/origin-registry.ts` returns no matches
- `grep "ShellBridge" packages/shell/src/napp-key-registry.ts` returns at least 1 match
- `grep "ShellBridge" packages/shell/src/origin-registry.ts` returns at least 1 match
</acceptance_criteria>
</task>

<task id="07-01-05">
<title>Update shim package types and imports</title>
<read_first>
- packages/shim/src/types.ts
- packages/shim/src/index.ts
- packages/shim/src/relay-shim.ts
- packages/shim/src/nipdb-shim.ts
</read_first>
<action>
In `packages/shim/src/types.ts`:
- Line 29: `perfect for bus traffic that should never persist beyond the pseudo-relay.` -> `perfect for bus traffic that should never persist beyond the ShellBridge.`
- Line 47: `/** The pseudo-relay URI used in NIP-42 AUTH challenges. */` -> `/** The ShellBridge URI used in NIP-42 AUTH challenges. */`
- Line 48: `export const PSEUDO_RELAY_URI = 'napplet://shell' as const;` -> `export const SHELL_BRIDGE_URI = 'napplet://shell' as const;`

In `packages/shim/src/index.ts`:
- Line 3: `// Communicates with the shell pseudo-relay using NIP-01 wire format over postMessage.` -> `// Communicates with the ShellBridge using NIP-01 wire format over postMessage.`
- Line 13: `import { BusKind, AUTH_KIND, PSEUDO_RELAY_URI, PROTOCOL_VERSION } from './types.js';` -> `import { BusKind, AUTH_KIND, SHELL_BRIDGE_URI, PROTOCOL_VERSION } from './types.js';`
- Line 29: JSDoc `shell pseudo-relay for delivery` -> `ShellBridge for delivery`
- Line 213: `['relay', PSEUDO_RELAY_URI],` -> `['relay', SHELL_BRIDGE_URI],`

In `packages/shim/src/relay-shim.ts`:
- Line 2: `// NIP-01 wire format over postMessage to the shell pseudo-relay.` -> `// NIP-01 wire format over postMessage to the ShellBridge.`

In `packages/shim/src/nipdb-shim.ts`:
- Line 3: `// to the shell pseudo-relay, which dispatches to WorkerRelayService (OPFS cache).` -> `// to the ShellBridge, which dispatches to WorkerRelayService (OPFS cache).`
</action>
<acceptance_criteria>
- `grep -r "PSEUDO_RELAY_URI" packages/shim/src/` returns no matches
- `grep -r "pseudo-relay" packages/shim/src/` returns no matches
- `grep "SHELL_BRIDGE_URI" packages/shim/src/types.ts` matches
- `grep "SHELL_BRIDGE_URI" packages/shim/src/index.ts` returns at least 2 matches (import + usage)
- `grep "ShellBridge" packages/shim/src/types.ts` returns at least 1 match
</acceptance_criteria>
</task>

<task id="07-01-06">
<title>Update demo app imports and types</title>
<read_first>
- apps/demo/src/shell-host.ts
</read_first>
<action>
In `apps/demo/src/shell-host.ts`:
- Line 9: `createPseudoRelay,` -> `createShellBridge,`
- Line 13: `type PseudoRelay,` -> `type ShellBridge,`
- Line 245: `export let relay: PseudoRelay;` -> `export let relay: ShellBridge;`
- Line 251: Comment `Boot the shell: create pseudo-relay, install tap, wire up proxy.` -> `Boot the shell: create ShellBridge, install tap, wire up proxy.`
- Line 253: `export function bootShell(): { tap: MessageTap; relay: PseudoRelay } {` -> `export function bootShell(): { tap: MessageTap; relay: ShellBridge } {`
- Line 275: `relay = createPseudoRelay(hooks);` -> `relay = createShellBridge(hooks);`
</action>
<acceptance_criteria>
- `grep "createPseudoRelay" apps/demo/src/shell-host.ts` returns no matches
- `grep "PseudoRelay" apps/demo/src/shell-host.ts` returns no matches
- `grep "createShellBridge" apps/demo/src/shell-host.ts` returns at least 2 matches (import + usage)
- `grep "ShellBridge" apps/demo/src/shell-host.ts` returns at least 3 matches (import + variable type + return type)
</acceptance_criteria>
</task>

<task id="07-01-07">
<title>Update test harness and test comments</title>
<read_first>
- tests/e2e/harness/harness.ts
- tests/helpers/message-tap.ts
- tests/e2e/inter-pane.spec.ts
- tests/e2e/replay.spec.ts
</read_first>
<action>
In `tests/e2e/harness/harness.ts`:
- Line 14: `import { createPseudoRelay, originRegistry, aclStore, nappKeyRegistry } from '@napplet/shell';` -> `import { createShellBridge, originRegistry, aclStore, nappKeyRegistry } from '@napplet/shell';`
- Line 15: `import type { PseudoRelay, Capability } from '@napplet/shell';` -> `import type { ShellBridge, Capability } from '@napplet/shell';`
- Line 29: `__getRelay__: () => PseudoRelay;` -> `__getRelay__: () => ShellBridge;`
- Line 66: Comment `The pseudo-relay sends messages` -> `The ShellBridge sends messages`
- Line 124: `const relay = createPseudoRelay(mockResult.hooks);` -> `const relay = createShellBridge(mockResult.hooks);`

In `tests/helpers/message-tap.ts`:
- Line 178: Comment `pseudo-relay's postMessage calls` -> `ShellBridge's postMessage calls`
- Line 216: Comment `messages before the pseudo-relay` -> `messages before the ShellBridge`

In `tests/e2e/inter-pane.spec.ts`:
- Line 4: Comment `the pseudo-relay's subscription-based event routing` -> `the ShellBridge's subscription-based event routing`

In `tests/e2e/replay.spec.ts`:
- Line 4: Comment `the pseudo-relay's replay detection` -> `the ShellBridge's replay detection`
</action>
<acceptance_criteria>
- `grep -r "createPseudoRelay" tests/` returns no matches
- `grep -r "PseudoRelay" tests/` returns no matches
- `grep "createShellBridge" tests/e2e/harness/harness.ts` returns at least 2 matches
- `grep "ShellBridge" tests/e2e/harness/harness.ts` returns at least 3 matches
- `grep "pseudo-relay" tests/helpers/message-tap.ts` returns no matches
- `grep "pseudo-relay" tests/e2e/inter-pane.spec.ts` returns no matches
- `grep "pseudo-relay" tests/e2e/replay.spec.ts` returns no matches
</acceptance_criteria>
</task>

<task id="07-01-08">
<title>Update SPEC.md — replace pseudo-relay with ShellBridge</title>
<read_first>
- SPEC.md
</read_first>
<action>
In `SPEC.md`:
- Line 30: `acting as a NIP-01 pseudo-relay and NIP-07 signer proxy` -> `acting as a NIP-01 ShellBridge and NIP-07 signer proxy`
- Line 244: `or the shell's configured pseudo-relay URI` -> `or the shell's configured ShellBridge URI`
- Line 1200: `pseudo-relay.ts, state-proxy.ts` -> `shell-bridge.ts, state-proxy.ts`
</action>
<acceptance_criteria>
- `grep -c "pseudo-relay" SPEC.md` returns 0
- `grep "ShellBridge" SPEC.md` returns at least 2 matches
- `grep "shell-bridge.ts" SPEC.md` returns at least 1 match
</acceptance_criteria>
</task>

<task id="07-01-09">
<title>Update shell package README.md</title>
<read_first>
- packages/shell/README.md
</read_first>
<action>
In `packages/shell/README.md`, replace all occurrences:
- `pseudo-relay` (in prose) -> `ShellBridge`
- `createPseudoRelay` -> `createShellBridge`
- `PseudoRelay` (type) -> `ShellBridge`
- `PSEUDO_RELAY_URI` -> `SHELL_BRIDGE_URI`

Specific lines:
- Line 5: `acts as a NIP-01 pseudo-relay between` -> `acts as a NIP-01 ShellBridge between`
- Line 17: `Create a PseudoRelay by calling createPseudoRelay(hooks)` -> `Create a ShellBridge by calling createShellBridge(hooks)`
- Line 20: `The pseudo-relay handles AUTH verification` -> `The ShellBridge handles AUTH verification`
- Line 31: `import { createPseudoRelay, originRegistry } from '@napplet/shell';` -> `import { createShellBridge, originRegistry } from '@napplet/shell';`
- Line 76: `const relay = createPseudoRelay(hooks);` -> `const relay = createShellBridge(hooks);`
- Line 98: `### createPseudoRelay(hooks)` -> `### createShellBridge(hooks)`
- Line 100: `Create a pseudo-relay instance` -> `Create a ShellBridge instance`
- Line 108: `**Returns:** PseudoRelay` -> `**Returns:** ShellBridge`
- Line 110: `### PseudoRelay` -> `### ShellBridge`
- Line 138: `without creating a full pseudo-relay` -> `without creating a full ShellBridge`
- Line 156: `| PSEUDO_RELAY_URI | 'napplet://shell' | Pseudo-relay URI for AUTH relay tag |` -> `| SHELL_BRIDGE_URI | 'napplet://shell' | ShellBridge URI for AUTH relay tag |`
- Line 174: `PseudoRelay,` -> `ShellBridge,`
</action>
<acceptance_criteria>
- `grep -c "pseudo-relay" packages/shell/README.md` returns 0 (case-insensitive check: `grep -ic "pseudo.relay" packages/shell/README.md` also returns 0)
- `grep -c "PseudoRelay" packages/shell/README.md` returns 0
- `grep -c "createPseudoRelay" packages/shell/README.md` returns 0
- `grep -c "PSEUDO_RELAY_URI" packages/shell/README.md` returns 0
- `grep "createShellBridge" packages/shell/README.md` returns at least 3 matches
- `grep "ShellBridge" packages/shell/README.md` returns at least 5 matches
</acceptance_criteria>
</task>

<task id="07-01-10">
<title>Update project CLAUDE.md with new names</title>
<read_first>
- CLAUDE.md
</read_first>
<action>
In `CLAUDE.md`, update all references:
- Line 12: `createPseudoRelay factory` -> `createShellBridge factory`
- Line 12: `storage proxy` -> `state proxy`
- Line 125: `pseudo-relay.ts` -> `shell-bridge.ts`
- Line 125: `storage-proxy.ts` -> `state-proxy.ts` (if still referencing old name; check first)
- Line 128: `createPseudoRelay()` -> `createShellBridge()`
- Line 132: `createPseudoRelay()` -> `createShellBridge()`
- Line 137: `PseudoRelay` -> `ShellBridge`
- Line 177: `pseudo-relay.ts` -> `shell-bridge.ts`
- Line 185: `createPseudoRelay(): PseudoRelay` -> `createShellBridge(): ShellBridge`
- Line 219: `pseudo-relay.ts` -> `shell-bridge.ts`
- Line 238: `createPseudoRelay(hooks)` -> `createShellBridge(hooks)`
</action>
<acceptance_criteria>
- `grep -c "createPseudoRelay" CLAUDE.md` returns 0
- `grep -c "PseudoRelay" CLAUDE.md` returns 0
- `grep -c "pseudo-relay" CLAUDE.md` returns 0
- `grep "createShellBridge" CLAUDE.md` returns at least 3 matches
- `grep "ShellBridge" CLAUDE.md` returns at least 3 matches
- `grep "shell-bridge.ts" CLAUDE.md` returns at least 2 matches
</acceptance_criteria>
</task>

<task id="07-01-11">
<title>Build and verify all packages compile</title>
<read_first>
- packages/shell/tsconfig.json
- packages/shim/tsconfig.json
</read_first>
<action>
Run build and type-check to verify all renames are consistent:

1. `pnpm build` — must exit 0 (all three packages build via turborepo)
2. `pnpm type-check` — must exit 0 (TypeScript validates all import paths and type references)

If build fails, check for any missed references to old names:
- `grep -r "pseudo-relay\|PseudoRelay\|createPseudoRelay\|PSEUDO_RELAY_URI" packages/ apps/ tests/ --include="*.ts"`
</action>
<acceptance_criteria>
- `pnpm build` exits with code 0
- `pnpm type-check` exits with code 0
- `grep -r "PSEUDO_RELAY_URI\|createPseudoRelay\|PseudoRelay" packages/shell/src/ packages/shim/src/ apps/demo/src/ tests/` returns no matches
</acceptance_criteria>
</task>

</tasks>

<verification>
## Verification Criteria

1. `packages/shell/src/pseudo-relay.ts` does not exist; `packages/shell/src/shell-bridge.ts` exists
2. `grep -r "createPseudoRelay\|PseudoRelay\|PSEUDO_RELAY_URI\|pseudo-relay" packages/ apps/ tests/ SPEC.md --include="*.ts" --include="*.md"` returns zero matches (excluding .planning/ directory)
3. `pnpm build` exits 0
4. `pnpm type-check` exits 0
5. Importing `createShellBridge` from `@napplet/shell` resolves correctly (verified by build)
6. The `ShellBridge` type is exported from `@napplet/shell` (verified by type-check)
7. `SHELL_BRIDGE_URI` constant equals `'napplet://shell'` (value unchanged)
</verification>

<must_haves>
- createShellBridge is the sole factory function name (REN-01)
- ShellBridge is the sole type name (REN-02)
- shell-bridge.ts is the sole source file name (REN-03)
- All test imports use new names (REN-04)
- Demo app uses new names (REN-05)
- SPEC.md uses ShellBridge throughout (REN-06)
- SHELL_BRIDGE_URI is the constant name (REN-07)
- README uses new API names (REN-08)
</must_haves>
