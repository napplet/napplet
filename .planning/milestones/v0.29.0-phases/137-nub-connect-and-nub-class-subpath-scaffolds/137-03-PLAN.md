---
phase: 137-nub-connect-and-nub-class-subpath-scaffolds
plan: 03
type: execute
wave: 3
depends_on: [137-01, 137-02]
files_modified:
  - packages/nub/package.json
  - packages/nub/tsup.config.ts
autonomous: true
requirements: [NUB-05, NUB-06, NUB-07, CLASS-05, CLASS-06]
must_haves:
  truths:
    - "packages/nub/package.json has 46 subpath exports total (38 existing + 8 new: 4 for connect + 4 for class)"
    - "packages/nub/tsup.config.ts has 46 entry points total matching the package.json exports 1:1"
    - "pnpm --filter @napplet/nub build exits 0 and emits all 8 new dist entry points (connect/{index,types,shim,sdk}.{js,d.ts} + class/{index,types,shim,sdk}.{js,d.ts})"
    - "pnpm --filter @napplet/nub type-check exits 0"
    - "Tree-shake contract prerequisites are in place: connect/types.ts and class/types.ts are types-only plus one pure function (connect only); neither imports from shim.ts / sdk.ts / index.ts barrel of their subpath; importing `@napplet/nub/connect/types` or `@napplet/nub/class/types` via `import type` triggers zero runtime emission (proof deferred to Phase 142 VER-03 harness extension)"
  artifacts:
    - path: "packages/nub/package.json"
      provides: "8 new subpath export entries under the exports map"
      contains: "./connect"
    - path: "packages/nub/tsup.config.ts"
      provides: "8 new entry points in the tsup config entry map"
      contains: "connect/index"
    - path: "packages/nub/dist/connect/"
      provides: "Built connect subpath dist outputs (index, types, shim, sdk) × (.js, .d.ts)"
    - path: "packages/nub/dist/class/"
      provides: "Built class subpath dist outputs (index, types, shim, sdk) × (.js, .d.ts)"
  key_links:
    - from: "packages/nub/package.json exports"
      to: "packages/nub/dist/connect/*.{js,d.ts}"
      via: "exports['./connect'].import and exports['./connect/*'].import paths"
      pattern: "\\./dist/connect/"
    - from: "packages/nub/package.json exports"
      to: "packages/nub/dist/class/*.{js,d.ts}"
      via: "exports['./class'].import and exports['./class/*'].import paths"
      pattern: "\\./dist/class/"
    - from: "packages/nub/tsup.config.ts entry map"
      to: "packages/nub/src/connect/*.ts + packages/nub/src/class/*.ts"
      via: "8 key-value entries producing 8 dist outputs"
      pattern: "connect/index|class/index"
---

<objective>
Wire the 8 new source files from Plans 01-02 into the `@napplet/nub` package's subpath-exports map and tsup build pipeline. After this plan, consumers can `import { installConnectShim } from '@napplet/nub/connect'` and `import { installClassShim } from '@napplet/nub/class'` with full type resolution, and a `pnpm --filter @napplet/nub build` produces all 16 expected dist artifacts (8 `.js` + 8 `.d.ts`).

This plan is terminal for Phase 137 — it validates the end-to-end build + type-check gates that downstream Phase 138 (vite-plugin imports `normalizeConnectOrigin` from `@napplet/nub/connect/types`) and Phase 139 (central shim imports both installers) will depend on.

Purpose: Phase 137 Wave 3 — only runs after Plans 01 + 02 land their 8 source files. Tree-shake contract verification is scaffolded here (types.ts files are types-only + one pure function, no runtime side effects that would defeat tree-shaking) but the full harness extension is Phase 142 VER-03.

Output: Updated `packages/nub/package.json` (46 subpath exports total) + updated `packages/nub/tsup.config.ts` (46 entry points). Successful build + type-check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-nub-connect-and-nub-class-subpath-scaffolds/137-CONTEXT.md
@.planning/phases/137-nub-connect-and-nub-class-subpath-scaffolds/137-01-SUMMARY.md
@.planning/phases/137-nub-connect-and-nub-class-subpath-scaffolds/137-02-SUMMARY.md
@packages/nub/package.json
@packages/nub/tsup.config.ts

<interfaces>
<!-- The current exports map shape — new entries must match this style exactly -->

package.json exports entry template (resource is the closest precedent — added in v0.28.0 Phase 126):
```json
"./resource": {
  "types": "./dist/resource/index.d.ts",
  "import": "./dist/resource/index.js"
},
"./resource/types": {
  "types": "./dist/resource/types.d.ts",
  "import": "./dist/resource/types.js"
},
"./resource/shim": {
  "types": "./dist/resource/shim.d.ts",
  "import": "./dist/resource/shim.js"
},
"./resource/sdk": {
  "types": "./dist/resource/sdk.d.ts",
  "import": "./dist/resource/sdk.js"
}
```

tsup.config.ts entry template:
```typescript
'resource/index': 'src/resource/index.ts',
'resource/types': 'src/resource/types.ts',
'resource/shim': 'src/resource/shim.ts',
'resource/sdk': 'src/resource/sdk.ts',
```
</interfaces>

<current_state>
**packages/nub/package.json** (as of Phase 136 end): 38 subpath exports across 10 existing NUBs (relay, storage, ifc, keys, theme [3 entries — no shim/sdk], media, notify, identity, config, resource). Note: `theme` only has `./theme`, `./theme/types` (2 entries, not 4) because it's types-only with no shim or sdk surface. The remaining 9 NUBs have 4 entries each = 36, plus theme's 2 = 38 total.

Wait — recheck. Let me count actual entries in the existing exports map:
- relay: 4 (index, types, shim, sdk)
- storage: 4
- ifc: 4
- keys: 4
- theme: 2 (index, types) — theme has no shim/sdk files
- media: 4
- notify: 4
- identity: 4
- config: 4
- resource: 4

Total: 4×9 + 2 = 38. Confirmed.

**After this plan:**
- connect: 4 (index, types, shim, sdk)
- class: 4 (index, types, shim, sdk)

New total: 38 + 8 = **46 subpath exports**.

**packages/nub/tsup.config.ts** (as of Phase 136 end): 38 entry points matching the package.json exports 1:1. After this plan: 46 entry points.
</current_state>

<ordering>
The package.json exports map ordering in the file is NOT strict — Node's exports resolver is key-based, not order-based. But following the established convention, the connect + class entries should be appended at the END of the existing exports map (after resource), grouped by subpath (all 4 connect entries together, then all 4 class entries together).

The tsup.config.ts entry map ordering is also not strict — tsup treats it as an object literal. Append the 8 new entries at the end of the existing entry map, matching the grouping (all 4 connect, then all 4 class).

This matches the historical pattern: every prior NUB was appended in the order it was added (relay first, resource last). Preserve that convention.
</ordering>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 8 subpath exports to packages/nub/package.json (NUB-05, CLASS-05)</name>
  <read_first>
    - packages/nub/package.json (current state — 38 existing exports; note the resource block at the end as the nearest structural template)
    - .planning/phases/137-nub-connect-and-nub-class-subpath-scaffolds/137-02-SUMMARY.md (confirms all 8 source files exist)
  </read_first>
  <files>packages/nub/package.json</files>
  <behavior>
    - Add 4 subpath exports for `./connect`, `./connect/types`, `./connect/shim`, `./connect/sdk` — each with `types: "./dist/connect/<file>.d.ts"` and `import: "./dist/connect/<file>.js"` pointing to the corresponding dist outputs
    - Add 4 subpath exports for `./class`, `./class/types`, `./class/shim`, `./class/sdk` — same pattern
    - Append after the existing resource block (last entry in current exports map), grouped by subpath (connect block first, then class block), matching the file-order convention of existing entries
    - Do NOT modify any other field (version, dependencies, scripts, keywords, etc. remain byte-identical except for the exports map additions)
    - The `description` field MAY be updated to reflect the new NUBs but this is optional (leave it alone to minimize diff surface — Phase 141 Documentation Sweep handles README / description updates)
  </behavior>
  <action>
Open `packages/nub/package.json` and append 8 new entries to the `exports` object, immediately after the existing `"./resource/sdk"` entry.

The exact block to insert (verbatim JSON, ordered connect-then-class with index/types/shim/sdk within each):

```json
    "./connect": {
      "types": "./dist/connect/index.d.ts",
      "import": "./dist/connect/index.js"
    },
    "./connect/types": {
      "types": "./dist/connect/types.d.ts",
      "import": "./dist/connect/types.js"
    },
    "./connect/shim": {
      "types": "./dist/connect/shim.d.ts",
      "import": "./dist/connect/shim.js"
    },
    "./connect/sdk": {
      "types": "./dist/connect/sdk.d.ts",
      "import": "./dist/connect/sdk.js"
    },
    "./class": {
      "types": "./dist/class/index.d.ts",
      "import": "./dist/class/index.js"
    },
    "./class/types": {
      "types": "./dist/class/types.d.ts",
      "import": "./dist/class/types.js"
    },
    "./class/shim": {
      "types": "./dist/class/shim.d.ts",
      "import": "./dist/class/shim.js"
    },
    "./class/sdk": {
      "types": "./dist/class/sdk.d.ts",
      "import": "./dist/class/sdk.js"
    }
```

Placement:
- Insert immediately after the closing `}` of the current `"./resource/sdk"` entry (around line 158 in the file).
- Before the closing `}` of the `exports` object (around line 159).
- The last entry before the exports-map-closing `}` MUST NOT have a trailing comma — the current file has `"./resource/sdk": { ... }` as the last entry without a trailing comma. After this task, `"./class/sdk": { ... }` will be the new last entry without a trailing comma.
- Preserve 2-space indentation (matches existing entries).

Do NOT modify:
- `name`, `version`, `description`, `type`, `files`, `sideEffects: false`, `publishConfig`, `dependencies`, `devDependencies`, `peerDependencies`, `peerDependenciesMeta`, `scripts`, `license`, `repository`, `keywords`.
- `sideEffects: false` stays — the tree-shake contract requires this (v0.26.0 Phase 117 locked it).
  </action>
  <verify>
    <automated>node -e "const p = require('./packages/nub/package.json'); const keys = Object.keys(p.exports); if (keys.length !== 46) { console.error('exports count:', keys.length, 'expected 46'); process.exit(1); } const required = ['./connect', './connect/types', './connect/shim', './connect/sdk', './class', './class/types', './class/shim', './class/sdk']; for (const k of required) { if (!p.exports[k]) { console.error('missing export:', k); process.exit(1); } if (p.exports[k].types !== './dist' + k + (k.includes('/') ? '.d.ts' : '/index.d.ts')) { const expected = k.includes('/') ? './dist' + k + '.d.ts' : './dist' + k + '/index.d.ts'; if (p.exports[k].types !== expected) { console.error('bad types path for', k, ':', p.exports[k].types, 'expected', expected); process.exit(1); } } } console.log('ok: 46 exports, all 8 new subpaths present'); if (p.sideEffects !== false) { console.error('sideEffects must remain false'); process.exit(1); } console.log('sideEffects: false preserved');"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/nub/package.json` `exports` object has 46 keys (grep-verifiable: `node -e "console.log(Object.keys(require('./packages/nub/package.json').exports).length)"` prints `46`)
    - All 8 new keys exist: `./connect`, `./connect/types`, `./connect/shim`, `./connect/sdk`, `./class`, `./class/types`, `./class/shim`, `./class/sdk`
    - Each new entry has both `types` and `import` fields pointing to `./dist/<subpath>/<file>.{d.ts,js}` paths
    - `sideEffects: false` is preserved (tree-shake contract)
    - JSON is valid (parseable by `node -e "require('./packages/nub/package.json')"`)
    - No other fields mutated (diff should show ONLY additions in the exports block)
  </acceptance_criteria>
  <done>
    8 new subpath exports added; JSON remains valid; sideEffects:false preserved; no other fields changed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 8 tsup entries + run build + type-check to prove the pipeline (NUB-06, NUB-07, CLASS-06)</name>
  <read_first>
    - packages/nub/tsup.config.ts (current state — 38 entries; note the resource block at the end as the nearest structural template)
    - packages/nub/package.json (output of Task 1 — 46 exports; the entry map below must match 1:1)
    - .planning/phases/137-nub-connect-and-nub-class-subpath-scaffolds/137-01-SUMMARY.md
    - .planning/phases/137-nub-connect-and-nub-class-subpath-scaffolds/137-02-SUMMARY.md
  </read_first>
  <files>packages/nub/tsup.config.ts</files>
  <behavior>
    - Add 8 entry points to the `entry` object in tsup.config.ts: 4 for connect (`connect/index`, `connect/types`, `connect/shim`, `connect/sdk`) and 4 for class (`class/index`, `class/types`, `class/shim`, `class/sdk`), each mapping to the corresponding `src/<subpath>/<file>.ts` path
    - Append after the existing resource entries (last block in current entry map), matching the existing 2-space indentation and single-quote string style
    - Run `pnpm --filter @napplet/nub build` — must exit 0 and emit 8 new dist outputs (connect/*.js, connect/*.d.ts, class/*.js, class/*.d.ts)
    - Run `pnpm --filter @napplet/nub type-check` — must exit 0
    - Spot-check dist outputs exist and are non-empty
  </behavior>
  <action>
### Sub-step 1: Update tsup.config.ts

Open `packages/nub/tsup.config.ts` and append 8 new entries to the `entry` object, immediately after the existing `'resource/sdk': 'src/resource/sdk.ts',` line.

The exact block to insert:

```typescript
    'connect/index': 'src/connect/index.ts',
    'connect/types': 'src/connect/types.ts',
    'connect/shim': 'src/connect/shim.ts',
    'connect/sdk': 'src/connect/sdk.ts',
    'class/index': 'src/class/index.ts',
    'class/types': 'src/class/types.ts',
    'class/shim': 'src/class/shim.ts',
    'class/sdk': 'src/class/sdk.ts',
```

Placement:
- Insert after `'resource/sdk': 'src/resource/sdk.ts',` (around line 42).
- Before the closing `}` of the `entry` object (around line 43).
- All new entries have trailing commas (matches all existing entries in the file).
- Preserve 4-space indentation (matches the rest of the entry block — it's inside `entry: { … }` inside `defineConfig({ … })`).
- Do NOT modify `format`, `dts`, `sourcemap`, `clean` — all stay as-is.

### Sub-step 2: Build + verify dist outputs

Run: `pnpm --filter @napplet/nub build`

Expected: exit 0. Emits (in addition to existing dist outputs):
- `packages/nub/dist/connect/index.js` + `.d.ts`
- `packages/nub/dist/connect/types.js` + `.d.ts`
- `packages/nub/dist/connect/shim.js` + `.d.ts`
- `packages/nub/dist/connect/sdk.js` + `.d.ts`
- `packages/nub/dist/class/index.js` + `.d.ts`
- `packages/nub/dist/class/types.js` + `.d.ts`
- `packages/nub/dist/class/shim.js` + `.d.ts`
- `packages/nub/dist/class/sdk.js` + `.d.ts`

If the build fails:
- Check imports in the 8 new src files — most likely cause is a path typo (e.g., missing `.js` extension on a relative import in ESM).
- Check that `import type { NappletGlobal } from '@napplet/core'` resolves — `@napplet/core` is a workspace dep and must be built first. The monorepo turborepo config typically handles this; if not, `pnpm --filter @napplet/core build` may need to run first (but in normal flow `pnpm -r build` does the right order).

### Sub-step 3: Type-check

Run: `pnpm --filter @napplet/nub type-check`

Expected: exit 0.

### Sub-step 4: Tree-shake prerequisite spot check

This task does NOT run the full tree-shake harness (Phase 142 VER-03 scope) but does verify the prerequisites are in place:

Run: `node -e "const src = require('fs').readFileSync('./packages/nub/dist/connect/types.js', 'utf8'); const runtimeCode = src.replace(/\\/\\/.*$/gm, '').replace(/\\/\\*[\\s\\S]*?\\*\\//g, '').trim(); console.log('types.js bytes:', Buffer.byteLength(src), 'runtime-ish:', runtimeCode.length); if (runtimeCode.includes('installConnectShim') || runtimeCode.includes('registerNub(')) { console.error('types.js leaked installer or registerNub'); process.exit(1); } console.log('ok: connect/types.js has no installer or registerNub emission');"`

Run the same check for `class/types.js`:
`node -e "const src = require('fs').readFileSync('./packages/nub/dist/class/types.js', 'utf8'); if (src.includes('installClassShim') || src.includes('registerNub(')) { console.error('class/types.js leaked installer or registerNub'); process.exit(1); } console.log('ok: class/types.js has no installer or registerNub emission');"`

Expected: both checks print `ok`. This confirms the tree-shake contract prerequisite — a downstream consumer importing only `@napplet/nub/connect/types` (via `import type`) will not pull in `installConnectShim` or `registerNub`, satisfying NUB-07 and CLASS-06 at the source-artifact level (full bundler-level proof is Phase 142).

If either types.js contains `installConnectShim` or `registerNub(`:
- Fix the source: types.ts MUST NOT import from shim.ts / sdk.ts / index.ts. Check for accidental imports.
- Re-run build + re-verify.
  </action>
  <verify>
    <automated>grep -q "'connect/index': 'src/connect/index.ts'" packages/nub/tsup.config.ts && grep -q "'connect/types': 'src/connect/types.ts'" packages/nub/tsup.config.ts && grep -q "'connect/shim': 'src/connect/shim.ts'" packages/nub/tsup.config.ts && grep -q "'connect/sdk': 'src/connect/sdk.ts'" packages/nub/tsup.config.ts && grep -q "'class/index': 'src/class/index.ts'" packages/nub/tsup.config.ts && grep -q "'class/types': 'src/class/types.ts'" packages/nub/tsup.config.ts && grep -q "'class/shim': 'src/class/shim.ts'" packages/nub/tsup.config.ts && grep -q "'class/sdk': 'src/class/sdk.ts'" packages/nub/tsup.config.ts && pnpm --filter @napplet/nub build && pnpm --filter @napplet/nub type-check && test -f packages/nub/dist/connect/index.js && test -f packages/nub/dist/connect/index.d.ts && test -f packages/nub/dist/connect/types.js && test -f packages/nub/dist/connect/types.d.ts && test -f packages/nub/dist/connect/shim.js && test -f packages/nub/dist/connect/shim.d.ts && test -f packages/nub/dist/connect/sdk.js && test -f packages/nub/dist/connect/sdk.d.ts && test -f packages/nub/dist/class/index.js && test -f packages/nub/dist/class/index.d.ts && test -f packages/nub/dist/class/types.js && test -f packages/nub/dist/class/types.d.ts && test -f packages/nub/dist/class/shim.js && test -f packages/nub/dist/class/shim.d.ts && test -f packages/nub/dist/class/sdk.js && test -f packages/nub/dist/class/sdk.d.ts && ! grep -q "installConnectShim\|registerNub(" packages/nub/dist/connect/types.js && ! grep -q "installClassShim\|registerNub(" packages/nub/dist/class/types.js</automated>
  </verify>
  <acceptance_criteria>
    - `packages/nub/tsup.config.ts` entry map has 46 entries (38 existing + 8 new) matching package.json exports 1:1
    - 8 new entries present with exact keys: `'connect/index'`, `'connect/types'`, `'connect/shim'`, `'connect/sdk'`, `'class/index'`, `'class/types'`, `'class/shim'`, `'class/sdk'`
    - `pnpm --filter @napplet/nub build` exits 0
    - `pnpm --filter @napplet/nub type-check` exits 0
    - All 16 new dist artifacts exist (`dist/connect/{index,types,shim,sdk}.{js,d.ts}` + `dist/class/{index,types,shim,sdk}.{js,d.ts}`)
    - `dist/connect/types.js` does NOT contain the string `installConnectShim` or `registerNub(` (tree-shake prerequisite for NUB-07)
    - `dist/class/types.js` does NOT contain the string `installClassShim` or `registerNub(` (tree-shake prerequisite for CLASS-06)
  </acceptance_criteria>
  <done>
    Build pipeline complete. All 8 new dist outputs emit successfully. Type-check green. Tree-shake prerequisite (types.js files have zero runtime emissions beyond the pure normalizer in connect/types.js) verified at the dist-artifact level. Phase 137 is ready for downstream Phase 138 (vite-plugin imports `normalizeConnectOrigin`) and Phase 139 (central shim imports `installConnectShim` + `installClassShim`).
  </done>
</task>

</tasks>

<verification>
Phase-level checks after both tasks complete:

1. `node -e "console.log(Object.keys(require('./packages/nub/package.json').exports).length)"` prints `46`.
2. `grep -c "'.*': 'src/" packages/nub/tsup.config.ts` prints `46`.
3. `pnpm --filter @napplet/nub build` exits 0.
4. `pnpm --filter @napplet/nub type-check` exits 0.
5. `ls packages/nub/dist/connect/ | wc -l` prints at least `8` (4 .js + 4 .d.ts, plus .js.map sourcemaps = typically 12).
6. `ls packages/nub/dist/class/ | wc -l` prints at least `8`.
7. Tree-shake prerequisite: both `dist/connect/types.js` and `dist/class/types.js` contain zero references to `installConnectShim` / `installClassShim` / `registerNub(` — confirmed via grep.
8. Phase-wide sanity: `pnpm --filter @napplet/nub/core build && pnpm --filter @napplet/nub build` green (core must build first due to workspace dep; `pnpm -r build` in dependency order also works).

Cross-phase sanity check (optional, validates downstream-facing contract):
9. A bundler test consuming only `@napplet/nub/connect/types` via `import type { NappletConnect, normalizeConnectOrigin }` SHOULD NOT pull in installer code — full proof deferred to Phase 142 VER-03 harness extension, but the source-level contract (types.ts has no imports from shim/sdk/index) is preserved and verifiable via grep now.
</verification>

<success_criteria>
- `packages/nub/package.json` has 46 subpath exports; all 8 new entries present; sideEffects:false preserved; JSON valid
- `packages/nub/tsup.config.ts` has 46 entry points; all 8 new entries present; valid TypeScript
- `pnpm --filter @napplet/nub build` green
- `pnpm --filter @napplet/nub type-check` green
- 16 new dist artifacts present (8 .js + 8 .d.ts)
- Tree-shake prerequisite satisfied: connect/types.js and class/types.js have zero `install*Shim` / `registerNub(` emissions
- NUB-05, NUB-06, NUB-07 satisfied (connect package wiring + tree-shake prerequisite)
- CLASS-05, CLASS-06 satisfied (class package wiring + tree-shake prerequisite)
- Phase 137 goal complete: both NUB subpaths exist, fully tree-shakable, ready for central shim/SDK integration in Phase 139
</success_criteria>

<output>
After completion, create `.planning/phases/137-nub-connect-and-nub-class-subpath-scaffolds/137-03-SUMMARY.md` documenting:
- Final package.json exports count (should be 46) and the 8 new entry keys
- Final tsup.config.ts entry count (46) and the 8 new keys
- `pnpm --filter @napplet/nub build` output summary (exit code, dist file sizes for the 8 new .js files — useful as a baseline for Phase 142 VER-03 harness extension)
- `pnpm --filter @napplet/nub type-check` output summary (exit code)
- Dist artifact inventory under `packages/nub/dist/connect/` and `packages/nub/dist/class/` (list of emitted files)
- Tree-shake prerequisite verification result (both types.js files clean of installer / registerNub references)
- Phase 137 completion status: all 13 REQs (NUB-01..07 + CLASS-01..06) satisfied
- Pointers for Phase 138 + Phase 139 executors: exact import paths (`@napplet/nub/connect/types` exports `normalizeConnectOrigin`; `@napplet/nub/connect/shim` exports `installConnectShim`; `@napplet/nub/class/shim` exports `installClassShim` + `handleClassMessage`)
</output>
