---
phase: 135-cross-repo-spec-work
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md
  - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md
  - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md
autonomous: true
requirements: [SPEC-04, SPEC-06, SPEC-07]

must_haves:
  truths:
    - "A reader can find the NUB-CLASS track's authoring rules inside NUB-CLASS.md (integers ascend, no reuse, no skipping; citation convention uses the file name `NUB-CLASS-$N`)"
    - "A shell implementer can read NUB-CLASS.md alone and know exactly what wire message to send (`class.assigned`, payload `{ class: number }`, shell→napplet at iframe ready, at most one terminal envelope per lifecycle)"
    - "A napplet author can read NUB-CLASS.md alone and know how to consume the runtime surface (`window.napplet.class` → `number | undefined`, undefined when shell does not implement `nub:class` or before the wire arrives)"
    - "NUB-CLASS-1.md defines the strict / no-user-declared-origins posture (CSP baseline with `connect-src 'none'`, no consent prompt, shell emits `class.assigned` with `class: 1`)"
    - "NUB-CLASS-2.md defines the user-approved explicit-origin posture (`connect-src <granted-origins>`, consent prompt, grant keyed on `(dTag, aggregateHash)`, shell emits `class.assigned` with `class: 2` on approval / `class: 1` on denial)"
    - "Neither NUB-CLASS-1.md nor NUB-CLASS-2.md mentions NUB-CONNECT, manifest `connect` tags, origin validation, or aggregateHash folding — those are NUB-CONNECT concerns, not class-track concerns"
    - "All three documents pass zero-grep hygiene (no `@napplet/`, no `kehto`, no `hyprgate`, no `packages/(nub|shim|sdk|vite-plugin)`)"
  artifacts:
    - path: ".planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md"
      provides: "NUB-CLASS track definition + wire message + runtime surface + template/guidance for sub-track members"
      contains: "class.assigned"
    - path: ".planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md"
      provides: "Strict baseline class definition (connect-src 'none')"
      contains: "connect-src 'none'"
    - path: ".planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md"
      provides: "User-approved explicit-origin class definition"
      contains: "(dTag, aggregateHash)"
  key_links:
    - from: ".planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md"
      to: ".planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md"
      via: "References section link"
      pattern: "NUB-CLASS\\.md"
    - from: ".planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md"
      to: ".planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md"
      via: "References section link"
      pattern: "NUB-CLASS\\.md"
---

<objective>
Author the three documents that form the NUB-CLASS sub-track: `NUB-CLASS.md` (the track definition + internal template/guidance for sub-track members), `NUB-CLASS-1.md` (strict posture), and `NUB-CLASS-2.md` (user-approved explicit-origin posture). These three files are authored together in one plan so the template in NUB-CLASS.md and the two concrete members stay consistent. NUB-CONNECT.md (Plan 03) cites `NUB-CLASS-2` by name, so `NUB-CLASS-2.md` must land here before Plan 03 executes.

Purpose: Establish the class-track machinery in the public NUBs repo draft bundle. NIP-5D returns to NUB-neutral transport-only purity (Plan 02); NUB-CLASS owns the "what class is this napplet?" question; individual `NUB-CLASS-$N` documents own specific class definitions.

Output: Three Markdown drafts at `.planning/phases/135-cross-repo-spec-work/drafts/`. Human opens the cross-repo PR against `napplet/nubs` in a later, out-of-scope step (same pattern as v0.28.0 Phase 132).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/135-cross-repo-spec-work/135-CONTEXT.md
@.planning/milestones/v0.28.0-phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md
</context>

<interfaces>
<!-- Structural template: NUB-RESOURCE.md (v0.28.0 Phase 132 draft). Executor MUST mirror this shape. -->
<!-- Setext heading (===), subtitle with dashes (---), status banner (`draft`), metadata block (NUB ID, Namespace, Discovery, Parent), ## Description, ## API Surface (only if API), ## Wire Protocol (only if wire), ## Security Considerations, ## References. -->

Locked wire shape for NUB-CLASS (from CONTEXT.md):
```typescript
// class.assigned — shell → napplet, at iframe ready, at-most-one terminal envelope per lifecycle
{ type: 'class.assigned'; id: string; class: number }
```

Locked runtime surface for NUB-CLASS:
```typescript
window.napplet.class: number | undefined;
// undefined when shell does not implement `nub:class` or before `class.assigned` wire arrives
```

Locked capability for NUB-CLASS: `shell.supports('nub:class')`

Citation convention (from CONTEXT.md specifics): NUBs cite class documents by their file name (`NUB-CLASS-1`, `NUB-CLASS-2`), not by the abstract phrase "Class 1" / "Class 2".
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author NUB-CLASS.md (track definition + template/guidance)</name>
  <files>.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md</files>
  <read_first>
    - .planning/milestones/v0.28.0-phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md (structural template: setext heading, subtitle, status banner, metadata block, section order)
    - .planning/phases/135-cross-repo-spec-work/135-CONTEXT.md (locked wire shape + runtime surface + template/guidance scope)
    - .planning/REQUIREMENTS.md (SPEC-04 requirement text)
    - .planning/research/PITFALLS.md (SPEC-P4 — class delegation boundary; RUNTIME-P1 — graceful degradation default)
  </read_first>
  <action>
Create `.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` as a new draft. Mirror the NUB-RESOURCE.md structural template EXACTLY:

1. **Setext heading + subtitle**:
   ```
   NUB-CLASS
   =========

   Napplet Class Authority
   -----------------------

   `draft`

   **NUB ID:** NUB-CLASS
   **Namespace:** `window.napplet.class`
   **Discovery:** `shell.supports("class")`
   **Parent:** NIP-5D
   ```

2. **## Description** (2-3 paragraphs):
   - Paragraph 1: NUB-CLASS establishes an abstract "napplet class" concept — an integer number assigned by the shell at iframe-ready time, delivered over the wire, and readable by the napplet at `window.napplet.class`. The class captures the shell's determination of the napplet's security posture (CSP shape, consent state, etc.). This NUB OWNS the class-track machinery; concrete class definitions live in sibling `NUB-CLASS-$N` documents.
   - Paragraph 2: The shell is the sole authority on class. Class is determined by which class-contributing NUBs are declared in the napplet manifest combined with any user-consent outcomes. The shell sends exactly one `class.assigned` envelope per napplet lifecycle, after the iframe signals readiness and before the napplet branches on class. Dynamic re-classification is out of scope for v1.
   - Paragraph 3: This NUB is voluntary. Napplets MUST gracefully degrade when `window.napplet.class` is `undefined` (shell does not implement `nub:class`, or wire has not yet arrived). Cross-NUB invariants (e.g., "in shells implementing both NUB-CLASS and NUB-CONNECT, `class === 2` iff `connect.granted === true`") are shell responsibilities documented by the NUBs that participate.

3. **## API Surface** (napplet-side runtime):
   ```typescript
   interface NappletGlobal {
     /**
      * Shell-assigned class number. `undefined` until the shell sends
      * `class.assigned`, or permanently `undefined` when the shell does not
      * implement `nub:class`.
      */
     readonly class?: number;
   }
   ```
   Prose: Napplets read `window.napplet.class`. Check `shell.supports('nub:class')` before depending on the value. Once assigned, the value is stable for the lifetime of the napplet document.

4. **## Wire Protocol** with the exact NUB-RESOURCE-style table:
   | Type | Direction | Payload fields |
   |------|-----------|----------------|
   | `class.assigned` | shell -> napplet | `id`, `class` |

   Follow with normative prose:
   - "The shell MUST send `class.assigned` after the iframe signals readiness (shim bootstrap complete) and before any other napplet-bound envelope for that iframe."
   - "The shell MUST send AT MOST ONE `class.assigned` envelope per napplet lifecycle. Dynamic re-classification is out of scope for v1."
   - "The `id` field is a freshly minted correlation identifier per NIP-5D envelope conventions; there is no napplet→shell response envelope."
   - "The `class` field is a non-negative integer. The integer value selects a concrete class definition from the `NUB-CLASS-$N` sub-track (e.g., `class: 1` selects `NUB-CLASS-1`)."
   - Include one worked example mirroring the NUB-RESOURCE.md example style:
     ```
     shell -> napplet: { "type": "class.assigned", "id": "c1", "class": 2 }
     ```

5. **## NUB-CLASS-$N Sub-Track** — THIS is the internal template + authoring guidance section required by SPEC-04. MUST include these subsections:
   - **Naming rules**: Class numbers are non-negative integers. Integers ascend (1, 2, 3, ...). A class number MUST NOT be reused once published to the public NUBs repo. A class number MUST NOT be skipped without spec-level justification written into the gap number's document as a "reserved" placeholder. File names are always `NUB-CLASS-$N.md` (e.g., `NUB-CLASS-1.md`).
   - **Required content structure for each sub-track member**: Every `NUB-CLASS-$N.md` MUST include (1) Setext heading + subtitle + status banner matching NUB-CLASS.md shape; (2) a single-paragraph Description naming the posture in one sentence; (3) a **CSP posture** section stating the exact CSP directive shape (e.g., `connect-src 'none'`); (4) a **Manifest prerequisites** section listing which manifest tags or NUBs trigger the class; (5) a **Shell responsibilities** section enumerating what the shell MUST do at iframe serve time (consent prompt? emit grant-derived header? persist state?); (6) a **Security considerations** section; (7) a **References** section citing `NUB-CLASS.md` as parent.
   - **Citation conventions**: NUBs and other specs MUST cite class documents by file name (e.g., "see `NUB-CLASS-2.md`") and MUST NOT use the abstract phrase "Class 2" or "class 2" as the primary reference. This convention prevents prose drift when a class document is renamed or superseded.
   - **Adding a new class**: Authors proposing a new class MUST open a PR to the public `napplet/nubs` repo with a new `NUB-CLASS-$N.md` file and an update to this section documenting the new entry. The class number is assigned at merge time by the NUBs-track maintainers.

6. **## Capability Advertisement**: Shells implementing this NUB MUST advertise `shell.supports('nub:class') === true`. Napplets MUST NOT depend on `window.napplet.class` having a value; always check `shell.supports('nub:class')` first and fall back to a sensible default (treat the napplet as unclassified).

7. **## Security Considerations** (3-4 subsections):
   - **Shell is sole authority**: Napplet code MUST NOT attempt to infer its own class from the environment; the shell's `class.assigned` is the only authoritative signal. A napplet that ignores `class.assigned` and assumes a class risks getting confused between a strict-posture shell and a permissive-posture shell.
   - **At-most-one envelope**: The at-most-one-terminal-envelope-per-lifecycle constraint prevents races where the napplet's behavior depends on late-arriving class signals. Shells MUST NOT emit a second `class.assigned` to "upgrade" or "downgrade" a class mid-session.
   - **Cross-NUB invariants are shell responsibilities**: NUBs that contribute to class determination (e.g., NUB-CONNECT) document their own invariants (e.g., "a napplet with `connect` tags and granted consent takes on `NUB-CLASS-2`"). Enforcing those invariants is a shell obligation, not a NUB-CLASS obligation. If a shell implements NUB-CONNECT but emits `class.assigned` with `class: 1` despite `connect.granted === true`, the shell is non-conformant to NUB-CONNECT — NUB-CLASS itself has no opinion.
   - **Graceful degradation**: `window.napplet.class === undefined` is a valid and well-defined state. Napplets that branch on class MUST handle the undefined case as "unclassified — assume the most restrictive defaults the napplet can function under."

8. **## References**: Cite `NIP-5D` (parent) and the published sub-track members (`NUB-CLASS-1.md`, `NUB-CLASS-2.md`).

CRITICAL constraints:
- Zero mentions of `@napplet/`, `kehto`, `hyprgate`, `packages/nub`, `packages/shim`, `packages/sdk`, `packages/vite-plugin`. This file is destined for the public NUBs repo.
- Zero mentions of `NUB-CONNECT` by name — NUB-CLASS is NUB-agnostic. Reference "NUBs that contribute to class determination" abstractly.
- Zero inline definitions of Class-1 or Class-2 postures — those live in the sibling documents. NUB-CLASS.md only defines the TRACK.
- Use RFC 2119 capitalized MUST / MUST NOT / SHOULD / MAY throughout normative prose.
  </action>
  <verify>
    <automated>test -f .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md &amp;&amp; grep -q "class.assigned" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md &amp;&amp; grep -q "NUB-CLASS-\$N" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md &amp;&amp; grep -q "^NUB-CLASS$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md &amp;&amp; grep -q "^=\{9,\}$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md &amp;&amp; grep -q "Naming rules" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` succeeds
    - `grep -c "^NUB-CLASS$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` returns `1` (setext heading text line present)
    - `grep -c "^=\{9,\}$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` returns at least `1` (setext heading underline present)
    - `grep -c "class.assigned" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` returns at least `3` (definition + prose + example)
    - `grep -q "window.napplet.class" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` succeeds
    - `grep -q "nub:class" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` succeeds
    - `grep -q "NUB-CLASS-\$N" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` succeeds (template section refers to sub-track members)
    - `grep -q "Naming rules" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` succeeds (authoring guidance section present)
    - `grep -qE "(at[- ]most[- ]one|at most one)" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` succeeds (terminal-envelope constraint documented)
    - `grep -E "@napplet/|kehto|hyprgate|packages/(nub\|shim\|sdk\|vite-plugin)" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` returns zero matches
    - `! grep -q "NUB-CONNECT" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` (NUB-CLASS is NUB-agnostic; no NUB-CONNECT name)
    - `! grep -qE "connect-src '?[a-zA-Z-]" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` (no inline class posture definitions; CSP directive shapes belong in sub-track members)
  </acceptance_criteria>
  <done>
NUB-CLASS.md exists at `.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md`. Contains setext heading, metadata block, Description, API Surface, Wire Protocol (with `class.assigned` envelope table + worked example), NUB-CLASS-$N Sub-Track authoring-guidance section (naming rules, required structure, citation conventions), Capability Advertisement, Security Considerations (4 subsections), References. Zero mentions of private-repo names or NUB-CONNECT. Zero inline Class-1/Class-2 posture definitions.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author NUB-CLASS-1.md (strict baseline posture)</name>
  <files>.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md</files>
  <read_first>
    - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md (authored in Task 1 — MUST match the required content structure defined by its "NUB-CLASS-$N Sub-Track" section)
    - .planning/milestones/v0.28.0-phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md (structural template for setext heading + status banner + metadata block)
    - .planning/REQUIREMENTS.md (SPEC-06 requirement text)
    - .planning/phases/135-cross-repo-spec-work/135-CONTEXT.md (Class-1 = strict baseline with connect-src 'none', no consent prompt, shell emits `class.assigned` with `class: 1`)
  </read_first>
  <action>
Create `.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md`. This document MUST follow the required content structure defined in NUB-CLASS.md's "NUB-CLASS-$N Sub-Track" section.

1. **Setext heading + metadata block**:
   ```
   NUB-CLASS-1
   ===========

   Strict Baseline Class
   ---------------------

   `draft`

   **NUB ID:** NUB-CLASS-1
   **Parent:** NUB-CLASS
   **Class number:** 1
   ```

2. **## Description** (single paragraph): "NUB-CLASS-1 is the strict baseline posture for napplets that do not declare any class-contributing NUB capabilities. Shells emit a restrictive Content Security Policy with `connect-src 'none'`, show no consent prompt, and send `class.assigned` with `class: 1`. This is the default class for any napplet whose manifest lacks tags from NUBs that would promote it to a higher class."

3. **## CSP Posture**: State the exact directive shape in a code block:
   ```
   connect-src 'none'
   ```
   Prose: "Shells emitting the NUB-CLASS-1 posture MUST include `connect-src 'none'` in the runtime CSP served with the napplet's HTML. The directive is the signal that the napplet has no user-approved direct-network origins. Other directives in the baseline CSP (e.g., `script-src`, `default-src`, `img-src`) are shell-policy concerns and are NOT specified by NUB-CLASS-1 — only the `connect-src` value is the class's defining characteristic."

4. **## Manifest Prerequisites**: "NUB-CLASS-1 is the default posture. A napplet manifest reaches NUB-CLASS-1 when no other class-contributing NUB's trigger conditions are met. Future `NUB-CLASS-$N` entries (with `$N > 1`) MUST document their trigger conditions; absence of all such triggers means the napplet is NUB-CLASS-1."

5. **## Shell Responsibilities** (bulleted MUSTs):
   - Shell MUST emit a runtime CSP header containing `connect-src 'none'` for the napplet's HTML response.
   - Shell MUST NOT prompt the user for any network-access consent when serving a NUB-CLASS-1 napplet (there is nothing to consent to).
   - Shell MUST send exactly one `class.assigned` envelope with payload `{ class: 1 }` at iframe-ready time per NUB-CLASS's wire-timing rules.
   - Shell SHOULD treat residual `<meta http-equiv="Content-Security-Policy">` in NUB-CLASS-1 napplet HTML as harmless (CSP intersection of two `connect-src 'none'` values is still `'none'`) but MAY log a deployment warning to encourage napplet authors to migrate away from meta-CSP emission.

6. **## Security Considerations**:
   - **Strictness floor, not a ceiling**: NUB-CLASS-1 is the minimum-trust posture. It is appropriate for napplets with no network requirements, napplets that rely exclusively on shell-mediated resource fetching (via other NUBs that provide proxied resource access), or napplets in development/test environments where network egress is disabled by policy.
   - **CSP intersection**: Because the browser enforces CSP as an intersection of header + meta directives, a NUB-CLASS-1 napplet that accidentally ships meta-CSP with `connect-src 'none'` will still behave correctly (intersection of `'none'` and `'none'` is `'none'`). This is why shells treat meta-CSP as harmless in the NUB-CLASS-1 case but refuse-to-serve in higher classes.
   - **No grant state**: NUB-CLASS-1 has no persistent state — no consent decision, no origin list, no `(dTag, aggregateHash)` key. Every napplet resolving to NUB-CLASS-1 is indistinguishable from every other at the class level.

7. **## References**: Cite `NUB-CLASS.md` as parent.

CRITICAL constraints:
- Zero mentions of `@napplet/`, `kehto`, `hyprgate`, `packages/(nub|shim|sdk|vite-plugin)`.
- Zero mentions of `NUB-CONNECT` by name. Abstract reference only: "other class-contributing NUBs" or "higher class triggers".
- Use RFC 2119 capitalized MUST / MUST NOT / SHOULD / MAY.
- Document is short (≈80-120 lines); do not pad.
  </action>
  <verify>
    <automated>test -f .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md &amp;&amp; grep -q "^NUB-CLASS-1$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md &amp;&amp; grep -q "connect-src 'none'" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md &amp;&amp; grep -q "class: 1" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md &amp;&amp; grep -q "Class number" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md` succeeds
    - `grep -c "^NUB-CLASS-1$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md` returns `1` (setext heading)
    - `grep -c "^=\{11,\}$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md` returns at least `1` (setext underline)
    - `grep -q "connect-src 'none'" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md` succeeds (CSP posture present)
    - `grep -q "class: 1" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md` succeeds (wire value documented)
    - `grep -qi "no consent" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md` succeeds (consent-prompt absence documented)
    - `grep -q "NUB-CLASS" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md` succeeds (parent reference)
    - `grep -E "@napplet/|kehto|hyprgate|packages/(nub\|shim\|sdk\|vite-plugin)" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md` returns zero matches
    - `! grep -q "NUB-CONNECT" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md` (no NUB-CONNECT name cross-contamination)
  </acceptance_criteria>
  <done>
NUB-CLASS-1.md exists with setext heading + metadata block (parent: NUB-CLASS, class number: 1), single-paragraph Description, CSP Posture (`connect-src 'none'`), Manifest Prerequisites (default posture when no class-contributing triggers match), Shell Responsibilities (emit CSP header + no consent prompt + send `class.assigned` with `class: 1`), Security Considerations, References. Passes zero-grep hygiene.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author NUB-CLASS-2.md (user-approved explicit-origin posture)</name>
  <files>.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md</files>
  <read_first>
    - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md (authored in Task 1 — required content structure)
    - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md (authored in Task 2 — parallel structure for consistency)
    - .planning/milestones/v0.28.0-phases/132-cross-repo-nubs-prs/drafts/NUB-RESOURCE.md (structural template)
    - .planning/REQUIREMENTS.md (SPEC-07 requirement text)
    - .planning/phases/135-cross-repo-spec-work/135-CONTEXT.md (Class-2 = user-approved explicit-origin CSP, consent prompt, grant keyed on (dTag, aggregateHash), falls back to class: 1 on denial)
    - .planning/research/PITFALLS.md (SHELL-P3, SHELL-P4 for posture-relevant shell behaviors that apply here)
  </read_first>
  <action>
Create `.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md`. This document MUST follow the required content structure defined in NUB-CLASS.md's "NUB-CLASS-$N Sub-Track" section, in structural parallel to NUB-CLASS-1.md.

1. **Setext heading + metadata block**:
   ```
   NUB-CLASS-2
   ===========

   User-Approved Explicit-Origin Class
   -----------------------------------

   `draft`

   **NUB ID:** NUB-CLASS-2
   **Parent:** NUB-CLASS
   **Class number:** 2
   ```

2. **## Description** (single paragraph): "NUB-CLASS-2 is the posture for napplets that declare explicit direct-network origins and have received user consent for those origins. Shells emit a runtime CSP with `connect-src <granted-origins>`, prompt the user on first load, persist the decision keyed on `(dTag, aggregateHash)`, and send `class.assigned` with `class: 2` when approved. If the user denies the prompt, the napplet is served under the NUB-CLASS-1 posture instead (`class: 1`, `connect-src 'none'`) — denied napplets are indistinguishable from napplets that never declared origins."

3. **## CSP Posture**: State the directive shape in a code block:
   ```
   connect-src <space-separated-list-of-granted-origins>
   ```
   Prose: "Shells emitting the NUB-CLASS-2 posture MUST include `connect-src <granted-origins>` in the runtime CSP, where `<granted-origins>` is the user-approved origin list for the napplet identified by `(dTag, aggregateHash)`. The origin list MUST be exactly the set the user approved — shells MUST NOT add, remove, or normalize origins between the approved set and the emitted directive. Other CSP directives in the baseline are shell-policy concerns; only the `connect-src` value is the class's defining characteristic."

4. **## Manifest Prerequisites**: "A napplet manifest reaches NUB-CLASS-2 when it declares at least one tag from a class-contributing NUB whose trigger condition for NUB-CLASS-2 is met, AND the user has approved the napplet's request at first load. The specific manifest tag shape and origin format are defined by the individual class-contributing NUB (see that NUB's spec for the manifest-tag surface). NUB-CLASS-2 itself does not define any manifest tags — it defines the posture."

5. **## Shell Responsibilities** (bulleted MUSTs, this section is larger than NUB-CLASS-1's because Class-2 requires consent machinery):
   - Shell MUST prompt the user at first load of any napplet matching `(dTag, aggregateHash)` that is not already in an approved-or-denied state. Grant persistence MUST be keyed on the exact `(dTag, aggregateHash)` composite; shells MUST NOT key on `dTag` alone.
   - Shell MUST emit a runtime CSP header containing `connect-src <approved-origins>` when serving an approved NUB-CLASS-2 napplet.
   - Shell MUST send exactly one `class.assigned` envelope with payload `{ class: 2 }` at iframe-ready time for approved napplets. For denied napplets, the shell MUST send `class.assigned` with `{ class: 1 }` (denied napplets are served under NUB-CLASS-1 posture).
   - Shell MUST refuse-to-serve any NUB-CLASS-2 napplet whose HTML contains `<meta http-equiv="Content-Security-Policy">`. Browser CSP intersection silently reduces header `connect-src <granted>` AND meta `connect-src 'none'` to `'none'`, suppressing the grant. The diagnostic MUST identify the offending meta element and point the napplet author at rebuild guidance.
   - Shell MUST expose a user-facing revocation affordance for approved grants. Revoked grants MUST move to a DENIED state (not deleted) so shells retain historical knowledge and can offer a re-approve path.
   - Shell SHOULD reload the napplet's iframe on revocation — the next load will emit `class.assigned` with `class: 1` and restrictive CSP; in-session mid-lifecycle class re-assignment is out of scope.

6. **## Grant Persistence Semantics**: Dedicated subsection.
   - Grants are keyed on the composite `(dTag, aggregateHash)`. Rebuilding the napplet with any change that alters `aggregateHash` (including changes to the declared origin set from any class-contributing NUB) produces a new key and auto-invalidates the prior grant — the user is prompted fresh.
   - Shells SHOULD show a diff UI on re-prompt when the same `dTag` has been previously approved but the `aggregateHash` has changed: "previously approved origins X; now requesting origins Y." This is a UX floor for informed re-consent.

7. **## Security Considerations**:
   - **Post-grant opacity**: Once origins are approved, the shell has no browser-level hook to inspect or filter subsequent network traffic between the napplet and the approved origins. This is a fundamental tradeoff of direct-network grants. Consent-prompt language MUST reflect this: "this napplet will be able to send and receive any data with the listed servers; the shell cannot see or filter that traffic."
   - **Cleartext origins**: Shells MAY refuse cleartext schemes (`http:`, `ws:`) entirely. When cleartext is permitted, the consent prompt MUST visibly mark cleartext origins so the user can evaluate the confidentiality tradeoff.
   - **Composite-key hygiene**: A shell that keys grants on `dTag` alone would allow a rebuilt napplet (new `aggregateHash`) to inherit a prior approval silently — this is a security bug. The `(dTag, aggregateHash)` composite is load-bearing.
   - **Residual meta-CSP is a Class-2 project-killer**: Unlike NUB-CLASS-1 where residual meta-CSP is harmless, a NUB-CLASS-2 napplet with residual meta-CSP has its granted origins silently suppressed by browser CSP intersection. The refuse-to-serve MUST exists specifically to prevent this silent-failure mode.

8. **## References**: Cite `NUB-CLASS.md` as parent.

CRITICAL constraints:
- Zero mentions of `@napplet/`, `kehto`, `hyprgate`, `packages/(nub|shim|sdk|vite-plugin)`.
- Zero mentions of `NUB-CONNECT` by name. Reference class-contributing NUBs abstractly: "the individual class-contributing NUB", "the NUB whose manifest tags contributed to the class determination".
- Zero mentions of specific origin-format rules (scheme whitelist, Punycode, default-port ban, wildcards) — those are NUB-CONNECT concerns, not NUB-CLASS-2 concerns.
- Zero mentions of specific manifest tag names (e.g., `["connect", "<origin>"]`) — those are NUB-CONNECT concerns.
- Zero mentions of aggregateHash fold procedures / canonical hashing — those are NUB-CONNECT concerns (Plan 03).
- Use RFC 2119 capitalized MUST / MUST NOT / SHOULD / MAY.
- Document length ≈ 150-200 lines (larger than NUB-CLASS-1 because of consent/persistence machinery).
  </action>
  <verify>
    <automated>test -f .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md &amp;&amp; grep -q "^NUB-CLASS-2$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md &amp;&amp; grep -q "class: 2" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md &amp;&amp; grep -q "(dTag, aggregateHash)" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md &amp;&amp; grep -q "connect-src" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` succeeds
    - `grep -c "^NUB-CLASS-2$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` returns `1` (setext heading)
    - `grep -c "^=\{11,\}$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` returns at least `1` (setext underline)
    - `grep -q "class: 2" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` succeeds (approval wire value)
    - `grep -q "class: 1" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` succeeds (denial fallback documented)
    - `grep -q "(dTag, aggregateHash)" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` succeeds (composite-key present)
    - `grep -q "connect-src" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` succeeds (CSP posture references the directive)
    - `grep -qi "revocation" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` succeeds (revocation affordance documented)
    - `grep -qE "(http-equiv|meta CSP|residual meta)" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` succeeds (residual meta-CSP refusal documented)
    - `grep -E "@napplet/|kehto|hyprgate|packages/(nub\|shim\|sdk\|vite-plugin)" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` returns zero matches
    - `! grep -q "NUB-CONNECT" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` (no NUB-CONNECT name — class doc is NUB-agnostic)
    - `! grep -qE "Punycode|xn--|default port|wildcard" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` (no origin-format rules leaking from NUB-CONNECT scope)
  </acceptance_criteria>
  <done>
NUB-CLASS-2.md exists with setext heading + metadata block (parent: NUB-CLASS, class number: 2), Description (mentions approval → class: 2 / denial → class: 1 fallback), CSP Posture (`connect-src <granted-origins>`), Manifest Prerequisites (abstract reference to class-contributing NUBs), Shell Responsibilities (consent prompt + header CSP + `class.assigned` + refuse-to-serve on residual meta-CSP + revocation affordance), Grant Persistence Semantics ((dTag, aggregateHash) composite key + diff UI on re-prompt), Security Considerations (post-grant opacity + cleartext + composite-key hygiene + residual meta-CSP), References. Passes zero-grep hygiene. No origin-format rules (those are NUB-CONNECT's turf).
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, run:

```bash
# All three drafts exist
test -f .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md
test -f .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md
test -f .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md

# Zero-grep hygiene across the three class-track drafts
! grep -rE '@napplet/|kehto|hyprgate|packages/(nub\|shim\|sdk\|vite-plugin)' \
    .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md \
    .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md \
    .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md

# Cross-track consistency: NUB-CLASS-1 and NUB-CLASS-2 both declare a class number
grep -q "Class number" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md
grep -q "Class number" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md

# NUB-CLASS.md documents the sub-track template
grep -q "NUB-CLASS-\$N" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md

# NUB-CLASS-2.md does NOT leak NUB-CONNECT origin-format rules
! grep -qE "Punycode|xn--|default port|wildcard" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md
```
</verification>

<success_criteria>
- Three draft files exist at `.planning/phases/135-cross-repo-spec-work/drafts/`: NUB-CLASS.md, NUB-CLASS-1.md, NUB-CLASS-2.md
- NUB-CLASS.md defines the wire message `class.assigned`, runtime surface `window.napplet.class`, capability `nub:class`, and sub-track authoring template
- NUB-CLASS-1.md defines the strict baseline posture (connect-src 'none', no consent, class: 1)
- NUB-CLASS-2.md defines the user-approved explicit-origin posture (connect-src <granted>, consent on (dTag, aggregateHash), class: 2 on approval / class: 1 on denial)
- Zero-grep hygiene clean across all three files
- Neither sub-track member redefines the track (no wire protocol, no runtime surface definitions in CLASS-1/CLASS-2)
- Neither sub-track member mentions NUB-CONNECT by name
- NUB-CLASS-2 does not leak NUB-CONNECT's origin-format concerns (Punycode, port rules, wildcards, aggregateHash fold)
</success_criteria>

<output>
After completion, create `.planning/phases/135-cross-repo-spec-work/135-01-SUMMARY.md` documenting:
- The three files authored
- Key decisions locked (wire shape, runtime surface, capability, template structure)
- Dependencies for downstream plans (Plan 03 cites NUB-CLASS-2 from this plan; Plan 04 verifies zero-grep over all drafts in this directory)
</output>
