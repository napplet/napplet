---
phase: 135-cross-repo-spec-work
plan: 04
type: execute
wave: 3
depends_on: [135-01, 135-02, 135-03]
files_modified: []
autonomous: true
requirements: [SPEC-05]

must_haves:
  truths:
    - "A single recursive grep across the four cross-repo drafts (NUB-CONNECT.md, NUB-CLASS.md, NUB-CLASS-1.md, NUB-CLASS-2.md) returns zero matches for `@napplet/`, `kehto`, `hyprgate`, or `packages/(nub|shim|sdk|vite-plugin)`"
    - "NUB-CONNECT.md cites NUB-CLASS-2 by file name at least 4 times and NUB-CLASS-1 at least once"
    - "All four drafts use setext heading + status banner + metadata block + RFC 2119 MUSTs per NUB-RESOURCE.md template precedent"
    - "NUB-CLASS-2.md does not leak NUB-CONNECT origin-format rules (Punycode, wildcards, default-port ban, aggregateHash fold)"
    - "NIP-5D.md is NUB-neutral: no NUB names, no class numbers, no concrete CSP directives, no perm:strict-csp / nub:class / nub:connect capability examples"
    - "All cross-repo draft + in-repo NIP-5D changes commit cleanly to git; final working tree is clean"
  artifacts:
    - path: ".planning/phases/135-cross-repo-spec-work/drafts/"
      provides: "Directory containing four zero-grep-clean cross-repo draft specs"
      contains: "NUB-CONNECT.md"
    - path: "specs/NIP-5D.md"
      provides: "NUB-neutral transport-only NIP-5D"
      contains: "Class-posture delegation"
  key_links:
    - from: "git commit (log entry)"
      to: "All phase deliverables"
      via: "Single commit staging the four drafts + the NIP-5D amendment"
      pattern: "phase 135.*cross-repo spec"
---

<objective>
Terminal verification and commit for Phase 135. Runs the zero-grep hygiene audit across the four cross-repo drafts (SPEC-05), verifies cross-document citation integrity (NUB-CONNECT → NUB-CLASS-2 + NUB-CLASS-1 by file name), verifies the NIP-5D amendment is NUB-neutral, and commits all Phase 135 deliverables to git in a single atomic commit.

Purpose: SPEC-05 is the zero-grep hygiene requirement for the entire phase's output (all four drafts at `.planning/phases/135-cross-repo-spec-work/drafts/`). This plan is intentionally a pure verification + commit plan — no new authoring. It depends on Plans 01, 02, and 03 having landed all their files.

Output: One git commit with the four cross-repo drafts + the NIP-5D amendment + the four SUMMARY.md files produced by Plans 01-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/135-cross-repo-spec-work/135-CONTEXT.md
@.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md
@.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md
@.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md
@.planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md
@specs/NIP-5D.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Zero-grep hygiene audit across all four cross-repo drafts</name>
  <files></files>
  <read_first>
    - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md
    - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md
    - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md
    - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md
    - .planning/REQUIREMENTS.md (SPEC-05 requirement text)
    - .planning/phases/135-cross-repo-spec-work/135-CONTEXT.md (zero-grep pattern confirmation)
  </read_first>
  <action>
Run the zero-grep hygiene audit across the four cross-repo draft files. This is a verification-only task — no files are modified.

Execute (output expected: zero matches):

```bash
grep -r -E '@napplet/|kehto|hyprgate|packages/(nub\|shim\|sdk\|vite-plugin)' \
    .planning/phases/135-cross-repo-spec-work/drafts/
```

Also verify cross-document citation integrity:

```bash
# NUB-CONNECT cites NUB-CLASS-2 at least 4 times
test $(grep -c "NUB-CLASS-2" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md) -ge 4

# NUB-CONNECT cites NUB-CLASS-1 at least once (default-posture fallback)
grep -q "NUB-CLASS-1" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md

# NUB-CONNECT does NOT inline-redefine class postures
! grep -qE "^Class [12] is " .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md
! grep -qE "^###+ Class [12]\b" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md

# NUB-CLASS does NOT mention NUB-CONNECT (stays NUB-agnostic)
! grep -q "NUB-CONNECT" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md

# NUB-CLASS-1 does NOT mention NUB-CONNECT
! grep -q "NUB-CONNECT" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md

# NUB-CLASS-2 does NOT mention NUB-CONNECT and does NOT leak origin-format rules
! grep -q "NUB-CONNECT" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md
! grep -qE "Punycode|xn--|default port|wildcard" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md

# All four drafts have setext headings (not ATX # headings) for the document title
grep -q "^=\{9,\}$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md
grep -q "^=\{11,\}$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md
grep -q "^=\{11,\}$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md
grep -q "^=\{11,\}$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md

# All four drafts carry the `draft` status banner
grep -q "^\`draft\`$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md
grep -q "^\`draft\`$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md
grep -q "^\`draft\`$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md
grep -q "^\`draft\`$" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md

# NUB-CONNECT.md conformance fixture contains a real 64-char lowercase hex digest (no placeholder)
! grep -q "COMPUTE THIS VALUE" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md
grep -qE "[a-f0-9]{64}" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md
```

If ANY of these checks fails, the task fails. Do NOT auto-fix — fix via re-running the offending plan (01, 02, or 03) in revision mode. Report which specific check failed and which draft is the culprit so the executor or a subsequent revision plan can address it targetedly.
  </action>
  <verify>
    <automated>! grep -r -E '@napplet/|kehto|hyprgate|packages/(nub\|shim\|sdk\|vite-plugin)' .planning/phases/135-cross-repo-spec-work/drafts/ &amp;&amp; test $(grep -c "NUB-CLASS-2" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md) -ge 4 &amp;&amp; ! grep -q "NUB-CONNECT" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md &amp;&amp; ! grep -q "NUB-CONNECT" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md &amp;&amp; ! grep -q "NUB-CONNECT" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md &amp;&amp; ! grep -q "COMPUTE THIS VALUE" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r -E '@napplet/|kehto|hyprgate|packages/(nub\|shim\|sdk\|vite-plugin)' .planning/phases/135-cross-repo-spec-work/drafts/` returns zero matches (SPEC-05 pass)
    - `test $(grep -c "NUB-CLASS-2" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md) -ge 4` succeeds
    - `grep -q "NUB-CLASS-1" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md` succeeds
    - `! grep -q "NUB-CONNECT" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` succeeds (NUB-CLASS stays NUB-agnostic)
    - `! grep -q "NUB-CONNECT" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md` succeeds
    - `! grep -q "NUB-CONNECT" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` succeeds
    - `! grep -qE "Punycode|xn--|default port|wildcard" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` succeeds (origin-format rules stay in NUB-CONNECT)
    - `! grep -q "COMPUTE THIS VALUE" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md` succeeds (conformance fixture is concrete, not a placeholder)
    - `grep -qE "[a-f0-9]{64}" .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md` succeeds (real SHA-256 hex present)
    - All four drafts have setext heading underlines (`===...` lines of sufficient length)
    - All four drafts carry the backtick-quoted `draft` status banner on its own line
  </acceptance_criteria>
  <done>
Zero-grep hygiene audit passes across all four cross-repo draft files. Cross-document citations are consistent (NUB-CONNECT cites NUB-CLASS-2 ≥4 times + NUB-CLASS-1 ≥1 time; class docs remain NUB-CONNECT-free). Structural conformance (setext headings, draft banners) verified. Conformance fixture in NUB-CONNECT.md contains a real hex digest. No offending strings identified; ready to proceed to NIP-5D audit (Task 2).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: NIP-5D.md NUB-neutrality audit</name>
  <files></files>
  <read_first>
    - specs/NIP-5D.md (after Plan 02's edits)
    - .planning/REQUIREMENTS.md (NIP5D-01, NIP5D-02)
    - .planning/phases/135-cross-repo-spec-work/135-CONTEXT.md (decisions: NIP-5D NUB-neutral, single generic delegation paragraph)
  </read_first>
  <action>
Run a targeted audit on `specs/NIP-5D.md` to confirm the NUB-neutral posture after Plan 02's edits. This is a verification-only task.

Execute:

```bash
# Offending v0.28.0 prose is gone
! grep -q "Browser-Enforced Resource Isolation" specs/NIP-5D.md
! grep -q "perm:strict-csp" specs/NIP-5D.md
! grep -q "Strict Content Security Policy" specs/NIP-5D.md

# NUB-neutral: zero NUB names
! grep -qE "NUB-(CLASS|CONNECT|RESOURCE|IDENTITY|MEDIA|NOTIFY|RELAY|KEYS|CONFIG|IFC|STORAGE|THEME)" specs/NIP-5D.md

# Zero concrete class numbers
! grep -qE "Class [12]|class: [12]|NUB-CLASS-[0-9]" specs/NIP-5D.md

# Zero concrete CSP directives
! grep -qE "connect-src|script-src|default-src|img-src|style-src|object-src|base-uri|form-action" specs/NIP-5D.md

# Zero concrete NUB-flavored capability examples
! grep -q "nub:connect" specs/NIP-5D.md
! grep -q "nub:class" specs/NIP-5D.md
! grep -q "perm:strict-csp" specs/NIP-5D.md

# New delegation paragraph present
grep -q "Class-posture delegation" specs/NIP-5D.md
grep -q "NUBs MAY define napplet classes" specs/NIP-5D.md
grep -q "out of scope for this NIP" specs/NIP-5D.md

# Load-bearing sandbox prose preserved
grep -q "allow-scripts" specs/NIP-5D.md
grep -q "allow-same-origin" specs/NIP-5D.md
grep -q "MessageEvent.source" specs/NIP-5D.md

# Section structure intact
grep -q "^## Security Considerations" specs/NIP-5D.md
grep -q "^## References" specs/NIP-5D.md
grep -q "Non-Guarantees" specs/NIP-5D.md
```

If ANY check fails, report which specific check failed. Do NOT auto-fix — revise Plan 02 as needed.
  </action>
  <verify>
    <automated>! grep -q "Browser-Enforced Resource Isolation" specs/NIP-5D.md &amp;&amp; ! grep -qE "NUB-(CLASS|CONNECT|RESOURCE)" specs/NIP-5D.md &amp;&amp; ! grep -qE "connect-src|script-src|default-src" specs/NIP-5D.md &amp;&amp; grep -q "Class-posture delegation" specs/NIP-5D.md &amp;&amp; grep -q "allow-scripts" specs/NIP-5D.md &amp;&amp; grep -q "allow-same-origin" specs/NIP-5D.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "Browser-Enforced Resource Isolation" specs/NIP-5D.md` succeeds
    - `! grep -q "perm:strict-csp" specs/NIP-5D.md` succeeds
    - `! grep -qE "NUB-(CLASS|CONNECT|RESOURCE|IDENTITY|MEDIA|NOTIFY|RELAY|KEYS|CONFIG|IFC|STORAGE|THEME)" specs/NIP-5D.md` succeeds (no NUB names)
    - `! grep -qE "Class [12]|class: [12]|NUB-CLASS-[0-9]" specs/NIP-5D.md` succeeds (no class numbers)
    - `! grep -qE "connect-src|script-src|default-src" specs/NIP-5D.md` succeeds (no concrete CSP directives)
    - `! grep -q "nub:connect" specs/NIP-5D.md` succeeds
    - `! grep -q "nub:class" specs/NIP-5D.md` succeeds
    - `grep -q "Class-posture delegation" specs/NIP-5D.md` succeeds (generic paragraph present)
    - `grep -q "NUBs MAY define napplet classes" specs/NIP-5D.md` succeeds
    - `grep -q "out of scope for this NIP" specs/NIP-5D.md` succeeds
    - `grep -q "allow-scripts" specs/NIP-5D.md` succeeds (sandbox MUST preserved)
    - `grep -q "allow-same-origin" specs/NIP-5D.md` succeeds (allow-same-origin MUST NOT preserved)
    - `grep -q "MessageEvent.source" specs/NIP-5D.md` succeeds (identity binding preserved)
    - `grep -q "^## Security Considerations" specs/NIP-5D.md` succeeds
    - `grep -q "^## References" specs/NIP-5D.md` succeeds
  </acceptance_criteria>
  <done>
NIP-5D.md passes the NUB-neutrality audit: zero NUB names, zero class numbers, zero concrete CSP directives, zero NUB-flavored capability examples; Class-posture delegation paragraph present; load-bearing sandbox + identity prose preserved. Ready to proceed to commit (Task 3).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Commit phase deliverables in single atomic commit</name>
  <files></files>
  <read_first>
    - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md
    - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md
    - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md
    - .planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md
    - specs/NIP-5D.md
    - .planning/phases/135-cross-repo-spec-work/135-CONTEXT.md (reference for commit message context)
  </read_first>
  <action>
Stage and commit all Phase 135 deliverables in a single atomic commit.

Files to stage (explicit paths only — do NOT use `git add -A` or `git add .`):

- `.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS.md` (new)
- `.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-1.md` (new)
- `.planning/phases/135-cross-repo-spec-work/drafts/NUB-CLASS-2.md` (new)
- `.planning/phases/135-cross-repo-spec-work/drafts/NUB-CONNECT.md` (new)
- `specs/NIP-5D.md` (modified)
- `.planning/phases/135-cross-repo-spec-work/135-01-SUMMARY.md` (if present from Plan 01)
- `.planning/phases/135-cross-repo-spec-work/135-02-SUMMARY.md` (if present from Plan 02)
- `.planning/phases/135-cross-repo-spec-work/135-03-SUMMARY.md` (if present from Plan 03)
- `.planning/phases/135-cross-repo-spec-work/135-04-SUMMARY.md` (this plan's summary — create as part of this task)
- `.planning/phases/135-cross-repo-spec-work/135-01-PLAN.md`
- `.planning/phases/135-cross-repo-spec-work/135-02-PLAN.md`
- `.planning/phases/135-cross-repo-spec-work/135-03-PLAN.md`
- `.planning/phases/135-cross-repo-spec-work/135-04-PLAN.md`
- `.planning/STATE.md` (if modified with phase progress updates)

Before committing, create `.planning/phases/135-cross-repo-spec-work/135-04-SUMMARY.md` documenting:
- The audit runs performed (zero-grep across drafts, NIP-5D NUB-neutrality)
- All acceptance criteria results (pass/fail for each)
- Zero-grep pass confirmation for SPEC-05
- Full list of files committed

Run the commit using the gsd-tools CLI (preferred) or direct git commands. Use a HEREDOC commit message:

```bash
git commit -m "$(cat <<'EOF'
docs(135): cross-repo spec work — NUB-CLASS track, NUB-CONNECT, NIP-5D neutrality

Four cross-repo draft specs at .planning/phases/135-cross-repo-spec-work/drafts/:
- NUB-CLASS.md establishes the class-track machinery (wire class.assigned,
  runtime window.napplet.class, capability nub:class) plus an internal
  NUB-CLASS-$N authoring template and citation conventions.
- NUB-CLASS-1.md is the strict baseline posture (connect-src 'none', no
  consent prompt, class: 1 wire value).
- NUB-CLASS-2.md is the user-approved explicit-origin posture
  (connect-src <granted>, consent on (dTag, aggregateHash), class: 2 on
  approval / class: 1 on denial, revocation affordance, residual meta-CSP
  refuse-to-serve).
- NUB-CONNECT.md defines manifest tag shape ["connect", "<origin>"], origin
  format (scheme whitelist, Punycode IDN, default-port ban, no wildcards),
  canonical connect:origins aggregateHash fold with conformance fixture,
  runtime NappletConnect API, capability advertisement (nub:connect +
  connect:scheme:http + connect:scheme:ws), graceful-degradation ladder.
  Cites NUB-CLASS-2.md by file name; does not redefine class postures.

specs/NIP-5D.md returns to NUB-neutral transport-only purity: the
v0.28.0 "Browser-Enforced Resource Isolation" subsection is removed;
a single generic "Class-posture delegation" paragraph replaces it;
zero NUB names, zero class numbers, zero concrete CSP directives remain.

Zero-grep hygiene clean across all four drafts (no @napplet/, no kehto,
no hyprgate, no packages/(nub|shim|sdk|vite-plugin)).

Requirements: SPEC-01, SPEC-02, SPEC-03, SPEC-04, SPEC-05, SPEC-06,
SPEC-07, SPEC-08, NIP5D-01, NIP5D-02.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

After commit, run `git status` to verify the working tree is clean (or only contains untracked files that were intentionally not staged, e.g., drafts for future phases).
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q "135" &amp;&amp; git log -1 --format='%s' | grep -qi "cross-repo" &amp;&amp; git status --porcelain | grep -v "^??" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%s'` output includes `135` and the phrase `cross-repo`
    - `git log -1 --name-only --format=''` lists at minimum: `NUB-CLASS.md`, `NUB-CLASS-1.md`, `NUB-CLASS-2.md`, `NUB-CONNECT.md`, `specs/NIP-5D.md`, plus the four `135-NN-PLAN.md` files
    - `git status --porcelain | grep -v "^??" | wc -l` returns `0` (no unstaged modifications or partial stages — a clean working tree for tracked files)
    - `.planning/phases/135-cross-repo-spec-work/135-04-SUMMARY.md` exists and is committed
    - The commit is a SINGLE commit (not split across multiple) — `git log --oneline -5` shows the phase-135 commit at HEAD with no Phase 135 splinters behind it
    - Commit message references all 10 phase REQ-IDs: SPEC-01..08 and NIP5D-01, NIP5D-02 (check with `git log -1 --format='%B' | grep -cE 'SPEC-0[1-8]|NIP5D-0[12]'` returning at least 10)
  </acceptance_criteria>
  <done>
All Phase 135 deliverables are committed in a single atomic commit. Commit message references the 10 phase REQ-IDs. Working tree is clean for tracked files. Phase 135 is complete and ready for downstream phases (136-142) to proceed.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, run:

```bash
# Zero-grep across all drafts
! grep -r -E '@napplet/|kehto|hyprgate|packages/(nub\|shim\|sdk\|vite-plugin)' \
    .planning/phases/135-cross-repo-spec-work/drafts/

# All four drafts exist
for f in NUB-CLASS.md NUB-CLASS-1.md NUB-CLASS-2.md NUB-CONNECT.md; do
    test -f ".planning/phases/135-cross-repo-spec-work/drafts/$f" || exit 1
done

# NIP-5D is NUB-neutral
! grep -qE "NUB-(CLASS|CONNECT|RESOURCE)" specs/NIP-5D.md
! grep -qE "connect-src|script-src|default-src" specs/NIP-5D.md
grep -q "Class-posture delegation" specs/NIP-5D.md

# Git is clean
test -z "$(git status --porcelain | grep -v '^??')"

# HEAD commit is the phase commit
git log -1 --format='%s' | grep -q "135"
```
</verification>

<success_criteria>
- SPEC-05 zero-grep audit passes across all four cross-repo drafts
- NUB-CONNECT.md cites NUB-CLASS-2.md by file name at least 4 times and NUB-CLASS-1.md at least once
- NUB-CLASS-*.md documents do not mention NUB-CONNECT by name (NUB-agnostic class track)
- NUB-CLASS-2.md does not leak NUB-CONNECT origin-format rules
- `specs/NIP-5D.md` is NUB-neutral (no NUB names, no class numbers, no concrete CSP directives, no NUB-flavored capability examples)
- All Phase 135 deliverables are committed in a single atomic commit referencing all 10 phase REQ-IDs
- Git working tree is clean for tracked files
</success_criteria>

<output>
The Plan 04 summary (`135-04-SUMMARY.md`) is created by Task 3 and includes the audit results + the file list + the commit SHA. The phase is complete after this plan.
</output>
