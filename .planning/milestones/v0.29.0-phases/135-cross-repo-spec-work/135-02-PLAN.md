---
phase: 135-cross-repo-spec-work
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/NIP-5D.md
autonomous: true
requirements: [NIP5D-01, NIP5D-02]

must_haves:
  truths:
    - "NIP-5D is NUB-neutral: the file mentions no specific NUB by name, no concrete class numbers, no concrete CSP directives inline"
    - "The v0.28.0 `Browser-Enforced Resource Isolation` subsection (lines 115-130) is gone — no `perm:strict-csp` example, no baseline meta-CSP template, no NUB-RESOURCE reference"
    - "A single generic Security Considerations paragraph delegates class taxonomy to the NUBs track without naming any NUB or class"
    - "The capability-query table (if present) uses only abstract `nub:/perm:/svc:` prefix examples, not NUB-flavored examples like `perm:strict-csp`"
    - "The transport, identity, manifest, and NUB-negotiation sections of NIP-5D remain semantically intact (sandbox reaffirmation, MessageEvent.source identity, capability query mechanism)"
  artifacts:
    - path: "specs/NIP-5D.md"
      provides: "NUB-neutral transport-only NIP-5D specification"
      contains: "NUBs MAY define napplet classes"
  key_links:
    - from: "specs/NIP-5D.md"
      to: "The NUBs track (abstract reference)"
      via: "Security Considerations generic paragraph"
      pattern: "out of scope for this NIP"
---

<objective>
Amend the in-repo `specs/NIP-5D.md` to remove all NUB-flavored prose introduced in v0.28.0 and restore the file to NUB-neutral transport-only purity. The "Browser-Enforced Resource Isolation" subsection (lines 115-130) is removed entirely; the `perm:strict-csp` capability-advertisement example is removed; one generic paragraph is added to Security Considerations that delegates class taxonomy to the NUBs track without naming any NUB or any class.

Purpose: NIP-5D is the transport + identity + manifest + NUB-negotiation envelope spec. Introducing NUB-specific prose (NUB-RESOURCE, `perm:strict-csp`, baseline CSP directives) in v0.28.0 was a layering violation that v0.29.0 unwinds. Class taxonomy lives in the NUBs track (NUB-CLASS and its sub-track members — authored in Plan 01). NIP-5D points at this abstractly without naming any specific NUB.

Output: A modified `specs/NIP-5D.md` with the offending subsection removed and a generic one-paragraph replacement added. This plan is independent of Plan 01 (NUB-CLASS track) — the generic paragraph does NOT reference NUB-CLASS by name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/135-cross-repo-spec-work/135-CONTEXT.md
@specs/NIP-5D.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove Browser-Enforced Resource Isolation subsection from NIP-5D.md</name>
  <files>specs/NIP-5D.md</files>
  <read_first>
    - specs/NIP-5D.md (full file — specifically lines 115-130, which contain the `### Browser-Enforced Resource Isolation` subsection and four paragraphs covering "Strict Content Security Policy", "Example minimal meta delivery", "Canonical fetch path", and "Sandbox reaffirmation")
    - .planning/phases/135-cross-repo-spec-work/135-CONTEXT.md (decisions section — NIP-5D amendment: remove entirely, add one generic paragraph)
    - .planning/REQUIREMENTS.md (NIP5D-01 requirement text)
  </read_first>
  <action>
Edit `specs/NIP-5D.md` to delete the entire `### Browser-Enforced Resource Isolation` subsection and its contents (starting at the `### Browser-Enforced Resource Isolation` heading and ending immediately before the next `##`-level heading or `**Non-Guarantees:**` paragraph).

Specifically, locate and remove:

1. The `### Browser-Enforced Resource Isolation` heading
2. The paragraph starting "The mitigations above describe ambient trust..."
3. The paragraph starting "**Strict Content Security Policy.**" through the end of its `perm:strict-csp` advertisement prose
4. The indented 4-space code block example containing `default-src 'none'`, `script-src 'nonce-...' 'self'`, `connect-src 'none'`, `img-src blob: data:`, etc.
5. The paragraph starting "**Canonical fetch path.**" through its NUB-RESOURCE specification reference
6. The paragraph starting "**Sandbox reaffirmation.**" — BUT: this paragraph contains load-bearing prose about `sandbox="allow-scripts"` MUST and `allow-same-origin` MUST NOT; PRESERVE the semantic content by MERGING it into Mitigation #1 above (rewrite Mitigation #1 from the Security Considerations section to capture the reaffirmation AND the rationale about opaque origins, service workers, and localStorage)
7. The final paragraph starting "Strict CSP, the resource NUB as canonical fetch path, and `sandbox=&quot;allow-scripts&quot;`..." — delete entirely (this paragraph only makes sense in the context of the removed subsection)

After the removal, the `## Security Considerations` section MUST transition directly from Mitigation #6 + the short "Storage isolation..." line into the `**Non-Guarantees:**` paragraph (which was previously line 132). Task 2 will insert the new generic paragraph in between.

Do NOT touch any other part of NIP-5D. Do NOT modify the transport section, identity section, manifest section, or NUB-negotiation section. Do NOT modify the References section.

IMPORTANT: If the Mitigation #1 rewrite is needed (to preserve the `allow-same-origin` rationale from the deleted "Sandbox reaffirmation" paragraph), keep it minimal: expand Mitigation #1 from `Iframe sandbox: allow-scripts is the only required token -- shells MUST NOT add allow-same-origin.` to include one additional sentence: "Adding `allow-same-origin` would grant the napplet a real origin, allowing it to register a service worker, read shell `localStorage`, and bypass shell mediation entirely — this prohibition is the load-bearing precondition for browser-enforced isolation of any kind."

Do NOT add the new generic paragraph in this task — Task 2 handles that.
  </action>
  <verify>
    <automated>! grep -q "Browser-Enforced Resource Isolation" specs/NIP-5D.md &amp;&amp; ! grep -q "perm:strict-csp" specs/NIP-5D.md &amp;&amp; ! grep -q "NUB-RESOURCE" specs/NIP-5D.md &amp;&amp; grep -q "allow-scripts" specs/NIP-5D.md &amp;&amp; grep -q "allow-same-origin" specs/NIP-5D.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "Browser-Enforced Resource Isolation" specs/NIP-5D.md` (subsection heading gone)
    - `! grep -q "perm:strict-csp" specs/NIP-5D.md` (no NUB-flavored capability example)
    - `! grep -q "NUB-RESOURCE" specs/NIP-5D.md` (no NUB name)
    - `! grep -q "Strict Content Security Policy" specs/NIP-5D.md` (v0.28.0 section prose gone)
    - `! grep -q "nonce-" specs/NIP-5D.md` (example meta-CSP block gone)
    - `! grep -qi "canonical fetch path" specs/NIP-5D.md` (v0.28.0 NUB-RESOURCE pointer gone)
    - `grep -q "allow-scripts" specs/NIP-5D.md` (sandbox MUST preserved in Security Considerations)
    - `grep -q "allow-same-origin" specs/NIP-5D.md` (allow-same-origin MUST NOT preserved)
    - `grep -q "MessageEvent.source" specs/NIP-5D.md` (identity-binding mitigation intact)
    - `grep -q "Non-Guarantees" specs/NIP-5D.md` (closing paragraph of Security Considerations intact)
    - `grep -q "^## Security Considerations" specs/NIP-5D.md` (Security Considerations heading still present)
    - `grep -q "^## References" specs/NIP-5D.md` (References heading still present)
  </acceptance_criteria>
  <done>
`specs/NIP-5D.md` no longer contains the `### Browser-Enforced Resource Isolation` subsection, the Strict Content Security Policy prose, the example meta-CSP block, the Canonical fetch path NUB-RESOURCE pointer, the Sandbox reaffirmation paragraph, or the closing synthesis paragraph. The file still contains the sandbox MUST and allow-same-origin MUST NOT prose (moved into Mitigation #1). The `## Security Considerations` heading, the six mitigation bullets, and the `**Non-Guarantees:**` paragraph remain structurally intact.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add generic NUB-neutral class-delegation paragraph to NIP-5D.md Security Considerations</name>
  <files>specs/NIP-5D.md</files>
  <read_first>
    - specs/NIP-5D.md (the file after Task 1's edit — specifically the position between the "Storage isolation, relay access control..." paragraph and the `**Non-Guarantees:**` paragraph where the new paragraph will be inserted)
    - .planning/phases/135-cross-repo-spec-work/135-CONTEXT.md (decisions section: exact paragraph language requirement — "NUBs MAY define napplet classes with different security postures delivered through shell-controlled HTTP response headers; class taxonomy and delivery mechanics are out of scope for this NIP")
    - .planning/REQUIREMENTS.md (NIP5D-02 requirement text — remove `perm:strict-csp` capability-advertisement example; keep generic `shell.supports(...)` table if present)
  </read_first>
  <action>
Insert a single new paragraph into `specs/NIP-5D.md`'s `## Security Considerations` section, positioned AFTER the "Storage isolation, relay access control, and ACL enforcement are defined by their respective NUB specs." line and BEFORE the `**Non-Guarantees:**` paragraph.

The new paragraph MUST use this exact wording, preserving emphasis on the bold lead:

```
**Class-posture delegation.** NUBs MAY define napplet classes with different security postures delivered through shell-controlled HTTP response headers. Class taxonomy, the mechanism for assigning a class to a napplet, and the wire or header shapes used to express a class are out of scope for this NIP. NUB specs that define class-contributing capabilities document their own posture and their own shell responsibilities; NIP-5D provides only the transport, identity, manifest-negotiation, and capability-query primitives on which such NUB-level machinery can layer.
```

Constraints on the paragraph:
- MUST NOT name any NUB (no `NUB-CLASS`, no `NUB-CONNECT`, no `NUB-RESOURCE`)
- MUST NOT name any concrete class (no `Class 1`, `Class 2`, `class: 1`, `class: 2`, `NUB-CLASS-1`, `NUB-CLASS-2`)
- MUST NOT include any concrete CSP directive (no `connect-src`, `script-src`, `default-src` mentions)
- MUST NOT reference any specific capability name (no `perm:strict-csp`, no `nub:connect`, no `nub:class`)
- MUST reference the NUBs track abstractly if at all — the wording above already satisfies this by saying "NUB specs that define class-contributing capabilities"

Additionally, audit any other part of NIP-5D that mentions the capability-query mechanism and verify no `perm:strict-csp` example leaks. If the file currently has a `shell.supports(...)` example or table elsewhere that uses `perm:strict-csp` as an illustrative example, replace that example with a generic prefix illustration using the form `nub:<domain>`, `perm:<capability>`, `svc:<service>` without any specific NUB name. Do NOT otherwise alter capability-query documentation.
  </action>
  <verify>
    <automated>grep -q "Class-posture delegation" specs/NIP-5D.md &amp;&amp; grep -q "NUBs MAY define napplet classes" specs/NIP-5D.md &amp;&amp; grep -q "out of scope for this NIP" specs/NIP-5D.md &amp;&amp; ! grep -qE "NUB-(CLASS|CONNECT|RESOURCE)" specs/NIP-5D.md &amp;&amp; ! grep -qE "Class [12]|class: [12]" specs/NIP-5D.md &amp;&amp; ! grep -q "connect-src" specs/NIP-5D.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Class-posture delegation" specs/NIP-5D.md` (new paragraph bold lead present)
    - `grep -q "NUBs MAY define napplet classes" specs/NIP-5D.md` (generic delegation language present)
    - `grep -q "out of scope for this NIP" specs/NIP-5D.md` (explicit scope boundary)
    - `! grep -qE "NUB-(CLASS|CONNECT|RESOURCE)" specs/NIP-5D.md` (zero NUB names)
    - `! grep -qE "Class [12]|class: [12]" specs/NIP-5D.md` (zero concrete class numbers)
    - `! grep -qE "NUB-CLASS-[0-9]" specs/NIP-5D.md` (zero class-document references)
    - `! grep -q "connect-src" specs/NIP-5D.md` (zero concrete CSP directives)
    - `! grep -q "script-src" specs/NIP-5D.md` (zero concrete CSP directives)
    - `! grep -q "default-src" specs/NIP-5D.md` (zero concrete CSP directives)
    - `! grep -q "perm:strict-csp" specs/NIP-5D.md` (zero deprecated-capability examples)
    - `! grep -q "nub:connect" specs/NIP-5D.md` (no NUB-CONNECT capability surface reference)
    - `! grep -q "nub:class" specs/NIP-5D.md` (no NUB-CLASS capability surface reference)
    - `grep -q "Non-Guarantees" specs/NIP-5D.md` (closing paragraph of Security Considerations preserved in order)
  </acceptance_criteria>
  <done>
`specs/NIP-5D.md` now contains a single `**Class-posture delegation.**` paragraph in the `## Security Considerations` section, positioned after the "Storage isolation..." line and before `**Non-Guarantees:**`. The paragraph uses only abstract language (no NUB names, no class numbers, no CSP directives, no capability names). The file is NUB-neutral: zero occurrences of `NUB-CLASS`, `NUB-CONNECT`, `NUB-RESOURCE`, `perm:strict-csp`, `nub:connect`, `nub:class`, `connect-src`, `script-src`, `default-src`, `Class 1`, `Class 2`, `class: 1`, `class: 2`.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run:

```bash
# Offending v0.28.0 prose is gone
! grep -q "Browser-Enforced Resource Isolation" specs/NIP-5D.md
! grep -q "perm:strict-csp" specs/NIP-5D.md
! grep -q "NUB-RESOURCE" specs/NIP-5D.md
! grep -q "Strict Content Security Policy" specs/NIP-5D.md

# New NUB-neutral paragraph is present
grep -q "Class-posture delegation" specs/NIP-5D.md
grep -q "out of scope for this NIP" specs/NIP-5D.md

# NIP-5D is NUB-neutral: zero specific NUB or class mentions
! grep -qE "NUB-(CLASS|CONNECT|RESOURCE)" specs/NIP-5D.md
! grep -qE "Class [12]|class: [12]|NUB-CLASS-[0-9]" specs/NIP-5D.md
! grep -qE "connect-src|script-src|default-src" specs/NIP-5D.md

# Load-bearing sandbox prose preserved
grep -q "allow-scripts" specs/NIP-5D.md
grep -q "allow-same-origin" specs/NIP-5D.md
grep -q "MessageEvent.source" specs/NIP-5D.md

# Structural intactness
grep -q "^## Security Considerations" specs/NIP-5D.md
grep -q "^## References" specs/NIP-5D.md
grep -q "Non-Guarantees" specs/NIP-5D.md
```
</verification>

<success_criteria>
- `specs/NIP-5D.md` contains no NUB-flavored prose (no NUB-RESOURCE, NUB-CLASS, NUB-CONNECT names; no Class 1/Class 2; no concrete CSP directives; no perm:strict-csp or nub:connect or nub:class capability examples)
- `specs/NIP-5D.md` contains one new generic paragraph delegating class taxonomy to the NUBs track with the exact bold lead `**Class-posture delegation.**`
- The `## Security Considerations` section's six mitigation bullets + "Storage isolation..." line + `**Non-Guarantees:**` closing paragraph all remain intact and in order, with the new paragraph inserted between "Storage isolation..." and `**Non-Guarantees:**`
- The `allow-scripts` / `allow-same-origin` prose that was in the deleted "Sandbox reaffirmation" paragraph is preserved by expanding Mitigation #1
- No other section of NIP-5D (transport, identity, manifest, NUB-negotiation, References) is touched
</success_criteria>

<output>
After completion, create `.planning/phases/135-cross-repo-spec-work/135-02-SUMMARY.md` documenting:
- Lines removed from NIP-5D.md (byte-accurate summary of the deleted subsection)
- The exact new paragraph inserted, verbatim
- Confirmation that Mitigation #1 was expanded (if the rewrite was applied) to preserve the `allow-same-origin` rationale
- Confirmation of zero NUB names, zero class numbers, zero concrete CSP directives in the updated file
</output>
