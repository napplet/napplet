---
phase: 136-empirical-csp-injection-block-verification
plan: 02
type: execute
wave: 2
depends_on:
  - "136-01"
files_modified:
  - .planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md
  - /tmp/napplet-136-phase-notes-grep.log
autonomous: true
requirements:
  - DETECT-02
  - DETECT-03
  - DETECT-04

must_haves:
  truths:
    - "136-PHASE-NOTES.md exists in the phase directory and is the consumable artifact Phase 137's amendment author reads when drafting NUB-IDENTITY Security Considerations + NUB-CLASS-1 MUST/SHOULD rows"
    - "136-PHASE-NOTES.md documents the 4-field violation-report shape observed in Plan 01 (violatedDirective, blockedURI, documentURI, sourceFile) verbatim — with the exact JSON snapshot captured from /tmp/napplet-136-report-shape.log"
    - "136-PHASE-NOTES.md names the DETECT-02 shell-side correlation mechanism: shell MUST correlate received violation reports to napplet identity via (dTag, aggregateHash) through the napplet HTML URL path"
    - "136-PHASE-NOTES.md names the DETECT-04 world:'MAIN' residual HONESTLY — extensions using chrome.scripting.executeScript({world:'MAIN'}) bypass page CSP entirely, no securitypolicyviolation fires, structural mitigation is NUB-CLASS-1 connect-src 'none' trapping plaintext inside the frame"
    - "136-PHASE-NOTES.md enumerates the DETECT-03 shell policy latitude: shell MAY refuse-to-serve future loads of offenders, MAY reject subsequent identity.decrypt envelopes, MAY surface to the user — spec defines the mechanism, shell owns the UX response"
    - "136-PHASE-NOTES.md does NOT author spec amendment text (Phase 137 territory) — it records observed behavior + documentation gates Phase 137 cites; the phase boundary is honored"
    - "The grep-verification log at /tmp/napplet-136-phase-notes-grep.log confirms all required literal strings are present in 136-PHASE-NOTES.md"
  artifacts:
    - path: ".planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md"
      provides: "Observed-behavior + shell-latitude + world:'MAIN'-residual documentation Phase 137 reads to author the DETECT-01..04 amendment language"
      min_lines: 80
      contains: "world: 'MAIN'"
    - path: "/tmp/napplet-136-phase-notes-grep.log"
      provides: "Grep evidence: all 7 required literal strings present in 136-PHASE-NOTES.md, GREP_EXIT=0 marker"
      contains: "GREP_EXIT=0"
  key_links:
    - from: ".planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md"
      to: "/tmp/napplet-136-injection-block.log"
      via: "verbatim citation of the Plan 01 evidence log path + JSON summary contents"
      pattern: "/tmp/napplet-136-injection-block.log"
    - from: ".planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md"
      to: "/tmp/napplet-136-report-shape.log"
      via: "verbatim citation of the Plan 01 report-shape log path + the 4-field reportShape object"
      pattern: "/tmp/napplet-136-report-shape.log"
    - from: "/tmp/napplet-136-phase-notes-grep.log"
      to: ".planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md"
      via: "grep -q on 7 required literal strings; GREP_EXIT=0 stamped on success"
      pattern: "GREP_EXIT=0"
---

<objective>
Synthesize Plan 01's empirical evidence into a single phase-deliverable `136-PHASE-NOTES.md` that Phase 137 reads when authoring the NUB-IDENTITY + NUB-CLASS-1 amendment. The notes file records (a) the observed CSP-block mechanism and violation-report shape; (b) the DETECT-02 shell-side correlation requirement; (c) the DETECT-03 shell policy latitude (MAY statements); (d) the DETECT-04 `world: 'MAIN'` residual documented HONESTLY — no page-side block exists, structural mitigation is `connect-src 'none'`. Grep-verify all required literal strings are present so Phase 137's amendment author can cite by line.

Purpose: Move Plan 01's `/tmp` evidence into the repo (in the phase directory, NOT package source) as the durable consumable for Phase 137. This is a DOCUMENTATION plan, not a spec-authoring plan — the notes file records observed behavior + the spec gates Phase 137 must honor; Phase 137 owns the amendment prose.

Output:
- `.planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md` — single Markdown artifact with 4 sections (Observed Behavior / Violation Report Shape / Shell Policy Latitude / world:'MAIN' Residual) and a closing Phase 137 Handoff section
- `/tmp/napplet-136-phase-notes-grep.log` — grep evidence confirming the 7 required literal strings
- Zero repo source changes outside `.planning/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/136-empirical-csp-injection-block-verification/136-CONTEXT.md
@.planning/phases/136-empirical-csp-injection-block-verification/136-01-PLAN.md
@/home/sandwich/AGENTS.md

<interfaces>
<!-- Plan 01 outputs — Plan 02 reads these VERBATIM into 136-PHASE-NOTES.md -->

```yaml
# Plan 01 evidence logs (exist after wave 1 completes):
# /tmp/napplet-136-injection-block.log — stdout JSON + VER04_EXIT=0
# /tmp/napplet-136-report-shape.log    — stderr JSON reportShape + VER04_EXIT=0
# /tmp/napplet-136-injection-block.cjs — the fixture itself (reference only)
```

```yaml
# Required literal strings in 136-PHASE-NOTES.md (grep-verifiable in Task 2):
# 1. "world: 'MAIN'"                   — names the extension-API bypass per DETECT-04
# 2. "chrome.scripting.executeScript"  — names the specific Extension API call that bypasses
# 3. "connect-src 'none'"              — names the structural mitigation per DETECT-04
# 4. "MAY refuse-to-serve"             — shell policy latitude per DETECT-03
# 5. "shell MAY reject"                — shell policy latitude per DETECT-03
# 6. "(dTag, aggregateHash)"           — shell-side correlation mechanism per DETECT-02
# 7. "violatedDirective"               — report-shape field name per DETECT-02
```

```yaml
# REQ-ID coverage in this plan:
# DETECT-02 — documented via Violation Report Shape section + correlation mechanism
# DETECT-03 — documented via Shell Policy Latitude section (3 MAY statements)
# DETECT-04 — documented via world:'MAIN' Residual section (honest acknowledgment)
# (DETECT-01 + VER-04 were satisfied in Plan 01; this plan cites Plan 01 evidence for them)
```

```yaml
# Phase boundary reminder (from CONTEXT.md deferred section):
# "if any task drifts into authoring spec amendment text, STOP — that is Phase 137 territory.
#  Phase 136 produces EVIDENCE ONLY. The 136-PHASE-NOTES.md is a record of observed behavior,
#  NOT a draft of the amendment."
# Plan 02 respects this: it records + documents, it does NOT amend NUB-IDENTITY.md or NUB-CLASS-1.md.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 136-PHASE-NOTES.md synthesizing Plan 01 evidence + DETECT-02/03/04 documentation</name>
  <files>.planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md</files>
  <read_first>
    - /tmp/napplet-136-injection-block.log (Plan 01 stdout evidence — read the actual JSON values to cite verbatim; do NOT guess)
    - /tmp/napplet-136-report-shape.log (Plan 01 stderr evidence — read the actual reportShape JSON values to cite verbatim)
    - /tmp/napplet-136-injection-block.cjs (Plan 01 fixture — reference for reproducibility section)
    - .planning/REQUIREMENTS.md (DETECT-02, DETECT-03, DETECT-04 acceptance text — the PHASE-NOTES documents WHAT must be TRUE per these REQs, so Phase 137 can cite the observation + the documented gate together)
    - .planning/phases/136-empirical-csp-injection-block-verification/136-CONTEXT.md (decisions > "world: 'MAIN' residual documentation" + "Shell policy latitude documentation" — locked language)
    - .planning/STATE.md (decisions — the PRINCIPLE lines about napplets being untrusted and shell-side enforcement being authoritative; echo the framing in the notes)
  </read_first>
  <action>
    Write `.planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md` with five sections. Use the `Write` tool (the file does not exist yet).

    The file is a RECORD of observations + documentation GATES; it is NOT amendment prose. Use declarative past/present tense ("The fixture observed...", "Shell MAY...") not prescriptive amendment-style ("Shells MUST..."). Spec MUSTs live in Phase 137's amendment draft.

    **Required structure** (literal section headings the executor MUST preserve verbatim — Phase 137's author searches by heading):

    ```markdown
    # Phase 136 Phase Notes — Empirical CSP Injection-Block Verification

    **Observed:** 2026-04-23
    **Phase:** 136 — Empirical CSP Injection-Block Verification
    **Milestone:** v0.29.0 Class-Gated Decrypt Surface
    **Status:** Observation complete — consumable by Phase 137 NUB-IDENTITY + NUB-CLASS-1 amendment author

    ## Summary

    A Playwright CJS fixture running Chromium headless under Wayland loaded a test napplet page under the NUB-CLASS-1 CSP posture and simulated a legacy NIP-07-style `<script>`-tag content-script injection. Chromium blocked the injection — `window.nostr` stayed `undefined` — and fired a `securitypolicyviolation` event whose `violatedDirective` is `script-src*`. The 4-field violation-report shape was captured. The residual `world: 'MAIN'` extension-API bypass is acknowledged honestly; no page-side mechanism blocks it — NUB-CLASS-1's `connect-src 'none'` is the structural mitigation that traps plaintext inside the frame regardless of how it was obtained.

    ## 1. Observed Behavior — CSP Legacy-Injection Block (DETECT-01 mechanism-observation, VER-04)

    Evidence: `/tmp/napplet-136-injection-block.log` (stdout JSON + `VER04_EXIT=0`).

    The fixture at `/tmp/napplet-136-injection-block.cjs`:
    - `require('/usr/lib/node_modules/playwright')` (CJS — Playwright system install is CJS-only)
    - `chromium.launch({ headless: true, args: ['--ozone-platform=wayland'] })`
    - Built a `data:text/html;charset=utf-8,...` page with first-`<head>`-child CSP meta containing the NUB-CLASS-1 10-directive baseline: `default-src 'none'; script-src 'nonce-napplet136' 'self'; connect-src 'none'; img-src blob: data:; font-src blob: data:; style-src 'self'; worker-src 'none'; object-src 'none'; base-uri 'none'; form-action 'none'`
    - Injected a legacy `<script>` via `document.createElement('script') + s.textContent = 'window.nostr = { __injected: true };' + document.head.appendChild(s)` (nonce absent on the injected element — simulating an extension content-script that cannot know the per-page nonce)

    Observed outcome (verbatim JSON from `/tmp/napplet-136-injection-block.log`):
    ```json
    {RUN-TIME-INSERT: paste the exact JSON line from /tmp/napplet-136-injection-block.log here}
    ```

    Interpretation:
    - `cspViolation: true` — Chromium emitted a console error matching `/Content Security Policy/i`
    - `windowNostrDefined: false` — the injected script's execution was blocked; `window.nostr` never became defined
    - `violatedDirective: "script-src*"` — the block was attributed to the `script-src` family (Chromium reports `script-src-elem` for element-level inline injections; both satisfy `startsWith('script-src')`)
    - `pass: true` — the mechanism works as specified

    ## 2. Violation Report Shape (DETECT-02 — what the shell's `report-to` endpoint would receive)

    Evidence: `/tmp/napplet-136-report-shape.log` (stderr JSON + `VER04_EXIT=0`).

    The in-page `securitypolicyviolation` event observable from page-side JS is a rough proxy for the report shape an HTTP `report-to` endpoint would receive (the Reporting API adds a wrapper with `body`, `type`, `age`, `url`, and `user_agent` fields; the `body` sub-object carries the same violation-specific fields captured below).

    Observed reportShape (verbatim JSON from `/tmp/napplet-136-report-shape.log`):
    ```json
    {RUN-TIME-INSERT: paste the exact reportShape JSON line from /tmp/napplet-136-report-shape.log here}
    ```

    Four load-bearing fields for shell correlation:
    - `violatedDirective` — which CSP directive fired the block (here: `script-src*`)
    - `blockedURI` — the blocked resource URI (here: `"inline"` for inline-script injection)
    - `documentURI` — the URL of the document that produced the violation (the napplet's `data:` or `https://` URL)
    - `sourceFile` — the URL of the script that initiated the violation (same as documentURI for our test)

    **DETECT-02 gate (for Phase 137 amendment to MUST-row):** Shell MUST process received `securitypolicyviolation` reports by correlating to napplet identity via `(dTag, aggregateHash)` through the napplet HTML URL path. The `documentURI` + `sourceFile` fields carry the URL; the shell's URL schema maps path to identity.

    ## 3. Shell Policy Latitude (DETECT-03 — mechanism defined, response is shell UX concern)

    Per DETECT-03 (REQUIREMENTS.md) and STATE.md PRINCIPLE "Security enforcement runs shell-side": the spec defines the MECHANISM, not the response. A shell that receives violation reports MAY choose any of the following policy responses (non-exhaustive):

    - **Shell MAY refuse-to-serve** subsequent loads of the offending napplet — refusing to serve the napplet HTML on future iframe instantiation requests
    - **Shell MAY reject** subsequent `identity.decrypt` envelopes from the offender — returning `policy-denied` or equivalent from the `IdentityDecryptErrorCode` vocabulary
    - **Shell MAY surface** the violation to the user (notification, settings UI entry, session log) at its discretion

    These are observable UX responses Phase 137's amendment notes as shell discretion; the amendment MUST NOT require a specific UX response (doing so would impose per-shell implementation burden without improving the security posture — the shell-side boundary is already authoritative per STATE.md PRINCIPLE lines).

    ## 4. `world: 'MAIN'` Extension-API Residual (DETECT-04 — honestly acknowledged, structurally mitigated by `connect-src 'none'`)

    **Honest acknowledgment required by DETECT-04 — do NOT claim a fix.**

    Chrome / Chromium extensions using `chrome.scripting.executeScript({world:'MAIN'})` (or the older `executeScript` with a manifest-v3 equivalent) inject JavaScript into the page's isolated world at the extension level. This injection path bypasses page CSP entirely per the Extensions WebAPI specification — the browser does NOT enforce page CSP against extension content-scripts running in the `MAIN` world.

    Operational consequences:
    - No `securitypolicyviolation` event fires (page CSP was never consulted)
    - No report is POSTed to the shell's `report-to` endpoint
    - The page-side JavaScript (the napplet) has no mechanism to detect or refuse the injection

    **Structural mitigation — NUB-CLASS-1 `connect-src 'none'`:** Even if an extension successfully injects arbitrary JavaScript via `world: 'MAIN'`, the injected code cannot exfiltrate plaintext over the network from inside a NUB-CLASS-1 napplet because `connect-src 'none'` blocks ALL outbound fetch/XHR/WebSocket/EventSource connections. Plaintext is trapped inside the frame regardless of how it was obtained. This is the ONLY cross-extension-bypass defense available at the page layer, and it is why NUB-CLASS-1 is the load-bearing gate for `identity.decrypt` eligibility.

    **Why this is documented (not fixed):** NIP-07 extension hardening is a browser/extension ecosystem concern (per REQUIREMENTS.md "Out of Scope"). The spec documents the residual so shell implementers + napplet authors + spec readers understand the true security boundary; it does not pretend the mechanism blocks what it does not block.

    ## 5. Phase 137 Handoff

    Phase 137 authors the NUB-IDENTITY + NUB-CLASS-1 amendment (bundled PR per CLASS1-03). This file provides the empirical substrate for that amendment:

    - **For DETECT-01 / DETECT-02 amendment language:** cite Section 1 (observed block behavior) + Section 2 (violation-report shape) — the report shape is what the CLASS1-02 MUST row specifies the shell processes
    - **For the shell's MAY rows / Security Considerations:** cite Section 3 (Shell Policy Latitude); the 3 MAY statements enumerate what shells can do without the amendment requiring any one choice
    - **For NUB-IDENTITY-05 Security Considerations subsection (c):** cite Section 4 VERBATIM — the `world: 'MAIN'` residual acknowledgment text is the honest framing the amendment MUST preserve

    Evidence files to reference (continue to live in `/tmp/` until reboot per AGENTS.md):
    - `/tmp/napplet-136-injection-block.cjs` — Playwright fixture (reproducible; rerun anytime)
    - `/tmp/napplet-136-injection-block.log` — stdout JSON + `VER04_EXIT=0`
    - `/tmp/napplet-136-report-shape.log` — stderr JSON reportShape + `VER04_EXIT=0`

    No repo source changes occurred during Phase 136 — the phase is empirical-only. All `@napplet/*` first-party surface for `identity.decrypt` was shipped in Phase 135.
    ```

    **Load-bearing mechanical steps for the executor**:

    1. FIRST: `cat /tmp/napplet-136-injection-block.log` and copy the actual stdout JSON line (the one that looks like `{"cspViolation":...}`) into Section 1's `{RUN-TIME-INSERT: ...}` placeholder. Do NOT leave the placeholder in the final file.

    2. SECOND: `cat /tmp/napplet-136-report-shape.log` and copy the actual reportShape JSON line (the one that looks like `{"reportShape":{...}}`) into Section 2's `{RUN-TIME-INSERT: ...}` placeholder. Do NOT leave the placeholder.

    3. If the actual `violatedDirective` observed differs from the example (e.g., was `script-src-elem` vs `script-src`), update Section 1's interpretation bullet accordingly — cite the actual observed value, not the template example.

    4. If the actual `blockedURI` observed is not exactly `"inline"` (e.g., Chromium returned `"inline"` vs `""` vs something else), update Section 2's "here: `"inline"`" annotation accordingly — cite the actual observed value.

    5. Preserve ALL section headings verbatim — Phase 137's author greps for them.

    6. Preserve ALL 7 required literal strings verbatim (see Task 2's grep list): `world: 'MAIN'`, `chrome.scripting.executeScript`, `connect-src 'none'`, `MAY refuse-to-serve`, `shell MAY reject`, `(dTag, aggregateHash)`, `violatedDirective`.

    **Phase boundary check before writing:** This file records OBSERVATIONS and documents GATES — it does NOT contain amendment prose. Do NOT add sections like "Proposed Amendment Text", "Draft Spec Language", or "MUST/SHOULD Table" — those live in Phase 137. If the executor feels tempted to draft MUST rows, STOP and re-read CONTEXT.md deferred section.
  </action>
  <verify>
    <automated>test -f .planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md && test "$(wc -l < .planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md)" -ge 80 && ! grep -q 'RUN-TIME-INSERT' .planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md && grep -q '^## Summary' .planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md && grep -q '^## 1. Observed Behavior' .planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md && grep -q '^## 2. Violation Report Shape' .planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md && grep -q '^## 3. Shell Policy Latitude' .planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md && grep -q '^## 4. ' .planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md && grep -q '^## 5. Phase 137 Handoff' .planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md</automated>
  </verify>
  <done>
    `136-PHASE-NOTES.md` exists in the phase directory (>=80 lines). All 5 required section headings present. No `RUN-TIME-INSERT` placeholder strings remain (the executor replaced them with actual Plan 01 JSON). File respects the phase boundary (records observations + gates; does not author amendment prose).
  </done>
</task>

<task type="auto">
  <name>Task 2: Grep-verify the 7 required literal strings + stamp GREP_EXIT=0</name>
  <files>/tmp/napplet-136-phase-notes-grep.log</files>
  <read_first>
    - .planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md (the Task 1 deliverable — all 7 strings must be present verbatim)
    - .planning/phases/136-empirical-csp-injection-block-verification/136-01-PLAN.md (must_haves.truths — the 7-literal-string list is the test matrix)
    - .planning/phases/136-empirical-csp-injection-block-verification/136-CONTEXT.md (specifics > Gate B — same 7-literal-string expectation)
  </read_first>
  <action>
    Run a 7-check grep sweep against `136-PHASE-NOTES.md`, capturing per-check PASS/FAIL and stamping `GREP_EXIT=0` on clean sweep. Log to `/tmp/napplet-136-phase-notes-grep.log` per AGENTS.md no-home-pollution rule.

    Exact shell invocation (run from repo root):

    ```bash
    cd /home/sandwich/Develop/napplet
    NOTES=.planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md
    LOG=/tmp/napplet-136-phase-notes-grep.log
    {
      echo "=== Phase 136 Plan 02 — PHASE-NOTES.md literal-string sweep ==="
      echo "File: $NOTES"
      echo ""
      FAIL=0
      check() {
        local label="$1"; local pattern="$2"
        if grep -qF "$pattern" "$NOTES"; then
          echo "PASS [$label] -- '$pattern'"
        else
          echo "FAIL [$label] -- '$pattern' NOT FOUND"
          FAIL=1
        fi
      }
      check "DETECT-04 world"          "world: 'MAIN'"
      check "DETECT-04 extension API"  "chrome.scripting.executeScript"
      check "DETECT-04 mitigation"     "connect-src 'none'"
      check "DETECT-03 latitude A"     "MAY refuse-to-serve"
      check "DETECT-03 latitude B"     "shell MAY reject"
      check "DETECT-02 correlation"    "(dTag, aggregateHash)"
      check "DETECT-02 report field"   "violatedDirective"
      echo ""
      if [ "$FAIL" = "0" ]; then
        echo "ALL 7 LITERAL STRINGS PRESENT"
        echo "GREP_EXIT=0"
      else
        echo "AT LEAST ONE LITERAL STRING MISSING"
        echo "GREP_EXIT=1"
      fi
    } > "$LOG" 2>&1
    ```

    Expected final lines of `/tmp/napplet-136-phase-notes-grep.log`:
    ```
    PASS [DETECT-04 world] -- 'world: '\''MAIN'\'''
    ... (5 more PASS lines)
    PASS [DETECT-02 report field] -- 'violatedDirective'

    ALL 7 LITERAL STRINGS PRESENT
    GREP_EXIT=0
    ```

    If ANY string is missing (`GREP_EXIT=1`):
    1. Identify which labeled check FAILed in the log
    2. Return to Task 1's deliverable and add the missing literal string in the appropriate section (world → Section 4; latitude → Section 3; correlation/report → Section 2)
    3. Re-run this task's sweep until `GREP_EXIT=0`
    4. Do NOT proceed to plan SUMMARY until all 7 pass
  </action>
  <verify>
    <automated>test -f /tmp/napplet-136-phase-notes-grep.log && grep -q 'GREP_EXIT=0' /tmp/napplet-136-phase-notes-grep.log && grep -q 'ALL 7 LITERAL STRINGS PRESENT' /tmp/napplet-136-phase-notes-grep.log && test "$(grep -c '^PASS ' /tmp/napplet-136-phase-notes-grep.log)" -eq 7 && test "$(grep -c '^FAIL ' /tmp/napplet-136-phase-notes-grep.log)" -eq 0</automated>
  </verify>
  <done>
    `/tmp/napplet-136-phase-notes-grep.log` contains 7 `PASS` lines, zero `FAIL` lines, the summary `ALL 7 LITERAL STRINGS PRESENT`, and the `GREP_EXIT=0` marker. DETECT-02, DETECT-03, DETECT-04 documentation gates are grep-verified. Phase 137's amendment author can cite `136-PHASE-NOTES.md` confidently — every required literal string is present.
  </done>
</task>

</tasks>

<verification>
**Plan-level acceptance gate** (after both tasks complete):

1. `.planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md` exists with >=80 lines (Task 1)
2. All 5 required section headings present verbatim (Summary / 1. Observed Behavior / 2. Violation Report Shape / 3. Shell Policy Latitude / 4. world:'MAIN' Residual / 5. Phase 137 Handoff) (Task 1)
3. No `RUN-TIME-INSERT` placeholder strings remain — executor substituted actual Plan 01 JSON (Task 1)
4. `/tmp/napplet-136-phase-notes-grep.log` reports 7 PASS + 0 FAIL + `GREP_EXIT=0` (Task 2)
5. Zero modifications to files under `packages/`, `specs/`, `src/` (phase boundary honored — empirical-only)

**One-shot verification command** (run from repo root after Task 2):
```bash
cd /home/sandwich/Develop/napplet
test -f .planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md \
  && grep -q 'GREP_EXIT=0' /tmp/napplet-136-phase-notes-grep.log \
  && grep -q 'ALL 7 LITERAL STRINGS PRESENT' /tmp/napplet-136-phase-notes-grep.log \
  && ! grep -q 'RUN-TIME-INSERT' .planning/phases/136-empirical-csp-injection-block-verification/136-PHASE-NOTES.md \
  && git diff --stat packages/ specs/ 2>/dev/null | grep -c '^' | test "$(cat)" = "0" \
  && echo "PHASE 136 PLAN 02 GREEN — PHASE 136 COMPLETE"
```
</verification>

<success_criteria>
- DETECT-02 documented: violation-report 4-field shape cited verbatim from Plan 01 evidence; `(dTag, aggregateHash)` correlation mechanism named
- DETECT-03 documented: 3 MAY statements enumerated (refuse-to-serve / reject identity.decrypt / surface to user); spec-defines-mechanism / shell-owns-UX framing locked
- DETECT-04 documented HONESTLY: `world: 'MAIN'` residual acknowledged; `chrome.scripting.executeScript` bypass path named; `connect-src 'none'` structural mitigation named; no-fix-claim language preserved
- All 7 required literal strings grep-verified present in `136-PHASE-NOTES.md`
- Phase boundary honored: file records observations + gates, does NOT author amendment prose (no MUST/SHOULD tables, no "Proposed Amendment Text" sections)
- Phase 137 has a single, structured, grep-indexable artifact to cite when authoring NUB-IDENTITY + NUB-CLASS-1 amendment
- All Plan 02 work lives in `.planning/phases/136-.../` + `/tmp/napplet-136-*`; zero repo source changes
</success_criteria>

<output>
After completion, create `.planning/phases/136-empirical-csp-injection-block-verification/136-02-SUMMARY.md` recording:
- Path + line count of `136-PHASE-NOTES.md`
- Verbatim paste of `/tmp/napplet-136-phase-notes-grep.log` (the 7-PASS sweep output)
- Confirmation that all 5 section headings + 7 literal strings are present
- Verbatim paste of the actual Plan 01 JSON values substituted into Sections 1 and 2 (the stdout + stderr JSON lines)
- If any deviation from the template was necessary (e.g., Chromium reported `script-src` instead of `script-src-elem`, or `blockedURI` was something other than `"inline"`), note the actual observed values + the Task 1 step that substituted them
- Confirmation that phase boundary held: no files under `packages/`, `specs/`, or `src/` were modified
- Handoff note: Phase 136 is complete and ready for Phase 137 `/gsd:plan-phase 137` (Phase 137 depends on both 135 (already complete) and 136 (this phase))
</output>
