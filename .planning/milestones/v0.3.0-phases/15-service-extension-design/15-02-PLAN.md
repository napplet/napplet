---
phase: 15
plan: 2
title: "Document service discovery protocol in SPEC.md"
wave: 1
depends_on: []
files_modified:
  - SPEC.md
autonomous: true
requirements_addressed: [SVC-03]
---

# Plan 15-02: Document Service Discovery Protocol in SPEC.md

<objective>
Add a "Service Discovery" section to SPEC.md that defines the service discovery message format (kind 29010), the request-response flow, and the service extension pattern. This documents the protocol design even though implementation is deferred to v0.4.0.
</objective>

<context>
SPEC.md currently has 16 sections covering the full napplet-shell protocol. Section 14 (Provisional NIP-C4 Kind Numbers) already reserves kinds in the napplet range. The new service discovery section should be added as a new top-level section, slotted after the existing "10. Hotkey Forwarding" section and before "11. Protocol Layers". This places it alongside the other capability sections (Audio 6, Relay 7, Window/Shell 8, Cache 9, Hotkey 10).

The service discovery kind 29010 is in the ephemeral bus range. It uses the same NIP-01 wire format as all other napplet-shell messages. A napplet sends a service discovery REQ, and the shell responds with one event per registered service, followed by EOSE.

The service extension pattern also needs documenting: how services register, how napplets address service-specific topics, and how the lifecycle works.
</context>

<tasks>

<task id="15-02-01" title="Add Service Discovery section to SPEC.md">
<read_first>
- SPEC.md (read sections 6 through 10 for the pattern, and section 14 for kind number conventions)
</read_first>
<action>
Insert a new section "11. Service Discovery [OPEN]" into SPEC.md. This requires renumbering existing sections 11-16 to 12-17. The new section goes after the current "10. Hotkey Forwarding [OPEN]" section and before the current "11. Protocol Layers [OPEN]" (which becomes 12).

**Section renumbering:**
- Current "11. Protocol Layers [OPEN]" → "12. Protocol Layers [OPEN]"
- Current "12. ACL Capabilities [OPEN]" → "13. ACL Capabilities [OPEN]"
- Current "13. Security Model [LOCKED]" → "14. Security Model [LOCKED]"
- Current "14. Provisional -- NIP-C4 Kind Numbers [OPEN]" → "15. Provisional -- NIP-C4 Kind Numbers [OPEN]"
- Current "15. Minimal Viable Implementation [OPEN]" → "16. Minimal Viable Implementation [OPEN]"
- Current "16. Implementation Notes" → "17. Implementation Notes"

Also update all internal cross-references. Search for "Section 11", "Section 12", "Section 13", "Section 14", "Section 15", "Section 16" and "Sections 1-3 and 13" references — update them to their new numbers.

**New section content:**

```markdown
## 11. Service Discovery [OPEN]

### 11.1 Overview

The shell MAY expose optional services beyond the core protocol (audio management, notifications, clipboard access, etc.). Napplets discover available services via a dedicated service discovery mechanism using kind 29010 events.

Service discovery is cooperative: the shell advertises what it supports, and napplets adapt their behavior based on available services. A napplet MUST NOT assume a service is available without first checking via discovery.

> **Status:** This section defines the protocol design for service discovery. Implementation is deferred to a future version. Shells and napplets SHOULD NOT implement service discovery until the spec is marked LOCKED.

### 11.2 Service Discovery Event (kind 29010)

| Field | Value |
|---|---|
| `kind` | `29010` (BusKind.SERVICE_DISCOVERY) |
| `pubkey` | Shell's ephemeral pubkey (for responses) or napplet's session pubkey (for requests) |
| `tags` | See below |
| `content` | JSON payload |

**Discovery Request (napplet → shell):**

The napplet sends a REQ for kind 29010 events. The shell responds with one event per registered service, followed by EOSE.

```json
["REQ", "svc-discovery", {"kinds": [29010]}]
```

**Discovery Response (shell → napplet):**

For each registered service, the shell sends:

```json
["EVENT", "svc-discovery", {
  "kind": 29010,
  "pubkey": "__shell__",
  "created_at": 1711800000,
  "tags": [
    ["s", "audio"],
    ["v", "1.0.0"],
    ["d", "Audio playback management and mute control"]
  ],
  "content": "{}",
  "id": "...",
  "sig": ""
}]
```

| Tag | Required | Description |
|---|---|---|
| `s` | MUST | Service name (unique identifier, e.g., `audio`, `notifications`, `clipboard`) |
| `v` | MUST | Semver version of the service implementation |
| `d` | MAY | Human-readable description of the service |

After sending all service descriptors, the shell sends EOSE:

```json
["EOSE", "svc-discovery"]
```

### 11.3 Service Message Routing

Services receive messages via INTER_PANE events (kind 29003) with topic strings prefixed by the service name. The topic format is:

```
{service-name}:{action}
```

For example, the `audio` service uses topics like:
- `audio:register` — Register an audio source
- `audio:unregister` — Unregister an audio source
- `audio:state-changed` — Update audio metadata

The shell routes INTER_PANE events to service handlers based on the topic prefix. Events with topics that match a registered service's name prefix are dispatched to that service's handler.

### 11.4 Service Lifecycle

1. **Registration:** The shell host registers services at startup via the `services` field in ShellHooks (or RuntimeHooks). Each service provides a `ServiceHandler` with a descriptor and request handler.

2. **Discovery:** After AUTH completes, the napplet MAY send a REQ for kind 29010 to discover available services. The shell responds with descriptors for all registered services.

3. **Interaction:** The napplet sends INTER_PANE events with service-prefixed topics. The shell dispatches these to the appropriate service handler.

4. **Cleanup:** When a napplet window is destroyed, the shell calls `onWindowDestroyed` on each service handler that implements it, allowing services to clean up per-window state.

### 11.5 Built-in vs Custom Services

The protocol distinguishes between built-in and custom services:

**Built-in services** are defined in this specification and have standardized topic formats:
- `audio` — Audio playback management (see Section 6)
- Future: `notifications`, `clipboard`, etc.

**Custom services** are shell-specific extensions. They follow the same topic-prefix pattern but are not standardized. Napplets MUST use service discovery to check for custom service availability before attempting to use them.

### 11.6 ACL Considerations

Service-level ACL gating is NOT defined in this version. Individual services MAY implement their own access control within their handlers. A future version MAY define per-service capability strings (e.g., `service:audio`, `service:notifications`).

### 11.7 Backwards Compatibility

Service discovery is entirely optional. Shells that do not register any services will not respond to kind 29010 REQs (the subscription receives EOSE immediately with no events). Napplets that do not perform service discovery continue to work with the core protocol unchanged.

Existing protocol features (audio management via Section 6 topics, storage proxy, signer proxy) continue to work independently of the service extension system. Migration of these features to the service pattern is a v0.4.0+ concern.

---
```
</action>
<acceptance_criteria>
- `grep -n '## 11\. Service Discovery' SPEC.md` returns exactly 1 result
- `grep -n 'kind.*29010' SPEC.md` returns at least 1 result
- `grep -n '### 11\.1 Overview' SPEC.md` returns exactly 1 result
- `grep -n '### 11\.2 Service Discovery Event' SPEC.md` returns exactly 1 result
- `grep -n '### 11\.3 Service Message Routing' SPEC.md` returns exactly 1 result
- `grep -n '### 11\.4 Service Lifecycle' SPEC.md` returns exactly 1 result
- `grep -n '### 11\.5 Built-in vs Custom Services' SPEC.md` returns exactly 1 result
- `grep -n '### 11\.6 ACL Considerations' SPEC.md` returns exactly 1 result
- `grep -n '### 11\.7 Backwards Compatibility' SPEC.md` returns exactly 1 result
- The section describes the REQ/EVENT/EOSE flow for discovery
- The section defines tag format: `s` (service name), `v` (version), `d` (description)
- The section explains topic-prefix routing pattern (e.g., `audio:register`)
</acceptance_criteria>
</task>

<task id="15-02-02" title="Renumber existing SPEC.md sections 11-16 to 12-17">
<read_first>
- SPEC.md (full file — search for all section number references)
</read_first>
<action>
After inserting the new section 11, renumber all subsequent sections:

1. Change `## 11. Protocol Layers [OPEN]` to `## 12. Protocol Layers [OPEN]`
   - Update all subsections: `### 11.x` → `### 12.x`

2. Change `## 12. ACL Capabilities [OPEN]` to `## 13. ACL Capabilities [OPEN]`
   - Update all subsections: `### 12.x` → `### 13.x`

3. Change `## 13. Security Model [LOCKED]` to `## 14. Security Model [LOCKED]`
   - Update all subsections: `### 13.x` → `### 14.x`

4. Change `## 14. Provisional -- NIP-C4 Kind Numbers [OPEN]` to `## 15. Provisional -- NIP-C4 Kind Numbers [OPEN]`
   - Update all subsections: `### 14.x` → `### 15.x`

5. Change `## 15. Minimal Viable Implementation [OPEN]` to `## 16. Minimal Viable Implementation [OPEN]`
   - Update all subsections: `### 15.x` → `### 16.x`

6. Change `## 16. Implementation Notes` to `## 17. Implementation Notes`
   - Update all subsections: `### 16.x` → `### 17.x`

7. Update internal cross-references. Search for these patterns and update:
   - "Sections 1-3 and 13" → "Sections 1-3 and 14" (in conformance section)
   - "Section 6" references to audio → keep as-is (no renumber)
   - Any other "Section N" references where N >= 11 → increment by 1

8. Add kind 29010 to section 15 (was section 14) "Provisional -- NIP-C4 Kind Numbers" in a new subsection or as an addition to existing tables:

Add after the existing NIP-C4 kind tables:

```markdown
### 15.4 Service Discovery Kind

| Constant | Kind Number | Status |
|----------|------------|--------|
| `BusKind.SERVICE_DISCOVERY` | 29010 | Internal (not a NIP kind) |

This kind is used for shell-to-napplet service discovery responses. It is part of the ephemeral bus range (29000-29999) and is NOT a NIP-assigned kind number. It follows the same allocation pattern as the existing BusKind constants (29000-29007).
```
</action>
<acceptance_criteria>
- `grep -n '## 12\. Protocol Layers' SPEC.md` returns exactly 1 result (was section 11)
- `grep -n '## 13\. ACL Capabilities' SPEC.md` returns exactly 1 result (was section 12)
- `grep -n '## 14\. Security Model' SPEC.md` returns exactly 1 result (was section 13)
- `grep -n '## 15\. Provisional' SPEC.md` returns exactly 1 result (was section 14)
- `grep -n '## 16\. Minimal Viable' SPEC.md` returns exactly 1 result (was section 15)
- `grep -n '## 17\. Implementation Notes' SPEC.md` returns exactly 1 result (was section 16)
- `grep -n '## 11\. Protocol Layers' SPEC.md` returns 0 results (old number gone)
- `grep -n 'Sections 1-3 and 14' SPEC.md` returns at least 1 result (cross-ref updated)
- `grep -n 'SERVICE_DISCOVERY.*29010' SPEC.md` returns at least 1 result in the provisional kinds section
- No broken cross-references (search for "Section 11" — should only appear in the new Service Discovery section)
</acceptance_criteria>
</task>

<task id="15-02-03" title="Add service discovery to Future Work section">
<read_first>
- SPEC.md (section 17.3 Future Work, formerly 16.3)
</read_first>
<action>
Update the "Future Work" subsection (now 17.3) to reference the service extension design:

Add the following items to the existing bullet list in the Future Work section:

```markdown
- Service discovery implementation (Section 11) — napplets query available shell services via kind 29010
- Per-service ACL capabilities — `service:audio`, `service:notifications`, etc.
- Service dependency declaration in NIP-5A manifests (`requires` tags)
```

These items connect the new spec section to the deferred v0.4.0 work.
</action>
<acceptance_criteria>
- `grep -n 'Service discovery implementation' SPEC.md` returns exactly 1 result in the Future Work section
- `grep -n 'Per-service ACL capabilities' SPEC.md` returns exactly 1 result
- `grep -n 'Service dependency declaration' SPEC.md` returns exactly 1 result
- The Future Work section references "Section 11" for service discovery
</acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:
1. SPEC.md contains a new "11. Service Discovery [OPEN]" section with subsections 11.1-11.7
2. All subsequent sections are renumbered correctly (12-17)
3. Internal cross-references are updated
4. Kind 29010 is documented in both the service discovery section and the provisional kinds section
5. The service discovery message format (REQ for kind 29010, EVENT responses with s/v/d tags, EOSE) is clearly specified
6. Future Work references service discovery implementation
7. No broken references or numbering gaps
</verification>

<must_haves>
- SPEC.md contains a "Service Discovery" section describing the message format (SVC-03)
- The message format specifies kind 29010, tag schema (s, v, d), and REQ/EVENT/EOSE flow
- Section numbering is consistent throughout the spec (no gaps, no duplicates)
- Cross-references are updated
- Backwards compatibility is addressed (service discovery is optional)
</must_haves>
