---
phase: 62-kehto-repo-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/Develop/kehto/package.json
  - ~/Develop/kehto/pnpm-workspace.yaml
  - ~/Develop/kehto/turbo.json
  - ~/Develop/kehto/tsconfig.json
  - ~/Develop/kehto/.gitignore
  - ~/Develop/kehto/.changeset/config.json
  - ~/Develop/kehto/.changeset/README.md
  - ~/Develop/kehto/packages/acl/package.json
  - ~/Develop/kehto/packages/acl/tsconfig.json
  - ~/Develop/kehto/packages/acl/tsup.config.ts
  - ~/Develop/kehto/packages/acl/src/index.ts
  - ~/Develop/kehto/packages/runtime/package.json
  - ~/Develop/kehto/packages/runtime/tsconfig.json
  - ~/Develop/kehto/packages/runtime/tsup.config.ts
  - ~/Develop/kehto/packages/runtime/src/index.ts
  - ~/Develop/kehto/packages/shell/package.json
  - ~/Develop/kehto/packages/shell/tsconfig.json
  - ~/Develop/kehto/packages/shell/tsup.config.ts
  - ~/Develop/kehto/packages/shell/src/index.ts
  - ~/Develop/kehto/packages/services/package.json
  - ~/Develop/kehto/packages/services/tsconfig.json
  - ~/Develop/kehto/packages/services/tsup.config.ts
  - ~/Develop/kehto/packages/services/src/index.ts
autonomous: true
requirements: [KEHTO-01, KEHTO-02]

must_haves:
  truths:
    - "~/Develop/kehto is a git repo with an initial commit"
    - "pnpm install succeeds and creates pnpm-lock.yaml"
    - "pnpm build succeeds across all four packages"
    - "pnpm type-check succeeds with zero errors"
    - "All four @kehto packages have correct ESM-only package.json"
  artifacts:
    - path: "~/Develop/kehto/package.json"
      provides: "Root monorepo config with pnpm workspaces + turborepo"
      contains: "\"packageManager\": \"pnpm@10.8.0\""
    - path: "~/Develop/kehto/turbo.json"
      provides: "Turborepo task pipeline"
      contains: "\"dependsOn\": [\"^build\"]"
    - path: "~/Develop/kehto/tsconfig.json"
      provides: "Shared TypeScript config — strict, ESM, ES2022"
      contains: "\"strict\": true"
    - path: "~/Develop/kehto/pnpm-workspace.yaml"
      provides: "pnpm workspace definition"
      contains: "packages/*"
    - path: "~/Develop/kehto/packages/acl/package.json"
      provides: "@kehto/acl package shell"
      contains: "\"name\": \"@kehto/acl\""
    - path: "~/Develop/kehto/packages/runtime/package.json"
      provides: "@kehto/runtime package shell"
      contains: "\"name\": \"@kehto/runtime\""
    - path: "~/Develop/kehto/packages/shell/package.json"
      provides: "@kehto/shell package shell"
      contains: "\"name\": \"@kehto/shell\""
    - path: "~/Develop/kehto/packages/services/package.json"
      provides: "@kehto/services package shell"
      contains: "\"name\": \"@kehto/services\""
  key_links:
    - from: "~/Develop/kehto/packages/runtime/package.json"
      to: "@kehto/acl"
      via: "workspace:* dependency"
      pattern: "\"@kehto/acl\": \"workspace:\\*\""
    - from: "~/Develop/kehto/packages/shell/package.json"
      to: "@kehto/runtime"
      via: "workspace:* dependency"
      pattern: "\"@kehto/runtime\": \"workspace:\\*\""
    - from: "~/Develop/kehto/packages/*/tsconfig.json"
      to: "~/Develop/kehto/tsconfig.json"
      via: "extends"
      pattern: "\"extends\": \"../../tsconfig.json\""
---

<objective>
Initialize ~/Develop/kehto as a fully buildable pnpm monorepo with turborepo orchestration, TypeScript strict/ESM-only configuration, and four package shells (@kehto/acl, @kehto/runtime, @kehto/shell, @kehto/services) that build and type-check cleanly.

Purpose: Create the foundational repo structure so Phase 63 can copy source code into it.
Output: A git repo at ~/Develop/kehto with `pnpm build` and `pnpm type-check` passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Reference files from @napplet monorepo (read these for exact conventions):
@/home/sandwich/Develop/napplet/package.json
@/home/sandwich/Develop/napplet/turbo.json
@/home/sandwich/Develop/napplet/tsconfig.json
@/home/sandwich/Develop/napplet/pnpm-workspace.yaml
@/home/sandwich/Develop/napplet/.changeset/config.json
@/home/sandwich/Develop/napplet/.gitignore
@/home/sandwich/Develop/napplet/packages/acl/package.json
@/home/sandwich/Develop/napplet/packages/acl/tsconfig.json
@/home/sandwich/Develop/napplet/packages/acl/tsup.config.ts
@/home/sandwich/Develop/napplet/packages/runtime/package.json
@/home/sandwich/Develop/napplet/packages/shell/package.json
@/home/sandwich/Develop/napplet/packages/services/package.json

<interfaces>
<!-- Dependency graph between kehto packages (mirrors @napplet layout): -->
<!-- @kehto/acl: zero deps (pure module) -->
<!-- @kehto/runtime: depends on @napplet/core (peer), @kehto/acl (workspace) -->
<!-- @kehto/shell: depends on @napplet/core (peer), @kehto/runtime (workspace) -->
<!-- @kehto/services: depends on @napplet/core (peer), @kehto/runtime (workspace) -->
<!--
  During Phase 62, @napplet/core is listed as peerDependency (not yet on npm).
  Phase 63 will add it as workspace-linked via pnpm overrides.
  Phase 67 switches to npm-published @napplet/core.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize monorepo root with git, pnpm workspace, turborepo, TypeScript, changesets, and gitignore</name>
  <read_first>
    /home/sandwich/Develop/napplet/package.json
    /home/sandwich/Develop/napplet/turbo.json
    /home/sandwich/Develop/napplet/tsconfig.json
    /home/sandwich/Develop/napplet/pnpm-workspace.yaml
    /home/sandwich/Develop/napplet/.changeset/config.json
    /home/sandwich/Develop/napplet/.gitignore
  </read_first>
  <files>
    ~/Develop/kehto/package.json
    ~/Develop/kehto/pnpm-workspace.yaml
    ~/Develop/kehto/turbo.json
    ~/Develop/kehto/tsconfig.json
    ~/Develop/kehto/.gitignore
    ~/Develop/kehto/.changeset/config.json
    ~/Develop/kehto/.changeset/README.md
  </files>
  <action>
    IMPORTANT: All file operations target ~/Develop/kehto, NOT the current @napplet repo.

    1. Initialize git in ~/Develop/kehto:
       ```bash
       cd ~/Develop/kehto && git init -b main
       ```

    2. Create ~/Develop/kehto/package.json — root monorepo config:
       ```json
       {
         "name": "kehto",
         "private": true,
         "type": "module",
         "scripts": {
           "build": "turbo run build",
           "type-check": "turbo run type-check",
           "test": "turbo run test",
           "test:unit": "turbo run test:unit",
           "lint": "turbo run lint",
           "version-packages": "changeset version",
           "publish-packages": "turbo run build && changeset publish"
         },
         "devDependencies": {
           "@changesets/cli": "^2.30.0",
           "turbo": "^2.5.0",
           "typescript": "^5.9.3"
         },
         "packageManager": "pnpm@10.8.0"
       }
       ```
       Key differences from @napplet: No test fixtures, no playwright, no vite at root level. Just the orchestration tools.

    3. Create ~/Develop/kehto/pnpm-workspace.yaml:
       ```yaml
       packages:
         - packages/*
       ```
       Simpler than @napplet — no apps/, tests/ globs yet. Those come in Phase 64 (demo migration).

    4. Create ~/Develop/kehto/turbo.json — identical to @napplet's:
       ```json
       {
         "$schema": "https://turbo.build/schema.json",
         "tasks": {
           "build": {
             "dependsOn": ["^build"],
             "outputs": ["dist/**"]
           },
           "test": {
             "dependsOn": ["build"]
           },
           "test:unit": {
             "dependsOn": ["build"],
             "outputs": ["coverage/**"]
           },
           "test:e2e": {
             "dependsOn": ["build"],
             "cache": false
           },
           "type-check": {
             "dependsOn": ["^build"]
           },
           "lint": {}
         }
       }
       ```

    5. Create ~/Develop/kehto/tsconfig.json — identical to @napplet's root:
       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "ESNext",
           "moduleResolution": "bundler",
           "declaration": true,
           "declarationMap": true,
           "sourceMap": true,
           "strict": true,
           "esModuleInterop": true,
           "skipLibCheck": true,
           "forceConsistentCasingInFileNames": true,
           "resolveJsonModule": true,
           "isolatedModules": true,
           "verbatimModuleSyntax": true
         }
       }
       ```

    6. Create ~/Develop/kehto/.gitignore:
       ```
       node_modules/
       dist/
       *.tsbuildinfo
       .turbo/
       test-results/
       playwright-report/
       .claude/
       ```

    7. Create ~/Develop/kehto/.changeset/config.json:
       ```json
       {
         "$schema": "https://unpkg.com/@changesets/config@3.1.1/schema.json",
         "changelog": "@changesets/cli/changelog",
         "commit": false,
         "fixed": [],
         "linked": [],
         "access": "public",
         "baseBranch": "main",
         "updateInternalDependencies": "patch",
         "ignore": []
       }
       ```

    8. Create ~/Develop/kehto/.changeset/README.md:
       ```markdown
       # Changesets

       Hello and welcome! This folder has been automatically generated by `@changesets/cli`, a build tool that works
       with multi-package repos, or single-package repos to help you version and publish your code. You can find the
       full documentation for it [in the main repo](https://github.com/changesets/changesets)
       ```
  </action>
  <verify>
    <automated>cd ~/Develop/kehto && git status && cat package.json | grep '"packageManager"' && cat tsconfig.json | grep '"strict": true' && cat turbo.json | grep '"dependsOn"' && cat pnpm-workspace.yaml | grep 'packages/*' && cat .changeset/config.json | grep '"access": "public"' && cat .gitignore | grep 'node_modules/'</automated>
  </verify>
  <acceptance_criteria>
    - ~/Develop/kehto/.git exists (git initialized)
    - package.json contains "packageManager": "pnpm@10.8.0"
    - package.json contains "type": "module"
    - package.json devDependencies has turbo, typescript, @changesets/cli
    - tsconfig.json contains "strict": true, "target": "ES2022", "module": "ESNext", "verbatimModuleSyntax": true
    - turbo.json has build task with "dependsOn": ["^build"]
    - pnpm-workspace.yaml lists "packages/*"
    - .changeset/config.json has "access": "public" and "baseBranch": "main"
    - .gitignore includes node_modules/, dist/, .turbo/, .claude/
  </acceptance_criteria>
  <done>Root monorepo configuration files created, matching @napplet conventions (ESM-only, strict TypeScript, turborepo pipeline, changesets).</done>
</task>

<task type="auto">
  <name>Task 2: Create four @kehto package directories with package.json, tsconfig, tsup, and stub index.ts</name>
  <read_first>
    /home/sandwich/Develop/napplet/packages/acl/package.json
    /home/sandwich/Develop/napplet/packages/acl/tsconfig.json
    /home/sandwich/Develop/napplet/packages/acl/tsup.config.ts
    /home/sandwich/Develop/napplet/packages/runtime/package.json
    /home/sandwich/Develop/napplet/packages/shell/package.json
    /home/sandwich/Develop/napplet/packages/services/package.json
  </read_first>
  <files>
    ~/Develop/kehto/packages/acl/package.json
    ~/Develop/kehto/packages/acl/tsconfig.json
    ~/Develop/kehto/packages/acl/tsup.config.ts
    ~/Develop/kehto/packages/acl/src/index.ts
    ~/Develop/kehto/packages/runtime/package.json
    ~/Develop/kehto/packages/runtime/tsconfig.json
    ~/Develop/kehto/packages/runtime/tsup.config.ts
    ~/Develop/kehto/packages/runtime/src/index.ts
    ~/Develop/kehto/packages/shell/package.json
    ~/Develop/kehto/packages/shell/tsconfig.json
    ~/Develop/kehto/packages/shell/tsup.config.ts
    ~/Develop/kehto/packages/shell/src/index.ts
    ~/Develop/kehto/packages/services/package.json
    ~/Develop/kehto/packages/services/tsconfig.json
    ~/Develop/kehto/packages/services/tsup.config.ts
    ~/Develop/kehto/packages/services/src/index.ts
  </files>
  <action>
    IMPORTANT: All file operations target ~/Develop/kehto/packages/, NOT @napplet.

    Create 4 package directories, each with: package.json, tsconfig.json, tsup.config.ts, src/index.ts.

    Shared tsconfig.json for ALL packages (identical):
    ```json
    {
      "extends": "../../tsconfig.json",
      "compilerOptions": {
        "outDir": "dist",
        "rootDir": "src",
        "lib": ["ES2022"]
      },
      "include": ["src"]
    }
    ```

    Shared tsup.config.ts for ALL packages (identical):
    ```typescript
    import { defineConfig } from 'tsup';

    export default defineConfig({
      entry: ['src/index.ts'],
      format: ['esm'],
      dts: true,
      sourcemap: true,
      clean: true,
    });
    ```

    --- @kehto/acl ---
    package.json:
    ```json
    {
      "name": "@kehto/acl",
      "version": "0.1.0",
      "description": "Pure, WASM-ready ACL module for the napplet protocol — zero dependencies, zero side effects",
      "type": "module",
      "main": "./dist/index.js",
      "module": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "exports": {
        ".": {
          "types": "./dist/index.d.ts",
          "import": "./dist/index.js"
        }
      },
      "files": ["dist"],
      "sideEffects": false,
      "publishConfig": {
        "access": "public"
      },
      "dependencies": {},
      "devDependencies": {
        "tsup": "^8.5.0",
        "typescript": "^5.9.3"
      },
      "scripts": {
        "build": "tsup",
        "type-check": "tsc --noEmit",
        "test:unit": "echo 'no unit tests yet'"
      },
      "license": "MIT",
      "repository": {
        "type": "git",
        "url": "git+https://github.com/kehto/runtime.git",
        "directory": "packages/acl"
      },
      "keywords": ["nostr", "napplet", "kehto", "acl", "capability"]
    }
    ```
    src/index.ts: `export {};` (empty barrel — source comes in Phase 63)

    --- @kehto/runtime ---
    package.json:
    ```json
    {
      "name": "@kehto/runtime",
      "version": "0.1.0",
      "description": "Browser-agnostic protocol engine for the napplet protocol — message dispatch, ACL enforcement, AUTH handshake",
      "type": "module",
      "main": "./dist/index.js",
      "module": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "exports": {
        ".": {
          "types": "./dist/index.d.ts",
          "import": "./dist/index.js"
        }
      },
      "files": ["dist"],
      "sideEffects": false,
      "publishConfig": {
        "access": "public"
      },
      "dependencies": {
        "@kehto/acl": "workspace:*",
        "@noble/hashes": "^2.0.0",
        "@noble/curves": "^2.0.0"
      },
      "peerDependencies": {
        "@napplet/core": ">=0.1.0"
      },
      "devDependencies": {
        "tsup": "^8.5.0",
        "typescript": "^5.9.3"
      },
      "scripts": {
        "build": "tsup",
        "type-check": "tsc --noEmit",
        "test:unit": "echo 'no unit tests yet'"
      },
      "license": "MIT",
      "repository": {
        "type": "git",
        "url": "git+https://github.com/kehto/runtime.git",
        "directory": "packages/runtime"
      },
      "keywords": ["nostr", "napplet", "kehto", "runtime", "protocol-engine"]
    }
    ```
    src/index.ts: `export {};` (source comes in Phase 63)

    --- @kehto/shell ---
    package.json:
    ```json
    {
      "name": "@kehto/shell",
      "version": "0.1.0",
      "description": "Shell runtime — relay bridge, ACL enforcement, signer proxy, storage proxy for hosting napplet iframe applications",
      "type": "module",
      "main": "./dist/index.js",
      "module": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "exports": {
        ".": {
          "types": "./dist/index.d.ts",
          "import": "./dist/index.js"
        }
      },
      "files": ["dist"],
      "sideEffects": false,
      "publishConfig": {
        "access": "public"
      },
      "dependencies": {
        "@kehto/runtime": "workspace:*"
      },
      "peerDependencies": {
        "@napplet/core": ">=0.1.0",
        "nostr-tools": ">=2.23.3 <3.0.0"
      },
      "devDependencies": {
        "nostr-tools": "^2.23.3",
        "tsup": "^8.5.0",
        "typescript": "^5.9.3"
      },
      "scripts": {
        "build": "tsup",
        "type-check": "tsc --noEmit",
        "test:unit": "echo 'no unit tests yet'"
      },
      "license": "MIT",
      "repository": {
        "type": "git",
        "url": "git+https://github.com/kehto/runtime.git",
        "directory": "packages/shell"
      },
      "keywords": ["nostr", "napplet", "kehto", "shell", "relay-bridge"]
    }
    ```
    src/index.ts: `export {};` (source comes in Phase 63)

    --- @kehto/services ---
    package.json:
    ```json
    {
      "name": "@kehto/services",
      "version": "0.1.0",
      "description": "Reference service implementations for the napplet protocol — audio, notifications, and extensible service handlers",
      "type": "module",
      "main": "./dist/index.js",
      "module": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "exports": {
        ".": {
          "types": "./dist/index.d.ts",
          "import": "./dist/index.js"
        }
      },
      "files": ["dist"],
      "sideEffects": false,
      "publishConfig": {
        "access": "public"
      },
      "dependencies": {
        "@kehto/runtime": "workspace:*"
      },
      "peerDependencies": {
        "@napplet/core": ">=0.1.0"
      },
      "devDependencies": {
        "tsup": "^8.5.0",
        "typescript": "^5.9.3"
      },
      "scripts": {
        "build": "tsup",
        "type-check": "tsc --noEmit",
        "test:unit": "echo 'no unit tests yet'"
      },
      "license": "MIT",
      "repository": {
        "type": "git",
        "url": "git+https://github.com/kehto/runtime.git",
        "directory": "packages/services"
      },
      "keywords": ["nostr", "napplet", "kehto", "services", "audio", "notifications"]
    }
    ```
    src/index.ts: `export {};` (source comes in Phase 63)

    After creating all files, run:
    ```bash
    cd ~/Develop/kehto && pnpm install && pnpm build && pnpm type-check
    ```

    Then create initial git commit:
    ```bash
    cd ~/Develop/kehto && git add -A && git commit -m "chore: initialize kehto monorepo with package shells"
    ```

    Note on @napplet/core peer dependency: runtime, shell, and services declare @napplet/core as a peerDependency. In Phase 62 the stub index.ts files don't import it, so build/type-check will pass without it installed. Phase 63 will add a workspace override to link it from the @napplet repo during development.
  </action>
  <verify>
    <automated>cd ~/Develop/kehto && pnpm build 2>&1 | tail -5 && pnpm type-check 2>&1 | tail -5 && echo "---" && grep '"name": "@kehto/acl"' packages/acl/package.json && grep '"name": "@kehto/runtime"' packages/runtime/package.json && grep '"name": "@kehto/shell"' packages/shell/package.json && grep '"name": "@kehto/services"' packages/services/package.json && echo "---" && grep '"@kehto/acl": "workspace:\*"' packages/runtime/package.json && grep '"@kehto/runtime": "workspace:\*"' packages/shell/package.json && grep '"@kehto/runtime": "workspace:\*"' packages/services/package.json && echo "---" && ls packages/acl/dist/index.js packages/runtime/dist/index.js packages/shell/dist/index.js packages/services/dist/index.js</automated>
  </verify>
  <acceptance_criteria>
    - packages/acl/package.json has "name": "@kehto/acl", "type": "module", "sideEffects": false
    - packages/runtime/package.json has "name": "@kehto/runtime", "@kehto/acl": "workspace:*" in dependencies
    - packages/runtime/package.json has "@napplet/core": ">=0.1.0" in peerDependencies
    - packages/shell/package.json has "name": "@kehto/shell", "@kehto/runtime": "workspace:*" in dependencies
    - packages/shell/package.json has "nostr-tools": ">=2.23.3 <3.0.0" in peerDependencies
    - packages/services/package.json has "name": "@kehto/services", "@kehto/runtime": "workspace:*" in dependencies
    - All 4 packages have tsconfig.json extending "../../tsconfig.json"
    - All 4 packages have tsup.config.ts with format: ['esm'], dts: true
    - All 4 packages have src/index.ts that exports (even if empty)
    - `pnpm build` succeeds — dist/index.js exists in all 4 packages
    - `pnpm type-check` succeeds with zero errors
    - All repository URLs point to github.com/kehto/runtime
    - git log shows at least one commit
  </acceptance_criteria>
  <done>Four @kehto package directories exist with valid package.json, tsconfig, tsup config, and stub source. pnpm build and type-check both succeed. Git repo has initial commit.</done>
</task>

</tasks>

<verification>
Run from ~/Develop/kehto:
1. `git log --oneline` — shows initial commit
2. `pnpm build` — all 4 packages build to dist/
3. `pnpm type-check` — zero TypeScript errors
4. `ls packages/*/dist/index.js` — all 4 dist files exist
5. `grep -r '"type": "module"' packages/*/package.json` — all ESM-only
6. `grep -r '"strict": true' tsconfig.json` — strict mode enabled
7. `cat pnpm-lock.yaml | head -1` — lockfile exists
</verification>

<success_criteria>
- ~/Develop/kehto is a git repo with at least one commit
- pnpm install creates pnpm-lock.yaml
- pnpm build succeeds across all 4 packages (dist/index.js in each)
- pnpm type-check passes with zero errors
- All package.json files have "type": "module" and correct @kehto scope names
- Inter-package workspace:* dependencies match the expected graph (runtime->acl, shell->runtime, services->runtime)
- @napplet/core listed as peerDependency in runtime, shell, services
</success_criteria>

<output>
After completion, create `.planning/phases/62-kehto-repo-scaffold/62-01-SUMMARY.md`
</output>
