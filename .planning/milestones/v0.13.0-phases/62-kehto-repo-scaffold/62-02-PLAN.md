---
phase: 62-kehto-repo-scaffold
plan: 02
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - ~/Develop/kehto/CLAUDE.md
  - ~/Develop/kehto/.planning/PROJECT.md
  - ~/Develop/kehto/.planning/REQUIREMENTS.md
  - ~/Develop/kehto/.planning/STATE.md
autonomous: true
requirements: [KEHTO-08]

must_haves:
  truths:
    - "~/Develop/kehto has a CLAUDE.md that describes the project, tech stack, and conventions"
    - "~/Develop/kehto/.planning/ directory contains PROJECT.md, REQUIREMENTS.md, and STATE.md"
    - "A future /gsd:new-project run can read these files and understand what kehto is"
  artifacts:
    - path: "~/Develop/kehto/CLAUDE.md"
      provides: "Project overview, tech stack, conventions for Claude agents"
      contains: "kehto"
    - path: "~/Develop/kehto/.planning/PROJECT.md"
      provides: "GSD project definition with core value, constraints, packages"
      contains: "Core Value"
    - path: "~/Develop/kehto/.planning/REQUIREMENTS.md"
      provides: "Placeholder requirements file for future milestones"
      contains: "Requirements"
    - path: "~/Develop/kehto/.planning/STATE.md"
      provides: "GSD state tracking file"
      contains: "Project State"
  key_links:
    - from: "~/Develop/kehto/CLAUDE.md"
      to: "~/Develop/kehto/.planning/PROJECT.md"
      via: "GSD section reference"
      pattern: "PROJECT.md"
---

<objective>
Seed ~/Develop/kehto with CLAUDE.md and .planning/ directory so future /gsd:new-project or /gsd:plan-phase invocations have enough context to understand the project, its packages, constraints, and conventions.

Purpose: Bootstrap GSD workflow context so kehto can be planned and executed as an independent project.
Output: CLAUDE.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/STATE.md committed to the kehto repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-kehto-repo-scaffold/62-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLAUDE.md with project overview, tech stack, conventions, and GSD enforcement</name>
  <read_first>
    /home/sandwich/Develop/napplet/CLAUDE.md
    /home/sandwich/Develop/kehto/package.json
  </read_first>
  <files>
    ~/Develop/kehto/CLAUDE.md
  </files>
  <action>
    Create ~/Develop/kehto/CLAUDE.md modeled after @napplet's CLAUDE.md but tailored for the kehto project.

    The file must contain these sections:

    **## Project Overview**
    ```
    This is the **kehto** monorepo — runtime packages for the napplet protocol. Kehto provides the server-side (shell) runtime that hosts napplet sandboxed iframe apps, including the protocol engine, ACL enforcement, service handlers, and browser adapter.

    Extracted from [@napplet](https://github.com/sandwichfarm/napplet) (v0.13.0 milestone). The @napplet repo retains the portable SDK packages (core, shim, sdk, vite-plugin); this repo contains the runtime and shell implementation.
    ```

    **## Packages**
    ```
    - `packages/acl` — **@kehto/acl** — Pure ACL module (zero deps, WASM-ready)
    - `packages/runtime` — **@kehto/runtime** — Protocol engine (message dispatch, AUTH, subscription lifecycle)
    - `packages/shell` — **@kehto/shell** — Browser adapter (ShellBridge, signer proxy, storage proxy)
    - `packages/services` — **@kehto/services** — Reference service handlers (audio, notifications)
    ```

    **## Tech Stack**
    ```
    - **TypeScript** (strict, ESM-only)
    - **tsup** for building each package
    - **turborepo** for monorepo orchestration
    - **pnpm** workspaces
    - **changesets** for versioning and publishing
    - **@napplet/core** as peer dependency (protocol types and constants)
    - **nostr-tools** as peer dependency (shell package)
    ```

    **## Build & Test**
    ```bash
    pnpm install
    pnpm build          # Build all via turborepo
    pnpm type-check     # TypeScript validation
    ```

    **## Relationship to @napplet**
    ```
    - This repo contains the **runtime/shell implementation** packages
    - @napplet contains the **portable SDK** (core, shim, sdk, vite-plugin)
    - @napplet/core is this repo's foundational dependency (protocol types, constants, message definitions)
    - Both repos follow identical code conventions and monorepo tooling
    ```

    **## Code Conventions**
    ```
    - ESM-only (no CJS output)
    - Zero framework dependencies (no Svelte, React, etc.)
    - All public API exports have JSDoc with @param, @returns, @example
    - 2-space indentation
    - TypeScript strict mode with verbatimModuleSyntax
    - File naming: lowercase with hyphens (acl-store.ts, storage-proxy.ts)
    - PascalCase for interfaces/types, camelCase for functions/variables
    - UPPER_SNAKE_CASE for constants
    ```

    **## GSD Workflow Enforcement** (same block as @napplet):
    ```
    Before using Edit, Write, or other file-changing tools, start work through a GSD command so planning artifacts and execution context stay in sync.

    Use these entry points:
    - `/gsd:quick` for small fixes, doc updates, and ad-hoc tasks
    - `/gsd:debug` for investigation and bug fixing
    - `/gsd:execute-phase` for planned phase work

    Do not make direct repo edits outside a GSD workflow unless the user explicitly asks to bypass it.
    ```

    Do NOT include the full Architecture or Conventions GSD sections from @napplet — those will be generated by /gsd:new-project once kehto has source code (Phase 63+).
  </action>
  <verify>
    <automated>cd ~/Develop/kehto && grep -c 'kehto' CLAUDE.md && grep '@kehto/acl' CLAUDE.md && grep '@kehto/runtime' CLAUDE.md && grep '@kehto/shell' CLAUDE.md && grep '@kehto/services' CLAUDE.md && grep '@napplet/core' CLAUDE.md && grep 'ESM-only' CLAUDE.md && grep 'pnpm install' CLAUDE.md && grep 'GSD Workflow' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - ~/Develop/kehto/CLAUDE.md exists
    - Contains "kehto" in project overview
    - Lists all 4 packages: @kehto/acl, @kehto/runtime, @kehto/shell, @kehto/services
    - Mentions @napplet/core as peer dependency
    - Documents ESM-only constraint
    - Includes Build & Test section with `pnpm install`, `pnpm build`, `pnpm type-check`
    - Includes Relationship to @napplet section
    - Includes Code Conventions section with 2-space indentation, strict mode
    - Includes GSD Workflow Enforcement section
    - Does NOT contain GSD:stack-start or GSD:conventions-start markers (those come from /gsd:new-project later)
  </acceptance_criteria>
  <done>CLAUDE.md created with project overview, package list, tech stack, conventions, build instructions, and GSD enforcement — sufficient for any Claude agent to understand the kehto project.</done>
</task>

<task type="auto">
  <name>Task 2: Create .planning/ directory with PROJECT.md, REQUIREMENTS.md, and STATE.md</name>
  <read_first>
    /home/sandwich/Develop/napplet/.planning/PROJECT.md
    /home/sandwich/Develop/napplet/.planning/STATE.md
    /home/sandwich/Develop/napplet/.planning/REQUIREMENTS.md
    /home/sandwich/Develop/kehto/CLAUDE.md
  </read_first>
  <files>
    ~/Develop/kehto/.planning/PROJECT.md
    ~/Develop/kehto/.planning/REQUIREMENTS.md
    ~/Develop/kehto/.planning/STATE.md
  </files>
  <action>
    IMPORTANT: All files go in ~/Develop/kehto/.planning/, NOT the @napplet .planning/.

    1. Create ~/Develop/kehto/.planning/PROJECT.md:

       ```markdown
       # Project: Kehto Runtime

       **Kehto** is the runtime half of the napplet protocol ecosystem. It provides the shell-side packages that host sandboxed Nostr iframe applications: protocol engine, ACL enforcement, service handlers, and browser adapter.

       ## Core Value

       Provide a modular, framework-agnostic runtime for hosting napplet applications — so any Nostr client can embed sandboxed mini-apps by integrating @kehto/shell.

       ## Packages

       | Package | Scope | Description |
       |---------|-------|-------------|
       | @kehto/acl | `packages/acl` | Pure ACL module — zero deps, WASM-ready capability enforcement |
       | @kehto/runtime | `packages/runtime` | Protocol engine — message dispatch, AUTH, subscriptions, ACL gates |
       | @kehto/shell | `packages/shell` | Browser adapter — ShellBridge, signer proxy, storage proxy, audio |
       | @kehto/services | `packages/services` | Reference service handlers — audio, notifications, extensible |

       ## Constraints

       - **ESM-only**: No CJS output
       - **Zero framework deps**: Framework-agnostic (no Svelte, React, Vue)
       - **@napplet/core peer dep**: All packages depend on @napplet/core for protocol types
       - **nostr-tools peer dep**: Shell depends on nostr-tools >=2.23.3 <3.0.0
       - **Monorepo tooling**: pnpm workspaces + turborepo + tsup + changesets

       ## Relationship to @napplet

       This repo was extracted from the [@napplet monorepo](https://github.com/sandwichfarm/napplet) during the v0.13.0 Runtime Decoupling milestone. The split:

       - **@napplet** (github.com/sandwichfarm/napplet): Portable SDK — core, shim, sdk, vite-plugin
       - **@kehto** (github.com/kehto/runtime): Runtime implementation — acl, runtime, shell, services

       @napplet/core is the shared foundation. It lives in @napplet and is consumed by @kehto as a peer dependency.

       ## Key Decisions

       | # | Decision | Rationale | Date |
       |---|----------|-----------|------|
       | 1 | Separate repo (not monorepo subfolder) | Independent release cadence, different maintainers possible | 2026-04-06 |
       | 2 | @napplet/core as peer dep | Core types shared; @napplet publishes, @kehto consumes | 2026-04-06 |
       | 3 | Mirror @napplet tooling exactly | Consistency, easy contributor onboarding | 2026-04-06 |
       ```

    2. Create ~/Develop/kehto/.planning/REQUIREMENTS.md:

       ```markdown
       # Requirements: Kehto Runtime

       **Defined:** 2026-04-06
       **Core Value:** Modular, framework-agnostic runtime for hosting napplet applications.

       ## Seed Requirements

       These requirements are placeholders from the initial scaffold. A proper milestone and requirements breakdown will be created when kehto gets its own /gsd:new-project run.

       - [x] **SCAFFOLD-01**: Monorepo initialized with pnpm, turborepo, TypeScript, ESM-only config
       - [x] **SCAFFOLD-02**: Four package directories created with valid package.json
       - [x] **SCAFFOLD-03**: GSD planning context seeded (this file, PROJECT.md, STATE.md)

       ## Future Requirements

       - Source migration from @napplet (tracked in @napplet v0.13.0 as KEHTO-03 through KEHTO-07)
       - CI/CD pipeline and npm publishing
       - Independent test suite
       - Documentation and README

       ---
       *Requirements defined: 2026-04-06*
       ```

    3. Create ~/Develop/kehto/.planning/STATE.md:

       ```markdown
       ---
       gsd_state_version: 1.0
       milestone: scaffold
       milestone_name: Initial Scaffold
       status: seeded
       stopped_at: Repo scaffolded from @napplet v0.13.0 Phase 62
       last_updated: "2026-04-06"
       last_activity: 2026-04-06
       progress:
         total_phases: 0
         completed_phases: 0
         total_plans: 0
         completed_plans: 0
         percent: 0
       ---

       # Project State

       ## Project Reference

       See: .planning/PROJECT.md

       **Core value:** Modular, framework-agnostic runtime for hosting napplet applications.
       **Current focus:** Seeded by @napplet v0.13.0 Phase 62 — awaiting source migration (Phase 63).

       ## Current Position

       Phase: — (no phases yet — source migration happens from @napplet repo)
       Plan: —
       Status: Seeded
       Last activity: 2026-04-06 — Repository scaffolded

       Progress: [----------] 0%

       ## Accumulated Context

       ### Decisions

       Decisions are logged in PROJECT.md Key Decisions table.

       ### Blockers/Concerns

       - Source migration (Phase 63) happens from @napplet repo, not from here
       - @napplet/core not yet published to npm — Phase 63 uses workspace override
       - No CI/CD yet — will be set up in a future kehto milestone

       ## Session Continuity

       Last session: 2026-04-06
       Stopped at: Repository scaffolded
       Resume: Source migration driven from @napplet Phase 63
       ```

    After creating all files, commit in kehto:
    ```bash
    cd ~/Develop/kehto && git add CLAUDE.md .planning/ && git commit -m "docs: seed CLAUDE.md and GSD planning context"
    ```
  </action>
  <verify>
    <automated>cd ~/Develop/kehto && test -f CLAUDE.md && test -f .planning/PROJECT.md && test -f .planning/REQUIREMENTS.md && test -f .planning/STATE.md && grep 'Core Value' .planning/PROJECT.md && grep 'SCAFFOLD-01' .planning/REQUIREMENTS.md && grep 'gsd_state_version' .planning/STATE.md && grep '@kehto/acl' .planning/PROJECT.md && grep '@kehto/runtime' .planning/PROJECT.md && grep '@kehto/shell' .planning/PROJECT.md && grep '@kehto/services' .planning/PROJECT.md && git log --oneline | head -5</automated>
  </verify>
  <acceptance_criteria>
    - ~/Develop/kehto/CLAUDE.md exists (created in Task 1)
    - ~/Develop/kehto/.planning/PROJECT.md exists and contains:
      - "Core Value" section
      - Package table listing all 4 @kehto packages
      - Constraints section mentioning ESM-only and @napplet/core peer dep
      - Key Decisions table with at least 3 decisions
    - ~/Develop/kehto/.planning/REQUIREMENTS.md exists and contains:
      - SCAFFOLD-01, SCAFFOLD-02, SCAFFOLD-03 requirement IDs
      - Future Requirements section mentioning source migration
    - ~/Develop/kehto/.planning/STATE.md exists and contains:
      - gsd_state_version: 1.0 in frontmatter
      - status: seeded
      - Reference to @napplet Phase 62 and Phase 63
    - git log shows the docs commit
  </acceptance_criteria>
  <done>CLAUDE.md and .planning/ directory seeded with PROJECT.md, REQUIREMENTS.md, and STATE.md — sufficient for a future /gsd:new-project to bootstrap kehto as an independent project.</done>
</task>

</tasks>

<verification>
Run from ~/Develop/kehto:
1. `cat CLAUDE.md | head -3` — starts with project overview
2. `cat .planning/PROJECT.md | grep "Core Value"` — has core value
3. `cat .planning/REQUIREMENTS.md | grep "SCAFFOLD"` — has seed requirements
4. `cat .planning/STATE.md | grep "gsd_state_version"` — valid state file
5. `git log --oneline` — shows both commits (scaffold + docs)
</verification>

<success_criteria>
- CLAUDE.md describes kehto project accurately (packages, stack, conventions, GSD enforcement)
- .planning/PROJECT.md has Core Value, Packages table, Constraints, Relationship to @napplet, Key Decisions
- .planning/REQUIREMENTS.md has seed requirements and future requirements placeholder
- .planning/STATE.md has valid GSD frontmatter and references the scaffold origin
- All files committed to git in ~/Develop/kehto
</success_criteria>

<output>
After completion, create `.planning/phases/62-kehto-repo-scaffold/62-02-SUMMARY.md`
</output>
