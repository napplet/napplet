---
phase: 67-cross-repo-wiring-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/sandwich/Develop/kehto/packages/acl/package.json
  - /home/sandwich/Develop/kehto/packages/runtime/package.json
  - /home/sandwich/Develop/kehto/packages/shell/package.json
  - /home/sandwich/Develop/kehto/packages/services/package.json
  - README.md
  - packages/core/README.md
  - packages/shim/README.md
  - packages/sdk/README.md
  - packages/vite-plugin/README.md
autonomous: true
requirements: [KEHTO-04, DOC-01, DOC-02]

must_haves:
  truths:
    - "Root README describes exactly 4 packages: core, shim, sdk, vite-plugin"
    - "Root README explains that @kehto packages provide the shell/runtime layer"
    - "Package READMEs for core, shim, sdk, and vite-plugin direct users to @kehto for runtime/shell integration"
    - "No references to @napplet/runtime, @napplet/shell, @napplet/acl, or @napplet/services remain in any @napplet README"
    - "All @kehto packages that import from @napplet/core declare it as a peerDependency"
  artifacts:
    - path: "README.md"
      provides: "4-package SDK overview with @kehto cross-reference"
      contains: "@kehto"
    - path: "packages/core/README.md"
      provides: "Core package docs with @kehto integration note"
      contains: "@kehto"
    - path: "packages/shim/README.md"
      provides: "Shim package docs redirecting shell references to @kehto"
      contains: "@kehto"
    - path: "packages/sdk/README.md"
      provides: "SDK package docs redirecting shell references to @kehto"
      contains: "@kehto"
    - path: "packages/vite-plugin/README.md"
      provides: "Vite plugin docs redirecting shell references to @kehto"
      contains: "@kehto"
  key_links:
    - from: "README.md"
      to: "@kehto packages"
      via: "cross-reference in Related section and package table"
      pattern: "@kehto"
    - from: "packages/*/README.md"
      to: "@kehto"
      via: "Integration Note sections"
      pattern: "@kehto/(runtime|shell)"
---

<objective>
Cross-repo wiring and documentation updates for the @napplet / @kehto split.

Purpose: This is the final phase of the v0.13.0 milestone. The @napplet monorepo now contains 4 packages (core, shim, sdk, vite-plugin) but all documentation still references the old 7-package structure. The @kehto repo already has @napplet/core declared as a peerDependency in the packages that need it, but @kehto/acl also needs a peerDependency declaration for completeness. All READMEs must be updated to reflect the split and direct users to @kehto for runtime/shell integration.

Output: Updated READMEs in @napplet repo, verified @kehto peerDependency declarations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-cross-repo-wiring-docs/67-CONTEXT.md

@README.md
@packages/core/README.md
@packages/shim/README.md
@packages/sdk/README.md
@packages/vite-plugin/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and complete @kehto peerDependency declarations</name>
  <files>/home/sandwich/Develop/kehto/packages/acl/package.json, /home/sandwich/Develop/kehto/packages/runtime/package.json, /home/sandwich/Develop/kehto/packages/shell/package.json, /home/sandwich/Develop/kehto/packages/services/package.json</files>
  <action>
    Verify that all @kehto packages which import from @napplet/core have it declared as a peerDependency (per KEHTO-04).

    Current state (already done in Phase 63):
    - @kehto/runtime: has `"peerDependencies": { "@napplet/core": ">=0.1.0" }` -- CORRECT
    - @kehto/shell: has `"peerDependencies": { "@napplet/core": ">=0.1.0" }` -- CORRECT
    - @kehto/services: has `"peerDependencies": { "@napplet/core": ">=0.1.0" }` -- CORRECT
    - @kehto/acl: has NO peerDependency on @napplet/core -- CHECK if it imports from @napplet/core

    Steps:
    1. Check if @kehto/acl source imports from @napplet/core: `grep -r "@napplet/core" /home/sandwich/Develop/kehto/packages/acl/src/`
    2. If it does import @napplet/core, add `"peerDependencies": { "@napplet/core": ">=0.1.0" }` to packages/acl/package.json
    3. If it does NOT import @napplet/core, leave it alone (acl is zero-dep by design)
    4. Verify the workspace override in kehto root package.json still has `"@napplet/core": "link:/home/sandwich/Develop/napplet/packages/core"` -- this stays until PUB-04 is completed
    5. Run `cd /home/sandwich/Develop/kehto && pnpm build` to confirm everything still builds

    Note: The full npm switch (removing workspace override, resolving from npm) is blocked on PUB-04. The workspace link stays. This task only ensures peerDependency declarations are complete.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/kehto && pnpm build && pnpm type-check</automated>
  </verify>
  <done>
    All @kehto packages that import @napplet/core declare it as a peerDependency. Build and type-check pass. Workspace override remains (PUB-04 not yet complete).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all @napplet READMEs for the 4-package SDK</name>
  <files>README.md, packages/core/README.md, packages/shim/README.md, packages/sdk/README.md, packages/vite-plugin/README.md</files>
  <action>
    Read each README before editing. Update all READMEs to reflect the @napplet/@kehto split (per DOC-01 and DOC-02).

    **Root README.md (DOC-01):**
    Rewrite to describe the 4-package SDK. Specific changes:
    - Package table: Remove @napplet/shell, @napplet/acl, @napplet/runtime, @napplet/services rows. Keep core, shim, sdk, vite-plugin.
    - Architecture section: Replace the 7-package dependency graph with a simpler 4-package graph showing core at the base, shim and sdk depending on core, vite-plugin standalone. Remove the "Runtime Communication Flow" diagram that shows @napplet/shell and @napplet/runtime -- replace with a simplified diagram showing the napplet-side only (shim/sdk talk to a shell via postMessage). Mention @kehto as the reference shell implementation.
    - Origin section: Keep the hyprgate reference. Add a note about the @kehto extraction.
    - Related section: Replace "@napplet/create" hyprgate reference with @kehto references:
      - **@kehto** -- Shell-side runtime packages (@kehto/runtime, @kehto/shell, @kehto/acl, @kehto/services)
      - Keep hyprgate reference
      - Keep NIP spec reference
    - Development section: Keep as-is (pnpm build, pnpm type-check, changesets)
    - Keep the iframe sandbox paragraph but reword to say "the host shell" instead of referencing specific @napplet/shell package

    **packages/core/README.md (DOC-02):**
    - Integration Note section at bottom: Replace the paragraph that references @napplet/acl, @napplet/runtime, @napplet/shell with updated text explaining that @napplet/core is consumed by @napplet/shim, @napplet/sdk, and @napplet/vite-plugin in this repo, and by @kehto/runtime, @kehto/shell, and @kehto/services in the @kehto repo.
    - Protocol Reference: Keep as-is (links to RUNTIME-SPEC.md and NIP-01)

    **packages/shim/README.md (DOC-02):**
    - Prerequisites: Change "[@napplet/runtime](https://www.npmjs.com/package/@napplet/runtime) (or [@napplet/shell]...)" to "[@kehto/shell](https://github.com/sandwichfarm/kehto) or another napplet protocol shell implementation"
    - Protocol Reference: Keep RUNTIME-SPEC.md and NIP links

    **packages/sdk/README.md (DOC-02):**
    - Prerequisites: Change "[@napplet/runtime](https://www.npmjs.com/package/@napplet/runtime) or [@napplet/shell]..." to "[@kehto/shell](https://github.com/sandwichfarm/kehto) or another napplet protocol shell implementation"
    - Keep everything else as-is

    **packages/vite-plugin/README.md (DOC-02):**
    - "What This Plugin Does" and surrounding text: Check for any references to @napplet/shell and replace with @kehto/shell
    - The line "testing locally with @napplet/shell" in "When to Use This": Change to "testing locally with @kehto/shell"
    - Runtime compatibility checking code example: The `import { hasService } from '@napplet/shim'` line is incorrect (shim has no named exports). Change to show `window.napplet.services.has('audio')` pattern instead.

    **Rules for all READMEs:**
    - Do NOT add emojis
    - Keep existing formatting style (ATX headings, pipe tables, fenced code blocks)
    - Use `@kehto` org name consistently (not "kehto")
    - Link to https://github.com/sandwichfarm/kehto for @kehto references
    - Keep MIT license line
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -r "@napplet/runtime\|@napplet/shell\|@napplet/acl\|@napplet/services" README.md packages/core/README.md packages/shim/README.md packages/sdk/README.md packages/vite-plugin/README.md | grep -v "@kehto" | grep -v "RUNTIME-SPEC" | grep -v "was extracted" | grep -v "previously lived" || echo "PASS: No stale @napplet package references found"</automated>
  </verify>
  <done>
    Root README describes exactly 4 packages (core, shim, sdk, vite-plugin) with @kehto cross-references. All package READMEs direct users to @kehto for runtime/shell needs. No stale references to removed packages remain.
  </done>
</task>

</tasks>

<verification>
1. `cd /home/sandwich/Develop/kehto && pnpm build && pnpm type-check` -- kehto still builds
2. `grep -c "@kehto" /home/sandwich/Develop/napplet/README.md` -- returns > 0 (cross-references exist)
3. `grep -c "@kehto" /home/sandwich/Develop/napplet/packages/core/README.md` -- returns > 0
4. `grep -c "@kehto" /home/sandwich/Develop/napplet/packages/shim/README.md` -- returns > 0
5. `grep -c "@kehto" /home/sandwich/Develop/napplet/packages/sdk/README.md` -- returns > 0
6. `grep -c "@kehto" /home/sandwich/Develop/napplet/packages/vite-plugin/README.md` -- returns > 0
7. No remaining references to @napplet/runtime, @napplet/shell, @napplet/acl, or @napplet/services in any @napplet README (excluding historical/explanatory mentions)
</verification>

<success_criteria>
- All @kehto packages with @napplet/core imports declare it as peerDependency (KEHTO-04 partial)
- Root README describes 4-package @napplet SDK with @kehto cross-references (DOC-01)
- Package READMEs for core, shim, sdk, vite-plugin direct to @kehto for runtime/shell (DOC-02)
- kehto build + type-check passes
- No stale references to removed @napplet packages in documentation
</success_criteria>

<output>
After completion, create `.planning/phases/67-cross-repo-wiring-docs/67-01-SUMMARY.md`
</output>
