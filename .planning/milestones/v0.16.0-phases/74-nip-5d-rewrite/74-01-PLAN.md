---
phase: 74-nip-5d-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/NIP-5D.md
autonomous: true
requirements: [SPEC-01, SPEC-02, SPEC-03, SPEC-04]

must_haves:
  truths:
    - "NIP-5D defines { type, ...payload } JSON envelope as the sole wire format"
    - "NIP-5D contains zero protocol message definitions (no verb tables, no REQ/EVENT/CLOSE/COUNT)"
    - "NIP-5D covers transport (postMessage), identity (MessageEvent.source), and manifest only"
    - "NIP-5D references NUBs as defining all protocol message types"
    - "NIP-5D defines NUB negotiation flow (manifest requires -> shell supports -> compatibility)"
    - "NIP-5D is concise enough for a NIP reviewer to understand in 5 minutes"
  artifacts:
    - path: "specs/NIP-5D.md"
      provides: "Complete NIP-5D v4 specification"
      contains: "{ type, ...payload }"
  key_links:
    - from: "specs/NIP-5D.md"
      to: "NIP-5A"
      via: "manifest reference for dTag, aggregateHash, requires tags"
      pattern: "NIP-5A"
    - from: "specs/NIP-5D.md"
      to: "NUB specs"
      via: "references NUBs for protocol messages"
      pattern: "NUB"
---

<objective>
Rewrite NIP-5D as a transport+identity+manifest+NUB-negotiation spec with generic JSON envelope. Remove all NIP-01 wire format tables and protocol message definitions. The spec should define HOW napplets and shells communicate (envelope format, postMessage transport, identity via MessageEvent.source) and HOW capabilities are negotiated (NUBs), but NOT what specific messages exist (that is NUB territory).

Purpose: NIP-5D currently contains relay verb tables from NIP-01. These must be replaced with the generic JSON envelope format, and protocol message definitions must move to NUB specs (Phase 75). This phase makes NIP-5D a clean transport+identity+negotiation spec.

Output: Rewritten `specs/NIP-5D.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-nip-5d-rewrite/74-CONTEXT.md
@specs/NIP-5D.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite NIP-5D spec</name>
  <files>specs/NIP-5D.md</files>
  <read_first>specs/NIP-5D.md, .planning/phases/74-nip-5d-rewrite/74-CONTEXT.md</read_first>
  <action>
Replace the entire contents of specs/NIP-5D.md with the following spec. This is the complete NIP-5D v4 text. Write it exactly as shown, then review the result for any formatting issues.

```
NIP-5D
======

Nostr Web Applets
-----------------

`draft` `optional`

This NIP defines a protocol for sandboxed web applications ("napplets") running in iframes to communicate with a hosting application ("shell") via postMessage using a generic JSON envelope. Protocol messages are defined by NUB (Napplet Unified Blueprint) extension specs.

## Terminology

| Term | Definition |
|------|------------|
| Shell | Web application hosting napplet iframes |
| Napplet | Sandboxed iframe application communicating with the shell via postMessage |
| dTag | Napplet type identifier from the [NIP-5A](5A.md) manifest `d` tag |
| Aggregate hash | SHA-256 of napplet build files per [NIP-5A](5A.md) |
| NUB | Napplet Unified Blueprint -- extension spec defining protocol messages for a capability domain |

## Transport

Communication uses `postMessage`. Napplet to shell: `window.parent.postMessage(msg, '*')`. Shell to napplet: `iframeWindow.postMessage(msg, '*')`. The `'*'` target origin is required because napplets have opaque origins (no `allow-same-origin`).

Napplet iframes MUST use this sandbox attribute:

    sandbox="allow-scripts"

The `allow-same-origin` token MUST NOT be present. Shells MAY add additional sandbox tokens (`allow-forms`, `allow-modals`, `allow-downloads`, `allow-popups`) based on shell policy. Napplets have no access to `localStorage`, `sessionStorage`, `IndexedDB`, direct WebSocket connections, or `window.nostr`. All storage, signing, and relay access is proxied through the shell.

The shell identifies senders via `MessageEvent.source` (unforgeable Window reference). Messages from unknown sources (iframes not created by the shell) MUST be silently dropped.

## Wire Format

All messages between napplet and shell are JSON objects with a `type` field:

    { "type": "<domain>.<action>", ...payload }

The `type` field is a string discriminant in `domain.action` format. Domains correspond to NUB capability names (e.g., `relay`, `signer`, `storage`, `ifc`). NUB specs define the valid type strings and payload shapes for their domain. This NIP does not enumerate message types.

Example messages (defined by their respective NUB specs):

    { "type": "relay.subscribe", "id": "sub1", "filters": [...] }
    { "type": "relay.event", "id": "sub1", "event": {...} }
    { "type": "signer.sign", "id": "req1", "template": {...} }
    { "type": "storage.get", "key": "prefs" }

Messages with an unrecognized `type` MUST be silently ignored. This allows forward compatibility as new NUBs are defined.

## Identity

The shell assigns napplet identity at iframe creation time. No multi-step negotiation is required.

When the shell creates a napplet iframe, it maps the iframe's `Window` reference to the napplet's `(dTag, aggregateHash)` tuple. The shell derives an internal identity pubkey for each napplet using `HMAC-SHA256(shellSecret, dTag + aggregateHash)` where `shellSecret` is a 32-byte random value persisted per shell instance. Same dTag + same aggregateHash + same shell = same identity.

Napplets send unsigned event templates (kind, created_at, tags, content) without id, pubkey, or sig fields. The shell stamps inbound messages with the napplet's derived pubkey internally. Events signed with a user's key ([NIP-07](07.md)/NIP-46) go through the shell's signer proxy, not the napplet's identity.

The shell MUST verify `MessageEvent.source` on every inbound message. Messages from Window references not mapped to a napplet identity MUST be silently dropped.

## Manifest and NUB Negotiation

Napplet manifests ([NIP-5A](5A.md) kind 35128) declare required capabilities using `requires` tags:

    ["requires", "relay"]
    ["requires", "signer"]
    ["requires", "storage"]

Each `requires` value is a short NUB name: `relay`, `signer`, `storage`, `ifc`. Manifests MUST NOT use spec identifiers like `NUB-RELAY`.

At napplet load time, the shell checks `requires` tags against its own capabilities. If a required capability is absent, the shell SHOULD reject the napplet or display a compatibility warning. If the manifest has no `requires` tags, the shell loads the napplet with whatever capabilities it provides.

### Runtime Capability Query

Napplets query capability support at runtime:

    window.napplet.shell.supports('relay')    // boolean
    window.napplet.shell.supports('signer')   // boolean

This covers both NUB capabilities and sandbox permissions:

    window.napplet.shell.supports('popups')   // boolean

Shells MUST implement `window.napplet.shell.supports()`. Napplets MUST gracefully degrade when a capability is absent.

Service discovery (e.g., audio, notifications) uses a separate API:

    window.napplet.services.has('audio')      // boolean

## NUB Extension Framework

Protocol messages are defined by NUB (Napplet Unified Blueprint) specs. Each NUB owns a message domain and defines the `type` strings, payload shapes, and semantics for that domain:

| NUB | Domain | Defines |
|-----|--------|---------|
| NUB-RELAY | `relay` | Relay proxy: subscribe, publish, event delivery, close |
| NUB-SIGNER | `signer` | Signing delegation: sign request, response, get-public-key |
| NUB-STORAGE | `storage` | Scoped storage: get, set, delete, keys |
| NUB-IFC | `ifc` | Inter-frame communication: dispatch and channel modes |

NUB specs are maintained at [github.com/napplet/nubs](https://github.com/napplet/nubs). Each NUB spec is self-contained and references this NIP only for envelope format and transport.

## Security Considerations

Napplets are untrusted code. The shell is trusted. The browser enforces iframe sandbox boundaries. `MessageEvent.source` provides unforgeable sender identity.

**Mitigations:**
1. Iframe sandbox: `allow-scripts` only -- shells MUST NOT add `allow-same-origin`.
2. postMessage `'*'` origin is required for opaque-origin iframes; sender identification uses `MessageEvent.source`, NOT `event.origin`.
3. Identity binding: the shell maps `MessageEvent.source` to napplet identity at iframe creation. The browser's `MessageEvent.source` is unforgeable within the same browsing context.
4. Internal identity confinement: events stamped with shell-derived napplet pubkeys MUST NOT be published to external relays; the user's signer ([NIP-07](07.md)/NIP-46) is the only source of externally published events.
5. Aggregate hash verification against [NIP-5A](5A.md) manifests; mismatch MAY result in napplet rejection.
6. Unrecognized message types are silently ignored, preventing capability probing.

Storage isolation, signing safety, relay access control, and ACL enforcement are defined by their respective NUB specs.

**Non-Guarantees:** The protocol does NOT protect against a compromised browser, a malicious shell, side-channel attacks, or social engineering.

## References

- [NIP-01](01.md) -- Basic protocol flow
- [NIP-07](07.md) -- `window.nostr` signer capability
- [NIP-5A](5A.md) -- Napplet manifest format and aggregate hash
```

IMPORTANT per CONTEXT.md decisions:
- Sandbox attribute MUST say `sandbox="allow-scripts"` only (per D: Sandbox Policy). The shell MAY add more tokens. Do NOT include allow-forms/allow-modals/allow-downloads in the MUST line.
- NUB short names: `relay`, `signer`, `storage`, `ifc` -- NOT spec identifiers like `NUB-RELAY` in requires tags (per D: NUB Negotiation).
- `window.napplet.shell.supports()` covers both NUB capabilities and sandbox permissions (per D: NUB Negotiation).
- Services keep separate API: `window.napplet.services.has('audio')` (per D: NUB Negotiation).
- NIP-45 removed from References since COUNT is no longer mentioned (it was a relay verb, now defined by NUB-RELAY).
- Wire format type field uses `domain.action` format (per D: Wire Format, type is string discriminant like `"relay.subscribe"`, `"signer.sign"`).
  </action>
  <verify>
    <automated>
echo "=== SPEC-01: JSON envelope defined ===" && grep -c '{ "type"' specs/NIP-5D.md && echo "=== SPEC-02: No NIP-01 verb tables ===" && (grep -cE '^\| `(EVENT|REQ|CLOSE|COUNT|OK|EOSE|CLOSED|NOTICE)` \|' specs/NIP-5D.md || echo "0 verb table rows - PASS") && echo "=== SPEC-02: No NIP-01 array format ===" && (grep -c '\["EVENT"' specs/NIP-5D.md || echo "0 array formats - PASS") && echo "=== SPEC-03: NUB referenced ===" && grep -c "NUB" specs/NIP-5D.md && echo "=== SPEC-04: Negotiation section exists ===" && grep -c "NUB Negotiation" specs/NIP-5D.md && echo "=== Sandbox: allow-scripts only ===" && grep -c 'sandbox="allow-scripts"' specs/NIP-5D.md && echo "=== No allow-same-origin ===" && grep -c "MUST NOT.*allow-same-origin" specs/NIP-5D.md
    </automated>
  </verify>
  <acceptance_criteria>
- grep -c '{ "type"' specs/NIP-5D.md returns >= 1 (JSON envelope defined)
- grep -cE '^\| `(EVENT|REQ|CLOSE)` \|' specs/NIP-5D.md returns 0 (no verb tables)
- grep -c '\["EVENT"' specs/NIP-5D.md returns 0 (no NIP-01 arrays)
- grep -c "NUB Negotiation" specs/NIP-5D.md returns 1 (negotiation section exists)
- grep -c 'sandbox="allow-scripts"' specs/NIP-5D.md returns 1 (minimal sandbox)
- grep -c "domain.action" specs/NIP-5D.md returns >= 1 (type format documented)
- grep -c "window.napplet.shell.supports" specs/NIP-5D.md returns >= 1 (runtime query)
- grep -c "window.napplet.services.has" specs/NIP-5D.md returns >= 1 (separate services API)
- wc -l < specs/NIP-5D.md returns <= 130 (concise spec)
  </acceptance_criteria>
  <done>NIP-5D rewritten with JSON envelope wire format, no protocol message definitions, NUB negotiation flow, and concise transport+identity+manifest scope</done>
</task>

<task type="auto">
  <name>Task 2: Verify spec internal consistency</name>
  <files>specs/NIP-5D.md</files>
  <read_first>specs/NIP-5D.md</read_first>
  <action>
Read the rewritten NIP-5D and verify the following consistency checks. If any check fails, fix the issue in-place.

1. **No NIP-01 wire format remnants**: Verify there are zero occurrences of `["EVENT"`, `["REQ"`, `["CLOSE"`, `["COUNT"`, `["OK"`, `["EOSE"`, `["CLOSED"`, `["NOTICE"`. The spec must not contain NIP-01 JSON array message format.

2. **No stale NIP references**: Every NIP mentioned in body text (NIP-01, NIP-07, NIP-5A, NIP-46) must either appear in the References section or be mentioned only inline without a hyperlink. NIP-45 should NOT appear (COUNT is now a relay NUB concern). NIP-42 should NOT appear (AUTH was removed in v0.15.0).

3. **No "handshake" or "AUTH"**: grep -ci "handshake" and grep -ci "\bAUTH\b" must return 0. These concepts were removed in v0.15.0.

4. **Terminology table complete**: Every term used capitalized in the spec (Shell, Napplet, dTag, Aggregate hash, NUB) must appear in the Terminology table.

5. **Short NUB names only in requires tags**: The requires tag examples must use short names (`relay`, `signer`, `storage`) not spec identifiers (`NUB-RELAY`). The NUB Extension Framework table may use full NUB names since that is the spec-level reference.

6. **Section order**: Verify the sections appear in this order: Terminology, Transport, Wire Format, Identity, Manifest and NUB Negotiation, NUB Extension Framework, Security Considerations, References.

7. **No "NIP-01 wire format" language**: The introductory sentence must say "generic JSON envelope" not "NIP-01 wire format". grep for "NIP-01 wire format" must return 0.

If all checks pass, report success. If any fail, make minimal targeted fixes.
  </action>
  <verify>
    <automated>
echo "=== Check 1: No NIP-01 arrays ===" && (grep -cE '\["(EVENT|REQ|CLOSE|COUNT|OK|EOSE|CLOSED|NOTICE)"' specs/NIP-5D.md && echo "FAIL: NIP-01 arrays found" || echo "PASS: 0 arrays") && echo "=== Check 2: No NIP-42 ===" && (grep -ci "NIP-42" specs/NIP-5D.md && echo "FAIL" || echo "PASS") && echo "=== Check 3: No NIP-45 ===" && (grep -ci "NIP-45" specs/NIP-5D.md && echo "FAIL" || echo "PASS") && echo "=== Check 4: No handshake ===" && (grep -ci "handshake" specs/NIP-5D.md && echo "FAIL" || echo "PASS") && echo "=== Check 5: No AUTH ===" && (grep -ciw "AUTH" specs/NIP-5D.md && echo "FAIL" || echo "PASS") && echo "=== Check 6: No NIP-01 wire format ===" && (grep -c "NIP-01 wire format" specs/NIP-5D.md && echo "FAIL" || echo "PASS") && echo "=== Check 7: JSON envelope in intro ===" && head -8 specs/NIP-5D.md | grep -c "JSON envelope" && echo "=== Check 8: Line count ===" && wc -l < specs/NIP-5D.md
    </automated>
  </verify>
  <acceptance_criteria>
- grep -cE '\["(EVENT|REQ|CLOSE|COUNT|OK|EOSE|CLOSED|NOTICE)"' specs/NIP-5D.md returns 0
- grep -ci "NIP-42" specs/NIP-5D.md returns 0
- grep -ci "NIP-45" specs/NIP-5D.md returns 0
- grep -ci "handshake" specs/NIP-5D.md returns 0
- grep -ciw "AUTH" specs/NIP-5D.md returns 0
- grep -c "NIP-01 wire format" specs/NIP-5D.md returns 0
- head -8 specs/NIP-5D.md | grep -c "JSON envelope" returns 1
  </acceptance_criteria>
  <done>NIP-5D passes all internal consistency checks: no NIP-01 remnants, no stale references, no removed concepts, correct section order, and concise length</done>
</task>

</tasks>

<verification>
After both tasks complete, the spec must satisfy all four requirements:
- SPEC-01: JSON envelope `{ type, ...payload }` is the defined wire format
- SPEC-02: Only transport, identity, manifest -- zero protocol message definitions
- SPEC-03: NUBs referenced as defining protocol messages
- SPEC-04: NUB negotiation flow defined (requires tags + shell.supports + compatibility)
</verification>

<success_criteria>
- specs/NIP-5D.md contains JSON envelope wire format definition
- specs/NIP-5D.md contains zero NIP-01 verb tables or array format examples
- specs/NIP-5D.md contains NUB negotiation section with requires tags and shell.supports()
- specs/NIP-5D.md is <= 130 lines (concise, 5-minute read)
- All grep-based acceptance criteria pass
</success_criteria>

<output>
After completion, create `.planning/phases/74-nip-5d-rewrite/74-01-SUMMARY.md`
</output>
