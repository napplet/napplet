---
phase: 32-fix-demo-ui-ux-bugs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "CLAUDE.md no longer describes NappKeypair as 'Stored in sessionStorage'"
    - "CLAUDE.md correctly describes NappKeypair as ephemeral in-memory keypair per page load"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Correct NappKeypair documentation"
      contains: "Ephemeral in-memory keypair"
---

<objective>
Fix stale CLAUDE.md documentation that incorrectly describes NappKeypair as using sessionStorage. The code was correctly refactored to ephemeral in-memory keypairs; only the documentation is wrong.

Purpose: The debug investigation confirmed that `packages/shim/src/napp-keypair.ts` generates a fresh ephemeral keypair on every call with no persistence. The CLAUDE.md description at the NappKeypair entry still says "Stored in sessionStorage" — this is a stale description from the pre-refactor version and actively misleads anyone reading the architecture section.

Output: Updated `CLAUDE.md` with corrected NappKeypair description.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-fix-demo-ui-ux-bugs/32-CONTEXT.md
@.planning/debug/demo-v060-correctness-issues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale NappKeypair description in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>CLAUDE.md</read_first>
  <action>
    Find the NappKeypair entry in CLAUDE.md (around line 235 per the debug report). It currently contains a description along the lines of "Stored in sessionStorage" or "session-scoped" keypair persistence.

    The debug investigation confirmed (`packages/shim/src/napp-keypair.ts`): `loadOrCreateKeypair()` generates a fresh ephemeral keypair on every call with `generateSecretKey()`. No sessionStorage is accessed. No persistence of any kind.

    Update the NappKeypair description in CLAUDE.md's Key Abstractions or Architecture section to accurately reflect:
    - **Old (wrong):** "Stored in sessionStorage; loaded/created deterministically per nappType; used to sign all outbound events"
    - **New (correct):** "Ephemeral in-memory keypair per page load (no persistence). Generated fresh on every page load via `generateSecretKey()`. Used to sign all outbound events from this napplet instance."

    Find the exact text by reading CLAUDE.md, then replace only the stale phrase. Do not change surrounding context, formatting, or other entries.
  </action>
  <verify>grep -n 'sessionStorage\|Ephemeral in-memory' CLAUDE.md | head -10</verify>
  <acceptance_criteria>
    - `CLAUDE.md` does NOT contain "Stored in sessionStorage" in the NappKeypair description
    - `CLAUDE.md` contains "Ephemeral in-memory keypair" near the NappKeypair entry
    - No other content in CLAUDE.md changed
  </acceptance_criteria>
  <done>CLAUDE.md NappKeypair description updated to reflect ephemeral in-memory behavior</done>
</task>

</tasks>

<verification>
- [ ] grep -i 'sessionStorage' CLAUDE.md | grep -i 'napp.*keypair\|keypair.*napp' returns empty (no stale sessionStorage reference in keypair context)
- [ ] grep 'Ephemeral in-memory' CLAUDE.md returns the updated line
</verification>

<success_criteria>
- CLAUDE.md NappKeypair section no longer references sessionStorage
- Updated description accurately describes the ephemeral in-memory behavior
- No unrelated changes to CLAUDE.md
</success_criteria>

<output>
After completion, create `.planning/phases/32-fix-demo-ui-ux-bugs/32-03-SUMMARY.md`
</output>
