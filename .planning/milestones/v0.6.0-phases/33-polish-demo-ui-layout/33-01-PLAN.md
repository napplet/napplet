---
phase: 33-polish-demo-ui-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/demo/index.html
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Napplet iframe fills the `.topology-frame-slot` container vertically without unused whitespace"
    - "Iframe content is not clipped or hidden by scrollbars"
    - "Resizing the inspector pane resizes the iframe proportionally"
  artifacts:
    - path: "apps/demo/index.html"
      provides: "CSS rule for iframe sizing within flex container"
      must_contain: "width: 100%; height: 100%;"
  key_links:
    - from: ".topology-frame-slot"
      to: "iframe element"
      via: "CSS flex sizing"
      pattern: "flex: 1.*width.*100%.*height.*100%"
---

<objective>
Fix napplet iframe container rendering to fill available space without unused whitespace.

Purpose: The iframe inside `.topology-frame-slot` currently doesn't consume the full container height due to missing explicit sizing. This wastes vertical space in the constrained layout.

Output: CSS rule adding `width: 100%; height: 100%;` to iframe elements inside `.topology-frame-slot`, enabling responsive filling of flex container.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-polish-demo-ui-layout/33-CONTEXT.md
@.planning/phases/33-polish-demo-ui-layout/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add iframe sizing CSS rule</name>
  <read_first>
    - apps/demo/index.html (current state of the file to understand existing CSS and HTML structure)
    - Current `.topology-frame-slot` CSS rule location and syntax
  </read_first>
  <action>
Add a new CSS rule for iframe sizing inside the `<style>` section of apps/demo/index.html. The rule must explicitly set iframe dimensions to match parent container:

```css
.topology-frame-slot iframe {
  width: 100%;
  height: 100%;
  border: none;
}
```

This rule:
- Applies to any iframe child of `.topology-frame-slot` (the napplet frame container)
- Sets width to 100% to fill parent horizontally
- Sets height to 100% to fill parent vertically (paired with parent's `flex: 1`)
- Removes default iframe border for a clean appearance

Implementation notes:
- Per D-01 (user decision): iframe fills `.topology-frame-slot` container with `flex: 1` (vertical space, no padding/margin gaps)
- Per D-02 (user decision): iframe element itself uses `width: 100%; height: 100%;` to match parent container exactly
- The `.topology-frame-slot` already has `flex: 1; min-height: 220px; position: relative;` — the new rule completes the responsive pattern
- No padding or margin adjustments needed on the rule — `.topology-frame-slot` already provides container styling boundaries
  </action>
  <verify>
File apps/demo/index.html contains a CSS rule matching the pattern `.topology-frame-slot iframe { width: 100%; height: 100%; }` (grep confirms all three properties present)
  </verify>
  <done>
CSS rule added and verified: iframe elements inside `.topology-frame-slot` now have explicit `width: 100%; height: 100%;` sizing, enabling responsive filling of the flex container without whitespace or scrollbars
  </done>
</task>

</tasks>

<verification>
After executing this plan:
1. Open apps/demo/index.html and search for the new rule `.topology-frame-slot iframe { width: 100%; height: 100%; border: none; }`
2. Run `pnpm dev` from apps/demo directory to start Vite dev server
3. Manually verify in browser: Napplet iframe fills the entire `.topology-frame-slot` container without unused whitespace or scrollbars
4. Resize the inspector pane on the right — iframe should resize proportionally without gaps
</verification>

<success_criteria>
- [ ] CSS rule present in apps/demo/index.html with exact properties: `width: 100%; height: 100%; border: none;`
- [ ] No syntax errors in the CSS file (Vite dev server builds without warnings)
- [ ] Visual verification: Iframe fills container without whitespace when Vite dev server runs
</success_criteria>

<output>
After completion, create `.planning/phases/33-polish-demo-ui-layout/33-01-SUMMARY.md`
</output>
