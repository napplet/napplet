---
phase: 33-polish-demo-ui-layout
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/demo/src/topology.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Input and output connection points are visually distinct on topology edges"
    - "Forward edges (napplet → shell) position output sockets on the right side"
    - "Reverse edges (shell → napplet) position input sockets on the left side"
    - "Socket gravity offsets are adjusted from purely horizontal to include vertical component"
  artifacts:
    - path: "apps/demo/src/topology.ts"
      provides: "Forward edge LeaderLine configuration with adjusted startSocketGravity/endSocketGravity"
      must_contain: "startSocketGravity.*12.*endSocketGravity.*12"
    - path: "apps/demo/src/topology.ts"
      provides: "Reverse edge LeaderLine configuration with adjusted startSocketGravity/endSocketGravity"
      must_contain: "startSocketGravity.*-12.*endSocketGravity.*-12"
  key_links:
    - from: "Forward edge (out line)"
      to: "Output socket positioning"
      via: "startSocketGravity: [12, -8]"
      pattern: "startSocketGravity: \\[12, -8\\]"
    - from: "Reverse edge (in line)"
      to: "Input socket positioning"
      via: "startSocketGravity: [-12, 8]"
      pattern: "startSocketGravity: \\[-12, 8\\]"
---

<objective>
Adjust socket gravity offsets to create visual distinction between input and output connection points on topology edges.

Purpose: Currently both forward and reverse edges use purely horizontal socket offsets ([12, 0] and [-12, 0]). Adding vertical offset ([12, -8] and [-12, 8]) positions output sockets distinctly from input sockets, improving visual clarity of message flow direction.

Output: Updated socket gravity values in forward and reverse edge LeaderLine constructors, positioning input and output connection points distinctly along cardinal directions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-polish-demo-ui-layout/33-CONTEXT.md
@.planning/phases/33-polish-demo-ui-layout/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adjust socket gravity offsets for forward and reverse edges</name>
  <read_first>
    - apps/demo/src/topology.ts (to locate edge initialization code, approximately lines 217-229)
    - Current startSocketGravity and endSocketGravity values in both forward edge (outLine) and reverse edge (inLine) constructors
    - Complete context of how LeaderLine is instantiated for both edge directions
  </read_first>
  <action>
Update socket gravity values in apps/demo/src/topology.ts for both forward and reverse edge constructors (approximately lines 217-229):

Current state:
```typescript
// Forward edge (napplet → shell)
const outLine = new LeaderLine(fromEl, toEl, {
  ...BASE_OPTIONS,
  startSocketGravity: [12, 0],    // Right, center
  endSocketGravity: [12, 0],      // Right, center
});

// Reverse edge (shell → napplet)
const inLine = new LeaderLine(toEl, fromEl, {
  ...BASE_OPTIONS,
  startSocketGravity: [-12, 0],   // Left, center
  endSocketGravity: [-12, 0],     // Left, center
});
```

New state (per D-06 and D-04 user decisions):
```typescript
// Forward edge (napplet → shell)
const outLine = new LeaderLine(fromEl, toEl, {
  ...BASE_OPTIONS,
  startSocketGravity: [12, -8],   // Output: right + slightly down
  endSocketGravity: [12, -8],
});

// Reverse edge (shell → napplet)
const inLine = new LeaderLine(toEl, fromEl, {
  ...BASE_OPTIONS,
  startSocketGravity: [-12, 8],   // Input: left + slightly up
  endSocketGravity: [-12, 8],
});
```

Implementation notes:
- Per D-06 (user decision): Input and output ports positioned using socket gravity offsets — inputs on left (negative X), outputs on right (positive X)
- Per D-04 (user decision): Socket gravity offsets position connectors at cardinal directions (top/bottom for vertical edges, left/right for horizontal)
- Socket gravity is a [x, y] tuple: X controls horizontal offset (positive = right, negative = left), Y controls vertical offset (positive = down, negative = up)
- Forward edge uses [12, -8]: 12px right (output), -8px up (visual distinctness from input)
- Reverse edge uses [-12, 8]: 12px left (input), 8px down (visual distinctness from output)
- Per Claude's discretion: These values (±8 for Y) are starting suggestions. Adjust if visual testing shows they need fine-tuning
- Both startSocketGravity and endSocketGravity must be updated consistently to position both endpoints of each line
  </action>
  <verify>
File apps/demo/src/topology.ts contains:
- Forward edge with `startSocketGravity: [12, -8]` and `endSocketGravity: [12, -8]`
- Reverse edge with `startSocketGravity: [-12, 8]` and `endSocketGravity: [-12, 8]`
(grep confirms all four occurrences with correct values)
  </verify>
  <done>
Socket gravity offsets updated for both forward and reverse edges. Input connection points (left, slightly up) are now visually distinct from output connection points (right, slightly down), improving clarity of message flow direction in the topology view
  </done>
</task>

</tasks>

<verification>
After executing this plan:
1. Open apps/demo/src/topology.ts and confirm both edge constructors have updated socket gravity values
2. Run TypeScript type-check: `pnpm type-check` (verify no type errors)
3. Run `pnpm dev` from apps/demo directory to start Vite dev server
4. Manually verify in browser:
   - Forward edges (napplet → shell) have connection points on right side, slightly lower than center
   - Reverse edges (shell → napplet) have connection points on left side, slightly higher than center
   - Input and output connection points are visually distinguishable
5. If visual appearance is poor (e.g., connection points look misaligned), adjust Y-offset values (try smaller values like ±4 or ±6) and test again
</verification>

<success_criteria>
- [ ] Forward edge (outLine) in apps/demo/src/topology.ts has `startSocketGravity: [12, -8]` and `endSocketGravity: [12, -8]`
- [ ] Reverse edge (inLine) in apps/demo/src/topology.ts has `startSocketGravity: [-12, 8]` and `endSocketGravity: [-12, 8]`
- [ ] TypeScript compilation succeeds (`pnpm type-check` exits 0)
- [ ] Vite dev server builds without errors
- [ ] Visual verification: Input and output connection points are visually distinct and properly positioned (executor judgment on final values if needed)
</success_criteria>

<output>
After completion, create `.planning/phases/33-polish-demo-ui-layout/33-03-SUMMARY.md`
</output>
