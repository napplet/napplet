---
phase: 33-polish-demo-ui-layout
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/demo/src/topology.ts
autonomous: true
gap_closure: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Input and output sockets are visually distinct with offset positioning"
    - "Forward edges have upward offset on output socket, downward offset on input socket"
    - "Reverse edges have downward offset on output socket, upward offset on input socket"
  artifacts:
    - path: "apps/demo/src/topology.ts"
      provides: "Forward edge socket gravities with opposite Y-offsets"
      must_contain: "startSocketGravity: \\[12, -8\\]"
      must_contain: "endSocketGravity: \\[12, 8\\]"
    - path: "apps/demo/src/topology.ts"
      provides: "Reverse edge socket gravities with opposite Y-offsets"
      must_contain: "startSocketGravity: \\[-12, 8\\]"
      must_contain: "endSocketGravity: \\[-12, -8\\]"
  key_links:
    - from: "Forward edge endSocketGravity"
      to: "Visual output socket offset"
      via: "[12, 8] places socket below node center"
      pattern: "outLine.*endSocketGravity.*\\[12, 8\\]"
    - from: "Reverse edge endSocketGravity"
      to: "Visual input socket offset"
      via: "[-12, -8] places socket above node center"
      pattern: "inLine.*endSocketGravity.*\\[-12, -8\\]"
---

<objective>
Fix socket gravity symmetry by using opposite Y-offsets for start and end sockets, enabling visual distinction between input and output connection points.

Purpose: Current socket gravity values are symmetric (same start/end values), which cancels visual distinction. Forward edges need opposite Y-offsets to visually separate output sockets (upward offset) from input sockets (downward offset). Reverse edges need the reverse configuration.

Output: Socket gravity values updated so input/output sockets are visually distinct and properly offset from each other on node edges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-polish-demo-ui-layout/33-CONTEXT.md
@.planning/phases/33-polish-demo-ui-layout/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update forward and reverse edge socket gravity offsets</name>
  <files>apps/demo/src/topology.ts</files>
  <action>
In apps/demo/src/topology.ts, locate the initTopologyEdges function (around line 190) and the socket gravity assignments (lines 208-220).

Current code (INCORRECT - symmetric values cancel distinction):
```typescript
const outLine = new LeaderLine(fromEl, toEl, {
  ...BASE_OPTIONS,
  startSocketGravity: [12, -8],
  endSocketGravity: [12, -8],        // ← WRONG: Same as start, cancels visual separation
});

const inLine = new LeaderLine(toEl, fromEl, {
  ...BASE_OPTIONS,
  startSocketGravity: [-12, 8],
  endSocketGravity: [-12, 8],        // ← WRONG: Same as start, cancels visual separation
});
```

Fix: Use opposite Y-offsets so start and end sockets are visually distinct

Expected result (CORRECT):
```typescript
const outLine = new LeaderLine(fromEl, toEl, {
  ...BASE_OPTIONS,
  startSocketGravity: [12, -8],
  endSocketGravity: [12, 8],         // ← CORRECT: Opposite Y-offset for visual distinction
});

const inLine = new LeaderLine(toEl, fromEl, {
  ...BASE_OPTIONS,
  startSocketGravity: [-12, 8],
  endSocketGravity: [-12, -8],       // ← CORRECT: Opposite Y-offset for visual distinction
});
```

Implementation:
- Line 211 (endSocketGravity for outLine): Change from `[12, -8]` to `[12, 8]`
- Line 218 (endSocketGravity for inLine): Change from `[-12, 8]` to `[-12, -8]`
- Keep startSocketGravity values unchanged (lines 210 and 217)
- No other changes to the LeaderLine instantiation or BASE_OPTIONS

Reasoning:
- Forward/out line: Start socket at [12, -8] (right, upward), end socket at [12, 8] (right, downward)
  This creates a visual distinction between output socket (upward) and input socket (downward)
- Reverse/in line: Start socket at [-12, 8] (left, downward), end socket at [-12, -8] (left, upward)
  This mirrors the forward configuration for symmetry but in opposite directions
  </action>
  <verify>
- Run: `grep -A2 "const outLine = new LeaderLine" apps/demo/src/topology.ts | grep "endSocketGravity"` — must show `[12, 8]`
- Run: `grep -A2 "const inLine = new LeaderLine" apps/demo/src/topology.ts | grep "endSocketGravity"` — must show `[-12, -8]`
- Run: `pnpm type-check` — must pass with no TypeScript errors
- Run: `pnpm dev` from apps/demo directory to build and start Vite dev server
  </verify>
  <done>
Forward and reverse edge socket gravity values updated with opposite Y-offsets. Input and output sockets are now visually distinct and properly separated on node edges.
  </done>
</task>

</tasks>

<verification>
After executing this plan:
1. Open apps/demo/src/topology.ts and navigate to initTopologyEdges function (line ~190)
2. Verify outLine (line ~208):
   - startSocketGravity: [12, -8] (unchanged)
   - endSocketGravity: [12, 8] (changed from -8 to 8)
3. Verify inLine (line ~215):
   - startSocketGravity: [-12, 8] (unchanged)
   - endSocketGravity: [-12, -8] (changed from 8 to -8)
4. Run `pnpm type-check` to ensure TypeScript compilation succeeds
5. Start Vite dev server: `pnpm dev` from apps/demo directory
6. Manually verify in browser:
   - Open the demo topology diagram
   - Forward edges should have visually distinct output socket (upward offset) and input socket (downward offset)
   - Reverse edges should mirror this configuration
   - Sockets should appear at different heights, creating clear visual separation between input/output
7. Verify no broken edges or rendering issues
</verification>

<success_criteria>
- [ ] Forward edge (outLine) endSocketGravity changed to [12, 8] (Y-offset from -8 to 8)
- [ ] Reverse edge (inLine) endSocketGravity changed to [-12, -8] (Y-offset from 8 to -8)
- [ ] Start socket gravity values unchanged (outLine [12, -8], inLine [-12, 8])
- [ ] TypeScript compilation succeeds (`pnpm type-check` exits 0)
- [ ] Vite dev server builds without errors
- [ ] Visual verification: Input and output sockets are visually distinct with offset positioning (executor judgment)
</success_criteria>

<output>
After completion, create `.planning/phases/33-polish-demo-ui-layout/33-07-SUMMARY.md`
</output>
