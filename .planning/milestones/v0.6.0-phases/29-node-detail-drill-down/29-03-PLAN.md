---
phase: 29-node-detail-drill-down
plan: 03
title: "Project recent activity into node summaries and harden inspector coverage"
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - apps/demo/src/main.ts
  - apps/demo/src/flow-animator.ts
  - apps/demo/src/debugger.ts
  - apps/demo/src/node-details.ts
  - apps/demo/src/node-inspector.ts
  - tests/unit/demo-node-details-model.test.ts
  - tests/unit/demo-node-inspector-render.test.ts
  - tests/e2e/demo-node-inspector.spec.ts
autonomous: true
requirements:
  - NODE-01
  - NODE-02
must_haves:
  truths:
    - "Each node detail record includes bounded recent-activity/history projected from live demo traffic or host state"
    - "Inspector views show both current state and recent activity/history for the selected node"
    - "Recent activity complements the debugger rather than duplicating the full global log"
    - "Regression coverage protects both model-level activity mapping and real inspector interaction behavior"
    - "`pnpm --filter @napplet/demo build` and targeted node-inspector tests remain green"
  artifacts:
    - path: "apps/demo/src/node-details.ts"
      provides: "Recent-activity projection into node detail records"
      contains: "recentActivity"
    - path: "apps/demo/src/node-inspector.ts"
      provides: "Expanded node activity/history sections"
      contains: "Recent Activity"
    - path: "tests/e2e/demo-node-inspector.spec.ts"
      provides: "Inspector interaction coverage in the real demo"
      contains: "node-inspector"
  key_links:
    - from: "apps/demo/src/node-details.ts"
      to: "apps/demo/src/debugger.ts"
      via: "Shared tapped-message/path classification"
      pattern: "classifyTappedMessagePath"
    - from: "apps/demo/src/node-details.ts"
      to: "apps/demo/src/flow-animator.ts"
      via: "Live activity signals and per-node projection"
      pattern: "tap.onMessage"
---

# Plan 29-03: Project Recent Activity Into Node Summaries and Harden Inspector Coverage

<objective>
Complete the inspector experience by feeding recent activity/history into each node and locking the behavior with targeted tests. This finishes the "current state plus recent behavior" expectation from the phase context while keeping the debugger as the global protocol ledger.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-node-detail-drill-down/29-CONTEXT.md
@.planning/phases/29-node-detail-drill-down/29-RESEARCH.md
@.planning/phases/29-node-detail-drill-down/29-01-PLAN.md
@.planning/phases/29-node-detail-drill-down/29-02-PLAN.md

@apps/demo/src/main.ts
@apps/demo/src/flow-animator.ts
@apps/demo/src/debugger.ts
@apps/demo/src/node-details.ts
@apps/demo/src/node-inspector.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build bounded per-node recent-activity projection</name>
  <files>apps/demo/src/node-details.ts, apps/demo/src/main.ts, apps/demo/src/flow-animator.ts</files>
  <read_first>
    - apps/demo/src/node-details.ts
    - apps/demo/src/debugger.ts
    - apps/demo/src/flow-animator.ts
    - apps/demo/src/main.ts
    - .planning/phases/29-node-detail-drill-down/29-RESEARCH.md
  </read_first>
  <action>
    Extend the node-detail model so each node can expose a bounded recent-activity/history list derived from live demo signals.

    Reuse existing message-tap and classification inputs rather than inventing a second event pipeline. Normalize activity entries into short records that can show:
    - path or action
    - direction or actor
    - success/blocked state when applicable
    - timestamp or relative recency

    Keep the history bounded with a fixed-size buffer per node so the inspector remains lightweight even during rapid traffic.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -q "recentActivity" apps/demo/src/node-details.ts && grep -q "classifyTappedMessagePath" apps/demo/src/node-details.ts && grep -q "tap.onMessage" apps/demo/src/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/demo/src/node-details.ts includes recent-activity/history support
    - The activity model is derived from existing tap/debugger truth sources
    - History is bounded rather than unbounded
    - Activity entries can distinguish success/blocked path outcomes where available
  </acceptance_criteria>
  <done>Each node can expose recent activity/history without duplicating the full debugger log</done>
</task>

<task type="auto">
  <name>Task 2: Render current-state and recent-activity sections in the inspector</name>
  <files>apps/demo/src/node-inspector.ts, apps/demo/src/node-details.ts, apps/demo/index.html</files>
  <read_first>
    - apps/demo/src/node-inspector.ts
    - apps/demo/src/node-details.ts
    - apps/demo/index.html
    - .planning/phases/29-node-detail-drill-down/29-CONTEXT.md
  </read_first>
  <action>
    Expand the inspector rendering so each selected node shows:
    - current-state sections appropriate to that role
    - a recent-activity/history section

    Keep the inspector structure consistent across node types while allowing role-specific fields. The goal is one recognizable inspector shell with different content blocks, not five unrelated panel designs.

    Make sure the recent-activity view complements the debugger by being local and filtered rather than a copy of the full live log.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -q "Recent Activity" apps/demo/src/node-inspector.ts && grep -q "Current State" apps/demo/src/node-inspector.ts && pnpm --filter @napplet/demo build</automated>
  </verify>
  <acceptance_criteria>
    - The inspector includes both current-state and recent-activity sections
    - The structure is consistent across node roles
    - The content is sourced from node-detail records
    - The demo still builds after inspector-content expansion
  </acceptance_criteria>
  <done>The inspector makes both current state and recent node behavior legible</done>
</task>

<task type="auto">
  <name>Task 3: Add interaction coverage for node inspection and debugger coexistence</name>
  <files>tests/unit/demo-node-details-model.test.ts, tests/unit/demo-node-inspector-render.test.ts, tests/e2e/demo-node-inspector.spec.ts</files>
  <read_first>
    - tests/unit/demo-node-details-model.test.ts
    - tests/unit/demo-node-inspector-render.test.ts
    - tests/e2e/demo-audit-correctness.spec.ts
    - apps/demo/src/node-inspector.ts
    - apps/demo/index.html
  </read_first>
  <action>
    Extend the unit tests and add `tests/e2e/demo-node-inspector.spec.ts` to cover the full inspector contract.

    Required coverage:
    - model tests assert recent-activity projection exists for each node role
    - render tests assert current-state and recent-activity sections are present in the inspector
    - e2e coverage clicks or otherwise selects supported nodes, confirms the inspector opens on the right, and confirms the bottom debugger remains present

    Keep the e2e test focused on the node-inspection workflow rather than re-testing deeper ACL/runtime behavior already covered elsewhere.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && test -f tests/e2e/demo-node-inspector.spec.ts && grep -q "Recent Activity" tests/unit/demo-node-inspector-render.test.ts && grep -q "debugger" tests/e2e/demo-node-inspector.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/demo-node-inspector.spec.ts exists
    - Unit tests cover recent-activity projection and inspector sections
    - The e2e test covers inspector opening while the debugger remains visible
    - The new coverage focuses on Phase 29 behavior instead of duplicating unrelated protocol tests
  </acceptance_criteria>
  <done>The full node-inspection workflow is protected by targeted regression coverage</done>
</task>

</tasks>

<verify>
  <summary>
    Run targeted unit and e2e coverage for node details and the inspector, then rebuild the demo. Confirm the inspector shows current state plus recent activity for selected nodes while the debugger stays available.
  </summary>
  <automated>
    cd /home/sandwich/Develop/napplet && pnpm vitest run tests/unit/demo-node-details-model.test.ts tests/unit/demo-node-inspector-render.test.ts
    cd /home/sandwich/Develop/napplet && pnpm playwright test tests/e2e/demo-node-inspector.spec.ts
    cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/demo build
  </automated>
  <manual>
    Open the demo, generate chat/bot traffic, inspect multiple nodes, and confirm the inspector's recent activity is node-local while the debugger remains the full protocol timeline below.
  </manual>
</verify>
