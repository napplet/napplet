---
phase: 27-demo-audit-correctness
plan: 03
title: "Lock in demo correctness with regression coverage and audit notes"
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - tests/e2e/demo-audit-correctness.spec.ts
  - tests/unit/demo-host-audit.test.ts
  - .planning/phases/27-demo-audit-correctness/27-AUDIT-NOTES.md
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
must_haves:
  truths:
    - "tests/e2e/demo-audit-correctness.spec.ts covers the reported chat relay/inter-pane confusion scenario"
    - "tests/e2e/demo-audit-correctness.spec.ts asserts the debugger or visible UI preserves exact denial strings such as 'denied: relay:write' or 'denied: sign:event'"
    - ".planning/phases/27-demo-audit-correctness/27-AUDIT-NOTES.md exists with reproducible manual checks for any scenario not automated"
    - "The remaining manual checks distinguish UI wording issues from ACL/runtime/service behavior"
    - "Phase 27 leaves behind enough evidence that later demo phases can build topology UI without re-auditing basic correctness"
  artifacts:
    - path: "tests/e2e/demo-audit-correctness.spec.ts"
      provides: "Demo-specific correctness regression coverage"
      contains: "relay:write"
    - path: ".planning/phases/27-demo-audit-correctness/27-AUDIT-NOTES.md"
      provides: "Manual audit checklist and findings log"
      contains: "Expected"
  key_links:
    - from: "tests/e2e/demo-audit-correctness.spec.ts"
      to: "apps/demo/src/debugger.ts"
      via: "Visible diagnosis assertions"
      pattern: "denied:"
    - from: ".planning/phases/27-demo-audit-correctness/27-AUDIT-NOTES.md"
      to: "apps/demo/napplets/chat/src/main.ts"
      via: "Manual reproduction steps"
      pattern: "chat:message|publish"
---

# Plan 27-03: Lock In Demo Correctness with Regression Coverage and Audit Notes

<objective>
Turn the audited demo behavior into durable evidence: automate the highest-value correctness scenarios and document any remaining manual checks clearly enough that future work can distinguish UI issues from real ACL, runtime, or service behavior without rediscovering the basics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-demo-audit-correctness/27-CONTEXT.md
@.planning/phases/27-demo-audit-correctness/27-RESEARCH.md
@.planning/phases/27-demo-audit-correctness/27-VALIDATION.md

@apps/demo/src/main.ts
@apps/demo/src/shell-host.ts
@apps/demo/src/debugger.ts
@apps/demo/napplets/chat/src/main.ts
@apps/demo/napplets/bot/src/main.ts
@tests/e2e/acl-matrix-relay.spec.ts
@tests/e2e/signer-delegation.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add demo-specific end-to-end correctness scenarios</name>
  <files>tests/e2e/demo-audit-correctness.spec.ts</files>
  <read_first>
    - tests/e2e/acl-matrix-relay.spec.ts
    - tests/e2e/signer-delegation.spec.ts
    - apps/demo/src/debugger.ts
    - apps/demo/src/shell-host.ts
    - apps/demo/napplets/chat/src/main.ts
    - apps/demo/napplets/bot/src/main.ts
  </read_first>
  <action>
    Create `tests/e2e/demo-audit-correctness.spec.ts` with focused demo-facing scenarios that assert visible diagnosis, not just protocol success.

    Add at least these exact scenarios:
    - revoke chat `relay:write`, send a chat message, and assert the demo still shows `chat:message`/bot inter-pane activity separately from a relay denial
    - revoke `state:read` or `state:write` and assert the visible message/debugger text contains `denied: state:read` or `denied: state:write`
    - revoke `sign:event` and assert the visible message/debugger text contains `denied: sign:event`

    Reuse existing Playwright helper patterns from nearby ACL/signer specs where practical, but make the assertions land on demo-specific outputs such as visible debugger text, status copy, or audit labels.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && test -f tests/e2e/demo-audit-correctness.spec.ts && grep -q "relay:write" tests/e2e/demo-audit-correctness.spec.ts && grep -q "denied: sign:event" tests/e2e/demo-audit-correctness.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/demo-audit-correctness.spec.ts exists
    - The file covers the exact revoked-capability scenarios listed in this task
    - At least one assertion checks that inter-pane success and relay failure are visible at the same time for the chat/bot scenario
    - At least one assertion checks an exact denial string, not only a generic error state
  </acceptance_criteria>
  <done>Demo correctness is protected by focused end-to-end regression scenarios</done>
</task>

<task type="auto">
  <name>Task 2: Expand unit-level audit coverage for diagnosis metadata</name>
  <files>tests/unit/demo-host-audit.test.ts</files>
  <read_first>
    - tests/unit/demo-host-audit.test.ts
    - apps/demo/src/debugger.ts
    - apps/demo/src/acl-panel.ts
    - apps/demo/src/shell-host.ts
  </read_first>
  <action>
    Extend `tests/unit/demo-host-audit.test.ts` so it covers diagnosis metadata and wording expectations added by Plans 01 and 02.

    Add assertions for:
    - ACL labels include `Relay Publish / Inter-Pane Send`, `Relay Subscribe`, `Signer Requests`, `State Read`, and `State Write`
    - debugger path labels include `relay-publish`, `inter-pane-send`, `state-read`, `state-write`, and `signer-request`
    - no exported/default copy still contains the stale strings `Read Shell` or `Write Shell`
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -q "Relay Publish / Inter-Pane Send" tests/unit/demo-host-audit.test.ts && ! grep -q "Read Shell" tests/unit/demo-host-audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Unit audit coverage checks the new ACL wording exactly
    - Unit audit coverage checks the debugger path labels exactly
    - The stale labels `Read Shell` and `Write Shell` are asserted absent
  </acceptance_criteria>
  <done>Diagnosis metadata and wording regressions are covered at the unit level</done>
</task>

<task type="auto">
  <name>Task 3: Write reproducible audit notes for remaining manual scenarios</name>
  <files>.planning/phases/27-demo-audit-correctness/27-AUDIT-NOTES.md</files>
  <read_first>
    - .planning/phases/27-demo-audit-correctness/27-CONTEXT.md
    - .planning/phases/27-demo-audit-correctness/27-RESEARCH.md
    - .planning/phases/27-demo-audit-correctness/27-VALIDATION.md
    - apps/demo/napplets/chat/src/main.ts
    - apps/demo/napplets/bot/src/main.ts
  </read_first>
  <action>
    Create `.planning/phases/27-demo-audit-correctness/27-AUDIT-NOTES.md` as the committed manual audit checklist for any scenario not fully automated.

    Include a table with these columns exactly:
    - `Scenario`
    - `Steps`
    - `Expected`
    - `If Wrong, Likely Layer`

    Cover at least these scenarios:
    - chat `relay:write` revoked but bot still replies through inter-pane
    - `state:read` revoked during history load
    - `state:write` revoked during save
    - `sign:event` revoked for a signer request
    - signer fallback vs signer service mode visibility

    In `If Wrong, Likely Layer`, use only these values where applicable:
    - `UI wording`
    - `demo host wiring`
    - `runtime ACL`
    - `service behavior`
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && test -f .planning/phases/27-demo-audit-correctness/27-AUDIT-NOTES.md && grep -q "| Scenario | Steps | Expected | If Wrong, Likely Layer |" .planning/phases/27-demo-audit-correctness/27-AUDIT-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/27-demo-audit-correctness/27-AUDIT-NOTES.md` exists
    - The notes file contains the exact four-column table named in this task
    - The notes file includes the five scenarios listed in this task
    - Each scenario identifies a likely fault layer using the exact values listed in this task
  </acceptance_criteria>
  <done>Remaining manual diagnosis work is captured as a reusable audit checklist</done>
</task>

</tasks>

<verify>
  <summary>
    Run the new demo-specific e2e suite, rerun the audit unit tests, and confirm the phase directory contains a reusable audit note for scenarios that still need human judgment.
  </summary>
  <automated>
    cd /home/sandwich/Develop/napplet && pnpm vitest run tests/unit/demo-host-audit.test.ts
    cd /home/sandwich/Develop/napplet && pnpm playwright test tests/e2e/demo-audit-correctness.spec.ts
  </automated>
  <manual>
    Read `27-AUDIT-NOTES.md` and confirm a maintainer could distinguish UI wording bugs from ACL/runtime/service behavior without extra tribal knowledge.
  </manual>
</verify>
