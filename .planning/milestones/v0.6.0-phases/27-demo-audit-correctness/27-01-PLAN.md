---
phase: 27-demo-audit-correctness
plan: 01
title: "Audit demo host wiring and align stale integrations"
wave: 1
depends_on: []
files_modified:
  - apps/demo/src/main.ts
  - apps/demo/src/shell-host.ts
  - apps/demo/src/signer-demo.ts
  - apps/demo/package.json
  - tests/unit/demo-host-audit.test.ts
autonomous: true
requirements:
  - DEMO-01
  - DEMO-03
must_haves:
  truths:
    - "apps/demo/src/shell-host.ts defines an explicit capability/path inventory for AUTH, relay publish, relay subscribe, inter-pane, state, and signer flows"
    - "apps/demo/src/shell-host.ts no longer relies on shell-era wording alone to describe protocol paths"
    - "apps/demo/src/main.ts initializes the demo against the current runtime/service architecture assumptions and reports host readiness clearly"
    - "If the demo keeps fallback signer wiring, apps/demo/src/signer-demo.ts and shell-host.ts label it as fallback/runtime behavior; if it switches, the demo registers a signer service explicitly"
    - "tests/unit/demo-host-audit.test.ts verifies the demo host exposes the expected path inventory and stale-integration guardrails"
    - "pnpm --filter @napplet/demo build succeeds after the host audit changes"
  artifacts:
    - path: "apps/demo/src/shell-host.ts"
      provides: "Audited demo host wiring and path metadata"
      contains: "relay-publish"
    - path: "apps/demo/src/main.ts"
      provides: "Demo boot flow aligned with audited host behavior"
      contains: "shell booted"
    - path: "tests/unit/demo-host-audit.test.ts"
      provides: "Host-audit regression coverage"
      contains: "path inventory"
  key_links:
    - from: "apps/demo/src/shell-host.ts"
      to: "packages/runtime/src/enforce.ts"
      via: "Capability/path truth source"
      pattern: "resolveCapabilities"
    - from: "apps/demo/src/shell-host.ts"
      to: "packages/runtime/src/runtime.ts"
      via: "Runtime/service alignment"
      pattern: "registerService|serviceRegistry|signer"
---

# Plan 27-01: Audit Demo Host Wiring and Align Stale Integrations

<objective>
Establish the real protocol paths the demo currently exercises, remove stale host assumptions that obscure current `@napplet/*` architecture, and leave the demo host with explicit path metadata that later UI/debugger work can trust. This plan is the correctness foundation for DEMO-01 and provides the architecture evidence DEMO-03 needs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-demo-audit-correctness/27-CONTEXT.md
@.planning/phases/27-demo-audit-correctness/27-RESEARCH.md

@apps/demo/src/main.ts
@apps/demo/src/shell-host.ts
@apps/demo/src/signer-demo.ts
@apps/demo/napplets/chat/src/main.ts
@apps/demo/napplets/bot/src/main.ts
@packages/runtime/src/enforce.ts
@packages/runtime/src/runtime.ts
@packages/services/src/signer-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit protocol-path inventory to the demo host</name>
  <files>apps/demo/src/shell-host.ts, apps/demo/src/main.ts</files>
  <read_first>
    - apps/demo/src/shell-host.ts
    - apps/demo/src/main.ts
    - apps/demo/napplets/chat/src/main.ts
    - apps/demo/napplets/bot/src/main.ts
    - packages/runtime/src/enforce.ts
    - .planning/phases/27-demo-audit-correctness/27-CONTEXT.md
  </read_first>
  <action>
    Update `apps/demo/src/shell-host.ts` so the demo host exports a concrete protocol-path inventory instead of relying on inferred debugger strings.

    Add a typed inventory that names at least these paths exactly:
    - `auth`
    - `relay-publish`
    - `relay-subscribe`
    - `inter-pane-send`
    - `inter-pane-receive`
    - `state-read`
    - `state-write`
    - `signer-request`
    - `signer-response`

    For each path, include the expected source capability and a short explanation derived from current runtime behavior:
    - `relay-publish` -> `relay:write`
    - `relay-subscribe` -> `relay:read`
    - `inter-pane-send` -> `relay:write`
    - `inter-pane-receive` -> `relay:read`
    - `state-read` -> `state:read`
    - `state-write` -> `state:write`
    - `signer-request` -> `sign:event`

    Update `apps/demo/src/main.ts` to use this inventory when announcing shell boot or host readiness so the demo boot flow makes it clear which paths are being exercised and audited, rather than implying a single "shell" path.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -q "relay-publish" apps/demo/src/shell-host.ts && grep -q "inter-pane-send" apps/demo/src/shell-host.ts && grep -q "signer-request" apps/demo/src/shell-host.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/demo/src/shell-host.ts contains explicit entries for `relay-publish`, `relay-subscribe`, `inter-pane-send`, `inter-pane-receive`, `state-read`, `state-write`, and `signer-request`
    - Each path entry contains the exact capability string it depends on
    - apps/demo/src/main.ts references audited path information during boot or debugger initialization
    - No new path names contradict `packages/runtime/src/enforce.ts`
  </acceptance_criteria>
  <done>Demo host exposes an explicit path inventory grounded in runtime capability routing</done>
</task>

<task type="auto">
  <name>Task 2: Align signer behavior with current runtime/service reality</name>
  <files>apps/demo/src/shell-host.ts, apps/demo/src/signer-demo.ts, apps/demo/package.json</files>
  <read_first>
    - apps/demo/src/shell-host.ts
    - apps/demo/src/signer-demo.ts
    - apps/demo/package.json
    - packages/runtime/src/runtime.ts
    - packages/services/src/signer-service.ts
    - packages/shell/src/index.ts
  </read_first>
  <action>
    Audit the demo's signer path and make the code state explicit.

    Choose one of these two concrete outcomes:

    1. Preferred if low-risk:
    register a signer service through the current runtime/service architecture so the demo uses `packages/services/src/signer-service.ts` semantics rather than only `hooks.auth.getSigner()`.

    2. Acceptable fallback if the service swap would overreach:
    keep the existing fallback signer hooks, but rename/demo-label the behavior clearly as a runtime fallback path and document in code comments or exported metadata that the demo is not yet showing a registered signer service.

    In either case, update `apps/demo/src/signer-demo.ts` and `apps/demo/src/shell-host.ts` so the code no longer implies the simplified signer is the full current architecture. If a new dependency import is required for service registration, update `apps/demo/package.json` accordingly while keeping the package buildable.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -Eq "registerService|fallback" apps/demo/src/shell-host.ts && grep -Eq "fallback|signer service|runtime" apps/demo/src/signer-demo.ts && pnpm --filter @napplet/demo build</automated>
  </verify>
  <acceptance_criteria>
    - The demo signer path is either explicitly registered as a service or explicitly labeled as fallback/runtime behavior
    - apps/demo/src/signer-demo.ts contains wording or code that distinguishes the demo signer from a real end-user signer flow
    - apps/demo/src/shell-host.ts contains either `registerService(` or an equivalent explicit fallback label that future phases can replace
    - `pnpm --filter @napplet/demo build` exits 0
  </acceptance_criteria>
  <done>Signer wiring is audited and no longer misrepresents the current architecture</done>
</task>

<task type="auto">
  <name>Task 3: Add host-audit regression coverage</name>
  <files>tests/unit/demo-host-audit.test.ts</files>
  <read_first>
    - tests/unit/shell-runtime-integration.test.ts
    - packages/runtime/src/dispatch.test.ts
    - apps/demo/src/shell-host.ts
    - apps/demo/src/main.ts
  </read_first>
  <action>
    Create `tests/unit/demo-host-audit.test.ts` with focused integration checks for the demo host audit layer.

    Add tests that assert:
    - the exported path inventory includes `relay-publish`, `inter-pane-send`, `state-read`, `state-write`, and `signer-request`
    - the inventory maps those paths to the exact capabilities `relay:write`, `relay:read`, `state:read`, `state:write`, and `sign:event`
    - the signer mode is exposed as either `service` or `fallback`, not left implicit

    Keep these tests independent from DOM rendering. They should validate host metadata and architecture labeling only.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && test -f tests/unit/demo-host-audit.test.ts && grep -q "relay-publish" tests/unit/demo-host-audit.test.ts && grep -q "sign:event" tests/unit/demo-host-audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/demo-host-audit.test.ts exists
    - The test file asserts the exact path names and capability strings from this plan
    - The tests check that signer mode is explicit rather than inferred
  </acceptance_criteria>
  <done>Demo host audit assumptions are covered by a dedicated regression test</done>
</task>

</tasks>

<verify>
  <summary>
    Build the demo and run the new host-audit unit test. Confirm the demo host now exposes explicit path metadata and an explicit signer mode, and that those values agree with `packages/runtime/src/enforce.ts`.
  </summary>
  <automated>
    cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/demo build
    cd /home/sandwich/Develop/napplet && pnpm vitest run tests/unit/demo-host-audit.test.ts
  </automated>
  <manual>
    Open the demo and confirm the boot/debugger messaging no longer treats all capability paths as a single "shell" bucket.
  </manual>
</verify>
