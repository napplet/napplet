---
phase: 28-architecture-topology-view
plan: 02
title: "Make animation and responsiveness topology-aware"
wave: 2
depends_on:
  - 01
files_modified:
  - apps/demo/src/flow-animator.ts
  - apps/demo/src/main.ts
  - apps/demo/src/topology.ts
  - apps/demo/index.html
  - tests/unit/demo-topology-model.test.ts
  - tests/unit/demo-topology-render.test.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
must_haves:
  truths:
    - "The flow animator highlights node and edge paths through shell, ACL, runtime, and service nodes instead of collapsing everything into `shell-box`"
    - "Message activity can target a specific service node when the tapped message corresponds to a service path such as signer traffic"
    - "The layered topology remains readable on narrow widths without reverting to the old three-column mental model"
    - "Regression tests cover topology-aware animation inputs and rendered hierarchy invariants"
    - "`pnpm --filter @napplet/demo build` stays green after the animator and layout updates"
  artifacts:
    - path: "apps/demo/src/flow-animator.ts"
      provides: "Topology-aware node and edge highlighting"
      contains: "topology-node-runtime"
    - path: "apps/demo/index.html"
      provides: "Responsive layered architecture markup"
      contains: "topology-services"
    - path: "tests/unit/demo-topology-model.test.ts"
      provides: "Animation/topology invariants"
      contains: "signer"
  key_links:
    - from: "apps/demo/src/flow-animator.ts"
      to: "apps/demo/src/topology.ts"
      via: "Node and edge ids for the layered view"
      pattern: "topology-node-"
    - from: "apps/demo/src/flow-animator.ts"
      to: "apps/demo/src/shell-host.ts"
      via: "Path classification and service-path awareness"
      pattern: "signer-request|signer-response|DEMO_PROTOCOL_PATHS"
---

# Plan 28-02: Make Animation and Responsiveness Topology-Aware

<objective>
Teach the new layered architecture through motion and regression coverage, so runtime and service activity light up the correct nodes and the topology remains accurate and readable across screen sizes. This turns the structural layout from Plan 01 into a trustworthy architecture view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-architecture-topology-view/28-CONTEXT.md
@.planning/phases/28-architecture-topology-view/28-RESEARCH.md

@apps/demo/index.html
@apps/demo/src/main.ts
@apps/demo/src/topology.ts
@apps/demo/src/flow-animator.ts
@apps/demo/src/debugger.ts
@apps/demo/src/sequence-diagram.ts
@apps/demo/src/shell-host.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route message highlights through topology node and edge ids</name>
  <files>apps/demo/src/flow-animator.ts, apps/demo/src/topology.ts, apps/demo/src/main.ts</files>
  <read_first>
    - apps/demo/src/flow-animator.ts
    - apps/demo/src/topology.ts
    - apps/demo/src/main.ts
    - apps/demo/src/shell-host.ts
    - apps/demo/src/debugger.ts
    - .planning/phases/28-architecture-topology-view/28-RESEARCH.md
  </read_first>
  <action>
    Rewrite `apps/demo/src/flow-animator.ts` so it no longer flashes only `${name}-box` and `shell-box`.

    Make the animator consume topology node ids and edge ids from `apps/demo/src/topology.ts`, and map tapped messages to ordered highlight paths such as:
    - napplet -> shell -> acl -> runtime for generic outbound traffic
    - runtime -> service -> runtime -> napplet for service responses when applicable
    - runtime -> napplet for relay deliveries after the architecture path has already crossed ACL/runtime

    Handle signer traffic explicitly so `signer-request` and `signer-response` can highlight `topology-node-service-signer` when that node exists. Update `apps/demo/src/main.ts` to pass any additional topology lookup helpers needed by the animator.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -q "topology-node-runtime" apps/demo/src/flow-animator.ts && grep -q "topology-node-acl" apps/demo/src/flow-animator.ts && grep -q "service-signer" apps/demo/src/flow-animator.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/demo/src/flow-animator.ts contains the exact strings `topology-node-runtime` and `topology-node-acl`
    - apps/demo/src/flow-animator.ts references a signer service node such as `topology-node-service-signer`
    - The animator no longer relies on `shell-box` as the single center highlight target
    - apps/demo/src/main.ts passes topology-aware inputs into the animator
  </acceptance_criteria>
  <done>Flow animation now reinforces the layered architecture instead of the old flattened center box</done>
</task>

<task type="auto">
  <name>Task 2: Preserve architecture readability on narrow layouts</name>
  <files>apps/demo/index.html, apps/demo/src/topology.ts</files>
  <read_first>
    - apps/demo/index.html
    - apps/demo/src/topology.ts
    - .planning/phases/28-architecture-topology-view/28-CONTEXT.md
  </read_first>
  <action>
    Update the flow-area CSS and markup so the architecture remains a layered stack on smaller screens instead of degrading into the old rotated-arrow three-column logic.

    Remove or replace the mobile-only assumptions that rotate `.arrow-col` elements from the old fixed pipeline. Ensure the responsive layout keeps:
    - napplets grouped as a top layer
    - shell, ACL, and runtime as separate vertical stops
    - services attached as a runtime branch or stacked runtime children

    Keep the debugger section and iframe containers functional while making the topology readable on both desktop and mobile widths.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && ! grep -q "rotate(90deg)" apps/demo/index.html && grep -q "topology-services" apps/demo/index.html</automated>
  </verify>
  <acceptance_criteria>
    - apps/demo/index.html no longer contains the exact string `rotate(90deg)` for the old arrow-column mobile fallback
    - apps/demo/index.html contains a dedicated service-region identifier such as `topology-services`
    - Responsive rules keep shell, ACL, and runtime as distinct labeled regions
    - The bottom debugger section remains present after responsive layout changes
  </acceptance_criteria>
  <done>The new topology stays architecture-faithful across viewport sizes</done>
</task>

<task type="auto">
  <name>Task 3: Expand regression coverage to lock topology-aware behavior</name>
  <files>tests/unit/demo-topology-model.test.ts, tests/unit/demo-topology-render.test.ts</files>
  <read_first>
    - tests/unit/demo-topology-model.test.ts
    - tests/unit/demo-topology-render.test.ts
    - apps/demo/src/flow-animator.ts
    - apps/demo/src/topology.ts
  </read_first>
  <action>
    Extend the new topology tests so they cover animation-facing and responsiveness-facing invariants.

    In `tests/unit/demo-topology-model.test.ts`, add checks that:
    - signer traffic can resolve to a `topology-node-service-signer` target when the signer service is present
    - service nodes remain direct children of runtime
    - napplet nodes remain peers in a napplet layer, not direct children of runtime

    In `tests/unit/demo-topology-render.test.ts`, add checks that:
    - the rendered layout includes a dedicated service region
    - shell, ACL, and runtime labels remain distinct
    - the architecture markup does not depend on the old merged `shell / acl` label or the old rotated arrow-column mobile structure
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -q "topology-node-service-signer" tests/unit/demo-topology-model.test.ts && grep -q "topology-services" tests/unit/demo-topology-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/demo-topology-model.test.ts asserts the exact string `topology-node-service-signer`
    - tests/unit/demo-topology-render.test.ts asserts the exact string `topology-services`
    - The tests verify shell, ACL, and runtime stay distinct regions
    - The tests fail if the old merged `shell / acl` label returns
  </acceptance_criteria>
  <done>Topology-aware behavior is protected against regressions as later demo phases add more nodes</done>
</task>

</tasks>

<verify>
  <summary>
    Run topology-focused Vitest coverage and rebuild the demo. Confirm message flashes can traverse shell, ACL, runtime, and signer-service nodes, and confirm the architecture view remains readable at narrow widths without collapsing back to the old three-column pipeline.
  </summary>
  <automated>
    cd /home/sandwich/Develop/napplet && pnpm vitest run tests/unit/demo-topology-model.test.ts tests/unit/demo-topology-render.test.ts
    cd /home/sandwich/Develop/napplet && pnpm --filter @napplet/demo build
  </automated>
  <manual>
    Open the demo, trigger chat/bot traffic and signer requests, and verify the highlighted route reads through shell, ACL, runtime, and the relevant service node rather than flashing one generic center box.
  </manual>
</verify>
