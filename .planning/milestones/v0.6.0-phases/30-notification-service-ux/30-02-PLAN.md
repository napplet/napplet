---
phase: 30-notification-service-ux
plan: 02
title: "Notification node, inspector, and toast UX"
wave: 2
depends_on: [30-01]
files_modified:
  - apps/demo/index.html
  - apps/demo/src/main.ts
  - apps/demo/src/flow-animator.ts
  - apps/demo/src/notification-demo.ts
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
  - NOTF-03
must_haves:
  truths:
    - "apps/demo/index.html contains a dedicated notification node and a toast layer in the visible demo layout"
    - "The notification node shows compact summary state including total and unread counts"
    - "The right-side inspector contains the full notification list plus controls for list, mark read, and dismiss"
    - "Dedicated node controls can trigger notifications through the real notification service path"
    - "Toast UI renders notification entries produced by the service callback path, not by a parallel fake model"
    - "apps/demo/src/flow-animator.ts recognizes notification activity and can light the notification node or service path"
    - "pnpm --filter @napplet/demo build succeeds"
  artifacts:
    - path: "apps/demo/index.html"
      provides: "Notification node, inspector shell, and toast container"
      contains: "notification"
    - path: "apps/demo/src/main.ts"
      provides: "DOM rendering for notification summary, inspector, and toasts"
      contains: "notification-inspector"
    - path: "apps/demo/src/flow-animator.ts"
      provides: "Notification node activity signaling"
      contains: "notification"
  key_links:
    - from: "apps/demo/src/main.ts"
      to: "apps/demo/src/notification-demo.ts"
      via: "snapshot subscription and lifecycle actions"
      pattern: "subscribe"
    - from: "apps/demo/src/flow-animator.ts"
      to: "apps/demo/index.html"
      via: "notification node element ids"
      pattern: "notification"
---

# Plan 30-02: Notification Node, Inspector, and Toast UX

<objective>
Render the notification service as a first-class part of the demo. Add a visible notification node in the topology, keep that node skim-friendly with compact counts, place the full list/history and item controls in the right-side inspector, and render educational toast UI that clearly references the service/protocol path underneath it.

This plan is the visible UX layer for all three phase requirements.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-notification-service-ux/30-CONTEXT.md
@.planning/phases/30-notification-service-ux/30-RESEARCH.md

<!-- Source of truth files -->
@apps/demo/index.html
@apps/demo/src/main.ts
@apps/demo/src/flow-animator.ts
@apps/demo/src/notification-demo.ts
@apps/demo/src/shell-host.ts
</context>

<interfaces>
### Required DOM surfaces

- `#notification-node` — topology node with compact summary
- `#notification-toast-layer` — visible toast container
- `#notification-inspector` — right-side inspector shell
- `#notification-list` — full notification inventory
- `#notification-node-create`, `#notification-node-list`, `#notification-node-mark-read`, `#notification-node-dismiss` — dedicated controls tied to real lifecycle actions

### Compact summary fields

- total notification count
- unread count
- last activity/source cue

### Inspector detail fields

- full list ordered newest-first
- per-item title/body/source/timestamp/read state
- per-item `mark read` and `dismiss` controls
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add notification node, toast layer, and inspector containers to the demo layout</name>
  <files>apps/demo/index.html</files>
  <read_first>
    - apps/demo/index.html
    - .planning/phases/29-node-detail-drill-down/29-CONTEXT.md
    - .planning/phases/30-notification-service-ux/30-CONTEXT.md
    - .planning/phases/30-notification-service-ux/30-RESEARCH.md
  </read_first>
  <action>
    Update `apps/demo/index.html` so the demo visibly includes the notification service as its own architecture surface.

    Add concrete containers and IDs for:
    - a notification node labeled `notification service` or `notifications`
    - compact text rows for `total`, `unread`, and a source/protocol cue
    - dedicated action buttons for:
      - create demo toast
      - list state
      - mark latest read
      - dismiss latest
    - a toast layer anchored above the debugger
    - a right-side inspector panel that stays visible without covering the bottom debugger

    Keep the main node compact and move detailed history/list markup into the inspector. Do not render the full notification inventory inline inside the node body.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -n "notification-node\\|notification-toast-layer\\|notification-inspector\\|notification-node-create\\|notification-node-list" apps/demo/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `apps/demo/index.html` contains `notification-node`
    - `apps/demo/index.html` contains `notification-toast-layer`
    - `apps/demo/index.html` contains `notification-inspector`
    - Dedicated node action controls exist for create, list, mark read, and dismiss
    - The node markup contains summary-only fields rather than the full inventory
  </acceptance_criteria>
  <done>Demo layout includes notification-specific containers for node, toasts, and inspector</done>
</task>

<task type="auto">
  <name>Task 2: Render notification summary, inspector details, and toasts from controller snapshots</name>
  <files>apps/demo/src/main.ts apps/demo/src/notification-demo.ts</files>
  <read_first>
    - apps/demo/src/main.ts
    - apps/demo/src/notification-demo.ts
    - apps/demo/index.html
    - packages/services/src/types.ts
  </read_first>
  <action>
    Update `apps/demo/src/main.ts` and `apps/demo/src/notification-demo.ts` so the notification UI is driven entirely from controller snapshots and lifecycle actions.

    Implement these concrete behaviors:
    - render total and unread counts into the node summary
    - render the newest notifications first in `#notification-list`
    - render toasts into `#notification-toast-layer` from the current snapshot, with visible text that references the service path, for example:
      - `notifications:create via service`
      - `notification-service update`
    - wire node buttons to real controller actions:
      - create demo toast with a concrete title/body payload
      - request list
      - mark the newest unread notification as read
      - dismiss the newest notification
    - update the inspector panel with full notification entries, including per-item buttons for read and dismiss

    Do not mutate DOM from `shell-host.ts`. Keep DOM rendering in `main.ts` and service/path state in the controller.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -n "notification-toast-layer\\|notification-list\\|mark read\\|dismiss\\|notifications:create via service\\|notification-service update" apps/demo/src/main.ts apps/demo/src/notification-demo.ts && pnpm --filter @napplet/demo build</automated>
  </verify>
  <acceptance_criteria>
    - `main.ts` renders node summary counts from controller snapshots
    - `main.ts` renders inspector list entries from controller snapshots
    - `main.ts` or `notification-demo.ts` contains visible service/protocol cue copy for toasts
    - Node controls call real controller lifecycle methods
    - Inspector entries include concrete `mark read` and `dismiss` actions
    - `pnpm --filter @napplet/demo build` succeeds
  </acceptance_criteria>
  <done>Notification summary, inspector, and toast surfaces render from real service-driven state</done>
</task>

<task type="auto">
  <name>Task 3: Extend activity signaling so the notification service path is visible in the topology</name>
  <files>apps/demo/src/flow-animator.ts apps/demo/src/main.ts</files>
  <read_first>
    - apps/demo/src/flow-animator.ts
    - apps/demo/src/main.ts
    - apps/demo/src/shell-host.ts
    - apps/demo/index.html
  </read_first>
  <action>
    Update `apps/demo/src/flow-animator.ts` and any supporting hooks in `apps/demo/src/main.ts` so notification activity highlights the new node and gives users readable service/protocol cues.

    Make the implementation concrete:
    - detect messages whose parsed topic starts with `notifications:`
    - flash the notification node when those messages flow
    - add a debugger/system message or summary label that includes the exact topic string, such as `notifications:create` or `notifications:list`
    - keep the current chat/bot signaling intact while adding notification-specific behavior

    If the current animator is too fixed to extend cleanly, explicitly refactor the box-id mapping instead of adding brittle one-off DOM queries.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -n "notifications:" apps/demo/src/flow-animator.ts apps/demo/src/main.ts && pnpm --filter @napplet/demo build && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `flow-animator.ts` contains notification-topic handling
    - Notification activity flashes or marks the notification node
    - A visible label/debug message includes the exact `notifications:*` topic string
    - Existing chat/bot animation behavior remains present
    - `pnpm type-check` passes
  </acceptance_criteria>
  <done>Notification path activity is visible in the topology and debugger cues</done>
</task>

</tasks>
