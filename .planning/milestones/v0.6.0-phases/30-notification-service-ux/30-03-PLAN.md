---
phase: 30-notification-service-ux
plan: 03
title: "Napplet-driven notification examples and regression coverage"
wave: 3
depends_on: [30-01, 30-02]
files_modified:
  - apps/demo/napplets/chat/src/main.ts
  - apps/demo/napplets/bot/src/main.ts
  - packages/services/src/notification-service.test.ts
  - tests/e2e/demo-notification-service.spec.ts
autonomous: true
requirements:
  - NOTF-02
  - NOTF-03
must_haves:
  truths:
    - "apps/demo/napplets/chat/src/main.ts emits a real notifications:create event from a concrete user flow"
    - "apps/demo/napplets/bot/src/main.ts emits a real notifications:create event from a concrete bot flow"
    - "packages/services/src/notification-service.test.ts exists and covers create, list, read, dismiss, and maxPerWindow behavior"
    - "tests/e2e/demo-notification-service.spec.ts exists and verifies node visibility, toast behavior, and inspector lifecycle controls in the demo"
    - "The Playwright spec verifies both a dedicated node-control path and a napplet-driven notification path"
    - "pnpm --filter @napplet/services test:unit passes"
  artifacts:
    - path: "apps/demo/napplets/chat/src/main.ts"
      provides: "Chat-side napplet notification trigger"
      contains: "notifications:create"
    - path: "apps/demo/napplets/bot/src/main.ts"
      provides: "Bot-side napplet notification trigger"
      contains: "notifications:create"
    - path: "packages/services/src/notification-service.test.ts"
      provides: "Unit coverage for notification lifecycle behavior"
      contains: "createNotificationService"
    - path: "tests/e2e/demo-notification-service.spec.ts"
      provides: "Demo-level regression coverage for notification UX"
      contains: "notification"
  key_links:
    - from: "apps/demo/napplets/chat/src/main.ts"
      to: "packages/services/src/notification-service.ts"
      via: "notifications:create topic path"
      pattern: "notifications:create"
    - from: "tests/e2e/demo-notification-service.spec.ts"
      to: "apps/demo/index.html"
      via: "notification node and inspector selectors"
      pattern: "notification-node"
---

# Plan 30-03: Napplet-Driven Notification Examples and Regression Coverage

<objective>
Prove that notifications originate from real napplet behavior as well as dedicated host controls, and lock the phase down with automated regression coverage. This plan finishes the “exercise it through visible toast behavior” part of the phase by ensuring users can trigger the same notification service from the architecture surface and from real demo napplet flows.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-notification-service-ux/30-CONTEXT.md
@.planning/phases/30-notification-service-ux/30-RESEARCH.md

<!-- Source of truth files -->
@apps/demo/napplets/chat/src/main.ts
@apps/demo/napplets/bot/src/main.ts
@packages/services/src/notification-service.ts
@tests/e2e/demo-audit-correctness.spec.ts
@packages/services/src/signer-service.test.ts
</context>

<interfaces>
### Napplet-driven trigger examples

- chat flow: after a user sends a message, emit `notifications:create` with a payload describing the message send path
- bot flow: when the bot learns a rule or emits a reply, emit `notifications:create` with a payload describing that bot-side behavior

### Required automated coverage

- `packages/services/src/notification-service.test.ts`
  - descriptor sanity
  - create lifecycle
  - list response
  - read transition
  - dismiss transition
  - FIFO eviction via `maxPerWindow`
- `tests/e2e/demo-notification-service.spec.ts`
  - notification node visible
  - node-control toast path works
  - napplet-driven toast path works
  - inspector list updates
  - read/dismiss controls update visible state
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add napplet-driven notification examples to chat and bot</name>
  <files>apps/demo/napplets/chat/src/main.ts apps/demo/napplets/bot/src/main.ts</files>
  <read_first>
    - apps/demo/napplets/chat/src/main.ts
    - apps/demo/napplets/bot/src/main.ts
    - packages/services/src/notification-service.ts
    - .planning/phases/30-notification-service-ux/30-CONTEXT.md
  </read_first>
  <action>
    Update both demo napplets to emit real notification-service traffic from concrete user-visible flows.

    Make the changes explicit:
    - in `apps/demo/napplets/chat/src/main.ts`, after a user sends a message, emit `notifications:create` with JSON content containing:
      - `title: "Chat message sent"`
      - `body: "<message text>"` or a truncated equivalent
    - in `apps/demo/napplets/bot/src/main.ts`, when the bot either learns a rule or sends a reply, emit `notifications:create` with JSON content containing:
      - `title: "Bot activity"`
      - `body: "<rule learned>"` or `"<reply text>"`
    - keep the existing chat/bot behavior intact; notification emission is additive
    - if helpful, listen for `notifications:created` acknowledgments and log the assigned id, but do not make the napplet flow depend on that response to succeed visibly

    Use the same event-emission mechanism the napplets already use for other inter-pane topics; do not add a host-only shortcut.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -n "notifications:create" apps/demo/napplets/chat/src/main.ts apps/demo/napplets/bot/src/main.ts && pnpm --filter @napplet/demo build</automated>
  </verify>
  <acceptance_criteria>
    - `chat/src/main.ts` contains `notifications:create`
    - `bot/src/main.ts` contains `notifications:create`
    - Chat and bot notification payloads use concrete `title` and `body` values
    - Existing message send and bot reply behavior remains present
    - `pnpm --filter @napplet/demo build` succeeds
  </acceptance_criteria>
  <done>Both demo napplets can trigger the notification service through real behavior</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for notification service lifecycle behavior</name>
  <files>packages/services/src/notification-service.test.ts</files>
  <read_first>
    - packages/services/src/notification-service.ts
    - packages/services/src/signer-service.test.ts
    - packages/services/src/types.ts
  </read_first>
  <action>
    Create `packages/services/src/notification-service.test.ts` following the style of `signer-service.test.ts`.

    Add concrete tests for:
    - descriptor values equal:
      - `name: 'notifications'`
      - `version: '1.0.0'`
    - `notifications:create` adds a notification and sends `notifications:created`
    - `notifications:list` returns a `notifications:listed` response containing current notifications for the requesting window
    - `notifications:read` flips `read` from `false` to `true`
    - `notifications:dismiss` removes the targeted notification
    - `maxPerWindow: 2` evicts the oldest notification after three creates
    - `onWindowDestroyed(windowId)` removes that window's notifications and triggers `onChange`

    Use concrete test topics and payloads rather than abstract helper names.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && test -f packages/services/src/notification-service.test.ts && pnpm --filter @napplet/services test:unit -- notification-service</automated>
  </verify>
  <acceptance_criteria>
    - `packages/services/src/notification-service.test.ts` exists
    - The spec asserts create, list, read, dismiss, and `maxPerWindow`
    - The spec verifies the `notifications:created` and `notifications:listed` response topics
    - `pnpm --filter @napplet/services test:unit -- notification-service` passes
  </acceptance_criteria>
  <done>Notification service lifecycle is covered by unit tests</done>
</task>

<task type="auto">
  <name>Task 3: Add demo Playwright coverage for notification node, toasts, and inspector lifecycle</name>
  <files>tests/e2e/demo-notification-service.spec.ts</files>
  <read_first>
    - tests/e2e/demo-audit-correctness.spec.ts
    - apps/demo/index.html
    - apps/demo/src/main.ts
    - apps/demo/napplets/chat/src/main.ts
    - apps/demo/napplets/bot/src/main.ts
  </read_first>
  <action>
    Create `tests/e2e/demo-notification-service.spec.ts` by following the direct-demo boot pattern used in `demo-audit-correctness.spec.ts`.

    Add concrete Playwright tests that:
    - open the real demo on port `4174`
    - verify the notification node is visible in the flow view
    - click the dedicated node control to create a notification and assert:
      - a toast appears
      - the notification node summary count increments
      - the debugger or visible UI contains `notifications:create`
    - trigger a chat or bot action that now emits `notifications:create` and assert a second toast appears from napplet-driven behavior
    - open the notification inspector and verify the full list updates
    - use `mark read` and `dismiss` controls and verify unread count/list state changes visibly

    Use stable selectors based on the IDs introduced in Plan 30-02 (`notification-node`, `notification-toast-layer`, `notification-inspector`, `notification-list`).
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet && test -f tests/e2e/demo-notification-service.spec.ts && npx playwright test tests/e2e/demo-notification-service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/demo-notification-service.spec.ts` exists
    - The spec checks a dedicated node-control notification path
    - The spec checks a napplet-driven notification path
    - The spec checks inspector read/dismiss behavior
    - The spec asserts at least one visible `notifications:create` cue
  </acceptance_criteria>
  <done>Demo notification UX is protected by end-to-end regression coverage</done>
</task>

</tasks>
