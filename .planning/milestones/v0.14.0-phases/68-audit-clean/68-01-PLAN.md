---
phase: 68-audit-clean
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - turbo.json
  - test-results/
  - PRBODY.md
  - vitest.config.ts
  - .changeset/config.json
autonomous: true
requirements: [SRC-01, SRC-02, SRC-03, SRC-04, CFG-01, CFG-02, CFG-03]

must_haves:
  truths:
    - "turbo.json contains no test:e2e task"
    - "test-results/ directory does not exist at repo root"
    - "PRBODY.md does not exist at repo root"
    - "vitest.config.ts aliases only reference current packages (core, shim)"
    - ".changeset/config.json references no extracted packages"
    - "root package.json scripts reference no extracted packages"
    - "Every export in packages/core/src/index.ts is either consumed by shim/sdk/vite-plugin or is a documented public API type"
    - "Every export in packages/shim, packages/sdk, packages/vite-plugin is consumed or is a public API entry point"
    - "pnpm build && pnpm type-check passes clean"
  artifacts:
    - path: "turbo.json"
      provides: "Task definitions for only build, test, test:unit, type-check, lint"
      contains: "test:unit"
    - path: "vitest.config.ts"
      provides: "Test config with only current package aliases"
      contains: "@napplet/core"
  key_links:
    - from: "turbo.json"
      to: "package.json scripts"
      via: "turbo task names must match package.json script names"
      pattern: "test:unit"
---

<objective>
Remove dead config entries, stale build artifacts, and verify no unused exports exist across all 4 packages.

Purpose: Eliminate leftover v0.13.0 extraction artifacts from source and config files so the repo is clean.
Output: Clean turbo.json, verified vitest.config.ts and changeset config, no stale root artifacts, confirmed export audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-audit-clean/68-CONTEXT.md

@turbo.json
@vitest.config.ts
@.changeset/config.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale artifacts and dead turbo task</name>
  <files>turbo.json, test-results/, PRBODY.md</files>
  <action>
1. Delete the `test-results/` directory entirely (stale Playwright artifacts from pre-extraction e2e tests). Use `rm -rf test-results/`.

2. Delete `PRBODY.md` at repo root (stray PR body file). Use `rm PRBODY.md`.

3. Edit `turbo.json`: Remove the `"test:e2e"` task entry (lines 15-18). No package implements a `test:e2e` script — this was left over from when the demo/test suite lived in this repo. The remaining tasks should be: `build`, `test`, `test:unit`, `type-check`, `lint`.

4. Verify turbo.json is valid JSON after edit.

Per SRC-04: turbo.json cleaned (dead test:e2e task removed).
Per CFG-01: test-results/ directory removed.
Per DOC-04 (partial): PRBODY.md removed.
  </action>
  <verify>
    <automated>test ! -d test-results && test ! -f PRBODY.md && ! grep -q 'test:e2e' turbo.json && node -e "JSON.parse(require('fs').readFileSync('turbo.json','utf8')); console.log('valid JSON')"</automated>
  </verify>
  <done>test-results/ gone, PRBODY.md gone, turbo.json has no test:e2e task, turbo.json is valid JSON with tasks: build, test, test:unit, type-check, lint.</done>
</task>

<task type="auto">
  <name>Task 2: Audit configs and verify exports</name>
  <files>vitest.config.ts, .changeset/config.json, package.json</files>
  <action>
**Config audit (SRC-03, CFG-02, CFG-03):**

1. Read `vitest.config.ts` — verify aliases only reference `@napplet/core` and `@napplet/shim` (both are current packages). There should be NO aliases for `@napplet/shell`, `@napplet/runtime`, `@napplet/acl`, or `@napplet/services`. Current content is clean — no changes needed unless stale aliases found.

2. Read `.changeset/config.json` — verify no `linked`, `fixed`, or `ignore` arrays reference extracted packages. Current content has empty arrays — no changes needed.

3. Read root `package.json` — verify `scripts` section has no `test:e2e`, `test:build`, `test:serve` references and no extracted package names. Current scripts are: build, test, test:unit, lint, type-check, version-packages, publish-packages — all clean.

**Export audit (SRC-01, SRC-02):**

4. Audit `packages/core/src/index.ts` exports: Every exported symbol must be either (a) consumed by packages/shim, packages/sdk, or packages/vite-plugin source code, OR (b) a documented public API type for downstream consumers (e.g., @kehto/shell). Check each export:
   - `NostrEvent`, `NostrFilter` — consumed by shim and sdk. KEEP.
   - `Capability`, `ALL_CAPABILITIES` — public API for ACL consumers. KEEP.
   - `ServiceDescriptor`, `ServiceInfo` — consumed by sdk (ServiceInfo). KEEP.
   - `Subscription`, `EventTemplate` — consumed by shim and sdk. KEEP.
   - `NappletGlobal` — consumed by shim and sdk. KEEP.
   - `RegisterPayload`, `IdentityPayload` — public API types for shell/runtime implementors (used by @kehto/shell). KEEP as public API.
   - `BusKind`, `AUTH_KIND`, `SHELL_BRIDGE_URI`, `PROTOCOL_VERSION`, `REPLAY_WINDOW_SECONDS`, `DESTRUCTIVE_KINDS`, `VERB_REGISTER`, `VERB_IDENTITY` — consumed by shim or are public API constants. KEEP.
   - `BusKindValue`, `TopicKey`, `TopicValue` — type utility exports. KEEP.
   - `TOPICS` — consumed by shim (state-shim.ts). KEEP.

5. Audit `packages/shim/src/index.ts` — side-effect-only module, no named exports by design. CLEAN.

6. Audit `packages/sdk/src/index.ts` — exports `relay`, `ipc`, `services`, `storage` objects plus re-exported types from core. All are public API. CLEAN.

7. Audit `packages/vite-plugin/src/index.ts` — exports `nip5aManifest` function and `Nip5aManifestOptions` interface. Both are the public API. CLEAN.

8. Run `pnpm build && pnpm type-check` to verify everything passes clean after Task 1's changes.

If any stale items found in steps 1-3, fix them. If any truly dead exports found in steps 4-7 (not public API surface), remove them.
  </action>
  <verify>
    <automated>! grep -q '@napplet/\(shell\|runtime\|acl\|services\)' vitest.config.ts .changeset/config.json package.json 2>/dev/null && pnpm build && pnpm type-check</automated>
  </verify>
  <done>vitest.config.ts has no stale aliases. .changeset/config.json has no extracted package refs. Root package.json scripts are clean. All 4 packages have no dead exports (every export is consumed or is public API). pnpm build and pnpm type-check pass clean.</done>
</task>

</tasks>

<verification>
- `test ! -d test-results` — stale Playwright artifacts gone
- `test ! -f PRBODY.md` — stray root file gone
- `! grep -q 'test:e2e' turbo.json` — dead turbo task removed
- `! grep -rq '@napplet/\(shell\|runtime\|acl\|services\)' vitest.config.ts .changeset/config.json package.json turbo.json` — no extracted package refs in config
- `pnpm build && pnpm type-check` — builds clean
</verification>

<success_criteria>
1. turbo.json has exactly 5 tasks: build, test, test:unit, type-check, lint
2. test-results/ directory does not exist
3. PRBODY.md does not exist
4. No config file references @napplet/shell, @napplet/runtime, @napplet/acl, or @napplet/services
5. All exports in all 4 packages are justified (consumed or public API)
6. pnpm build && pnpm type-check exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/68-audit-clean/68-01-SUMMARY.md`
</output>
