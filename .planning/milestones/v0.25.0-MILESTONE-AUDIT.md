---
milestone: v0.25.0
milestone_name: Config NUB
audited: 2026-04-17
status: passed
scores:
  requirements: 38/38
  phases: 6/6
  integration: verified (type-level)
  flows: verified (build gate + runtime wire surface)
---

# v0.25.0 Milestone Audit — Config NUB

## Summary

All 6 phases completed and verified. All 38 requirements satisfied. Cross-phase integration verified by full-monorepo `pnpm type-check` gate at each phase boundary. Public NUB-CONFIG spec drafted and PR #13 opened at napplet/nubs.

## Phase Verification Aggregate

| Phase | Name | VERIFICATION Status | Score |
|-------|------|---------------------|-------|
| 111 | NUB-CONFIG Spec | passed | 11/11 |
| 112 | NUB Config Package Scaffold | passed | 12/12 |
| 113 | NUB Config Shim + SDK | passed | 12/12 |
| 114 | Vite-Plugin Extension | passed | 7/7 |
| 115 | Core / Shim / SDK Integration + Wire | passed | 11/11 |
| 116 | Documentation | passed | 6/6 |

All 6 phases ✅. No phase reported gaps_found, human_needed, or blockers.

## Requirements Coverage (38/38)

| Category | REQ-IDs | Phase | Status |
|----------|---------|-------|--------|
| Spec | SPEC-01..08 (8) | 111 | ✓ satisfied |
| Package types | NUB-01, NUB-02, NUB-05, NUB-06 (4) | 112 | ✓ satisfied |
| Package shim + SDK | NUB-03, NUB-04 (2) | 113 | ✓ satisfied |
| Vite-plugin | VITE-01..07 (7) | 114 | ✓ satisfied |
| Wire surface | WIRE-01..06 (6) | 115 | ✓ satisfied |
| Core integration | CORE-01, CORE-02 (2) | 115 | ✓ satisfied |
| Shim integration | SHIM-01 (1) | 115 | ✓ satisfied |
| SDK integration | SDK-01 (1) | 115 | ✓ satisfied |
| Capability | CAP-01 (1) | 115 | ✓ satisfied |
| Documentation | DOC-01..06 (6) | 116 | ✓ satisfied |

**Orphans:** None. All REQ-IDs in traceability table map to a verified phase.

## Cross-Phase Integration

Verified by the full-monorepo `pnpm type-check` gate run after each phase:

- **112 → 115**: `packages/nubs/config/src/types.ts` types consumed by shim/SDK type-level config namespace ✓
- **113 → 115**: `installConfigShim`/`handleConfigMessage` from phase 113 are imported and mounted in `@napplet/shim` in phase 115 ✓
- **111 → 112/113/114**: Spec contract (wire types, error codes, Core Subset) consumed by package types, shim validation, and vite-plugin build-time guards ✓
- **114 → 113**: Manifest's `['config', ...]` tag + `<meta name="napplet-config-schema">` emissions consumed by shim's synchronous meta-read at install ✓
- **Full monorepo build**: 13/13 packages build clean; 22/22 turbo type-check tasks green

## Wire Surface Round-Trip Verification

Six wire messages confirmed routable end-to-end by type system + shim routing branch:

| Wire | Direction | Verified |
|------|-----------|----------|
| config.registerSchema | napplet → shell | ✓ correlated `.result` ACK |
| config.get | napplet → shell | ✓ correlated `config.values` response |
| config.subscribe | napplet → shell | ✓ triggers immediate + push `config.values` |
| config.unsubscribe | napplet → shell | ✓ ref-counted stop |
| config.values | shell → napplet | ✓ dual-use (response + push) |
| config.openSettings | napplet → shell | ✓ section deep-link |

Plus `config.schemaError` (shell → napplet async) routes correctly.

## Tech Debt

**None at critical severity.**

Minor harmonization notes tracked in phase SUMMARYs (not blocking):
- Phase 113 flagged `onSchemaError` returns `() => void` vs spec TS sketch `Subscription`. Plan-locked `() => void` shipped. Shim/SDK/core types all aligned. Revisit in NUB-CONFIG v2 if a real napplet needs Subscription semantics.
- Phase 113 flagged `registerSchema` typed `Promise<void>` vs spec TS sketch `void`. Positive-ACK wire behavior makes `Promise` correct; spec TS sketch was an aspirational doc artifact. No action.

## Deferred Requirements (to future milestones)

Captured in REQUIREMENTS.md "Future Requirements" section:
- FUT-01: `config.unregisterSchema` (HMR ergonomics)
- FUT-02: `config.getSecret(key)` (Tier 1 secret handling)
- FUT-03: `pattern` keyword readmission (once shell-safe regex strategy specified)
- FUT-04: Tier 2 secret handling (encrypted at rest)
- FUT-05: Richer `format` widget rendering
- FUT-06: Change-diff push envelope
- FUT-07: `examples` keyword
- FUT-08: Per-instance config layering

All explicitly scoped out of v0.25.0 during requirements phase — not surprises.

## Public Spec Status

- NUB-CONFIG.md drafted on `nub-config` branch at /home/sandwich/Develop/nubs/
- PR #13 opened at napplet/nubs (human-confirmed 2026-04-17)
- README.md registry row added on same branch
- Zero `@napplet/*` references in public-repo content or commit messages (verified multiple times across phases 111 and 116)

## Nyquist Compliance

Not applicable for this milestone — phases 111 (spec) and 116 (docs) produce markdown, not testable code; phases 112-115 are type-level integrations covered by the `pnpm type-check` gate. No VALIDATION.md files created or needed.

## Verdict

**passed** — milestone achieved. Ready for `/gsd:complete-milestone v0.25.0`.
