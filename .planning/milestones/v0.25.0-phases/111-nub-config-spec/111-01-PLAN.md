---
phase: 111-nub-config-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/sandwich/Develop/nubs/NUB-CONFIG.md
autonomous: true
requirements: [SPEC-01]
must_haves:
  truths:
    - "A new git branch 'nub-config' exists in /home/sandwich/Develop/nubs/ cut from master"
    - "A file NUB-CONFIG.md exists on that branch at the repo root"
    - "The file has the correct setext header (NUB-CONFIG === / Per-Napplet Declarative Configuration ---), the draft label, the NUB ID / Namespace / Discovery metadata lines, a Description section, an API Surface section with TypeScript interfaces, and a Wire Protocol section with a message table and one example envelope per message type"
    - "The file contains zero @napplet/ references"
  artifacts:
    - path: "/home/sandwich/Develop/nubs/NUB-CONFIG.md"
      provides: "NUB-CONFIG spec skeleton — header, description, API surface, wire protocol"
      contains: "NUB-CONFIG"
  key_links:
    - from: "nubs repo branch 'nub-config'"
      to: "NUB-CONFIG.md file"
      via: "git add + git commit on the branch"
      pattern: "git ls-tree nub-config -- NUB-CONFIG.md"
---

<objective>
Scaffold the public NUB-CONFIG specification on a new branch of the napplet/nubs PUBLIC repo. Lay down the header, description, API surface (TypeScript interfaces only — no implementations), and the wire protocol table with one example envelope per message type.

Purpose: Establishes the structural skeleton that subsequent plans fill in (schema contract, shell guarantees, anti-features, security, error envelopes). Matches the template of NUB-IDENTITY / NUB-NOTIFY.
Output: NUB-CONFIG.md on the `nub-config` branch of /home/sandwich/Develop/nubs/, committed but NOT pushed.

CRITICAL CONSTRAINTS:
- This work happens in /home/sandwich/Develop/nubs/ (the PUBLIC nubs repo) — NOT in /home/sandwich/Develop/napplet/.
- The spec file MUST contain ZERO `@napplet/*` references. It is a PUBLIC spec; no references to private packages, repos, or implementations.
- The PR is NOT opened in this plan. That is gated behind user confirmation in plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sandwich/Develop/napplet/.planning/REQUIREMENTS.md
@/home/sandwich/Develop/napplet/.planning/phases/111-nub-config-spec/111-CONTEXT.md
@/home/sandwich/Develop/napplet/.planning/research/ARCHITECTURE.md
@/home/sandwich/Develop/nubs/CLAUDE.md
@/home/sandwich/Develop/nubs/TEMPLATE-NN.md
@/home/sandwich/Develop/nubs/README.md

<interfaces>
<!-- Structural pattern extracted from the NUB-IDENTITY spec on the nub-identity branch. -->
<!-- Read that file directly via `git show nub-identity:NUB-IDENTITY.md` in /home/sandwich/Develop/nubs/ to see the full pattern. -->

NUB-IDENTITY structure (to be matched verbatim for NUB-CONFIG):

```
NUB-IDENTITY
============

Read-Only User Identity Queries
--------------------------------

`draft`

**NUB ID:** NUB-IDENTITY
**Namespace:** `window.napplet.identity`
**Discovery:** `shell.supports("identity")`

## Description
(1-2 paragraphs)

## API Surface
```typescript
interface NappletIdentity { ... }
```
(plus per-method bullet descriptions)

## Wire Protocol
(table with Type | Direction | Payload fields, then Examples subsection with envelope snippets)

## Shell Behavior
## Security Considerations
## Implementations
- (none yet)
```

For NUB-CONFIG the header is:

```
NUB-CONFIG
==========

Per-Napplet Declarative Configuration
-------------------------------------

`draft`

**NUB ID:** NUB-CONFIG
**Namespace:** `window.napplet.config`
**Discovery:** `shell.supports("config")`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create nub-config branch and stub NUB-CONFIG.md on it</name>
  <files>/home/sandwich/Develop/nubs/NUB-CONFIG.md</files>
  <read_first>
    - /home/sandwich/Develop/nubs/CLAUDE.md
    - /home/sandwich/Develop/nubs/TEMPLATE-NN.md
    - /home/sandwich/Develop/nubs/README.md
    - Current branch list via `cd /home/sandwich/Develop/nubs && git branch`
    - NUB-IDENTITY precedent via `cd /home/sandwich/Develop/nubs && git show nub-identity:NUB-IDENTITY.md`
    - /home/sandwich/Develop/napplet/.planning/phases/111-nub-config-spec/111-CONTEXT.md (spec structure decision)
  </read_first>
  <action>
    Work entirely inside /home/sandwich/Develop/nubs/ (the PUBLIC repo). Do NOT touch /home/sandwich/Develop/napplet/ in this task.

    1. Ensure working tree is clean on master:
       - `cd /home/sandwich/Develop/nubs && git status --porcelain` must be empty
       - `git checkout master && git pull --ff-only`
    2. Create a new branch cut from master:
       - `git checkout -b nub-config`
    3. Create the initial NUB-CONFIG.md stub at the repo root (single file, zero existing content) with ONLY the header block — later tasks fill in content. Exact bytes of the file after this task:

       ```
       NUB-CONFIG
       ==========

       Per-Napplet Declarative Configuration
       -------------------------------------

       `draft`

       **NUB ID:** NUB-CONFIG
       **Namespace:** `window.napplet.config`
       **Discovery:** `shell.supports("config")`
       **Parent:** NIP-5D

       ## Description

       (to be filled)
       ```

       NOTE: This must be setext-style (underlines with `=` and `-`), NOT ATX (`#`), for the top two levels, matching NUB-IDENTITY exactly. Sections inside the document use ATX `##`.
    4. Do NOT commit yet. Leave the file staged only after task 2 adds content — task 2 and task 3 commit together to avoid half-populated commits on the branch.
    5. Verify branch state: `git rev-parse --verify nub-config` must succeed and HEAD must point to it.

    Absolute rule: no `@napplet/` string may appear in this file at any time. No link to napplet.com, github.com/napplet/napplet, or any private package.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nubs &amp;&amp; git rev-parse --verify nub-config &amp;&amp; test -f NUB-CONFIG.md &amp;&amp; grep -q '^NUB-CONFIG$' NUB-CONFIG.md &amp;&amp; grep -q '^==========$' NUB-CONFIG.md &amp;&amp; grep -q '^\*\*NUB ID:\*\* NUB-CONFIG$' NUB-CONFIG.md &amp;&amp; grep -q '^\*\*Namespace:\*\* `window.napplet.config`$' NUB-CONFIG.md &amp;&amp; grep -q '^\*\*Discovery:\*\* `shell.supports("config")`$' NUB-CONFIG.md &amp;&amp; grep -q '^\*\*Parent:\*\* NIP-5D$' NUB-CONFIG.md &amp;&amp; ! grep -q '@napplet/' NUB-CONFIG.md</automated>
  </verify>
  <acceptance_criteria>
    - cd /home/sandwich/Develop/nubs && git rev-parse --verify nub-config    # branch exists
    - test -f /home/sandwich/Develop/nubs/NUB-CONFIG.md    # file exists
    - grep -q '^NUB-CONFIG$' /home/sandwich/Develop/nubs/NUB-CONFIG.md    # setext header
    - grep -q '^==========$' /home/sandwich/Develop/nubs/NUB-CONFIG.md    # setext underline
    - grep -q '^\*\*NUB ID:\*\* NUB-CONFIG$' /home/sandwich/Develop/nubs/NUB-CONFIG.md
    - grep -q '^\*\*Namespace:\*\* `window.napplet.config`$' /home/sandwich/Develop/nubs/NUB-CONFIG.md
    - grep -q '^\*\*Discovery:\*\* `shell.supports("config")`$' /home/sandwich/Develop/nubs/NUB-CONFIG.md
    - grep -q '^\*\*Parent:\*\* NIP-5D$' /home/sandwich/Develop/nubs/NUB-CONFIG.md
    - ! grep -q '@napplet/' /home/sandwich/Develop/nubs/NUB-CONFIG.md    # zero private-repo refs
  </acceptance_criteria>
  <done>
    - Branch `nub-config` exists in the nubs repo with HEAD pointing to it
    - NUB-CONFIG.md exists at repo root with the exact setext header shown in &lt;action&gt;
    - File contains zero @napplet/ references
  </done>
</task>

<task type="auto">
  <name>Task 2: Fill Description + API Surface + Wire Protocol sections and commit the scaffold</name>
  <files>/home/sandwich/Develop/nubs/NUB-CONFIG.md</files>
  <read_first>
    - /home/sandwich/Develop/nubs/NUB-CONFIG.md (current stub from task 1)
    - NUB-IDENTITY full structure via `cd /home/sandwich/Develop/nubs && git show nub-identity:NUB-IDENTITY.md`
    - /home/sandwich/Develop/napplet/.planning/REQUIREMENTS.md (wire surface WIRE-01..06 for message name authority)
    - /home/sandwich/Develop/napplet/.planning/research/ARCHITECTURE.md lines 111-250 (wire message definitions + TypeScript types + dual-use config.values rationale)
    - /home/sandwich/Develop/napplet/.planning/phases/111-nub-config-spec/111-CONTEXT.md (correlation-ID field is `id` per locked decision; positive-ACK pattern for registerSchema)
  </read_first>
  <action>
    Append these three sections to NUB-CONFIG.md (replacing the `(to be filled)` placeholder). Write directly in the nubs repo file; do not mirror to napplet repo.

    SECTION 1 — `## Description` (replace the placeholder). Exactly 2 paragraphs, roughly:

    Para 1 — What NUB-CONFIG is:
    > NUB-CONFIG provides per-napplet declarative configuration. A napplet declares a JSON Schema (draft-07 or later) describing its user-facing settings surface; the shell renders a settings UI, validates user input against the schema, persists values in a napplet-scoped store, and delivers live values back to the napplet via snapshot + push.

    Para 2 — The shell-sole-writer invariant:
    > The shell is the sole writer of configuration values. Napplets cannot mutate configuration over the wire — they may only read (`config.get`), subscribe to live updates (`config.subscribe`), register a schema (`config.registerSchema`), and request that the shell open its settings UI (`config.openSettings`). All values a napplet sees have been validated by the shell against the currently-registered schema, with declared defaults applied.

    SECTION 2 — `## API Surface`. TypeScript interface block inside a fenced ```typescript code block. No implementation code. Include, in this order:

    ```typescript
    interface NappletConfig {
      /** Runtime escape hatch — prefer manifest-declared schema. */
      registerSchema(schema: ConfigSchema, version?: number): void;

      /** One-shot snapshot of current validated + defaulted values. */
      get(): Promise<ConfigValues>;

      /** Subscribe to live updates; first delivery is an immediate snapshot. */
      subscribe(callback: (values: ConfigValues) => void): Subscription;

      /** Listen for schema-registration errors. */
      onSchemaError(callback: (err: ConfigSchemaError) => void): Subscription;

      /** Request the shell open its settings UI, optionally deep-linking by section. */
      openSettings(options?: { section?: string }): void;

      /** Read-access to the currently-registered schema (from manifest or registerSchema). */
      readonly schema: ConfigSchema | null;
    }

    /**
     * JSON Schema (draft-07+) describing a napplet's configuration surface.
     * Restricted to the Core Subset defined in Schema Contract below.
     */
    type ConfigSchema = Record<string, unknown>;

    /**
     * Shell-validated configuration values, shaped by the registered schema.
     * Keys correspond to schema properties; values are JSON-serializable.
     */
    type ConfigValues = Record<string, unknown>;

    interface ConfigSchemaError {
      code: 'invalid-schema' | 'version-conflict' | 'unsupported-draft'
          | 'ref-not-allowed' | 'pattern-not-allowed' | 'secret-with-default';
      message: string;
    }

    interface Subscription {
      close(): void;
    }
    ```

    Then one-paragraph prose per method (mirroring NUB-IDENTITY's bullet-by-method style). Each bullet is 1-3 lines describing when to call and what it returns. Make explicit that `registerSchema` is an escape hatch and the manifest path is preferred (authority without naming any specific build tool).

    SECTION 3 — `## Wire Protocol`. Paragraph preamble matching NUB-IDENTITY:
    > `config.*` messages use the NIP-5D wire format (`{ "type": "domain.action", ...payload }`).

    Then the message table with exactly these 9 rows (6 napplet→shell + 3 shell→napplet). Correlation-ID field is `id` (per 111-CONTEXT decision). `config.values` is dual-use: `id` present = response to `config.get`; `id` absent = subscription push.

    | Type | Direction | Payload fields |
    |------|-----------|----------------|
    | `config.registerSchema` | napplet -> shell | `id`, `schema`, `version?` |
    | `config.registerSchema.result` | shell -> napplet | `id`, `ok`, `error?`, `code?` |
    | `config.get` | napplet -> shell | `id` |
    | `config.subscribe` | napplet -> shell | (none) |
    | `config.unsubscribe` | napplet -> shell | (none) |
    | `config.openSettings` | napplet -> shell | `section?` |
    | `config.values` | shell -> napplet | `id?`, `values` |
    | `config.schemaError` | shell -> napplet | `error`, `code` |

    Design notes bullet list matching NUB-IDENTITY style, stating:
    - All correlation uses `id`.
    - `config.values` is dual-use: with `id` it answers `config.get`; without `id` it is a subscription push.
    - `config.registerSchema` uses positive-ACK (`ok: true` or `ok: false` with `code`+`error`) — not error-only.
    - `config.subscribe` MUST produce an immediate initial `config.values` push (snapshot delivery).
    - `config.unsubscribe` is the clean-teardown path; iframe unmount also implicitly unsubscribes.

    Then `### Examples` subsection with one minimal JSON envelope per message type (mirror NUB-IDENTITY formatting — triple-backtick blocks, `->` for napplet→shell and `<-` for shell→napplet). Cover at minimum these cases:
    1. Register schema success (registerSchema + registerSchema.result ok)
    2. Register schema failure (registerSchema + registerSchema.result with ok:false, code:"pattern-not-allowed")
    3. Get values (config.get + config.values with id)
    4. Subscribe + initial snapshot (config.subscribe then config.values without id)
    5. Push update after user changes a value (another config.values without id)
    6. Unsubscribe (config.unsubscribe)
    7. Open settings with section deep-link (config.openSettings with section: "notifications")
    8. Schema error push after malformed registerSchema in background (config.schemaError)

    Then commit ALL changes so far (the stub from task 1 + this content) in a single commit on the `nub-config` branch:
    - Commit command (run inside /home/sandwich/Develop/nubs/):
      `git add NUB-CONFIG.md && git commit -m "docs: add NUB-CONFIG spec skeleton (description, API surface, wire protocol)"`

    The commit MUST contain zero `@napplet/` references (neither in the file nor in the commit message). Per the nubs CLAUDE.md rule "Commit messages — describe the protocol change only, never reference private packages."
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nubs &amp;&amp; git rev-parse --verify nub-config &amp;&amp; git log nub-config --oneline -1 | grep -q 'NUB-CONFIG' &amp;&amp; grep -q '^## Description$' NUB-CONFIG.md &amp;&amp; grep -q '^## API Surface$' NUB-CONFIG.md &amp;&amp; grep -q '^## Wire Protocol$' NUB-CONFIG.md &amp;&amp; grep -q 'window.napplet.config' NUB-CONFIG.md &amp;&amp; grep -q '`config.registerSchema`' NUB-CONFIG.md &amp;&amp; grep -q '`config.get`' NUB-CONFIG.md &amp;&amp; grep -q '`config.subscribe`' NUB-CONFIG.md &amp;&amp; grep -q '`config.unsubscribe`' NUB-CONFIG.md &amp;&amp; grep -q '`config.values`' NUB-CONFIG.md &amp;&amp; grep -q '`config.openSettings`' NUB-CONFIG.md &amp;&amp; grep -q '`config.registerSchema.result`' NUB-CONFIG.md &amp;&amp; grep -q '`config.schemaError`' NUB-CONFIG.md &amp;&amp; grep -q 'interface NappletConfig' NUB-CONFIG.md &amp;&amp; grep -q 'type ConfigSchema' NUB-CONFIG.md &amp;&amp; grep -q 'type ConfigValues' NUB-CONFIG.md &amp;&amp; grep -q '^### Examples$' NUB-CONFIG.md &amp;&amp; ! grep -q '@napplet/' NUB-CONFIG.md &amp;&amp; ! git log nub-config --format=%B -1 | grep -q '@napplet/'</automated>
  </verify>
  <acceptance_criteria>
    - grep -q '^## Description$' /home/sandwich/Develop/nubs/NUB-CONFIG.md
    - grep -q '^## API Surface$' /home/sandwich/Develop/nubs/NUB-CONFIG.md
    - grep -q '^## Wire Protocol$' /home/sandwich/Develop/nubs/NUB-CONFIG.md
    - grep -q '^### Examples$' /home/sandwich/Develop/nubs/NUB-CONFIG.md
    - grep -q 'interface NappletConfig' /home/sandwich/Develop/nubs/NUB-CONFIG.md
    - grep -q 'type ConfigSchema' /home/sandwich/Develop/nubs/NUB-CONFIG.md
    - grep -q 'type ConfigValues' /home/sandwich/Develop/nubs/NUB-CONFIG.md
    - grep -c '`config\.registerSchema`\|`config\.get`\|`config\.subscribe`\|`config\.unsubscribe`\|`config\.openSettings`\|`config\.values`\|`config\.registerSchema\.result`\|`config\.schemaError`' /home/sandwich/Develop/nubs/NUB-CONFIG.md    # expect ≥ 8 occurrences (one per message type)
    - cd /home/sandwich/Develop/nubs && git log nub-config --oneline -1 | grep -q 'NUB-CONFIG'    # commit exists
    - cd /home/sandwich/Develop/nubs && ! git log nub-config --format=%B -1 | grep -q '@napplet/'    # commit message clean
    - ! grep -q '@napplet/' /home/sandwich/Develop/nubs/NUB-CONFIG.md
  </acceptance_criteria>
  <done>
    - NUB-CONFIG.md on the `nub-config` branch contains Description, API Surface (with NappletConfig / ConfigSchema / ConfigValues / ConfigSchemaError / Subscription types), and Wire Protocol sections
    - Wire Protocol table lists all 6 napplet→shell and all 3 shell→napplet message types
    - Examples subsection contains >=8 envelope blocks (one per message type + one error case)
    - One commit exists on the `nub-config` branch with message describing only the protocol change — no @napplet/ references in file or commit message
  </done>
</task>

</tasks>

<verification>
- Branch `nub-config` exists and is checked out
- NUB-CONFIG.md has a valid setext header, the metadata block, and sections: Description, API Surface, Wire Protocol (with Examples subsection)
- Message table enumerates all six napplet→shell message types (registerSchema, get, subscribe, unsubscribe, openSettings + registerSchema.result correlated response) and all three shell→napplet types (values, schemaError, registerSchema.result)
- No `@napplet/` reference anywhere in the file or in any commit made on this branch
- Single commit on the branch covers the scaffold work
</verification>

<success_criteria>
- `grep -q '^NUB-CONFIG$' /home/sandwich/Develop/nubs/NUB-CONFIG.md` succeeds
- `! grep -q '@napplet/' /home/sandwich/Develop/nubs/NUB-CONFIG.md` succeeds
- `cd /home/sandwich/Develop/nubs && git rev-parse --verify nub-config` succeeds
- Every message type from requirements WIRE-01..06 appears in the wire table
</success_criteria>

<output>
After completion, create `.planning/phases/111-nub-config-spec/111-01-SUMMARY.md` with:
- Branch name and commit SHA
- File path + byte size
- List of sections written
- Confirmation of no @napplet/ leakage
</output>
