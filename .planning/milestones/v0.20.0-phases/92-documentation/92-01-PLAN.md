---
phase: 92-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/NIP-5D.md
  - packages/nubs/keys/README.md
  - packages/core/README.md
  - packages/shim/README.md
  - packages/sdk/README.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "NIP-5D lists all six NUB domains including keys with a reference to NUB-KEYS"
    - "nub-keys README has a wire protocol message reference table with all 6 message types"
    - "Core README mentions keys in NubDomain table and NUB_DOMAINS array"
    - "Shim README documents window.napplet.keys with smart forwarding rules"
    - "SDK README documents keys namespace with registerAction, unregisterAction, onAction"
  artifacts:
    - path: "specs/NIP-5D.md"
      provides: "NUB domain table with keys row"
      contains: "keys"
    - path: "packages/nubs/keys/README.md"
      provides: "Wire protocol message reference"
      contains: "keys.forward"
    - path: "packages/core/README.md"
      provides: "keys domain documentation"
      contains: "'keys'"
    - path: "packages/shim/README.md"
      provides: "keys shim documentation"
      contains: "window.napplet.keys"
    - path: "packages/sdk/README.md"
      provides: "keys SDK documentation"
      contains: "keys.registerAction"
  key_links:
    - from: "specs/NIP-5D.md"
      to: "NUB-KEYS spec"
      via: "NUB domain table reference"
      pattern: "keys.*NUB-KEYS"
    - from: "packages/nubs/keys/README.md"
      to: "NUB-KEYS spec"
      via: "Protocol Reference link"
      pattern: "NUB-KEYS"
---

<objective>
Verify and complete keys NUB documentation across all READMEs and NIP-5D.

Purpose: Developers can learn how to use the keys NUB from package READMEs and NIP-5D. Most documentation was added in cross-phase commit 4e798d0 -- this plan verifies completeness against acceptance criteria and closes any gaps.

Output: All five documentation files verified against DOC-01/DOC-02/DOC-03 criteria, with NIP-5D updated to include a NUB domain table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/92-documentation/92-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NUB domain table to NIP-5D</name>
  <files>specs/NIP-5D.md</files>
  <read_first>specs/NIP-5D.md</read_first>
  <action>
NIP-5D currently has a generic "NUB Extension Framework" section that uses a hypothetical `foo` NUB as example but does not enumerate the actual NUB domains. Add a "## Known NUBs" section after the "NUB Extension Framework" section and before "Security Considerations" that lists all six NUB domains in a table. Per DOC-02, this table MUST include a `keys` row referencing NUB-KEYS.

Add this section to specs/NIP-5D.md:

```
## Known NUBs

The following NUB specs are defined for this protocol:

| Domain    | Spec     | Scope                                    |
|-----------|----------|------------------------------------------|
| `relay`   | NUB-RELAY   | Relay proxy (subscribe, publish, query)  |
| `signer`  | NUB-SIGNER  | Signing delegation (NIP-07 surface)      |
| `storage` | NUB-STORAGE | Scoped key-value storage proxy           |
| `ifc`     | NUB-IFC     | Inter-frame communication                |
| `theme`   | NUB-THEME   | Theme tokens and appearance settings     |
| `keys`    | NUB-KEYS    | Keyboard forwarding and action keybindings |

Shells MAY support any subset of these NUBs. New NUBs may be proposed independently of this NIP.
```

Place this section immediately before the "## Security Considerations" heading.

Also add NUB spec references to the "## References" section at the bottom:

```
- [NUB-KEYS](https://github.com/napplet/nubs/blob/main/NUB-KEYS.md) -- Keyboard forwarding and action keybindings
```

Do NOT add reference links for the other NUBs unless they already have public spec URLs (they are draft PRs). Only add NUB-KEYS since that is the focus of this milestone.
  </action>
  <verify>
    <automated>grep -c "keys.*NUB-KEYS" specs/NIP-5D.md</automated>
  </verify>
  <acceptance_criteria>
- specs/NIP-5D.md contains a "## Known NUBs" section
- The Known NUBs table has exactly 6 rows (relay, signer, storage, ifc, theme, keys)
- The keys row references NUB-KEYS
- The References section includes a link to the NUB-KEYS spec
- No other sections of NIP-5D are modified
  </acceptance_criteria>
  <done>NIP-5D NUB domain table includes a keys row referencing NUB-KEYS (DOC-02)</done>
</task>

<task type="auto">
  <name>Task 2: Verify existing README documentation completeness</name>
  <files>packages/nubs/keys/README.md, packages/core/README.md, packages/shim/README.md, packages/sdk/README.md</files>
  <read_first>packages/nubs/keys/README.md, packages/core/README.md, packages/shim/README.md, packages/sdk/README.md</read_first>
  <action>
Verify that existing README documentation meets all acceptance criteria. The documentation was added in cross-phase commit 4e798d0. Check each file against these criteria and fix any gaps:

**DOC-01: @napplet/nub-keys README** (packages/nubs/keys/README.md):
- MUST have a wire protocol message reference table
- Table MUST list all 6 message types: keys.forward, keys.registerAction, keys.registerAction.result, keys.unregisterAction, keys.bindings, keys.action
- MUST show direction (Napplet -> Shell vs Shell -> Napplet)
- MUST include payload fields for each message type
- MUST document the Action, RegisterResult, and KeyBinding supporting types
- MUST document key combo format (modifier ordering)
- MUST link to NUB-KEYS spec

**DOC-03: Core README** (packages/core/README.md):
- NubDomain union MUST include 'keys'
- NUB_DOMAINS array MUST include 'keys'
- Domain table MUST have a keys row with description "Keyboard forwarding and action keybindings"
- Integration Note MUST list @napplet/nub-keys

**DOC-03: Shim README** (packages/shim/README.md):
- Quick Start MUST show keys.registerAction and keys.onAction usage
- window.napplet Shape MUST include keys namespace
- MUST have a "window.napplet.keys" subsection documenting registerAction, unregisterAction, onAction
- MUST document smart forwarding rules (text inputs, bare modifiers, IME, reserved keys, bound vs unbound)
- Wire Format section MUST list keys.forward, keys.registerAction, keys.unregisterAction (outbound) and keys.registerAction.result, keys.bindings, keys.action (inbound)

**DOC-03: SDK README** (packages/sdk/README.md):
- Quick Start MUST show keys usage (registerAction, onAction)
- API Reference MUST have a keys subsection with registerAction, unregisterAction, onAction methods
- Types section MUST include KeysNubMessage and Action
- NUB Domain Constants MUST include KEYS_DOMAIN
- SDK vs Shim table MUST mention keys in the import example

For each file, read it completely and verify every criterion above. If ALL criteria pass, no changes needed. If any criterion fails, make the minimal edit to satisfy it.

Given the cross-phase commit already addressed this, expect most criteria to already pass. Only edit files that have actual gaps.
  </action>
  <verify>
    <automated>grep -c "keys.forward" packages/nubs/keys/README.md && grep -c "'keys'" packages/core/README.md && grep -c "window.napplet.keys" packages/shim/README.md && grep -c "keys.registerAction" packages/sdk/README.md</automated>
  </verify>
  <acceptance_criteria>
- nub-keys README has message reference table with all 6 message types (keys.forward, keys.registerAction, keys.registerAction.result, keys.unregisterAction, keys.bindings, keys.action)
- Core README NubDomain includes 'keys', NUB_DOMAINS includes 'keys', domain table has keys row
- Shim README has keys in Quick Start, window.napplet shape, dedicated subsection, smart forwarding rules, wire format
- SDK README has keys in Quick Start, API reference subsection, types table, domain constants, import examples
- All four READMEs are internally consistent with each other (same method signatures, same type names)
  </acceptance_criteria>
  <done>All package READMEs verified complete for keys NUB documentation (DOC-01, DOC-03)</done>
</task>

</tasks>

<verification>
Run these checks to confirm all documentation criteria:

```bash
# DOC-01: nub-keys README has message reference table
grep -c "keys\.forward\|keys\.registerAction\|keys\.bindings\|keys\.action\|keys\.unregisterAction" packages/nubs/keys/README.md
# Expected: >= 6 matches (each message type at least once)

# DOC-02: NIP-5D has keys in NUB domain table
grep "keys.*NUB-KEYS" specs/NIP-5D.md
# Expected: at least 1 match

# DOC-03: Core README mentions keys
grep "'keys'" packages/core/README.md
# Expected: at least 1 match

# DOC-03: Shim README documents keys
grep "window\.napplet\.keys" packages/shim/README.md
# Expected: at least 1 match

# DOC-03: SDK README documents keys
grep "keys\.registerAction\|keys\.onAction\|keys\.unregisterAction" packages/sdk/README.md
# Expected: at least 1 match

# Build still passes
pnpm build && pnpm type-check
```
</verification>

<success_criteria>
1. `@napplet/nub-keys` README has a wire protocol message reference table with all 6 message types matching NUB-KEYS spec (DOC-01)
2. NIP-5D has a "Known NUBs" section with a domain table including a keys row referencing NUB-KEYS (DOC-02)
3. Core, shim, and SDK READMEs all document the keys NUB with usage examples for action registration and smart forwarding (DOC-03)
4. `pnpm build && pnpm type-check` still passes
</success_criteria>

<output>
After completion, create `.planning/phases/92-documentation/92-01-SUMMARY.md`
</output>
