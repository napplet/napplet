---
phase: 90-shim-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shim/src/keys-shim.ts
  - packages/shim/src/index.ts
autonomous: true
requirements:
  - SHIM-01
  - SHIM-02
  - SHIM-03
  - SHIM-04

must_haves:
  truths:
    - "keyboard-shim.ts is deleted; keys-shim.ts exists and is loaded by the shim entry point"
    - "Shell keys.bindings messages update the local suppress list; bound combos trigger local action handlers instead of forwarding"
    - "IME composition, bare modifier keys, and Tab/Shift+Tab are never forwarded or suppressed"
    - "window.napplet.keys exposes registerAction(), unregisterAction(), and onAction()"
    - "Unbound keydown events outside text inputs produce keys.forward postMessage to the parent"
  artifacts:
    - path: "packages/shim/src/keys-shim.ts"
      provides: "NUB-KEYS smart forwarding, suppress list, action API"
      exports: ["installKeysShim", "handleKeysMessage", "registerAction", "unregisterAction", "onAction"]
    - path: "packages/shim/src/index.ts"
      provides: "Shim entry point that loads keys-shim"
      contains: "installKeysShim"
  key_links:
    - from: "packages/shim/src/index.ts"
      to: "packages/shim/src/keys-shim.ts"
      via: "import { installKeysShim, handleKeysMessage, registerAction, unregisterAction, onAction }"
      pattern: "import.*installKeysShim.*from.*keys-shim"
    - from: "packages/shim/src/keys-shim.ts"
      to: "window.parent"
      via: "postMessage for keys.forward, keys.registerAction, keys.unregisterAction"
      pattern: "window\\.parent\\.postMessage"
    - from: "packages/shim/src/index.ts"
      to: "window.napplet.keys"
      via: "installs registerAction, unregisterAction, onAction on global"
      pattern: "keys:.*registerAction"
---

<objective>
Verify that the existing keys-shim.ts implementation (from cross-phase commit 4e798d0) satisfies all NUB-KEYS shim requirements. The code is already written, builds, and type-checks. This plan validates it against acceptance criteria and produces the summary artifact.

Purpose: Confirm the shim implementation matches the NUB-KEYS spec before proceeding to SDK wrappers (Phase 91).
Output: Verification of existing code + phase summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/90-shim-implementation/90-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify keys-shim.ts against all acceptance criteria</name>
  <files>packages/shim/src/keys-shim.ts, packages/shim/src/index.ts</files>
  <read_first>
    - packages/shim/src/keys-shim.ts
    - packages/shim/src/index.ts
    - packages/core/src/types.ts (NappletGlobal.keys type definition)
  </read_first>
  <action>
The implementation already exists from commit 4e798d0. Verify each requirement:

**SHIM-01: keyboard-shim.ts deleted, keys-shim.ts loaded by entry point**
- Confirm `packages/shim/src/keyboard-shim.ts` does NOT exist (ls returns nothing)
- Confirm `packages/shim/src/keys-shim.ts` EXISTS
- Confirm `packages/shim/src/index.ts` imports from `./keys-shim.js` and calls `installKeysShim()`

**SHIM-02: Suppress list from keys.bindings, bound combos trigger local handler**
- In keys-shim.ts, find `handleBindings()` which clears `suppressMap` and repopulates from `msg.bindings`
- In `handleKeydown()`, find the suppress check: `suppressMap.has(combo)` triggers `event.preventDefault()` and calls local action handlers instead of forwarding

**SHIM-03: Safety guards (IME, bare modifiers, Tab/Shift+Tab)**
- `isComposing` guard: `if (event.isComposing) return;`
- Bare modifier guard: `isModifierOnly()` checks for Control/Alt/Shift/Meta
- Reserved keys: `RESERVED_KEYS = new Set(['Tab', 'Shift+Tab', 'Escape'])` -- these bypass suppress check via `!RESERVED_KEYS.has(combo)`
- Note: Escape is also in reserved set (conservative safety). Tab/Shift+Tab explicitly listed per spec.

**SHIM-04: window.napplet.keys API surface**
- In index.ts, find the `window.napplet` installation block with `keys: { registerAction, unregisterAction, onAction }`
- Verify function signatures match NappletGlobal type:
  - `registerAction(action: { id, label, defaultKey? }): Promise<{ actionId, binding? }>`
  - `unregisterAction(actionId: string): void`
  - `onAction(actionId: string, callback: () => void): Subscription`

**SC-5: Unbound keys produce keys.forward**
- In `handleKeydown()`, after suppress check falls through, find the `KeysForwardMessage` construction and `window.parent.postMessage(msg, '*')`

**Build verification:**
- Run `pnpm build` -- must succeed with 0 errors
- Run `pnpm type-check` -- must succeed with 0 errors

If ANY criterion fails, fix the code. If all pass, proceed to summary.
  </action>
  <acceptance_criteria>
    - `ls packages/shim/src/keyboard-shim.ts` returns "No such file"
    - `ls packages/shim/src/keys-shim.ts` returns the file path
    - `grep -c 'installKeysShim' packages/shim/src/index.ts` returns at least 2 (import + call)
    - `grep -c 'handleKeysMessage' packages/shim/src/index.ts` returns at least 2 (import + call)
    - `grep -c 'suppressMap' packages/shim/src/keys-shim.ts` returns >= 4
    - `grep -c 'isComposing' packages/shim/src/keys-shim.ts` returns >= 1
    - `grep -c 'isModifierOnly' packages/shim/src/keys-shim.ts` returns >= 2
    - `grep 'RESERVED_KEYS' packages/shim/src/keys-shim.ts` matches a Set containing 'Tab'
    - `grep -c 'keys.forward' packages/shim/src/keys-shim.ts` returns >= 1
    - `grep -c 'registerAction' packages/shim/src/keys-shim.ts` returns >= 3
    - `grep -c 'unregisterAction' packages/shim/src/keys-shim.ts` returns >= 2
    - `grep -c 'onAction' packages/shim/src/keys-shim.ts` returns >= 2
    - `grep 'keys:' packages/shim/src/index.ts` appears in the window.napplet installation block
    - `pnpm build` exits 0
    - `pnpm type-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm build && pnpm type-check && echo "PASS: build + type-check green"</automated>
  </verify>
  <done>
    All 4 requirements (SHIM-01 through SHIM-04) verified against existing code. Build and type-check pass. No code changes needed (or minimal fixes applied if any criterion failed).
  </done>
</task>

</tasks>

<verification>
- `packages/shim/src/keyboard-shim.ts` does not exist
- `packages/shim/src/keys-shim.ts` exists with installKeysShim, handleKeysMessage, registerAction, unregisterAction, onAction exports
- `packages/shim/src/index.ts` imports from keys-shim and calls installKeysShim()
- `packages/shim/src/index.ts` installs keys namespace on window.napplet
- `pnpm build` and `pnpm type-check` pass with zero errors
</verification>

<success_criteria>
- All 5 phase success criteria from ROADMAP verified against existing implementation
- Build and type-check green across all 10 packages
- Summary artifact created at `.planning/phases/90-shim-implementation/90-01-SUMMARY.md`
</success_criteria>

<output>
After completion, create `.planning/phases/90-shim-implementation/90-01-SUMMARY.md`
</output>
