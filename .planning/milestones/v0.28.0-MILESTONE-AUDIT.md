---
milestone: v0.28.0
milestone_name: Browser-Enforced Resource Isolation
audited: 2026-04-21T10:27:34Z
status: passed
scores:
  requirements: 65/65
  phases: 10/10
  integration: 6/6 cross-phase wiring links verified
  flows: 1/1 E2E flow complete (napplet → SDK → shim → postMessage → shell → Blob)
gaps: []
tech_debt: []
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 10
  overall: not-applicable (no RESEARCH.md / VALIDATION.md authored for this milestone — research happened at milestone scoping, not per-phase)
---

# Milestone Audit: v0.28.0 — Browser-Enforced Resource Isolation

**Audited:** 2026-04-21
**Status:** ✅ PASSED
**Score:** 65/65 requirements satisfied | 10/10 phases complete | 6/6 cross-phase wiring verified | E2E flow PASS

## Definition of Done

Convert napplet iframe security from ambient trust ("napplets shouldn't fetch directly") to browser-enforced isolation ("napplets cannot fetch directly, the browser blocks it"). Ship one new NUB (`resource`) with `resource.bytes(url) → Blob`, scheme-pluggable URL space (`https:`, `blossom:`, `nostr:`, `data:`), optional sidecar pre-resolution on relay events, strict CSP enforcement at the iframe boundary via `@napplet/vite-plugin`, and the spec amendments needed to lock the model in.

## Phase Summary

| Phase | Name | REQs | Status | Verification |
|-------|------|------|--------|--------------|
| 125 | Core Type Surface | 3 (CORE-01..03) | ✅ Complete | passed (6/6) |
| 126 | Resource NUB Scaffold + `data:` Scheme | 8 (RES-01..07, SCH-01) | ✅ Complete | passed (8/8) |
| 127 | NUB-RELAY Sidecar Amendment | 4 (SIDE-01..04) | ✅ Complete | passed (4/4) |
| 128 | Central Shim Integration | 5 (SHIM-01..03, CAP-01..02) | ✅ Complete | passed (5/5) — DEF-125-01 closed |
| 129 | Central SDK Integration | 3 (SDK-01..03) | ✅ Complete | passed (5/5) |
| 130 | Vite-Plugin Strict CSP | 8 (CSP-01..07, CAP-03) | ✅ Complete | passed (11/11) — 4 project-killer pitfalls gated |
| 131 | NIP-5D In-Repo Spec Amendment | 1 (SPEC-01) | ✅ Complete | passed (7/7) |
| 132 | Cross-Repo Nubs PRs | 18 (SPEC-02..06, SCH-02..04, POL-01..06, SVG-01..03, SIDE-05) | ✅ Complete | passed (6/6) |
| 133 | Documentation + Demo Coordination | 8 (DOC-01..07, DEMO-01) | ✅ Complete | passed (9/9) |
| 134 | Verification & Milestone Close | 7 (VER-01..07) | ✅ Complete | passed (7/7) |

**Total:** 65 REQ-IDs across 10 phases — all satisfied.

## Cross-Phase Integration

All 6 critical wiring links verified against actual source code:

| Link | Status |
|------|--------|
| Phase 125 → Phase 126: `'resource'` in `NubDomain` consumed by resource NUB package | ✅ WIRED |
| Phase 126 ↔ Phase 127: `ResourceSidecarEntry` type-only borrow by relay NUB | ✅ WIRED |
| Phase 126 → Phase 128: shim integration imports `installResourceShim`, `handleResourceMessage`, `bytes`, `bytesAsObjectURL` | ✅ WIRED |
| Phase 126 → Phase 129: SDK barrel re-exports resource namespace + 11 types + `RESOURCE_DOMAIN` | ✅ WIRED |
| Phase 130 → Phase 131: `perm:strict-csp` capability identifier referenced verbatim in NIP-5D | ✅ WIRED |
| Phase 132 ↔ Phase 134: NUB-RESOURCE.md spec uses `error:` field matching shipped types.ts (post-Phase-134 fix) | ✅ ALIGNED |

## E2E Flow

**Napplet → SDK → shim → postMessage → shell → Blob return**

1. Napplet imports `@napplet/sdk` and calls `resource.bytes(url)` — lazy delegate to `window.napplet.resource.bytes`
2. Mounted at `packages/shim/src/index.ts:191` — resolves to `bytes()` from `@napplet/nub/resource/shim`
3. `bytes()` short-circuits `data:` URLs inline (zero postMessage); for other schemes, sends `resource.bytes` envelope to `window.parent`
4. Single-flight `Map<canonicalURL, Promise<Blob>>` prevents stampede
5. Shell returns `resource.bytes.result` envelope; central shim router (line 98) matches `type.startsWith('resource.')` and dispatches to `handleResourceMessage`
6. `handleResourceMessage` settles the pending Promise with the received Blob

**Verdict:** Complete end-to-end. Every step traced through real source code. The `data:` scheme short-circuit is implemented and Phase-126 smoke-tested.

## Project-Killer Pitfall Mitigations (from research/PITFALLS.md)

| Pitfall | Anchored Phase | Status |
|---------|----------------|--------|
| 1: CSP meta placement (must be first <head> child) | 130 | ✅ Build-time assertion in `closeBundle` + `transformIndexHtml` `order: 'pre'` + `injectTo: 'head-prepend'` |
| 2: Header-only directives silently ignored in meta | 130 | ✅ Build-time rejection of `frame-ancestors`, `sandbox`, `report-uri`, `report-to` |
| 5: `allow-same-origin` service-worker bypass | 131 | ✅ NIP-5D Security Considerations reaffirms `sandbox="allow-scripts"` only |
| 6: SSRF via private IPs and DNS rebinding | 132 | ✅ NUB-RESOURCE MUSTs block at DNS resolution time covering RFC1918 + loopback + link-local + cloud metadata 169.254.169.254 |
| 7: SVG `<foreignObject>` / billion-laughs DoS | 132 | ✅ NUB-RESOURCE MUSTs sandboxed Worker, no network, 5 MiB/4096×4096/2s caps |
| 8: Cross-repo spec drift / `@napplet/*` leakage | 132 | ✅ All 4 nubs drafts zero-grep clean; VER-06 confirmed |
| 10: Sidecar privacy leak | 132 | ✅ NUB-RELAY-AMENDMENT.md mandates default OFF + privacy rationale + per-event-kind allowlist |
| 13: Cache stampede | 127 | ✅ Single-flight Map proven by N=10 stampede test |
| 18: Vite HMR connect-src leak to prod | 130 | ✅ Build-time `assertNoDevLeakage` rejects `ws://` in prod manifest |
| 19: Inline scripts need nonces | 130 | ✅ Nonce-based `script-src`; never `'unsafe-inline'` or `'unsafe-eval'` |
| 21: Positive CSP block assertion (vs absence) | 134 | ✅ VER-02 simulation correlates `securitypolicyviolation` event + `requestfailed` for same URL |

All 11 project-killer / serious pitfalls actively mitigated and verified.

## Tech Debt / Deferred Items

None blocking milestone completion. Documented carry-over:

- **Cross-repo PRs to napplet/nubs**: Per Phase 132 CONTEXT.md scope decision, the 4 spec drafts exist as local artifacts in `.planning/phases/132-cross-repo-nubs-prs/drafts/`. Manual follow-up: create branches, push, open PRs.
- **Demo napplets**: Per Phase 133 + DEMO-01, demos are explicitly delegated to the downstream shell repo (Option B). PROJECT.md and NUB-RESOURCE draft contain coordination notes.
- **CARRIED from prior milestones (NOT v0.28.0 issues)**: PUB-04 (npm publish blocked on human npm auth), RES-01 from v0.12.0 era (NIP number conflict with Scrolls PR#2281).

## Nyquist Coverage

Not applicable. v0.28.0 milestone-level research replaced per-phase RESEARCH.md authoring (4 parallel researchers ran at milestone kickoff; results in `.planning/research/{STACK,FEATURES,ARCHITECTURE,PITFALLS,SUMMARY}.md`). Per-phase VALIDATION.md was not authored. All phases relied on per-task `<automated>` verification in plan files instead, which is appropriate for the deterministic, infrastructure-heavy nature of this milestone.

## Final Verdict

✅ **PASSED** — Milestone v0.28.0 ready for `/gsd:complete-milestone`.

All 65 requirements satisfied. All 10 phases verified. All cross-phase wiring intact. E2E flow complete. All 11 project-killer pitfalls actively gated by build-time assertions or spec MUSTs. No critical blockers, no unsatisfied requirements, no orphaned REQ-IDs.

The single non-trivial issue surfaced during execution (NUB-RESOURCE.md spec/impl drift on `code:` vs `error:` field) was caught in Phase 133 and resolved in Phase 134 with 19 surgical substitutions verified clean.
