---
phase: 131-nip-5d-in-repo-spec-amendment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/NIP-5D.md
autonomous: true
requirements:
  - SPEC-01
must_haves:
  truths:
    - "specs/NIP-5D.md contains a new subsection titled exactly 'Browser-Enforced Resource Isolation' inside Security Considerations."
    - "The subsection states strict CSP enforcement SHOULD be applied by shells."
    - "The subsection names the capability identifier 'perm:strict-csp' literally."
    - "The subsection reaffirms that napplet iframes use sandbox=\"allow-scripts\" only and that allow-same-origin MUST NOT be added."
    - "The subsection cross-references NUB-RESOURCE by name (no URL, no @napplet/* package reference)."
    - "Zero matches for @napplet/ in specs/NIP-5D.md (public-repo hygiene)."
    - "Zero matches for kehto in specs/NIP-5D.md (public-repo hygiene)."
    - "Zero matches for hyprgate in specs/NIP-5D.md (public-repo hygiene)."
  artifacts:
    - path: "specs/NIP-5D.md"
      provides: "NIP-5D spec amended with Browser-Enforced Resource Isolation subsection"
      contains: "Browser-Enforced Resource Isolation"
    - path: "specs/NIP-5D.md"
      provides: "Capability identifier perm:strict-csp documented"
      contains: "perm:strict-csp"
    - path: "specs/NIP-5D.md"
      provides: "Cross-reference to NUB-RESOURCE"
      contains: "NUB-RESOURCE"
  key_links:
    - from: "specs/NIP-5D.md (Security Considerations)"
      to: "NUB-RESOURCE (cross-repo, by name only)"
      via: "Prose cross-reference inside Browser-Enforced Resource Isolation subsection"
      pattern: "NUB-RESOURCE"
    - from: "specs/NIP-5D.md (Browser-Enforced Resource Isolation)"
      to: "perm:strict-csp capability identifier (already implicit in NamespacedCapability template)"
      via: "Literal mention of the capability string"
      pattern: "perm:strict-csp"
    - from: "specs/NIP-5D.md (Transport section sandbox token)"
      to: "specs/NIP-5D.md (Browser-Enforced Resource Isolation subsection)"
      via: "Reaffirmation of sandbox=\"allow-scripts\" and explicit prohibition of allow-same-origin"
      pattern: "allow-scripts.*allow-same-origin|allow-same-origin.*allow-scripts"
---

<objective>
Amend `specs/NIP-5D.md` with a new "Browser-Enforced Resource Isolation" subsection inside the existing Security Considerations section. The subsection documents the v0.28.0 strict-CSP security posture: strict CSP SHOULD be enforced by shells, the `perm:strict-csp` capability identifier advertises that posture, the resource NUB (cross-referenced by name as NUB-RESOURCE) is the canonical fetch path for napplet-bound network-sourced bytes, and `sandbox="allow-scripts"` (no `allow-same-origin`) is reaffirmed to close the service-worker bypass vector.

Purpose: Lock the v0.28.0 security model into the in-repo spec so any reader of NIP-5D understands that browser-enforced isolation (CSP + sandbox + resource NUB) is the canonical posture, without leaking any private `@napplet/*` package names or downstream consumer names (`kehto`, `hyprgate`) into the public-facing spec.

Output: A single amended file (`specs/NIP-5D.md`) with one new subsection added to Security Considerations. No source code changes. Workspace `pnpm -r type-check` remains green (it was green at end of Phase 130 and this plan touches no source).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/131-nip-5d-in-repo-spec-amendment/131-CONTEXT.md
@specs/NIP-5D.md

<interfaces>
<!-- The spec uses plain markdown (atx-style ## headings, indented code blocks 4-space, tables with pipes). Existing Security Considerations section uses ## level for the section heading and a numbered list under "**Mitigations:**". Existing references list at bottom links other NIPs by relative path (e.g., [NIP-5A](5A.md)). The Transport section already establishes the canonical sandbox prose: `sandbox="allow-scripts"` is the only required token; `allow-same-origin` MUST NOT be present. Re-use this exact phrasing for consistency. -->

Existing structural anchors in specs/NIP-5D.md (verified on disk):
- Line 31: `    sandbox="allow-scripts"` (the canonical sandbox example, 4-space indented code block)
- Line 33: prose stating "The `allow-same-origin` token MUST NOT be present."
- Line 101: `## Security Considerations` heading
- Lines 105-111: `**Mitigations:**` numbered list (1-6)
- Line 113: prose referring storage / relay / ACL isolation to respective NUB specs
- Line 115: `**Non-Guarantees:**` paragraph
- Line 117: `## References` heading
- Line 119: existing cross-reference style: `- [NIP-5A](5A.md) -- Napplet manifest format and aggregate hash`

The new subsection MUST be inserted INSIDE the existing `## Security Considerations` section (i.e., between line 113 and line 115, before `**Non-Guarantees:**`), as a `### Browser-Enforced Resource Isolation` subheading (atx-style, one level deeper than the section heading). Do NOT create a new top-level `##` section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Browser-Enforced Resource Isolation subsection to specs/NIP-5D.md</name>
  <files>specs/NIP-5D.md</files>

  <read_first>
    Before editing, re-read `specs/NIP-5D.md` IN FULL to confirm:
    1. The exact current line numbers of `## Security Considerations` (currently line 101), the `**Mitigations:**` block (105-111), the storage/ACL paragraph (line 113), and `**Non-Guarantees:**` (line 115).
    2. The exact phrasing already used for the sandbox token in the Transport section (lines 29-33).
    3. The cross-reference style at line 119 in `## References`.
    4. That the file currently has zero occurrences of `@napplet/`, `kehto`, or `hyprgate` (baseline verified clean).
    Use line numbers from the live file rather than this plan's snapshot if they have shifted.
  </read_first>

  <action>
    Edit `specs/NIP-5D.md`. Insert a new third-level subsection inside the existing `## Security Considerations` section, placed AFTER the storage/ACL paragraph (currently line 113: `Storage isolation, relay access control, and ACL enforcement are defined by their respective NUB specs.`) and BEFORE the `**Non-Guarantees:**` paragraph (currently line 115).

    Insert exactly the following block (verbatim — heading text, capability string, and cross-reference name are load-bearing acceptance criteria):

    ```markdown
    ### Browser-Enforced Resource Isolation

    The mitigations above describe ambient trust ("napplets shouldn't fetch directly"). Shells SHOULD additionally enforce browser-level isolation so that napplets *cannot* fetch directly even if their code attempts to.

    **Strict Content Security Policy.** Shells SHOULD deliver napplet HTML under a strict Content Security Policy that disallows `'unsafe-inline'` and `'unsafe-eval'` in `script-src`, sets `connect-src 'none'` in production builds, sets `default-src 'none'`, and uses nonce-based `script-src` for any inline scripts. The CSP SHOULD be delivered as the first child of `<head>` so that no element is parsed before the policy binds. Shells that enforce this posture SHOULD advertise the `perm:strict-csp` capability via `window.napplet.shell.supports('perm:strict-csp')` so napplets can detect the posture at runtime.

    Example minimal meta delivery:

        <meta http-equiv="Content-Security-Policy"
              content="default-src 'none'; script-src 'nonce-...' 'self'; connect-src 'none'; img-src blob: data:; style-src 'self'; object-src 'none'; base-uri 'none'; form-action 'none'">

    **Canonical fetch path.** Network-sourced bytes (avatars, banners, media artwork, blossom blobs, `data:` payloads, etc.) SHOULD reach napplets only through the resource NUB. The shell mediates the fetch, applies its policy (private-IP block, response size cap, timeouts, MIME byte-sniffing, SVG rasterization), and returns a `Blob` to the napplet. See the napplet/nubs registry NUB-RESOURCE specification for the message catalog, scheme handlers, and shell behavior contract.

    **Sandbox reaffirmation.** As stated in the Transport section, napplet iframes MUST use `sandbox="allow-scripts"` and MUST NOT add `allow-same-origin`. Adding `allow-same-origin` would grant the napplet a real origin, allowing it to register a service worker, read shell `localStorage`, and bypass the resource NUB entirely. This prohibition is the load-bearing precondition for browser-enforced isolation; the strict-CSP posture above relies on the napplet remaining at an opaque origin.

    Strict CSP, the resource NUB as canonical fetch path, and `sandbox="allow-scripts"` (without `allow-same-origin`) compose to convert ambient trust into browser-enforced isolation: the napplet *cannot* perform a network fetch the shell did not mediate.
    ```

    Notes for the editor:
    - Use the `Edit` tool with the storage/ACL paragraph (`Storage isolation, relay access control, and ACL enforcement are defined by their respective NUB specs.`) as the unique anchor; insert a blank line, then the new subsection, then a blank line, then the existing `**Non-Guarantees:**` line.
    - The example CSP block uses 4-space indentation (matching the existing `sandbox="allow-scripts"` indented code block at line 31), NOT triple-backticks. This keeps the spec consistent with its own conventions.
    - Do NOT mention `@napplet/vite-plugin`, `@napplet/shim`, `@napplet/sdk`, `@napplet/core`, or any `@napplet/*` package name. Do NOT mention `kehto` or `hyprgate`.
    - Do NOT add a URL for NUB-RESOURCE (the spec doesn't exist yet — Phase 132 drafts it). Reference it by name only as "the napplet/nubs registry NUB-RESOURCE specification".
    - Do NOT modify any other section of the spec.
  </action>

  <verify>
    <automated>cd /home/sandwich/Develop/napplet && grep -c "### Browser-Enforced Resource Isolation" specs/NIP-5D.md | grep -qx 1 && grep -q "perm:strict-csp" specs/NIP-5D.md && grep -q "NUB-RESOURCE" specs/NIP-5D.md && grep -q 'sandbox="allow-scripts"' specs/NIP-5D.md && grep -q "allow-same-origin" specs/NIP-5D.md && grep -q "SHOULD" specs/NIP-5D.md && [ "$(grep -c '@napplet/' specs/NIP-5D.md)" = "0" ] && [ "$(grep -c 'kehto' specs/NIP-5D.md)" = "0" ] && [ "$(grep -c 'hyprgate' specs/NIP-5D.md)" = "0" ] && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    All of the following must hold (each is grep-verifiable from the repo root):

    1. `grep -c "### Browser-Enforced Resource Isolation" specs/NIP-5D.md` returns exactly `1` (the new subsection heading exists exactly once).
    2. `grep -q "perm:strict-csp" specs/NIP-5D.md` succeeds (capability identifier is present literally).
    3. `grep -q "NUB-RESOURCE" specs/NIP-5D.md` succeeds (cross-reference by name is present).
    4. `grep -q 'sandbox="allow-scripts"' specs/NIP-5D.md` still succeeds (sandbox token reaffirmed; not removed from Transport section).
    5. `grep -E -q "allow-same-origin.*MUST NOT|MUST NOT.*allow-same-origin" specs/NIP-5D.md` succeeds (allow-same-origin prohibition language present somewhere in the spec — already true at line 33, the new subsection adds an additional reaffirmation but the original prohibition must remain).
    6. `grep -q "SHOULD" specs/NIP-5D.md` succeeds within the new subsection's region (strict-CSP normative level).
    7. `grep -c "@napplet/" specs/NIP-5D.md` returns exactly `0` (zero leakage of private package names — public-repo hygiene per project policy).
    8. `grep -c "kehto" specs/NIP-5D.md` returns exactly `0` (zero leakage of downstream consumer name per repo policy).
    9. `grep -c "hyprgate" specs/NIP-5D.md` returns exactly `0` (zero leakage of downstream consumer name per repo policy).
    10. The string `"window.napplet.shell.supports('perm:strict-csp')"` appears in the file (runtime usage example).
    11. The example CSP `<meta http-equiv="Content-Security-Policy"` block is present (one minimal example, per CONTEXT discretion).
    12. No other section of `specs/NIP-5D.md` is modified — `git diff specs/NIP-5D.md` shows only insertions inside the Security Considerations section between the storage/ACL paragraph and `**Non-Guarantees:**`.
  </acceptance_criteria>

  <done>
    `specs/NIP-5D.md` contains the new `### Browser-Enforced Resource Isolation` subsection inside Security Considerations, all 12 acceptance criteria above are satisfied, and `git diff specs/NIP-5D.md` shows additions only (no deletions, no edits to other sections).
  </done>
</task>

<task type="auto">
  <name>Task 2: Workspace hygiene check + write SUMMARY</name>
  <files>.planning/phases/131-nip-5d-in-repo-spec-amendment/131-01-SUMMARY.md</files>

  <read_first>
    Before writing the summary:
    1. Re-read `specs/NIP-5D.md` to confirm Task 1 landed cleanly.
    2. Re-read `$HOME/.claude/get-shit-done/templates/summary.md` for the SUMMARY format.
    3. Re-read `.planning/phases/131-nip-5d-in-repo-spec-amendment/131-CONTEXT.md` decisions block to confirm all locked decisions were honored.
  </read_first>

  <action>
    Two-part task, both surgical:

    Part A — Hygiene re-verification (idempotent — same checks Task 1 ran, plus type-check):
    - Re-run all 9 grep assertions from Task 1's `<verify>` block and confirm each passes.
    - Run `pnpm -r type-check` from the repo root and confirm exit code 0 across all 14 workspace packages (this plan touches no source, so this should remain green from the end of Phase 130). If type-check fails, the failure is unrelated to this plan and must be investigated before the SUMMARY is written.

    Part B — Write `.planning/phases/131-nip-5d-in-repo-spec-amendment/131-01-SUMMARY.md` following the template at `$HOME/.claude/get-shit-done/templates/summary.md`. Required content:
    - Phase: 131 (NIP-5D In-Repo Spec Amendment)
    - Plan: 01
    - Requirement closed: SPEC-01
    - Files modified: `specs/NIP-5D.md` (additions only — new `### Browser-Enforced Resource Isolation` subsection inside Security Considerations)
    - The new subsection's exact heading text and what it documents (strict-CSP SHOULD posture, `perm:strict-csp` capability identifier, NUB-RESOURCE cross-reference by name, `sandbox="allow-scripts"` reaffirmation with `allow-same-origin` prohibition reasoning)
    - Public-repo hygiene confirmed: zero `@napplet/`, zero `kehto`, zero `hyprgate` matches in `specs/NIP-5D.md`
    - Workspace state: `pnpm -r type-check` exit code (expected 0); no source files touched
    - Decisions surfaced: any deviations from CONTEXT (expected: none — heading text "Browser-Enforced Resource Isolation" chosen per CONTEXT discretion; minimal CSP meta example included per CONTEXT discretion recommendation)
    - Forward links: Phase 132 will draft the actual NUB-RESOURCE spec in the public napplet/nubs repo (this amendment cross-references it by name only, deliberately ahead of its drafting)
  </action>

  <verify>
    <automated>cd /home/sandwich/Develop/napplet && [ -f .planning/phases/131-nip-5d-in-repo-spec-amendment/131-01-SUMMARY.md ] && grep -q "SPEC-01" .planning/phases/131-nip-5d-in-repo-spec-amendment/131-01-SUMMARY.md && grep -q "Browser-Enforced Resource Isolation" .planning/phases/131-nip-5d-in-repo-spec-amendment/131-01-SUMMARY.md && grep -q "perm:strict-csp" .planning/phases/131-nip-5d-in-repo-spec-amendment/131-01-SUMMARY.md && pnpm -r type-check 2>&1 | tail -5 && echo PASS</automated>
  </verify>

  <acceptance_criteria>
    1. `.planning/phases/131-nip-5d-in-repo-spec-amendment/131-01-SUMMARY.md` exists.
    2. `grep -q "SPEC-01" .planning/phases/131-nip-5d-in-repo-spec-amendment/131-01-SUMMARY.md` succeeds (requirement-ID traceability).
    3. `grep -q "Browser-Enforced Resource Isolation" .planning/phases/131-nip-5d-in-repo-spec-amendment/131-01-SUMMARY.md` succeeds (subsection heading documented in summary).
    4. `grep -q "perm:strict-csp" .planning/phases/131-nip-5d-in-repo-spec-amendment/131-01-SUMMARY.md` succeeds (capability identifier documented in summary).
    5. `grep -q "NUB-RESOURCE" .planning/phases/131-nip-5d-in-repo-spec-amendment/131-01-SUMMARY.md` succeeds (cross-reference documented in summary).
    6. `pnpm -r type-check` from repo root exits 0 (workspace stays green; this plan touched zero source files so DEF-125-01 stays closed).
    7. All 9 grep hygiene checks from Task 1 still pass after Task 2 (no regression).
  </acceptance_criteria>

  <done>
    SUMMARY.md exists at the correct path with all 5 required content greps satisfied, and `pnpm -r type-check` is still 0 across all 14 workspace packages.
  </done>
</task>

</tasks>

<verification>
Phase-level verification (after both tasks complete):

1. Single-file diff: `git diff --stat specs/NIP-5D.md` shows insertions only on one file; no other source file is modified.
2. Spec hygiene: `grep -c "@napplet/" specs/NIP-5D.md` = 0, `grep -c "kehto" specs/NIP-5D.md` = 0, `grep -c "hyprgate" specs/NIP-5D.md` = 0.
3. Spec content: `grep -c "### Browser-Enforced Resource Isolation" specs/NIP-5D.md` = 1; `grep -q "perm:strict-csp" specs/NIP-5D.md`; `grep -q "NUB-RESOURCE" specs/NIP-5D.md`; `grep -q "SHOULD" specs/NIP-5D.md`.
4. Sandbox reaffirmation: original Transport-section `sandbox="allow-scripts"` line still present at the same location; new subsection adds explicit reasoning for the `allow-same-origin` prohibition.
5. Workspace integrity: `pnpm -r type-check` exits 0 (no source touched).
6. Summary committed: `.planning/phases/131-nip-5d-in-repo-spec-amendment/131-01-SUMMARY.md` exists and traces SPEC-01.
</verification>

<success_criteria>
SPEC-01 is closed when:
- `specs/NIP-5D.md` contains a `### Browser-Enforced Resource Isolation` subsection inside `## Security Considerations`.
- The subsection states strict CSP enforcement is a SHOULD posture for shells and names `perm:strict-csp` as the capability identifier.
- The subsection cross-references NUB-RESOURCE by name (zero URL, zero `@napplet/*` reference).
- The subsection reaffirms `sandbox="allow-scripts"` and explains why `allow-same-origin` MUST NOT be added (closes service-worker bypass vector — Pitfall 5).
- `grep -c "@napplet/" specs/NIP-5D.md` = 0.
- `grep -c "kehto" specs/NIP-5D.md` = 0.
- `grep -c "hyprgate" specs/NIP-5D.md` = 0.
- Workspace `pnpm -r type-check` exits 0 (DEF-125-01 stays closed).
- Plan SUMMARY exists and traces SPEC-01.
</success_criteria>

<output>
After completion, create `.planning/phases/131-nip-5d-in-repo-spec-amendment/131-01-SUMMARY.md` (Task 2 produces it).
</output>
