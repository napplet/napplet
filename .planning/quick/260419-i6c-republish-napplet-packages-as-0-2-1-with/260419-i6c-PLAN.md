---
phase: quick
plan: 260419-i6c
type: execute
wave: 1
depends_on: []
requirements: [PUB-05]
autonomous: false
files_modified:
  - packages/core/package.json
  - packages/shim/package.json
  - packages/sdk/package.json
  - packages/vite-plugin/package.json
  - packages/nubs/config/package.json
  - packages/nubs/identity/package.json
  - packages/nubs/ifc/package.json
  - packages/nubs/keys/package.json
  - packages/nubs/media/package.json
  - packages/nubs/notify/package.json
  - packages/nubs/relay/package.json
  - packages/nubs/storage/package.json
  - packages/nubs/theme/package.json
  - .changeset/republish-0-2-1.md
  - pnpm-lock.yaml
  - CHANGELOG.md

must_haves:
  truths:
    - "All 13 @napplet/* packages published to npm at version 0.2.1"
    - "Published tarballs contain resolved version strings (not workspace:*) in dependencies"
    - "Installing @napplet/shim@0.2.1 from npm resolves transitively without errors"
    - "User explicitly approved the real publish step before it ran"
  artifacts:
    - path: ".changeset/republish-0-2-1.md"
      provides: "Patch-level changeset marking all 13 packages for bump"
      contains: "patch"
    - path: "packages/shim/package.json"
      provides: "Version bumped to 0.2.1"
      contains: '"version": "0.2.1"'
    - path: "packages/sdk/package.json"
      provides: "Version bumped to 0.2.1"
      contains: '"version": "0.2.1"'
  key_links:
    - from: "pnpm publish -r"
      to: "npm registry"
      via: "workspace protocol rewrite"
      pattern: "workspace:\\* rewritten to \"0.2.1\" in tarball package.json"
    - from: "registry tarball @napplet/shim@0.2.1"
      to: "resolved semver versions"
      via: "curl registry API"
      pattern: "grep -c 'workspace:' == 0"
---

<objective>
Republish all 13 @napplet/* packages to npm at v0.2.1 with properly-resolved dependency version strings. The v0.2.0 publish shipped 8 packages with unresolved `workspace:*` specs in their published package.json, breaking npm installs. pnpm's workspace protocol SHOULD rewrite `workspace:*` → `"0.2.1"` in published tarballs, but only when publishing via `pnpm publish -r` (not when the v0.2.0 artifacts were assembled).

Purpose: Restore install-ability of the napplet SDK from npm for external consumers. v0.2.0 is effectively broken on the registry; 0.2.1 supersedes it.

Output: 13 packages republished at 0.2.1 on npm with verified-clean dependency strings (zero `workspace:` occurrences in any published package.json).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@package.json
@.changeset/config.json
@packages/core/package.json
@packages/shim/package.json
@packages/sdk/package.json
@packages/vite-plugin/package.json

<background>
- Repo state: all 13 packages currently at "version": "0.2.0". No pending changeset files in .changeset/ (only README.md + config.json).
- Workspace-dep graph: @napplet/core has no workspace deps. @napplet/vite-plugin has no workspace deps. All 9 NUB packages depend on @napplet/core via `workspace:*`. @napplet/shim depends on @napplet/core + 8 of 9 NUBs (all except theme) via `workspace:*`. @napplet/sdk depends on @napplet/core + all 9 NUBs via `workspace:*`.
- Publish tooling: Root `publish-packages` script is `turbo run build && changeset publish`. We bypass changeset publish in favor of `pnpm publish -r --access public --no-git-checks` because we want pnpm's workspace-protocol rewrite to run on every workspace package (including ones without a changeset entry, since we're versioning uniformly via a single patch changeset).
- npm auth: user is already authenticated (v0.2.0 published successfully). Do not add an `npm whoami` task unless the publish step fails.
- Registry verification endpoint shape: `https://registry.npmjs.org/@napplet/<pkg>/0.2.1` returns JSON with a `.dependencies` map. On a healthy publish, dependency values are semver strings (e.g., `"^0.2.1"` or `"0.2.1"`), never strings containing `workspace:`.
</background>

<affected_packages>
All 13 @napplet/* packages:
1. @napplet/core (no workspace deps — clean-published at v0.2.0 but bumps for uniformity)
2. @napplet/vite-plugin (no workspace deps — clean-published at v0.2.0 but bumps for uniformity)
3. @napplet/shim (9 workspace deps)
4. @napplet/sdk (10 workspace deps)
5-13. @napplet/nub-{config,identity,ifc,keys,media,notify,relay,storage,theme} (1 workspace dep each: @napplet/core)
</affected_packages>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create patch changeset for all 13 packages and run changeset version</name>
  <files>
    .changeset/republish-0-2-1.md (new)
    packages/core/package.json
    packages/shim/package.json
    packages/sdk/package.json
    packages/vite-plugin/package.json
    packages/nubs/config/package.json
    packages/nubs/identity/package.json
    packages/nubs/ifc/package.json
    packages/nubs/keys/package.json
    packages/nubs/media/package.json
    packages/nubs/notify/package.json
    packages/nubs/relay/package.json
    packages/nubs/storage/package.json
    packages/nubs/theme/package.json
    CHANGELOG.md (may be generated per-package)
    pnpm-lock.yaml
  </files>
  <action>
    Create a single changeset file and use `pnpm changeset version` to bump all 13 packages in lockstep from 0.2.0 → 0.2.1. This is cleaner than manual bumps and produces per-package CHANGELOG entries.

    Step 1: Write the changeset file at `.changeset/republish-0-2-1.md` with this exact content (frontmatter lists all 13 packages as `patch`, followed by a short summary):

    ```markdown
    ---
    "@napplet/core": patch
    "@napplet/shim": patch
    "@napplet/sdk": patch
    "@napplet/vite-plugin": patch
    "@napplet/nub-config": patch
    "@napplet/nub-identity": patch
    "@napplet/nub-ifc": patch
    "@napplet/nub-keys": patch
    "@napplet/nub-media": patch
    "@napplet/nub-notify": patch
    "@napplet/nub-relay": patch
    "@napplet/nub-storage": patch
    "@napplet/nub-theme": patch
    ---

    Republish at 0.2.1 to ship resolved workspace dependency versions. The 0.2.0 tarballs on npm contained unresolved `workspace:*` specs in dependencies, breaking installs. This patch bump exists solely to produce correctly-assembled tarballs via `pnpm publish -r` (which rewrites `workspace:*` → concrete versions at pack time).
    ```

    Step 2: Run `pnpm changeset version` from the repo root. This consumes the changeset, bumps every listed package from 0.2.0 → 0.2.1, rewrites any `workspace:*` in the repo (for `updateInternalDependencies: "patch"` per .changeset/config.json this may tighten to `workspace:0.2.1` or similar — NOT a concern; pnpm publish still resolves workspace protocol specs at pack time), generates/updates per-package CHANGELOG.md entries, and deletes the consumed changeset file.

    Step 3: Run `pnpm install` (no args) from the repo root so pnpm-lock.yaml re-resolves against the new versions. Without this step, the next `pnpm build` or `pnpm publish -r` will warn about a stale lockfile and may refuse to proceed.

    Important:
    - Do NOT manually edit any package.json version fields. Let changeset version do it.
    - `commit: false` in .changeset/config.json means changeset version will NOT auto-commit — that's fine, we commit at the end of this task.
    - If `pnpm changeset version` exits non-zero, STOP and report the error. Do not proceed to install/build/publish.
    - If the consumed changeset file is auto-deleted but `pnpm install` produces no lockfile diff (rare — only if every dep was already at 0.2.1 internally), proceed anyway; `pnpm publish -r` still works.
    - Expect a dirty working tree after this task touching: the 13 package.json files, per-package CHANGELOG.md files (one per package that received an entry — likely all 13), pnpm-lock.yaml, and the deletion of .changeset/republish-0-2-1.md.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet &amp;&amp; grep -l '"version": "0.2.1"' packages/core/package.json packages/shim/package.json packages/sdk/package.json packages/vite-plugin/package.json packages/nubs/*/package.json | wc -l | grep -qx 13 &amp;&amp; test ! -f .changeset/republish-0-2-1.md &amp;&amp; echo VERSION-BUMP-OK</automated>
  </verify>
  <done>All 13 package.json files show `"version": "0.2.1"`. Changeset file consumed (deleted). pnpm-lock.yaml updated. Per-package CHANGELOG.md files contain a 0.2.1 entry referencing the patch summary.</done>
</task>

<task type="auto">
  <name>Task 2: Full build and dry-run publish to preview the exact tarball contents</name>
  <files>
    (read-only; builds dist/ directories and prints pack manifests — no repo files changed beyond dist/)
  </files>
  <action>
    Produce fresh build artifacts matching the new 0.2.1 versions, then run a dry-run publish to preview exactly what `pnpm publish -r` will ship.

    Step 1: From the repo root, run `pnpm build` (alias for `turbo run build`). This rebuilds every package's `dist/` directory via tsup. This step MUST succeed with exit 0. If any package fails to build, STOP and report — do not proceed to publish.

    Step 2: From the repo root, run a dry-run publish:
    ```
    pnpm publish -r --access public --no-git-checks --dry-run
    ```
    pnpm prints the pack manifest for each workspace package, including the resolved `dependencies` section as it will appear in the tarball. `workspace:*` specs MUST appear as concrete semver strings in the dry-run output (e.g., `"@napplet/core": "0.2.1"` or `"@napplet/core": "^0.2.1"`).

    Step 3: Capture the dry-run output to `/tmp/napplet-publish-dryrun.log` for the human-verify checkpoint:
    ```
    pnpm publish -r --access public --no-git-checks --dry-run 2>&amp;1 | tee /tmp/napplet-publish-dryrun.log
    ```

    Step 4: Quick sanity-grep to fail fast if pnpm did NOT rewrite workspace specs:
    ```
    ! grep -q 'workspace:' /tmp/napplet-publish-dryrun.log
    ```
    This expression exits 0 only if zero `workspace:` occurrences are present in the dry-run manifest output. If it exits 1, STOP — the rewrite isn't happening, which means the real publish will ship broken tarballs AGAIN. Do not proceed to Task 4.

    Notes:
    - `--no-git-checks` bypasses pnpm's "uncommitted changes" guard. We have uncommitted changes from Task 1 (intentionally — the commit happens in Task 6, AFTER verification).
    - `--access public` is redundant given each package's `publishConfig.access: "public"` but explicit is safer.
    - Dry-run does NOT hit the network for publishing, but pnpm may still contact the registry to check if versions exist. That's fine.
  </action>
  <verify>
    <automated>test -s /tmp/napplet-publish-dryrun.log &amp;&amp; ! grep -q 'workspace:' /tmp/napplet-publish-dryrun.log &amp;&amp; grep -q '@napplet/shim' /tmp/napplet-publish-dryrun.log &amp;&amp; grep -q '@napplet/sdk' /tmp/napplet-publish-dryrun.log &amp;&amp; echo DRYRUN-CLEAN</automated>
  </verify>
  <done>Turbo build succeeded for all 13 packages. Dry-run log exists at `/tmp/napplet-publish-dryrun.log`, references @napplet/shim and @napplet/sdk, and contains zero `workspace:` substrings — proving pnpm will rewrite the protocol at real-publish time.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human approval gate before IRREVERSIBLE npm publish</name>
  <what-built>
    Task 1 bumped all 13 @napplet/* packages to 0.2.1 (via changeset version) and updated the lockfile.
    Task 2 ran a full turbo build and captured the dry-run publish output at `/tmp/napplet-publish-dryrun.log`, verified to contain zero `workspace:` substrings.
    Nothing has been published to npm yet.
  </what-built>
  <how-to-verify>
    Publishing to npm is IRREVERSIBLE. npm does not allow republishing the same version number, so a bad publish at 0.2.1 would force a bump to 0.2.2 (and leave broken 0.2.1 on the registry indefinitely). Before the real publish:

    1. Review the dry-run log:
       ```
       less /tmp/napplet-publish-dryrun.log
       ```
       Expected: 13 package pack manifests. Each workspace dep (e.g., `@napplet/core`, `@napplet/nub-*`) should appear as a concrete version string, NEVER as `workspace:*`. Packages should all be at version 0.2.1.

    2. Spot-check a risk package (shim has the most workspace deps — 9 of them):
       ```
       awk '/@napplet\/shim/,/^$/' /tmp/napplet-publish-dryrun.log | grep -A 15 'dependencies'
       ```
       Expected: every line under `dependencies` shows a semver string, no `workspace:` anywhere.

    3. Confirm you want to ship 0.2.1 NOW. Once approved, the executor will run `pnpm publish -r --access public --no-git-checks` for real, and those tarballs become permanent on the npm registry.

    4. Confirm npm auth is still valid: `npm whoami` should return your npm handle. (Only check if the real publish fails on the first run.)
  </how-to-verify>
  <resume-signal>Type "publish" to proceed with the real npm publish, or "stop" to halt and leave the version bumps uncommitted for later review.</resume-signal>
  <done>User has explicitly approved the real publish after reviewing the dry-run log.</done>
</task>

<task type="auto">
  <name>Task 4: Real pnpm publish -r to npm registry</name>
  <files>
    (no repo files changed — npm publish does not touch the working tree)
  </files>
  <action>
    Execute the real recursive publish. This is the destructive step — tarballs uploaded here become permanent on npm.

    From the repo root, run:
    ```
    pnpm publish -r --access public --no-git-checks 2>&amp;1 | tee /tmp/napplet-publish-real.log
    ```

    Expected behavior:
    - pnpm iterates every workspace package with `publishConfig.access: "public"` (all 13).
    - For each package, pnpm assembles a tarball, rewrites `workspace:*` specs to concrete versions at pack time, and POSTs to `https://registry.npmjs.org/`.
    - Each successful publish prints `+ @napplet/<name>@0.2.1`.
    - Total: 13 `+ @napplet/...@0.2.1` lines in the log.

    Failure modes and handling:
    - `E409 Conflict` / "cannot publish over the previously published versions" → a package at 0.2.1 already exists. This should NOT happen (we're bumping FROM 0.2.0), but if it does, STOP and report which package conflicts. Do NOT force-bump to 0.2.2 without user input.
    - `ENEEDAUTH` / "This command requires you to be logged in" → npm auth expired. Run `npm whoami` to confirm, then ask user to `npm login` and retry publish.
    - Partial publish (some packages publish, others fail) → STOP. Log which ones succeeded. The user may need to intervene. DO NOT re-run the full `pnpm publish -r` blindly, as it will try to republish the ones that succeeded and hit E409.

    After publish completes:
    - Verify the log shows 13 success lines:
      ```
      grep -c '^+ @napplet/' /tmp/napplet-publish-real.log
      ```
      Expected: 13. If less, the publish was partial — STOP and surface the failure to the user.
  </action>
  <verify>
    <automated>test -s /tmp/napplet-publish-real.log &amp;&amp; [ "$(grep -c '^+ @napplet/' /tmp/napplet-publish-real.log)" = "13" ] &amp;&amp; echo PUBLISH-13-OK</automated>
  </verify>
  <done>All 13 @napplet/* packages show `+ @napplet/<name>@0.2.1` lines in the publish log. No error lines. npm registry now holds 0.2.1 for every package.</done>
</task>

<task type="auto">
  <name>Task 5: Registry verification — fetch @napplet/shim and @napplet/sdk tarballs, assert zero workspace: substrings</name>
  <files>
    (read-only network fetch; no repo files changed)
  </files>
  <action>
    Confirm the published tarballs are healthy by querying the npm registry for the two packages with the most workspace deps (shim: 9 deps, sdk: 10 deps). These are the highest-risk packages — if they're clean, the others almost certainly are too.

    Step 1: Fetch @napplet/shim@0.2.1 metadata and count workspace: occurrences:
    ```
    SHIM_COUNT=$(curl -fsSL https://registry.npmjs.org/@napplet/shim/0.2.1 | python3 -m json.tool | grep -c 'workspace:' || echo 0)
    echo "@napplet/shim workspace: occurrences: $SHIM_COUNT"
    ```
    Expected: `0`. Non-zero means the publish is broken — STOP and report.

    Step 2: Fetch @napplet/sdk@0.2.1 metadata and count workspace: occurrences:
    ```
    SDK_COUNT=$(curl -fsSL https://registry.npmjs.org/@napplet/sdk/0.2.1 | python3 -m json.tool | grep -c 'workspace:' || echo 0)
    echo "@napplet/sdk workspace: occurrences: $SDK_COUNT"
    ```
    Expected: `0`.

    Step 3 (bonus spot-check): pretty-print the shim dependencies block so the human reviewer can eyeball it:
    ```
    curl -fsSL https://registry.npmjs.org/@napplet/shim/0.2.1 | python3 -c "import sys, json; d = json.load(sys.stdin); print(json.dumps(d.get('dependencies', {}), indent=2))"
    ```
    Expected: a JSON object mapping 9 `@napplet/*` keys to concrete version strings (e.g., `"0.2.1"` or `"^0.2.1"`). Zero `workspace:` substrings.

    Step 4: Combined assertion (this is the single truth-source):
    ```
    test "$SHIM_COUNT" = "0" &amp;&amp; test "$SDK_COUNT" = "0" &amp;&amp; echo REGISTRY-CLEAN
    ```

    Notes:
    - npm registry may cache for 30-60s after publish. If either curl returns a 404, wait ~60s and retry once. Persistent 404 means the publish didn't actually hit the registry — STOP and investigate.
    - `grep -c` returns 0 on zero matches (not non-zero), so `|| echo 0` is defensive against grep's exit-code-1-on-no-match behavior masking a successful-zero result.
  </action>
  <verify>
    <automated>SHIM_COUNT=$(curl -fsSL https://registry.npmjs.org/@napplet/shim/0.2.1 | python3 -m json.tool | grep -c 'workspace:' || echo 0); SDK_COUNT=$(curl -fsSL https://registry.npmjs.org/@napplet/sdk/0.2.1 | python3 -m json.tool | grep -c 'workspace:' || echo 0); test "$SHIM_COUNT" = "0" &amp;&amp; test "$SDK_COUNT" = "0" &amp;&amp; echo REGISTRY-CLEAN</automated>
  </verify>
  <done>Both @napplet/shim@0.2.1 and @napplet/sdk@0.2.1 registry responses contain zero `workspace:` substrings. Dependencies render as concrete semver strings. External consumers can now install these packages from npm cleanly.</done>
</task>

<task type="auto">
  <name>Task 6: Commit version bumps and lockfile</name>
  <files>
    (git commit — stages package.json bumps, per-package CHANGELOG.md entries, pnpm-lock.yaml, and the deletion of .changeset/republish-0-2-1.md)
  </files>
  <action>
    Commit the repo state produced by Task 1 (version bumps + changeset consumption + lockfile update). The npm publish itself is not committed — npm publish doesn't touch the repo.

    Step 1: Stage exactly the expected files (avoid `git add -A` which could sweep unrelated files):
    ```
    cd /home/sandwich/Develop/napplet
    git add \
      packages/core/package.json \
      packages/shim/package.json \
      packages/sdk/package.json \
      packages/vite-plugin/package.json \
      packages/nubs/config/package.json \
      packages/nubs/identity/package.json \
      packages/nubs/ifc/package.json \
      packages/nubs/keys/package.json \
      packages/nubs/media/package.json \
      packages/nubs/notify/package.json \
      packages/nubs/relay/package.json \
      packages/nubs/storage/package.json \
      packages/nubs/theme/package.json \
      packages/core/CHANGELOG.md \
      packages/shim/CHANGELOG.md \
      packages/sdk/CHANGELOG.md \
      packages/vite-plugin/CHANGELOG.md \
      packages/nubs/*/CHANGELOG.md \
      pnpm-lock.yaml \
      .changeset/republish-0-2-1.md
    ```
    Note: the changeset file was deleted by `pnpm changeset version` — `git add` on a deleted path stages the deletion. Some CHANGELOG.md paths may not exist yet on first changeset run; `git add` tolerates missing paths with a warning but stages what's present. If the warnings are noisy, replace the `packages/*/CHANGELOG.md` expansions with a pre-check: `ls packages/core/CHANGELOG.md 2>/dev/null &amp;&amp; git add packages/core/CHANGELOG.md` (etc.).

    Step 2: Review staged diff for sanity:
    ```
    git diff --cached --stat
    ```
    Expected: ~13 package.json version changes, 13 CHANGELOG entries, one pnpm-lock.yaml update, one changeset deletion.

    Step 3: Commit:
    ```
    git commit -m "chore: bump packages to 0.2.1"
    ```

    Do NOT tag the commit. Do NOT push. Leave that to the user.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/napplet &amp;&amp; git log -1 --pretty=%s | grep -q '0.2.1' &amp;&amp; git diff --quiet HEAD -- packages/core/package.json packages/shim/package.json packages/sdk/package.json &amp;&amp; echo COMMIT-OK</automated>
  </verify>
  <done>Single commit on current branch titled `chore: bump packages to 0.2.1`. Working tree clean with respect to packages/*/package.json and pnpm-lock.yaml. No push performed.</done>
</task>

</tasks>

<verification>
End-to-end truth: after all tasks complete,
```
curl -fsSL https://registry.npmjs.org/@napplet/shim/0.2.1 | python3 -m json.tool | grep -c 'workspace:'
```
outputs `0`, and `git log -1 --pretty=%s` shows `chore: bump packages to 0.2.1`.
</verification>

<success_criteria>
- [ ] All 13 @napplet/* packages at 0.2.1 on npm registry
- [ ] `curl https://registry.npmjs.org/@napplet/shim/0.2.1 | grep -c 'workspace:'` = `0`
- [ ] `curl https://registry.npmjs.org/@napplet/sdk/0.2.1 | grep -c 'workspace:'` = `0`
- [ ] User explicitly approved the real publish before Task 4 ran
- [ ] Single commit `chore: bump packages to 0.2.1` exists with bumps + lockfile + CHANGELOGs
- [ ] Dry-run log at `/tmp/napplet-publish-dryrun.log` retained for audit trail
- [ ] Real publish log at `/tmp/napplet-publish-real.log` shows 13 `+ @napplet/...@0.2.1` lines
</success_criteria>

<output>
After completion, the executor should return a concise summary noting: (a) all 13 package names + 0.2.1 published, (b) registry grep counts for shim + sdk both 0, (c) commit hash of the version-bump commit, (d) any anomalies worth surfacing. No SUMMARY.md file needed for a quick plan.
</output>
