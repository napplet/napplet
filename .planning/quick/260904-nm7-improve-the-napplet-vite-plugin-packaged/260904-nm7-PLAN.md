---
quick_id: 260904-nm7
phase: quick-260904-nm7
plan: 01
type: execute
mode: quick-full
wave: 1
depends_on: []
files_modified:
  - packages/vite-plugin/src/optimizer/loader-screen.ts
  - packages/vite-plugin/src/optimizer/loader.ts
  - packages/vite-plugin/src/optimizer/loader-shared.ts
  - packages/vite-plugin/src/optimizer/loader-runtime.ts
  - packages/vite-plugin/src/optimizer/loader-source.ts
  - packages/vite-plugin/src/optimizer/loader.test.ts
  - packages/vite-plugin/src/optimizer/pipeline.ts
  - packages/vite-plugin/src/optimizer/pipeline-render.ts
  - packages/vite-plugin/src/optimizer/pipeline.test.ts
  - packages/vite-plugin/src/optimizer/large-fixture-runtime.ts
  - packages/vite-plugin/src/optimizer/large-fixture-runtime.test.ts
  - packages/vite-plugin/src/optimizer/large-fixture-services.ts
  - packages/vite-plugin/src/optimizer/large-fixture.ts
  - packages/vite-plugin/src/optimizer/large-fixture.evidence.json
  - scripts/validate-packaged-loader-evidence.mjs
  - scripts/validate-packaged-loader-evidence.test.mjs
  - scripts/lib/packaged-loader-evidence-core.mjs
  - scripts/lib/packaged-loader-evidence-local.mjs
  - scripts/lib/packaged-loader-evidence-remote.mjs
  - scripts/lib/packaged-loader-evidence-services.mjs
  - scripts/lib/packaged-loader-evidence-shared.mjs
  - scripts/lib/packaged-loader-evidence-timeline.mjs
  - .changeset/polished-packaged-loader.md
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/01-initial.png
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/02-active-35s.png
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/03-partial.png
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/04-error.png
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/05-ready.png
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/06-cancelled.png
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/07-light.png
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/08-dark.png
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/09-reduced-motion.png
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/10-keyboard-retry.png
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/paja-loader-timeline.json
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/paja-loader-trace.zip
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/260904-nm7-REVIEW.md
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/260904-nm7-VERIFICATION.md
  - .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/260904-nm7-SUMMARY.md
  - .planning/STATE.md
  - .omx/state/packaged-loader-ux/ralph-progress.json
autonomous: true
requirements:
  - QUICK-260904-NM7
  - LOADER-UX-01
  - LOADER-UX-02
  - LOADER-UX-03
  - LOADER-UX-04
  - LOADER-UX-05
  - LOADER-UX-06
  - LOADER-UX-07
  - RELEASE-01
must_haves:
  truths:
    - "Every optimized packaged napplet renders a polished, opaque, full-viewport loader immediately from inline HTML/CSS/JS, with a responsive centered card, system fonts, restrained high-contrast light/dark colors, native controls, visible focus, and a static reduced-motion treatment; it loads no remote visual dependency."
    - "While at least one startup resource is active, the loader exposes a semantic indeterminate `<progress>` with no value and announces `Loading resources N of T` using tabular whole-resource counts that advance exactly once only after length and SHA-256 verification; it never presents bytes, percentages, rates, chunks, or ETA."
    - "Multiple single-resource calls remain concurrent, duplicate calls coalesce, and ordered `bytesMany` results process every row independently: verified successful siblings enter the cache even when another row fails, failed-source identities remain retryable, the aggregate application promise remains pending, retry targets only failed/cancelled rows, and the final array preserves original input order."
    - "The loader remains visible until every required resource in the observed startup cohort resolves, then the packaged application replaces it as one document handoff without an empty or partially reconstructed application frame."
    - "A failed or explicitly cancelled attempt keeps the original application-facing promise recoverable, names the affected resource only through sanitized `textContent`, exposes keyboard-operable Retry, reuses verified independent results, and sends cancellation only through the existing NAP-RESOURCE AbortSignal path."
    - "Existing bundling, parser-scoped URL rewriting, exact-byte integrity checks, private-table ordering, CSP/sandbox posture, memory/batch bounds, rollback/no-op behavior, resource cleanup, and zero-entry builds remain unchanged and covered by regression tests."
    - "A freshly built and remotely deployed packaged napplet proves initial, active-after-35-seconds, partial, retryable failure, cancellation, keyboard retry, light, dark, reduced-motion, and final-application states through Paja using real resource bytes; a retained deterministic validator independently derives timing/order, validates image/trace structure and hashes, resolves the public naddr, refetches bytes, recomputes index/resource/aggregate identity, cross-checks evidence, and scans for the ephemeral secret."
    - "A patch changeset ships only `@napplet/vite-plugin`, all targeted/full/security/quality gates pass, independent review and GSD verification pass, and an OPEN non-force-pushed PR has head `feat/packaged-loader-ux`, base `feat/vite-plugin-blossom-optimization`, exact remote/local head equality, and the current base as an ancestor; the PR is not merged."
  artifacts:
    - path: "packages/vite-plugin/src/optimizer/loader-screen.ts"
      provides: "Deterministic inline loader markup and styling for initial, active, success, failure, and cancelled projections"
      contains: "prefers-reduced-motion"
    - path: "packages/vite-plugin/src/optimizer/loader.ts"
      provides: "Cancellable, retryable, concurrent private resource runtime and generated loader source without an absolute timeout"
      contains: "AbortController"
    - path: "packages/vite-plugin/src/optimizer/loader.test.ts"
      provides: "State, concurrency, integrity, cancellation, retry, batching, diagnostic-safety, and no-timeout regressions"
    - path: "packages/vite-plugin/src/optimizer/pipeline.ts"
      provides: "Immediate head/body injection through the existing measured and transactionally committed render path"
    - path: "packages/vite-plugin/src/optimizer/pipeline.test.ts"
      provides: "Self-contained injection, zero-entry, rollback, URL-rewrite, CSP/sandbox, and aggregate-order regressions"
    - path: "packages/vite-plugin/src/optimizer/large-fixture-runtime.test.ts"
      provides: "Browser-like execution tests for every generated loader state, atomic handoff, parallel startup, native keyboard controls, and safe DOM updates"
    - path: ".changeset/polished-packaged-loader.md"
      provides: "Patch release note for the changed `@napplet/vite-plugin` artifact behavior"
    - path: ".planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/paja-loader-timeline.json"
      provides: "Exact production request/state/cancellation/retry/handoff timing and deployed artifact identity"
    - path: ".planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/paja-loader-trace.zip"
      provides: "Replayable production Paja browser trace"
    - path: "scripts/validate-packaged-loader-evidence.mjs"
      provides: "Fail-closed retained validator for timeline ordering/timing, PNG and trace structure/hashes, public naddr/deployed bytes/aggregate identity, cross-evidence associations, and secret absence"
    - path: "scripts/validate-packaged-loader-evidence.test.mjs"
      provides: "Deterministic valid and corrupted evidence cases for every validator rejection boundary"
    - path: ".omx/state/packaged-loader-ux/ralph-progress.json"
      provides: "Latest strict visual-verdict score, reasoning, suggestions, and next actions"
    - path: ".planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/260904-nm7-REVIEW.md"
      provides: "Independent diff review with no unresolved high or medium findings"
    - path: ".planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/260904-nm7-VERIFICATION.md"
      provides: "Requirement-by-requirement GSD completion evidence"
  key_links:
    - from: "packages/vite-plugin/src/optimizer/pipeline.ts"
      to: "packages/vite-plugin/src/optimizer/loader-screen.ts"
      via: "the same `entries.length > 0` render path inserts inline style/runtime in `<head>` and loader markup immediately after `<body>` before application modules run"
      pattern: "render.*Loader|injectPrivateMetadata"
    - from: "generated application resource callsites"
      to: "window.napplet.resource.bytes / bytesMany"
      via: "`window.__nappletPrivateResourceLoader` coalesces in-flight work, forwards an attempt-owned AbortSignal, retains ordered bulk requests, and never uses raw fetch or manual postMessage"
      pattern: "resource\.bytes|resource\.bytesMany|signal"
    - from: "terminal resource Blob"
      to: "loader status live region"
      via: "completion increments only after exact length and SHA-256 verification, then updates the persistent atomic status through `textContent`"
      pattern: "verify|textContent|aria-live"
    - from: "Retry button"
      to: "failed/cancelled resource attempt"
      via: "a fresh controller retries only unresolved work while the original Response/object-URL promise remains pending and verified cache entries remain reusable"
      pattern: "retry|AbortController|inFlight"
    - from: "successful startup cohort"
      to: "packaged application document"
      via: "the loader stays mounted through the promise continuation and is replaced by the existing `document.open/write/close` application handoff on the next frame"
      pattern: "requestAnimationFrame|document"
    - from: ".planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/paja-loader-timeline.json"
      to: "public Nostr manifest, deployed index, recorded resource endpoints, screenshots, and Playwright trace"
      via: "`scripts/validate-packaged-loader-evidence.mjs` derives event order and elapsed time from timestamps, resolves/refetches public identities, recomputes byte/hash/aggregate values, validates binary structures, and rejects cross-file mismatch or secret canaries"
      pattern: "validate-packaged-loader-evidence|sha256|aggregate"
    - from: "packages/vite-plugin/src/optimizer/pipeline.ts"
      to: "packages/vite-plugin/src/manifest.ts"
      via: "loader bytes enter final measurement before aggregate hashing, while rollback and zero-entry paths emit neither UI nor a dangling resource requirement"
      pattern: "renderOptimizedHtml|committedResourceCount"
    - from: "feat/packaged-loader-ux"
      to: "feat/vite-plugin-blossom-optimization"
      via: "normal Git push plus live GitHub PR metadata, current-base ancestry, and exact remote/local head equality"
      pattern: "feat/packaged-loader-ux|feat/vite-plugin-blossom-optimization"
---

# Quick Task 260904-nm7: Improve the Packaged-Napplet Loader UX

<objective>
Replace the bare packaged-resource waiting view with an immediate, self-contained, accessible system-status loader that reports only honest verified-resource progress, remains active without a local deadline, supports safe cancellation/retry, and hands off atomically to the packaged application.

Purpose: make a real Paja-loaded packaged napplet visibly alive and actionable throughout long resource recovery without changing protocol, transport, integrity, artifact, URL-rewrite, or sandbox contracts.

Output: tested `@napplet/vite-plugin` loader/pipeline changes, patch release metadata, deterministic local visual validation, real delayed-resource Paja evidence, independent review/verification artifacts, and an open stacked PR from `feat/packaged-loader-ux` to `feat/vite-plugin-blossom-optimization`.
</objective>

<execution_context>
@/home/sandwich/.codex/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.codex/get-shit-done/templates/summary.md
@/home/sandwich/.codex/skills/visual-verdict/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/260904-nm7-RESEARCH.md
@.planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/260904-nm7-VALIDATION.md
@/home/sandwich/.codex/attachments/56de2291-135d-46aa-ac2e-732b42d6af31/pasted-text-1.txt
@packages/vite-plugin/src/optimizer/loader.ts
@packages/vite-plugin/src/optimizer/loader.test.ts
@packages/vite-plugin/src/optimizer/pipeline.ts
@packages/vite-plugin/src/optimizer/pipeline.test.ts
@packages/vite-plugin/src/optimizer/references.ts
@packages/vite-plugin/src/optimizer/references.test.ts
@packages/vite-plugin/src/optimizer/large-fixture.ts
@packages/vite-plugin/src/optimizer/large-fixture-runtime.ts
@packages/vite-plugin/src/optimizer/large-fixture.test.ts
@packages/vite-plugin/src/optimizer/large-fixture.evidence.json
@packages/vite-plugin/src/manifest.ts
@packages/vite-plugin/package.json
@package.json
@packages/cli/README.md

<interfaces>
<!-- Existing contracts extracted from the live base. Preserve these boundaries unless the task explicitly extends private behavior. -->

From `packages/vite-plugin/src/optimizer/loader.ts`:
```typescript
export interface ResourceTableEntry {
  source: string;
  uri: string;
  sha256: string;
  bytes: number;
  mime: string;
}

interface ResourceDomain {
  bytes(uri: string, options?: { servers?: string[]; signal?: AbortSignal }): Promise<Blob>;
  bytesMany(requests: ResourceBytesRequest[], options?: { signal?: AbortSignal }): Promise<ResourceBytesItem[]>;
}

export interface ResourceRuntimeOptions {
  entries: readonly ResourceTableEntry[];
  window?: ResourceWindow;
  url?: ObjectUrlApi;
  digest?: (blob: Blob) => Promise<string>;
  maxAssetBytes?: number;
  maxLiveBytes?: number;
  maxBatchSize?: number;
  maxConcurrentDigests?: number;
  timeoutMs?: number;
}

export class ResourceRuntime {
  async resolve(source: string): Promise<Blob>;
  async resolveMany(sources: string[]): Promise<Blob[]>;
  async response(source: string): Promise<Response>;
  async objectUrl(source: string): Promise<string>;
  release(source?: string): void;
  teardown(): void;
}

export function renderPrivateResourceTable(entries: readonly ResourceTableEntry[]): string;
export function renderResourceLoader(entries: readonly ResourceTableEntry[]): string;
```

From `packages/vite-plugin/src/optimizer/pipeline.ts`:
```typescript
export interface RenderInput {
  build: RetainedBuild;
  selected: readonly RetainedAsset[];
  entries?: readonly ResourceTableEntry[];
  inventory?: ReferenceInventory;
}

export interface RenderedArtifact {
  html: string;
  bytes: number;
  entries: ResourceTableEntry[];
}

export function renderOptimizedHtml(input: RenderInput): RenderedArtifact;
export function planExternalAssets(input: RetainedBuild): OptimizationPlan;
export async function optimizeSingleFileArtifact(input: OptimizeArtifactInput, services: OptimizationServices): Promise<OptimizationReport>;
```

From `packages/vite-plugin/src/optimizer/references.ts`:
```typescript
export function inventoryArtifactReferences(input: ReferenceBuild): ReferenceInventory;
export function classifyAssetReferences(asset: RetainedAsset, inventory: ReferenceInventory): AssetEligibility;
export function rewriteArtifactReferences(input: RewriteInput): RewrittenArtifact;
```

From `packages/vite-plugin/src/optimizer/large-fixture-runtime.ts`:
```typescript
export async function executeFinalArtifact(
  finalHtml: string,
  entries: ResourceTableEntry[],
  uploaded: Map<string, Uint8Array>,
  sha256: (bytes: Uint8Array) => string,
): Promise<string[]>;
```

Canonical constraints: NAP-RESOURCE PR #80 head `fa6bcc6935aa19e7b70ab2a2c721dafca77c78e1` defines terminal complete Blobs, ordered bulk result rows, and AbortSignal-driven `resource.cancel`, but no byte/chunk/progress fields. NIP-5D PR #2303 head `24711d9c47bbdd07908bf1d52bf677d9cbc530f0` remains authoritative for verified `srcdoc`, `sandbox="allow-scripts"`, inline single-file execution, and shell-injected policy. Loader UI/state is private signed-artifact plumbing and must not be described as a protocol requirement.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and implement the honest cancellable loader state machine</name>
  <files>packages/vite-plugin/src/optimizer/loader-screen.ts, packages/vite-plugin/src/optimizer/loader.ts, packages/vite-plugin/src/optimizer/loader.test.ts, /tmp/napplet-packaged-loader-reference.html, /tmp/reference-01-initial.png, /tmp/reference-02-active-35s.png, /tmp/reference-03-partial.png, /tmp/reference-04-error.png, /tmp/reference-05-ready.png, /tmp/reference-06-cancelled.png, /tmp/reference-07-light.png, /tmp/reference-08-dark.png, /tmp/reference-09-reduced-motion.png, /tmp/reference-10-keyboard-retry.png</files>
  <behavior>
    - "Initial: inline markup is display-ready before any startup call, with a persistent atomic live region, no numeric claim, no activity animation, and `aria-busy=false`."
    - "Active/partial: the first unique call makes a native `<progress>` visible without a `value`; after the startup cohort closes on one idle animation frame, status reads `Loading resources N of T`; `N` advances once per unique source only after length and digest verification, including terminal rows from an ordered bulk response."
    - "Concurrency: all sibling single-resource calls are outstanding before the first resolves, duplicates share one in-flight promise, and `resolveMany` keeps one ordered request group rather than decomposing it into sequential `bytes` calls."
    - "Long wait: advancing fake time beyond 30 seconds neither rejects nor changes the active state; only a terminal resource result or explicit cancellation settles the attempt."
    - "Mixed bulk/failure/cancellation/retry: a mixed ordered bulk result verifies and caches each successful sibling independently, records each failed source without discarding success, keeps the aggregate application-facing promise pending, retries only failed/cancelled rows under fresh controllers, and resolves the same aggregate promise in original source order after recovery."
    - "Safety/accessibility: resource display names have C0/C1 controls replaced and enter the DOM only through `textContent`; retry/cancel are native buttons with at least 44px targets and visible focus; normal text reaches at least 4.5:1 and controls/focus indicators at least 3:1 contrast; motion is disabled under reduced motion; no gradient, remote asset/font, protocol branding, `transition: all`, percentage, bytes, rate, chunk, or ETA copy is emitted."
  </behavior>
  <action>Before production styling, use `apply_patch` to create the uncommitted standalone `/tmp/napplet-packaged-loader-reference.html` and capture exactly ten reference PNGs with Playwright. The one-to-one mapping is fixed: `/tmp/reference-01-initial.png -> evidence/01-initial.png`, `/tmp/reference-02-active-35s.png -> evidence/02-active-35s.png`, `/tmp/reference-03-partial.png -> evidence/03-partial.png`, `/tmp/reference-04-error.png -> evidence/04-error.png`, `/tmp/reference-05-ready.png -> evidence/05-ready.png`, `/tmp/reference-06-cancelled.png -> evidence/06-cancelled.png`, `/tmp/reference-07-light.png -> evidence/07-light.png`, `/tmp/reference-08-dark.png -> evidence/08-dark.png`, `/tmp/reference-09-reduced-motion.png -> evidence/09-reduced-motion.png`, and `/tmp/reference-10-keyboard-retry.png -> evidence/10-keyboard-retry.png`. Each reference must render its named state explicitly, including the ready/application-transition target and focused keyboard-retry target, in the specified refined utilitarian/system-status aesthetic. Use system fonts, the research palette, full-viewport opaque canvas, restrained centered card, one border/accent, tabular counts, 44px controls, visible focus, light/dark variants, and a static reduced-motion variant. Calculate the chosen foreground/background pairs and require WCAG ratios of at least 4.5:1 for normal text and 3:1 for controls/focus indicators. Do not copy a third-party product, add remote assets, or commit the temporary document/screenshots.</action>

  <action>Write failing `loader.test.ts` cases for every behavior above before implementation and observe RED. Add `loader-screen.ts` as the deterministic source of self-contained inline markup/CSS and small state projection; keep resource labels out of generated HTML templates and write them only through `textContent`, use `dir="auto"` plus `overflow-wrap:anywhere`, keep the status node persistent with `role="status" aria-live="polite" aria-atomic="true"`, toggle `aria-busy`, and show the indeterminate native progress element only while work is active. Provide native Cancel while active and native Retry after failure/cancellation; their styling must preserve visible `:focus-visible`, sufficient contrast, and static reduced-motion activity without a gradient or external font/asset.</action>

  <action>Refactor the TypeScript reference runtime and emitted runtime source together around an in-flight registry keyed by original source, an observed-startup cohort closed after one idle animation frame, verified completion accounting, attempt-owned AbortControllers, and a retry latch that keeps the original caller promise recoverable. Pass `{ signal }` through the existing `resource.bytes` and `resource.bytesMany` options so cancellation uses canonical NAP-RESOURCE behavior; never construct an envelope or call raw `fetch`. Remove `DEFAULT_TIMEOUT_MS`, `withTimeout`, `timeoutMs`, and every napplet-side deadline instead of increasing the deadline. Per NAP-RESOURCE PR #80 head `fa6bcc6935aa19e7b70ab2a2c721dafca77c78e1`, validate bulk result input-order/length association, then independently verify/cache every successful row even when siblings fail, retain failed-source identities and errors, keep the aggregate application promise pending, retry only failed/cancelled rows with fresh controllers, and reconstruct its final Blob array in original input order; a failed row must never discard or refetch a verified successful sibling. Preserve canonical table validation, exact length/lowercase SHA-256 verification, 10 MiB asset and 50 MiB live-Blob bounds, bounded digest work, Response MIME, reference-counted object URLs, release/teardown cleanup, and lazy demand; do not prefetch the private table to calculate a denominator. Run tests GREEN and refactor without changing behavior. Validate that all ten temporary reference PNGs exist, have valid PNG signatures and non-zero dimensions, and visually encode their named states; do not run `$visual-verdict` until Task 2 creates matching generated screenshots. Commit the green runtime/screen/tests by explicit paths with an intent-first conventional message, useful Lore trailers, and a Codex co-author trailer.</action>
  <verify>
    <automated>pnpm --dir packages/vite-plugin exec vitest run --config vitest.config.ts src/optimizer/loader.test.ts &amp;&amp; pnpm --filter @napplet/vite-plugin type-check &amp;&amp; node -e "const fs=require('fs');const names=['01-initial','02-active-35s','03-partial','04-error','05-ready','06-cancelled','07-light','08-dark','09-reduced-motion','10-keyboard-retry'];for(const name of names){const b=fs.readFileSync('/tmp/reference-'+name+'.png');if(b.length&lt;24||b.subarray(0,8).toString('hex')!=='89504e470d0a1a0a'||b.readUInt32BE(16)&lt;1||b.readUInt32BE(20)&lt;1)process.exit(1)}" &amp;&amp; git diff --check</automated>
  </verify>
  <done>The loader state machine is test-first and green: it presents honest accessible states, counts only verified complete resources, remains concurrent and active beyond 30 seconds, retries/cancels without stranding the original application call, emits no invented progress or transport, preserves all bounds/integrity/cleanup rules, and all ten valid state-specific visual references exist for Task 2 comparison.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the loader into the measured artifact and prove atomic browser behavior</name>
  <files>packages/vite-plugin/src/optimizer/pipeline.ts, packages/vite-plugin/src/optimizer/pipeline.test.ts, packages/vite-plugin/src/optimizer/large-fixture-runtime.ts, packages/vite-plugin/src/optimizer/large-fixture-runtime.test.ts, packages/vite-plugin/src/optimizer/large-fixture.evidence.json</files>
  <behavior>
    - "A committed optimized artifact puts loader style/runtime in `<head>` and display-ready loader markup immediately after the opening `<body>`, before deferred/module application execution, with every byte included in candidate/final measurements and the manifest aggregate hash."
    - "A zero-entry, at-target, ineligible, or rolled-back build contains no loader UI/private runtime and no dangling `requires:resource`, while selected-resource success retains the existing private table and loader ordering."
    - "The emitted artifact renders initial, active, partial, failure, cancelled, retry, and success states in a browser-like DOM; HTML-shaped/control-character resource names remain text, native buttons accept keyboard activation, light/dark/reduced-motion markup is present, and no byte-derived visible value exists."
    - "All eager application resource calls are issued without serialization; mixed successful/failed bulk rows retain verified siblings and retry only failures; one resource may stay active past 30 seconds while siblings complete; the loader stays mounted until the final verified resource and existing application continuation replace the document atomically in original input order."
    - "Existing parser-scoped HTML/srcset/CSS/JavaScript rewrites, integrity failures, URL/object ownership, ordered batches, rollback restoration, CSP/sandbox absence/preservation, single-file output, and final aggregate hash remain covered."
  </behavior>
  <action>Write RED integration cases in `pipeline.test.ts` and a new `large-fixture-runtime.test.ts` before changing production wiring. Assert exact injection position/order, self-containment, no UI for every zero-commit path, loader bytes in `RenderedArtifact.bytes`, stable table ordering, and unchanged manifest/aggregate ordering. Exercise the real generated script with deferred terminal Blobs in the existing browser-like harness (use the already installed DOM test support or extend the VM harness; add no dependency): assert every eager request is outstanding before resolving one, hold one beyond 30 seconds, delay digest completion to prove the count remains unchanged until verification, and add an explicit mixed ordered bulk vector such as `[success A, failure B, success C]`. Prove A/C verify and enter cache exactly once, B's original source remains in the error state, the aggregate caller promise stays pending, Retry requests only B, and the final result resolves `[A, retried B, C]` without re-requesting or discarding A/C. Also inject a malicious-looking/control-character source to prove `textContent` safety, activate Cancel/Retry by keyboard, and make the application continuation replace the document only after all original promises resolve.</action>

  <action>Update `pipeline.ts` so `entries.length > 0` alone causes the existing final renderer to inject the self-contained style/private table/runtime into `<head>` and display-ready loader markup immediately after the opening `<body>`; tolerate ordinary body attributes and keep the runtime installed before application modules. Reuse `loader-screen.ts` renderers rather than duplicating presentation strings. Do not touch `references.ts`, reference classification, URL rewrite rules, `manifest.ts`, CSP, sandbox tokens, network transport, signed-table integrity fields, or transaction order. Failed/no-op optimization must still restore/emit the ordinary all-inline artifact without loader remnants or a resource requirement.</action>

  <action>Extend `large-fixture-runtime.ts` only enough to expose deterministic loader state/timing/handoff evidence from the emitted artifact while retaining its exact-byte request assertions. After the new and existing suites pass, regenerate `large-fixture.evidence.json` from the actual fixture output so final HTML/index/aggregate hashes reflect the shipped inline loader; do not hand-edit hashes or weaken assertions. Capture all ten integrated generated state screenshots as `/tmp/generated-&lt;state&gt;.png` using the same viewport/theme/motion/focus conditions as the fixed reference mapping, compare every generated image to its one matching Task 1 reference with `$visual-verdict`, and enforce the verdict-before-next-edit loop separately for each mapping. Persist the ten complete records under `local_comparisons` in `.omx/state/packaged-loader-ux/ralph-progress.json` and require each score >=90, `verdict: "pass"`, and `category_match: true`. Remove no existing regression and commit the artifact wiring, tests, deterministic fixture evidence, and persisted verdict file by explicit path with a separate intent-first Lore commit and Codex co-author trailer.</action>
  <verify>
    <automated>pnpm --dir packages/vite-plugin exec vitest run --config vitest.config.ts src/optimizer/loader.test.ts src/optimizer/pipeline.test.ts src/optimizer/references.test.ts src/optimizer/large-fixture-runtime.test.ts src/optimizer/large-fixture.test.ts &amp;&amp; pnpm --filter @napplet/vite-plugin type-check &amp;&amp; pnpm --filter @napplet/vite-plugin build &amp;&amp; git diff --exit-code HEAD^ -- packages/vite-plugin/src/optimizer/references.ts packages/vite-plugin/src/optimizer/references.test.ts packages/vite-plugin/src/manifest.ts &amp;&amp; node -e "const fs=require('fs');const p=JSON.parse(fs.readFileSync('.omx/state/packaged-loader-ux/ralph-progress.json','utf8'));if(!Array.isArray(p.local_comparisons)||p.local_comparisons.length!==10||p.local_comparisons.some(x=&gt;x.score&lt;90||x.verdict!=='pass'||x.category_match!==true||typeof x.reasoning!=='string'||!Array.isArray(x.differences)||!Array.isArray(x.suggestions)||!Array.isArray(x.next_actions)))process.exit(1)" &amp;&amp; git diff --check</automated>
  </verify>
  <done>The loader is reachable immediately only in committed optimized artifacts, all loader bytes participate in final measurement/hashing, the emitted runtime proves every requested state and atomic handoff without serializing work or timing out, zero/rollback and parser-rewrite/security behavior remain green, deterministic fixture hashes are regenerated from real output, and visual validation passes at 90 or above.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the production Paja flow, pass independent gates, and open the stacked PR</name>
  <files>scripts/validate-packaged-loader-evidence.mjs, scripts/validate-packaged-loader-evidence.test.mjs, .changeset/polished-packaged-loader.md, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/01-initial.png, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/02-active-35s.png, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/03-partial.png, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/04-error.png, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/05-ready.png, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/06-cancelled.png, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/07-light.png, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/08-dark.png, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/09-reduced-motion.png, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/10-keyboard-retry.png, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/paja-loader-timeline.json, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence/paja-loader-trace.zip, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/260904-nm7-REVIEW.md, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/260904-nm7-VERIFICATION.md, .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/260904-nm7-SUMMARY.md, .planning/STATE.md</files>
  <behavior>
    - "A complete deterministic fixture passes the retained evidence validator, while independently corrupted timeline schema/order/timestamps, missing session/state, PNG signature/dimensions/hash, trace ZIP structure/hash, naddr/manifest/index/resource/aggregate associations, cross-evidence hashes, and secret canaries each fail closed."
    - "The production validator derives an active interval greater than 35 seconds from monotonic recorded timestamps, never from a trusted summary boolean, and requires all four named closed sessions plus all ten screenshot states."
    - "The validator resolves the public naddr through its relay hints, verifies the signed matching manifest, fetches the deployed index and recorded resource endpoints, recomputes byte lengths/SHA-256 plus canonical NIP-5A aggregate identity, and matches them to timeline and retained artifact hashes."
    - "Every retained artifact is scanned against the live ephemeral secret and its common encodings before that secret is destroyed; neither validator output nor committed evidence contains or prints the secret."
    - "Publication has two immutable checkpoints: H1 contains implementation, validator, changeset, and evidence and is independently reviewed; H2 adds only review/GSD metadata that explicitly records `reviewed_sha: H1`; the final audit is read-only and never treats an artifact as attesting its own containing commit."
  </behavior>
  <action>Wave 0: write failing `node:test` cases in `scripts/validate-packaged-loader-evidence.test.mjs` before implementing `scripts/validate-packaged-loader-evidence.mjs`. Use only existing dependencies and Node built-ins. Define and fail closed on a versioned timeline schema with monotonic request/state/retry/cancel/final-app events, exact session names/states, screenshot metadata, trace metadata, manifest/index/resource endpoint identities, and cross-artifact SHA-256. Parse PNG signature and IHDR to require non-zero recorded dimensions; parse the ZIP end/central directory, require Playwright trace entries, and inflate text metadata with built-in `zlib` for secret scanning; do not accept non-empty-file checks. Decode the public naddr, query its relay hints for the signed author/kind/d-tag manifest, fetch the deployed index and each recorded real resource endpoint, recompute exact lengths/SHA-256, and recompute the canonical NIP-5A aggregate from sorted `<sha256> <absolute-path>\n` UTF-8 lines per https://github.com/nostr-protocol/nips/blob/master/5A.md#aggregate-hash. Accept a `--secret-file` only for local scanning, never log its value, scan retained evidence/reports/diff/PR text against raw and common encoded forms, and expose injectable relay/fetch seams so deterministic tests need no live network. Its `--publication` mode must fetch Git/PR state read-only, derive H1 from both reports, require H2=local=remote=PR head, exact H1..H2 metadata paths, byte-identical source/evidence trees, current base ancestry, OPEN/unmerged state, current body evidence, and no PR force-push timeline event. Tests must include one valid vector and a separate rejection for every field/order/timing/binary/remote/hash/secret/publication boundary named in `<behavior>`. Add `.changeset/polished-packaged-loader.md` with a patch bump for `@napplet/vite-plugin` only and user-facing prose describing immediate accessible verified-resource status plus safe retry; make no protocol claim and leave the base branch's changesets unchanged.</action>

  <action>Build and deploy a fresh production packaged napplet from this exact branch without opening, reading, staging, stashing, editing, or deleting `packages/cli/.napplet/config.json`. The user's explicit real-production Paja requirement authorizes exactly one fresh ephemeral deployment, so create a fixture with `mktemp -d /tmp/napplet-packaged-loader-ux.XXXXXX`, use `apply_patch` for its temporary files, and generate ten deterministic 7-9 MiB resources plus an eager `Promise.all` application that reconstructs its final document through the existing response rewrites. Import the freshly built local plugin and enable real large-asset optimization. Generate a one-time signing key at the explicit mode-0600 path `/tmp/napplet-260904-nm7-evidence.key` only after requiring that path not to exist; never echo it, place it in repository state, or record it in evidence. Adapt only the plugin's documented `largeAssetOptimization.node` test seams in the temporary Vite config so this ephemeral signer supplies valid kind-24242 upload authorization and signed relay/server discovery fixtures while the real network policy, pinned HTTPS upload, Blossom service, optimizer, Vite build, final HTML, and resource bytes remain production paths. Select live writable relay/Blossom endpoints after read-only health checks, record their public URLs but never the secret, and require each private-table digest to be remotely retrievable with exact byte length and SHA-256 before deployment.</action>

  <action>From the temporary fixture directory, run the repository-built CLI executable with `napplet init --force --source-dir dist --name packaged-loader-ux-260904-&lt;unique-suffix&gt;` plus the verified relay/server, then run the matching `napplet deploy --dry-run --name ... --prompt-sec` and non-dry deploy by redirecting the mode-0600 ephemeral secret file into the hidden prompt. Capture the new naddr, index hash, aggregate hash, private-table hashes, event id, relay, and Blossom origins without recording private key material. Require the fetched deployed `/index.html` bytes to hash exactly to the new manifest and never reuse/overwrite `large-single-file-test` or another historical d-tag.</action>

  <action>Run a temporary Playwright proof harness against `https://kehto.github.io/web/paja/?naddr=&lt;new-pointer&gt;` in separately named sessions `packaged-loader-long`, `packaged-loader-retry`, `packaged-loader-cancel`, and `packaged-loader-a11y`, closing each BrowserContext/browser in `finally`. Install routing before navigation and delay only requests whose URL contains a hash from the new private table; always continue the original real remote request bytes. Stagger completions so every eager request starts before the first terminal result, at least one remains active beyond 35 seconds, and a genuine partial count is visible. In the retry session abort one known resource route on its first attempt, capture its safely rendered label and focused Retry, then use Enter and Space across the automated/unit and browser evidence to retry successfully. In the cancellation session activate the native Cancel control while work is active, observe the canonical `resource.cancel` message at the Paja parent message boundary, capture the cancelled state, and keyboard-retry with a fresh request. In the accessibility session emulate light, dark, and reduced-motion preferences and exercise focus order/visible focus. Capture the ten exact evidence PNGs and the trace listed in `<files>`. Write the versioned timeline as raw timestamped events and associations, not pass/fail booleans: session open/close, navigation, loader states/counts, request start/terminal, digest verification, mixed-row success/failure, cancellation observation, retry, final app marker, console/page errors, public naddr/manifest/path/x/server tags, exact index/resource endpoints, byte lengths, SHA-256 values, PNG dimensions/hashes, trace entry/hash, browser/version, and no secret material. Run each fixed one-to-one `$visual-verdict` mapping from Task 1 against the production evidence only after its matching screenshot exists, and persist the authoritative ten records under `production_comparisons` in `.omx/state/packaged-loader-ux/ralph-progress.json`; each must have score >=90, `verdict: "pass"`, `category_match: true`, reasoning, differences, suggestions, and next_actions before any next visual edit. Run `node scripts/validate-packaged-loader-evidence.mjs <evidence-dir> --secret-file /tmp/napplet-260904-nm7-evidence.key` so the retained validator, not hand-written booleans or file existence, proves the evidence while the key is still available for leak scanning. Retain the temporary fixture/key until the H2 final audit; commit only the useful evidence, validator/tests, changeset, and authoritative verdict file by explicit path, never the reference images, proof harness, fixture, or secret.</action>

  <action>H1 preparation: fetch the live optimizer base and any existing feature ref before freezing implementation. The only permitted push target anywhere in this plan is `feat/packaged-loader-ux`; no command may target `feat/vite-plugin-blossom-optimization`, use a deletion refspec, leading `+`, `--force`, or `--force-with-lease`. If the base advanced, merge it normally before H1 and rerun affected evidence; if an unexpected feature ref exists and is not an ancestor, integrate it normally or stop rather than overwrite it. Commit all implementation, tests, validator, changeset, and retained production evidence with atomic intent-first Lore commits, then define the final implementation/evidence commit as H1. Run targeted tests, validator tests, the live production evidence validator with `/tmp/napplet-260904-nm7-evidence.key`, `pnpm build`, `pnpm type-check`, `pnpm -r test:unit`, `pnpm lint`, `pnpm test`, `pnpm audit --audit-level high`, changed-code AI-slop at 100/100, dependency/diff checks, and visual comparison validation on H1. If any gate changes source/evidence, create a new H1 and rerun all gates. Require the fetched base to be H1's ancestor before publication.</action>

  <action>Publish H1 only with the literal normal push `git push -u origin feat/packaged-loader-ux`; fetch the feature ref and require remote H1 equality before any PR mutation.</action>

  <action>Write the complete H1 PR body to `/tmp/napplet-260904-nm7-pr-body.md` with `apply_patch`, then run `gh pr create --repo napplet/web --head feat/packaged-loader-ux --base feat/vite-plugin-blossom-optimization --title "Improve packaged napplet loading feedback" --body-file /tmp/napplet-260904-nm7-pr-body.md` to open the unmerged stacked PR with H1, evidence, exact commands, and the explicit private-artifact/no-protocol statement.</action>

  <action>H1 review and H2 metadata sequence: only after H1 is normally pushed and the live PR is OPEN, delegate a comprehensive independent code reviewer and an independent GSD verifier. Both inspect exactly H1 plus the live H1 PR: the reviewer covers correctness, accessibility, security, protocol fidelity, mixed-row concurrency/cache/retry, cleanup, validator quality, and test adequacy; the verifier checks every implementation, production-evidence, and H1-publication row against H1 tests and refetched production evidence while explicitly treating final H2 equality/body/audit predicates as pending future work. Write `260904-nm7-REVIEW.md` with `reviewed_sha: <H1>`, `status: passed`, and zero unresolved high/medium findings. Write `260904-nm7-VERIFICATION.md` with `reviewed_sha: <H1>`, `status: gaps_found`, `requirements_failed: 0`, and `publication_pending: true`; its only gaps must be the not-yet-created metadata-only H2 and final external publication audit. These reports attest H1 only and must never claim to attest the commit that contains them. If either pass requests a source/evidence change, make no metadata commit: return to the H1 sequence, apply the fix, rerun all H1 gates, normal-push the new H1 to the same feature ref, update the PR, and restart both reviews. When both passes request no source/evidence change, create the completed `260904-nm7-SUMMARY.md`, finalize `260904-nm7-VALIDATION.md`, and update `.planning/STATE.md`, then commit exactly PLAN, RESEARCH, VALIDATION, REVIEW, VERIFICATION, SUMMARY, and STATE as metadata-only H2 with a Lore commit. Rerun the targeted/validator/full/audit/slop/visual gates and live production evidence validator on H2, require the source/evidence trees to be byte-identical to H1, and push H2 only with literal `git push origin feat/packaged-loader-ux`.</action>

  <action>Final read-only publication audit: forbid every source, test, validator, changeset, screenshot, timeline, or trace edit after H1; any such change invalidates H1 and requires the entire H1 gate/review/H2 sequence to restart. Have fresh independent reviewer/verifier passes inspect H1 and H2 without writing a self-attesting repository file. They must prove `H1..H2` contains only the seven expected metadata paths (PLAN, RESEARCH, VALIDATION, REVIEW, VERIFICATION, SUMMARY, and STATE), source/evidence tree hashes at H2 equal H1, the reports' `reviewed_sha` values equal H1, all H2 gates and the live evidence validator passed, and no secret appears in retained artifacts/diff/PR body. The final external verifier alone closes the H2 publication predicates and may claim overall completion. Fetch both refs and query GitHub: the live base must be H2's ancestor, local HEAD and `origin/feat/packaged-loader-ux` and PR head must equal H2, the PR must be OPEN and unmerged with exact base/head names, and the PR timeline must contain no force-push event for this feature. Treat the rule that the base ref is never a push target as an execution invariant enforced from the orchestration tool-call log and the two literal permitted push commands, not as a fact derivable from Git state. Update the PR body with H1 review provenance, H2 head/evidence/gates, the final read-only audit result, and the exact two executed feature-branch push commands; capture the audit in the PR body and orchestrator output, not in a file that would create H3. Leave `/tmp/napplet-260904-nm7-evidence.key` available for the final automated validator invocation below; that gate removes the explicit key file only after successful validation. Remove the already-validated temporary fixture separately, and do not merge the PR.</action>
  <verify>
    <automated>node --test scripts/validate-packaged-loader-evidence.test.mjs &amp;&amp; pnpm --dir packages/vite-plugin exec vitest run --config vitest.config.ts src/optimizer/loader.test.ts src/optimizer/pipeline.test.ts src/optimizer/references.test.ts src/optimizer/large-fixture-runtime.test.ts src/optimizer/large-fixture.test.ts &amp;&amp; pnpm build &amp;&amp; pnpm type-check &amp;&amp; pnpm -r test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm test &amp;&amp; pnpm audit --audit-level high &amp;&amp; pnpm dlx aislop@0.12.0 scan --changes --base origin/feat/vite-plugin-blossom-optimization . &amp;&amp; git diff --check origin/feat/vite-plugin-blossom-optimization...HEAD &amp;&amp; git diff --exit-code origin/feat/vite-plugin-blossom-optimization...HEAD -- package.json packages/vite-plugin/package.json pnpm-lock.yaml packages/vite-plugin/src/optimizer/references.ts packages/vite-plugin/src/optimizer/references.test.ts packages/vite-plugin/src/manifest.ts &amp;&amp; node scripts/validate-packaged-loader-evidence.mjs .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/evidence --secret-file /tmp/napplet-260904-nm7-evidence.key --publication --repo napplet/web --head feat/packaged-loader-ux --base feat/vite-plugin-blossom-optimization --review .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/260904-nm7-REVIEW.md --verification .planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/260904-nm7-VERIFICATION.md &amp;&amp; node -e "const fs=require('fs');const p=JSON.parse(fs.readFileSync('.omx/state/packaged-loader-ux/ralph-progress.json','utf8'));if(!Array.isArray(p.production_comparisons)||p.production_comparisons.length!==10||p.production_comparisons.some(x=&gt;!Number.isInteger(x.score)||x.score&lt;90||x.verdict!=='pass'||x.category_match!==true||typeof x.reasoning!=='string'||!Array.isArray(x.differences)||!Array.isArray(x.suggestions)||!Array.isArray(x.next_actions)))process.exit(1)" &amp;&amp; test "$(git status --short -- packages/cli/.napplet/config.json)" = "?? packages/cli/.napplet/config.json" &amp;&amp; rm -- /tmp/napplet-260904-nm7-evidence.key &amp;&amp; test ! -e /tmp/napplet-260904-nm7-evidence.key</automated>
  </verify>
  <done>H1 contains the complete reviewed implementation/evidence and passes the retained validator plus all gates; H2 adds only REVIEW/VERIFICATION/SUMMARY/STATE metadata whose reports explicitly attest H1 and mark future publication predicates pending; the final external read-only audit alone closes overall completion by proving source/evidence identity across H1..H2, live base ancestry, exact local/remote/PR H2 equality, OPEN unmerged state, current PR evidence, no feature force event, the base-push execution invariant, and no secret leakage; the explicit ephemeral key is removed only after the final validator succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Signed private table -> loader DOM | Resource source text is signed metadata but still untrusted display input; it must never become executable markup or a selector. |
| Packaged app -> private loader -> NAP-RESOURCE | Lazy application calls cross into an injected capability; retrieval/cancellation must stay on existing `bytes`/`bytesMany` plus AbortSignal. |
| Terminal resource Blob -> application Response/object URL | Complete external bytes become executable/application-visible only after table-authorized length and SHA-256 verification. |
| Loader state -> application document | UI updates and final document reconstruction share a DOM lifetime; premature removal could expose blank or partial application state. |
| Local branch/evidence -> GitHub stacked PR | Commits, screenshots, hashes, and verification claims become remote review evidence and must not overwrite the base branch or leak the ephemeral signer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-NM7-01 | Tampering / Information disclosure | Failure diagnostic projection | mitigate | Keep the original source only as a Map key, replace control characters for display, use fixed error copy and `textContent`, and test HTML-shaped input creates no node or raw runtime/server diagnostic. |
| T-NM7-02 | Tampering | Blob handoff | mitigate | Retain exact byte-length, lowercase SHA-256, canonical URI, ordered bulk-row, and signed-table checks before count increments or Response/object-URL exposure; verify/cache successful bulk siblings independently and retry only identified failures. |
| T-NM7-03 | Denial of Service | Long resource recovery | mitigate | Remove the napplet deadline, retain memory/asset/batch/digest bounds and in-flight coalescing, keep parallel work parallel, and expose explicit AbortSignal cancellation/retry without polling or busy loops. |
| T-NM7-04 | Spoofing / Elevation of privilege | Resource transport boundary | mitigate | Use only injected `window.napplet.resource.bytes`/`bytesMany`; add no raw browser fetch, manual postMessage, manifest field, sandbox token, or capability claim. |
| T-NM7-05 | Repudiation | Visual and production proof | mitigate | Store exact public hashes, timestamps, session closure, errors, cancellation/retry, screenshots, trace, and independent review/verification on the same final head; never store the ephemeral secret. |
| T-NM7-06 | Tampering | Git publication | mitigate | Fetch live refs, require base ancestry and remote/local equality, push only the feature ref normally, prohibit every force form, verify the live OPEN PR metadata, and do not merge. |
</threat_model>

## Source Coverage Audit

| Source | ID | Feature / Constraint | Task | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | QUICK-260904-NM7 | Production-packaged napplets no longer show a bare/frozen wait state during long active recovery | 1, 2, 3 | COVERED | State/UI, artifact wiring, and real Paja proof form one vertical delivery. |
| REQ | LOADER-UX-01 | Inline all loader HTML/CSS/JS; immediate responsive polished light/dark view with no packaged resource dependency | 1, 2 | COVERED | `loader-screen.ts` plus head/body injection; self-containment tests. |
| REQ | LOADER-UX-02 | Animated semantic indeterminate progress only while active; honest verified whole-resource `N of T`; no invented byte progress | 1, 2, 3 | COVERED | Verification-gated reducer, DOM tests, and Paja screenshots/timeline. |
| REQ | LOADER-UX-03 | Remain responsive beyond 30 seconds and preserve kehto/web #263 no-timeout behavior | 1, 2, 3 | COVERED | Deadline removal, fake-time regression, active-after-35s production evidence. |
| REQ | LOADER-UX-04 | Replace loader atomically only after every required resource resolves | 1, 2, 3 | COVERED | Original promises remain pending through retry; browser harness and Paja final marker prove handoff. |
| REQ | LOADER-UX-05 | Styled actionable resource-specific failure; safe retry; semantic/live ARIA; reduced motion; keyboard; focus; contrast | 1, 2, 3 | COVERED | Native controls, safe DOM projection, visual gate, unit/browser/Paja evidence. |
| REQ | LOADER-UX-06 | Preserve bundling, integrity, URL rewriting, CSP, sandbox, cancellation, rollback, ordered batches, concurrency, and zero-entry behavior; no dependency | 1, 2, 3 | COVERED | Existing boundaries remain untouched and targeted/full/diff/dependency gates protect them. |
| REQ | LOADER-UX-07 | Unit/integration coverage for initial, active, partial, success, cancellation, retry, failure, safe labels, a11y themes/motion, no serialization, no byte fiction | 1, 2 | COVERED | RED/GREEN behavior lists and emitted-runtime browser tests cover every state/invariant. |
| REQ | RELEASE-01 | Fresh Paja deployment with real >30s resources, evidence, patch changeset, all gates, independent review/verification, and non-force stacked PR | 3 | COVERED | Exact production, release, review, ancestry, remote equality, and PR gates are explicit. |
| RESEARCH | STATE-MACHINE | Initial/active/partial/failure/cancelled/retry/success reducer and first-idle-frame startup cohort | 1, 2 | COVERED | Implemented without prefetching table entries. |
| RESEARCH | CONCURRENCY | In-flight coalescing, existing bulk groups, successful-sibling retention, failed-source retry, and original-order aggregate reconstruction | 1, 2 | COVERED | Unit and generated-artifact tests include an explicit mixed success/failure/retry batch. |
| RESEARCH | SECURITY | Safe `textContent`, control-character filtering, exact integrity, bounded memory/work, existing capability only | 1, 2, 3 | COVERED | Included in behavior, threat model, review, and evidence audit. |
| RESEARCH | VISUAL | Refined system-status aesthetic, reference-first visual workflow, light/dark/reduced motion, 90+ verdict | 1, 2, 3 | COVERED | Temporary references precede styling; local and production screenshots share persisted strict verdict. |
| RESEARCH | PAJA | Fresh ephemeral d-tag/key, real remote bytes, delayed routes, screenshots/timeline/trace | 3 | COVERED | Historical manifest is never overwritten and temporary secret is never retained. |
| RESEARCH | EVIDENCE | Retained validator rejects unverifiable timing/binary/remote/hash/secret/publication claims | 3 | COVERED | Test-first validator independently derives and refetches proof; the final gate invokes it. |
| CONTEXT | — | No quick-task CONTEXT.md or D-XX decisions exist | — | N/A | Direct pasted requirements and researched canonical boundaries are fully represented above. |

<verification>
1. RED/GREEN tests prove initial, active, partial, success, failure, cancellation, retry, safe diagnostic, keyboard/focus, light/dark/reduced-motion, no-timeout, no-serialization, verified-count, and no-invented-byte behavior; the explicit mixed bulk vector proves verified successful siblings survive, only failed/cancelled sources retry, the aggregate promise stays pending, and final results preserve original order.
2. Pipeline and large-fixture suites prove inline self-containment, immediate injection, zero-entry/no-op/rollback absence, parser-scoped rewriting, exact integrity, bounded memory/work, CSP/sandbox preservation, loader-byte measurement, final manifest ordering, and atomic application handoff.
3. Every one of the ten required production screenshots maps to its exact pre-styling reference through `$visual-verdict`; `.omx/state/packaged-loader-ux/ralph-progress.json` contains ten complete records, each score >=90, `verdict: "pass"`, `category_match: true`, reasoning, differences, suggestions, and next_actions.
4. The retained validator, not timeline booleans or file existence, derives the >35-second interval and event order, validates required sessions/states plus PNG/ZIP structures/hashes, resolves the public naddr, refetches and recomputes manifest/index/resource/aggregate identity, cross-checks retained hashes, and scans all retained/external publication surfaces against the live ephemeral secret.
5. Targeted suites, validator corruption tests, `pnpm build`, `pnpm type-check`, `pnpm -r test:unit`, `pnpm lint`, `pnpm test`, `pnpm audit --audit-level high`, changed-code AI-slop 100/100, dependency/diff checks, and live production validation pass on H1 and again after metadata-only H2.
6. Independent code review and GSD verification attest only H1; H2 contains exactly REVIEW, VERIFICATION, SUMMARY, and STATE, and a separate final read-only audit proves source/evidence identity plus current publication state without creating a self-attesting H3.
7. `packages/cli/.napplet/config.json` remains untracked and was never opened, read, staged, stashed, edited, deleted, or disclosed; the explicit ephemeral key is never printed/committed and is removed only after the final validator succeeds.
8. Fresh remote refs prove the current optimizer base is an ancestor of H2, local HEAD and `origin/feat/packaged-loader-ux` and PR head equal H2, and GitHub reports an OPEN unmerged PR with exact base/head, current evidence/verification body, and no force-push event; no workflow command targets the base ref.
</verification>

<success_criteria>
- A packaged napplet presents a polished self-contained loader immediately and never appears frozen during a genuine resource wait longer than 30 seconds.
- Resource status is accessible and honest: activity is indeterminate, counts reflect only verified complete Blobs, failure/cancellation is safely actionable, and no byte/percentage/ETA fiction appears.
- Retry preserves the original application startup; mixed bulk successes remain verified/cached while only failures retry; concurrency and original result order remain intact; and the loader gives way only through an atomic complete-document handoff.
- Bundling, single-file measurement/hashing, integrity, URL rewriting, CSP, sandbox, cancellation, rollback, bounds, zero-entry output, and dependency surfaces do not regress.
- Local tests and ten one-to-one visual checks plus a fresh real-byte Paja deployment provide independently refetchable/recomputable evidence through the retained validator, with H1 review/GSD verification and a separate H2 publication audit passing.
- `@napplet/vite-plugin` has a patch changeset and an OPEN, unmerged, non-force-pushed PR from `feat/packaged-loader-ux` to the current `feat/vite-plugin-blossom-optimization` head; the unrelated CLI config is absent from the PR and untouched.
</success_criteria>

<output>
After both H1 reviews pass and before H2 publication, create `.planning/quick/260904-nm7-improve-the-napplet-vite-plugin-packaged/260904-nm7-SUMMARY.md` and include it in the exact metadata-only H2 commit so completion creates no H3.
</output>
