---
phase: quick-260826-q11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - packages/vite-plugin/src/optimizer/loader.ts
  - packages/vite-plugin/src/optimizer/loader.test.ts
  - packages/vite-plugin/src/optimizer/large-fixture.ts
  - packages/vite-plugin/src/optimizer/large-fixture.evidence.json
autonomous: true
requirements:
  - QUICK-260826-q11
must_haves:
  truths:
    - "PR #205 contains the already-merged #206 NAP-RESOURCE package contract through a normal merge from origin/main; the existing PR lineage is preserved and no force-push is used."
    - "Both the TypeScript ResourceRuntime and its emitted browser loader call resource.bytesMany with ordered ResourceBytesRequest objects shaped exactly as { url } for optimizer-owned Blossom URIs."
    - "The optimizer does not invent server hints, add a hint source, append BUD parameters, or create any protocol field, operation, manifest tag, or public option."
    - "Focused loader regressions and the 50 MiB+ deterministic fixture prove request-object recovery, exact-byte verification, and artifact hashes generated by the current merged branch."
    - "PR #205 records that #206 is satisfied, reports the regenerated fixture hashes, and returns to a mergeable state with current required checks passing."
  artifacts:
    - path: "packages/vite-plugin/src/optimizer/loader.ts"
      provides: "Contract-compatible direct and generated NAP-RESOURCE bulk loader"
      contains: "missing.map((entry) => ({ url: entry.uri }))"
    - path: "packages/vite-plugin/src/optimizer/loader.test.ts"
      provides: "Regression oracle for exact ResourceBytesRequest[] calls"
    - path: "packages/vite-plugin/src/optimizer/large-fixture.ts"
      provides: "End-to-end fixture adapter that consumes request objects without fabricating servers"
    - path: "packages/vite-plugin/src/optimizer/large-fixture.evidence.json"
      provides: "Deterministic hashes for the current emitted loader bytes"
  key_links:
    - from: "packages/vite-plugin/src/optimizer/loader.ts"
      to: "packages/core/src/types/global/runtime-api.ts"
      via: "The private structural ResourceDomain mirrors the released ResourceApi bulk request-object contract"
      pattern: "ResourceBytesRequest|bytesMany\\(requests"
    - from: "packages/vite-plugin/src/optimizer/loader.ts"
      to: "window.napplet.resource.bytesMany"
      via: "Both ResourceRuntime.resolveMany and renderResourceLoader map each private table entry to an object containing its canonical URL"
      pattern: "map\\(\\(entry\\) => \\(\\{ url: entry\\.uri \\}\\)\\)"
    - from: "packages/vite-plugin/src/optimizer/large-fixture.ts"
      to: "packages/vite-plugin/src/optimizer/large-fixture.evidence.json"
      via: "The real Vite build and exact-byte recovery produce the pinned final-index and aggregate hashes"
      pattern: "finalIndexSha256|aggregateHash"
---

# Quick Task 260826-q11: Integrate NAP-RESOURCE Request-Object Compatibility into PR #205

<objective>
Merge the released NAP-RESOURCE server-hint package contract into PR #205 and migrate the optimizer's private bulk loader to the canonical request-object interface.

Purpose: make the already-verified large-asset optimizer interoperable with the `ResourceBytesRequest[]` API released by #206, while preserving the dedicated-PR boundary and adding no protocol surface beyond the proposal.

Output: a non-force-pushed PR #205 branch whose direct/generated loaders send `{ url }` request objects, whose fixture evidence matches the current artifact bytes, and whose PR body/checks accurately report the completed dependency.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<canonical_refs>
Protocol behavior is derived from the living NAP proposal, not from repository implementations or the historical fix commit:

- NAP-RESOURCE proposal at verified revision `9511232f69313aa7953d110e35d32cc28d506f66`: https://github.com/napplet/naps/blob/9511232f69313aa7953d110e35d32cc28d506f66/naps/NAP-RESOURCE.md
- Semantic server-hint revision recorded by that proposal: `7531258`
- Relevant proposal sections: `API Surface`, `Wire Protocol`, `ResourceBytesRequest fields`, `Blossom Location Hints`, and `URL And Cache Keys`

The proposal defines `resource.bytesMany` as a non-empty ordered list of `ResourceBytesRequest { url, servers? }` objects and processes each entry independently. The optimizer owns canonical `blossom:sha256:<hex>` URLs but receives no server-hint metadata, so its conformant request is exactly `{ url: entry.uri }`; it must not synthesize `servers`, append discovery parameters, or implement host fallback/security policy.
</canonical_refs>

<context>
@AGENTS.md
@.planning/STATE.md
@packages/core/src/types/global/runtime-api.ts
@packages/vite-plugin/src/optimizer/loader.ts
@packages/vite-plugin/src/optimizer/loader.test.ts
@packages/vite-plugin/src/optimizer/large-fixture.ts
@packages/vite-plugin/src/optimizer/large-fixture.test.ts
@packages/vite-plugin/src/optimizer/large-fixture.evidence.json
@.changeset/tidy-blossom-assets.md

<interfaces>
Execution boundaries:

- Work only in `/tmp/napplet-pr205-resource-contract.MR5yHD`; do not switch, stash, reset, or edit the user's dirty primary checkout.
- The isolated branch starts at PR #205 head `bc77a610edd779bb763353a015d2cee79bb8c7cc`. Fetch current refs before execution; require `19e0029b` (merged #206) to be an ancestor of `origin/main`.
- Integrate `origin/main` with a merge commit. Do not rebase the PR, rewrite its existing commits, use `--force`, use `--force-with-lease`, or replace `.planning/STATE.md` wholesale with either merge side.
- Historical commit `bfaa2428503d1e9d7fa4677998500e6a0b188b28` is implementation evidence only. Transfer only the verified optimizer migration represented by its four optimizer paths; do not replay its resource-package, conformance, documentation, policy, debug, or changeset changes because #206 already owns and released them.
- Keep `ResourceBytesRequest` structural and private inside the optimizer. Do not add a runtime dependency, export, plugin option, manifest field/tag, mapping field, network path, or server-hint source.
- `.changeset/tidy-blossom-assets.md` already versions the unpublished Vite optimizer output in PR #205. Do not add another changeset for this compatibility correction unless execution discovers that the existing entry was removed.
- Remote authorization is limited to pushing the existing PR head without force and editing PR #205's body; do not merge or close the PR.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Merge released main and carry one request-object path through the direct and generated loaders</name>
  <files>.planning/STATE.md, packages/vite-plugin/src/optimizer/loader.ts, packages/vite-plugin/src/optimizer/loader.test.ts</files>
  <precondition>`origin/main` contains merged PR #206 (`git merge-base --is-ancestor 19e0029b origin/main`), the isolated checkout still descends from PR head `bc77a610`, and its only pre-existing local change is this quick-plan artifact.</precondition>
  <behavior>
    - "Direct path: ResourceRuntime.resolveMany calls bytesMany with ordered entries equal to [{ url: firstUri }, { url: secondUri }] rather than raw strings."
    - "Generated path: renderResourceLoader emits the same missing-entry mapping to exact { url } objects."
    - "Boundary: neither direct nor generated request objects contain a servers property because the optimizer has no canonical hint input."
    - "Response validation still rejects reordered, failed, missing-Blob, or wrong-URL items before caching or exposure."
  </behavior>
  <action>Fetch `origin/main` and the PR head, then merge current `origin/main` into the isolated PR lineage with `--no-ff` and without committing automatically. Resolve the expected `.planning/STATE.md` conflict by retaining PR #205's Phase 162 current-position/verification history and all non-duplicated main-line quick-task, release, and operator records; remove conflict markers manually instead of choosing an entire side. Commit the merge with an explicit merge message and Codex co-author trailer. If any additional conflict appears, inspect both sides and preserve both concerns rather than discarding either history.

After the merge, make the focused loader test fail on the current raw-string calls before changing production code. Add an optimizer-private `ResourceBytesRequest` structural interface with `url` and optional `servers`, align the private `bytes` options with the released single-request signature, and change private `bytesMany` to accept request objects. In `ResourceRuntime.resolveMany`, build one `{ url: entry.uri }` object per pending resource and validate returned item URLs against those requests. Apply the identical mapping inside `renderResourceLoader`; its helper parameter must describe requests, and the emitted code must call `bytesMany(missing.map((entry) => ({ url: entry.uri })))`. Update mocks and exact call assertions for batching/failure paths. Do not pass a `servers` key, import a package type, add a public option, or change the existing cache, timeout, digest, whole-Blob, or fail-closed behavior. Commit the compatibility code and regression atomically with a conventional commit and Codex co-author trailer.</action>
  <verify>
    <automated>git merge-base --is-ancestor origin/main HEAD &amp;&amp; git merge-base --is-ancestor bc77a610 HEAD &amp;&amp; pnpm --dir packages/vite-plugin exec vitest run --config vitest.config.ts src/optimizer/loader.test.ts &amp;&amp; git diff --check</automated>
  </verify>
  <done>The PR lineage contains current main through a merge commit, `.planning/STATE.md` preserves both histories, the regression was observed red before implementation and green after it, and both loader forms send only exact URL request objects while retaining all existing verification/failure behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the real optimizer fixture and pin evidence from the current artifact</name>
  <files>packages/vite-plugin/src/optimizer/large-fixture.ts, packages/vite-plugin/src/optimizer/large-fixture.evidence.json</files>
  <action>Migrate the large fixture's `bytesMany` adapter from string values to `Array&lt;{ url: string }&gt;`, record the actual request objects, look up uploaded bytes by each request's `url`, and assert the fixture used non-empty exact `{ url }` entries. Retain no `servers` field because the fixture's private resource table carries no server-hint input. Run the deterministic fixture against the merged implementation; because the emitted loader source changes signed artifact bytes, capture the actual received final-index SHA-256 and aggregate hash from that current run, update only those pinned values in `large-fixture.evidence.json`, and rerun to green. Do not copy the hashes from `bfaa2428` without observing the current artifact, and do not change candidate bytes, selected assets, upload evidence, mapping privacy, whole-Blob limits, or existing assertions to make the fixture pass. Commit the fixture adapter and regenerated evidence atomically with a conventional commit and Codex co-author trailer.</action>
  <verify>
    <automated>pnpm --dir packages/vite-plugin exec vitest run --config vitest.config.ts src/optimizer/loader.test.ts src/optimizer/large-fixture.test.ts &amp;&amp; pnpm --filter @napplet/vite-plugin type-check &amp;&amp; pnpm --filter @napplet/vite-plugin build &amp;&amp; git diff --check</automated>
  </verify>
  <done>The real 50 MiB+ build/recovery fixture passes through request objects, every recovered Blob remains byte-identical and hash-verified, and the evidence file contains hashes measured from the current merged artifact rather than historical guesses.</done>
</task>

<task type="auto">
  <name>Task 3: Verify, non-force-push, and refresh PR #205's completed dependency contract</name>
  <files>packages/vite-plugin/src/optimizer/large-fixture.evidence.json, .changeset/tidy-blossom-assets.md (read-only verification); GitHub PR #205 body (remote metadata)</files>
  <action>Run the focused Vite suite and the complete repository integration/release/security gates against the merged branch. Confirm the existing changeset still includes `@napplet/vite-plugin` and that no second server-hint/resource-package changeset entered this PR. Audit the active optimizer sources for a raw-string `bytesMany` call and for any optimizer-authored `servers` value using structural tests or AST-aware inspection rather than comment-counting file-wide greps.

Fetch the remote PR head immediately before publishing. If it advanced, merge that remote head normally and rerun the affected/full gates; never overwrite it. Require `origin/feat/vite-plugin-blossom-optimization` to be an ancestor of local `HEAD`, then push with `git push origin HEAD:feat/vite-plugin-blossom-optimization` and no force option. After the push succeeds, edit PR #205's existing body without replacing unrelated sections: update the fixture's final-index and aggregate hashes from `large-fixture.evidence.json`; replace the stale "merge #206 first" dependency language with a completed-dependency note that #206 is merged and present through the main merge; state that the optimizer sends URL-only `{ url }` request objects because it owns no hint source; and link the canonical proposal revision `9511232f69313aa7953d110e35d32cc28d506f66`. Preserve the existing security, verification, scope, and private-mapping explanations. Watch PR checks to completion and verify GitHub reports the branch mergeable. Do not merge or close the PR.</action>
  <verify>
    <automated>node --test scripts/check-build-secret-leaks.test.mjs &amp;&amp; node scripts/check-build-secret-leaks.mjs &amp;&amp; pnpm build &amp;&amp; pnpm type-check &amp;&amp; pnpm -r test:unit &amp;&amp; pnpm test &amp;&amp; pnpm lint &amp;&amp; pnpm check:jsr &amp;&amp; pnpm test:release-tooling &amp;&amp; pnpm audit --audit-level high &amp;&amp; pnpm dlx aislop@0.12.0 scan --changes --base origin/main . &amp;&amp; git diff --check &amp;&amp; gh pr checks 205 --repo napplet/web</automated>
  </verify>
  <done>The full local gate is green, the existing PR branch contains all new commits through a normal push, PR #205 accurately records the satisfied #206 dependency and current hashes, required GitHub checks pass, and GitHub no longer reports the PR as conflicting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Private optimizer table -> runtime NAP-RESOURCE API | Build-authored Blossom URLs cross into a host-owned resource fetch boundary whose public bulk signature now requires request objects. |
| Runtime response -> optimized asset cache | Untrusted host-returned Blob rows must retain order/URL/length/digest validation before exposure. |
| `origin/main` -> long-lived PR #205 history | Released protocol/package commits and the optimizer history must converge without losing planning provenance or rewriting reviewable commits. |
| Local evidence -> GitHub PR body | Deterministic artifact hashes and dependency claims become reviewer-facing release evidence and must match the pushed commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q11-01 | Tampering | Direct/generated `bytesMany` payload | high | mitigate | Assert exact ordered `{ url }` request objects in focused tests and migrate both source/runtime-template paths together. |
| T-Q11-02 | Spoofing / SSRF | Blossom `servers` metadata | high | transfer | Do not manufacture or consume hints in the optimizer; the canonical NAP keeps hint acceptance, fallback, DNS/IP policy, redirects, and hash enforcement in the host runtime. |
| T-Q11-03 | Tampering | Returned resource rows | high | mitigate | Preserve existing URL/order/Blob/length/SHA-256 gates and prove corrupt/reordered rows remain noncommittable. |
| T-Q11-04 | Repudiation | Merge and deterministic evidence | medium | mitigate | Use a merge commit, preserve both STATE histories, regenerate hashes from the current fixture, and publish those exact values in PR #205. |
| T-Q11-05 | Denial of Service | Branch/check integration | medium | mitigate | Merge rather than rebase, push without force, retain batch/timeout/concurrency/live-byte bounds, and require focused plus full local/remote checks. |
</threat_model>

## Source Coverage Audit

| Source | ID | Feature / Constraint | Task | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Chase NAP-RESOURCE after server hints were added to the resource envelope | 1, 2, 3 | COVERED | #206 supplies the released public contract; this plan closes PR #205's remaining optimizer caller and ships the updated PR state. |
| REQ | QUICK-260826-q11 | Merge current `origin/main` into the existing PR lineage without force-pushing | 1, 3 | COVERED | A normal merge commit converges histories; the final push requires the remote head to remain an ancestor. |
| REQ | QUICK-260826-q11 | Send `{ url }` request objects from direct and generated loaders without inventing `servers` | 1 | COVERED | Exact source and emitted-loader mappings plus focused regression assertions implement the canonical optional-hint contract. |
| REQ | QUICK-260826-q11 | Update tests and deterministic fixture evidence | 1, 2 | COVERED | Unit mocks/calls and the 50 MiB+ real fixture move together; hashes are measured from the merged artifact. |
| REQ | QUICK-260826-q11 | Update PR #205 dependency language and verification evidence | 3 | COVERED | Body edit records #206 as satisfied, links the canonical revision, and uses regenerated hashes. |
| REQ | QUICK-260826-q11 | Preserve the dirty primary checkout by working only in the isolated path | 1, 2, 3 | COVERED | All branch/file/remote operations are explicitly confined to the supplied worktree. |
| REQ | QUICK-260826-q11 | Add no new protocol surface | 1, 2, 3 | COVERED | The optimizer uses only released `resource.bytes`/`bytesMany`; hints, policy, tags, and transport additions are prohibited. |
| RESEARCH | — | No RESEARCH.md exists for this quick task | — | N/A | Discovery Level 0: canonical proposal revision `9511232`, merged #206, current PR source, merge-tree output, and historical verified optimizer diff determine the implementation. |
| CONTEXT | — | No quick-task CONTEXT.md or D-XX decision artifact exists | — | N/A | The user's direct decisions—alpha proposal interfaces are implementable and the server-hint package work belongs in its own PR—are enforced by consuming merged #206 rather than replaying it into #205. |

<verification>
1. `origin/main` and PR head `bc77a610` are both ancestors of the final local branch, and history contains a merge commit rather than a rebase/rewrite.
2. Focused loader tests observe exact request objects at the direct path and emitted source, with no optimizer-authored `servers` field.
3. The real large fixture passes all existing authorization, upload, privacy, corruption, exact-byte, aggregate-hash, size-boundary, and execution assertions with regenerated current hashes.
4. Vite build/type/unit checks and root build/type/unit/test/lint/JSR/release/audit/secret/AI-slop/diff gates pass after the merge.
5. The existing PR head is updated through a normal push, PR #205's body records #206 as satisfied and contains the evidence JSON's exact hashes, required checks pass, and the PR is no longer conflicting.
</verification>

<success_criteria>
- PR #205 is based on released package contracts from #206 without duplicating the dedicated protocol PR's files or changeset.
- Every optimizer-owned bulk resource call conforms to `ResourceBytesRequest[]` and uses only `{ url }` because no server hint enters this path.
- No raw-string request entry, synthesized server list, BUD query parameter, new public option, or other invented protocol surface remains in the optimizer.
- The deterministic artifact evidence and PR narrative match the exact pushed commit.
- Local and GitHub checks pass, the PR is mergeable, and no user-owned primary-worktree or remote history was overwritten.
</success_criteria>

<output>
Create `.planning/quick/260826-q11-integrate-nap-resource-request-object-co/260826-q11-SUMMARY.md` when done.
</output>
