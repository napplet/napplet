---
quick_id: 260904-h0g
phase: quick-260904-h0g
plan: 01
type: execute
mode: quick-full
wave: 1
depends_on: []
files_modified:
  - packages/vite-plugin/src/optimizer/node-platform.ts
  - packages/vite-plugin/src/optimizer/node-platform.test.ts
  - packages/cli/src/blossom-upload.ts
  - packages/cli/tests/deploy_network_test.ts
  - packages/vite-plugin/src/optimizer/references.ts
  - packages/vite-plugin/src/optimizer/references.test.ts
  - packages/vite-plugin/src/optimizer/pipeline.ts
  - packages/vite-plugin/src/optimizer/pipeline.test.ts
  - .changeset/fix-pinned-fetch-and-scoped-render.md
autonomous: true
requirements:
  - ISSUE-212
  - ISSUE-213
  - ISSUE-214
  - QUICK-260904-h0g
must_haves:
  truths:
    - "On Node v22.23.1, the pinned HTTPS transport completes a real `node:https.request()` when Node requests `lookup` results with `options.all === true`, while the callback also retains the single-address result shape and returns only addresses supplied by the validated endpoint."
    - "A normal Deno `executeNetworkDeploy` call that omits `options.fetch` uploads through runtime `globalThis.fetch`, while an explicitly injected fetch remains authoritative for tests and callers."
    - "Baseline asset inlining and byte measurement rewrite only reference locations and forms proven by `inventoryArtifactReferences`; identical asset-path text in unrelated JavaScript strings and comments remains byte-for-byte unchanged."
    - "Existing optimizer selection, supported HTML/CSS/JavaScript reference handling, response limits, DNS pinning, upload verification, rollback, progress, and relay-publication behavior remain green."
    - "A stacked GitHub PR exists with head `fix/issues-212-213`, base `feat/vite-plugin-blossom-optimization`, and a live body that independently contains `Fixes #212`, `Fixes #213`, and `Fixes #214` plus current verification commands and the exact verified head SHA."
    - "The unrelated untracked `packages/cli/.napplet/config.json` remains unmodified and uncommitted."
  artifacts:
    - path: "packages/vite-plugin/src/optimizer/node-platform.ts"
      provides: "Node lookup adapter that emits the callback shape requested by `node:https` from validated endpoint addresses only"
      contains: "options.all"
    - path: "packages/vite-plugin/src/optimizer/node-platform.test.ts"
      provides: "Single/all lookup-shape coverage plus a regression through the real local Node HTTPS request path"
    - path: "packages/cli/src/blossom-upload.ts"
      provides: "Deno Blossom adapter with injected-fetch precedence and runtime-fetch fallback"
      contains: "options.fetch ?? globalThis.fetch"
    - path: "packages/cli/tests/deploy_network_test.ts"
      provides: "Network deploy regression that omits the fetch option"
    - path: "packages/vite-plugin/src/optimizer/references.ts"
      provides: "Parser-backed reference inventory and form-scoped rewrite boundary"
    - path: "packages/vite-plugin/src/optimizer/pipeline.ts"
      provides: "Baseline renderer and measurements driven by inventoried reference locations instead of global text replacement"
    - path: "packages/vite-plugin/src/optimizer/pipeline.test.ts"
      provides: "Regression proving one supported sentinel is inlined while matching unrelated string/comment text and selection thresholds are preserved"
    - path: ".changeset/fix-pinned-fetch-and-scoped-render.md"
      provides: "Patch release notes for the changed `@napplet/vite-plugin` and `@napplet/cli` shipped outputs"
  key_links:
    - from: "packages/vite-plugin/src/optimizer/node-platform.ts"
      to: "node:https.request"
      via: "the custom lookup callback branches on `options.all` and derives every returned address/family record from `ValidatedEndpoint.addresses`"
      pattern: "lookup|options\\.all|endpoint\\.addresses"
    - from: "packages/cli/src/deploy-network.ts"
      to: "packages/cli/src/blossom-upload.ts"
      via: "`executeNetworkDeploy` forwards optional transport services to `uploadFilesToServers`, whose `uploadExactBlobs` adapter selects injected fetch or `globalThis.fetch`"
      pattern: "uploadFilesToServers|globalThis\\.fetch"
    - from: "packages/vite-plugin/src/optimizer/pipeline.ts"
      to: "packages/vite-plugin/src/optimizer/references.ts"
      via: "`renderOptimizedHtml` and every `planExternalAssets` measurement consume the parser-proven inventory/rewrite result"
      pattern: "inventoryArtifactReferences|rewrite.*References"
    - from: "fix/issues-212-213"
      to: "feat/vite-plugin-blossom-optimization"
      via: "GitHub pull request head/base metadata and exact remote/local head equality"
      pattern: "fix/issues-212-213|feat/vite-plugin-blossom-optimization"
    - from: "GitHub pull request body"
      to: "napplet/web issue #212"
      via: "independent closing reference"
      pattern: "Fixes #212"
    - from: "GitHub pull request body"
      to: "napplet/web issue #213"
      via: "independent closing reference"
      pattern: "Fixes #213"
    - from: "GitHub pull request body"
      to: "napplet/web issue #214"
      via: "independent closing reference"
      pattern: "Fixes #214"
---

# Quick Task 260904-h0g: Resolve GitHub Issues #212, #213, and #214 on the Optimizer Base

<objective>
Fix the Node pinned-lookup callback, restore the Deno CLI's default upload transport, and make optimizer baseline rewriting reference-aware, then ship all three regressions as one stacked pull request against `feat/vite-plugin-blossom-optimization`.

Purpose: make PR #205's live optimization and CLI deploy paths usable on the reported Node and Deno runtimes without weakening address pinning, corrupting unrelated bundle text, changing selection math, or inventing protocol surface.

Output: tested patch changes and release metadata on `fix/issues-212-213`, pushed without force, with an open GitHub PR whose base is `feat/vite-plugin-blossom-optimization` and whose body resolves #212, #213, and #214.
</objective>

<execution_context>
@/home/sandwich/.codex/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@packages/build-tools/src/network-policy.ts
@packages/build-tools/src/blossom.ts
@packages/vite-plugin/src/optimizer/node-platform.ts
@packages/vite-plugin/src/optimizer/node-services.ts
@packages/vite-plugin/src/optimizer/references.ts
@packages/vite-plugin/src/optimizer/references.test.ts
@packages/vite-plugin/src/optimizer/pipeline.ts
@packages/vite-plugin/src/optimizer/pipeline.test.ts
@packages/cli/src/blossom-upload.ts
@packages/cli/src/deploy-network.ts
@packages/cli/tests/deploy_network_test.ts
@packages/vite-plugin/package.json
@packages/cli/package.json
@packages/cli/deno.json
@package.json

<interfaces>
The executor should use these existing contracts directly:

```typescript
export interface ValidatedEndpoint {
  url: URL;
  hostname: string;
  addresses: readonly string[];
}

export async function nodePinnedFetch(
  endpoint: ValidatedEndpoint,
  init: RequestInit,
): Promise<Response>;

export interface UploadFilesToServersOptions {
  fetch?: typeof fetch;
  now?: () => number;
  networkPolicy?: NetworkPolicy;
  resolve?: PublicAddressResolver["resolve"];
  onProgress?: (progress: UploadResultProgress) => void;
}

export interface NetworkDeployOptions extends Pick<UploadFilesToServersOptions, "networkPolicy" | "resolve"> {
  fetch?: typeof fetch;
  publish?: RelayPublisher;
  now?: () => number;
  onProgress?: (progress: NetworkDeployProgress) => void;
}

export interface ArtifactReference {
  source: string;
  form: ReferenceForm;
  supported: boolean;
  location: string;
}

export function inventoryArtifactReferences(input: ReferenceBuild): ReferenceInventory;
export function renderOptimizedHtml(input: RenderInput): RenderedArtifact;
```

Current broken links:

- `nodePinnedFetch` always calls the legacy `(address, family)` lookup callback even when Node requests `all: true`.
- `uploadFilesToServers` calls `uploadExactBlobs(..., { fetch: options.fetch, ... })`, so the normal CLI path supplies `undefined`.
- `renderOptimizedHtml` calls `html.split(asset.reference).join(dataUri(asset))`, so non-reference text is rewritten and counted as asset bytes.

Execution starts on local `fix/issues-212-213` at the current `origin/feat/vite-plugin-blossom-optimization` tip. Before editing, fetch that remote base, require it to remain an ancestor of `HEAD` (or integrate a newly advanced base without discarding local planning work), and record `sha256sum packages/cli/.napplet/config.json` in `/tmp/napplet-260904-h0g-config.sha256`. Never open, modify, stage, stash, delete, or commit that unrelated untracked file; verify the checksum again before publication.

These are implementation-transport and artifact-rewrite fixes. Do not add or change a NIP-5D/NAP message, manifest tag, capability, sandbox/loading rule, public package API, or dependency.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair the Node and Deno transport defaults with runtime-path regressions</name>
  <files>packages/vite-plugin/src/optimizer/node-platform.ts, packages/vite-plugin/src/optimizer/node-platform.test.ts, packages/cli/src/blossom-upload.ts, packages/cli/tests/deploy_network_test.ts</files>
  <behavior>
    - "#212 all-result case: a lookup request with `options.all === true` receives an ordered array of `{ address, family }` records derived only from `endpoint.addresses`."
    - "#212 single-result case: a lookup request without `all` receives the first validated address and its numeric family through the legacy callback shape."
    - "#212 integration case: `nodePinnedFetch` completes against a loopback HTTPS fixture through the actual Node v22 `node:https.request()` implementation; the test must fail on the current `ERR_INVALID_IP_ADDRESS` behavior and must not directly invoke or mock away the request path."
    - "#213 default case: `executeNetworkDeploy` succeeds when its options omit `fetch` and a test-scoped replacement of runtime `globalThis.fetch` handles HEAD/PUT calls."
    - "#213 override case: the existing injected fetch tests continue to observe the caller-provided transport rather than the runtime global."
  </behavior>
  <action>For #212, first add `node-platform.test.ts` and observe RED on Node v22.23.1. Factor an internal, typed pinned-lookup function in `node-platform.ts` and make `nodePinnedFetch` use it. Classify each address with Node's IP-family utility, reject an empty or malformed validated-address list, preserve endpoint order, return the complete record array when `options.all === true`, and otherwise return only the first record through the single-address callback overload. Do not call system DNS or introduce an unvalidated fallback. Cover both callback shapes directly, then run `nodePinnedFetch` against a test-local HTTPS server using the real `node:https.request()` implementation; keep any self-signed-certificate relaxation confined to the Vitest worker/test and restore process state plus close the server in `finally`.

For #213, add a Deno regression to `deploy_network_test.ts` before changing production code. Reuse `createFakeFetch`, temporarily replace `globalThis.fetch`, call `executeNetworkDeploy` without a `fetch` property while retaining deterministic `publish`, `now`, and `resolve` injections, assert the expected HEAD/PUT uploads and relay publication, and restore the original global in `finally`. Then change only the `uploadExactBlobs` service adapter in `blossom-upload.ts` to `fetch: options.fetch ?? globalThis.fetch`; retain injected-fetch precedence and all network-policy, exact-hash, retry, rollback, progress, and error-redaction behavior. Keep the two issue fixes reviewable as separate green commits, stage only their explicit paths, and use conventional intent-first messages with useful Lore trailers and a Codex co-author trailer.</action>
  <verify>
    <automated>pnpm --dir packages/vite-plugin exec vitest run --config vitest.config.ts src/optimizer/node-platform.test.ts &amp;&amp; deno test --config packages/cli/deno.json --allow-read --allow-write --allow-run --allow-env packages/cli/tests/deploy_network_test.ts &amp;&amp; pnpm --filter @napplet/vite-plugin type-check &amp;&amp; pnpm --filter @napplet/cli type-check &amp;&amp; git diff --check</automated>
  </verify>
  <done>Both regressions were seen failing before implementation and pass afterward: Node supports all/single lookup callback shapes through endpoint-owned addresses and the real HTTPS path, while Deno deploy defaults to runtime fetch without changing explicit injection semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make baseline inlining and size selection consume parser-proven references</name>
  <files>packages/vite-plugin/src/optimizer/references.ts, packages/vite-plugin/src/optimizer/references.test.ts, packages/vite-plugin/src/optimizer/pipeline.ts, packages/vite-plugin/src/optimizer/pipeline.test.ts</files>
  <behavior>
    - "#214 collision case: when `chunks/part-00.bin` appears in one supported `fetch(__nappletAssetUrl(...))` sentinel and also in an unrelated JavaScript string and comment, baseline rendering produces one data URI and leaves the unrelated occurrences byte-for-byte intact."
    - "#214 measurement case: `initialBytes`, each candidate measurement, `finalBytes`, and the at-target/over-target decision equal the bytes of the parser-scoped rendered HTML, not a global replacement with duplicate Base64 payloads."
    - "Compatibility case: every HTML, srcset, CSS, and JavaScript reference form already recognized by `inventoryArtifactReferences` retains its current baseline inlining or selected-resource behavior, while non-reference text is never rewritten."
  </behavior>
  <action>Write the focused `pipeline.test.ts` regression first and observe RED. Build a retained artifact whose JavaScript contains the same path in a supported fetch sentinel, a normal string, and a comment; assert the exact rendered text, exactly one baseline data URI, exact expected byte count, and a target boundary that would select/offload differently if the path were globally duplicated. Extend `references.ts` only as needed to expose a form-specific, parser-backed rewrite result from the locations/forms already produced by `inventoryArtifactReferences`; add `references.test.ts` cases that prove recognized HTML/CSS/JavaScript references are rewritten while equal text outside those recognized forms is untouched. Preserve fragments, quoting/escaping, deterministic ordering, and the existing eligibility meaning of `supported`.

In `pipeline.ts`, construct the reference inventory before the initial render and reuse the same authoritative inventory for baseline bytes, every candidate measurement, and the final artifact. Replace `html.split(asset.reference).join(...)` with the inventory/form-scoped rewrite path. Baseline rendering must still inline all actual reference forms the inventory recognizes, including forms that are deliberately ineligible for externalization; selected resources must still replace only the supported fetch sentinel with `window.__nappletPrivateResourceLoader.response(...)`. Do not use a whole-document textual replacement or a regex that treats a bare asset path as sufficient proof. Run the full existing references and pipeline suites so HTML attributes, srcset, parsed CSS URLs, JavaScript sentinel classification, whole-Blob limits, stable ordering, and transaction behavior remain protected. Commit the parser/rewrite correction and regressions together with an intent-first conventional message, relevant Lore trailers, and a Codex co-author trailer.</action>
  <verify>
    <automated>pnpm --dir packages/vite-plugin exec vitest run --config vitest.config.ts src/optimizer/references.test.ts src/optimizer/pipeline.test.ts &amp;&amp; pnpm --filter @napplet/vite-plugin type-check &amp;&amp; pnpm --filter @napplet/vite-plugin build &amp;&amp; git diff --check</automated>
  </verify>
  <done>The renderer has no global bare-path replacement, parser-proven resource references alone change, unrelated strings/comments remain exact, and all baseline/selection measurements use those scoped bytes without regressing existing recognized forms.</done>
</task>

<task type="auto">
  <name>Task 3: Add release metadata, run the complete gate, and open the stacked three-issue PR</name>
  <files>.changeset/fix-pinned-fetch-and-scoped-render.md; Git history for fix/issues-212-213; GitHub pull request metadata for napplet/web</files>
  <action>Add one patch changeset listing both `@napplet/vite-plugin` (#212 and #214) and `@napplet/cli` (#213), with release prose describing the observable bug fixes rather than protocol claims. Keep the base branch's existing `.changeset/tidy-blossom-assets.md` unchanged; the new changeset documents this stacked correction even though the base's pending minor bump remains the eventual dominant bump. Commit the changeset by explicit path with an intent-first conventional message, useful Lore trailers, and a Codex co-author trailer.

Run the targeted regressions again, then the repository-required build, type-check, recursive unit, lint, diff, and AI-slop gates. The changed-code AI-slop command is `pnpm dlx aislop@0.12.0 scan --changes --base origin/feat/vite-plugin-blossom-optimization .` and must return 100/100 for this branch; do not disable a pinned rule. Confirm no docs describe the broken behavior, no new dependency or protocol surface entered the diff, and `/tmp/napplet-260904-h0g-config.sha256` still verifies the untracked CLI config. Stage by explicit path only and never use `git add -A` or `git add -u`.

Immediately before publication, fetch `origin/feat/vite-plugin-blossom-optimization`; if it advanced, integrate it without discarding or overwriting local work and rerun every affected/full gate. Require the fetched base to be an ancestor of `HEAD`. Also check whether `origin/fix/issues-212-213` appeared since planning; if it exists and is not an ancestor of local `HEAD`, integrate it or stop rather than overwrite it. Push with `git push -u origin fix/issues-212-213` and no force option, fetch both remote refs again, and require the pushed remote head SHA to equal the verified local `HEAD` exactly. Open the PR with `gh pr create --repo napplet/web --head fix/issues-212-213 --base feat/vite-plugin-blossom-optimization`; its body must explain each root cause and regression, include `Fixes #212`, `Fixes #213`, and `Fixes #214` as three independent lines, name the local Node/Deno versions used, include the exact pushed commit SHA, and contain an unambiguous `## Verification` section listing the completed `pnpm build`, `pnpm type-check`, `pnpm -r test:unit`, `pnpm lint`, and changed-code AI-slop commands. State that the changes are transport/artifact plumbing with no protocol change, and disclose no unrelated config content. Query the live PR after creation and verify its OPEN state, exact head/base pair, all three independent closing lines, exact head SHA, and required verification section before reporting its URL.</action>
  <verify>
    <automated>pnpm --dir packages/vite-plugin exec vitest run --config vitest.config.ts src/optimizer/node-platform.test.ts src/optimizer/references.test.ts src/optimizer/pipeline.test.ts &amp;&amp; deno test --config packages/cli/deno.json --allow-read --allow-write --allow-run --allow-env packages/cli/tests/deploy_network_test.ts &amp;&amp; pnpm build &amp;&amp; pnpm type-check &amp;&amp; pnpm -r test:unit &amp;&amp; pnpm lint &amp;&amp; pnpm dlx aislop@0.12.0 scan --changes --base origin/feat/vite-plugin-blossom-optimization . &amp;&amp; git diff --check origin/feat/vite-plugin-blossom-optimization...HEAD &amp;&amp; sha256sum --check --status /tmp/napplet-260904-h0g-config.sha256 &amp;&amp; git fetch --quiet origin feat/vite-plugin-blossom-optimization fix/issues-212-213 &amp;&amp; git merge-base --is-ancestor origin/feat/vite-plugin-blossom-optimization HEAD &amp;&amp; test "$(git rev-parse origin/fix/issues-212-213)" = "$(git rev-parse HEAD)" &amp;&amp; test "$(gh pr view fix/issues-212-213 --repo napplet/web --json state,baseRefName,headRefName --jq '[.state,.baseRefName,.headRefName] | join(":")')" = "OPEN:feat/vite-plugin-blossom-optimization:fix/issues-212-213" &amp;&amp; gh pr view fix/issues-212-213 --repo napplet/web --json body --jq .body | grep -Fq 'Fixes #212' &amp;&amp; gh pr view fix/issues-212-213 --repo napplet/web --json body --jq .body | grep -Fq 'Fixes #213' &amp;&amp; gh pr view fix/issues-212-213 --repo napplet/web --json body --jq .body | grep -Fq 'Fixes #214' &amp;&amp; gh pr view fix/issues-212-213 --repo napplet/web --json body --jq .body | grep -Fq '## Verification' &amp;&amp; gh pr view fix/issues-212-213 --repo napplet/web --json body --jq .body | grep -Fq "$(git rev-parse HEAD)" &amp;&amp; gh pr view fix/issues-212-213 --repo napplet/web --json body --jq .body | grep -Fq 'pnpm build' &amp;&amp; gh pr view fix/issues-212-213 --repo napplet/web --json body --jq .body | grep -Fq 'pnpm type-check' &amp;&amp; gh pr view fix/issues-212-213 --repo napplet/web --json body --jq .body | grep -Fq 'pnpm -r test:unit' &amp;&amp; gh pr view fix/issues-212-213 --repo napplet/web --json body --jq .body | grep -Fq 'pnpm lint' &amp;&amp; gh pr view fix/issues-212-213 --repo napplet/web --json body --jq .body | grep -Fq 'pnpm dlx aislop@0.12.0 scan --changes --base origin/feat/vite-plugin-blossom-optimization .'</automated>
  </verify>
  <done>Both packages have patch release metadata, all targeted and repository gates pass on the exact pushed head, the fetched feature base is its ancestor, `origin/fix/issues-212-213` equals local `HEAD`, the unrelated config checksum is unchanged and the file is uncommitted, and the live OPEN PR has the exact base/head pair plus three independent closing lines and current verification evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validated endpoint -> Node HTTPS socket | DNS-policy output becomes connection routing; the custom lookup must not re-resolve or introduce an address outside `ValidatedEndpoint.addresses`. |
| Deno CLI options/runtime -> Blossom uploader | A caller-controlled injected fetch or the Deno runtime fetch crosses into upload authorization and exact-byte network operations. |
| Retained bundle text -> optimized HTML | Parser-recognized resource references coexist with arbitrary application strings/comments that must be treated as opaque bytes. |
| Local branch -> GitHub stacked PR | Commits and verification claims become remote reviewer evidence and must target the requested optimizer base without overwriting another head. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-H0G-01 | Spoofing / SSRF | `nodePinnedFetch` lookup | mitigate | Return only records derived from the already validated endpoint address list for both callback shapes; never call DNS or fall back to the hostname. |
| T-H0G-02 | Denial of Service | Node all-address callback and Deno missing fetch | mitigate | Exercise the real Node HTTPS path and normal no-fetch Deno deploy path, while preserving bounded response/upload behavior and explicit override injection. |
| T-H0G-03 | Tampering | `renderOptimizedHtml` | mitigate | Make inventory locations/forms the sole rewrite authority and assert equal bare text in strings/comments remains byte-identical. |
| T-H0G-04 | Repudiation | Release/PR evidence | mitigate | Use atomic Lore commits, a package changeset, exact local gates, non-force push, and live head/base/body PR verification. |
| T-H0G-05 | Information disclosure | Untracked CLI config | mitigate | Hash without printing contents, never stage or open it, verify the checksum before push, and omit its contents from logs/PR text. |
</threat_model>

## Source Coverage Audit

| Source | ID | Feature / Constraint | Task | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Open one stacked PR against `feat/vite-plugin-blossom-optimization` that resolves all reported issues | 3 | COVERED | Live PR metadata is part of the automated completion gate. |
| REQ | ISSUE-212 | Honor Node single/all lookup callback shapes using only validated addresses and prove the real Node HTTPS path | 1 | COVERED | Direct callback-shape tests plus a loopback HTTPS integration regression. |
| REQ | ISSUE-213 | Use injected fetch when present and runtime `globalThis.fetch` otherwise; test `executeNetworkDeploy` without custom fetch | 1 | COVERED | The regression replaces the runtime global but omits the options field. |
| REQ | ISSUE-214 | Rewrite only inventoried supported reference locations/forms and keep unrelated equal strings/comments untouched with correct measurements | 2 | COVERED | Parser/rewrite and pipeline tests cover exact output plus target selection math. |
| REQ | QUICK-260904-h0g | Preserve behavior, add changesets and regressions, run full gates, and ship the requested PR | 1, 2, 3 | COVERED | Focused TDD commits feed the complete package/repository/publication gate. |
| REQ | QUICK-260904-h0g | Preserve unrelated `packages/cli/.napplet/config.json` | 1, 3 | COVERED | Hash-only before/after proof and explicit-path staging prevent accidental inclusion. |
| RESEARCH | — | No quick-task RESEARCH.md exists | — | N/A | Discovery Level 0: live issue bodies, the current base implementation, existing test patterns, and package scripts determine the fixes; no dependency or external API choice is introduced. |
| CONTEXT | — | No quick-task CONTEXT.md or D-XX decision artifact exists | — | N/A | The direct task constraints and authoritative issue acceptance evidence are represented as requirements above. |

<verification>
1. The Node regression runs on Node v22.23.1, proves both callback shapes, and completes through real `node:https.request()` without returning a non-endpoint address.
2. The Deno regression calls `executeNetworkDeploy` with no `fetch` option, observes runtime-global HEAD/PUT use, and leaves existing injected-fetch cases green.
3. The collision fixture contains one real sentinel and equal string/comment text; only the real reference is inlined, and exact initial/candidate/final byte assertions drive the correct selection result.
4. Targeted suites, package builds/type checks, root `pnpm build`, `pnpm type-check`, `pnpm -r test:unit`, `pnpm lint`, changed-code AI-slop 100/100, and committed-range diff checks all pass after final base integration.
5. Both affected packages have changeset coverage, no dependency/protocol surface appears, and `packages/cli/.napplet/config.json` remains checksum-identical and untracked.
6. The fetched `origin/feat/vite-plugin-blossom-optimization` is an ancestor of the verified local `HEAD`, and fetched `origin/fix/issues-212-213` equals that local SHA exactly.
7. GitHub reports an OPEN PR with the exact head/base pair; its live body independently contains `Fixes #212`, `Fixes #213`, and `Fixes #214`, the exact verified head SHA, and a `## Verification` section listing every required full-gate command.
</verification>

<success_criteria>
- Node 22 pinned HTTPS requests no longer fail because of the `all: true` lookup callback shape, and address pinning remains fail-closed to validated endpoint answers.
- Deno CLI network deployment works through runtime fetch when no override is supplied, while deterministic injected transports still work.
- Optimizer baseline bytes and selection decisions reflect one substitution per parser-proven reference, never coincidental application text.
- Existing transport, optimizer, deploy, protocol, build, type, unit, lint, and slop gates remain green with release metadata for both changed packages.
- The requested stacked PR is open against the exact feature base, resolves all three issues, and contains no unrelated config or force-pushed history.
</success_criteria>

<output>
After completion, create `.planning/quick/260904-h0g-resolve-github-issues-212-and-213-on-top/260904-h0g-SUMMARY.md`.
</output>
